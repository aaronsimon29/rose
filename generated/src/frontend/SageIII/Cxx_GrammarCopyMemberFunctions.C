// MACHINE GENERATED SOURCE FILE WITH ROSE (Grammar.h)--- DO NOT MODIFY!

#include "sage3basic.h"

#ifndef ROSE_USE_INTERNAL_FRONTEND_DEVELOPMENT
   #include "AST_FILE_IO.h"
#endif 
// The header file ("rose_config.h") should only be included by source files that require it.
#include "rose_config.h"

#if _MSC_VER
#define USE_CPP_NEW_DELETE_OPERATORS 0
#endif

#define ROSE_ALLOC_TRACE 0
#undef mprintf
#define mprintf Rose::Diagnostics::mfprintf(Rose::ir_node_mlog[Rose::Diagnostics::DEBUG])

#include <Rose/BinaryAnalysis/RegisterDictionary.h>

using namespace std;

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNode::copy ( SgCopyHelp& help) const
   {
     SgNode* result = NULL;

  // printf ("Copy SgNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNode*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNode*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNode: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNode*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNode(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNode::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 205 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSupport::copy ( SgCopyHelp& help) const
   {
     SgSupport* result = NULL;

  // printf ("Copy SgSupport = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSupport*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSupport*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSupport: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSupport*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSupport(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSupport = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSupport::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 388 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModifier::copy ( SgCopyHelp& help) const
   {
     SgModifier* result = NULL;

  // printf ("Copy SgModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 571 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModifierNodes::copy ( SgCopyHelp& help) const
   {
     SgModifierNodes* result = NULL;

  // printf ("Copy SgModifierNodes = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgModifierNodes*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgModifierNodes*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgModifierNodes: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgModifierNodes*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgModifierNodes(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): nodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nodes
  // case: toBeCopied == COPY_DATA for nodes
     SgModifierTypePtrVector nodes_copy = p_nodes; 
     result->p_nodes = nodes_copy; 
  // Copy non-constructor parameter data member (access function): next_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for next
  // case: toBeCopied == COPY_DATA for next
     SgModifierNodes* next_copy = p_next; 
     result->p_next = next_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgModifierNodes = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgModifierNodes::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 765 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConstVolatileModifier::copy ( SgCopyHelp& help) const
   {
     SgConstVolatileModifier* result = NULL;

  // printf ("Copy SgConstVolatileModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgConstVolatileModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgConstVolatileModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgConstVolatileModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgConstVolatileModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgConstVolatileModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgConstVolatileModifier::cv_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgConstVolatileModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgConstVolatileModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 954 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStorageModifier::copy ( SgCopyHelp& help) const
   {
     SgStorageModifier* result = NULL;

  // printf ("Copy SgStorageModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStorageModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStorageModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStorageModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStorageModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStorageModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgStorageModifier::storage_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
  // Copy non-constructor parameter data member (access function): thread_local_storage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for thread_local_storage
  // case: toBeCopied == COPY_DATA for thread_local_storage
     bool thread_local_storage_copy = p_thread_local_storage; 
     result->p_thread_local_storage = thread_local_storage_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStorageModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStorageModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 1148 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAccessModifier::copy ( SgCopyHelp& help) const
   {
     SgAccessModifier* result = NULL;

  // printf ("Copy SgAccessModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAccessModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAccessModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAccessModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAccessModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAccessModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgAccessModifier::access_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAccessModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAccessModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 1337 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionModifier::copy ( SgCopyHelp& help) const
   {
     SgFunctionModifier* result = NULL;

  // printf ("Copy SgFunctionModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifierVector
  // case: toBeCopied == COPY_DATA for modifierVector
     SgBitVector modifierVector_copy = p_modifierVector; 
     result->p_modifierVector = modifierVector_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_constructor_destructor_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_constructor_destructor_priority
  // case: toBeCopied == COPY_DATA for gnu_attribute_constructor_destructor_priority
     unsigned long int gnu_attribute_constructor_destructor_priority_copy = p_gnu_attribute_constructor_destructor_priority; 
     result->p_gnu_attribute_constructor_destructor_priority = gnu_attribute_constructor_destructor_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_named_weak_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_named_weak_reference
  // case: toBeCopied == COPY_DATA for gnu_attribute_named_weak_reference
     std::string gnu_attribute_named_weak_reference_copy = p_gnu_attribute_named_weak_reference; 
     result->p_gnu_attribute_named_weak_reference = gnu_attribute_named_weak_reference_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_named_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_named_alias
  // case: toBeCopied == COPY_DATA for gnu_attribute_named_alias
     std::string gnu_attribute_named_alias_copy = p_gnu_attribute_named_alias; 
     result->p_gnu_attribute_named_alias = gnu_attribute_named_alias_copy; 
  // Copy non-constructor parameter data member (access function): opencl_vec_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for opencl_vec_type
  // case: toBeCopied == COPY_DATA for opencl_vec_type
     SgType * opencl_vec_type_copy = p_opencl_vec_type; 
     result->p_opencl_vec_type = opencl_vec_type_copy; 
  // Copy non-constructor parameter data member (access function): opencl_work_group_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for opencl_work_group_size
  // case: toBeCopied == COPY_DATA for opencl_work_group_size
     SgFunctionModifier::opencl_work_group_size_t opencl_work_group_size_copy = p_opencl_work_group_size; 
     result->p_opencl_work_group_size = opencl_work_group_size_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 1551 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUPC_AccessModifier::copy ( SgCopyHelp& help) const
   {
     SgUPC_AccessModifier* result = NULL;

  // printf ("Copy SgUPC_AccessModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUPC_AccessModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUPC_AccessModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUPC_AccessModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUPC_AccessModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUPC_AccessModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgUPC_AccessModifier::upc_access_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
  // Copy non-constructor parameter data member (access function): isShared_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isShared
  // case: toBeCopied == COPY_DATA for isShared
     bool isShared_copy = p_isShared; 
     result->p_isShared = isShared_copy; 
  // Copy non-constructor parameter data member (access function): layout_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for layout
  // case: toBeCopied == COPY_DATA for layout
     long layout_copy = p_layout; 
     result->p_layout = layout_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUPC_AccessModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUPC_AccessModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 1750 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSpecialFunctionModifier::copy ( SgCopyHelp& help) const
   {
     SgSpecialFunctionModifier* result = NULL;

  // printf ("Copy SgSpecialFunctionModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSpecialFunctionModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSpecialFunctionModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSpecialFunctionModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSpecialFunctionModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSpecialFunctionModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifierVector
  // case: toBeCopied == COPY_DATA for modifierVector
     SgBitVector modifierVector_copy = p_modifierVector; 
     result->p_modifierVector = modifierVector_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSpecialFunctionModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSpecialFunctionModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 1939 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElaboratedTypeModifier::copy ( SgCopyHelp& help) const
   {
     SgElaboratedTypeModifier* result = NULL;

  // printf ("Copy SgElaboratedTypeModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElaboratedTypeModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElaboratedTypeModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElaboratedTypeModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElaboratedTypeModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElaboratedTypeModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgElaboratedTypeModifier::elaborated_type_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElaboratedTypeModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElaboratedTypeModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 2128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLinkageModifier::copy ( SgCopyHelp& help) const
   {
     SgLinkageModifier* result = NULL;

  // printf ("Copy SgLinkageModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLinkageModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLinkageModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLinkageModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLinkageModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLinkageModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgLinkageModifier::linkage_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLinkageModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLinkageModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 2317 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBaseClassModifier::copy ( SgCopyHelp& help) const
   {
     SgBaseClassModifier* result = NULL;

  // printf ("Copy SgBaseClassModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBaseClassModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBaseClassModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBaseClassModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBaseClassModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBaseClassModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgBaseClassModifier::baseclass_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_accessModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for accessModifier
  // case: toBeCopied == COPY_DATA for accessModifier
     SgAccessModifier accessModifier_copy = p_accessModifier; 
     result->p_accessModifier = accessModifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBaseClassModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBaseClassModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 2511 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStructureModifier::copy ( SgCopyHelp& help) const
   {
     SgStructureModifier* result = NULL;

  // printf ("Copy SgStructureModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStructureModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStructureModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStructureModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStructureModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStructureModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgStructureModifier::jovial_structure_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
  // Copy non-constructor parameter data member (access function): bits_per_entry_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bits_per_entry
  // case: toBeCopied == COPY_DATA for bits_per_entry
     int bits_per_entry_copy = p_bits_per_entry; 
     result->p_bits_per_entry = bits_per_entry_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStructureModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStructureModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 2705 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeModifier::copy ( SgCopyHelp& help) const
   {
     SgTypeModifier* result = NULL;

  // printf ("Copy SgTypeModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifierVector
  // case: toBeCopied == COPY_DATA for modifierVector
     SgBitVector modifierVector_copy = p_modifierVector; 
     result->p_modifierVector = modifierVector_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_upcModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upcModifier
  // case: toBeCopied == COPY_DATA for upcModifier
     SgUPC_AccessModifier upcModifier_copy = p_upcModifier; 
     result->p_upcModifier = upcModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_structureModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for structureModifier
  // case: toBeCopied == COPY_DATA for structureModifier
     SgStructureModifier structureModifier_copy = p_structureModifier; 
     result->p_structureModifier = structureModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_constVolatileModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constVolatileModifier
  // case: toBeCopied == COPY_DATA for constVolatileModifier
     SgConstVolatileModifier constVolatileModifier_copy = p_constVolatileModifier; 
     result->p_constVolatileModifier = constVolatileModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_elaboratedTypeModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for elaboratedTypeModifier
  // case: toBeCopied == COPY_DATA for elaboratedTypeModifier
     SgElaboratedTypeModifier elaboratedTypeModifier_copy = p_elaboratedTypeModifier; 
     result->p_elaboratedTypeModifier = elaboratedTypeModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_gnu_extension_machine_mode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_machine_mode
  // case: toBeCopied == COPY_DATA for gnu_extension_machine_mode
     SgTypeModifier::gnu_extension_machine_mode_enum gnu_extension_machine_mode_copy = p_gnu_extension_machine_mode; 
     result->p_gnu_extension_machine_mode = gnu_extension_machine_mode_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_alignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_alignment
  // case: toBeCopied == COPY_DATA for gnu_attribute_alignment
     int gnu_attribute_alignment_copy = p_gnu_attribute_alignment; 
     result->p_gnu_attribute_alignment = gnu_attribute_alignment_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_sentinel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_sentinel
  // case: toBeCopied == COPY_DATA for gnu_attribute_sentinel
     long gnu_attribute_sentinel_copy = p_gnu_attribute_sentinel; 
     result->p_gnu_attribute_sentinel = gnu_attribute_sentinel_copy; 
  // Copy non-constructor parameter data member (access function): address_space_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address_space_value
  // case: toBeCopied == COPY_DATA for address_space_value
     unsigned address_space_value_copy = p_address_space_value; 
     result->p_address_space_value = address_space_value_copy; 
  // Copy non-constructor parameter data member (access function): vector_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vector_size
  // case: toBeCopied == COPY_DATA for vector_size
     unsigned vector_size_copy = p_vector_size; 
     result->p_vector_size = vector_size_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 2939 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeclarationModifier::copy ( SgCopyHelp& help) const
   {
     SgDeclarationModifier* result = NULL;

  // printf ("Copy SgDeclarationModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDeclarationModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDeclarationModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDeclarationModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDeclarationModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDeclarationModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifierVector
  // case: toBeCopied == COPY_DATA for modifierVector
     SgBitVector modifierVector_copy = p_modifierVector; 
     result->p_modifierVector = modifierVector_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_typeModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeModifier
  // case: toBeCopied == COPY_DATA for typeModifier
     SgTypeModifier typeModifier_copy = p_typeModifier; 
     result->p_typeModifier = typeModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_accessModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for accessModifier
  // case: toBeCopied == COPY_DATA for accessModifier
     SgAccessModifier accessModifier_copy = p_accessModifier; 
     result->p_accessModifier = accessModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_storageModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storageModifier
  // case: toBeCopied == COPY_DATA for storageModifier
     SgStorageModifier storageModifier_copy = p_storageModifier; 
     result->p_storageModifier = storageModifier_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_section_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_section_name
  // case: toBeCopied == COPY_DATA for gnu_attribute_section_name
     std::string gnu_attribute_section_name_copy = p_gnu_attribute_section_name; 
     result->p_gnu_attribute_section_name = gnu_attribute_section_name_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_visability
  // case: toBeCopied == COPY_DATA for gnu_attribute_visability
     SgDeclarationModifier::gnu_declaration_visability_enum gnu_attribute_visability_copy = p_gnu_attribute_visability; 
     result->p_gnu_attribute_visability = gnu_attribute_visability_copy; 
  // Copy non-constructor parameter data member (access function): microsoft_uuid_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for microsoft_uuid_string
  // case: toBeCopied == COPY_DATA for microsoft_uuid_string
     std::string microsoft_uuid_string_copy = p_microsoft_uuid_string; 
     result->p_microsoft_uuid_string = microsoft_uuid_string_copy; 
  // Copy non-constructor parameter data member (access function): microsoft_property_get_function_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for microsoft_property_get_function_name
  // case: toBeCopied == COPY_DATA for microsoft_property_get_function_name
     std::string microsoft_property_get_function_name_copy = p_microsoft_property_get_function_name; 
     result->p_microsoft_property_get_function_name = microsoft_property_get_function_name_copy; 
  // Copy non-constructor parameter data member (access function): microsoft_property_put_function_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for microsoft_property_put_function_name
  // case: toBeCopied == COPY_DATA for microsoft_property_put_function_name
     std::string microsoft_property_put_function_name_copy = p_microsoft_property_put_function_name; 
     result->p_microsoft_property_put_function_name = microsoft_property_put_function_name_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDeclarationModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDeclarationModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 3168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOpenclAccessModeModifier::copy ( SgCopyHelp& help) const
   {
     SgOpenclAccessModeModifier* result = NULL;

  // printf ("Copy SgOpenclAccessModeModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOpenclAccessModeModifier*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOpenclAccessModeModifier*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOpenclAccessModeModifier: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOpenclAccessModeModifier*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOpenclAccessModeModifier(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgOpenclAccessModeModifier::access_mode_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOpenclAccessModeModifier = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOpenclAccessModeModifier::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 3357 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgName::copy ( SgCopyHelp& help) const
   {
     SgName* result = NULL;

  // printf ("Copy SgName = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgName*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgName*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgName: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgName*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgName(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_char
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for char
  // case: toBeCopied == COPY_DATA for char
     std::string char_copy = p_char; 
     result->p_char = char_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgName = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgName::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 3546 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSymbolTable::copy ( SgCopyHelp& help) const
   {
     SgSymbolTable* result = NULL;

  // printf ("Copy SgSymbolTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSymbolTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSymbolTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSymbolTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSymbolTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSymbolTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_iterator
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iterator
  // case: toBeCopied == COPY_DATA for iterator
     hash_iterator iterator_copy = p_iterator; 
     result->p_iterator = iterator_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): no_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_name
  // case: toBeCopied == COPY_DATA for no_name
     bool no_name_copy = p_no_name; 
     result->p_no_name = no_name_copy; 
  // Copy non-constructor parameter data member (access function): table_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for table
  // case: toBeCopied == COPY_DATA for table
      rose_hash_multimap* table_copy = p_table; 
     result->p_table = table_copy; 
  // Copy non-constructor parameter data member (access function): symbolSet_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbolSet
  // case: toBeCopied == COPY_DATA for symbolSet
     SgNodeSet symbolSet_copy = p_symbolSet; 
     result->p_symbolSet = symbolSet_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_case_insensitive
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for case_insensitive
  // case: toBeCopied == COPY_DATA for case_insensitive
     bool case_insensitive_copy = p_case_insensitive; 
     result->p_case_insensitive = case_insensitive_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_aliasSymbolCausalNodeSet
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for aliasSymbolCausalNodeSet
  // case: toBeCopied == COPY_DATA for aliasSymbolCausalNodeSet
     static SgUnorderedNodeSet aliasSymbolCausalNodeSet_copy = p_aliasSymbolCausalNodeSet; 
     result->p_aliasSymbolCausalNodeSet = aliasSymbolCausalNodeSet_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_force_search_of_base_classes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for force_search_of_base_classes
  // case: toBeCopied == COPY_DATA for force_search_of_base_classes
     static bool force_search_of_base_classes_copy = p_force_search_of_base_classes; 
     result->p_force_search_of_base_classes = force_search_of_base_classes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_mode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_mode
  // case: toBeCopied == COPY_DATA for name_qualification_mode
     static bool name_qualification_mode_copy = p_name_qualification_mode; 
     result->p_name_qualification_mode = name_qualification_mode_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSymbolTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSymbolTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 3775 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAttribute::copy ( SgCopyHelp& help) const
   {
     SgAttribute* result = NULL;

  // printf ("Copy SgAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAttribute*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAttribute*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAttribute: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAttribute*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAttribute(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAttribute::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 3963 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPragma::copy ( SgCopyHelp& help) const
   {
     SgPragma* result = NULL;

  // printf ("Copy SgPragma = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPragma*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPragma*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPragma: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPragma*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPragma(  name_copy, startOfConstruct_copy, endOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_printed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for printed
  // case: toBeCopied == COPY_DATA for printed
     short printed_copy = p_printed; 
     result->p_printed = printed_copy; 
  // Copy non-constructor parameter data member (access function): args_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for args
  // case: toBeCopied == COPY_DATA for args
     SgExprListExp* args_copy = p_args; 
     result->p_args = args_copy; 
  // Copy non-constructor parameter data member (access function): associatedStatement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedStatement
  // case: toBeCopied == COPY_DATA for associatedStatement
     SgStatement* associatedStatement_copy = p_associatedStatement; 
     result->p_associatedStatement = associatedStatement_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPragma = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPragma::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 4207 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitAttribute::copy ( SgCopyHelp& help) const
   {
     SgBitAttribute* result = NULL;

  // printf ("Copy SgBitAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBitAttribute*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBitAttribute*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBitAttribute: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBitAttribute*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
  // Copy constructor parameter data member: bitflag_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitflag
  // case: toBeCopied == COPY_DATA for bitflag
     unsigned long int bitflag_copy = p_bitflag; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBitAttribute(  name_copy, bitflag_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBitAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBitAttribute::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 4399 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFuncDecl_attr::copy ( SgCopyHelp& help) const
   {
     SgFuncDecl_attr* result = NULL;

  // printf ("Copy SgFuncDecl_attr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFuncDecl_attr*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFuncDecl_attr*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFuncDecl_attr: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFuncDecl_attr*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFuncDecl_attr(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): bitflag_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitflag
  // case: toBeCopied == COPY_DATA for bitflag
     unsigned long int bitflag_copy = p_bitflag; 
     result->p_bitflag = bitflag_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFuncDecl_attr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFuncDecl_attr::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 4593 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassDecl_attr::copy ( SgCopyHelp& help) const
   {
     SgClassDecl_attr* result = NULL;

  // printf ("Copy SgClassDecl_attr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClassDecl_attr*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClassDecl_attr*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClassDecl_attr: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClassDecl_attr*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClassDecl_attr(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): bitflag_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitflag
  // case: toBeCopied == COPY_DATA for bitflag
     unsigned long int bitflag_copy = p_bitflag; 
     result->p_bitflag = bitflag_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClassDecl_attr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClassDecl_attr::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 4787 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* Sg_File_Info::copy ( SgCopyHelp& help) const
   {
     Sg_File_Info* result = NULL;

  // printf ("Copy Sg_File_Info = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<Sg_File_Info*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<Sg_File_Info*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy Sg_File_Info: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<Sg_File_Info*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new Sg_File_Info(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file_id
  // case: toBeCopied == COPY_DATA for file_id
     int file_id_copy = p_file_id; 
     result->p_file_id = file_id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for line
  // case: toBeCopied == COPY_DATA for line
     int line_copy = p_line; 
     result->p_line = line_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_col
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for col
  // case: toBeCopied == COPY_DATA for col
     int col_copy = p_col; 
     result->p_col = col_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_classificationBitField
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for classificationBitField
  // case: toBeCopied == COPY_DATA for classificationBitField
     unsigned int classificationBitField_copy = p_classificationBitField; 
     result->p_classificationBitField = classificationBitField_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_physical_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physical_file_id
  // case: toBeCopied == COPY_DATA for physical_file_id
     int physical_file_id_copy = p_physical_file_id; 
     result->p_physical_file_id = physical_file_id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_physical_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physical_line
  // case: toBeCopied == COPY_DATA for physical_line
     int physical_line_copy = p_physical_line; 
     result->p_physical_line = physical_line_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_sequence_number
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_number
  // case: toBeCopied == COPY_DATA for source_sequence_number
     unsigned int source_sequence_number_copy = p_source_sequence_number; 
     result->p_source_sequence_number = source_sequence_number_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_fileIDsToUnparse()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileIDsToUnparse
  // case: toBeCopied == COPY_DATA for fileIDsToUnparse
     SgFileIdList fileIDsToUnparse_copy = p_fileIDsToUnparse; 
     result->p_fileIDsToUnparse = fileIDsToUnparse_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_fileLineNumbersToUnparse()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileLineNumbersToUnparse
  // case: toBeCopied == COPY_DATA for fileLineNumbersToUnparse
     SgFileLineNumberList fileLineNumbersToUnparse_copy = p_fileLineNumbersToUnparse; 
     result->p_fileLineNumbersToUnparse = fileLineNumbersToUnparse_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileidtoname_map
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileidtoname_map
  // case: toBeCopied == COPY_DATA for fileidtoname_map
     static std::map<int, std::string> fileidtoname_map_copy = p_fileidtoname_map; 
     result->p_fileidtoname_map = fileidtoname_map_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nametofileid_map
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nametofileid_map
  // case: toBeCopied == COPY_DATA for nametofileid_map
     static std::map<std::string, int> nametofileid_map_copy = p_nametofileid_map; 
     result->p_nametofileid_map = nametofileid_map_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy Sg_File_Info = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling Sg_File_Info::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 5026 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFile::copy ( SgCopyHelp& help) const
   {
     SgFile* result = NULL;

  // printf ("Copy SgFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFile*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFile*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFile: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFile*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFile(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFile::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 5882 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSourceFile::copy ( SgCopyHelp& help) const
   {
     SgSourceFile* result = NULL;

  // printf ("Copy SgSourceFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSourceFile*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSourceFile*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSourceFile: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSourceFile*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSourceFile(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 
  // Copy non-constructor parameter data member (access function): globalScope_copy
     SgGlobal* globalScope_copy; 
  // case: not a listType for (using conditionalToCopyVariable)globalScope
          if (get_globalScope() != NULL) 
             { 
               globalScope_copy = static_cast<SgGlobal*>(help.copyAst(get_globalScope())); 
             } 
            else 
             { 
               globalScope_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_globalScope != NULL) delete result->p_globalScope; 
     result->p_globalScope = globalScope_copy; 
  // case: not a listType for (using conditionalToSetParent)globalScope
          if ( (globalScope_copy != NULL) && (globalScope_copy->get_parent() == NULL) && (isSgType(globalScope_copy) == NULL) ) 
             { 
               globalScope_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_module_list()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module_list
  // case: toBeCopied == COPY_DATA for module_list
     SgModuleStatementPtrList module_list_copy = p_module_list; 
     result->p_module_list = module_list_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_token_list()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for token_list
  // case: toBeCopied == COPY_DATA for token_list
     SgTokenPtrList token_list_copy = p_token_list; 
     result->p_token_list = token_list_copy; 
  // Copy non-constructor parameter data member (access function): temp_holding_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for temp_holding_scope
  // case: toBeCopied == COPY_DATA for temp_holding_scope
     SgGlobal* temp_holding_scope_copy = p_temp_holding_scope; 
     result->p_temp_holding_scope = temp_holding_scope_copy; 
  // Copy non-constructor parameter data member (access function): isHeaderFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isHeaderFile
  // case: toBeCopied == COPY_DATA for isHeaderFile
     bool isHeaderFile_copy = p_isHeaderFile; 
     result->p_isHeaderFile = isHeaderFile_copy; 
  // Copy non-constructor parameter data member (access function): isHeaderFileIncludedMoreThanOnce_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isHeaderFileIncludedMoreThanOnce
  // case: toBeCopied == COPY_DATA for isHeaderFileIncludedMoreThanOnce
     bool isHeaderFileIncludedMoreThanOnce_copy = p_isHeaderFileIncludedMoreThanOnce; 
     result->p_isHeaderFileIncludedMoreThanOnce = isHeaderFileIncludedMoreThanOnce_copy; 
  // Copy non-constructor parameter data member (access function): headerFileReport_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for headerFileReport
  // case: toBeCopied == COPY_DATA for headerFileReport
     SgHeaderFileReport* headerFileReport_copy = p_headerFileReport; 
     result->p_headerFileReport = headerFileReport_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_extraIncludeDirectorySpecifierBeforeList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extraIncludeDirectorySpecifierBeforeList
  // case: toBeCopied == COPY_DATA for extraIncludeDirectorySpecifierBeforeList
     SgStringList extraIncludeDirectorySpecifierBeforeList_copy = p_extraIncludeDirectorySpecifierBeforeList; 
     result->p_extraIncludeDirectorySpecifierBeforeList = extraIncludeDirectorySpecifierBeforeList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_extraIncludeDirectorySpecifierAfterList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extraIncludeDirectorySpecifierAfterList
  // case: toBeCopied == COPY_DATA for extraIncludeDirectorySpecifierAfterList
     SgStringList extraIncludeDirectorySpecifierAfterList_copy = p_extraIncludeDirectorySpecifierAfterList; 
     result->p_extraIncludeDirectorySpecifierAfterList = extraIncludeDirectorySpecifierAfterList_copy; 
  // Copy non-constructor parameter data member (access function): associated_include_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associated_include_file
  // case: toBeCopied == COPY_DATA for associated_include_file
     SgIncludeFile* associated_include_file_copy = p_associated_include_file; 
     result->p_associated_include_file = associated_include_file_copy; 
  // Copy non-constructor parameter data member (access function): processedToIncludeCppDirectivesAndComments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for processedToIncludeCppDirectivesAndComments
  // case: toBeCopied == COPY_DATA for processedToIncludeCppDirectivesAndComments
     bool processedToIncludeCppDirectivesAndComments_copy = p_processedToIncludeCppDirectivesAndComments; 
     result->p_processedToIncludeCppDirectivesAndComments = processedToIncludeCppDirectivesAndComments_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_extra_nodes_for_namequal_init()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra_nodes_for_namequal_init
  // case: toBeCopied == COPY_DATA for extra_nodes_for_namequal_init
     SgNodePtrList extra_nodes_for_namequal_init_copy = p_extra_nodes_for_namequal_init; 
     result->p_extra_nodes_for_namequal_init = extra_nodes_for_namequal_init_copy; 
  // Copy non-constructor parameter data member (access function): isDynamicLibrary_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDynamicLibrary
  // case: toBeCopied == COPY_DATA for isDynamicLibrary
     bool isDynamicLibrary_copy = p_isDynamicLibrary; 
     result->p_isDynamicLibrary = isDynamicLibrary_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSourceFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSourceFile::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 6817 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnknownFile::copy ( SgCopyHelp& help) const
   {
     SgUnknownFile* result = NULL;

  // printf ("Copy SgUnknownFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnknownFile*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnknownFile*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnknownFile: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnknownFile*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnknownFile(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 
  // Copy non-constructor parameter data member (access function): globalScope_copy
     SgGlobal* globalScope_copy; 
  // case: not a listType for (using conditionalToCopyVariable)globalScope
          if (get_globalScope() != NULL) 
             { 
               globalScope_copy = static_cast<SgGlobal*>(help.copyAst(get_globalScope())); 
             } 
            else 
             { 
               globalScope_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_globalScope != NULL) delete result->p_globalScope; 
     result->p_globalScope = globalScope_copy; 
  // case: not a listType for (using conditionalToSetParent)globalScope
          if ( (globalScope_copy != NULL) && (globalScope_copy->get_parent() == NULL) && (isSgType(globalScope_copy) == NULL) ) 
             { 
               globalScope_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnknownFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnknownFile::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 7692 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBinaryComposite::copy ( SgCopyHelp& help) const
   {
     SgBinaryComposite* result = NULL;

  // printf ("Copy SgBinaryComposite = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBinaryComposite*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBinaryComposite*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBinaryComposite: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBinaryComposite*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBinaryComposite(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 
  // Copy non-constructor parameter data member (access function): genericFileList_copy
     SgAsmGenericFileList* genericFileList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)genericFileList
          if (get_genericFileList() != NULL) 
             { 
               genericFileList_copy = static_cast<SgAsmGenericFileList*>(help.copyAst(get_genericFileList())); 
             } 
            else 
             { 
               genericFileList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_genericFileList != NULL) delete result->p_genericFileList; 
     result->p_genericFileList = genericFileList_copy; 
  // case: not a listType for (using conditionalToSetParent)genericFileList
          if ( (genericFileList_copy != NULL) && (genericFileList_copy->get_parent() == NULL) && (isSgType(genericFileList_copy) == NULL) ) 
             { 
               genericFileList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): interpretations_copy
     SgAsmInterpretationList* interpretations_copy; 
  // case: not a listType for (using conditionalToCopyVariable)interpretations
          if (get_interpretations() != NULL) 
             { 
               interpretations_copy = static_cast<SgAsmInterpretationList*>(help.copyAst(get_interpretations())); 
             } 
            else 
             { 
               interpretations_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_interpretations != NULL) delete result->p_interpretations; 
     result->p_interpretations = interpretations_copy; 
  // case: not a listType for (using conditionalToSetParent)interpretations
          if ( (interpretations_copy != NULL) && (interpretations_copy->get_parent() == NULL) && (isSgType(interpretations_copy) == NULL) ) 
             { 
               interpretations_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBinaryComposite = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBinaryComposite::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 8586 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJvmComposite::copy ( SgCopyHelp& help) const
   {
     SgJvmComposite* result = NULL;

  // printf ("Copy SgJvmComposite = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJvmComposite*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJvmComposite*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJvmComposite: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJvmComposite*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJvmComposite(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 
  // Copy non-constructor parameter data member (access function): genericFileList_copy
     SgAsmGenericFileList* genericFileList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)genericFileList
          if (get_genericFileList() != NULL) 
             { 
               genericFileList_copy = static_cast<SgAsmGenericFileList*>(help.copyAst(get_genericFileList())); 
             } 
            else 
             { 
               genericFileList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_genericFileList != NULL) delete result->p_genericFileList; 
     result->p_genericFileList = genericFileList_copy; 
  // case: not a listType for (using conditionalToSetParent)genericFileList
          if ( (genericFileList_copy != NULL) && (genericFileList_copy->get_parent() == NULL) && (isSgType(genericFileList_copy) == NULL) ) 
             { 
               genericFileList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): interpretations_copy
     SgAsmInterpretationList* interpretations_copy; 
  // case: not a listType for (using conditionalToCopyVariable)interpretations
          if (get_interpretations() != NULL) 
             { 
               interpretations_copy = static_cast<SgAsmInterpretationList*>(help.copyAst(get_interpretations())); 
             } 
            else 
             { 
               interpretations_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_interpretations != NULL) delete result->p_interpretations; 
     result->p_interpretations = interpretations_copy; 
  // case: not a listType for (using conditionalToSetParent)interpretations
          if ( (interpretations_copy != NULL) && (interpretations_copy->get_parent() == NULL) && (isSgType(interpretations_copy) == NULL) ) 
             { 
               interpretations_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJvmComposite = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJvmComposite::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 9480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgProject::copy ( SgCopyHelp& help) const
   {
     SgProject* result = NULL;

  // printf ("Copy SgProject = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgProject*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgProject*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgProject: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgProject*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgProject(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): fileList_ptr_copy
     SgFileList* fileList_ptr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fileList_ptr
          if (get_fileList_ptr() != NULL) 
             { 
               fileList_ptr_copy = static_cast<SgFileList*>(help.copyAst(get_fileList_ptr())); 
             } 
            else 
             { 
               fileList_ptr_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_fileList_ptr != NULL) delete result->p_fileList_ptr; 
     result->p_fileList_ptr = fileList_ptr_copy; 
  // case: not a listType for (using conditionalToSetParent)fileList_ptr
          if ( (fileList_ptr_copy != NULL) && (fileList_ptr_copy->get_parent() == NULL) && (isSgType(fileList_ptr_copy) == NULL) ) 
             { 
               fileList_ptr_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendErrorCode
  // case: toBeCopied == COPY_DATA for backendErrorCode
     int backendErrorCode_copy = p_backendErrorCode; 
     result->p_backendErrorCode = backendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): keep_going_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for keep_going
  // case: toBeCopied == COPY_DATA for keep_going
     bool keep_going_copy = p_keep_going; 
     result->p_keep_going = keep_going_copy; 
  // Copy non-constructor parameter data member (access function): unparser__clobber_input_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparser__clobber_input_file
  // case: toBeCopied == COPY_DATA for unparser__clobber_input_file
     bool unparser__clobber_input_file_copy = p_unparser__clobber_input_file; 
     result->p_unparser__clobber_input_file = unparser__clobber_input_file_copy; 
  // Copy non-constructor parameter data member (access function): outputFileName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFileName
  // case: toBeCopied == COPY_DATA for outputFileName
     std::string outputFileName_copy = p_outputFileName; 
     result->p_outputFileName = outputFileName_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_sourceFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameList
  // case: toBeCopied == COPY_DATA for sourceFileNameList
     SgStringList sourceFileNameList_copy = p_sourceFileNameList; 
     result->p_sourceFileNameList = sourceFileNameList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_objectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameList
  // case: toBeCopied == COPY_DATA for objectFileNameList
     SgStringList objectFileNameList_copy = p_objectFileNameList; 
     result->p_objectFileNameList = objectFileNameList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_libraryFileList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryFileList
  // case: toBeCopied == COPY_DATA for libraryFileList
     SgStringList libraryFileList_copy = p_libraryFileList; 
     result->p_libraryFileList = libraryFileList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_librarySpecifierList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for librarySpecifierList
  // case: toBeCopied == COPY_DATA for librarySpecifierList
     SgStringList librarySpecifierList_copy = p_librarySpecifierList; 
     result->p_librarySpecifierList = librarySpecifierList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_libraryDirectorySpecifierList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryDirectorySpecifierList
  // case: toBeCopied == COPY_DATA for libraryDirectorySpecifierList
     SgStringList libraryDirectorySpecifierList_copy = p_libraryDirectorySpecifierList; 
     result->p_libraryDirectorySpecifierList = libraryDirectorySpecifierList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_includeDirectorySpecifierList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for includeDirectorySpecifierList
  // case: toBeCopied == COPY_DATA for includeDirectorySpecifierList
     SgStringList includeDirectorySpecifierList_copy = p_includeDirectorySpecifierList; 
     result->p_includeDirectorySpecifierList = includeDirectorySpecifierList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_macroSpecifierList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for macroSpecifierList
  // case: toBeCopied == COPY_DATA for macroSpecifierList
     SgStringList macroSpecifierList_copy = p_macroSpecifierList; 
     result->p_macroSpecifierList = macroSpecifierList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_preincludeFileList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preincludeFileList
  // case: toBeCopied == COPY_DATA for preincludeFileList
     SgStringList preincludeFileList_copy = p_preincludeFileList; 
     result->p_preincludeFileList = preincludeFileList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_preincludeDirectoryList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preincludeDirectoryList
  // case: toBeCopied == COPY_DATA for preincludeDirectoryList
     SgStringList preincludeDirectoryList_copy = p_preincludeDirectoryList; 
     result->p_preincludeDirectoryList = preincludeDirectoryList_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): prelink_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prelink
  // case: toBeCopied == COPY_DATA for prelink
     bool prelink_copy = p_prelink; 
     result->p_prelink = prelink_copy; 
  // Copy non-constructor parameter data member (access function): template_instantiation_mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_instantiation_mode
  // case: toBeCopied == COPY_DATA for template_instantiation_mode
     SgProject::template_instantiation_enum template_instantiation_mode_copy = p_template_instantiation_mode; 
     result->p_template_instantiation_mode = template_instantiation_mode_copy; 
  // Copy non-constructor parameter data member (access function): ast_merge_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ast_merge
  // case: toBeCopied == COPY_DATA for ast_merge
     bool ast_merge_copy = p_ast_merge; 
     result->p_ast_merge = ast_merge_copy; 
  // Copy non-constructor parameter data member (access function): projectSpecificDatabaseFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for projectSpecificDatabaseFile
  // case: toBeCopied == COPY_DATA for projectSpecificDatabaseFile
     std::string projectSpecificDatabaseFile_copy = p_projectSpecificDatabaseFile; 
     result->p_projectSpecificDatabaseFile = projectSpecificDatabaseFile_copy; 
  // Copy non-constructor parameter data member (access function): C_PreprocessorOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_PreprocessorOnly
  // case: toBeCopied == COPY_DATA for C_PreprocessorOnly
     bool C_PreprocessorOnly_copy = p_C_PreprocessorOnly; 
     result->p_C_PreprocessorOnly = C_PreprocessorOnly_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): compilationPerformanceFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compilationPerformanceFile
  // case: toBeCopied == COPY_DATA for compilationPerformanceFile
     std::string compilationPerformanceFile_copy = p_compilationPerformanceFile; 
     result->p_compilationPerformanceFile = compilationPerformanceFile_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_includePathList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for includePathList
  // case: toBeCopied == COPY_DATA for includePathList
     SgStringList includePathList_copy = p_includePathList; 
     result->p_includePathList = includePathList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_excludePathList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for excludePathList
  // case: toBeCopied == COPY_DATA for excludePathList
     SgStringList excludePathList_copy = p_excludePathList; 
     result->p_excludePathList = excludePathList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_includeFileList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for includeFileList
  // case: toBeCopied == COPY_DATA for includeFileList
     SgStringList includeFileList_copy = p_includeFileList; 
     result->p_includeFileList = includeFileList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_excludeFileList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for excludeFileList
  // case: toBeCopied == COPY_DATA for excludeFileList
     SgStringList excludeFileList_copy = p_excludeFileList; 
     result->p_excludeFileList = excludeFileList_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): dataBaseFilename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dataBaseFilename
  // case: toBeCopied == COPY_DATA for dataBaseFilename
     std::string dataBaseFilename_copy = p_dataBaseFilename; 
     result->p_dataBaseFilename = dataBaseFilename_copy; 
  // Copy non-constructor parameter data member (access function): directoryList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directoryList
  // case: toBeCopied == COPY_DATA for directoryList
     SgDirectoryList* directoryList_copy = p_directoryList; 
     result->p_directoryList = directoryList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_C_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Cxx_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_C11_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C11_only
  // case: toBeCopied == COPY_DATA for C11_only
     bool C11_only_copy = p_C11_only; 
     result->p_C11_only = C11_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Cxx0x_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx0x_only
  // case: toBeCopied == COPY_DATA for Cxx0x_only
     bool Cxx0x_only_copy = p_Cxx0x_only; 
     result->p_Cxx0x_only = Cxx0x_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Cxx11_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx11_only
  // case: toBeCopied == COPY_DATA for Cxx11_only
     bool Cxx11_only_copy = p_Cxx11_only; 
     result->p_Cxx11_only = Cxx11_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_C14_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C14_only
  // case: toBeCopied == COPY_DATA for C14_only
     bool C14_only_copy = p_C14_only; 
     result->p_C14_only = C14_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Cxx14_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx14_only
  // case: toBeCopied == COPY_DATA for Cxx14_only
     bool Cxx14_only_copy = p_Cxx14_only; 
     result->p_Cxx14_only = Cxx14_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Fortran_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_ofp_jvm_options_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_ofp_jvm_options
  // case: toBeCopied == COPY_DATA for Fortran_ofp_jvm_options
     std::list<std::string> Fortran_ofp_jvm_options_copy = p_Fortran_ofp_jvm_options; 
     result->p_Fortran_ofp_jvm_options = Fortran_ofp_jvm_options_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Java_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Jvm_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Jovial_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Ada_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_linking_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_linking
  // case: toBeCopied == COPY_DATA for openmp_linking
     bool openmp_linking_copy = p_openmp_linking; 
     result->p_openmp_linking = openmp_linking_copy; 
  // Copy non-constructor parameter data member (access function): Java_ecj_jvm_options_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_ecj_jvm_options
  // case: toBeCopied == COPY_DATA for Java_ecj_jvm_options
     std::list<std::string> Java_ecj_jvm_options_copy = p_Java_ecj_jvm_options; 
     result->p_Java_ecj_jvm_options = Java_ecj_jvm_options_copy; 
  // Copy non-constructor parameter data member (access function): Java_batch_mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_batch_mode
  // case: toBeCopied == COPY_DATA for Java_batch_mode
     bool Java_batch_mode_copy = p_Java_batch_mode; 
     result->p_Java_batch_mode = Java_batch_mode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Java_classpath
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_classpath
  // case: toBeCopied == COPY_DATA for Java_classpath
     std::list<std::string> Java_classpath_copy = p_Java_classpath; 
     result->p_Java_classpath = Java_classpath_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Java_sourcepath
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_sourcepath
  // case: toBeCopied == COPY_DATA for Java_sourcepath
     std::list<std::string> Java_sourcepath_copy = p_Java_sourcepath; 
     result->p_Java_sourcepath = Java_sourcepath_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Java_destdir
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_destdir
  // case: toBeCopied == COPY_DATA for Java_destdir
     std::string Java_destdir_copy = p_Java_destdir; 
     result->p_Java_destdir = Java_destdir_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Java_source_destdir
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_source_destdir
  // case: toBeCopied == COPY_DATA for Java_source_destdir
     std::string Java_source_destdir_copy = p_Java_source_destdir; 
     result->p_Java_source_destdir = Java_source_destdir_copy; 
  // Copy non-constructor parameter data member (access function): Java_s_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_s
  // case: toBeCopied == COPY_DATA for Java_s
     std::string Java_s_copy = p_Java_s; 
     result->p_Java_s = Java_s_copy; 
  // Copy non-constructor parameter data member (access function): Java_source_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_source
  // case: toBeCopied == COPY_DATA for Java_source
     std::string Java_source_copy = p_Java_source; 
     result->p_Java_source = Java_source_copy; 
  // Copy non-constructor parameter data member (access function): Java_target_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_target
  // case: toBeCopied == COPY_DATA for Java_target
     std::string Java_target_copy = p_Java_target; 
     result->p_Java_target = Java_target_copy; 
  // Copy non-constructor parameter data member (access function): Java_encoding_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_encoding
  // case: toBeCopied == COPY_DATA for Java_encoding
     std::string Java_encoding_copy = p_Java_encoding; 
     result->p_Java_encoding = Java_encoding_copy; 
  // Copy non-constructor parameter data member (access function): Java_g_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_g
  // case: toBeCopied == COPY_DATA for Java_g
     std::string Java_g_copy = p_Java_g; 
     result->p_Java_g = Java_g_copy; 
  // Copy non-constructor parameter data member (access function): Java_nowarn_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_nowarn
  // case: toBeCopied == COPY_DATA for Java_nowarn
     bool Java_nowarn_copy = p_Java_nowarn; 
     result->p_Java_nowarn = Java_nowarn_copy; 
  // Copy non-constructor parameter data member (access function): Java_verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_verbose
  // case: toBeCopied == COPY_DATA for Java_verbose
     bool Java_verbose_copy = p_Java_verbose; 
     result->p_Java_verbose = Java_verbose_copy; 
  // Copy non-constructor parameter data member (access function): Java_deprecation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_deprecation
  // case: toBeCopied == COPY_DATA for Java_deprecation
     bool Java_deprecation_copy = p_Java_deprecation; 
     result->p_Java_deprecation = Java_deprecation_copy; 
  // Copy non-constructor parameter data member (access function): Java_bootclasspath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_bootclasspath
  // case: toBeCopied == COPY_DATA for Java_bootclasspath
     std::list<std::string> Java_bootclasspath_copy = p_Java_bootclasspath; 
     result->p_Java_bootclasspath = Java_bootclasspath_copy; 
  // Copy non-constructor parameter data member (access function): addCppDirectivesToAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for addCppDirectivesToAST
  // case: toBeCopied == COPY_DATA for addCppDirectivesToAST
     bool addCppDirectivesToAST_copy = p_addCppDirectivesToAST; 
     result->p_addCppDirectivesToAST = addCppDirectivesToAST_copy; 
  // Copy non-constructor parameter data member (access function): includingPreprocessingInfosMap_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for includingPreprocessingInfosMap
  // case: toBeCopied == COPY_DATA for includingPreprocessingInfosMap
     std::map<std::string, std::set<PreprocessingInfo*> > includingPreprocessingInfosMap_copy = p_includingPreprocessingInfosMap; 
     result->p_includingPreprocessingInfosMap = includingPreprocessingInfosMap_copy; 
  // Copy non-constructor parameter data member (access function): quotedIncludesSearchPaths_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for quotedIncludesSearchPaths
  // case: toBeCopied == COPY_DATA for quotedIncludesSearchPaths
     std::list<std::string> quotedIncludesSearchPaths_copy = p_quotedIncludesSearchPaths; 
     result->p_quotedIncludesSearchPaths = quotedIncludesSearchPaths_copy; 
  // Copy non-constructor parameter data member (access function): bracketedIncludesSearchPaths_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bracketedIncludesSearchPaths
  // case: toBeCopied == COPY_DATA for bracketedIncludesSearchPaths
     std::list<std::string> bracketedIncludesSearchPaths_copy = p_bracketedIncludesSearchPaths; 
     result->p_bracketedIncludesSearchPaths = bracketedIncludesSearchPaths_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFilesRootFolder_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFilesRootFolder
  // case: toBeCopied == COPY_DATA for unparseHeaderFilesRootFolder
     std::string unparseHeaderFilesRootFolder_copy = p_unparseHeaderFilesRootFolder; 
     result->p_unparseHeaderFilesRootFolder = unparseHeaderFilesRootFolder_copy; 
  // Copy non-constructor parameter data member (access function): frontendConstantFolding_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendConstantFolding
  // case: toBeCopied == COPY_DATA for frontendConstantFolding
     bool frontendConstantFolding_copy = p_frontendConstantFolding; 
     result->p_frontendConstantFolding = frontendConstantFolding_copy; 
  // Copy non-constructor parameter data member (access function): globalScopeAcrossFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for globalScopeAcrossFiles
  // case: toBeCopied == COPY_DATA for globalScopeAcrossFiles
     SgGlobal* globalScopeAcrossFiles_copy = p_globalScopeAcrossFiles; 
     result->p_globalScopeAcrossFiles = globalScopeAcrossFiles_copy; 
  // Copy non-constructor parameter data member (access function): unparse_in_same_directory_as_input_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_in_same_directory_as_input_file
  // case: toBeCopied == COPY_DATA for unparse_in_same_directory_as_input_file
     bool unparse_in_same_directory_as_input_file_copy = p_unparse_in_same_directory_as_input_file; 
     result->p_unparse_in_same_directory_as_input_file = unparse_in_same_directory_as_input_file_copy; 
  // Copy non-constructor parameter data member (access function): stop_after_compilation_do_not_assemble_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stop_after_compilation_do_not_assemble_file
  // case: toBeCopied == COPY_DATA for stop_after_compilation_do_not_assemble_file
     bool stop_after_compilation_do_not_assemble_file_copy = p_stop_after_compilation_do_not_assemble_file; 
     result->p_stop_after_compilation_do_not_assemble_file = stop_after_compilation_do_not_assemble_file_copy; 
  // Copy non-constructor parameter data member (access function): gnuOptionForUndefinedSymbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnuOptionForUndefinedSymbol
  // case: toBeCopied == COPY_DATA for gnuOptionForUndefinedSymbol
     std::string gnuOptionForUndefinedSymbol_copy = p_gnuOptionForUndefinedSymbol; 
     result->p_gnuOptionForUndefinedSymbol = gnuOptionForUndefinedSymbol_copy; 
  // Copy non-constructor parameter data member (access function): mode_32_bit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode_32_bit
  // case: toBeCopied == COPY_DATA for mode_32_bit
     bool mode_32_bit_copy = p_mode_32_bit; 
     result->p_mode_32_bit = mode_32_bit_copy; 
  // Copy non-constructor parameter data member (access function): noclobber_output_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for noclobber_output_file
  // case: toBeCopied == COPY_DATA for noclobber_output_file
     bool noclobber_output_file_copy = p_noclobber_output_file; 
     result->p_noclobber_output_file = noclobber_output_file_copy; 
  // Copy non-constructor parameter data member (access function): noclobber_if_different_output_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for noclobber_if_different_output_file
  // case: toBeCopied == COPY_DATA for noclobber_if_different_output_file
     bool noclobber_if_different_output_file_copy = p_noclobber_if_different_output_file; 
     result->p_noclobber_if_different_output_file = noclobber_if_different_output_file_copy; 
  // Copy non-constructor parameter data member (access function): suppressConstantFoldingPostProcessing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppressConstantFoldingPostProcessing
  // case: toBeCopied == COPY_DATA for suppressConstantFoldingPostProcessing
     bool suppressConstantFoldingPostProcessing_copy = p_suppressConstantFoldingPostProcessing; 
     result->p_suppressConstantFoldingPostProcessing = suppressConstantFoldingPostProcessing_copy; 
  // Copy non-constructor parameter data member (access function): suppressNameQualificationAcrossWholeTranslationUnit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppressNameQualificationAcrossWholeTranslationUnit
  // case: toBeCopied == COPY_DATA for suppressNameQualificationAcrossWholeTranslationUnit
     bool suppressNameQualificationAcrossWholeTranslationUnit_copy = p_suppressNameQualificationAcrossWholeTranslationUnit; 
     result->p_suppressNameQualificationAcrossWholeTranslationUnit = suppressNameQualificationAcrossWholeTranslationUnit_copy; 
  // Copy non-constructor parameter data member (access function): appendPID_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for appendPID
  // case: toBeCopied == COPY_DATA for appendPID
     bool appendPID_copy = p_appendPID; 
     result->p_appendPID = appendPID_copy; 
  // Copy non-constructor parameter data member (access function): reportOnHeaderFileUnparsing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reportOnHeaderFileUnparsing
  // case: toBeCopied == COPY_DATA for reportOnHeaderFileUnparsing
     bool reportOnHeaderFileUnparsing_copy = p_reportOnHeaderFileUnparsing; 
     result->p_reportOnHeaderFileUnparsing = reportOnHeaderFileUnparsing_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_applicationRootDirectory
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for applicationRootDirectory
  // case: toBeCopied == COPY_DATA for applicationRootDirectory
     std::string applicationRootDirectory_copy = p_applicationRootDirectory; 
     result->p_applicationRootDirectory = applicationRootDirectory_copy; 
  // Copy non-constructor parameter data member (access function): usingApplicationRootDirectory_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usingApplicationRootDirectory
  // case: toBeCopied == COPY_DATA for usingApplicationRootDirectory
     bool usingApplicationRootDirectory_copy = p_usingApplicationRootDirectory; 
     result->p_usingApplicationRootDirectory = usingApplicationRootDirectory_copy; 
  // Copy non-constructor parameter data member (access function): usingDeferredTransformations_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usingDeferredTransformations
  // case: toBeCopied == COPY_DATA for usingDeferredTransformations
     bool usingDeferredTransformations_copy = p_usingDeferredTransformations; 
     result->p_usingDeferredTransformations = usingDeferredTransformations_copy; 
  // Copy non-constructor parameter data member (access function): astfile_out_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for astfile_out
  // case: toBeCopied == COPY_DATA for astfile_out
     std::string astfile_out_copy = p_astfile_out; 
     result->p_astfile_out = astfile_out_copy; 
  // Copy non-constructor parameter data member (access function): astfiles_in_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for astfiles_in
  // case: toBeCopied == COPY_DATA for astfiles_in
     std::list<std::string> astfiles_in_copy = p_astfiles_in; 
     result->p_astfiles_in = astfiles_in_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_extraIncludeDirectorySpecifierBeforeList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extraIncludeDirectorySpecifierBeforeList
  // case: toBeCopied == COPY_DATA for extraIncludeDirectorySpecifierBeforeList
     SgStringList extraIncludeDirectorySpecifierBeforeList_copy = p_extraIncludeDirectorySpecifierBeforeList; 
     result->p_extraIncludeDirectorySpecifierBeforeList = extraIncludeDirectorySpecifierBeforeList_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_extraIncludeDirectorySpecifierAfterList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extraIncludeDirectorySpecifierAfterList
  // case: toBeCopied == COPY_DATA for extraIncludeDirectorySpecifierAfterList
     SgStringList extraIncludeDirectorySpecifierAfterList_copy = p_extraIncludeDirectorySpecifierAfterList; 
     result->p_extraIncludeDirectorySpecifierAfterList = extraIncludeDirectorySpecifierAfterList_copy; 
  // Copy non-constructor parameter data member (access function): skip_post_processing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_post_processing
  // case: toBeCopied == COPY_DATA for skip_post_processing
     bool skip_post_processing_copy = p_skip_post_processing; 
     result->p_skip_post_processing = skip_post_processing_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgProject = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgProject::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 10134 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOptions::copy ( SgCopyHelp& help) const
   {
     SgOptions* result = NULL;

  // printf ("Copy SgOptions = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOptions*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOptions*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOptions: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOptions*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOptions(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_default_output
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for default_output
  // case: toBeCopied == COPY_DATA for default_output
     std::ostream* default_output_copy = p_default_output; 
     result->p_default_output = default_output_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_debug_output
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for debug_output
  // case: toBeCopied == COPY_DATA for debug_output
     std::ostream* debug_output_copy = p_debug_output; 
     result->p_debug_output = debug_output_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_error_output
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for error_output
  // case: toBeCopied == COPY_DATA for error_output
     std::ostream* error_output_copy = p_error_output; 
     result->p_error_output = error_output_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_logging_output
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for logging_output
  // case: toBeCopied == COPY_DATA for logging_output
     std::ostream* logging_output_copy = p_logging_output; 
     result->p_logging_output = logging_output_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_debug_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for debug_level
  // case: toBeCopied == COPY_DATA for debug_level
     int debug_level_copy = p_debug_level; 
     result->p_debug_level = debug_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_logging_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for logging_level
  // case: toBeCopied == COPY_DATA for logging_level
     int logging_level_copy = p_logging_level; 
     result->p_logging_level = logging_level_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOptions = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOptions::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 10348 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnparse_Info::copy ( SgCopyHelp& help) const
   {
     SgUnparse_Info* result = NULL;

  // printf ("Copy SgUnparse_Info = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnparse_Info*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnparse_Info*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnparse_Info: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnparse_Info*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnparse_Info(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_unparse_attribute
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_attribute
  // case: toBeCopied == COPY_DATA for unparse_attribute
     SgBitVector unparse_attribute_copy = p_unparse_attribute; 
     result->p_unparse_attribute = unparse_attribute_copy; 
  // Copy non-constructor parameter data member (access function): access_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_attribute
  // case: toBeCopied == COPY_DATA for access_attribute
     int access_attribute_copy = p_access_attribute; 
     result->p_access_attribute = access_attribute_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nested_expression
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nested_expression
  // case: toBeCopied == COPY_DATA for nested_expression
     int nested_expression_copy = p_nested_expression; 
     result->p_nested_expression = nested_expression_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operator_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operator_name
  // case: toBeCopied == COPY_DATA for operator_name
     std::string operator_name_copy = p_operator_name; 
     result->p_operator_name = operator_name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_var_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for var_name
  // case: toBeCopied == COPY_DATA for var_name
     SgName var_name_copy = p_var_name; 
     result->p_var_name = var_name_copy; 
  // Copy non-constructor parameter data member (access function): declstatement_ptr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declstatement_ptr
  // case: toBeCopied == COPY_DATA for declstatement_ptr
     SgDeclarationStatement* declstatement_ptr_copy = p_declstatement_ptr; 
     result->p_declstatement_ptr = declstatement_ptr_copy; 
  // Copy non-constructor parameter data member (access function): declaration_of_context_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration_of_context
  // case: toBeCopied == COPY_DATA for declaration_of_context
     SgDeclarationStatement* declaration_of_context_copy = p_declaration_of_context; 
     result->p_declaration_of_context = declaration_of_context_copy; 
  // Copy non-constructor parameter data member (access function): current_context_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_context
  // case: toBeCopied == COPY_DATA for current_context
     SgNamedType* current_context_copy = p_current_context; 
     result->p_current_context = current_context_copy; 
  // Copy non-constructor parameter data member (access function): array_index_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for array_index_list
  // case: toBeCopied == COPY_DATA for array_index_list
     SgName array_index_list_copy = p_array_index_list; 
     result->p_array_index_list = array_index_list_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_structureTagProcessingList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for structureTagProcessingList
  // case: toBeCopied == COPY_DATA for structureTagProcessingList
     static SgTypePtrList structureTagProcessingList_copy = p_structureTagProcessingList; 
     result->p_structureTagProcessingList = structureTagProcessingList_copy; 
  // Copy non-constructor parameter data member (access function): current_namespace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_namespace
  // case: toBeCopied == COPY_DATA for current_namespace
     SgNamespaceDeclarationStatement* current_namespace_copy = p_current_namespace; 
     result->p_current_namespace = current_namespace_copy; 
  // Copy non-constructor parameter data member (access function): outputCodeGenerationFormatDelimiters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputCodeGenerationFormatDelimiters
  // case: toBeCopied == COPY_DATA for outputCodeGenerationFormatDelimiters
     bool outputCodeGenerationFormatDelimiters_copy = p_outputCodeGenerationFormatDelimiters; 
     result->p_outputCodeGenerationFormatDelimiters = outputCodeGenerationFormatDelimiters_copy; 
  // Copy non-constructor parameter data member (access function): qualifiedNameList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): current_function_call_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_function_call
  // case: toBeCopied == COPY_DATA for current_function_call
     SgFunctionCallExp* current_function_call_copy = p_current_function_call; 
     result->p_current_function_call = current_function_call_copy; 
  // Copy non-constructor parameter data member (access function): current_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_scope
  // case: toBeCopied == COPY_DATA for current_scope
     SgScopeStatement* current_scope_copy = p_current_scope; 
     result->p_current_scope = current_scope_copy; 
  // Copy non-constructor parameter data member (access function): reference_node_for_qualification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reference_node_for_qualification
  // case: toBeCopied == COPY_DATA for reference_node_for_qualification
     SgNode* reference_node_for_qualification_copy = p_reference_node_for_qualification; 
     result->p_reference_node_for_qualification = reference_node_for_qualification_copy; 
  // Copy non-constructor parameter data member (access function): name_qualification_length_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (access function): type_elaboration_required_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (access function): global_qualification_required_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): nestingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nestingLevel
  // case: toBeCopied == COPY_DATA for nestingLevel
     int nestingLevel_copy = p_nestingLevel; 
     result->p_nestingLevel = nestingLevel_copy; 
  // Copy non-constructor parameter data member (access function): language_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for language
  // case: toBeCopied == COPY_DATA for language
     SgFile::languageOption_enum language_copy = p_language; 
     result->p_language = language_copy; 
  // Copy non-constructor parameter data member (access function): current_source_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_source_file
  // case: toBeCopied == COPY_DATA for current_source_file
     SgSourceFile* current_source_file_copy = p_current_source_file; 
     result->p_current_source_file = current_source_file_copy; 
  // Copy non-constructor parameter data member (access function): use_generated_name_for_template_arguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_generated_name_for_template_arguments
  // case: toBeCopied == COPY_DATA for use_generated_name_for_template_arguments
     bool use_generated_name_for_template_arguments_copy = p_use_generated_name_for_template_arguments; 
     result->p_use_generated_name_for_template_arguments = use_generated_name_for_template_arguments_copy; 
  // Copy non-constructor parameter data member (access function): user_defined_literal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for user_defined_literal
  // case: toBeCopied == COPY_DATA for user_defined_literal
     bool user_defined_literal_copy = p_user_defined_literal; 
     result->p_user_defined_literal = user_defined_literal_copy; 
  // Copy non-constructor parameter data member (access function): declstatement_associated_with_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declstatement_associated_with_type
  // case: toBeCopied == COPY_DATA for declstatement_associated_with_type
     SgDeclarationStatement* declstatement_associated_with_type_copy = p_declstatement_associated_with_type; 
     result->p_declstatement_associated_with_type = declstatement_associated_with_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_extern_C_with_braces
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extern_C_with_braces
  // case: toBeCopied == COPY_DATA for extern_C_with_braces
     static bool extern_C_with_braces_copy = p_extern_C_with_braces; 
     result->p_extern_C_with_braces = extern_C_with_braces_copy; 
  // Copy non-constructor parameter data member (access function): context_for_added_parentheses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for context_for_added_parentheses
  // case: toBeCopied == COPY_DATA for context_for_added_parentheses
     bool context_for_added_parentheses_copy = p_context_for_added_parentheses; 
     result->p_context_for_added_parentheses = context_for_added_parentheses_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnparse_Info = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnparse_Info::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 10667 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBaseClass::copy ( SgCopyHelp& help) const
   {
     SgBaseClass* result = NULL;

  // printf ("Copy SgBaseClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBaseClass*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBaseClass*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBaseClass: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBaseClass*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_class_copy
     SgClassDeclaration* base_class_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class
          if (get_base_class() != NULL) 
             { 
               base_class_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_base_class())); 
             } 
            else 
             { 
               base_class_copy = NULL; 
             } 
  // Copy constructor parameter data member: isDirectBaseClass_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDirectBaseClass
  // case: toBeCopied == COPY_DATA for isDirectBaseClass
     bool isDirectBaseClass_copy = p_isDirectBaseClass; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBaseClass(  base_class_copy, isDirectBaseClass_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): baseClassModifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseClassModifier
  // case: toBeCopied == CLONE_PTR for baseClassModifier
     SgBaseClassModifier* baseClassModifier_copy = NULL; 
     if ( p_baseClassModifier != NULL ) 
        { 
          baseClassModifier_copy = new SgBaseClassModifier( *p_baseClassModifier); 
        } 
       else 
        { 
          baseClassModifier_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseClassModifier != NULL) delete result->p_baseClassModifier; 
  /* add assignment to result here */ 
     result->p_baseClassModifier = baseClassModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // case: not a listType for (using conditionalToSetParent)base_class
          if ( (base_class_copy != NULL) && (base_class_copy->get_parent() == NULL) && (isSgType(base_class_copy) == NULL) ) 
             { 
               base_class_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBaseClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBaseClass::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 10903 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExpBaseClass::copy ( SgCopyHelp& help) const
   {
     SgExpBaseClass* result = NULL;

  // printf ("Copy SgExpBaseClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgExpBaseClass*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgExpBaseClass*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgExpBaseClass: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgExpBaseClass*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_class_copy
     SgClassDeclaration* base_class_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class
          if (get_base_class() != NULL) 
             { 
               base_class_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_base_class())); 
             } 
            else 
             { 
               base_class_copy = NULL; 
             } 
  // Copy constructor parameter data member: isDirectBaseClass_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDirectBaseClass
  // case: toBeCopied == COPY_DATA for isDirectBaseClass
     bool isDirectBaseClass_copy = p_isDirectBaseClass; 
  // Copy constructor parameter data member: base_class_exp_copy
     SgExpression* base_class_exp_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class_exp
          if (get_base_class_exp() != NULL) 
             { 
               base_class_exp_copy = static_cast<SgExpression*>(help.copyAst(get_base_class_exp())); 
             } 
            else 
             { 
               base_class_exp_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgExpBaseClass(  base_class_copy, isDirectBaseClass_copy, base_class_exp_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): baseClassModifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseClassModifier
  // case: toBeCopied == CLONE_PTR for baseClassModifier
     SgBaseClassModifier* baseClassModifier_copy = NULL; 
     if ( p_baseClassModifier != NULL ) 
        { 
          baseClassModifier_copy = new SgBaseClassModifier( *p_baseClassModifier); 
        } 
       else 
        { 
          baseClassModifier_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseClassModifier != NULL) delete result->p_baseClassModifier; 
  /* add assignment to result here */ 
     result->p_baseClassModifier = baseClassModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // case: not a listType for (using conditionalToSetParent)base_class_exp
          if ( (base_class_exp_copy != NULL) && (base_class_exp_copy->get_parent() == NULL) && (isSgType(base_class_exp_copy) == NULL) ) 
             { 
               base_class_exp_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)base_class
          if ( (base_class_copy != NULL) && (base_class_copy->get_parent() == NULL) && (isSgType(base_class_copy) == NULL) ) 
             { 
               base_class_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgExpBaseClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgExpBaseClass::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 11155 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealBaseClass::copy ( SgCopyHelp& help) const
   {
     SgNonrealBaseClass* result = NULL;

  // printf ("Copy SgNonrealBaseClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNonrealBaseClass*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNonrealBaseClass*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNonrealBaseClass: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNonrealBaseClass*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_class_copy
     SgClassDeclaration* base_class_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class
          if (get_base_class() != NULL) 
             { 
               base_class_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_base_class())); 
             } 
            else 
             { 
               base_class_copy = NULL; 
             } 
  // Copy constructor parameter data member: isDirectBaseClass_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDirectBaseClass
  // case: toBeCopied == COPY_DATA for isDirectBaseClass
     bool isDirectBaseClass_copy = p_isDirectBaseClass; 
  // Copy constructor parameter data member: base_class_nonreal_copy
     SgNonrealDecl* base_class_nonreal_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class_nonreal
          if (get_base_class_nonreal() != NULL) 
             { 
               base_class_nonreal_copy = static_cast<SgNonrealDecl*>(help.copyAst(get_base_class_nonreal())); 
             } 
            else 
             { 
               base_class_nonreal_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNonrealBaseClass(  base_class_copy, isDirectBaseClass_copy, base_class_nonreal_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): baseClassModifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseClassModifier
  // case: toBeCopied == CLONE_PTR for baseClassModifier
     SgBaseClassModifier* baseClassModifier_copy = NULL; 
     if ( p_baseClassModifier != NULL ) 
        { 
          baseClassModifier_copy = new SgBaseClassModifier( *p_baseClassModifier); 
        } 
       else 
        { 
          baseClassModifier_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseClassModifier != NULL) delete result->p_baseClassModifier; 
  /* add assignment to result here */ 
     result->p_baseClassModifier = baseClassModifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // case: not a listType for (using conditionalToSetParent)base_class_nonreal
          if ( (base_class_nonreal_copy != NULL) && (base_class_nonreal_copy->get_parent() == NULL) && (isSgType(base_class_nonreal_copy) == NULL) ) 
             { 
               base_class_nonreal_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)base_class
          if ( (base_class_copy != NULL) && (base_class_copy->get_parent() == NULL) && (isSgType(base_class_copy) == NULL) ) 
             { 
               base_class_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNonrealBaseClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNonrealBaseClass::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 11407 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypedefSeq::copy ( SgCopyHelp& help) const
   {
     SgTypedefSeq* result = NULL;

  // printf ("Copy SgTypedefSeq = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypedefSeq*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypedefSeq*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypedefSeq: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypedefSeq*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypedefSeq(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_typedefs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypePtrList typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypedefSeq = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypedefSeq::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 11596 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateParameter::copy ( SgCopyHelp& help) const
   {
     SgTemplateParameter* result = NULL;

  // printf ("Copy SgTemplateParameter = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateParameter*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateParameter*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateParameter: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateParameter*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: parameterType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterType
  // case: toBeCopied == COPY_DATA for parameterType
     SgTemplateParameter::template_parameter_enum parameterType_copy = p_parameterType; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
  // Copy constructor parameter data member: defaultTypeParameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for defaultTypeParameter
  // case: toBeCopied == COPY_DATA for defaultTypeParameter
     SgType* defaultTypeParameter_copy = p_defaultTypeParameter; 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: defaultExpressionParameter_copy
     SgExpression* defaultExpressionParameter_copy; 
  // case: not a listType for (using conditionalToCopyVariable)defaultExpressionParameter
          if (get_defaultExpressionParameter() != NULL) 
             { 
               defaultExpressionParameter_copy = static_cast<SgExpression*>(help.copyAst(get_defaultExpressionParameter())); 
             } 
            else 
             { 
               defaultExpressionParameter_copy = NULL; 
             } 
  // Copy constructor parameter data member: templateDeclaration_copy
     SgDeclarationStatement* templateDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)templateDeclaration
          if (get_templateDeclaration() != NULL) 
             { 
               templateDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_templateDeclaration())); 
             } 
            else 
             { 
               templateDeclaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: defaultTemplateDeclarationParameter_copy
     SgDeclarationStatement* defaultTemplateDeclarationParameter_copy; 
  // case: not a listType for (using conditionalToCopyVariable)defaultTemplateDeclarationParameter
          if (get_defaultTemplateDeclarationParameter() != NULL) 
             { 
               defaultTemplateDeclarationParameter_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_defaultTemplateDeclarationParameter())); 
             } 
            else 
             { 
               defaultTemplateDeclarationParameter_copy = NULL; 
             } 
  // Copy constructor parameter data member: initializedName_copy
     SgInitializedName* initializedName_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializedName
          if (get_initializedName() != NULL) 
             { 
               initializedName_copy = static_cast<SgInitializedName*>(help.copyAst(get_initializedName())); 
             } 
            else 
             { 
               initializedName_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateParameter(  parameterType_copy, type_copy, defaultTypeParameter_copy, expression_copy, defaultExpressionParameter_copy, templateDeclaration_copy, defaultTemplateDeclarationParameter_copy, initializedName_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)defaultExpressionParameter
          if ( (defaultExpressionParameter_copy != NULL) && (defaultExpressionParameter_copy->get_parent() == NULL) && (isSgType(defaultExpressionParameter_copy) == NULL) ) 
             { 
               defaultExpressionParameter_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)templateDeclaration
          if ( (templateDeclaration_copy != NULL) && (templateDeclaration_copy->get_parent() == NULL) && (isSgType(templateDeclaration_copy) == NULL) ) 
             { 
               templateDeclaration_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)defaultTemplateDeclarationParameter
          if ( (defaultTemplateDeclarationParameter_copy != NULL) && (defaultTemplateDeclarationParameter_copy->get_parent() == NULL) && (isSgType(defaultTemplateDeclarationParameter_copy) == NULL) ) 
             { 
               defaultTemplateDeclarationParameter_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)initializedName
          if ( (initializedName_copy != NULL) && (initializedName_copy->get_parent() == NULL) && (isSgType(initializedName_copy) == NULL) ) 
             { 
               initializedName_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateParameter = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateParameter::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 11873 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateArgument::copy ( SgCopyHelp& help) const
   {
     SgTemplateArgument* result = NULL;

  // printf ("Copy SgTemplateArgument = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateArgument*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateArgument*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateArgument: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateArgument*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: argumentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argumentType
  // case: toBeCopied == COPY_DATA for argumentType
     SgTemplateArgument::template_argument_enum argumentType_copy = p_argumentType; 
  // Copy constructor parameter data member: isArrayBoundUnknownType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isArrayBoundUnknownType
  // case: toBeCopied == COPY_DATA for isArrayBoundUnknownType
     bool isArrayBoundUnknownType_copy = p_isArrayBoundUnknownType; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: templateDeclaration_copy
     SgDeclarationStatement* templateDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)templateDeclaration
          if (get_templateDeclaration() != NULL) 
             { 
               templateDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_templateDeclaration())); 
             } 
            else 
             { 
               templateDeclaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: explicitlySpecified_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicitlySpecified
  // case: toBeCopied == COPY_DATA for explicitlySpecified
     bool explicitlySpecified_copy = p_explicitlySpecified; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateArgument(  argumentType_copy, isArrayBoundUnknownType_copy, type_copy, expression_copy, templateDeclaration_copy, explicitlySpecified_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): unparsable_type_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsable_type_alias
  // case: toBeCopied == COPY_DATA for unparsable_type_alias
     SgType* unparsable_type_alias_copy = p_unparsable_type_alias; 
     result->p_unparsable_type_alias = unparsable_type_alias_copy; 
  // Copy non-constructor parameter data member (access function): initializedName_copy
     SgInitializedName* initializedName_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializedName
          if (get_initializedName() != NULL) 
             { 
               initializedName_copy = static_cast<SgInitializedName*>(help.copyAst(get_initializedName())); 
             } 
            else 
             { 
               initializedName_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_initializedName != NULL) delete result->p_initializedName; 
     result->p_initializedName = initializedName_copy; 
  // case: not a listType for (using conditionalToSetParent)initializedName
          if ( (initializedName_copy != NULL) && (initializedName_copy->get_parent() == NULL) && (isSgType(initializedName_copy) == NULL) ) 
             { 
               initializedName_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_type
     int name_qualification_length_for_type_copy = p_name_qualification_length_for_type; 
     result->p_name_qualification_length_for_type = name_qualification_length_for_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_type
     bool type_elaboration_required_for_type_copy = p_type_elaboration_required_for_type; 
     result->p_type_elaboration_required_for_type = type_elaboration_required_for_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_type
     bool global_qualification_required_for_type_copy = p_global_qualification_required_for_type; 
     result->p_global_qualification_required_for_type = global_qualification_required_for_type_copy; 
  // Copy non-constructor parameter data member (access function): previous_instance_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for previous_instance
  // case: toBeCopied == COPY_DATA for previous_instance
     SgTemplateArgument* previous_instance_copy = p_previous_instance; 
     result->p_previous_instance = previous_instance_copy; 
  // Copy non-constructor parameter data member (access function): next_instance_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for next_instance
  // case: toBeCopied == COPY_DATA for next_instance
     SgTemplateArgument* next_instance_copy = p_next_instance; 
     result->p_next_instance = next_instance_copy; 
  // Copy non-constructor parameter data member (access function): is_pack_element_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_pack_element
  // case: toBeCopied == COPY_DATA for is_pack_element
     bool is_pack_element_copy = p_is_pack_element; 
     result->p_is_pack_element = is_pack_element_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)templateDeclaration
          if ( (templateDeclaration_copy != NULL) && (templateDeclaration_copy->get_parent() == NULL) && (isSgType(templateDeclaration_copy) == NULL) ) 
             { 
               templateDeclaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateArgument = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateArgument::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 12180 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDirectory::copy ( SgCopyHelp& help) const
   {
     SgDirectory* result = NULL;

  // printf ("Copy SgDirectory = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDirectory*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDirectory*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDirectory: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDirectory*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDirectory(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): fileList_copy
     SgFileList* fileList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fileList
          if (get_fileList() != NULL) 
             { 
               fileList_copy = static_cast<SgFileList*>(help.copyAst(get_fileList())); 
             } 
            else 
             { 
               fileList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_fileList != NULL) delete result->p_fileList; 
     result->p_fileList = fileList_copy; 
  // case: not a listType for (using conditionalToSetParent)fileList
          if ( (fileList_copy != NULL) && (fileList_copy->get_parent() == NULL) && (isSgType(fileList_copy) == NULL) ) 
             { 
               fileList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): directoryList_copy
     SgDirectoryList* directoryList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)directoryList
          if (get_directoryList() != NULL) 
             { 
               directoryList_copy = static_cast<SgDirectoryList*>(help.copyAst(get_directoryList())); 
             } 
            else 
             { 
               directoryList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_directoryList != NULL) delete result->p_directoryList; 
     result->p_directoryList = directoryList_copy; 
  // case: not a listType for (using conditionalToSetParent)directoryList
          if ( (directoryList_copy != NULL) && (directoryList_copy->get_parent() == NULL) && (isSgType(directoryList_copy) == NULL) ) 
             { 
               directoryList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDirectory = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDirectory::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 12423 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFileList::copy ( SgCopyHelp& help) const
   {
     SgFileList* result = NULL;

  // printf ("Copy SgFileList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFileList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFileList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFileList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFileList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFileList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_listOfFiles()
     SgFilePtrList listOfFiles_copy; 
  // case: listType for listOfFiles
  // case: listType (typeIsPointerToList == false) for listOfFiles
     SgFilePtrList::const_iterator source_listOfFiles_iterator = get_listOfFiles().begin(); 
     for ( /* empty by design */; source_listOfFiles_iterator != get_listOfFiles().end(); ++source_listOfFiles_iterator) 
        { 
          SgFile* source_list_element = *source_listOfFiles_iterator; 
          SgFile* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgFile*>(help.copyAst(*source_listOfFiles_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          listOfFiles_copy.push_back(copy_list_element); 
        } 
     result->p_listOfFiles = listOfFiles_copy; 
  // case: listType for listOfFiles
  // case: listType (typeIsPointerToList == false) for listOfFiles
     SgFilePtrList::const_iterator listOfFiles_iterator = result->get_listOfFiles().begin(); 
     for ( /* empty by design */; listOfFiles_iterator != result->get_listOfFiles().end(); ++listOfFiles_iterator) 
        { 
          SgFile* list_element = *listOfFiles_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFileList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFileList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 12654 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDirectoryList::copy ( SgCopyHelp& help) const
   {
     SgDirectoryList* result = NULL;

  // printf ("Copy SgDirectoryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDirectoryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDirectoryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDirectoryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDirectoryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDirectoryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_listOfDirectories()
     SgDirectoryPtrList listOfDirectories_copy; 
  // case: listType for listOfDirectories
  // case: listType (typeIsPointerToList == false) for listOfDirectories
     SgDirectoryPtrList::const_iterator source_listOfDirectories_iterator = get_listOfDirectories().begin(); 
     for ( /* empty by design */; source_listOfDirectories_iterator != get_listOfDirectories().end(); ++source_listOfDirectories_iterator) 
        { 
          SgDirectory* source_list_element = *source_listOfDirectories_iterator; 
          SgDirectory* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDirectory*>(help.copyAst(*source_listOfDirectories_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          listOfDirectories_copy.push_back(copy_list_element); 
        } 
     result->p_listOfDirectories = listOfDirectories_copy; 
  // case: listType for listOfDirectories
  // case: listType (typeIsPointerToList == false) for listOfDirectories
     SgDirectoryPtrList::const_iterator listOfDirectories_iterator = result->get_listOfDirectories().begin(); 
     for ( /* empty by design */; listOfDirectories_iterator != result->get_listOfDirectories().end(); ++listOfDirectories_iterator) 
        { 
          SgDirectory* list_element = *listOfDirectories_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDirectoryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDirectoryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 12869 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionParameterTypeList::copy ( SgCopyHelp& help) const
   {
     SgFunctionParameterTypeList* result = NULL;

  // printf ("Copy SgFunctionParameterTypeList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionParameterTypeList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionParameterTypeList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionParameterTypeList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionParameterTypeList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionParameterTypeList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_arguments
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for arguments
  // case: toBeCopied == COPY_DATA for arguments
     SgTypePtrList arguments_copy = p_arguments; 
     result->p_arguments = arguments_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionParameterTypeList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionParameterTypeList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 13058 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgQualifiedName::copy ( SgCopyHelp& help) const
   {
     SgQualifiedName* result = NULL;

  // printf ("Copy SgQualifiedName = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgQualifiedName*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgQualifiedName*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgQualifiedName: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgQualifiedName*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgQualifiedName(  scope_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgQualifiedName = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgQualifiedName::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 13246 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateArgumentList::copy ( SgCopyHelp& help) const
   {
     SgTemplateArgumentList* result = NULL;

  // printf ("Copy SgTemplateArgumentList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateArgumentList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateArgumentList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateArgumentList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateArgumentList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateArgumentList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): args_copy
     SgTemplateArgumentPtrList args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
     SgTemplateArgumentPtrList::const_iterator source_args_iterator = get_args().begin(); 
     for ( /* empty by design */; source_args_iterator != get_args().end(); ++source_args_iterator) 
        { 
          SgTemplateArgument* source_list_element = *source_args_iterator; 
          SgTemplateArgument* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgTemplateArgument*>(help.copyAst(*source_args_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          args_copy.push_back(copy_list_element); 
        } 
     result->p_args = args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
     SgTemplateArgumentPtrList::const_iterator args_iterator = result->get_args().begin(); 
     for ( /* empty by design */; args_iterator != result->get_args().end(); ++args_iterator) 
        { 
          SgTemplateArgument* list_element = *args_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateArgumentList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateArgumentList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 13461 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateParameterList::copy ( SgCopyHelp& help) const
   {
     SgTemplateParameterList* result = NULL;

  // printf ("Copy SgTemplateParameterList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateParameterList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateParameterList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateParameterList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateParameterList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateParameterList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): args_copy
     SgTemplateParameterPtrList args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
     SgTemplateParameterPtrList::const_iterator source_args_iterator = get_args().begin(); 
     for ( /* empty by design */; source_args_iterator != get_args().end(); ++source_args_iterator) 
        { 
          SgTemplateParameter* source_list_element = *source_args_iterator; 
          SgTemplateParameter* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgTemplateParameter*>(help.copyAst(*source_args_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          args_copy.push_back(copy_list_element); 
        } 
     result->p_args = args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
     SgTemplateParameterPtrList::const_iterator args_iterator = result->get_args().begin(); 
     for ( /* empty by design */; args_iterator != result->get_args().end(); ++args_iterator) 
        { 
          SgTemplateParameter* list_element = *args_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateParameterList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateParameterList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 13676 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraph::copy ( SgCopyHelp& help) const
   {
     SgGraph* result = NULL;

  // printf ("Copy SgGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGraph*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGraph*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGraph: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGraph*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGraph(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGraph::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 13936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncidenceDirectedGraph::copy ( SgCopyHelp& help) const
   {
     SgIncidenceDirectedGraph* result = NULL;

  // printf ("Copy SgIncidenceDirectedGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIncidenceDirectedGraph*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIncidenceDirectedGraph*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIncidenceDirectedGraph: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIncidenceDirectedGraph*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIncidenceDirectedGraph(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesOut()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesOut
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesOut
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesOut_copy = p_node_index_to_edge_multimap_edgesOut; 
     result->p_node_index_to_edge_multimap_edgesOut = node_index_to_edge_multimap_edgesOut_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesIn()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesIn
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesIn
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesIn_copy = p_node_index_to_edge_multimap_edgesIn; 
     result->p_node_index_to_edge_multimap_edgesIn = node_index_to_edge_multimap_edgesIn_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIncidenceDirectedGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIncidenceDirectedGraph::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 14206 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBidirectionalGraph::copy ( SgCopyHelp& help) const
   {
     SgBidirectionalGraph* result = NULL;

  // printf ("Copy SgBidirectionalGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBidirectionalGraph*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBidirectionalGraph*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBidirectionalGraph: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBidirectionalGraph*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBidirectionalGraph(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesOut()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesOut
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesOut
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesOut_copy = p_node_index_to_edge_multimap_edgesOut; 
     result->p_node_index_to_edge_multimap_edgesOut = node_index_to_edge_multimap_edgesOut_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesIn()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesIn
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesIn
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesIn_copy = p_node_index_to_edge_multimap_edgesIn; 
     result->p_node_index_to_edge_multimap_edgesIn = node_index_to_edge_multimap_edgesIn_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBidirectionalGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBidirectionalGraph::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 14476 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStringKeyedBidirectionalGraph::copy ( SgCopyHelp& help) const
   {
     SgStringKeyedBidirectionalGraph* result = NULL;

  // printf ("Copy SgStringKeyedBidirectionalGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStringKeyedBidirectionalGraph*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStringKeyedBidirectionalGraph*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStringKeyedBidirectionalGraph: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStringKeyedBidirectionalGraph*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStringKeyedBidirectionalGraph(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesOut()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesOut
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesOut
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesOut_copy = p_node_index_to_edge_multimap_edgesOut; 
     result->p_node_index_to_edge_multimap_edgesOut = node_index_to_edge_multimap_edgesOut_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesIn()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesIn
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesIn
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesIn_copy = p_node_index_to_edge_multimap_edgesIn; 
     result->p_node_index_to_edge_multimap_edgesIn = node_index_to_edge_multimap_edgesIn_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStringKeyedBidirectionalGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStringKeyedBidirectionalGraph::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 14746 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntKeyedBidirectionalGraph::copy ( SgCopyHelp& help) const
   {
     SgIntKeyedBidirectionalGraph* result = NULL;

  // printf ("Copy SgIntKeyedBidirectionalGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIntKeyedBidirectionalGraph*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIntKeyedBidirectionalGraph*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIntKeyedBidirectionalGraph: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIntKeyedBidirectionalGraph*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIntKeyedBidirectionalGraph(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesOut()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesOut
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesOut
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesOut_copy = p_node_index_to_edge_multimap_edgesOut; 
     result->p_node_index_to_edge_multimap_edgesOut = node_index_to_edge_multimap_edgesOut_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesIn()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesIn
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesIn
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesIn_copy = p_node_index_to_edge_multimap_edgesIn; 
     result->p_node_index_to_edge_multimap_edgesIn = node_index_to_edge_multimap_edgesIn_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIntKeyedBidirectionalGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIntKeyedBidirectionalGraph::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 15016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncidenceUndirectedGraph::copy ( SgCopyHelp& help) const
   {
     SgIncidenceUndirectedGraph* result = NULL;

  // printf ("Copy SgIncidenceUndirectedGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIncidenceUndirectedGraph*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIncidenceUndirectedGraph*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIncidenceUndirectedGraph: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIncidenceUndirectedGraph*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIncidenceUndirectedGraph(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIncidenceUndirectedGraph = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIncidenceUndirectedGraph::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 15276 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraphNode::copy ( SgCopyHelp& help) const
   {
     SgGraphNode* result = NULL;

  // printf ("Copy SgGraphNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGraphNode*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGraphNode*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGraphNode: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGraphNode*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGraphNode(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): SgNode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for SgNode
  // case: toBeCopied == COPY_DATA for SgNode
     SgNode* SgNode_copy = p_SgNode; 
     result->p_SgNode = SgNode_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGraphNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGraphNode::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 15501 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraphEdge::copy ( SgCopyHelp& help) const
   {
     SgGraphEdge* result = NULL;

  // printf ("Copy SgGraphEdge = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGraphEdge*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGraphEdge*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGraphEdge: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGraphEdge*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: node_A_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_A
  // case: toBeCopied == COPY_DATA for node_A
     SgGraphNode* node_A_copy = p_node_A; 
  // Copy constructor parameter data member: node_B_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_B
  // case: toBeCopied == COPY_DATA for node_B
     SgGraphNode* node_B_copy = p_node_B; 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGraphEdge(  node_A_copy, node_B_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGraphEdge = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGraphEdge::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 15729 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDirectedGraphEdge::copy ( SgCopyHelp& help) const
   {
     SgDirectedGraphEdge* result = NULL;

  // printf ("Copy SgDirectedGraphEdge = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDirectedGraphEdge*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDirectedGraphEdge*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDirectedGraphEdge: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDirectedGraphEdge*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: node_A_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_A
  // case: toBeCopied == COPY_DATA for node_A
     SgGraphNode* node_A_copy = p_node_A; 
  // Copy constructor parameter data member: node_B_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_B
  // case: toBeCopied == COPY_DATA for node_B
     SgGraphNode* node_B_copy = p_node_B; 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDirectedGraphEdge(  node_A_copy, node_B_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDirectedGraphEdge = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDirectedGraphEdge::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 15957 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUndirectedGraphEdge::copy ( SgCopyHelp& help) const
   {
     SgUndirectedGraphEdge* result = NULL;

  // printf ("Copy SgUndirectedGraphEdge = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUndirectedGraphEdge*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUndirectedGraphEdge*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUndirectedGraphEdge: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUndirectedGraphEdge*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: node_A_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_A
  // case: toBeCopied == COPY_DATA for node_A
     SgGraphNode* node_A_copy = p_node_A; 
  // Copy constructor parameter data member: node_B_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_B
  // case: toBeCopied == COPY_DATA for node_B
     SgGraphNode* node_B_copy = p_node_B; 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUndirectedGraphEdge(  node_A_copy, node_B_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUndirectedGraphEdge = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUndirectedGraphEdge::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 16185 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraphNodeList::copy ( SgCopyHelp& help) const
   {
     SgGraphNodeList* result = NULL;

  // printf ("Copy SgGraphNodeList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGraphNodeList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGraphNodeList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGraphNodeList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGraphNodeList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGraphNodeList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGraphNodeList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGraphNodeList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 16368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraphEdgeList::copy ( SgCopyHelp& help) const
   {
     SgGraphEdgeList* result = NULL;

  // printf ("Copy SgGraphEdgeList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGraphEdgeList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGraphEdgeList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGraphEdgeList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGraphEdgeList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGraphEdgeList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGraphEdgeList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGraphEdgeList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 16551 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeTable::copy ( SgCopyHelp& help) const
   {
     SgTypeTable* result = NULL;

  // printf ("Copy SgTypeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): type_table_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_table
  // case: toBeCopied == COPY_DATA for type_table
     SgSymbolTable* type_table_copy = p_type_table; 
     result->p_type_table = type_table_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 16740 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNameGroup::copy ( SgCopyHelp& help) const
   {
     SgNameGroup* result = NULL;

  // printf ("Copy SgNameGroup = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNameGroup*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNameGroup*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNameGroup: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNameGroup*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNameGroup(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): group_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for group_name
  // case: toBeCopied == COPY_DATA for group_name
     std::string group_name_copy = p_group_name; 
     result->p_group_name = group_name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_list
  // case: toBeCopied == COPY_DATA for name_list
     SgStringList name_list_copy = p_name_list; 
     result->p_name_list = name_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNameGroup = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNameGroup::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 16934 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDimensionObject::copy ( SgCopyHelp& help) const
   {
     SgDimensionObject* result = NULL;

  // printf ("Copy SgDimensionObject = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDimensionObject*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDimensionObject*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDimensionObject: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDimensionObject*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDimensionObject(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): array_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for array
  // case: toBeCopied == COPY_DATA for array
     SgInitializedName* array_copy = p_array; 
     result->p_array = array_copy; 
  // Copy non-constructor parameter data member (access function): shape_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shape
  // case: toBeCopied == COPY_DATA for shape
     SgExprListExp* shape_copy = p_shape; 
     result->p_shape = shape_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDimensionObject = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDimensionObject::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 17128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFormatItem::copy ( SgCopyHelp& help) const
   {
     SgFormatItem* result = NULL;

  // printf ("Copy SgFormatItem = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFormatItem*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFormatItem*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFormatItem: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFormatItem*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFormatItem(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): repeat_specification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for repeat_specification
  // case: toBeCopied == COPY_DATA for repeat_specification
     int repeat_specification_copy = p_repeat_specification; 
     result->p_repeat_specification = repeat_specification_copy; 
  // Copy non-constructor parameter data member (access function): data_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgExpression* data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (access function): format_item_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for format_item_list
  // case: toBeCopied == COPY_DATA for format_item_list
     SgFormatItemList* format_item_list_copy = p_format_item_list; 
     result->p_format_item_list = format_item_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFormatItem = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFormatItem::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 17327 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFormatItemList::copy ( SgCopyHelp& help) const
   {
     SgFormatItemList* result = NULL;

  // printf ("Copy SgFormatItemList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFormatItemList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFormatItemList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFormatItemList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFormatItemList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFormatItemList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_format_item_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for format_item_list
  // case: toBeCopied == COPY_DATA for format_item_list
     SgFormatItemPtrList format_item_list_copy = p_format_item_list; 
     result->p_format_item_list = format_item_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFormatItemList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFormatItemList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 17516 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDataStatementGroup::copy ( SgCopyHelp& help) const
   {
     SgDataStatementGroup* result = NULL;

  // printf ("Copy SgDataStatementGroup = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDataStatementGroup*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDataStatementGroup*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDataStatementGroup: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDataStatementGroup*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDataStatementGroup(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_object_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for object_list
  // case: toBeCopied == COPY_DATA for object_list
     SgDataStatementObjectPtrList object_list_copy = p_object_list; 
     result->p_object_list = object_list_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_value_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value_list
  // case: toBeCopied == COPY_DATA for value_list
     SgDataStatementValuePtrList value_list_copy = p_value_list; 
     result->p_value_list = value_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDataStatementGroup = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDataStatementGroup::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 17710 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDataStatementObject::copy ( SgCopyHelp& help) const
   {
     SgDataStatementObject* result = NULL;

  // printf ("Copy SgDataStatementObject = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDataStatementObject*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDataStatementObject*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDataStatementObject: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDataStatementObject*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDataStatementObject(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): variableReference_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variableReference_list
  // case: toBeCopied == COPY_DATA for variableReference_list
     SgExprListExp* variableReference_list_copy = p_variableReference_list; 
     result->p_variableReference_list = variableReference_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDataStatementObject = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDataStatementObject::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 17899 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncludeFile::copy ( SgCopyHelp& help) const
   {
     SgIncludeFile* result = NULL;

  // printf ("Copy SgIncludeFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIncludeFile*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIncludeFile*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIncludeFile: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIncludeFile*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for filename
  // case: toBeCopied == COPY_DATA for filename
     SgName filename_copy = p_filename; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIncludeFile(  filename_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isIncludedMoreThanOnce_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isIncludedMoreThanOnce
  // case: toBeCopied == COPY_DATA for isIncludedMoreThanOnce
     bool isIncludedMoreThanOnce_copy = p_isIncludedMoreThanOnce; 
     result->p_isIncludedMoreThanOnce = isIncludedMoreThanOnce_copy; 
  // Copy non-constructor parameter data member (access function): isPrimaryUse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPrimaryUse
  // case: toBeCopied == COPY_DATA for isPrimaryUse
     bool isPrimaryUse_copy = p_isPrimaryUse; 
     result->p_isPrimaryUse = isPrimaryUse_copy; 
  // Copy non-constructor parameter data member (access function): file_hash_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file_hash
  // case: toBeCopied == COPY_DATA for file_hash
     std::string file_hash_copy = p_file_hash; 
     result->p_file_hash = file_hash_copy; 
  // Copy non-constructor parameter data member (access function): name_used_in_include_directive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_used_in_include_directive
  // case: toBeCopied == COPY_DATA for name_used_in_include_directive
     SgName name_used_in_include_directive_copy = p_name_used_in_include_directive; 
     result->p_name_used_in_include_directive = name_used_in_include_directive_copy; 
  // Copy non-constructor parameter data member (access function): isSystemInclude_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSystemInclude
  // case: toBeCopied == COPY_DATA for isSystemInclude
     bool isSystemInclude_copy = p_isSystemInclude; 
     result->p_isSystemInclude = isSystemInclude_copy; 
  // Copy non-constructor parameter data member (access function): isPreinclude_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPreinclude
  // case: toBeCopied == COPY_DATA for isPreinclude
     bool isPreinclude_copy = p_isPreinclude; 
     result->p_isPreinclude = isPreinclude_copy; 
  // Copy non-constructor parameter data member (access function): requires_explict_path_for_unparsed_headers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_explict_path_for_unparsed_headers
  // case: toBeCopied == COPY_DATA for requires_explict_path_for_unparsed_headers
     bool requires_explict_path_for_unparsed_headers_copy = p_requires_explict_path_for_unparsed_headers; 
     result->p_requires_explict_path_for_unparsed_headers = requires_explict_path_for_unparsed_headers_copy; 
  // Copy non-constructor parameter data member (access function): can_be_supported_using_token_based_unparsing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for can_be_supported_using_token_based_unparsing
  // case: toBeCopied == COPY_DATA for can_be_supported_using_token_based_unparsing
     bool can_be_supported_using_token_based_unparsing_copy = p_can_be_supported_using_token_based_unparsing; 
     result->p_can_be_supported_using_token_based_unparsing = can_be_supported_using_token_based_unparsing_copy; 
  // Copy non-constructor parameter data member (access function): directory_prefix_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directory_prefix
  // case: toBeCopied == COPY_DATA for directory_prefix
     SgName directory_prefix_copy = p_directory_prefix; 
     result->p_directory_prefix = directory_prefix_copy; 
  // Copy non-constructor parameter data member (access function): name_without_path_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_without_path
  // case: toBeCopied == COPY_DATA for name_without_path
     SgName name_without_path_copy = p_name_without_path; 
     result->p_name_without_path = name_without_path_copy; 
  // Copy non-constructor parameter data member (access function): applicationRootDirectory_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for applicationRootDirectory
  // case: toBeCopied == COPY_DATA for applicationRootDirectory
     SgName applicationRootDirectory_copy = p_applicationRootDirectory; 
     result->p_applicationRootDirectory = applicationRootDirectory_copy; 
  // Copy non-constructor parameter data member (access function): will_be_unparsed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for will_be_unparsed
  // case: toBeCopied == COPY_DATA for will_be_unparsed
     bool will_be_unparsed_copy = p_will_be_unparsed; 
     result->p_will_be_unparsed = will_be_unparsed_copy; 
  // Copy non-constructor parameter data member (access function): isRoseSystemInclude_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRoseSystemInclude
  // case: toBeCopied == COPY_DATA for isRoseSystemInclude
     bool isRoseSystemInclude_copy = p_isRoseSystemInclude; 
     result->p_isRoseSystemInclude = isRoseSystemInclude_copy; 
  // Copy non-constructor parameter data member (access function): from_system_include_dir_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_system_include_dir
  // case: toBeCopied == COPY_DATA for from_system_include_dir
     bool from_system_include_dir_copy = p_from_system_include_dir; 
     result->p_from_system_include_dir = from_system_include_dir_copy; 
  // Copy non-constructor parameter data member (access function): preinclude_macros_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinclude_macros_only
  // case: toBeCopied == COPY_DATA for preinclude_macros_only
     bool preinclude_macros_only_copy = p_preinclude_macros_only; 
     result->p_preinclude_macros_only = preinclude_macros_only_copy; 
  // Copy non-constructor parameter data member (access function): isApplicationFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isApplicationFile
  // case: toBeCopied == COPY_DATA for isApplicationFile
     bool isApplicationFile_copy = p_isApplicationFile; 
     result->p_isApplicationFile = isApplicationFile_copy; 
  // Copy non-constructor parameter data member (access function): isRootSourceFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRootSourceFile
  // case: toBeCopied == COPY_DATA for isRootSourceFile
     bool isRootSourceFile_copy = p_isRootSourceFile; 
     result->p_isRootSourceFile = isRootSourceFile_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIncludeFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIncludeFile::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 18173 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDataStatementValue::copy ( SgCopyHelp& help) const
   {
     SgDataStatementValue* result = NULL;

  // printf ("Copy SgDataStatementValue = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDataStatementValue*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDataStatementValue*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDataStatementValue: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDataStatementValue*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: data_initialization_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_initialization_format
  // case: toBeCopied == COPY_DATA for data_initialization_format
     SgDataStatementValue::data_statement_value_enum data_initialization_format_copy = p_data_initialization_format; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDataStatementValue(  data_initialization_format_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): initializer_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for initializer_list
  // case: toBeCopied == COPY_DATA for initializer_list
     SgExprListExp* initializer_list_copy = p_initializer_list; 
     result->p_initializer_list = initializer_list_copy; 
  // Copy non-constructor parameter data member (access function): repeat_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for repeat_expression
  // case: toBeCopied == COPY_DATA for repeat_expression
     SgExpression* repeat_expression_copy = p_repeat_expression; 
     result->p_repeat_expression = repeat_expression_copy; 
  // Copy non-constructor parameter data member (access function): constant_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constant_expression
  // case: toBeCopied == COPY_DATA for constant_expression
     SgExpression* constant_expression_copy = p_constant_expression; 
     result->p_constant_expression = constant_expression_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDataStatementValue = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDataStatementValue::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 18377 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaImportStatementList::copy ( SgCopyHelp& help) const
   {
     SgJavaImportStatementList* result = NULL;

  // printf ("Copy SgJavaImportStatementList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaImportStatementList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaImportStatementList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaImportStatementList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaImportStatementList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaImportStatementList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_java_import_list()
     SgJavaImportStatementPtrList java_import_list_copy; 
  // case: listType for java_import_list
  // case: listType (typeIsPointerToList == false) for java_import_list
     SgJavaImportStatementPtrList::const_iterator source_java_import_list_iterator = get_java_import_list().begin(); 
     for ( /* empty by design */; source_java_import_list_iterator != get_java_import_list().end(); ++source_java_import_list_iterator) 
        { 
          SgJavaImportStatement* source_list_element = *source_java_import_list_iterator; 
          SgJavaImportStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgJavaImportStatement*>(help.copyAst(*source_java_import_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          java_import_list_copy.push_back(copy_list_element); 
        } 
     result->p_java_import_list = java_import_list_copy; 
  // case: listType for java_import_list
  // case: listType (typeIsPointerToList == false) for java_import_list
     SgJavaImportStatementPtrList::const_iterator java_import_list_iterator = result->get_java_import_list().begin(); 
     for ( /* empty by design */; java_import_list_iterator != result->get_java_import_list().end(); ++java_import_list_iterator) 
        { 
          SgJavaImportStatement* list_element = *java_import_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaImportStatementList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaImportStatementList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 18592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaClassDeclarationList::copy ( SgCopyHelp& help) const
   {
     SgJavaClassDeclarationList* result = NULL;

  // printf ("Copy SgJavaClassDeclarationList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaClassDeclarationList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaClassDeclarationList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaClassDeclarationList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaClassDeclarationList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaClassDeclarationList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (list access function): result->get_java_class_list()
     SgClassDeclarationPtrList java_class_list_copy; 
  // case: listType for java_class_list
  // case: listType (typeIsPointerToList == false) for java_class_list
     SgClassDeclarationPtrList::const_iterator source_java_class_list_iterator = get_java_class_list().begin(); 
     for ( /* empty by design */; source_java_class_list_iterator != get_java_class_list().end(); ++source_java_class_list_iterator) 
        { 
          SgClassDeclaration* source_list_element = *source_java_class_list_iterator; 
          SgClassDeclaration* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgClassDeclaration*>(help.copyAst(*source_java_class_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          java_class_list_copy.push_back(copy_list_element); 
        } 
     result->p_java_class_list = java_class_list_copy; 
  // case: listType for java_class_list
  // case: listType (typeIsPointerToList == false) for java_class_list
     SgClassDeclarationPtrList::const_iterator java_class_list_iterator = result->get_java_class_list().begin(); 
     for ( /* empty by design */; java_class_list_iterator != result->get_java_class_list().end(); ++java_class_list_iterator) 
        { 
          SgClassDeclaration* list_element = *java_class_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaClassDeclarationList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaClassDeclarationList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 18807 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgHeaderFileReport::copy ( SgCopyHelp& help) const
   {
     SgHeaderFileReport* result = NULL;

  // printf ("Copy SgHeaderFileReport = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgHeaderFileReport*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgHeaderFileReport*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgHeaderFileReport: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgHeaderFileReport*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgHeaderFileReport(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgHeaderFileReport = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgHeaderFileReport::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 18990 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgType::copy ( SgCopyHelp& help) const
   {
     SgType* result = NULL;

  // printf ("Copy SgType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 19259 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnknown::copy ( SgCopyHelp& help) const
   {
     SgTypeUnknown* result = NULL;

  // printf ("Copy SgTypeUnknown = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeUnknown*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeUnknown*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeUnknown: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeUnknown*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeUnknown(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): type_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_name
  // case: toBeCopied == COPY_DATA for type_name
     std::string type_name_copy = p_type_name; 
     result->p_type_name = type_name_copy; 
  // Copy non-constructor parameter data member (access function): has_type_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_type_name
  // case: toBeCopied == COPY_DATA for has_type_name
     bool has_type_name_copy = p_has_type_name; 
     result->p_has_type_name = has_type_name_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeUnknown = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeUnknown::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 19538 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeChar::copy ( SgCopyHelp& help) const
   {
     SgTypeChar* result = NULL;

  // printf ("Copy SgTypeChar = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeChar*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeChar*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeChar: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeChar*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeChar(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeChar = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeChar::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 19807 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedChar::copy ( SgCopyHelp& help) const
   {
     SgTypeSignedChar* result = NULL;

  // printf ("Copy SgTypeSignedChar = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeSignedChar*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeSignedChar*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeSignedChar: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeSignedChar*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeSignedChar(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeSignedChar = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeSignedChar::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 20076 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedChar::copy ( SgCopyHelp& help) const
   {
     SgTypeUnsignedChar* result = NULL;

  // printf ("Copy SgTypeUnsignedChar = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeUnsignedChar*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeUnsignedChar*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeUnsignedChar: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeUnsignedChar*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeUnsignedChar(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeUnsignedChar = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeUnsignedChar::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 20345 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeShort::copy ( SgCopyHelp& help) const
   {
     SgTypeShort* result = NULL;

  // printf ("Copy SgTypeShort = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeShort*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeShort*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeShort: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeShort*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeShort(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeShort = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeShort::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 20614 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedShort::copy ( SgCopyHelp& help) const
   {
     SgTypeSignedShort* result = NULL;

  // printf ("Copy SgTypeSignedShort = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeSignedShort*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeSignedShort*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeSignedShort: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeSignedShort*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeSignedShort(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeSignedShort = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeSignedShort::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 20883 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedShort::copy ( SgCopyHelp& help) const
   {
     SgTypeUnsignedShort* result = NULL;

  // printf ("Copy SgTypeUnsignedShort = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeUnsignedShort*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeUnsignedShort*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeUnsignedShort: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeUnsignedShort*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeUnsignedShort(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeUnsignedShort = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeUnsignedShort::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 21152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeInt::copy ( SgCopyHelp& help) const
   {
     SgTypeInt* result = NULL;

  // printf ("Copy SgTypeInt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeInt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeInt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeInt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeInt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: field_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for field_size
  // case: toBeCopied == COPY_DATA for field_size
     int field_size_copy = p_field_size; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeInt(  field_size_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeInt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeInt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 21426 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedInt::copy ( SgCopyHelp& help) const
   {
     SgTypeSignedInt* result = NULL;

  // printf ("Copy SgTypeSignedInt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeSignedInt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeSignedInt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeSignedInt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeSignedInt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeSignedInt(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeSignedInt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeSignedInt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 21695 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedInt::copy ( SgCopyHelp& help) const
   {
     SgTypeUnsignedInt* result = NULL;

  // printf ("Copy SgTypeUnsignedInt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeUnsignedInt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeUnsignedInt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeUnsignedInt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeUnsignedInt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeUnsignedInt(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeUnsignedInt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeUnsignedInt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 21964 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeLong::copy ( SgCopyHelp& help) const
   {
     SgTypeLong* result = NULL;

  // printf ("Copy SgTypeLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeLong*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeLong*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeLong: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeLong*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeLong(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeLong::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 22233 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedLong::copy ( SgCopyHelp& help) const
   {
     SgTypeSignedLong* result = NULL;

  // printf ("Copy SgTypeSignedLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeSignedLong*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeSignedLong*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeSignedLong: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeSignedLong*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeSignedLong(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeSignedLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeSignedLong::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 22502 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedLong::copy ( SgCopyHelp& help) const
   {
     SgTypeUnsignedLong* result = NULL;

  // printf ("Copy SgTypeUnsignedLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeUnsignedLong*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeUnsignedLong*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeUnsignedLong: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeUnsignedLong*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeUnsignedLong(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeUnsignedLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeUnsignedLong::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 22771 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeVoid::copy ( SgCopyHelp& help) const
   {
     SgTypeVoid* result = NULL;

  // printf ("Copy SgTypeVoid = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeVoid*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeVoid*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeVoid: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeVoid*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeVoid(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeVoid = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeVoid::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 23040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeGlobalVoid::copy ( SgCopyHelp& help) const
   {
     SgTypeGlobalVoid* result = NULL;

  // printf ("Copy SgTypeGlobalVoid = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeGlobalVoid*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeGlobalVoid*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeGlobalVoid: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeGlobalVoid*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeGlobalVoid(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeGlobalVoid = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeGlobalVoid::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 23309 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeWchar::copy ( SgCopyHelp& help) const
   {
     SgTypeWchar* result = NULL;

  // printf ("Copy SgTypeWchar = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeWchar*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeWchar*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeWchar: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeWchar*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeWchar(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeWchar = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeWchar::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 23578 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat::copy ( SgCopyHelp& help) const
   {
     SgTypeFloat* result = NULL;

  // printf ("Copy SgTypeFloat = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeFloat*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeFloat*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeFloat: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeFloat*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeFloat(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeFloat = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeFloat::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 23847 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeDouble::copy ( SgCopyHelp& help) const
   {
     SgTypeDouble* result = NULL;

  // printf ("Copy SgTypeDouble = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeDouble*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeDouble*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeDouble: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeDouble*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeDouble(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeDouble = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeDouble::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 24116 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeLongLong::copy ( SgCopyHelp& help) const
   {
     SgTypeLongLong* result = NULL;

  // printf ("Copy SgTypeLongLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeLongLong*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeLongLong*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeLongLong: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeLongLong*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeLongLong(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeLongLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeLongLong::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 24385 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedLongLong::copy ( SgCopyHelp& help) const
   {
     SgTypeSignedLongLong* result = NULL;

  // printf ("Copy SgTypeSignedLongLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeSignedLongLong*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeSignedLongLong*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeSignedLongLong: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeSignedLongLong*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeSignedLongLong(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeSignedLongLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeSignedLongLong::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 24654 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedLongLong::copy ( SgCopyHelp& help) const
   {
     SgTypeUnsignedLongLong* result = NULL;

  // printf ("Copy SgTypeUnsignedLongLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeUnsignedLongLong*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeUnsignedLongLong*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeUnsignedLongLong: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeUnsignedLongLong*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeUnsignedLongLong(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeUnsignedLongLong = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeUnsignedLongLong::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 24923 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSigned128bitInteger::copy ( SgCopyHelp& help) const
   {
     SgTypeSigned128bitInteger* result = NULL;

  // printf ("Copy SgTypeSigned128bitInteger = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeSigned128bitInteger*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeSigned128bitInteger*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeSigned128bitInteger: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeSigned128bitInteger*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeSigned128bitInteger(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeSigned128bitInteger = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeSigned128bitInteger::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 25192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsigned128bitInteger::copy ( SgCopyHelp& help) const
   {
     SgTypeUnsigned128bitInteger* result = NULL;

  // printf ("Copy SgTypeUnsigned128bitInteger = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeUnsigned128bitInteger*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeUnsigned128bitInteger*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeUnsigned128bitInteger: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeUnsigned128bitInteger*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeUnsigned128bitInteger(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeUnsigned128bitInteger = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeUnsigned128bitInteger::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 25461 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat80::copy ( SgCopyHelp& help) const
   {
     SgTypeFloat80* result = NULL;

  // printf ("Copy SgTypeFloat80 = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeFloat80*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeFloat80*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeFloat80: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeFloat80*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeFloat80(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeFloat80 = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeFloat80::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 25730 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeLongDouble::copy ( SgCopyHelp& help) const
   {
     SgTypeLongDouble* result = NULL;

  // printf ("Copy SgTypeLongDouble = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeLongDouble*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeLongDouble*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeLongDouble: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeLongDouble*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeLongDouble(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeLongDouble = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeLongDouble::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 25999 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeString::copy ( SgCopyHelp& help) const
   {
     SgTypeString* result = NULL;

  // printf ("Copy SgTypeString = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeString*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeString*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeString: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeString*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: lengthExpression_copy
     SgExpression* lengthExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lengthExpression
          if (get_lengthExpression() != NULL) 
             { 
               lengthExpression_copy = static_cast<SgExpression*>(help.copyAst(get_lengthExpression())); 
             } 
            else 
             { 
               lengthExpression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeString(  lengthExpression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)lengthExpression
          if ( (lengthExpression_copy != NULL) && (lengthExpression_copy->get_parent() == NULL) && (isSgType(lengthExpression_copy) == NULL) ) 
             { 
               lengthExpression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeString = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeString::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 26285 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeBool::copy ( SgCopyHelp& help) const
   {
     SgTypeBool* result = NULL;

  // printf ("Copy SgTypeBool = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeBool*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeBool*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeBool: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeBool*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeBool(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeBool = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeBool::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 26554 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPointerType::copy ( SgCopyHelp& help) const
   {
     SgPointerType* result = NULL;

  // printf ("Copy SgPointerType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPointerType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPointerType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPointerType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPointerType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != NULL) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPointerType(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
          if ( (base_type_copy != NULL) && (base_type_copy->get_parent() == NULL) && (isSgType(base_type_copy) == NULL) ) 
             { 
               base_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPointerType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPointerType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 26840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPointerMemberType::copy ( SgCopyHelp& help) const
   {
     SgPointerMemberType* result = NULL;

  // printf ("Copy SgPointerMemberType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPointerMemberType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPointerMemberType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPointerMemberType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPointerMemberType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != NULL) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = NULL; 
             } 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgType* class_type_copy = p_class_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPointerMemberType(  base_type_copy, class_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
          if ( (base_type_copy != NULL) && (base_type_copy->get_parent() == NULL) && (isSgType(base_type_copy) == NULL) ) 
             { 
               base_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPointerMemberType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPointerMemberType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 27130 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReferenceType::copy ( SgCopyHelp& help) const
   {
     SgReferenceType* result = NULL;

  // printf ("Copy SgReferenceType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgReferenceType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgReferenceType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgReferenceType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgReferenceType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgReferenceType(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgReferenceType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgReferenceType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 27404 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamedType::copy ( SgCopyHelp& help) const
   {
     SgNamedType* result = NULL;

  // printf ("Copy SgNamedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNamedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNamedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNamedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNamedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNamedType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNamedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNamedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 27700 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassType::copy ( SgCopyHelp& help) const
   {
     SgClassType* result = NULL;

  // printf ("Copy SgClassType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClassType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClassType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClassType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClassType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClassType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // Copy non-constructor parameter data member (access function): packed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packed
  // case: toBeCopied == COPY_DATA for packed
     bool packed_copy = p_packed; 
     result->p_packed = packed_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClassType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClassType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 28001 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaParameterType::copy ( SgCopyHelp& help) const
   {
     SgJavaParameterType* result = NULL;

  // printf ("Copy SgJavaParameterType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaParameterType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaParameterType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaParameterType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaParameterType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaParameterType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // Copy non-constructor parameter data member (access function): packed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packed
  // case: toBeCopied == COPY_DATA for packed
     bool packed_copy = p_packed; 
     result->p_packed = packed_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaParameterType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaParameterType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 28302 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialTableType::copy ( SgCopyHelp& help) const
   {
     SgJovialTableType* result = NULL;

  // printf ("Copy SgJovialTableType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialTableType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialTableType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialTableType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialTableType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialTableType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // Copy non-constructor parameter data member (access function): packed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packed
  // case: toBeCopied == COPY_DATA for packed
     bool packed_copy = p_packed; 
     result->p_packed = packed_copy; 
  // Copy non-constructor parameter data member (access function): base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
     result->p_base_type = base_type_copy; 
  // Copy non-constructor parameter data member (access function): dim_info_copy
     SgExprListExp* dim_info_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dim_info
          if (get_dim_info() != NULL) 
             { 
               dim_info_copy = static_cast<SgExprListExp*>(help.copyAst(get_dim_info())); 
             } 
            else 
             { 
               dim_info_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dim_info != NULL) delete result->p_dim_info; 
     result->p_dim_info = dim_info_copy; 
  // case: not a listType for (using conditionalToSetParent)dim_info
          if ( (dim_info_copy != NULL) && (dim_info_copy->get_parent() == NULL) && (isSgType(dim_info_copy) == NULL) ) 
             { 
               dim_info_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): rank_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rank
  // case: toBeCopied == COPY_DATA for rank
     int rank_copy = p_rank; 
     result->p_rank = rank_copy; 
  // Copy non-constructor parameter data member (access function): structure_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for structure_specifier
  // case: toBeCopied == COPY_DATA for structure_specifier
     SgJovialTableType::StructureSpecifier structure_specifier_copy = p_structure_specifier; 
     result->p_structure_specifier = structure_specifier_copy; 
  // Copy non-constructor parameter data member (access function): bits_per_entry_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bits_per_entry
  // case: toBeCopied == COPY_DATA for bits_per_entry
     unsigned int bits_per_entry_copy = p_bits_per_entry; 
     result->p_bits_per_entry = bits_per_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialTableType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialTableType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 28642 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumType::copy ( SgCopyHelp& help) const
   {
     SgEnumType* result = NULL;

  // printf ("Copy SgEnumType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEnumType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEnumType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEnumType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEnumType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEnumType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEnumType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEnumType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 28938 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypedefType::copy ( SgCopyHelp& help) const
   {
     SgTypedefType* result = NULL;

  // printf ("Copy SgTypedefType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypedefType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypedefType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypedefType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypedefType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: parent_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_scope
  // case: toBeCopied == COPY_DATA for parent_scope
     SgSymbol* parent_scope_copy = p_parent_scope; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypedefType(  declaration_copy, parent_scope_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypedefType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypedefType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 29238 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealType::copy ( SgCopyHelp& help) const
   {
     SgNonrealType* result = NULL;

  // printf ("Copy SgNonrealType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNonrealType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNonrealType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNonrealType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNonrealType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNonrealType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNonrealType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNonrealType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 29534 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaParameterizedType::copy ( SgCopyHelp& help) const
   {
     SgJavaParameterizedType* result = NULL;

  // printf ("Copy SgJavaParameterizedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaParameterizedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaParameterizedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaParameterizedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaParameterizedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: raw_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for raw_type
  // case: toBeCopied == COPY_DATA for raw_type
     SgNamedType* raw_type_copy = p_raw_type; 
  // Copy constructor parameter data member: type_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_list
  // case: toBeCopied == COPY_DATA for type_list
     SgTemplateParameterList* type_list_copy = p_type_list; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaParameterizedType(  declaration_copy, raw_type_copy, type_list_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaParameterizedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaParameterizedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 29838 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaQualifiedType::copy ( SgCopyHelp& help) const
   {
     SgJavaQualifiedType* result = NULL;

  // printf ("Copy SgJavaQualifiedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaQualifiedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaQualifiedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaQualifiedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaQualifiedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: parent_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_type
  // case: toBeCopied == COPY_DATA for parent_type
     SgNamedType * parent_type_copy = p_parent_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgNamedType * type_copy = p_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaQualifiedType(  declaration_copy, parent_type_copy, type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaQualifiedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaQualifiedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 30142 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaWildcardType::copy ( SgCopyHelp& help) const
   {
     SgJavaWildcardType* result = NULL;

  // printf ("Copy SgJavaWildcardType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaWildcardType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaWildcardType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaWildcardType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaWildcardType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: bound_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound_type
  // case: toBeCopied == COPY_DATA for bound_type
     SgType* bound_type_copy = p_bound_type; 
  // Copy constructor parameter data member: is_unbound_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_unbound
  // case: toBeCopied == COPY_DATA for is_unbound
     bool is_unbound_copy = p_is_unbound; 
  // Copy constructor parameter data member: has_extends_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_extends
  // case: toBeCopied == COPY_DATA for has_extends
     bool has_extends_copy = p_has_extends; 
  // Copy constructor parameter data member: has_super_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_super
  // case: toBeCopied == COPY_DATA for has_super
     bool has_super_copy = p_has_super; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaWildcardType(  declaration_copy, bound_type_copy, is_unbound_copy, has_extends_copy, has_super_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaWildcardType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaWildcardType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 30454 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskType::copy ( SgCopyHelp& help) const
   {
     SgAdaTaskType* result = NULL;

  // printf ("Copy SgAdaTaskType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTaskType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTaskType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTaskType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTaskType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTaskType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTaskType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTaskType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 30750 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedType::copy ( SgCopyHelp& help) const
   {
     SgAdaProtectedType* result = NULL;

  // printf ("Copy SgAdaProtectedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaProtectedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaProtectedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaProtectedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaProtectedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaProtectedType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaProtectedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaProtectedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 31046 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFormalType::copy ( SgCopyHelp& help) const
   {
     SgAdaFormalType* result = NULL;

  // printf ("Copy SgAdaFormalType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaFormalType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaFormalType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaFormalType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaFormalType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: formal_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for formal_type
  // case: toBeCopied == COPY_DATA for formal_type
     SgType* formal_type_copy = p_formal_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaFormalType(  declaration_copy, formal_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // Copy non-constructor parameter data member (access function): is_private_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_private
  // case: toBeCopied == COPY_DATA for is_private
     bool is_private_copy = p_is_private; 
     result->p_is_private = is_private_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaFormalType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaFormalType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 31351 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDiscriminatedType::copy ( SgCopyHelp& help) const
   {
     SgAdaDiscriminatedType* result = NULL;

  // printf ("Copy SgAdaDiscriminatedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaDiscriminatedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaDiscriminatedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaDiscriminatedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaDiscriminatedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaDiscriminatedType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaDiscriminatedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaDiscriminatedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 31647 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgScopedType::copy ( SgCopyHelp& help) const
   {
     SgScopedType* result = NULL;

  // printf ("Copy SgScopedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgScopedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgScopedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgScopedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgScopedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgScopedType(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgScopedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgScopedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 31943 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModifierType::copy ( SgCopyHelp& help) const
   {
     SgModifierType* result = NULL;

  // printf ("Copy SgModifierType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgModifierType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgModifierType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgModifierType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgModifierType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != NULL) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgModifierType(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_typeModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeModifier
  // case: toBeCopied == COPY_DATA for typeModifier
     SgTypeModifier typeModifier_copy = p_typeModifier; 
     result->p_typeModifier = typeModifier_copy; 
  // Copy non-constructor parameter data member (access function): frontend_type_reference_copy
     char* frontend_type_reference_copy; 
  // case: typeName == char* or char** for frontend_type_reference
     if (p_frontend_type_reference != NULL) 
          frontend_type_reference_copy = strdup(p_frontend_type_reference); 
     frontend_type_reference_copy = p_frontend_type_reference; 
     result->p_frontend_type_reference = frontend_type_reference_copy;
  // case: typeName == char* or char** for frontend_type_reference
  // case: not a listType for (using conditionalToSetParent)base_type
          if ( (base_type_copy != NULL) && (base_type_copy->get_parent() == NULL) && (isSgType(base_type_copy) == NULL) ) 
             { 
               base_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgModifierType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgModifierType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 32242 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionType::copy ( SgCopyHelp& help) const
   {
     SgFunctionType* result = NULL;

  // printf ("Copy SgFunctionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != NULL) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = NULL; 
             } 
  // Copy constructor parameter data member: has_ellipses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_ellipses
  // case: toBeCopied == COPY_DATA for has_ellipses
     bool has_ellipses_copy = p_has_ellipses; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionType(  return_type_copy, has_ellipses_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): orig_return_type_copy
     SgType* orig_return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)orig_return_type
          if (get_orig_return_type() != NULL) 
             { 
               orig_return_type_copy = static_cast<SgType*>(help.copyAst(get_orig_return_type())); 
             } 
            else 
             { 
               orig_return_type_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_orig_return_type != NULL) delete result->p_orig_return_type; 
     result->p_orig_return_type = orig_return_type_copy; 
  // case: not a listType for (using conditionalToSetParent)orig_return_type
          if ( (orig_return_type_copy != NULL) && (orig_return_type_copy->get_parent() == NULL) && (isSgType(orig_return_type_copy) == NULL) ) 
             { 
               orig_return_type_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): argument_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_list
  // case: toBeCopied == COPY_DATA for argument_list
     SgFunctionParameterTypeList* argument_list_copy = p_argument_list; 
     result->p_argument_list = argument_list_copy; 
  // case: not a listType for (using conditionalToSetParent)return_type
          if ( (return_type_copy != NULL) && (return_type_copy->get_parent() == NULL) && (isSgType(return_type_copy) == NULL) ) 
             { 
               return_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 32556 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMemberFunctionType::copy ( SgCopyHelp& help) const
   {
     SgMemberFunctionType* result = NULL;

  // printf ("Copy SgMemberFunctionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMemberFunctionType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMemberFunctionType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMemberFunctionType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMemberFunctionType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != NULL) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = NULL; 
             } 
  // Copy constructor parameter data member: has_ellipses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_ellipses
  // case: toBeCopied == COPY_DATA for has_ellipses
     bool has_ellipses_copy = p_has_ellipses; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgType* class_type_copy = p_class_type; 
  // Copy constructor parameter data member: mfunc_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mfunc_specifier
  // case: toBeCopied == COPY_DATA for mfunc_specifier
     unsigned int mfunc_specifier_copy = p_mfunc_specifier; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMemberFunctionType(  return_type_copy, has_ellipses_copy, class_type_copy, mfunc_specifier_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): orig_return_type_copy
     SgType* orig_return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)orig_return_type
          if (get_orig_return_type() != NULL) 
             { 
               orig_return_type_copy = static_cast<SgType*>(help.copyAst(get_orig_return_type())); 
             } 
            else 
             { 
               orig_return_type_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_orig_return_type != NULL) delete result->p_orig_return_type; 
     result->p_orig_return_type = orig_return_type_copy; 
  // case: not a listType for (using conditionalToSetParent)orig_return_type
          if ( (orig_return_type_copy != NULL) && (orig_return_type_copy->get_parent() == NULL) && (isSgType(orig_return_type_copy) == NULL) ) 
             { 
               orig_return_type_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): argument_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_list
  // case: toBeCopied == COPY_DATA for argument_list
     SgFunctionParameterTypeList* argument_list_copy = p_argument_list; 
     result->p_argument_list = argument_list_copy; 
  // case: not a listType for (using conditionalToSetParent)return_type
          if ( (return_type_copy != NULL) && (return_type_copy->get_parent() == NULL) && (isSgType(return_type_copy) == NULL) ) 
             { 
               return_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMemberFunctionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMemberFunctionType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 32878 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPartialFunctionType::copy ( SgCopyHelp& help) const
   {
     SgPartialFunctionType* result = NULL;

  // printf ("Copy SgPartialFunctionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPartialFunctionType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPartialFunctionType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPartialFunctionType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPartialFunctionType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != NULL) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = NULL; 
             } 
  // Copy constructor parameter data member: has_ellipses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_ellipses
  // case: toBeCopied == COPY_DATA for has_ellipses
     bool has_ellipses_copy = p_has_ellipses; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgType* class_type_copy = p_class_type; 
  // Copy constructor parameter data member: mfunc_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mfunc_specifier
  // case: toBeCopied == COPY_DATA for mfunc_specifier
     unsigned int mfunc_specifier_copy = p_mfunc_specifier; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPartialFunctionType(  return_type_copy, has_ellipses_copy, class_type_copy, mfunc_specifier_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): orig_return_type_copy
     SgType* orig_return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)orig_return_type
          if (get_orig_return_type() != NULL) 
             { 
               orig_return_type_copy = static_cast<SgType*>(help.copyAst(get_orig_return_type())); 
             } 
            else 
             { 
               orig_return_type_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_orig_return_type != NULL) delete result->p_orig_return_type; 
     result->p_orig_return_type = orig_return_type_copy; 
  // case: not a listType for (using conditionalToSetParent)orig_return_type
          if ( (orig_return_type_copy != NULL) && (orig_return_type_copy->get_parent() == NULL) && (isSgType(orig_return_type_copy) == NULL) ) 
             { 
               orig_return_type_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): argument_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_list
  // case: toBeCopied == COPY_DATA for argument_list
     SgFunctionParameterTypeList* argument_list_copy = p_argument_list; 
     result->p_argument_list = argument_list_copy; 
  // case: not a listType for (using conditionalToSetParent)return_type
          if ( (return_type_copy != NULL) && (return_type_copy->get_parent() == NULL) && (isSgType(return_type_copy) == NULL) ) 
             { 
               return_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPartialFunctionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPartialFunctionType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 33200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPartialFunctionModifierType::copy ( SgCopyHelp& help) const
   {
     SgPartialFunctionModifierType* result = NULL;

  // printf ("Copy SgPartialFunctionModifierType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPartialFunctionModifierType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPartialFunctionModifierType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPartialFunctionModifierType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPartialFunctionModifierType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != NULL) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = NULL; 
             } 
  // Copy constructor parameter data member: has_ellipses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_ellipses
  // case: toBeCopied == COPY_DATA for has_ellipses
     bool has_ellipses_copy = p_has_ellipses; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgType* class_type_copy = p_class_type; 
  // Copy constructor parameter data member: mfunc_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mfunc_specifier
  // case: toBeCopied == COPY_DATA for mfunc_specifier
     unsigned int mfunc_specifier_copy = p_mfunc_specifier; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPartialFunctionModifierType(  return_type_copy, has_ellipses_copy, class_type_copy, mfunc_specifier_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): orig_return_type_copy
     SgType* orig_return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)orig_return_type
          if (get_orig_return_type() != NULL) 
             { 
               orig_return_type_copy = static_cast<SgType*>(help.copyAst(get_orig_return_type())); 
             } 
            else 
             { 
               orig_return_type_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_orig_return_type != NULL) delete result->p_orig_return_type; 
     result->p_orig_return_type = orig_return_type_copy; 
  // case: not a listType for (using conditionalToSetParent)orig_return_type
          if ( (orig_return_type_copy != NULL) && (orig_return_type_copy->get_parent() == NULL) && (isSgType(orig_return_type_copy) == NULL) ) 
             { 
               orig_return_type_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): argument_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_list
  // case: toBeCopied == COPY_DATA for argument_list
     SgFunctionParameterTypeList* argument_list_copy = p_argument_list; 
     result->p_argument_list = argument_list_copy; 
  // case: not a listType for (using conditionalToSetParent)return_type
          if ( (return_type_copy != NULL) && (return_type_copy->get_parent() == NULL) && (isSgType(return_type_copy) == NULL) ) 
             { 
               return_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPartialFunctionModifierType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPartialFunctionModifierType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 33522 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgArrayType::copy ( SgCopyHelp& help) const
   {
     SgArrayType* result = NULL;

  // printf ("Copy SgArrayType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgArrayType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgArrayType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgArrayType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgArrayType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
  // Copy constructor parameter data member: index_copy
     SgExpression* index_copy; 
  // case: not a listType for (using conditionalToCopyVariable)index
          if (get_index() != NULL) 
             { 
               index_copy = static_cast<SgExpression*>(help.copyAst(get_index())); 
             } 
            else 
             { 
               index_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgArrayType(  base_type_copy, index_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): dim_info_copy
     SgExprListExp* dim_info_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dim_info
          if (get_dim_info() != NULL) 
             { 
               dim_info_copy = static_cast<SgExprListExp*>(help.copyAst(get_dim_info())); 
             } 
            else 
             { 
               dim_info_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dim_info != NULL) delete result->p_dim_info; 
     result->p_dim_info = dim_info_copy; 
  // case: not a listType for (using conditionalToSetParent)dim_info
          if ( (dim_info_copy != NULL) && (dim_info_copy->get_parent() == NULL) && (isSgType(dim_info_copy) == NULL) ) 
             { 
               dim_info_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): rank_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rank
  // case: toBeCopied == COPY_DATA for rank
     int rank_copy = p_rank; 
     result->p_rank = rank_copy; 
  // Copy non-constructor parameter data member (access function): number_of_elements_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for number_of_elements
  // case: toBeCopied == COPY_DATA for number_of_elements
     int number_of_elements_copy = p_number_of_elements; 
     result->p_number_of_elements = number_of_elements_copy; 
  // Copy non-constructor parameter data member (access function): is_variable_length_array_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_variable_length_array
  // case: toBeCopied == COPY_DATA for is_variable_length_array
     bool is_variable_length_array_copy = p_is_variable_length_array; 
     result->p_is_variable_length_array = is_variable_length_array_copy; 
  // case: not a listType for (using conditionalToSetParent)index
          if ( (index_copy != NULL) && (index_copy->get_parent() == NULL) && (isSgType(index_copy) == NULL) ) 
             { 
               index_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgArrayType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgArrayType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 33846 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeEllipse::copy ( SgCopyHelp& help) const
   {
     SgTypeEllipse* result = NULL;

  // printf ("Copy SgTypeEllipse = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeEllipse*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeEllipse*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeEllipse: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeEllipse*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeEllipse(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeEllipse = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeEllipse::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 34115 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateType::copy ( SgCopyHelp& help) const
   {
     SgTemplateType* result = NULL;

  // printf ("Copy SgTemplateType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateType(  name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): template_parameter_position_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_position
  // case: toBeCopied == COPY_DATA for template_parameter_position
     int template_parameter_position_copy = p_template_parameter_position; 
     result->p_template_parameter_position = template_parameter_position_copy; 
  // Copy non-constructor parameter data member (access function): template_parameter_depth_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_depth
  // case: toBeCopied == COPY_DATA for template_parameter_depth
     int template_parameter_depth_copy = p_template_parameter_depth; 
     result->p_template_parameter_depth = template_parameter_depth_copy; 
  // Copy non-constructor parameter data member (access function): class_type_copy
     SgType * class_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)class_type
          if (get_class_type() != NULL) 
             { 
               class_type_copy = static_cast<SgType *>(help.copyAst(get_class_type())); 
             } 
            else 
             { 
               class_type_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_class_type != NULL) delete result->p_class_type; 
     result->p_class_type = class_type_copy; 
  // case: not a listType for (using conditionalToSetParent)class_type
          if ( (class_type_copy != NULL) && (class_type_copy->get_parent() == NULL) && (isSgType(class_type_copy) == NULL) ) 
             { 
               class_type_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): parent_class_type_copy
     SgType * parent_class_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parent_class_type
          if (get_parent_class_type() != NULL) 
             { 
               parent_class_type_copy = static_cast<SgType *>(help.copyAst(get_parent_class_type())); 
             } 
            else 
             { 
               parent_class_type_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parent_class_type != NULL) delete result->p_parent_class_type; 
     result->p_parent_class_type = parent_class_type_copy; 
  // case: not a listType for (using conditionalToSetParent)parent_class_type
          if ( (parent_class_type_copy != NULL) && (parent_class_type_copy->get_parent() == NULL) && (isSgType(parent_class_type_copy) == NULL) ) 
             { 
               parent_class_type_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): template_parameter_copy
     SgTemplateParameter * template_parameter_copy; 
  // case: not a listType for (using conditionalToCopyVariable)template_parameter
          if (get_template_parameter() != NULL) 
             { 
               template_parameter_copy = static_cast<SgTemplateParameter *>(help.copyAst(get_template_parameter())); 
             } 
            else 
             { 
               template_parameter_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_template_parameter != NULL) delete result->p_template_parameter; 
     result->p_template_parameter = template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)template_parameter
          if ( (template_parameter_copy != NULL) && (template_parameter_copy->get_parent() == NULL) && (isSgType(template_parameter_copy) == NULL) ) 
             { 
               template_parameter_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_tpl_args()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tpl_args
  // case: toBeCopied == COPY_DATA for tpl_args
     SgTemplateArgumentPtrList tpl_args_copy = p_tpl_args; 
     result->p_tpl_args = tpl_args_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_part_spec_tpl_args()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for part_spec_tpl_args
  // case: toBeCopied == COPY_DATA for part_spec_tpl_args
     SgTemplateArgumentPtrList part_spec_tpl_args_copy = p_part_spec_tpl_args; 
     result->p_part_spec_tpl_args = part_spec_tpl_args_copy; 
  // Copy non-constructor parameter data member (access function): packed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packed
  // case: toBeCopied == COPY_DATA for packed
     bool packed_copy = p_packed; 
     result->p_packed = packed_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 34471 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgQualifiedNameType::copy ( SgCopyHelp& help) const
   {
     SgQualifiedNameType* result = NULL;

  // printf ("Copy SgQualifiedNameType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgQualifiedNameType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgQualifiedNameType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgQualifiedNameType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgQualifiedNameType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgQualifiedNameType(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgQualifiedNameType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgQualifiedNameType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 34750 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeComplex::copy ( SgCopyHelp& help) const
   {
     SgTypeComplex* result = NULL;

  // printf ("Copy SgTypeComplex = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeComplex*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeComplex*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeComplex: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeComplex*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeComplex(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeComplex = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeComplex::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 35024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeImaginary::copy ( SgCopyHelp& help) const
   {
     SgTypeImaginary* result = NULL;

  // printf ("Copy SgTypeImaginary = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeImaginary*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeImaginary*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeImaginary: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeImaginary*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeImaginary(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeImaginary = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeImaginary::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 35298 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeDefault::copy ( SgCopyHelp& help) const
   {
     SgTypeDefault* result = NULL;

  // printf ("Copy SgTypeDefault = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeDefault*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeDefault*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeDefault: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeDefault*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeDefault(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
     result->p_name = name_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeDefault = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeDefault::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 35572 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeCAFTeam::copy ( SgCopyHelp& help) const
   {
     SgTypeCAFTeam* result = NULL;

  // printf ("Copy SgTypeCAFTeam = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeCAFTeam*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeCAFTeam*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeCAFTeam: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeCAFTeam*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeCAFTeam(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeCAFTeam = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeCAFTeam::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 35841 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeCrayPointer::copy ( SgCopyHelp& help) const
   {
     SgTypeCrayPointer* result = NULL;

  // printf ("Copy SgTypeCrayPointer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeCrayPointer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeCrayPointer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeCrayPointer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeCrayPointer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeCrayPointer(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeCrayPointer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeCrayPointer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 36110 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeLabel::copy ( SgCopyHelp& help) const
   {
     SgTypeLabel* result = NULL;

  // printf ("Copy SgTypeLabel = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeLabel*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeLabel*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeLabel: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeLabel*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeLabel(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
     result->p_name = name_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeLabel = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeLabel::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 36384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaUnionType::copy ( SgCopyHelp& help) const
   {
     SgJavaUnionType* result = NULL;

  // printf ("Copy SgJavaUnionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaUnionType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaUnionType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaUnionType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaUnionType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaUnionType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): type_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_list
  // case: toBeCopied == COPY_DATA for type_list
     SgTypePtrList type_list_copy = p_type_list; 
     result->p_type_list = type_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaUnionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaUnionType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 36658 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRvalueReferenceType::copy ( SgCopyHelp& help) const
   {
     SgRvalueReferenceType* result = NULL;

  // printf ("Copy SgRvalueReferenceType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRvalueReferenceType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRvalueReferenceType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRvalueReferenceType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRvalueReferenceType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRvalueReferenceType(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRvalueReferenceType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRvalueReferenceType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 36932 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeNullptr::copy ( SgCopyHelp& help) const
   {
     SgTypeNullptr* result = NULL;

  // printf ("Copy SgTypeNullptr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeNullptr*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeNullptr*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeNullptr: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeNullptr*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeNullptr(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeNullptr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeNullptr::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 37201 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeclType::copy ( SgCopyHelp& help) const
   {
     SgDeclType* result = NULL;

  // printf ("Copy SgDeclType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDeclType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDeclType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDeclType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDeclType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_expression
  // case: toBeCopied == COPY_DATA for base_expression
     SgExpression* base_expression_copy = p_base_expression; 
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDeclType(  base_expression_copy, base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDeclType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDeclType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 37479 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeOfType::copy ( SgCopyHelp& help) const
   {
     SgTypeOfType* result = NULL;

  // printf ("Copy SgTypeOfType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeOfType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeOfType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeOfType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeOfType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_expression
  // case: toBeCopied == COPY_DATA for base_expression
     SgExpression* base_expression_copy = p_base_expression; 
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeOfType(  base_expression_copy, base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeOfType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeOfType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 37757 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeMatrix::copy ( SgCopyHelp& help) const
   {
     SgTypeMatrix* result = NULL;

  // printf ("Copy SgTypeMatrix = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeMatrix*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeMatrix*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeMatrix: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeMatrix*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeMatrix(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
     result->p_base_type = base_type_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeMatrix = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeMatrix::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 38031 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeTuple::copy ( SgCopyHelp& help) const
   {
     SgTypeTuple* result = NULL;

  // printf ("Copy SgTypeTuple = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeTuple*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeTuple*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeTuple: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeTuple*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeTuple(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_types
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for types
  // case: toBeCopied == COPY_DATA for types
     SgTypePtrList types_copy = p_types; 
     result->p_types = types_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeTuple = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeTuple::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 38305 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeChar16::copy ( SgCopyHelp& help) const
   {
     SgTypeChar16* result = NULL;

  // printf ("Copy SgTypeChar16 = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeChar16*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeChar16*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeChar16: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeChar16*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeChar16(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeChar16 = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeChar16::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 38574 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeChar32::copy ( SgCopyHelp& help) const
   {
     SgTypeChar32* result = NULL;

  // printf ("Copy SgTypeChar32 = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeChar32*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeChar32*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeChar32: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeChar32*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeChar32(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeChar32 = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeChar32::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 38843 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat128::copy ( SgCopyHelp& help) const
   {
     SgTypeFloat128* result = NULL;

  // printf ("Copy SgTypeFloat128 = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeFloat128*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeFloat128*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeFloat128: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeFloat128*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeFloat128(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeFloat128 = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeFloat128::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 39112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFixed::copy ( SgCopyHelp& help) const
   {
     SgTypeFixed* result = NULL;

  // printf ("Copy SgTypeFixed = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeFixed*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeFixed*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeFixed: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeFixed*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: scale_copy
     SgExpression* scale_copy; 
  // case: not a listType for (using conditionalToCopyVariable)scale
          if (get_scale() != NULL) 
             { 
               scale_copy = static_cast<SgExpression*>(help.copyAst(get_scale())); 
             } 
            else 
             { 
               scale_copy = NULL; 
             } 
  // Copy constructor parameter data member: fraction_copy
     SgExpression* fraction_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fraction
          if (get_fraction() != NULL) 
             { 
               fraction_copy = static_cast<SgExpression*>(help.copyAst(get_fraction())); 
             } 
            else 
             { 
               fraction_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeFixed(  scale_copy, fraction_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)scale
          if ( (scale_copy != NULL) && (scale_copy->get_parent() == NULL) && (isSgType(scale_copy) == NULL) ) 
             { 
               scale_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)fraction
          if ( (fraction_copy != NULL) && (fraction_copy->get_parent() == NULL) && (isSgType(fraction_copy) == NULL) ) 
             { 
               fraction_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeFixed = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeFixed::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 39414 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAutoType::copy ( SgCopyHelp& help) const
   {
     SgAutoType* result = NULL;

  // printf ("Copy SgAutoType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAutoType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAutoType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAutoType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAutoType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAutoType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAutoType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAutoType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 39683 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAccessType::copy ( SgCopyHelp& help) const
   {
     SgAdaAccessType* result = NULL;

  // printf ("Copy SgAdaAccessType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaAccessType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaAccessType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaAccessType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaAccessType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != NULL) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = NULL; 
             } 
  // Copy constructor parameter data member: is_general_access_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_general_access
  // case: toBeCopied == COPY_DATA for is_general_access
     bool is_general_access_copy = p_is_general_access; 
  // Copy constructor parameter data member: is_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_anonymous
  // case: toBeCopied == COPY_DATA for is_anonymous
     bool is_anonymous_copy = p_is_anonymous; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaAccessType(  base_type_copy, is_general_access_copy, is_anonymous_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
          if ( (base_type_copy != NULL) && (base_type_copy->get_parent() == NULL) && (isSgType(base_type_copy) == NULL) ) 
             { 
               base_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaAccessType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaAccessType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 39977 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaSubtype::copy ( SgCopyHelp& help) const
   {
     SgAdaSubtype* result = NULL;

  // printf ("Copy SgAdaSubtype = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaSubtype*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaSubtype*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaSubtype: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaSubtype*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != NULL) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = NULL; 
             } 
  // Copy constructor parameter data member: constraint_copy
     SgAdaTypeConstraint* constraint_copy; 
  // case: not a listType for (using conditionalToCopyVariable)constraint
          if (get_constraint() != NULL) 
             { 
               constraint_copy = static_cast<SgAdaTypeConstraint*>(help.copyAst(get_constraint())); 
             } 
            else 
             { 
               constraint_copy = NULL; 
             } 
  // Copy constructor parameter data member: fromRootType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fromRootType
  // case: toBeCopied == COPY_DATA for fromRootType
     bool fromRootType_copy = p_fromRootType; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaSubtype(  base_type_copy, constraint_copy, fromRootType_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
          if ( (base_type_copy != NULL) && (base_type_copy->get_parent() == NULL) && (isSgType(base_type_copy) == NULL) ) 
             { 
               base_type_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)constraint
          if ( (constraint_copy != NULL) && (constraint_copy->get_parent() == NULL) && (isSgType(constraint_copy) == NULL) ) 
             { 
               constraint_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaSubtype = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaSubtype::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 40283 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDiscreteType::copy ( SgCopyHelp& help) const
   {
     SgAdaDiscreteType* result = NULL;

  // printf ("Copy SgAdaDiscreteType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaDiscreteType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaDiscreteType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaDiscreteType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaDiscreteType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaDiscreteType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaDiscreteType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaDiscreteType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 40552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaModularType::copy ( SgCopyHelp& help) const
   {
     SgAdaModularType* result = NULL;

  // printf ("Copy SgAdaModularType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaModularType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaModularType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaModularType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaModularType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: modexpr_copy
     SgExpression* modexpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)modexpr
          if (get_modexpr() != NULL) 
             { 
               modexpr_copy = static_cast<SgExpression*>(help.copyAst(get_modexpr())); 
             } 
            else 
             { 
               modexpr_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaModularType(  modexpr_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)modexpr
          if ( (modexpr_copy != NULL) && (modexpr_copy->get_parent() == NULL) && (isSgType(modexpr_copy) == NULL) ) 
             { 
               modexpr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaModularType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaModularType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 40838 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDerivedType::copy ( SgCopyHelp& help) const
   {
     SgAdaDerivedType* result = NULL;

  // printf ("Copy SgAdaDerivedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaDerivedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaDerivedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaDerivedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaDerivedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != NULL) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaDerivedType(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
          if ( (base_type_copy != NULL) && (base_type_copy->get_parent() == NULL) && (isSgType(base_type_copy) == NULL) ) 
             { 
               base_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaDerivedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaDerivedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 41124 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaSubroutineType::copy ( SgCopyHelp& help) const
   {
     SgAdaSubroutineType* result = NULL;

  // printf ("Copy SgAdaSubroutineType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaSubroutineType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaSubroutineType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaSubroutineType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaSubroutineType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  // Copy constructor parameter data member: functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
  // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != NULL) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = NULL; 
             } 
  // Copy constructor parameter data member: is_protected_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_protected
  // case: toBeCopied == COPY_DATA for is_protected
     bool is_protected_copy = p_is_protected; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaSubroutineType(  parameterList_copy, functionParameterScope_copy, return_type_copy, is_protected_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)return_type
          if ( (return_type_copy != NULL) && (return_type_copy->get_parent() == NULL) && (isSgType(return_type_copy) == NULL) ) 
             { 
               return_type_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaSubroutineType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaSubroutineType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 41434 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialBitType::copy ( SgCopyHelp& help) const
   {
     SgJovialBitType* result = NULL;

  // printf ("Copy SgJovialBitType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialBitType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialBitType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialBitType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialBitType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     SgExpression* size_copy = p_size; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialBitType(  size_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialBitType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialBitType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 41708 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRangeType::copy ( SgCopyHelp& help) const
   {
     SgRangeType* result = NULL;

  // printf ("Copy SgRangeType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRangeType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRangeType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRangeType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRangeType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRangeType(  base_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != NULL) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != NULL) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
          if ( (type_kind_copy != NULL) && (type_kind_copy->get_parent() == NULL) && (isSgType(type_kind_copy) == NULL) ) 
             { 
               type_kind_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRangeType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRangeType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 41982 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLocatedNode::copy ( SgCopyHelp& help) const
   {
     SgLocatedNode* result = NULL;

  // printf ("Copy SgLocatedNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLocatedNode*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLocatedNode*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLocatedNode: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLocatedNode*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLocatedNode(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLocatedNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLocatedNode::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 42227 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgToken::copy ( SgCopyHelp& help) const
   {
     SgToken* result = NULL;

  // printf ("Copy SgToken = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgToken*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgToken*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgToken: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgToken*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgToken(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgToken = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgToken::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 42472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLocatedNodeSupport::copy ( SgCopyHelp& help) const
   {
     SgLocatedNodeSupport* result = NULL;

  // printf ("Copy SgLocatedNodeSupport = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLocatedNodeSupport*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLocatedNodeSupport*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLocatedNodeSupport: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLocatedNodeSupport*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLocatedNodeSupport(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLocatedNodeSupport = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLocatedNodeSupport::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 42717 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCommonBlockObject::copy ( SgCopyHelp& help) const
   {
     SgCommonBlockObject* result = NULL;

  // printf ("Copy SgCommonBlockObject = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCommonBlockObject*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCommonBlockObject*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCommonBlockObject: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCommonBlockObject*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCommonBlockObject(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): block_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for block_name
  // case: toBeCopied == COPY_DATA for block_name
     std::string block_name_copy = p_block_name; 
     result->p_block_name = block_name_copy; 
  // Copy non-constructor parameter data member (access function): variable_reference_list_copy
     SgExprListExp* variable_reference_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variable_reference_list
          if (get_variable_reference_list() != NULL) 
             { 
               variable_reference_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_variable_reference_list())); 
             } 
            else 
             { 
               variable_reference_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_variable_reference_list != NULL) delete result->p_variable_reference_list; 
     result->p_variable_reference_list = variable_reference_list_copy; 
  // case: not a listType for (using conditionalToSetParent)variable_reference_list
          if ( (variable_reference_list_copy != NULL) && (variable_reference_list_copy->get_parent() == NULL) && (isSgType(variable_reference_list_copy) == NULL) ) 
             { 
               variable_reference_list_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCommonBlockObject = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCommonBlockObject::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 42986 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInitializedName::copy ( SgCopyHelp& help) const
   {
     SgInitializedName* result = NULL;

  // printf ("Copy SgInitializedName = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgInitializedName*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgInitializedName*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgInitializedName: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgInitializedName*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: typeptr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeptr
  // case: toBeCopied == COPY_DATA for typeptr
     SgType* typeptr_copy = p_typeptr; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgInitializedName(  startOfConstruct_copy, name_copy, typeptr_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): microsoft_uuid_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for microsoft_uuid_string
  // case: toBeCopied == COPY_DATA for microsoft_uuid_string
     std::string microsoft_uuid_string_copy = p_microsoft_uuid_string; 
     result->p_microsoft_uuid_string = microsoft_uuid_string_copy; 
  // Copy non-constructor parameter data member (access function): initptr_copy
     SgInitializer* initptr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initptr
          if (get_initptr() != NULL) 
             { 
               initptr_copy = static_cast<SgInitializer*>(help.copyAst(get_initptr())); 
             } 
            else 
             { 
               initptr_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_initptr != NULL) delete result->p_initptr; 
     result->p_initptr = initptr_copy; 
  // case: not a listType for (using conditionalToSetParent)initptr
          if ( (initptr_copy != NULL) && (initptr_copy->get_parent() == NULL) && (isSgType(initptr_copy) == NULL) ) 
             { 
               initptr_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): prev_decl_item_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prev_decl_item
  // case: toBeCopied == COPY_DATA for prev_decl_item
     SgInitializedName* prev_decl_item_copy = p_prev_decl_item; 
     result->p_prev_decl_item = prev_decl_item_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_is_initializer
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_initializer
  // case: toBeCopied == COPY_DATA for is_initializer
     bool is_initializer_copy = p_is_initializer; 
     result->p_is_initializer = is_initializer_copy; 
  // Copy non-constructor parameter data member (access function): declptr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declptr
  // case: toBeCopied == COPY_DATA for declptr
     SgDeclarationStatement* declptr_copy = p_declptr; 
     result->p_declptr = declptr_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): preinitialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinitialization
  // case: toBeCopied == COPY_DATA for preinitialization
     SgInitializedName::preinitialization_enum preinitialization_copy = p_preinitialization; 
     result->p_preinitialization = preinitialization_copy; 
  // Copy non-constructor parameter data member (access function): register_name_code_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for register_name_code
  // case: toBeCopied == COPY_DATA for register_name_code
     SgInitializedName::asm_register_name_enum register_name_code_copy = p_register_name_code; 
     result->p_register_name_code = register_name_code_copy; 
  // Copy non-constructor parameter data member (access function): excess_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for excess_specifier
  // case: toBeCopied == COPY_DATA for excess_specifier
     SgInitializedName::excess_specifier_enum excess_specifier_copy = p_excess_specifier; 
     result->p_excess_specifier = excess_specifier_copy; 
  // Copy non-constructor parameter data member (access function): register_name_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for register_name_string
  // case: toBeCopied == COPY_DATA for register_name_string
     std::string register_name_string_copy = p_register_name_string; 
     result->p_register_name_string = register_name_string_copy; 
  // Copy non-constructor parameter data member (access function): requiresGlobalNameQualificationOnType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (access function): shapeDeferred_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shapeDeferred
  // case: toBeCopied == COPY_DATA for shapeDeferred
     bool shapeDeferred_copy = p_shapeDeferred; 
     result->p_shapeDeferred = shapeDeferred_copy; 
  // Copy non-constructor parameter data member (access function): initializationDeferred_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for initializationDeferred
  // case: toBeCopied == COPY_DATA for initializationDeferred
     bool initializationDeferred_copy = p_initializationDeferred; 
     result->p_initializationDeferred = initializationDeferred_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_modifierVector
  // case: toBeCopied == COPY_DATA for gnu_attribute_modifierVector
     SgBitVector gnu_attribute_modifierVector_copy = p_gnu_attribute_modifierVector; 
     result->p_gnu_attribute_modifierVector = gnu_attribute_modifierVector_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_initialization_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_initialization_priority
  // case: toBeCopied == COPY_DATA for gnu_attribute_initialization_priority
     unsigned long int gnu_attribute_initialization_priority_copy = p_gnu_attribute_initialization_priority; 
     result->p_gnu_attribute_initialization_priority = gnu_attribute_initialization_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_named_weak_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_named_weak_reference
  // case: toBeCopied == COPY_DATA for gnu_attribute_named_weak_reference
     std::string gnu_attribute_named_weak_reference_copy = p_gnu_attribute_named_weak_reference; 
     result->p_gnu_attribute_named_weak_reference = gnu_attribute_named_weak_reference_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_named_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_named_alias
  // case: toBeCopied == COPY_DATA for gnu_attribute_named_alias
     std::string gnu_attribute_named_alias_copy = p_gnu_attribute_named_alias; 
     result->p_gnu_attribute_named_alias = gnu_attribute_named_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_cleanup_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_cleanup_function
  // case: toBeCopied == COPY_DATA for gnu_attribute_cleanup_function
     std::string gnu_attribute_cleanup_function_copy = p_gnu_attribute_cleanup_function; 
     result->p_gnu_attribute_cleanup_function = gnu_attribute_cleanup_function_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_section_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_section_name
  // case: toBeCopied == COPY_DATA for gnu_attribute_section_name
     std::string gnu_attribute_section_name_copy = p_gnu_attribute_section_name; 
     result->p_gnu_attribute_section_name = gnu_attribute_section_name_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_alignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_alignment
  // case: toBeCopied == COPY_DATA for gnu_attribute_alignment
     int gnu_attribute_alignment_copy = p_gnu_attribute_alignment; 
     result->p_gnu_attribute_alignment = gnu_attribute_alignment_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_visability
  // case: toBeCopied == COPY_DATA for gnu_attribute_visability
     SgDeclarationModifier::gnu_declaration_visability_enum gnu_attribute_visability_copy = p_gnu_attribute_visability; 
     result->p_gnu_attribute_visability = gnu_attribute_visability_copy; 
  // Copy non-constructor parameter data member (access function): protected_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for protected_declaration
  // case: toBeCopied == COPY_DATA for protected_declaration
     bool protected_declaration_copy = p_protected_declaration; 
     result->p_protected_declaration = protected_declaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_type
     int name_qualification_length_for_type_copy = p_name_qualification_length_for_type; 
     result->p_name_qualification_length_for_type = name_qualification_length_for_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_type
     bool type_elaboration_required_for_type_copy = p_type_elaboration_required_for_type; 
     result->p_type_elaboration_required_for_type = type_elaboration_required_for_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_type
     bool global_qualification_required_for_type_copy = p_global_qualification_required_for_type; 
     result->p_global_qualification_required_for_type = global_qualification_required_for_type_copy; 
  // Copy non-constructor parameter data member (access function): hasArrayTypeWithEmptyBracketSyntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasArrayTypeWithEmptyBracketSyntax
  // case: toBeCopied == COPY_DATA for hasArrayTypeWithEmptyBracketSyntax
     bool hasArrayTypeWithEmptyBracketSyntax_copy = p_hasArrayTypeWithEmptyBracketSyntax; 
     result->p_hasArrayTypeWithEmptyBracketSyntax = hasArrayTypeWithEmptyBracketSyntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Alignas_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Alignas_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Alignas_keyword
     bool using_C11_Alignas_keyword_copy = p_using_C11_Alignas_keyword; 
     result->p_using_C11_Alignas_keyword = using_C11_Alignas_keyword_copy; 
  // Copy non-constructor parameter data member (access function): constant_or_type_argument_for_Alignas_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constant_or_type_argument_for_Alignas_keyword
  // case: toBeCopied == COPY_DATA for constant_or_type_argument_for_Alignas_keyword
     SgNode* constant_or_type_argument_for_Alignas_keyword_copy = p_constant_or_type_argument_for_Alignas_keyword; 
     result->p_constant_or_type_argument_for_Alignas_keyword = constant_or_type_argument_for_Alignas_keyword_copy; 
  // Copy non-constructor parameter data member (access function): using_auto_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_auto_keyword
  // case: toBeCopied == COPY_DATA for using_auto_keyword
     bool using_auto_keyword_copy = p_using_auto_keyword; 
     result->p_using_auto_keyword = using_auto_keyword_copy; 
  // Copy non-constructor parameter data member (access function): auto_decltype_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for auto_decltype
  // case: toBeCopied == COPY_DATA for auto_decltype
     SgType * auto_decltype_copy = p_auto_decltype; 
     result->p_auto_decltype = auto_decltype_copy; 
  // Copy non-constructor parameter data member (access function): using_device_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_device_keyword
  // case: toBeCopied == COPY_DATA for using_device_keyword
     bool using_device_keyword_copy = p_using_device_keyword; 
     result->p_using_device_keyword = using_device_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // Copy non-constructor parameter data member (access function): using_assignment_copy_constructor_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_assignment_copy_constructor_syntax
  // case: toBeCopied == COPY_DATA for using_assignment_copy_constructor_syntax
     bool using_assignment_copy_constructor_syntax_copy = p_using_assignment_copy_constructor_syntax; 
     result->p_using_assignment_copy_constructor_syntax = using_assignment_copy_constructor_syntax_copy; 
  // Copy non-constructor parameter data member (access function): needs_definitions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for needs_definitions
  // case: toBeCopied == COPY_DATA for needs_definitions
     bool needs_definitions_copy = p_needs_definitions; 
     result->p_needs_definitions = needs_definitions_copy; 
  // Copy non-constructor parameter data member (access function): is_parameter_pack_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_parameter_pack
  // case: toBeCopied == COPY_DATA for is_parameter_pack
     bool is_parameter_pack_copy = p_is_parameter_pack; 
     result->p_is_parameter_pack = is_parameter_pack_copy; 
  // Copy non-constructor parameter data member (access function): is_pack_element_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_pack_element
  // case: toBeCopied == COPY_DATA for is_pack_element
     bool is_pack_element_copy = p_is_pack_element; 
     result->p_is_pack_element = is_pack_element_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgInitializedName = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgInitializedName::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 43448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInterfaceBody::copy ( SgCopyHelp& help) const
   {
     SgInterfaceBody* result = NULL;

  // printf ("Copy SgInterfaceBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgInterfaceBody*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgInterfaceBody*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgInterfaceBody: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgInterfaceBody*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: function_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for function_name
  // case: toBeCopied == COPY_DATA for function_name
     SgName function_name_copy = p_function_name; 
  // Copy constructor parameter data member: functionDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionDeclaration
  // case: toBeCopied == COPY_DATA for functionDeclaration
     SgFunctionDeclaration* functionDeclaration_copy = p_functionDeclaration; 
  // Copy constructor parameter data member: use_function_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_function_name
  // case: toBeCopied == COPY_DATA for use_function_name
     bool use_function_name_copy = p_use_function_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgInterfaceBody(  startOfConstruct_copy, function_name_copy, functionDeclaration_copy, use_function_name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgInterfaceBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgInterfaceBody::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 43705 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgHeaderFileBody::copy ( SgCopyHelp& help) const
   {
     SgHeaderFileBody* result = NULL;

  // printf ("Copy SgHeaderFileBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgHeaderFileBody*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgHeaderFileBody*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgHeaderFileBody: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgHeaderFileBody*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgHeaderFileBody(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): include_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for include_file
  // case: toBeCopied == COPY_DATA for include_file
     SgSourceFile* include_file_copy = p_include_file; 
     result->p_include_file = include_file_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgHeaderFileBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgHeaderFileBody::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 43955 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRenamePair::copy ( SgCopyHelp& help) const
   {
     SgRenamePair* result = NULL;

  // printf ("Copy SgRenamePair = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRenamePair*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRenamePair*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRenamePair: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRenamePair*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: local_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for local_name
  // case: toBeCopied == COPY_DATA for local_name
     SgName local_name_copy = p_local_name; 
  // Copy constructor parameter data member: use_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_name
  // case: toBeCopied == COPY_DATA for use_name
     SgName use_name_copy = p_use_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRenamePair(  startOfConstruct_copy, local_name_copy, use_name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRenamePair = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRenamePair::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 44208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaMemberValuePair::copy ( SgCopyHelp& help) const
   {
     SgJavaMemberValuePair* result = NULL;

  // printf ("Copy SgJavaMemberValuePair = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaMemberValuePair*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaMemberValuePair*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaMemberValuePair: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaMemberValuePair*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaMemberValuePair(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != NULL) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_value != NULL) delete result->p_value; 
     result->p_value = value_copy; 
  // case: not a listType for (using conditionalToSetParent)value
          if ( (value_copy != NULL) && (value_copy->get_parent() == NULL) && (isSgType(value_copy) == NULL) ) 
             { 
               value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaMemberValuePair = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaMemberValuePair::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 44477 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpClause::copy ( SgCopyHelp& help) const
   {
     SgOmpClause* result = NULL;

  // printf ("Copy SgOmpClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpClause(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 44722 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpNowaitClause::copy ( SgCopyHelp& help) const
   {
     SgOmpNowaitClause* result = NULL;

  // printf ("Copy SgOmpNowaitClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpNowaitClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpNowaitClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpNowaitClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpNowaitClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpNowaitClause(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpNowaitClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpNowaitClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 44967 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpBeginClause::copy ( SgCopyHelp& help) const
   {
     SgOmpBeginClause* result = NULL;

  // printf ("Copy SgOmpBeginClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpBeginClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpBeginClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpBeginClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpBeginClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpBeginClause(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpBeginClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpBeginClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 45212 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpEndClause::copy ( SgCopyHelp& help) const
   {
     SgOmpEndClause* result = NULL;

  // printf ("Copy SgOmpEndClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpEndClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpEndClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpEndClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpEndClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpEndClause(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpEndClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpEndClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 45457 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpUntiedClause::copy ( SgCopyHelp& help) const
   {
     SgOmpUntiedClause* result = NULL;

  // printf ("Copy SgOmpUntiedClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpUntiedClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpUntiedClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpUntiedClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpUntiedClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpUntiedClause(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpUntiedClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpUntiedClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 45702 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDefaultClause::copy ( SgCopyHelp& help) const
   {
     SgOmpDefaultClause* result = NULL;

  // printf ("Copy SgOmpDefaultClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpDefaultClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpDefaultClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpDefaultClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpDefaultClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: data_sharing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_sharing
  // case: toBeCopied == COPY_DATA for data_sharing
     SgOmpClause::omp_default_option_enum data_sharing_copy = p_data_sharing; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpDefaultClause(  startOfConstruct_copy, data_sharing_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpDefaultClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpDefaultClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 45951 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpAtomicClause::copy ( SgCopyHelp& help) const
   {
     SgOmpAtomicClause* result = NULL;

  // printf ("Copy SgOmpAtomicClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpAtomicClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpAtomicClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpAtomicClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpAtomicClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: atomicity_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for atomicity
  // case: toBeCopied == COPY_DATA for atomicity
     SgOmpClause::omp_atomic_clause_enum atomicity_copy = p_atomicity; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpAtomicClause(  startOfConstruct_copy, atomicity_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpAtomicClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpAtomicClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 46200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpProcBindClause::copy ( SgCopyHelp& help) const
   {
     SgOmpProcBindClause* result = NULL;

  // printf ("Copy SgOmpProcBindClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpProcBindClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpProcBindClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpProcBindClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpProcBindClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: policy_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for policy
  // case: toBeCopied == COPY_DATA for policy
     SgOmpClause::omp_proc_bind_policy_enum policy_copy = p_policy; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpProcBindClause(  startOfConstruct_copy, policy_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpProcBindClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpProcBindClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 46449 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpExpressionClause::copy ( SgCopyHelp& help) const
   {
     SgOmpExpressionClause* result = NULL;

  // printf ("Copy SgOmpExpressionClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpExpressionClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpExpressionClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpExpressionClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpExpressionClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpExpressionClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpExpressionClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpExpressionClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 46710 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpOrderedClause::copy ( SgCopyHelp& help) const
   {
     SgOmpOrderedClause* result = NULL;

  // printf ("Copy SgOmpOrderedClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpOrderedClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpOrderedClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpOrderedClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpOrderedClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpOrderedClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpOrderedClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpOrderedClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 46971 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpCollapseClause::copy ( SgCopyHelp& help) const
   {
     SgOmpCollapseClause* result = NULL;

  // printf ("Copy SgOmpCollapseClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpCollapseClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpCollapseClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpCollapseClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpCollapseClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpCollapseClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpCollapseClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpCollapseClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 47232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpIfClause::copy ( SgCopyHelp& help) const
   {
     SgOmpIfClause* result = NULL;

  // printf ("Copy SgOmpIfClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpIfClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpIfClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpIfClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpIfClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpIfClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpIfClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpIfClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 47493 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpNumThreadsClause::copy ( SgCopyHelp& help) const
   {
     SgOmpNumThreadsClause* result = NULL;

  // printf ("Copy SgOmpNumThreadsClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpNumThreadsClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpNumThreadsClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpNumThreadsClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpNumThreadsClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpNumThreadsClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpNumThreadsClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpNumThreadsClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 47754 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDeviceClause::copy ( SgCopyHelp& help) const
   {
     SgOmpDeviceClause* result = NULL;

  // printf ("Copy SgOmpDeviceClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpDeviceClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpDeviceClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpDeviceClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpDeviceClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpDeviceClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpDeviceClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpDeviceClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 48015 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSafelenClause::copy ( SgCopyHelp& help) const
   {
     SgOmpSafelenClause* result = NULL;

  // printf ("Copy SgOmpSafelenClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpSafelenClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpSafelenClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpSafelenClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpSafelenClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpSafelenClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpSafelenClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpSafelenClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 48276 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSimdlenClause::copy ( SgCopyHelp& help) const
   {
     SgOmpSimdlenClause* result = NULL;

  // printf ("Copy SgOmpSimdlenClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpSimdlenClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpSimdlenClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpSimdlenClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpSimdlenClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpSimdlenClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpSimdlenClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpSimdlenClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 48537 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpFinalClause::copy ( SgCopyHelp& help) const
   {
     SgOmpFinalClause* result = NULL;

  // printf ("Copy SgOmpFinalClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpFinalClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpFinalClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpFinalClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpFinalClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpFinalClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpFinalClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpFinalClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 48798 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpPriorityClause::copy ( SgCopyHelp& help) const
   {
     SgOmpPriorityClause* result = NULL;

  // printf ("Copy SgOmpPriorityClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpPriorityClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpPriorityClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpPriorityClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpPriorityClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpPriorityClause(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpPriorityClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpPriorityClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 49059 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpInbranchClause::copy ( SgCopyHelp& help) const
   {
     SgOmpInbranchClause* result = NULL;

  // printf ("Copy SgOmpInbranchClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpInbranchClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpInbranchClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpInbranchClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpInbranchClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpInbranchClause(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpInbranchClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpInbranchClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 49304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpNotinbranchClause::copy ( SgCopyHelp& help) const
   {
     SgOmpNotinbranchClause* result = NULL;

  // printf ("Copy SgOmpNotinbranchClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpNotinbranchClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpNotinbranchClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpNotinbranchClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpNotinbranchClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpNotinbranchClause(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpNotinbranchClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpNotinbranchClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 49549 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpVariablesClause::copy ( SgCopyHelp& help) const
   {
     SgOmpVariablesClause* result = NULL;

  // printf ("Copy SgOmpVariablesClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpVariablesClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpVariablesClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpVariablesClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpVariablesClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpVariablesClause(  startOfConstruct_copy, variables_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpVariablesClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpVariablesClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 49810 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpCopyprivateClause::copy ( SgCopyHelp& help) const
   {
     SgOmpCopyprivateClause* result = NULL;

  // printf ("Copy SgOmpCopyprivateClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpCopyprivateClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpCopyprivateClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpCopyprivateClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpCopyprivateClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpCopyprivateClause(  startOfConstruct_copy, variables_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpCopyprivateClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpCopyprivateClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 50071 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpPrivateClause::copy ( SgCopyHelp& help) const
   {
     SgOmpPrivateClause* result = NULL;

  // printf ("Copy SgOmpPrivateClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpPrivateClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpPrivateClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpPrivateClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpPrivateClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpPrivateClause(  startOfConstruct_copy, variables_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpPrivateClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpPrivateClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 50332 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpFirstprivateClause::copy ( SgCopyHelp& help) const
   {
     SgOmpFirstprivateClause* result = NULL;

  // printf ("Copy SgOmpFirstprivateClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpFirstprivateClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpFirstprivateClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpFirstprivateClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpFirstprivateClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpFirstprivateClause(  startOfConstruct_copy, variables_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpFirstprivateClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpFirstprivateClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 50593 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSharedClause::copy ( SgCopyHelp& help) const
   {
     SgOmpSharedClause* result = NULL;

  // printf ("Copy SgOmpSharedClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpSharedClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpSharedClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpSharedClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpSharedClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpSharedClause(  startOfConstruct_copy, variables_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpSharedClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpSharedClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 50854 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpCopyinClause::copy ( SgCopyHelp& help) const
   {
     SgOmpCopyinClause* result = NULL;

  // printf ("Copy SgOmpCopyinClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpCopyinClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpCopyinClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpCopyinClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpCopyinClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpCopyinClause(  startOfConstruct_copy, variables_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpCopyinClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpCopyinClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 51115 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpLastprivateClause::copy ( SgCopyHelp& help) const
   {
     SgOmpLastprivateClause* result = NULL;

  // printf ("Copy SgOmpLastprivateClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpLastprivateClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpLastprivateClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpLastprivateClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpLastprivateClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpLastprivateClause(  startOfConstruct_copy, variables_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpLastprivateClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpLastprivateClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 51376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpReductionClause::copy ( SgCopyHelp& help) const
   {
     SgOmpReductionClause* result = NULL;

  // printf ("Copy SgOmpReductionClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpReductionClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpReductionClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpReductionClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpReductionClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
  // Copy constructor parameter data member: operation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operation
  // case: toBeCopied == COPY_DATA for operation
     SgOmpClause::omp_reduction_operator_enum operation_copy = p_operation; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpReductionClause(  startOfConstruct_copy, variables_copy, operation_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpReductionClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpReductionClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 51641 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpMapClause::copy ( SgCopyHelp& help) const
   {
     SgOmpMapClause* result = NULL;

  // printf ("Copy SgOmpMapClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpMapClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpMapClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpMapClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpMapClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
  // Copy constructor parameter data member: operation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operation
  // case: toBeCopied == COPY_DATA for operation
     SgOmpClause::omp_map_operator_enum operation_copy = p_operation; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpMapClause(  startOfConstruct_copy, variables_copy, operation_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): array_dimensions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for array_dimensions
  // case: toBeCopied == COPY_DATA for array_dimensions
     std::map<SgSymbol*,  std::vector < std::pair <SgExpression*, SgExpression*> > > array_dimensions_copy = p_array_dimensions; 
     result->p_array_dimensions = array_dimensions_copy; 
  // Copy non-constructor parameter data member (access function): dist_data_policies_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dist_data_policies
  // case: toBeCopied == COPY_DATA for dist_data_policies
     std::map<SgSymbol*,  std::vector < std::pair <SgOmpClause::omp_map_dist_data_enum, SgExpression*> > > dist_data_policies_copy = p_dist_data_policies; 
     result->p_dist_data_policies = dist_data_policies_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpMapClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpMapClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 51916 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpUniformClause::copy ( SgCopyHelp& help) const
   {
     SgOmpUniformClause* result = NULL;

  // printf ("Copy SgOmpUniformClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpUniformClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpUniformClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpUniformClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpUniformClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpUniformClause(  startOfConstruct_copy, variables_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpUniformClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpUniformClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 52177 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpAlignedClause::copy ( SgCopyHelp& help) const
   {
     SgOmpAlignedClause* result = NULL;

  // printf ("Copy SgOmpAlignedClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpAlignedClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpAlignedClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpAlignedClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpAlignedClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
  // Copy constructor parameter data member: alignment_copy
     SgExpression* alignment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alignment
          if (get_alignment() != NULL) 
             { 
               alignment_copy = static_cast<SgExpression*>(help.copyAst(get_alignment())); 
             } 
            else 
             { 
               alignment_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpAlignedClause(  startOfConstruct_copy, variables_copy, alignment_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)alignment
          if ( (alignment_copy != NULL) && (alignment_copy->get_parent() == NULL) && (isSgType(alignment_copy) == NULL) ) 
             { 
               alignment_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpAlignedClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpAlignedClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 52454 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpLinearClause::copy ( SgCopyHelp& help) const
   {
     SgOmpLinearClause* result = NULL;

  // printf ("Copy SgOmpLinearClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpLinearClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpLinearClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpLinearClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpLinearClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
  // Copy constructor parameter data member: step_copy
     SgExpression* step_copy; 
  // case: not a listType for (using conditionalToCopyVariable)step
          if (get_step() != NULL) 
             { 
               step_copy = static_cast<SgExpression*>(help.copyAst(get_step())); 
             } 
            else 
             { 
               step_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpLinearClause(  startOfConstruct_copy, variables_copy, step_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)step
          if ( (step_copy != NULL) && (step_copy->get_parent() == NULL) && (isSgType(step_copy) == NULL) ) 
             { 
               step_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpLinearClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpLinearClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 52731 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDependClause::copy ( SgCopyHelp& help) const
   {
     SgOmpDependClause* result = NULL;

  // printf ("Copy SgOmpDependClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpDependClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpDependClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpDependClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpDependClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != NULL) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = NULL; 
             } 
  // Copy constructor parameter data member: dependence_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dependence_type
  // case: toBeCopied == COPY_DATA for dependence_type
     SgOmpClause::omp_dependence_type_enum dependence_type_copy = p_dependence_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpDependClause(  startOfConstruct_copy, variables_copy, dependence_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): array_dimensions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for array_dimensions
  // case: toBeCopied == COPY_DATA for array_dimensions
     std::map<SgSymbol*,  std::vector < std::pair <SgExpression*, SgExpression*> > > array_dimensions_copy = p_array_dimensions; 
     result->p_array_dimensions = array_dimensions_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
          if ( (variables_copy != NULL) && (variables_copy->get_parent() == NULL) && (isSgType(variables_copy) == NULL) ) 
             { 
               variables_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpDependClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpDependClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 53001 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpScheduleClause::copy ( SgCopyHelp& help) const
   {
     SgOmpScheduleClause* result = NULL;

  // printf ("Copy SgOmpScheduleClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpScheduleClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpScheduleClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpScheduleClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpScheduleClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     SgOmpClause::omp_schedule_kind_enum kind_copy = p_kind; 
  // Copy constructor parameter data member: chunk_size_copy
     SgExpression* chunk_size_copy; 
  // case: not a listType for (using conditionalToCopyVariable)chunk_size
          if (get_chunk_size() != NULL) 
             { 
               chunk_size_copy = static_cast<SgExpression*>(help.copyAst(get_chunk_size())); 
             } 
            else 
             { 
               chunk_size_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpScheduleClause(  startOfConstruct_copy, kind_copy, chunk_size_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)chunk_size
          if ( (chunk_size_copy != NULL) && (chunk_size_copy->get_parent() == NULL) && (isSgType(chunk_size_copy) == NULL) ) 
             { 
               chunk_size_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpScheduleClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpScheduleClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 53266 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpMergeableClause::copy ( SgCopyHelp& help) const
   {
     SgOmpMergeableClause* result = NULL;

  // printf ("Copy SgOmpMergeableClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpMergeableClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpMergeableClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpMergeableClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpMergeableClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpMergeableClause(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpMergeableClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpMergeableClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 53511 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLambdaCapture::copy ( SgCopyHelp& help) const
   {
     SgLambdaCapture* result = NULL;

  // printf ("Copy SgLambdaCapture = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLambdaCapture*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLambdaCapture*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLambdaCapture: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLambdaCapture*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: capture_variable_copy
     SgExpression* capture_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)capture_variable
          if (get_capture_variable() != NULL) 
             { 
               capture_variable_copy = static_cast<SgExpression*>(help.copyAst(get_capture_variable())); 
             } 
            else 
             { 
               capture_variable_copy = NULL; 
             } 
  // Copy constructor parameter data member: source_closure_variable_copy
     SgExpression* source_closure_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)source_closure_variable
          if (get_source_closure_variable() != NULL) 
             { 
               source_closure_variable_copy = static_cast<SgExpression*>(help.copyAst(get_source_closure_variable())); 
             } 
            else 
             { 
               source_closure_variable_copy = NULL; 
             } 
  // Copy constructor parameter data member: closure_variable_copy
     SgExpression* closure_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)closure_variable
          if (get_closure_variable() != NULL) 
             { 
               closure_variable_copy = static_cast<SgExpression*>(help.copyAst(get_closure_variable())); 
             } 
            else 
             { 
               closure_variable_copy = NULL; 
             } 
  // Copy constructor parameter data member: capture_by_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for capture_by_reference
  // case: toBeCopied == COPY_DATA for capture_by_reference
     bool capture_by_reference_copy = p_capture_by_reference; 
  // Copy constructor parameter data member: implicit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for implicit
  // case: toBeCopied == COPY_DATA for implicit
     bool implicit_copy = p_implicit; 
  // Copy constructor parameter data member: pack_expansion_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pack_expansion
  // case: toBeCopied == COPY_DATA for pack_expansion
     bool pack_expansion_copy = p_pack_expansion; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLambdaCapture(  startOfConstruct_copy, capture_variable_copy, source_closure_variable_copy, closure_variable_copy, capture_by_reference_copy, implicit_copy, pack_expansion_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)capture_variable
          if ( (capture_variable_copy != NULL) && (capture_variable_copy->get_parent() == NULL) && (isSgType(capture_variable_copy) == NULL) ) 
             { 
               capture_variable_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)source_closure_variable
          if ( (source_closure_variable_copy != NULL) && (source_closure_variable_copy->get_parent() == NULL) && (isSgType(source_closure_variable_copy) == NULL) ) 
             { 
               source_closure_variable_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)closure_variable
          if ( (closure_variable_copy != NULL) && (closure_variable_copy->get_parent() == NULL) && (isSgType(closure_variable_copy) == NULL) ) 
             { 
               closure_variable_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLambdaCapture = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLambdaCapture::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 53816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLambdaCaptureList::copy ( SgCopyHelp& help) const
   {
     SgLambdaCaptureList* result = NULL;

  // printf ("Copy SgLambdaCaptureList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLambdaCaptureList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLambdaCaptureList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLambdaCaptureList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLambdaCaptureList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLambdaCaptureList(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_capture_list()
     SgLambdaCapturePtrList capture_list_copy; 
  // case: listType for capture_list
  // case: listType (typeIsPointerToList == false) for capture_list
     SgLambdaCapturePtrList::const_iterator source_capture_list_iterator = get_capture_list().begin(); 
     for ( /* empty by design */; source_capture_list_iterator != get_capture_list().end(); ++source_capture_list_iterator) 
        { 
          SgLambdaCapture* source_list_element = *source_capture_list_iterator; 
          SgLambdaCapture* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgLambdaCapture*>(help.copyAst(*source_capture_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          capture_list_copy.push_back(copy_list_element); 
        } 
     result->p_capture_list = capture_list_copy; 
  // case: listType for capture_list
  // case: listType (typeIsPointerToList == false) for capture_list
     SgLambdaCapturePtrList::const_iterator capture_list_iterator = result->get_capture_list().begin(); 
     for ( /* empty by design */; capture_list_iterator != result->get_capture_list().end(); ++capture_list_iterator) 
        { 
          SgLambdaCapture* list_element = *capture_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLambdaCaptureList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLambdaCaptureList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 54092 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTypeConstraint::copy ( SgCopyHelp& help) const
   {
     SgAdaTypeConstraint* result = NULL;

  // printf ("Copy SgAdaTypeConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTypeConstraint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTypeConstraint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTypeConstraint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTypeConstraint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTypeConstraint(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTypeConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTypeConstraint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 54337 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRangeConstraint::copy ( SgCopyHelp& help) const
   {
     SgAdaRangeConstraint* result = NULL;

  // printf ("Copy SgAdaRangeConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaRangeConstraint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaRangeConstraint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaRangeConstraint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaRangeConstraint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: range_copy
     SgExpression* range_copy; 
  // case: not a listType for (using conditionalToCopyVariable)range
          if (get_range() != NULL) 
             { 
               range_copy = static_cast<SgExpression*>(help.copyAst(get_range())); 
             } 
            else 
             { 
               range_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaRangeConstraint(  startOfConstruct_copy, range_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)range
          if ( (range_copy != NULL) && (range_copy->get_parent() == NULL) && (isSgType(range_copy) == NULL) ) 
             { 
               range_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaRangeConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaRangeConstraint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 54598 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaIndexConstraint::copy ( SgCopyHelp& help) const
   {
     SgAdaIndexConstraint* result = NULL;

  // printf ("Copy SgAdaIndexConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaIndexConstraint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaIndexConstraint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaIndexConstraint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaIndexConstraint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaIndexConstraint(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_indexRanges()
     SgExpressionPtrList indexRanges_copy; 
  // case: listType for indexRanges
  // case: listType (typeIsPointerToList == false) for indexRanges
     SgExpressionPtrList::const_iterator source_indexRanges_iterator = get_indexRanges().begin(); 
     for ( /* empty by design */; source_indexRanges_iterator != get_indexRanges().end(); ++source_indexRanges_iterator) 
        { 
          SgExpression* source_list_element = *source_indexRanges_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_indexRanges_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          indexRanges_copy.push_back(copy_list_element); 
        } 
     result->p_indexRanges = indexRanges_copy; 
  // case: listType for indexRanges
  // case: listType (typeIsPointerToList == false) for indexRanges
     SgExpressionPtrList::const_iterator indexRanges_iterator = result->get_indexRanges().begin(); 
     for ( /* empty by design */; indexRanges_iterator != result->get_indexRanges().end(); ++indexRanges_iterator) 
        { 
          SgExpression* list_element = *indexRanges_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaIndexConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaIndexConstraint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 54874 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDigitsConstraint::copy ( SgCopyHelp& help) const
   {
     SgAdaDigitsConstraint* result = NULL;

  // printf ("Copy SgAdaDigitsConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaDigitsConstraint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaDigitsConstraint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaDigitsConstraint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaDigitsConstraint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: digits_copy
     SgExpression* digits_copy; 
  // case: not a listType for (using conditionalToCopyVariable)digits
          if (get_digits() != NULL) 
             { 
               digits_copy = static_cast<SgExpression*>(help.copyAst(get_digits())); 
             } 
            else 
             { 
               digits_copy = NULL; 
             } 
  // Copy constructor parameter data member: subConstraint_copy
     SgAdaTypeConstraint* subConstraint_copy; 
  // case: not a listType for (using conditionalToCopyVariable)subConstraint
          if (get_subConstraint() != NULL) 
             { 
               subConstraint_copy = static_cast<SgAdaTypeConstraint*>(help.copyAst(get_subConstraint())); 
             } 
            else 
             { 
               subConstraint_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaDigitsConstraint(  startOfConstruct_copy, digits_copy, subConstraint_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)digits
          if ( (digits_copy != NULL) && (digits_copy->get_parent() == NULL) && (isSgType(digits_copy) == NULL) ) 
             { 
               digits_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)subConstraint
          if ( (subConstraint_copy != NULL) && (subConstraint_copy->get_parent() == NULL) && (isSgType(subConstraint_copy) == NULL) ) 
             { 
               subConstraint_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaDigitsConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaDigitsConstraint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 55151 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDeltaConstraint::copy ( SgCopyHelp& help) const
   {
     SgAdaDeltaConstraint* result = NULL;

  // printf ("Copy SgAdaDeltaConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaDeltaConstraint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaDeltaConstraint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaDeltaConstraint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaDeltaConstraint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: delta_copy
     SgExpression* delta_copy; 
  // case: not a listType for (using conditionalToCopyVariable)delta
          if (get_delta() != NULL) 
             { 
               delta_copy = static_cast<SgExpression*>(help.copyAst(get_delta())); 
             } 
            else 
             { 
               delta_copy = NULL; 
             } 
  // Copy constructor parameter data member: subConstraint_copy
     SgAdaTypeConstraint* subConstraint_copy; 
  // case: not a listType for (using conditionalToCopyVariable)subConstraint
          if (get_subConstraint() != NULL) 
             { 
               subConstraint_copy = static_cast<SgAdaTypeConstraint*>(help.copyAst(get_subConstraint())); 
             } 
            else 
             { 
               subConstraint_copy = NULL; 
             } 
  // Copy constructor parameter data member: isDecimal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDecimal
  // case: toBeCopied == COPY_DATA for isDecimal
     bool isDecimal_copy = p_isDecimal; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaDeltaConstraint(  startOfConstruct_copy, delta_copy, subConstraint_copy, isDecimal_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)delta
          if ( (delta_copy != NULL) && (delta_copy->get_parent() == NULL) && (isSgType(delta_copy) == NULL) ) 
             { 
               delta_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)subConstraint
          if ( (subConstraint_copy != NULL) && (subConstraint_copy->get_parent() == NULL) && (isSgType(subConstraint_copy) == NULL) ) 
             { 
               subConstraint_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaDeltaConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaDeltaConstraint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 55432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDiscriminantConstraint::copy ( SgCopyHelp& help) const
   {
     SgAdaDiscriminantConstraint* result = NULL;

  // printf ("Copy SgAdaDiscriminantConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaDiscriminantConstraint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaDiscriminantConstraint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaDiscriminantConstraint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaDiscriminantConstraint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaDiscriminantConstraint(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_discriminants()
     SgExpressionPtrList discriminants_copy; 
  // case: listType for discriminants
  // case: listType (typeIsPointerToList == false) for discriminants
     SgExpressionPtrList::const_iterator source_discriminants_iterator = get_discriminants().begin(); 
     for ( /* empty by design */; source_discriminants_iterator != get_discriminants().end(); ++source_discriminants_iterator) 
        { 
          SgExpression* source_list_element = *source_discriminants_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_discriminants_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          discriminants_copy.push_back(copy_list_element); 
        } 
     result->p_discriminants = discriminants_copy; 
  // case: listType for discriminants
  // case: listType (typeIsPointerToList == false) for discriminants
     SgExpressionPtrList::const_iterator discriminants_iterator = result->get_discriminants().begin(); 
     for ( /* empty by design */; discriminants_iterator != result->get_discriminants().end(); ++discriminants_iterator) 
        { 
          SgExpression* list_element = *discriminants_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaDiscriminantConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaDiscriminantConstraint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 55708 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaNullConstraint::copy ( SgCopyHelp& help) const
   {
     SgAdaNullConstraint* result = NULL;

  // printf ("Copy SgAdaNullConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaNullConstraint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaNullConstraint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaNullConstraint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaNullConstraint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaNullConstraint(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaNullConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaNullConstraint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 55953 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStatement::copy ( SgCopyHelp& help) const
   {
     SgStatement* result = NULL;

  // printf ("Copy SgStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 56208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgScopeStatement::copy ( SgCopyHelp& help) const
   {
     SgScopeStatement* result = NULL;

  // printf ("Copy SgScopeStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgScopeStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgScopeStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgScopeStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgScopeStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgScopeStatement(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgScopeStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgScopeStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 56466 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGlobal::copy ( SgCopyHelp& help) const
   {
     SgGlobal* result = NULL;

  // printf ("Copy SgGlobal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGlobal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGlobal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGlobal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGlobal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGlobal(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator source_declarations_iterator = get_declarations().begin(); 
     for ( /* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          declarations_copy.push_back(copy_list_element); 
        } 
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator declarations_iterator = result->get_declarations().begin(); 
     for ( /* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) 
        { 
          SgDeclarationStatement* list_element = *declarations_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGlobal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGlobal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 56752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBasicBlock::copy ( SgCopyHelp& help) const
   {
     SgBasicBlock* result = NULL;

  // printf ("Copy SgBasicBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBasicBlock*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBasicBlock*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBasicBlock: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBasicBlock*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBasicBlock(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator source_statements_iterator = get_statements().begin(); 
     for ( /* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) 
        { 
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          statements_copy.push_back(copy_list_element); 
        } 
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator statements_iterator = result->get_statements().begin(); 
     for ( /* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) 
        { 
          SgStatement* list_element = *statements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): asm_function_body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_function_body
  // case: toBeCopied == COPY_DATA for asm_function_body
     std::string asm_function_body_copy = p_asm_function_body; 
     result->p_asm_function_body = asm_function_body_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBasicBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBasicBlock::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 57051 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIfStmt::copy ( SgCopyHelp& help) const
   {
     SgIfStmt* result = NULL;

  // printf ("Copy SgIfStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIfStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIfStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIfStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIfStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: conditional_copy
     SgStatement* conditional_copy; 
  // case: not a listType for (using conditionalToCopyVariable)conditional
          if (get_conditional() != NULL) 
             { 
               conditional_copy = static_cast<SgStatement*>(help.copyAst(get_conditional())); 
             } 
            else 
             { 
               conditional_copy = NULL; 
             } 
  // Copy constructor parameter data member: true_body_copy
     SgStatement* true_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)true_body
          if (get_true_body() != NULL) 
             { 
               true_body_copy = static_cast<SgStatement*>(help.copyAst(get_true_body())); 
             } 
            else 
             { 
               true_body_copy = NULL; 
             } 
  // Copy constructor parameter data member: false_body_copy
     SgStatement* false_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)false_body
          if (get_false_body() != NULL) 
             { 
               false_body_copy = static_cast<SgStatement*>(help.copyAst(get_false_body())); 
             } 
            else 
             { 
               false_body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIfStmt(  startOfConstruct_copy, conditional_copy, true_body_copy, false_body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): is_if_constexpr_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_if_constexpr_statement
  // case: toBeCopied == COPY_DATA for is_if_constexpr_statement
     bool is_if_constexpr_statement_copy = p_is_if_constexpr_statement; 
     result->p_is_if_constexpr_statement = is_if_constexpr_statement_copy; 
  // Copy non-constructor parameter data member (access function): if_constexpr_value_known_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for if_constexpr_value_known
  // case: toBeCopied == COPY_DATA for if_constexpr_value_known
     bool if_constexpr_value_known_copy = p_if_constexpr_value_known; 
     result->p_if_constexpr_value_known = if_constexpr_value_known_copy; 
  // Copy non-constructor parameter data member (access function): if_constexpr_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for if_constexpr_value
  // case: toBeCopied == COPY_DATA for if_constexpr_value
     bool if_constexpr_value_copy = p_if_constexpr_value; 
     result->p_if_constexpr_value = if_constexpr_value_copy; 
  // Copy non-constructor parameter data member (access function): else_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for else_numeric_label
  // case: toBeCopied == COPY_DATA for else_numeric_label
     SgLabelRefExp* else_numeric_label_copy = p_else_numeric_label; 
     result->p_else_numeric_label = else_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): use_then_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_then_keyword
  // case: toBeCopied == COPY_DATA for use_then_keyword
     bool use_then_keyword_copy = p_use_then_keyword; 
     result->p_use_then_keyword = use_then_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_else_if_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_else_if_statement
  // case: toBeCopied == COPY_DATA for is_else_if_statement
     bool is_else_if_statement_copy = p_is_else_if_statement; 
     result->p_is_else_if_statement = is_else_if_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)conditional
          if ( (conditional_copy != NULL) && (conditional_copy->get_parent() == NULL) && (isSgType(conditional_copy) == NULL) ) 
             { 
               conditional_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)true_body
          if ( (true_body_copy != NULL) && (true_body_copy->get_parent() == NULL) && (isSgType(true_body_copy) == NULL) ) 
             { 
               true_body_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)false_body
          if ( (false_body_copy != NULL) && (false_body_copy->get_parent() == NULL) && (isSgType(false_body_copy) == NULL) ) 
             { 
               false_body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIfStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIfStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 57399 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgForStatement::copy ( SgCopyHelp& help) const
   {
     SgForStatement* result = NULL;

  // printf ("Copy SgForStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgForStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgForStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgForStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgForStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: test_copy
     SgStatement* test_copy; 
  // case: not a listType for (using conditionalToCopyVariable)test
          if (get_test() != NULL) 
             { 
               test_copy = static_cast<SgStatement*>(help.copyAst(get_test())); 
             } 
            else 
             { 
               test_copy = NULL; 
             } 
  // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != NULL) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = NULL; 
             } 
  // Copy constructor parameter data member: loop_body_copy
     SgStatement* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != NULL) 
             { 
               loop_body_copy = static_cast<SgStatement*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgForStatement(  startOfConstruct_copy, test_copy, increment_copy, loop_body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): for_init_stmt_copy
     SgForInitStatement* for_init_stmt_copy; 
  // case: not a listType for (using conditionalToCopyVariable)for_init_stmt
          if (get_for_init_stmt() != NULL) 
             { 
               for_init_stmt_copy = static_cast<SgForInitStatement*>(help.copyAst(get_for_init_stmt())); 
             } 
            else 
             { 
               for_init_stmt_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_for_init_stmt != NULL) delete result->p_for_init_stmt; 
     result->p_for_init_stmt = for_init_stmt_copy; 
  // case: not a listType for (using conditionalToSetParent)for_init_stmt
          if ( (for_init_stmt_copy != NULL) && (for_init_stmt_copy->get_parent() == NULL) && (isSgType(for_init_stmt_copy) == NULL) ) 
             { 
               for_init_stmt_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): else_body_copy
     SgStatement* else_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)else_body
          if (get_else_body() != NULL) 
             { 
               else_body_copy = static_cast<SgStatement*>(help.copyAst(get_else_body())); 
             } 
            else 
             { 
               else_body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_else_body != NULL) delete result->p_else_body; 
     result->p_else_body = else_body_copy; 
  // case: not a listType for (using conditionalToSetParent)else_body
          if ( (else_body_copy != NULL) && (else_body_copy->get_parent() == NULL) && (isSgType(else_body_copy) == NULL) ) 
             { 
               else_body_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // case: not a listType for (using conditionalToSetParent)test
          if ( (test_copy != NULL) && (test_copy->get_parent() == NULL) && (isSgType(test_copy) == NULL) ) 
             { 
               test_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)increment
          if ( (increment_copy != NULL) && (increment_copy->get_parent() == NULL) && (isSgType(increment_copy) == NULL) ) 
             { 
               increment_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)loop_body
          if ( (loop_body_copy != NULL) && (loop_body_copy->get_parent() == NULL) && (isSgType(loop_body_copy) == NULL) ) 
             { 
               loop_body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgForStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgForStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 57745 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionDefinition::copy ( SgCopyHelp& help) const
   {
     SgFunctionDefinition* result = NULL;

  // printf ("Copy SgFunctionDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionDefinition*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionDefinition*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionDefinition: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionDefinition*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionDefinition(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionDefinition::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 58016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateFunctionDefinition::copy ( SgCopyHelp& help) const
   {
     SgTemplateFunctionDefinition* result = NULL;

  // printf ("Copy SgTemplateFunctionDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateFunctionDefinition*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateFunctionDefinition*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateFunctionDefinition: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateFunctionDefinition*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateFunctionDefinition(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateFunctionDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateFunctionDefinition::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 58287 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassDefinition::copy ( SgCopyHelp& help) const
   {
     SgClassDefinition* result = NULL;

  // printf ("Copy SgClassDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClassDefinition*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClassDefinition*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClassDefinition: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClassDefinition*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClassDefinition(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_members()
     SgDeclarationStatementPtrList members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
     SgDeclarationStatementPtrList::const_iterator source_members_iterator = get_members().begin(); 
     for ( /* empty by design */; source_members_iterator != get_members().end(); ++source_members_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_members_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_members_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          members_copy.push_back(copy_list_element); 
        } 
     result->p_members = members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
     SgDeclarationStatementPtrList::const_iterator members_iterator = result->get_members().begin(); 
     for ( /* empty by design */; members_iterator != result->get_members().end(); ++members_iterator) 
        { 
          SgDeclarationStatement* list_element = *members_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (list access function): result->get_inheritances()
     SgBaseClassPtrList inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
     SgBaseClassPtrList::const_iterator source_inheritances_iterator = get_inheritances().begin(); 
     for ( /* empty by design */; source_inheritances_iterator != get_inheritances().end(); ++source_inheritances_iterator) 
        { 
          SgBaseClass* source_list_element = *source_inheritances_iterator; 
          SgBaseClass* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgBaseClass*>(help.copyAst(*source_inheritances_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          inheritances_copy.push_back(copy_list_element); 
        } 
     result->p_inheritances = inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
     SgBaseClassPtrList::const_iterator inheritances_iterator = result->get_inheritances().begin(); 
     for ( /* empty by design */; inheritances_iterator != result->get_inheritances().end(); ++inheritances_iterator) 
        { 
          SgBaseClass* list_element = *inheritances_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): packingAlignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packingAlignment
  // case: toBeCopied == COPY_DATA for packingAlignment
     unsigned int packingAlignment_copy = p_packingAlignment; 
     result->p_packingAlignment = packingAlignment_copy; 
  // Copy non-constructor parameter data member (access function): isSequence_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSequence
  // case: toBeCopied == COPY_DATA for isSequence
     bool isSequence_copy = p_isSequence; 
     result->p_isSequence = isSequence_copy; 
  // Copy non-constructor parameter data member (access function): isPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPrivate
  // case: toBeCopied == COPY_DATA for isPrivate
     bool isPrivate_copy = p_isPrivate; 
     result->p_isPrivate = isPrivate_copy; 
  // Copy non-constructor parameter data member (access function): isAbstract_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAbstract
  // case: toBeCopied == COPY_DATA for isAbstract
     bool isAbstract_copy = p_isAbstract; 
     result->p_isAbstract = isAbstract_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClassDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClassDefinition::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 58624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationDefn::copy ( SgCopyHelp& help) const
   {
     SgTemplateInstantiationDefn* result = NULL;

  // printf ("Copy SgTemplateInstantiationDefn = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateInstantiationDefn*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateInstantiationDefn*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateInstantiationDefn: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateInstantiationDefn*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateInstantiationDefn(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_members()
     SgDeclarationStatementPtrList members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
     SgDeclarationStatementPtrList::const_iterator source_members_iterator = get_members().begin(); 
     for ( /* empty by design */; source_members_iterator != get_members().end(); ++source_members_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_members_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_members_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          members_copy.push_back(copy_list_element); 
        } 
     result->p_members = members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
     SgDeclarationStatementPtrList::const_iterator members_iterator = result->get_members().begin(); 
     for ( /* empty by design */; members_iterator != result->get_members().end(); ++members_iterator) 
        { 
          SgDeclarationStatement* list_element = *members_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (list access function): result->get_inheritances()
     SgBaseClassPtrList inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
     SgBaseClassPtrList::const_iterator source_inheritances_iterator = get_inheritances().begin(); 
     for ( /* empty by design */; source_inheritances_iterator != get_inheritances().end(); ++source_inheritances_iterator) 
        { 
          SgBaseClass* source_list_element = *source_inheritances_iterator; 
          SgBaseClass* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgBaseClass*>(help.copyAst(*source_inheritances_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          inheritances_copy.push_back(copy_list_element); 
        } 
     result->p_inheritances = inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
     SgBaseClassPtrList::const_iterator inheritances_iterator = result->get_inheritances().begin(); 
     for ( /* empty by design */; inheritances_iterator != result->get_inheritances().end(); ++inheritances_iterator) 
        { 
          SgBaseClass* list_element = *inheritances_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): packingAlignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packingAlignment
  // case: toBeCopied == COPY_DATA for packingAlignment
     unsigned int packingAlignment_copy = p_packingAlignment; 
     result->p_packingAlignment = packingAlignment_copy; 
  // Copy non-constructor parameter data member (access function): isSequence_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSequence
  // case: toBeCopied == COPY_DATA for isSequence
     bool isSequence_copy = p_isSequence; 
     result->p_isSequence = isSequence_copy; 
  // Copy non-constructor parameter data member (access function): isPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPrivate
  // case: toBeCopied == COPY_DATA for isPrivate
     bool isPrivate_copy = p_isPrivate; 
     result->p_isPrivate = isPrivate_copy; 
  // Copy non-constructor parameter data member (access function): isAbstract_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAbstract
  // case: toBeCopied == COPY_DATA for isAbstract
     bool isAbstract_copy = p_isAbstract; 
     result->p_isAbstract = isAbstract_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateInstantiationDefn = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateInstantiationDefn::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 58961 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateClassDefinition::copy ( SgCopyHelp& help) const
   {
     SgTemplateClassDefinition* result = NULL;

  // printf ("Copy SgTemplateClassDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateClassDefinition*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateClassDefinition*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateClassDefinition: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateClassDefinition*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateClassDefinition(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_members()
     SgDeclarationStatementPtrList members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
     SgDeclarationStatementPtrList::const_iterator source_members_iterator = get_members().begin(); 
     for ( /* empty by design */; source_members_iterator != get_members().end(); ++source_members_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_members_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_members_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          members_copy.push_back(copy_list_element); 
        } 
     result->p_members = members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
     SgDeclarationStatementPtrList::const_iterator members_iterator = result->get_members().begin(); 
     for ( /* empty by design */; members_iterator != result->get_members().end(); ++members_iterator) 
        { 
          SgDeclarationStatement* list_element = *members_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (list access function): result->get_inheritances()
     SgBaseClassPtrList inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
     SgBaseClassPtrList::const_iterator source_inheritances_iterator = get_inheritances().begin(); 
     for ( /* empty by design */; source_inheritances_iterator != get_inheritances().end(); ++source_inheritances_iterator) 
        { 
          SgBaseClass* source_list_element = *source_inheritances_iterator; 
          SgBaseClass* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgBaseClass*>(help.copyAst(*source_inheritances_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          inheritances_copy.push_back(copy_list_element); 
        } 
     result->p_inheritances = inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
     SgBaseClassPtrList::const_iterator inheritances_iterator = result->get_inheritances().begin(); 
     for ( /* empty by design */; inheritances_iterator != result->get_inheritances().end(); ++inheritances_iterator) 
        { 
          SgBaseClass* list_element = *inheritances_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): packingAlignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packingAlignment
  // case: toBeCopied == COPY_DATA for packingAlignment
     unsigned int packingAlignment_copy = p_packingAlignment; 
     result->p_packingAlignment = packingAlignment_copy; 
  // Copy non-constructor parameter data member (access function): isSequence_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSequence
  // case: toBeCopied == COPY_DATA for isSequence
     bool isSequence_copy = p_isSequence; 
     result->p_isSequence = isSequence_copy; 
  // Copy non-constructor parameter data member (access function): isPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPrivate
  // case: toBeCopied == COPY_DATA for isPrivate
     bool isPrivate_copy = p_isPrivate; 
     result->p_isPrivate = isPrivate_copy; 
  // Copy non-constructor parameter data member (access function): isAbstract_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAbstract
  // case: toBeCopied == COPY_DATA for isAbstract
     bool isAbstract_copy = p_isAbstract; 
     result->p_isAbstract = isAbstract_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateClassDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateClassDefinition::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 59298 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWhileStmt::copy ( SgCopyHelp& help) const
   {
     SgWhileStmt* result = NULL;

  // printf ("Copy SgWhileStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgWhileStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgWhileStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgWhileStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgWhileStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: condition_copy
     SgStatement* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != NULL) 
             { 
               condition_copy = static_cast<SgStatement*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgWhileStmt(  startOfConstruct_copy, condition_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): else_body_copy
     SgStatement* else_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)else_body
          if (get_else_body() != NULL) 
             { 
               else_body_copy = static_cast<SgStatement*>(help.copyAst(get_else_body())); 
             } 
            else 
             { 
               else_body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_else_body != NULL) delete result->p_else_body; 
     result->p_else_body = else_body_copy; 
  // case: not a listType for (using conditionalToSetParent)else_body
          if ( (else_body_copy != NULL) && (else_body_copy->get_parent() == NULL) && (isSgType(else_body_copy) == NULL) ) 
             { 
               else_body_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
          if ( (condition_copy != NULL) && (condition_copy->get_parent() == NULL) && (isSgType(condition_copy) == NULL) ) 
             { 
               condition_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgWhileStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgWhileStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 59619 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDoWhileStmt::copy ( SgCopyHelp& help) const
   {
     SgDoWhileStmt* result = NULL;

  // printf ("Copy SgDoWhileStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDoWhileStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDoWhileStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDoWhileStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDoWhileStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  // Copy constructor parameter data member: condition_copy
     SgStatement* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != NULL) 
             { 
               condition_copy = static_cast<SgStatement*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDoWhileStmt(  startOfConstruct_copy, body_copy, condition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)condition
          if ( (condition_copy != NULL) && (condition_copy->get_parent() == NULL) && (isSgType(condition_copy) == NULL) ) 
             { 
               condition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDoWhileStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDoWhileStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 59906 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSwitchStatement::copy ( SgCopyHelp& help) const
   {
     SgSwitchStatement* result = NULL;

  // printf ("Copy SgSwitchStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSwitchStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSwitchStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSwitchStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSwitchStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: item_selector_copy
     SgStatement* item_selector_copy; 
  // case: not a listType for (using conditionalToCopyVariable)item_selector
          if (get_item_selector() != NULL) 
             { 
               item_selector_copy = static_cast<SgStatement*>(help.copyAst(get_item_selector())); 
             } 
            else 
             { 
               item_selector_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSwitchStatement(  startOfConstruct_copy, item_selector_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // case: not a listType for (using conditionalToSetParent)item_selector
          if ( (item_selector_copy != NULL) && (item_selector_copy->get_parent() == NULL) && (isSgType(item_selector_copy) == NULL) ) 
             { 
               item_selector_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSwitchStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSwitchStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 60203 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCatchOptionStmt::copy ( SgCopyHelp& help) const
   {
     SgCatchOptionStmt* result = NULL;

  // printf ("Copy SgCatchOptionStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCatchOptionStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCatchOptionStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCatchOptionStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCatchOptionStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: condition_copy
     SgVariableDeclaration* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != NULL) 
             { 
               condition_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  // Copy constructor parameter data member: trystmt_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for trystmt
  // case: toBeCopied == COPY_DATA for trystmt
     SgTryStmt* trystmt_copy = p_trystmt; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCatchOptionStmt(  startOfConstruct_copy, condition_copy, body_copy, trystmt_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
          if ( (condition_copy != NULL) && (condition_copy->get_parent() == NULL) && (isSgType(condition_copy) == NULL) ) 
             { 
               condition_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCatchOptionStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCatchOptionStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 60494 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamespaceDefinitionStatement::copy ( SgCopyHelp& help) const
   {
     SgNamespaceDefinitionStatement* result = NULL;

  // printf ("Copy SgNamespaceDefinitionStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNamespaceDefinitionStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNamespaceDefinitionStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNamespaceDefinitionStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNamespaceDefinitionStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: namespaceDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for namespaceDeclaration
  // case: toBeCopied == COPY_DATA for namespaceDeclaration
     SgNamespaceDeclarationStatement* namespaceDeclaration_copy = p_namespaceDeclaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNamespaceDefinitionStatement(  startOfConstruct_copy, namespaceDeclaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator source_declarations_iterator = get_declarations().begin(); 
     for ( /* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          declarations_copy.push_back(copy_list_element); 
        } 
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator declarations_iterator = result->get_declarations().begin(); 
     for ( /* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) 
        { 
          SgDeclarationStatement* list_element = *declarations_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): previousNamespaceDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for previousNamespaceDefinition
  // case: toBeCopied == COPY_DATA for previousNamespaceDefinition
     SgNamespaceDefinitionStatement* previousNamespaceDefinition_copy = p_previousNamespaceDefinition; 
     result->p_previousNamespaceDefinition = previousNamespaceDefinition_copy; 
  // Copy non-constructor parameter data member (access function): nextNamespaceDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nextNamespaceDefinition
  // case: toBeCopied == COPY_DATA for nextNamespaceDefinition
     SgNamespaceDefinitionStatement* nextNamespaceDefinition_copy = p_nextNamespaceDefinition; 
     result->p_nextNamespaceDefinition = nextNamespaceDefinition_copy; 
  // Copy non-constructor parameter data member (access function): global_definition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_definition
  // case: toBeCopied == COPY_DATA for global_definition
     SgNamespaceDefinitionStatement* global_definition_copy = p_global_definition; 
     result->p_global_definition = global_definition_copy; 
  // Copy non-constructor parameter data member (access function): isUnionOfReentrantNamespaceDefinitions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnionOfReentrantNamespaceDefinitions
  // case: toBeCopied == COPY_DATA for isUnionOfReentrantNamespaceDefinitions
     bool isUnionOfReentrantNamespaceDefinitions_copy = p_isUnionOfReentrantNamespaceDefinitions; 
     result->p_isUnionOfReentrantNamespaceDefinitions = isUnionOfReentrantNamespaceDefinitions_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNamespaceDefinitionStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNamespaceDefinitionStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 60804 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBlockDataStatement::copy ( SgCopyHelp& help) const
   {
     SgBlockDataStatement* result = NULL;

  // printf ("Copy SgBlockDataStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBlockDataStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBlockDataStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBlockDataStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBlockDataStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBlockDataStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgBasicBlock* body_copy = p_body; 
     result->p_body = body_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBlockDataStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBlockDataStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 61064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssociateStatement::copy ( SgCopyHelp& help) const
   {
     SgAssociateStatement* result = NULL;

  // printf ("Copy SgAssociateStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAssociateStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAssociateStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAssociateStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAssociateStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAssociateStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_associates()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associates
  // case: toBeCopied == COPY_DATA for associates
     SgDeclarationStatementPtrList associates_copy = p_associates; 
     result->p_associates = associates_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAssociateStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAssociateStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 61343 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFortranDo::copy ( SgCopyHelp& help) const
   {
     SgFortranDo* result = NULL;

  // printf ("Copy SgFortranDo = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFortranDo*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFortranDo*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFortranDo: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFortranDo*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: initialization_copy
     SgExpression* initialization_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initialization
          if (get_initialization() != NULL) 
             { 
               initialization_copy = static_cast<SgExpression*>(help.copyAst(get_initialization())); 
             } 
            else 
             { 
               initialization_copy = NULL; 
             } 
  // Copy constructor parameter data member: bound_copy
     SgExpression* bound_copy; 
  // case: not a listType for (using conditionalToCopyVariable)bound
          if (get_bound() != NULL) 
             { 
               bound_copy = static_cast<SgExpression*>(help.copyAst(get_bound())); 
             } 
            else 
             { 
               bound_copy = NULL; 
             } 
  // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != NULL) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFortranDo(  startOfConstruct_copy, initialization_copy, bound_copy, increment_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): old_style_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for old_style
  // case: toBeCopied == COPY_DATA for old_style
     bool old_style_copy = p_old_style; 
     result->p_old_style = old_style_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)initialization
          if ( (initialization_copy != NULL) && (initialization_copy->get_parent() == NULL) && (isSgType(initialization_copy) == NULL) ) 
             { 
               initialization_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)bound
          if ( (bound_copy != NULL) && (bound_copy->get_parent() == NULL) && (isSgType(bound_copy) == NULL) ) 
             { 
               bound_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)increment
          if ( (increment_copy != NULL) && (increment_copy->get_parent() == NULL) && (isSgType(increment_copy) == NULL) ) 
             { 
               increment_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFortranDo = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFortranDo::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 61682 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFortranNonblockedDo::copy ( SgCopyHelp& help) const
   {
     SgFortranNonblockedDo* result = NULL;

  // printf ("Copy SgFortranNonblockedDo = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFortranNonblockedDo*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFortranNonblockedDo*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFortranNonblockedDo: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFortranNonblockedDo*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: initialization_copy
     SgExpression* initialization_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initialization
          if (get_initialization() != NULL) 
             { 
               initialization_copy = static_cast<SgExpression*>(help.copyAst(get_initialization())); 
             } 
            else 
             { 
               initialization_copy = NULL; 
             } 
  // Copy constructor parameter data member: bound_copy
     SgExpression* bound_copy; 
  // case: not a listType for (using conditionalToCopyVariable)bound
          if (get_bound() != NULL) 
             { 
               bound_copy = static_cast<SgExpression*>(help.copyAst(get_bound())); 
             } 
            else 
             { 
               bound_copy = NULL; 
             } 
  // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != NULL) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  // Copy constructor parameter data member: end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_statement
  // case: toBeCopied == COPY_DATA for end_statement
     SgStatement* end_statement_copy = p_end_statement; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFortranNonblockedDo(  startOfConstruct_copy, initialization_copy, bound_copy, increment_copy, body_copy, end_statement_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): old_style_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for old_style
  // case: toBeCopied == COPY_DATA for old_style
     bool old_style_copy = p_old_style; 
     result->p_old_style = old_style_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)initialization
          if ( (initialization_copy != NULL) && (initialization_copy->get_parent() == NULL) && (isSgType(initialization_copy) == NULL) ) 
             { 
               initialization_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)bound
          if ( (bound_copy != NULL) && (bound_copy->get_parent() == NULL) && (isSgType(bound_copy) == NULL) ) 
             { 
               bound_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)increment
          if ( (increment_copy != NULL) && (increment_copy->get_parent() == NULL) && (isSgType(increment_copy) == NULL) ) 
             { 
               increment_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFortranNonblockedDo = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFortranNonblockedDo::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 62025 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgForAllStatement::copy ( SgCopyHelp& help) const
   {
     SgForAllStatement* result = NULL;

  // printf ("Copy SgForAllStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgForAllStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgForAllStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgForAllStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgForAllStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: forall_header_copy
     SgExprListExp* forall_header_copy; 
  // case: not a listType for (using conditionalToCopyVariable)forall_header
          if (get_forall_header() != NULL) 
             { 
               forall_header_copy = static_cast<SgExprListExp*>(help.copyAst(get_forall_header())); 
             } 
            else 
             { 
               forall_header_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgForAllStatement(  startOfConstruct_copy, forall_header_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): forall_statement_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forall_statement_kind
  // case: toBeCopied == COPY_DATA for forall_statement_kind
     SgForAllStatement::forall_statement_kind_enum forall_statement_kind_copy = p_forall_statement_kind; 
     result->p_forall_statement_kind = forall_statement_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)forall_header
          if ( (forall_header_copy != NULL) && (forall_header_copy->get_parent() == NULL) && (isSgType(forall_header_copy) == NULL) ) 
             { 
               forall_header_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgForAllStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgForAllStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 62332 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcForAllStatement::copy ( SgCopyHelp& help) const
   {
     SgUpcForAllStatement* result = NULL;

  // printf ("Copy SgUpcForAllStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcForAllStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcForAllStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcForAllStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcForAllStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: test_copy
     SgStatement* test_copy; 
  // case: not a listType for (using conditionalToCopyVariable)test
          if (get_test() != NULL) 
             { 
               test_copy = static_cast<SgStatement*>(help.copyAst(get_test())); 
             } 
            else 
             { 
               test_copy = NULL; 
             } 
  // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != NULL) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = NULL; 
             } 
  // Copy constructor parameter data member: affinity_copy
     SgExpression* affinity_copy; 
  // case: not a listType for (using conditionalToCopyVariable)affinity
          if (get_affinity() != NULL) 
             { 
               affinity_copy = static_cast<SgExpression*>(help.copyAst(get_affinity())); 
             } 
            else 
             { 
               affinity_copy = NULL; 
             } 
  // Copy constructor parameter data member: loop_body_copy
     SgStatement* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != NULL) 
             { 
               loop_body_copy = static_cast<SgStatement*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcForAllStatement(  startOfConstruct_copy, test_copy, increment_copy, affinity_copy, loop_body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): for_init_stmt_copy
     SgForInitStatement* for_init_stmt_copy; 
  // case: not a listType for (using conditionalToCopyVariable)for_init_stmt
          if (get_for_init_stmt() != NULL) 
             { 
               for_init_stmt_copy = static_cast<SgForInitStatement*>(help.copyAst(get_for_init_stmt())); 
             } 
            else 
             { 
               for_init_stmt_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_for_init_stmt != NULL) delete result->p_for_init_stmt; 
     result->p_for_init_stmt = for_init_stmt_copy; 
  // case: not a listType for (using conditionalToSetParent)for_init_stmt
          if ( (for_init_stmt_copy != NULL) && (for_init_stmt_copy->get_parent() == NULL) && (isSgType(for_init_stmt_copy) == NULL) ) 
             { 
               for_init_stmt_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)test
          if ( (test_copy != NULL) && (test_copy->get_parent() == NULL) && (isSgType(test_copy) == NULL) ) 
             { 
               test_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)increment
          if ( (increment_copy != NULL) && (increment_copy->get_parent() == NULL) && (isSgType(increment_copy) == NULL) ) 
             { 
               increment_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)affinity
          if ( (affinity_copy != NULL) && (affinity_copy->get_parent() == NULL) && (isSgType(affinity_copy) == NULL) ) 
             { 
               affinity_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)loop_body
          if ( (loop_body_copy != NULL) && (loop_body_copy->get_parent() == NULL) && (isSgType(loop_body_copy) == NULL) ) 
             { 
               loop_body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcForAllStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcForAllStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 62670 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCAFWithTeamStatement::copy ( SgCopyHelp& help) const
   {
     SgCAFWithTeamStatement* result = NULL;

  // printf ("Copy SgCAFWithTeamStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCAFWithTeamStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCAFWithTeamStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCAFWithTeamStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCAFWithTeamStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: teamId_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for teamId
  // case: toBeCopied == COPY_DATA for teamId
     SgVarRefExp* teamId_copy = p_teamId; 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCAFWithTeamStatement(  startOfConstruct_copy, teamId_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): endHasTeamId_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endHasTeamId
  // case: toBeCopied == COPY_DATA for endHasTeamId
     bool endHasTeamId_copy = p_endHasTeamId; 
     result->p_endHasTeamId = endHasTeamId_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCAFWithTeamStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCAFWithTeamStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 62950 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaForEachStatement::copy ( SgCopyHelp& help) const
   {
     SgJavaForEachStatement* result = NULL;

  // printf ("Copy SgJavaForEachStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaForEachStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaForEachStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaForEachStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaForEachStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: element_copy
     SgVariableDeclaration* element_copy; 
  // case: not a listType for (using conditionalToCopyVariable)element
          if (get_element() != NULL) 
             { 
               element_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_element())); 
             } 
            else 
             { 
               element_copy = NULL; 
             } 
  // Copy constructor parameter data member: collection_copy
     SgExpression* collection_copy; 
  // case: not a listType for (using conditionalToCopyVariable)collection
          if (get_collection() != NULL) 
             { 
               collection_copy = static_cast<SgExpression*>(help.copyAst(get_collection())); 
             } 
            else 
             { 
               collection_copy = NULL; 
             } 
  // Copy constructor parameter data member: loop_body_copy
     SgStatement* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != NULL) 
             { 
               loop_body_copy = static_cast<SgStatement*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaForEachStatement(  startOfConstruct_copy, element_copy, collection_copy, loop_body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)element
          if ( (element_copy != NULL) && (element_copy->get_parent() == NULL) && (isSgType(element_copy) == NULL) ) 
             { 
               element_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)collection
          if ( (collection_copy != NULL) && (collection_copy->get_parent() == NULL) && (isSgType(collection_copy) == NULL) ) 
             { 
               collection_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)loop_body
          if ( (loop_body_copy != NULL) && (loop_body_copy->get_parent() == NULL) && (isSgType(loop_body_copy) == NULL) ) 
             { 
               loop_body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaForEachStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaForEachStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 63253 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaLabelStatement::copy ( SgCopyHelp& help) const
   {
     SgJavaLabelStatement* result = NULL;

  // printf ("Copy SgJavaLabelStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaLabelStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaLabelStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaLabelStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaLabelStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     SgName label_copy = p_label; 
  // Copy constructor parameter data member: statement_copy
     SgStatement* statement_copy; 
  // case: not a listType for (using conditionalToCopyVariable)statement
          if (get_statement() != NULL) 
             { 
               statement_copy = static_cast<SgStatement*>(help.copyAst(get_statement())); 
             } 
            else 
             { 
               statement_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaLabelStatement(  startOfConstruct_copy, label_copy, statement_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)statement
          if ( (statement_copy != NULL) && (statement_copy->get_parent() == NULL) && (isSgType(statement_copy) == NULL) ) 
             { 
               statement_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaLabelStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaLabelStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 63528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMatlabForStatement::copy ( SgCopyHelp& help) const
   {
     SgMatlabForStatement* result = NULL;

  // printf ("Copy SgMatlabForStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMatlabForStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMatlabForStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMatlabForStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMatlabForStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: index_copy
     SgExpression* index_copy; 
  // case: not a listType for (using conditionalToCopyVariable)index
          if (get_index() != NULL) 
             { 
               index_copy = static_cast<SgExpression*>(help.copyAst(get_index())); 
             } 
            else 
             { 
               index_copy = NULL; 
             } 
  // Copy constructor parameter data member: range_copy
     SgExpression* range_copy; 
  // case: not a listType for (using conditionalToCopyVariable)range
          if (get_range() != NULL) 
             { 
               range_copy = static_cast<SgExpression*>(help.copyAst(get_range())); 
             } 
            else 
             { 
               range_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMatlabForStatement(  startOfConstruct_copy, index_copy, range_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)index
          if ( (index_copy != NULL) && (index_copy->get_parent() == NULL) && (isSgType(index_copy) == NULL) ) 
             { 
               index_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)range
          if ( (range_copy != NULL) && (range_copy->get_parent() == NULL) && (isSgType(range_copy) == NULL) ) 
             { 
               range_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMatlabForStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMatlabForStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 63831 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionParameterScope::copy ( SgCopyHelp& help) const
   {
     SgFunctionParameterScope* result = NULL;

  // printf ("Copy SgFunctionParameterScope = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionParameterScope*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionParameterScope*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionParameterScope: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionParameterScope*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionParameterScope(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator source_declarations_iterator = get_declarations().begin(); 
     for ( /* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          declarations_copy.push_back(copy_list_element); 
        } 
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator declarations_iterator = result->get_declarations().begin(); 
     for ( /* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) 
        { 
          SgDeclarationStatement* list_element = *declarations_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionParameterScope = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionParameterScope::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 64117 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeclarationScope::copy ( SgCopyHelp& help) const
   {
     SgDeclarationScope* result = NULL;

  // printf ("Copy SgDeclarationScope = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDeclarationScope*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDeclarationScope*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDeclarationScope: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDeclarationScope*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDeclarationScope(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDeclarationScope = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDeclarationScope::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 64372 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRangeBasedForStatement::copy ( SgCopyHelp& help) const
   {
     SgRangeBasedForStatement* result = NULL;

  // printf ("Copy SgRangeBasedForStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRangeBasedForStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRangeBasedForStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRangeBasedForStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRangeBasedForStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: iterator_declaration_copy
     SgVariableDeclaration* iterator_declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iterator_declaration
          if (get_iterator_declaration() != NULL) 
             { 
               iterator_declaration_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_iterator_declaration())); 
             } 
            else 
             { 
               iterator_declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: range_declaration_copy
     SgVariableDeclaration* range_declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)range_declaration
          if (get_range_declaration() != NULL) 
             { 
               range_declaration_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_range_declaration())); 
             } 
            else 
             { 
               range_declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: begin_declaration_copy
     SgVariableDeclaration* begin_declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)begin_declaration
          if (get_begin_declaration() != NULL) 
             { 
               begin_declaration_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_begin_declaration())); 
             } 
            else 
             { 
               begin_declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: end_declaration_copy
     SgVariableDeclaration* end_declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)end_declaration
          if (get_end_declaration() != NULL) 
             { 
               end_declaration_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_end_declaration())); 
             } 
            else 
             { 
               end_declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: not_equal_expression_copy
     SgExpression* not_equal_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)not_equal_expression
          if (get_not_equal_expression() != NULL) 
             { 
               not_equal_expression_copy = static_cast<SgExpression*>(help.copyAst(get_not_equal_expression())); 
             } 
            else 
             { 
               not_equal_expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: increment_expression_copy
     SgExpression* increment_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment_expression
          if (get_increment_expression() != NULL) 
             { 
               increment_expression_copy = static_cast<SgExpression*>(help.copyAst(get_increment_expression())); 
             } 
            else 
             { 
               increment_expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: loop_body_copy
     SgStatement* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != NULL) 
             { 
               loop_body_copy = static_cast<SgStatement*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRangeBasedForStatement(  startOfConstruct_copy, iterator_declaration_copy, range_declaration_copy, begin_declaration_copy, end_declaration_copy, not_equal_expression_copy, increment_expression_copy, loop_body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)iterator_declaration
          if ( (iterator_declaration_copy != NULL) && (iterator_declaration_copy->get_parent() == NULL) && (isSgType(iterator_declaration_copy) == NULL) ) 
             { 
               iterator_declaration_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)range_declaration
          if ( (range_declaration_copy != NULL) && (range_declaration_copy->get_parent() == NULL) && (isSgType(range_declaration_copy) == NULL) ) 
             { 
               range_declaration_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)begin_declaration
          if ( (begin_declaration_copy != NULL) && (begin_declaration_copy->get_parent() == NULL) && (isSgType(begin_declaration_copy) == NULL) ) 
             { 
               begin_declaration_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)end_declaration
          if ( (end_declaration_copy != NULL) && (end_declaration_copy->get_parent() == NULL) && (isSgType(end_declaration_copy) == NULL) ) 
             { 
               end_declaration_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)not_equal_expression
          if ( (not_equal_expression_copy != NULL) && (not_equal_expression_copy->get_parent() == NULL) && (isSgType(not_equal_expression_copy) == NULL) ) 
             { 
               not_equal_expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)increment_expression
          if ( (increment_expression_copy != NULL) && (increment_expression_copy->get_parent() == NULL) && (isSgType(increment_expression_copy) == NULL) ) 
             { 
               increment_expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)loop_body
          if ( (loop_body_copy != NULL) && (loop_body_copy->get_parent() == NULL) && (isSgType(loop_body_copy) == NULL) ) 
             { 
               loop_body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRangeBasedForStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRangeBasedForStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 64739 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialForThenStatement::copy ( SgCopyHelp& help) const
   {
     SgJovialForThenStatement* result = NULL;

  // printf ("Copy SgJovialForThenStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialForThenStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialForThenStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialForThenStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialForThenStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: initialization_copy
     SgExpression* initialization_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initialization
          if (get_initialization() != NULL) 
             { 
               initialization_copy = static_cast<SgExpression*>(help.copyAst(get_initialization())); 
             } 
            else 
             { 
               initialization_copy = NULL; 
             } 
  // Copy constructor parameter data member: while_expression_copy
     SgExpression* while_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)while_expression
          if (get_while_expression() != NULL) 
             { 
               while_expression_copy = static_cast<SgExpression*>(help.copyAst(get_while_expression())); 
             } 
            else 
             { 
               while_expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: by_or_then_expression_copy
     SgExpression* by_or_then_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)by_or_then_expression
          if (get_by_or_then_expression() != NULL) 
             { 
               by_or_then_expression_copy = static_cast<SgExpression*>(help.copyAst(get_by_or_then_expression())); 
             } 
            else 
             { 
               by_or_then_expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: loop_body_copy
     SgBasicBlock* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != NULL) 
             { 
               loop_body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialForThenStatement(  startOfConstruct_copy, initialization_copy, while_expression_copy, by_or_then_expression_copy, loop_body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): loop_statement_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for loop_statement_type
  // case: toBeCopied == COPY_DATA for loop_statement_type
     SgJovialForThenStatement::loop_statement_type_enum loop_statement_type_copy = p_loop_statement_type; 
     result->p_loop_statement_type = loop_statement_type_copy; 
  // case: not a listType for (using conditionalToSetParent)initialization
          if ( (initialization_copy != NULL) && (initialization_copy->get_parent() == NULL) && (isSgType(initialization_copy) == NULL) ) 
             { 
               initialization_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)while_expression
          if ( (while_expression_copy != NULL) && (while_expression_copy->get_parent() == NULL) && (isSgType(while_expression_copy) == NULL) ) 
             { 
               while_expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)by_or_then_expression
          if ( (by_or_then_expression_copy != NULL) && (by_or_then_expression_copy->get_parent() == NULL) && (isSgType(by_or_then_expression_copy) == NULL) ) 
             { 
               by_or_then_expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)loop_body
          if ( (loop_body_copy != NULL) && (loop_body_copy->get_parent() == NULL) && (isSgType(loop_body_copy) == NULL) ) 
             { 
               loop_body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialForThenStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialForThenStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 65063 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAcceptStmt::copy ( SgCopyHelp& help) const
   {
     SgAdaAcceptStmt* result = NULL;

  // printf ("Copy SgAdaAcceptStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaAcceptStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaAcceptStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaAcceptStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaAcceptStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaAcceptStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): parameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterScope
  // case: toBeCopied == COPY_DATA for parameterScope
     SgFunctionParameterScope* parameterScope_copy = p_parameterScope; 
     result->p_parameterScope = parameterScope_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): entry_copy
     SgExpression* entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entry
          if (get_entry() != NULL) 
             { 
               entry_copy = static_cast<SgExpression*>(help.copyAst(get_entry())); 
             } 
            else 
             { 
               entry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entry != NULL) delete result->p_entry; 
     result->p_entry = entry_copy; 
  // case: not a listType for (using conditionalToSetParent)entry
          if ( (entry_copy != NULL) && (entry_copy->get_parent() == NULL) && (isSgType(entry_copy) == NULL) ) 
             { 
               entry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): index_copy
     SgExpression* index_copy; 
  // case: not a listType for (using conditionalToCopyVariable)index
          if (get_index() != NULL) 
             { 
               index_copy = static_cast<SgExpression*>(help.copyAst(get_index())); 
             } 
            else 
             { 
               index_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_index != NULL) delete result->p_index; 
     result->p_index = index_copy; 
  // case: not a listType for (using conditionalToSetParent)index
          if ( (index_copy != NULL) && (index_copy->get_parent() == NULL) && (isSgType(index_copy) == NULL) ) 
             { 
               index_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaAcceptStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaAcceptStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 65399 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageSpec::copy ( SgCopyHelp& help) const
   {
     SgAdaPackageSpec* result = NULL;

  // printf ("Copy SgAdaPackageSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaPackageSpec*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaPackageSpec*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaPackageSpec: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaPackageSpec*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaPackageSpec(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): hasPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasPrivate
  // case: toBeCopied == COPY_DATA for hasPrivate
     bool hasPrivate_copy = p_hasPrivate; 
     result->p_hasPrivate = hasPrivate_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator source_declarations_iterator = get_declarations().begin(); 
     for ( /* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          declarations_copy.push_back(copy_list_element); 
        } 
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator declarations_iterator = result->get_declarations().begin(); 
     for ( /* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) 
        { 
          SgDeclarationStatement* list_element = *declarations_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaPackageSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaPackageSpec::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 65690 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageBody::copy ( SgCopyHelp& help) const
   {
     SgAdaPackageBody* result = NULL;

  // printf ("Copy SgAdaPackageBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaPackageBody*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaPackageBody*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaPackageBody: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaPackageBody*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaPackageBody(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator source_statements_iterator = get_statements().begin(); 
     for ( /* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) 
        { 
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          statements_copy.push_back(copy_list_element); 
        } 
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator statements_iterator = result->get_statements().begin(); 
     for ( /* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) 
        { 
          SgStatement* list_element = *statements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaPackageBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaPackageBody::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 65976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskSpec::copy ( SgCopyHelp& help) const
   {
     SgAdaTaskSpec* result = NULL;

  // printf ("Copy SgAdaTaskSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTaskSpec*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTaskSpec*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTaskSpec: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTaskSpec*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTaskSpec(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgAdaTaskBody* body_copy = p_body; 
     result->p_body = body_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator source_declarations_iterator = get_declarations().begin(); 
     for ( /* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          declarations_copy.push_back(copy_list_element); 
        } 
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator declarations_iterator = result->get_declarations().begin(); 
     for ( /* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) 
        { 
          SgDeclarationStatement* list_element = *declarations_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): hasPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasPrivate
  // case: toBeCopied == COPY_DATA for hasPrivate
     bool hasPrivate_copy = p_hasPrivate; 
     result->p_hasPrivate = hasPrivate_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTaskSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTaskSpec::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 66272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskBody::copy ( SgCopyHelp& help) const
   {
     SgAdaTaskBody* result = NULL;

  // printf ("Copy SgAdaTaskBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTaskBody*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTaskBody*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTaskBody: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTaskBody*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTaskBody(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for spec
  // case: toBeCopied == COPY_DATA for spec
     SgAdaTaskSpec* spec_copy = p_spec; 
     result->p_spec = spec_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator source_statements_iterator = get_statements().begin(); 
     for ( /* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) 
        { 
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          statements_copy.push_back(copy_list_element); 
        } 
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator statements_iterator = result->get_statements().begin(); 
     for ( /* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) 
        { 
          SgStatement* list_element = *statements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTaskBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTaskBody::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 66563 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedBody::copy ( SgCopyHelp& help) const
   {
     SgAdaProtectedBody* result = NULL;

  // printf ("Copy SgAdaProtectedBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaProtectedBody*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaProtectedBody*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaProtectedBody: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaProtectedBody*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaProtectedBody(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for spec
  // case: toBeCopied == COPY_DATA for spec
     SgAdaProtectedSpec* spec_copy = p_spec; 
     result->p_spec = spec_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator source_statements_iterator = get_statements().begin(); 
     for ( /* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) 
        { 
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          statements_copy.push_back(copy_list_element); 
        } 
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator statements_iterator = result->get_statements().begin(); 
     for ( /* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) 
        { 
          SgStatement* list_element = *statements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaProtectedBody = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaProtectedBody::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 66854 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedSpec::copy ( SgCopyHelp& help) const
   {
     SgAdaProtectedSpec* result = NULL;

  // printf ("Copy SgAdaProtectedSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaProtectedSpec*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaProtectedSpec*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaProtectedSpec: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaProtectedSpec*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaProtectedSpec(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgAdaProtectedBody* body_copy = p_body; 
     result->p_body = body_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator source_declarations_iterator = get_declarations().begin(); 
     for ( /* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          declarations_copy.push_back(copy_list_element); 
        } 
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator declarations_iterator = result->get_declarations().begin(); 
     for ( /* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) 
        { 
          SgDeclarationStatement* list_element = *declarations_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): hasPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasPrivate
  // case: toBeCopied == COPY_DATA for hasPrivate
     bool hasPrivate_copy = p_hasPrivate; 
     result->p_hasPrivate = hasPrivate_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaProtectedSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaProtectedSpec::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 67150 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericDefn::copy ( SgCopyHelp& help) const
   {
     SgAdaGenericDefn* result = NULL;

  // printf ("Copy SgAdaGenericDefn = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaGenericDefn*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaGenericDefn*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaGenericDefn: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaGenericDefn*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaGenericDefn(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator source_declarations_iterator = get_declarations().begin(); 
     for ( /* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          declarations_copy.push_back(copy_list_element); 
        } 
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
     SgDeclarationStatementPtrList::const_iterator declarations_iterator = result->get_declarations().begin(); 
     for ( /* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) 
        { 
          SgDeclarationStatement* list_element = *declarations_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaGenericDefn = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaGenericDefn::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 67436 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionTypeTable::copy ( SgCopyHelp& help) const
   {
     SgFunctionTypeTable* result = NULL;

  // printf ("Copy SgFunctionTypeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionTypeTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionTypeTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionTypeTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionTypeTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionTypeTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): function_type_table_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for function_type_table
  // case: toBeCopied == COPY_DATA for function_type_table
     SgSymbolTable* function_type_table_copy = p_function_type_table; 
     result->p_function_type_table = function_type_table_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionTypeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionTypeTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 67699 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeclarationStatement::copy ( SgCopyHelp& help) const
   {
     SgDeclarationStatement* result = NULL;

  // printf ("Copy SgDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDeclarationStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDeclarationStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDeclarationStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDeclarationStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDeclarationStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDeclarationStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 68024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionParameterList::copy ( SgCopyHelp& help) const
   {
     SgFunctionParameterList* result = NULL;

  // printf ("Copy SgFunctionParameterList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionParameterList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionParameterList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionParameterList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionParameterList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionParameterList(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_args()
     SgInitializedNamePtrList args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
     SgInitializedNamePtrList::const_iterator source_args_iterator = get_args().begin(); 
     for ( /* empty by design */; source_args_iterator != get_args().end(); ++source_args_iterator) 
        { 
          SgInitializedName* source_list_element = *source_args_iterator; 
          SgInitializedName* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_args_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          args_copy.push_back(copy_list_element); 
        } 
     result->p_args = args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
     SgInitializedNamePtrList::const_iterator args_iterator = result->get_args().begin(); 
     for ( /* empty by design */; args_iterator != result->get_args().end(); ++args_iterator) 
        { 
          SgInitializedName* list_element = *args_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionParameterList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionParameterList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 68380 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariableDeclaration::copy ( SgCopyHelp& help) const
   {
     SgVariableDeclaration* result = NULL;

  // printf ("Copy SgVariableDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVariableDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVariableDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVariableDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVariableDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVariableDeclaration(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseTypeDefiningDeclaration
     SgDeclarationStatement* baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)baseTypeDefiningDeclaration
          if (get_baseTypeDefiningDeclaration() != NULL) 
             { 
               baseTypeDefiningDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_baseTypeDefiningDeclaration())); 
             } 
            else 
             { 
               baseTypeDefiningDeclaration_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseTypeDefiningDeclaration != NULL) delete result->p_baseTypeDefiningDeclaration; 
     result->p_baseTypeDefiningDeclaration = baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)baseTypeDefiningDeclaration
          if ( (baseTypeDefiningDeclaration_copy != NULL) && (baseTypeDefiningDeclaration_copy->get_parent() == NULL) && (isSgType(baseTypeDefiningDeclaration_copy) == NULL) ) 
             { 
               baseTypeDefiningDeclaration_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgInitializedNamePtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgInitializedNamePtrList::const_iterator source_variables_iterator = get_variables().begin(); 
     for ( /* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) 
        { 
          SgInitializedName* source_list_element = *source_variables_iterator; 
          SgInitializedName* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          variables_copy.push_back(copy_list_element); 
        } 
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgInitializedNamePtrList::const_iterator variables_iterator = result->get_variables().begin(); 
     for ( /* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) 
        { 
          SgInitializedName* list_element = *variables_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): variableDeclarationContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variableDeclarationContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for variableDeclarationContainsBaseTypeDefiningDeclaration
     bool variableDeclarationContainsBaseTypeDefiningDeclaration_copy = p_variableDeclarationContainsBaseTypeDefiningDeclaration; 
     result->p_variableDeclarationContainsBaseTypeDefiningDeclaration = variableDeclarationContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_initialization_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_initialization_priority
  // case: toBeCopied == COPY_DATA for gnu_extension_initialization_priority
     unsigned short gnu_extension_initialization_priority_copy = p_gnu_extension_initialization_priority; 
     result->p_gnu_extension_initialization_priority = gnu_extension_initialization_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_declaration_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_declaration_attribute
  // case: toBeCopied == COPY_DATA for gnu_extension_declaration_attribute
     SgVariableDeclaration::gnu_extension_declaration_attributes_enum gnu_extension_declaration_attribute_copy = p_gnu_extension_declaration_attribute; 
     result->p_gnu_extension_declaration_attribute = gnu_extension_declaration_attribute_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): is_thread_local_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_thread_local
  // case: toBeCopied == COPY_DATA for is_thread_local
     bool is_thread_local_copy = p_is_thread_local; 
     result->p_is_thread_local = is_thread_local_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): builtFromUseOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for builtFromUseOnly
  // case: toBeCopied == COPY_DATA for builtFromUseOnly
     bool builtFromUseOnly_copy = p_builtFromUseOnly; 
     result->p_builtFromUseOnly = builtFromUseOnly_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVariableDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVariableDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 68835 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateVariableDeclaration::copy ( SgCopyHelp& help) const
   {
     SgTemplateVariableDeclaration* result = NULL;

  // printf ("Copy SgTemplateVariableDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateVariableDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateVariableDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateVariableDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateVariableDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateVariableDeclaration(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseTypeDefiningDeclaration
     SgDeclarationStatement* baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)baseTypeDefiningDeclaration
          if (get_baseTypeDefiningDeclaration() != NULL) 
             { 
               baseTypeDefiningDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_baseTypeDefiningDeclaration())); 
             } 
            else 
             { 
               baseTypeDefiningDeclaration_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseTypeDefiningDeclaration != NULL) delete result->p_baseTypeDefiningDeclaration; 
     result->p_baseTypeDefiningDeclaration = baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)baseTypeDefiningDeclaration
          if ( (baseTypeDefiningDeclaration_copy != NULL) && (baseTypeDefiningDeclaration_copy->get_parent() == NULL) && (isSgType(baseTypeDefiningDeclaration_copy) == NULL) ) 
             { 
               baseTypeDefiningDeclaration_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgInitializedNamePtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgInitializedNamePtrList::const_iterator source_variables_iterator = get_variables().begin(); 
     for ( /* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) 
        { 
          SgInitializedName* source_list_element = *source_variables_iterator; 
          SgInitializedName* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          variables_copy.push_back(copy_list_element); 
        } 
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgInitializedNamePtrList::const_iterator variables_iterator = result->get_variables().begin(); 
     for ( /* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) 
        { 
          SgInitializedName* list_element = *variables_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): variableDeclarationContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variableDeclarationContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for variableDeclarationContainsBaseTypeDefiningDeclaration
     bool variableDeclarationContainsBaseTypeDefiningDeclaration_copy = p_variableDeclarationContainsBaseTypeDefiningDeclaration; 
     result->p_variableDeclarationContainsBaseTypeDefiningDeclaration = variableDeclarationContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_initialization_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_initialization_priority
  // case: toBeCopied == COPY_DATA for gnu_extension_initialization_priority
     unsigned short gnu_extension_initialization_priority_copy = p_gnu_extension_initialization_priority; 
     result->p_gnu_extension_initialization_priority = gnu_extension_initialization_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_declaration_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_declaration_attribute
  // case: toBeCopied == COPY_DATA for gnu_extension_declaration_attribute
     SgVariableDeclaration::gnu_extension_declaration_attributes_enum gnu_extension_declaration_attribute_copy = p_gnu_extension_declaration_attribute; 
     result->p_gnu_extension_declaration_attribute = gnu_extension_declaration_attribute_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): is_thread_local_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_thread_local
  // case: toBeCopied == COPY_DATA for is_thread_local
     bool is_thread_local_copy = p_is_thread_local; 
     result->p_is_thread_local = is_thread_local_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): builtFromUseOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for builtFromUseOnly
  // case: toBeCopied == COPY_DATA for builtFromUseOnly
     bool builtFromUseOnly_copy = p_builtFromUseOnly; 
     result->p_builtFromUseOnly = builtFromUseOnly_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateVariableDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateVariableDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 69310 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateVariableInstantiation::copy ( SgCopyHelp& help) const
   {
     SgTemplateVariableInstantiation* result = NULL;

  // printf ("Copy SgTemplateVariableInstantiation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateVariableInstantiation*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateVariableInstantiation*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateVariableInstantiation: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateVariableInstantiation*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateVariableDeclaration* templateDeclaration_copy = p_templateDeclaration; 
  // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateVariableInstantiation(  startOfConstruct_copy, templateDeclaration_copy, templateArguments_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseTypeDefiningDeclaration
     SgDeclarationStatement* baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)baseTypeDefiningDeclaration
          if (get_baseTypeDefiningDeclaration() != NULL) 
             { 
               baseTypeDefiningDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_baseTypeDefiningDeclaration())); 
             } 
            else 
             { 
               baseTypeDefiningDeclaration_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseTypeDefiningDeclaration != NULL) delete result->p_baseTypeDefiningDeclaration; 
     result->p_baseTypeDefiningDeclaration = baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)baseTypeDefiningDeclaration
          if ( (baseTypeDefiningDeclaration_copy != NULL) && (baseTypeDefiningDeclaration_copy->get_parent() == NULL) && (isSgType(baseTypeDefiningDeclaration_copy) == NULL) ) 
             { 
               baseTypeDefiningDeclaration_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgInitializedNamePtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgInitializedNamePtrList::const_iterator source_variables_iterator = get_variables().begin(); 
     for ( /* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) 
        { 
          SgInitializedName* source_list_element = *source_variables_iterator; 
          SgInitializedName* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          variables_copy.push_back(copy_list_element); 
        } 
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgInitializedNamePtrList::const_iterator variables_iterator = result->get_variables().begin(); 
     for ( /* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) 
        { 
          SgInitializedName* list_element = *variables_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): variableDeclarationContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variableDeclarationContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for variableDeclarationContainsBaseTypeDefiningDeclaration
     bool variableDeclarationContainsBaseTypeDefiningDeclaration_copy = p_variableDeclarationContainsBaseTypeDefiningDeclaration; 
     result->p_variableDeclarationContainsBaseTypeDefiningDeclaration = variableDeclarationContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_initialization_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_initialization_priority
  // case: toBeCopied == COPY_DATA for gnu_extension_initialization_priority
     unsigned short gnu_extension_initialization_priority_copy = p_gnu_extension_initialization_priority; 
     result->p_gnu_extension_initialization_priority = gnu_extension_initialization_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_declaration_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_declaration_attribute
  // case: toBeCopied == COPY_DATA for gnu_extension_declaration_attribute
     SgVariableDeclaration::gnu_extension_declaration_attributes_enum gnu_extension_declaration_attribute_copy = p_gnu_extension_declaration_attribute; 
     result->p_gnu_extension_declaration_attribute = gnu_extension_declaration_attribute_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): is_thread_local_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_thread_local
  // case: toBeCopied == COPY_DATA for is_thread_local
     bool is_thread_local_copy = p_is_thread_local; 
     result->p_is_thread_local = is_thread_local_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): builtFromUseOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for builtFromUseOnly
  // case: toBeCopied == COPY_DATA for builtFromUseOnly
     bool builtFromUseOnly_copy = p_builtFromUseOnly; 
     result->p_builtFromUseOnly = builtFromUseOnly_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): templateHeader_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateHeader
  // case: toBeCopied == COPY_DATA for templateHeader
     SgName templateHeader_copy = p_templateHeader; 
     result->p_templateHeader = templateHeader_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateVariableInstantiation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateVariableInstantiation::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 69783 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariableDefinition::copy ( SgCopyHelp& help) const
   {
     SgVariableDefinition* result = NULL;

  // printf ("Copy SgVariableDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVariableDefinition*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVariableDefinition*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVariableDefinition: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVariableDefinition*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: vardefn_copy
     SgInitializedName* vardefn_copy; 
  // case: not a listType for (using conditionalToCopyVariable)vardefn
          if (get_vardefn() != NULL) 
             { 
               vardefn_copy = static_cast<SgInitializedName*>(help.copyAst(get_vardefn())); 
             } 
            else 
             { 
               vardefn_copy = NULL; 
             } 
  // Copy constructor parameter data member: bitfield_copy
     SgExpression* bitfield_copy; 
  // case: not a listType for (using conditionalToCopyVariable)bitfield
          if (get_bitfield() != NULL) 
             { 
               bitfield_copy = static_cast<SgExpression*>(help.copyAst(get_bitfield())); 
             } 
            else 
             { 
               bitfield_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVariableDefinition(  startOfConstruct_copy, vardefn_copy, bitfield_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)vardefn
          if ( (vardefn_copy != NULL) && (vardefn_copy->get_parent() == NULL) && (isSgType(vardefn_copy) == NULL) ) 
             { 
               vardefn_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)bitfield
          if ( (bitfield_copy != NULL) && (bitfield_copy->get_parent() == NULL) && (isSgType(bitfield_copy) == NULL) ) 
             { 
               bitfield_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVariableDefinition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVariableDefinition::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 70140 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClinkageDeclarationStatement::copy ( SgCopyHelp& help) const
   {
     SgClinkageDeclarationStatement* result = NULL;

  // printf ("Copy SgClinkageDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClinkageDeclarationStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClinkageDeclarationStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClinkageDeclarationStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClinkageDeclarationStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClinkageDeclarationStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_languageSpecifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for languageSpecifier
  // case: toBeCopied == COPY_DATA for languageSpecifier
     std::string languageSpecifier_copy = p_languageSpecifier; 
     result->p_languageSpecifier = languageSpecifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClinkageDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClinkageDeclarationStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 70470 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClinkageStartStatement::copy ( SgCopyHelp& help) const
   {
     SgClinkageStartStatement* result = NULL;

  // printf ("Copy SgClinkageStartStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClinkageStartStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClinkageStartStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClinkageStartStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClinkageStartStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClinkageStartStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_languageSpecifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for languageSpecifier
  // case: toBeCopied == COPY_DATA for languageSpecifier
     std::string languageSpecifier_copy = p_languageSpecifier; 
     result->p_languageSpecifier = languageSpecifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClinkageStartStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClinkageStartStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 70800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClinkageEndStatement::copy ( SgCopyHelp& help) const
   {
     SgClinkageEndStatement* result = NULL;

  // printf ("Copy SgClinkageEndStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClinkageEndStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClinkageEndStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClinkageEndStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClinkageEndStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClinkageEndStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_languageSpecifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for languageSpecifier
  // case: toBeCopied == COPY_DATA for languageSpecifier
     std::string languageSpecifier_copy = p_languageSpecifier; 
     result->p_languageSpecifier = languageSpecifier_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClinkageEndStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClinkageEndStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 71130 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumDeclaration::copy ( SgCopyHelp& help) const
   {
     SgEnumDeclaration* result = NULL;

  // printf ("Copy SgEnumDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEnumDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEnumDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEnumDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEnumDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgEnumType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgEnumType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEnumDeclaration(  startOfConstruct_copy, name_copy, type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): embedded_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedded
  // case: toBeCopied == COPY_DATA for embedded
     bool embedded_copy = p_embedded; 
     result->p_embedded = embedded_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgType* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_enumerators()
     SgInitializedNamePtrList enumerators_copy; 
  // case: listType for enumerators
  // case: listType (typeIsPointerToList == false) for enumerators
     SgInitializedNamePtrList::const_iterator source_enumerators_iterator = get_enumerators().begin(); 
     for ( /* empty by design */; source_enumerators_iterator != get_enumerators().end(); ++source_enumerators_iterator) 
        { 
          SgInitializedName* source_list_element = *source_enumerators_iterator; 
          SgInitializedName* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_enumerators_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          enumerators_copy.push_back(copy_list_element); 
        } 
     result->p_enumerators = enumerators_copy; 
  // case: listType for enumerators
  // case: listType (typeIsPointerToList == false) for enumerators
     SgInitializedNamePtrList::const_iterator enumerators_iterator = result->get_enumerators().begin(); 
     for ( /* empty by design */; enumerators_iterator != result->get_enumerators().end(); ++enumerators_iterator) 
        { 
          SgInitializedName* list_element = *enumerators_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): field_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for field_type
  // case: toBeCopied == CLONE_PTR for field_type
     SgType* field_type_copy = NULL; 
     if ( p_field_type != NULL ) 
        { 
          field_type_copy = new SgType( *p_field_type); 
        } 
       else 
        { 
          field_type_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_field_type != NULL) delete result->p_field_type; 
  /* add assignment to result here */ 
     result->p_field_type = field_type_copy; 
  // Copy non-constructor parameter data member (access function): isScopedEnum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isScopedEnum
  // case: toBeCopied == COPY_DATA for isScopedEnum
     bool isScopedEnum_copy = p_isScopedEnum; 
     result->p_isScopedEnum = isScopedEnum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEnumDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEnumDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 71563 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStmt::copy ( SgCopyHelp& help) const
   {
     SgAsmStmt* result = NULL;

  // printf ("Copy SgAsmStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): assemblyCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for assemblyCode
  // case: toBeCopied == COPY_DATA for assemblyCode
     std::string assemblyCode_copy = p_assemblyCode; 
     result->p_assemblyCode = assemblyCode_copy; 
  // Copy non-constructor parameter data member (access function): useGnuExtendedFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useGnuExtendedFormat
  // case: toBeCopied == COPY_DATA for useGnuExtendedFormat
     bool useGnuExtendedFormat_copy = p_useGnuExtendedFormat; 
     result->p_useGnuExtendedFormat = useGnuExtendedFormat_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operands
     SgExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgExpressionPtrList::const_iterator source_operands_iterator = get_operands().begin(); 
     for ( /* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) 
        { 
          SgExpression* source_list_element = *source_operands_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          operands_copy.push_back(copy_list_element); 
        } 
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgExpressionPtrList::const_iterator operands_iterator = result->get_operands().begin(); 
     for ( /* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) 
        { 
          SgExpression* list_element = *operands_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_clobberRegisterList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clobberRegisterList
  // case: toBeCopied == COPY_DATA for clobberRegisterList
     SgAsmStmt::AsmRegisterNameList clobberRegisterList_copy = p_clobberRegisterList; 
     result->p_clobberRegisterList = clobberRegisterList_copy; 
  // Copy non-constructor parameter data member (access function): isVolatile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isVolatile
  // case: toBeCopied == COPY_DATA for isVolatile
     bool isVolatile_copy = p_isVolatile; 
     result->p_isVolatile = isVolatile_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 71939 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAttributeSpecificationStatement::copy ( SgCopyHelp& help) const
   {
     SgAttributeSpecificationStatement* result = NULL;

  // printf ("Copy SgAttributeSpecificationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAttributeSpecificationStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAttributeSpecificationStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAttributeSpecificationStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAttributeSpecificationStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAttributeSpecificationStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_list
  // case: toBeCopied == COPY_DATA for name_list
     SgStringList name_list_copy = p_name_list; 
     result->p_name_list = name_list_copy; 
  // Copy non-constructor parameter data member (access function): attribute_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_kind
  // case: toBeCopied == COPY_DATA for attribute_kind
     SgAttributeSpecificationStatement::attribute_spec_enum attribute_kind_copy = p_attribute_kind; 
     result->p_attribute_kind = attribute_kind_copy; 
  // Copy non-constructor parameter data member (access function): intent_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for intent
  // case: toBeCopied == COPY_DATA for intent
     int intent_copy = p_intent; 
     result->p_intent = intent_copy; 
  // Copy non-constructor parameter data member (access function): parameter_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameter_list
  // case: toBeCopied == COPY_DATA for parameter_list
     SgExprListExp* parameter_list_copy = p_parameter_list; 
     result->p_parameter_list = parameter_list_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data_statement_group_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_statement_group_list
  // case: toBeCopied == COPY_DATA for data_statement_group_list
     SgDataStatementGroupPtrList data_statement_group_list_copy = p_data_statement_group_list; 
     result->p_data_statement_group_list = data_statement_group_list_copy; 
  // Copy non-constructor parameter data member (access function): bind_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bind_list
  // case: toBeCopied == COPY_DATA for bind_list
     SgExprListExp* bind_list_copy = p_bind_list; 
     result->p_bind_list = bind_list_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dimension_object_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dimension_object_list
  // case: toBeCopied == COPY_DATA for dimension_object_list
     SgDimensionObjectPtrList dimension_object_list_copy = p_dimension_object_list; 
     result->p_dimension_object_list = dimension_object_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAttributeSpecificationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAttributeSpecificationStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 72299 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFormatStatement::copy ( SgCopyHelp& help) const
   {
     SgFormatStatement* result = NULL;

  // printf ("Copy SgFormatStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFormatStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFormatStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFormatStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFormatStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: format_item_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for format_item_list
  // case: toBeCopied == COPY_DATA for format_item_list
     SgFormatItemList* format_item_list_copy = p_format_item_list; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFormatStatement(  startOfConstruct_copy, format_item_list_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFormatStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFormatStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 72628 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateDeclaration::copy ( SgCopyHelp& help) const
   {
     SgTemplateDeclaration* result = NULL;

  // printf ("Copy SgTemplateDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateDeclaration(  startOfConstruct_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): template_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_kind
  // case: toBeCopied == COPY_DATA for template_kind
     SgTemplateDeclaration::template_type_enum template_kind_copy = p_template_kind; 
     result->p_template_kind = template_kind_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 72982 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgTemplateInstantiationDirectiveStatement* result = NULL;

  // printf ("Copy SgTemplateInstantiationDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateInstantiationDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateInstantiationDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateInstantiationDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateInstantiationDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateInstantiationDirectiveStatement(  startOfConstruct_copy, declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): do_not_instantiate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for do_not_instantiate
  // case: toBeCopied == COPY_DATA for do_not_instantiate
     bool do_not_instantiate_copy = p_do_not_instantiate; 
     result->p_do_not_instantiate = do_not_instantiate_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateInstantiationDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateInstantiationDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 73328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUseStatement::copy ( SgCopyHelp& help) const
   {
     SgUseStatement* result = NULL;

  // printf ("Copy SgUseStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUseStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUseStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUseStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUseStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: only_option_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for only_option
  // case: toBeCopied == COPY_DATA for only_option
     bool only_option_copy = p_only_option; 
  // Copy constructor parameter data member: module_nature_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module_nature
  // case: toBeCopied == COPY_DATA for module_nature
     std::string module_nature_copy = p_module_nature; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUseStatement(  startOfConstruct_copy, name_copy, only_option_copy, module_nature_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_rename_list()
     SgRenamePairPtrList rename_list_copy; 
  // case: listType for rename_list
  // case: listType (typeIsPointerToList == false) for rename_list
     SgRenamePairPtrList::const_iterator source_rename_list_iterator = get_rename_list().begin(); 
     for ( /* empty by design */; source_rename_list_iterator != get_rename_list().end(); ++source_rename_list_iterator) 
        { 
          SgRenamePair* source_list_element = *source_rename_list_iterator; 
          SgRenamePair* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgRenamePair*>(help.copyAst(*source_rename_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          rename_list_copy.push_back(copy_list_element); 
        } 
     result->p_rename_list = rename_list_copy; 
  // case: listType for rename_list
  // case: listType (typeIsPointerToList == false) for rename_list
     SgRenamePairPtrList::const_iterator rename_list_iterator = result->get_rename_list().begin(); 
     for ( /* empty by design */; rename_list_iterator != result->get_rename_list().end(); ++rename_list_iterator) 
        { 
          SgRenamePair* list_element = *rename_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): module_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module
  // case: toBeCopied == COPY_DATA for module
     SgModuleStatement* module_copy = p_module; 
     result->p_module = module_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUseStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUseStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 73701 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgParameterStatement::copy ( SgCopyHelp& help) const
   {
     SgParameterStatement* result = NULL;

  // printf ("Copy SgParameterStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgParameterStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgParameterStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgParameterStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgParameterStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgParameterStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgParameterStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgParameterStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 74026 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamespaceDeclarationStatement::copy ( SgCopyHelp& help) const
   {
     SgNamespaceDeclarationStatement* result = NULL;

  // printf ("Copy SgNamespaceDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNamespaceDeclarationStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNamespaceDeclarationStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNamespaceDeclarationStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNamespaceDeclarationStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: definition_copy
     SgNamespaceDefinitionStatement* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgNamespaceDefinitionStatement*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
  // Copy constructor parameter data member: isUnnamedNamespace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnnamedNamespace
  // case: toBeCopied == COPY_DATA for isUnnamedNamespace
     bool isUnnamedNamespace_copy = p_isUnnamedNamespace; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNamespaceDeclarationStatement(  startOfConstruct_copy, name_copy, definition_copy, isUnnamedNamespace_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): isInlinedNamespace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isInlinedNamespace
  // case: toBeCopied == COPY_DATA for isInlinedNamespace
     bool isInlinedNamespace_copy = p_isInlinedNamespace; 
     result->p_isInlinedNamespace = isInlinedNamespace_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNamespaceDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNamespaceDeclarationStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 74380 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEquivalenceStatement::copy ( SgCopyHelp& help) const
   {
     SgEquivalenceStatement* result = NULL;

  // printf ("Copy SgEquivalenceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEquivalenceStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEquivalenceStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEquivalenceStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEquivalenceStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEquivalenceStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): equivalence_set_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for equivalence_set_list
  // case: toBeCopied == COPY_DATA for equivalence_set_list
     SgExprListExp* equivalence_set_list_copy = p_equivalence_set_list; 
     result->p_equivalence_set_list = equivalence_set_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEquivalenceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEquivalenceStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 74710 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInterfaceStatement::copy ( SgCopyHelp& help) const
   {
     SgInterfaceStatement* result = NULL;

  // printf ("Copy SgInterfaceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgInterfaceStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgInterfaceStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgInterfaceStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgInterfaceStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: generic_spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generic_spec
  // case: toBeCopied == COPY_DATA for generic_spec
     SgInterfaceStatement::generic_spec_enum generic_spec_copy = p_generic_spec; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgInterfaceStatement(  startOfConstruct_copy, name_copy, generic_spec_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_interface_body_list()
     SgInterfaceBodyPtrList interface_body_list_copy; 
  // case: listType for interface_body_list
  // case: listType (typeIsPointerToList == false) for interface_body_list
     SgInterfaceBodyPtrList::const_iterator source_interface_body_list_iterator = get_interface_body_list().begin(); 
     for ( /* empty by design */; source_interface_body_list_iterator != get_interface_body_list().end(); ++source_interface_body_list_iterator) 
        { 
          SgInterfaceBody* source_list_element = *source_interface_body_list_iterator; 
          SgInterfaceBody* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInterfaceBody*>(help.copyAst(*source_interface_body_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          interface_body_list_copy.push_back(copy_list_element); 
        } 
     result->p_interface_body_list = interface_body_list_copy; 
  // case: listType for interface_body_list
  // case: listType (typeIsPointerToList == false) for interface_body_list
     SgInterfaceBodyPtrList::const_iterator interface_body_list_iterator = result->get_interface_body_list().begin(); 
     for ( /* empty by design */; interface_body_list_iterator != result->get_interface_body_list().end(); ++interface_body_list_iterator) 
        { 
          SgInterfaceBody* list_element = *interface_body_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgInterfaceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgInterfaceStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 75079 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamespaceAliasDeclarationStatement::copy ( SgCopyHelp& help) const
   {
     SgNamespaceAliasDeclarationStatement* result = NULL;

  // printf ("Copy SgNamespaceAliasDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNamespaceAliasDeclarationStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNamespaceAliasDeclarationStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNamespaceAliasDeclarationStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNamespaceAliasDeclarationStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: namespaceDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for namespaceDeclaration
  // case: toBeCopied == COPY_DATA for namespaceDeclaration
     SgNamespaceDeclarationStatement* namespaceDeclaration_copy = p_namespaceDeclaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNamespaceAliasDeclarationStatement(  startOfConstruct_copy, name_copy, namespaceDeclaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): is_alias_for_another_namespace_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_alias_for_another_namespace_alias
  // case: toBeCopied == COPY_DATA for is_alias_for_another_namespace_alias
     bool is_alias_for_another_namespace_alias_copy = p_is_alias_for_another_namespace_alias; 
     result->p_is_alias_for_another_namespace_alias = is_alias_for_another_namespace_alias_copy; 
  // Copy non-constructor parameter data member (access function): namespaceAliasDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for namespaceAliasDeclaration
  // case: toBeCopied == COPY_DATA for namespaceAliasDeclaration
     SgNamespaceAliasDeclarationStatement* namespaceAliasDeclaration_copy = p_namespaceAliasDeclaration; 
     result->p_namespaceAliasDeclaration = namespaceAliasDeclaration_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNamespaceAliasDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNamespaceAliasDeclarationStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 75437 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCommonBlock::copy ( SgCopyHelp& help) const
   {
     SgCommonBlock* result = NULL;

  // printf ("Copy SgCommonBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCommonBlock*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCommonBlock*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCommonBlock: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCommonBlock*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCommonBlock(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_block_list
     SgCommonBlockObjectPtrList block_list_copy; 
  // case: listType for block_list
  // case: listType (typeIsPointerToList == false) for block_list
     SgCommonBlockObjectPtrList::const_iterator source_block_list_iterator = get_block_list().begin(); 
     for ( /* empty by design */; source_block_list_iterator != get_block_list().end(); ++source_block_list_iterator) 
        { 
          SgCommonBlockObject* source_list_element = *source_block_list_iterator; 
          SgCommonBlockObject* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgCommonBlockObject*>(help.copyAst(*source_block_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          block_list_copy.push_back(copy_list_element); 
        } 
     result->p_block_list = block_list_copy; 
  // case: listType for block_list
  // case: listType (typeIsPointerToList == false) for block_list
     SgCommonBlockObjectPtrList::const_iterator block_list_iterator = result->get_block_list().begin(); 
     for ( /* empty by design */; block_list_iterator != result->get_block_list().end(); ++block_list_iterator) 
        { 
          SgCommonBlockObject* list_element = *block_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCommonBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCommonBlock::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 75793 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypedefDeclaration::copy ( SgCopyHelp& help) const
   {
     SgTypedefDeclaration* result = NULL;

  // printf ("Copy SgTypedefDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypedefDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypedefDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypedefDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypedefDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgTypedefType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgTypedefType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: parent_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_scope
  // case: toBeCopied == COPY_DATA for parent_scope
     SgSymbol* parent_scope_copy = p_parent_scope; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypedefDeclaration(  startOfConstruct_copy, name_copy, base_type_copy, type_copy, declaration_copy, parent_scope_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): typedefBaseTypeContainsDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefBaseTypeContainsDefiningDeclaration
  // case: toBeCopied == COPY_DATA for typedefBaseTypeContainsDefiningDeclaration
     bool typedefBaseTypeContainsDefiningDeclaration_copy = p_typedefBaseTypeContainsDefiningDeclaration; 
     result->p_typedefBaseTypeContainsDefiningDeclaration = typedefBaseTypeContainsDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_base_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_base_type
     int name_qualification_length_for_base_type_copy = p_name_qualification_length_for_base_type; 
     result->p_name_qualification_length_for_base_type = name_qualification_length_for_base_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_base_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_base_type
     bool type_elaboration_required_for_base_type_copy = p_type_elaboration_required_for_base_type; 
     result->p_type_elaboration_required_for_base_type = type_elaboration_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_base_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_base_type
     bool global_qualification_required_for_base_type_copy = p_global_qualification_required_for_base_type; 
     result->p_global_qualification_required_for_base_type = global_qualification_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypedefDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypedefDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 76218 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateTypedefDeclaration::copy ( SgCopyHelp& help) const
   {
     SgTemplateTypedefDeclaration* result = NULL;

  // printf ("Copy SgTemplateTypedefDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateTypedefDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateTypedefDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateTypedefDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateTypedefDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgTypedefType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgTypedefType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: parent_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_scope
  // case: toBeCopied == COPY_DATA for parent_scope
     SgSymbol* parent_scope_copy = p_parent_scope; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateTypedefDeclaration(  startOfConstruct_copy, name_copy, base_type_copy, type_copy, declaration_copy, parent_scope_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): typedefBaseTypeContainsDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefBaseTypeContainsDefiningDeclaration
  // case: toBeCopied == COPY_DATA for typedefBaseTypeContainsDefiningDeclaration
     bool typedefBaseTypeContainsDefiningDeclaration_copy = p_typedefBaseTypeContainsDefiningDeclaration; 
     result->p_typedefBaseTypeContainsDefiningDeclaration = typedefBaseTypeContainsDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_base_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_base_type
     int name_qualification_length_for_base_type_copy = p_name_qualification_length_for_base_type; 
     result->p_name_qualification_length_for_base_type = name_qualification_length_for_base_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_base_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_base_type
     bool type_elaboration_required_for_base_type_copy = p_type_elaboration_required_for_base_type; 
     result->p_type_elaboration_required_for_base_type = type_elaboration_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_base_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_base_type
     bool global_qualification_required_for_base_type_copy = p_global_qualification_required_for_base_type; 
     result->p_global_qualification_required_for_base_type = global_qualification_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateTypedefDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateTypedefDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 76663 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationTypedefDeclaration::copy ( SgCopyHelp& help) const
   {
     SgTemplateInstantiationTypedefDeclaration* result = NULL;

  // printf ("Copy SgTemplateInstantiationTypedefDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateInstantiationTypedefDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateInstantiationTypedefDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateInstantiationTypedefDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateInstantiationTypedefDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgTypedefType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgTypedefType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: parent_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_scope
  // case: toBeCopied == COPY_DATA for parent_scope
     SgSymbol* parent_scope_copy = p_parent_scope; 
  // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateTypedefDeclaration* templateDeclaration_copy = p_templateDeclaration; 
  // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateInstantiationTypedefDeclaration(  startOfConstruct_copy, name_copy, base_type_copy, type_copy, declaration_copy, parent_scope_copy, templateDeclaration_copy, templateArguments_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): typedefBaseTypeContainsDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefBaseTypeContainsDefiningDeclaration
  // case: toBeCopied == COPY_DATA for typedefBaseTypeContainsDefiningDeclaration
     bool typedefBaseTypeContainsDefiningDeclaration_copy = p_typedefBaseTypeContainsDefiningDeclaration; 
     result->p_typedefBaseTypeContainsDefiningDeclaration = typedefBaseTypeContainsDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_base_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_base_type
     int name_qualification_length_for_base_type_copy = p_name_qualification_length_for_base_type; 
     result->p_name_qualification_length_for_base_type = name_qualification_length_for_base_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_base_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_base_type
     bool type_elaboration_required_for_base_type_copy = p_type_elaboration_required_for_base_type; 
     result->p_type_elaboration_required_for_base_type = type_elaboration_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_base_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_base_type
     bool global_qualification_required_for_base_type_copy = p_global_qualification_required_for_base_type; 
     result->p_global_qualification_required_for_base_type = global_qualification_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): templateHeader_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateHeader
  // case: toBeCopied == COPY_DATA for templateHeader
     SgName templateHeader_copy = p_templateHeader; 
     result->p_templateHeader = templateHeader_copy; 
  // Copy non-constructor parameter data member (access function): nameResetFromMangledForm_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameResetFromMangledForm
  // case: toBeCopied == COPY_DATA for nameResetFromMangledForm
     bool nameResetFromMangledForm_copy = p_nameResetFromMangledForm; 
     result->p_nameResetFromMangledForm = nameResetFromMangledForm_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateInstantiationTypedefDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateInstantiationTypedefDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 77111 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStatementFunctionStatement::copy ( SgCopyHelp& help) const
   {
     SgStatementFunctionStatement* result = NULL;

  // printf ("Copy SgStatementFunctionStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStatementFunctionStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStatementFunctionStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStatementFunctionStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStatementFunctionStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: function_copy
     SgFunctionDeclaration* function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)function
          if (get_function() != NULL) 
             { 
               function_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_function())); 
             } 
            else 
             { 
               function_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStatementFunctionStatement(  startOfConstruct_copy, function_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)function
          if ( (function_copy != NULL) && (function_copy->get_parent() == NULL) && (isSgType(function_copy) == NULL) ) 
             { 
               function_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStatementFunctionStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStatementFunctionStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 77468 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCtorInitializerList::copy ( SgCopyHelp& help) const
   {
     SgCtorInitializerList* result = NULL;

  // printf ("Copy SgCtorInitializerList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCtorInitializerList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCtorInitializerList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCtorInitializerList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCtorInitializerList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCtorInitializerList(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_ctors()
     SgInitializedNamePtrList ctors_copy; 
  // case: listType for ctors
  // case: listType (typeIsPointerToList == false) for ctors
     SgInitializedNamePtrList::const_iterator source_ctors_iterator = get_ctors().begin(); 
     for ( /* empty by design */; source_ctors_iterator != get_ctors().end(); ++source_ctors_iterator) 
        { 
          SgInitializedName* source_list_element = *source_ctors_iterator; 
          SgInitializedName* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_ctors_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          ctors_copy.push_back(copy_list_element); 
        } 
     result->p_ctors = ctors_copy; 
  // case: listType for ctors
  // case: listType (typeIsPointerToList == false) for ctors
     SgInitializedNamePtrList::const_iterator ctors_iterator = result->get_ctors().begin(); 
     for ( /* empty by design */; ctors_iterator != result->get_ctors().end(); ++ctors_iterator) 
        { 
          SgInitializedName* list_element = *ctors_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCtorInitializerList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCtorInitializerList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 77824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPragmaDeclaration::copy ( SgCopyHelp& help) const
   {
     SgPragmaDeclaration* result = NULL;

  // printf ("Copy SgPragmaDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPragmaDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPragmaDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPragmaDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPragmaDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: pragma_copy
     SgPragma* pragma_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pragma
          if (get_pragma() != NULL) 
             { 
               pragma_copy = static_cast<SgPragma*>(help.copyAst(get_pragma())); 
             } 
            else 
             { 
               pragma_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPragmaDeclaration(  startOfConstruct_copy, pragma_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)pragma
          if ( (pragma_copy != NULL) && (pragma_copy->get_parent() == NULL) && (isSgType(pragma_copy) == NULL) ) 
             { 
               pragma_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPragmaDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPragmaDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 78165 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUsingDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgUsingDirectiveStatement* result = NULL;

  // printf ("Copy SgUsingDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUsingDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUsingDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUsingDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUsingDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: namespaceDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for namespaceDeclaration
  // case: toBeCopied == COPY_DATA for namespaceDeclaration
     SgNamespaceDeclarationStatement* namespaceDeclaration_copy = p_namespaceDeclaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUsingDirectiveStatement(  startOfConstruct_copy, namespaceDeclaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUsingDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUsingDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 78509 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassDeclaration::copy ( SgCopyHelp& help) const
   {
     SgClassDeclaration* result = NULL;

  // printf ("Copy SgClassDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClassDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClassDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClassDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClassDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClassDeclaration(  startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClassDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClassDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 78964 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateClassDeclaration::copy ( SgCopyHelp& help) const
   {
     SgTemplateClassDeclaration* result = NULL;

  // printf ("Copy SgTemplateClassDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateClassDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateClassDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateClassDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateClassDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateClassDeclaration(  startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateClassDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateClassDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 79444 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationDecl::copy ( SgCopyHelp& help) const
   {
     SgTemplateInstantiationDecl* result = NULL;

  // printf ("Copy SgTemplateInstantiationDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateInstantiationDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateInstantiationDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateInstantiationDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateInstantiationDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
  // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateClassDeclaration* templateDeclaration_copy = p_templateDeclaration; 
  // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateInstantiationDecl(  startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy, templateDeclaration_copy, templateArguments_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): templateHeader_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateHeader
  // case: toBeCopied == COPY_DATA for templateHeader
     SgName templateHeader_copy = p_templateHeader; 
     result->p_templateHeader = templateHeader_copy; 
  // Copy non-constructor parameter data member (access function): nameResetFromMangledForm_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameResetFromMangledForm
  // case: toBeCopied == COPY_DATA for nameResetFromMangledForm
     bool nameResetFromMangledForm_copy = p_nameResetFromMangledForm; 
     result->p_nameResetFromMangledForm = nameResetFromMangledForm_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateInstantiationDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateInstantiationDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 79922 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDerivedTypeStatement::copy ( SgCopyHelp& help) const
   {
     SgDerivedTypeStatement* result = NULL;

  // printf ("Copy SgDerivedTypeStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDerivedTypeStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDerivedTypeStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDerivedTypeStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDerivedTypeStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDerivedTypeStatement(  startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDerivedTypeStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDerivedTypeStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 80382 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModuleStatement::copy ( SgCopyHelp& help) const
   {
     SgModuleStatement* result = NULL;

  // printf ("Copy SgModuleStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgModuleStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgModuleStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgModuleStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgModuleStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgModuleStatement(  startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgModuleStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgModuleStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 80842 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaPackageDeclaration::copy ( SgCopyHelp& help) const
   {
     SgJavaPackageDeclaration* result = NULL;

  // printf ("Copy SgJavaPackageDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaPackageDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaPackageDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaPackageDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaPackageDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaPackageDeclaration(  startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaPackageDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaPackageDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 81297 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialTableStatement::copy ( SgCopyHelp& help) const
   {
     SgJovialTableStatement* result = NULL;

  // printf ("Copy SgJovialTableStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialTableStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialTableStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialTableStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialTableStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = NULL; 
     if ( p_type != NULL ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = NULL; 
        } 
  // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialTableStatement(  startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (access function): table_entry_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for table_entry_size
  // case: toBeCopied == COPY_DATA for table_entry_size
     SgExpression* table_entry_size_copy = p_table_entry_size; 
     result->p_table_entry_size = table_entry_size_copy; 
  // Copy non-constructor parameter data member (access function): has_table_entry_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_table_entry_size
  // case: toBeCopied == COPY_DATA for has_table_entry_size
     bool has_table_entry_size_copy = p_has_table_entry_size; 
     result->p_has_table_entry_size = has_table_entry_size_copy; 
  // Copy non-constructor parameter data member (access function): words_per_entry_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for words_per_entry
  // case: toBeCopied == COPY_DATA for words_per_entry
     SgJovialTableStatement::WordsPerEntry words_per_entry_copy = p_words_per_entry; 
     result->p_words_per_entry = words_per_entry_copy; 
  // Copy non-constructor parameter data member (access function): like_table_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for like_table_name
  // case: toBeCopied == COPY_DATA for like_table_name
     std::string like_table_name_copy = p_like_table_name; 
     result->p_like_table_name = like_table_name_copy; 
  // Copy non-constructor parameter data member (access function): has_like_option_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_like_option
  // case: toBeCopied == COPY_DATA for has_like_option
     bool has_like_option_copy = p_has_like_option; 
     result->p_has_like_option = has_like_option_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialTableStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialTableStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 81777 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImplicitStatement::copy ( SgCopyHelp& help) const
   {
     SgImplicitStatement* result = NULL;

  // printf ("Copy SgImplicitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgImplicitStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgImplicitStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgImplicitStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgImplicitStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for implicit_none
  // case: toBeCopied == COPY_DATA for implicit_none
     bool implicit_none_copy = p_implicit_none; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgImplicitStatement(  startOfConstruct_copy, implicit_none_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): implicit_spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for implicit_spec
  // case: toBeCopied == COPY_DATA for implicit_spec
     SgImplicitStatement::implicit_spec_enum implicit_spec_copy = p_implicit_spec; 
     result->p_implicit_spec = implicit_spec_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgInitializedNamePtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgInitializedNamePtrList::const_iterator source_variables_iterator = get_variables().begin(); 
     for ( /* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) 
        { 
          SgInitializedName* source_list_element = *source_variables_iterator; 
          SgInitializedName* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          variables_copy.push_back(copy_list_element); 
        } 
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgInitializedNamePtrList::const_iterator variables_iterator = result->get_variables().begin(); 
     for ( /* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) 
        { 
          SgInitializedName* list_element = *variables_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgImplicitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgImplicitStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 82142 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUsingDeclarationStatement::copy ( SgCopyHelp& help) const
   {
     SgUsingDeclarationStatement* result = NULL;

  // printf ("Copy SgUsingDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUsingDeclarationStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUsingDeclarationStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUsingDeclarationStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUsingDeclarationStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgDeclarationStatement* declaration_copy = p_declaration; 
  // Copy constructor parameter data member: initializedName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for initializedName
  // case: toBeCopied == COPY_DATA for initializedName
     SgInitializedName* initializedName_copy = p_initializedName; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUsingDeclarationStatement(  startOfConstruct_copy, declaration_copy, initializedName_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): is_inheriting_constructor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_inheriting_constructor
  // case: toBeCopied == COPY_DATA for is_inheriting_constructor
     bool is_inheriting_constructor_copy = p_is_inheriting_constructor; 
     result->p_is_inheriting_constructor = is_inheriting_constructor_copy; 
  // Copy non-constructor parameter data member (access function): adaTypeAttribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaTypeAttribute
  // case: toBeCopied == COPY_DATA for adaTypeAttribute
     SgName adaTypeAttribute_copy = p_adaTypeAttribute; 
     result->p_adaTypeAttribute = adaTypeAttribute_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUsingDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUsingDeclarationStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 82500 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamelistStatement::copy ( SgCopyHelp& help) const
   {
     SgNamelistStatement* result = NULL;

  // printf ("Copy SgNamelistStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNamelistStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNamelistStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNamelistStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNamelistStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNamelistStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_group_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for group_list
  // case: toBeCopied == COPY_DATA for group_list
     SgNameGroupPtrList group_list_copy = p_group_list; 
     result->p_group_list = group_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNamelistStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNamelistStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 82830 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImportStatement::copy ( SgCopyHelp& help) const
   {
     SgImportStatement* result = NULL;

  // printf ("Copy SgImportStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgImportStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgImportStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgImportStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgImportStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgImportStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_import_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for import_list
  // case: toBeCopied == COPY_DATA for import_list
     SgExpressionPtrList import_list_copy = p_import_list; 
     result->p_import_list = import_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgImportStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgImportStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 83160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionDeclaration::copy ( SgCopyHelp& help) const
   {
     SgFunctionDeclaration* result = NULL;

  // printf ("Copy SgFunctionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionDeclaration(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 83702 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateFunctionDeclaration::copy ( SgCopyHelp& help) const
   {
     SgTemplateFunctionDeclaration* result = NULL;

  // printf ("Copy SgTemplateFunctionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateFunctionDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateFunctionDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateFunctionDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateFunctionDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateFunctionDeclaration(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): string_represents_function_body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_represents_function_body
  // case: toBeCopied == COPY_DATA for string_represents_function_body
     bool string_represents_function_body_copy = p_string_represents_function_body; 
     result->p_string_represents_function_body = string_represents_function_body_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateFunctionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateFunctionDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 84269 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMemberFunctionDeclaration::copy ( SgCopyHelp& help) const
   {
     SgMemberFunctionDeclaration* result = NULL;

  // printf ("Copy SgMemberFunctionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMemberFunctionDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMemberFunctionDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMemberFunctionDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMemberFunctionDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMemberFunctionDeclaration(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_associatedClassDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedClassDeclaration
  // case: toBeCopied == COPY_DATA for associatedClassDeclaration
     SgDeclarationStatement* associatedClassDeclaration_copy = p_associatedClassDeclaration; 
     result->p_associatedClassDeclaration = associatedClassDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMemberFunctionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMemberFunctionDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 84816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateMemberFunctionDeclaration::copy ( SgCopyHelp& help) const
   {
     SgTemplateMemberFunctionDeclaration* result = NULL;

  // printf ("Copy SgTemplateMemberFunctionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateMemberFunctionDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateMemberFunctionDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateMemberFunctionDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateMemberFunctionDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateMemberFunctionDeclaration(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_associatedClassDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedClassDeclaration
  // case: toBeCopied == COPY_DATA for associatedClassDeclaration
     SgDeclarationStatement* associatedClassDeclaration_copy = p_associatedClassDeclaration; 
     result->p_associatedClassDeclaration = associatedClassDeclaration_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): string_represents_function_body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_represents_function_body
  // case: toBeCopied == COPY_DATA for string_represents_function_body
     bool string_represents_function_body_copy = p_string_represents_function_body; 
     result->p_string_represents_function_body = string_represents_function_body_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateMemberFunctionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateMemberFunctionDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 85388 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationMemberFunctionDecl::copy ( SgCopyHelp& help) const
   {
     SgTemplateInstantiationMemberFunctionDecl* result = NULL;

  // printf ("Copy SgTemplateInstantiationMemberFunctionDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateInstantiationMemberFunctionDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateInstantiationMemberFunctionDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateInstantiationMemberFunctionDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateInstantiationMemberFunctionDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
  // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateMemberFunctionDeclaration* templateDeclaration_copy = p_templateDeclaration; 
  // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateInstantiationMemberFunctionDecl(  startOfConstruct_copy, name_copy, type_copy, definition_copy, templateDeclaration_copy, templateArguments_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_associatedClassDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedClassDeclaration
  // case: toBeCopied == COPY_DATA for associatedClassDeclaration
     SgDeclarationStatement* associatedClassDeclaration_copy = p_associatedClassDeclaration; 
     result->p_associatedClassDeclaration = associatedClassDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): nameResetFromMangledForm_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameResetFromMangledForm
  // case: toBeCopied == COPY_DATA for nameResetFromMangledForm
     bool nameResetFromMangledForm_copy = p_nameResetFromMangledForm; 
     result->p_nameResetFromMangledForm = nameResetFromMangledForm_copy; 
  // Copy non-constructor parameter data member (access function): template_argument_list_is_explicit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_argument_list_is_explicit
  // case: toBeCopied == COPY_DATA for template_argument_list_is_explicit
     bool template_argument_list_is_explicit_copy = p_template_argument_list_is_explicit; 
     result->p_template_argument_list_is_explicit = template_argument_list_is_explicit_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateInstantiationMemberFunctionDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateInstantiationMemberFunctionDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 85958 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationFunctionDecl::copy ( SgCopyHelp& help) const
   {
     SgTemplateInstantiationFunctionDecl* result = NULL;

  // printf ("Copy SgTemplateInstantiationFunctionDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateInstantiationFunctionDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateInstantiationFunctionDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateInstantiationFunctionDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateInstantiationFunctionDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
  // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateFunctionDeclaration* templateDeclaration_copy = p_templateDeclaration; 
  // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateInstantiationFunctionDecl(  startOfConstruct_copy, name_copy, type_copy, definition_copy, templateDeclaration_copy, templateArguments_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): nameResetFromMangledForm_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameResetFromMangledForm
  // case: toBeCopied == COPY_DATA for nameResetFromMangledForm
     bool nameResetFromMangledForm_copy = p_nameResetFromMangledForm; 
     result->p_nameResetFromMangledForm = nameResetFromMangledForm_copy; 
  // Copy non-constructor parameter data member (access function): template_argument_list_is_explicit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_argument_list_is_explicit
  // case: toBeCopied == COPY_DATA for template_argument_list_is_explicit
     bool template_argument_list_is_explicit_copy = p_template_argument_list_is_explicit; 
     result->p_template_argument_list_is_explicit = template_argument_list_is_explicit_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateInstantiationFunctionDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateInstantiationFunctionDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 86523 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgProgramHeaderStatement::copy ( SgCopyHelp& help) const
   {
     SgProgramHeaderStatement* result = NULL;

  // printf ("Copy SgProgramHeaderStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgProgramHeaderStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgProgramHeaderStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgProgramHeaderStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgProgramHeaderStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgProgramHeaderStatement(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): program_statement_explicit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for program_statement_explicit
  // case: toBeCopied == COPY_DATA for program_statement_explicit
     bool program_statement_explicit_copy = p_program_statement_explicit; 
     result->p_program_statement_explicit = program_statement_explicit_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgProgramHeaderStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgProgramHeaderStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 87075 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgProcedureHeaderStatement::copy ( SgCopyHelp& help) const
   {
     SgProcedureHeaderStatement* result = NULL;

  // printf ("Copy SgProcedureHeaderStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgProcedureHeaderStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgProcedureHeaderStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgProcedureHeaderStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgProcedureHeaderStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgProcedureHeaderStatement(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): subprogram_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for subprogram_kind
  // case: toBeCopied == COPY_DATA for subprogram_kind
     SgProcedureHeaderStatement::subprogram_kind_enum subprogram_kind_copy = p_subprogram_kind; 
     result->p_subprogram_kind = subprogram_kind_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): result_name_copy
     SgInitializedName* result_name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)result_name
          if (get_result_name() != NULL) 
             { 
               result_name_copy = static_cast<SgInitializedName*>(help.copyAst(get_result_name())); 
             } 
            else 
             { 
               result_name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_result_name != NULL) delete result->p_result_name; 
     result->p_result_name = result_name_copy; 
  // case: not a listType for (using conditionalToSetParent)result_name
          if ( (result_name_copy != NULL) && (result_name_copy->get_parent() == NULL) && (isSgType(result_name_copy) == NULL) ) 
             { 
               result_name_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgProcedureHeaderStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgProcedureHeaderStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 87646 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEntryStatement::copy ( SgCopyHelp& help) const
   {
     SgEntryStatement* result = NULL;

  // printf ("Copy SgEntryStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEntryStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEntryStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEntryStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEntryStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEntryStatement(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): result_name_copy
     SgInitializedName* result_name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)result_name
          if (get_result_name() != NULL) 
             { 
               result_name_copy = static_cast<SgInitializedName*>(help.copyAst(get_result_name())); 
             } 
            else 
             { 
               result_name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_result_name != NULL) delete result->p_result_name; 
     result->p_result_name = result_name_copy; 
  // case: not a listType for (using conditionalToSetParent)result_name
          if ( (result_name_copy != NULL) && (result_name_copy->get_parent() == NULL) && (isSgType(result_name_copy) == NULL) ) 
             { 
               result_name_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEntryStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEntryStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 88207 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaEntryDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaEntryDecl* result = NULL;

  // printf ("Copy SgAdaEntryDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaEntryDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaEntryDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaEntryDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaEntryDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaEntryDecl(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): entryIndex_copy
     SgInitializedName* entryIndex_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entryIndex
          if (get_entryIndex() != NULL) 
             { 
               entryIndex_copy = static_cast<SgInitializedName*>(help.copyAst(get_entryIndex())); 
             } 
            else 
             { 
               entryIndex_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entryIndex != NULL) delete result->p_entryIndex; 
     result->p_entryIndex = entryIndex_copy; 
  // case: not a listType for (using conditionalToSetParent)entryIndex
          if ( (entryIndex_copy != NULL) && (entryIndex_copy->get_parent() == NULL) && (isSgType(entryIndex_copy) == NULL) ) 
             { 
               entryIndex_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): entryBarrier_copy
     SgExpression* entryBarrier_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entryBarrier
          if (get_entryBarrier() != NULL) 
             { 
               entryBarrier_copy = static_cast<SgExpression*>(help.copyAst(get_entryBarrier())); 
             } 
            else 
             { 
               entryBarrier_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entryBarrier != NULL) delete result->p_entryBarrier; 
     result->p_entryBarrier = entryBarrier_copy; 
  // case: not a listType for (using conditionalToSetParent)entryBarrier
          if ( (entryBarrier_copy != NULL) && (entryBarrier_copy->get_parent() == NULL) && (isSgType(entryBarrier_copy) == NULL) ) 
             { 
               entryBarrier_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaEntryDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaEntryDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 88787 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFunctionRenamingDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaFunctionRenamingDecl* result = NULL;

  // printf ("Copy SgAdaFunctionRenamingDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaFunctionRenamingDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaFunctionRenamingDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaFunctionRenamingDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaFunctionRenamingDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
  // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaFunctionRenamingDecl(  startOfConstruct_copy, name_copy, type_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != NULL) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != NULL) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
          if ( (parameterList_copy != NULL) && (parameterList_copy->get_parent() == NULL) && (isSgType(parameterList_copy) == NULL) ) 
             { 
               parameterList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != NULL) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != NULL) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
          if ( (decoratorList_copy != NULL) && (decoratorList_copy->get_parent() == NULL) && (isSgType(decoratorList_copy) == NULL) ) 
             { 
               decoratorList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): renamed_function_copy
     SgExpression* renamed_function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)renamed_function
          if (get_renamed_function() != NULL) 
             { 
               renamed_function_copy = static_cast<SgExpression*>(help.copyAst(get_renamed_function())); 
             } 
            else 
             { 
               renamed_function_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_renamed_function != NULL) delete result->p_renamed_function; 
     result->p_renamed_function = renamed_function_copy; 
  // case: not a listType for (using conditionalToSetParent)renamed_function
          if ( (renamed_function_copy != NULL) && (renamed_function_copy->get_parent() == NULL) && (isSgType(renamed_function_copy) == NULL) ) 
             { 
               renamed_function_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaFunctionRenamingDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaFunctionRenamingDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 89348 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgContainsStatement::copy ( SgCopyHelp& help) const
   {
     SgContainsStatement* result = NULL;

  // printf ("Copy SgContainsStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgContainsStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgContainsStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgContainsStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgContainsStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgContainsStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgContainsStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgContainsStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 89673 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgC_PreprocessorDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgC_PreprocessorDirectiveStatement* result = NULL;

  // printf ("Copy SgC_PreprocessorDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgC_PreprocessorDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgC_PreprocessorDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgC_PreprocessorDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgC_PreprocessorDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgC_PreprocessorDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgC_PreprocessorDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgC_PreprocessorDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 90003 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncludeDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgIncludeDirectiveStatement* result = NULL;

  // printf ("Copy SgIncludeDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIncludeDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIncludeDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIncludeDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIncludeDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIncludeDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 
  // Copy non-constructor parameter data member (access function): headerFileBody_copy
     SgHeaderFileBody* headerFileBody_copy; 
  // case: not a listType for (using conditionalToCopyVariable)headerFileBody
          if (get_headerFileBody() != NULL) 
             { 
               headerFileBody_copy = static_cast<SgHeaderFileBody*>(help.copyAst(get_headerFileBody())); 
             } 
            else 
             { 
               headerFileBody_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_headerFileBody != NULL) delete result->p_headerFileBody; 
     result->p_headerFileBody = headerFileBody_copy; 
  // case: not a listType for (using conditionalToSetParent)headerFileBody
          if ( (headerFileBody_copy != NULL) && (headerFileBody_copy->get_parent() == NULL) && (isSgType(headerFileBody_copy) == NULL) ) 
             { 
               headerFileBody_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): include_file_heirarchy_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for include_file_heirarchy
  // case: toBeCopied == COPY_DATA for include_file_heirarchy
     SgIncludeFile* include_file_heirarchy_copy = p_include_file_heirarchy; 
     result->p_include_file_heirarchy = include_file_heirarchy_copy; 
  // Copy non-constructor parameter data member (access function): name_used_in_include_directive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_used_in_include_directive
  // case: toBeCopied == COPY_DATA for name_used_in_include_directive
     std::string name_used_in_include_directive_copy = p_name_used_in_include_directive; 
     result->p_name_used_in_include_directive = name_used_in_include_directive_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIncludeDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIncludeDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 90362 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDefineDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgDefineDirectiveStatement* result = NULL;

  // printf ("Copy SgDefineDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDefineDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDefineDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDefineDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDefineDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDefineDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDefineDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDefineDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 90692 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUndefDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgUndefDirectiveStatement* result = NULL;

  // printf ("Copy SgUndefDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUndefDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUndefDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUndefDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUndefDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUndefDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUndefDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUndefDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 91022 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIfdefDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgIfdefDirectiveStatement* result = NULL;

  // printf ("Copy SgIfdefDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIfdefDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIfdefDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIfdefDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIfdefDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIfdefDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIfdefDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIfdefDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 91352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIfndefDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgIfndefDirectiveStatement* result = NULL;

  // printf ("Copy SgIfndefDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIfndefDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIfndefDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIfndefDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIfndefDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIfndefDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIfndefDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIfndefDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 91682 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIfDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgIfDirectiveStatement* result = NULL;

  // printf ("Copy SgIfDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIfDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIfDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIfDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIfDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIfDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIfDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIfDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 92012 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeadIfDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgDeadIfDirectiveStatement* result = NULL;

  // printf ("Copy SgDeadIfDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDeadIfDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDeadIfDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDeadIfDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDeadIfDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDeadIfDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDeadIfDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDeadIfDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 92342 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElseDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgElseDirectiveStatement* result = NULL;

  // printf ("Copy SgElseDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElseDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElseDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElseDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElseDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElseDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElseDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElseDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 92672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElseifDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgElseifDirectiveStatement* result = NULL;

  // printf ("Copy SgElseifDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElseifDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElseifDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElseifDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElseifDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElseifDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElseifDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElseifDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 93002 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEndifDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgEndifDirectiveStatement* result = NULL;

  // printf ("Copy SgEndifDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEndifDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEndifDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEndifDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEndifDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEndifDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEndifDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEndifDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 93332 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLineDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgLineDirectiveStatement* result = NULL;

  // printf ("Copy SgLineDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLineDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLineDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLineDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLineDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLineDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLineDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLineDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 93662 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWarningDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgWarningDirectiveStatement* result = NULL;

  // printf ("Copy SgWarningDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgWarningDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgWarningDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgWarningDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgWarningDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgWarningDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgWarningDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgWarningDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 93992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgErrorDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgErrorDirectiveStatement* result = NULL;

  // printf ("Copy SgErrorDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgErrorDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgErrorDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgErrorDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgErrorDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgErrorDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgErrorDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgErrorDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 94322 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEmptyDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgEmptyDirectiveStatement* result = NULL;

  // printf ("Copy SgEmptyDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEmptyDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEmptyDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEmptyDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEmptyDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEmptyDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEmptyDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEmptyDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 94652 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncludeNextDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgIncludeNextDirectiveStatement* result = NULL;

  // printf ("Copy SgIncludeNextDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIncludeNextDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIncludeNextDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIncludeNextDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIncludeNextDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIncludeNextDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIncludeNextDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIncludeNextDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 94982 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIdentDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgIdentDirectiveStatement* result = NULL;

  // printf ("Copy SgIdentDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIdentDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIdentDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIdentDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIdentDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIdentDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIdentDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIdentDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 95312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLinemarkerDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgLinemarkerDirectiveStatement* result = NULL;

  // printf ("Copy SgLinemarkerDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLinemarkerDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLinemarkerDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLinemarkerDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLinemarkerDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLinemarkerDirectiveStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 
  // Copy non-constructor parameter data member (access function): linenumber_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linenumber
  // case: toBeCopied == COPY_DATA for linenumber
     int linenumber_copy = p_linenumber; 
     result->p_linenumber = linenumber_copy; 
  // Copy non-constructor parameter data member (access function): filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for filename
  // case: toBeCopied == COPY_DATA for filename
     std::string filename_copy = p_filename; 
     result->p_filename = filename_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_flaglist()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flaglist
  // case: toBeCopied == COPY_DATA for flaglist
     SgUnsignedCharList flaglist_copy = p_flaglist; 
     result->p_flaglist = flaglist_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLinemarkerDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLinemarkerDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 95657 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpThreadprivateStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpThreadprivateStatement* result = NULL;

  // printf ("Copy SgOmpThreadprivateStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpThreadprivateStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpThreadprivateStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpThreadprivateStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpThreadprivateStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpThreadprivateStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgVarRefExpPtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgVarRefExpPtrList::const_iterator source_variables_iterator = get_variables().begin(); 
     for ( /* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) 
        { 
          SgVarRefExp* source_list_element = *source_variables_iterator; 
          SgVarRefExp* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgVarRefExp*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          variables_copy.push_back(copy_list_element); 
        } 
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgVarRefExpPtrList::const_iterator variables_iterator = result->get_variables().begin(); 
     for ( /* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) 
        { 
          SgVarRefExp* list_element = *variables_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpThreadprivateStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpThreadprivateStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 96013 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFortranIncludeLine::copy ( SgCopyHelp& help) const
   {
     SgFortranIncludeLine* result = NULL;

  // printf ("Copy SgFortranIncludeLine = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFortranIncludeLine*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFortranIncludeLine*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFortranIncludeLine: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFortranIncludeLine*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for filename
  // case: toBeCopied == COPY_DATA for filename
     std::string filename_copy = p_filename; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFortranIncludeLine(  startOfConstruct_copy, filename_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFortranIncludeLine = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFortranIncludeLine::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 96342 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaImportStatement::copy ( SgCopyHelp& help) const
   {
     SgJavaImportStatement* result = NULL;

  // printf ("Copy SgJavaImportStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaImportStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaImportStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaImportStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaImportStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: path_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for path
  // case: toBeCopied == COPY_DATA for path
     SgName path_copy = p_path; 
  // Copy constructor parameter data member: containsWildCard_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsWildCard
  // case: toBeCopied == COPY_DATA for containsWildCard
     bool containsWildCard_copy = p_containsWildCard; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaImportStatement(  startOfConstruct_copy, path_copy, containsWildCard_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaImportStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaImportStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 96675 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaPackageStatement::copy ( SgCopyHelp& help) const
   {
     SgJavaPackageStatement* result = NULL;

  // printf ("Copy SgJavaPackageStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaPackageStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaPackageStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaPackageStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaPackageStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaPackageStatement(  startOfConstruct_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaPackageStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaPackageStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 97004 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStmtDeclarationStatement::copy ( SgCopyHelp& help) const
   {
     SgStmtDeclarationStatement* result = NULL;

  // printf ("Copy SgStmtDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStmtDeclarationStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStmtDeclarationStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStmtDeclarationStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStmtDeclarationStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: statement_copy
     SgStatement* statement_copy; 
  // case: not a listType for (using conditionalToCopyVariable)statement
          if (get_statement() != NULL) 
             { 
               statement_copy = static_cast<SgStatement*>(help.copyAst(get_statement())); 
             } 
            else 
             { 
               statement_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStmtDeclarationStatement(  startOfConstruct_copy, statement_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)statement
          if ( (statement_copy != NULL) && (statement_copy->get_parent() == NULL) && (isSgType(statement_copy) == NULL) ) 
             { 
               statement_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStmtDeclarationStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStmtDeclarationStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 97345 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStaticAssertionDeclaration::copy ( SgCopyHelp& help) const
   {
     SgStaticAssertionDeclaration* result = NULL;

  // printf ("Copy SgStaticAssertionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStaticAssertionDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStaticAssertionDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStaticAssertionDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStaticAssertionDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: condition_copy
     SgExpression* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != NULL) 
             { 
               condition_copy = static_cast<SgExpression*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = NULL; 
             } 
  // Copy constructor parameter data member: string_literal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_literal
  // case: toBeCopied == COPY_DATA for string_literal
     SgName string_literal_copy = p_string_literal; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStaticAssertionDeclaration(  startOfConstruct_copy, condition_copy, string_literal_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
          if ( (condition_copy != NULL) && (condition_copy->get_parent() == NULL) && (isSgType(condition_copy) == NULL) ) 
             { 
               condition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStaticAssertionDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStaticAssertionDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 97690 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDeclareSimdStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpDeclareSimdStatement* result = NULL;

  // printf ("Copy SgOmpDeclareSimdStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpDeclareSimdStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpDeclareSimdStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpDeclareSimdStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpDeclareSimdStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpDeclareSimdStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpDeclareSimdStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpDeclareSimdStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 98046 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMicrosoftAttributeDeclaration::copy ( SgCopyHelp& help) const
   {
     SgMicrosoftAttributeDeclaration* result = NULL;

  // printf ("Copy SgMicrosoftAttributeDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMicrosoftAttributeDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMicrosoftAttributeDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMicrosoftAttributeDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMicrosoftAttributeDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: attribute_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_string
  // case: toBeCopied == COPY_DATA for attribute_string
     SgName attribute_string_copy = p_attribute_string; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMicrosoftAttributeDeclaration(  startOfConstruct_copy, attribute_string_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMicrosoftAttributeDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMicrosoftAttributeDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 98375 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialCompoolStatement::copy ( SgCopyHelp& help) const
   {
     SgJovialCompoolStatement* result = NULL;

  // printf ("Copy SgJovialCompoolStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialCompoolStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialCompoolStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialCompoolStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialCompoolStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialCompoolStatement(  startOfConstruct_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialCompoolStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialCompoolStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 98704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialDirectiveStatement::copy ( SgCopyHelp& help) const
   {
     SgJovialDirectiveStatement* result = NULL;

  // printf ("Copy SgJovialDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialDirectiveStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialDirectiveStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialDirectiveStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialDirectiveStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: content_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for content_string
  // case: toBeCopied == COPY_DATA for content_string
     std::string content_string_copy = p_content_string; 
  // Copy constructor parameter data member: directive_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directive_type
  // case: toBeCopied == COPY_DATA for directive_type
     SgJovialDirectiveStatement::directive_types directive_type_copy = p_directive_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialDirectiveStatement(  startOfConstruct_copy, content_string_copy, directive_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialDirectiveStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialDirectiveStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 99037 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialDefineDeclaration::copy ( SgCopyHelp& help) const
   {
     SgJovialDefineDeclaration* result = NULL;

  // printf ("Copy SgJovialDefineDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialDefineDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialDefineDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialDefineDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialDefineDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: define_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for define_string
  // case: toBeCopied == COPY_DATA for define_string
     std::string define_string_copy = p_define_string; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialDefineDeclaration(  startOfConstruct_copy, define_string_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialDefineDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialDefineDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 99366 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialLabelDeclaration::copy ( SgCopyHelp& help) const
   {
     SgJovialLabelDeclaration* result = NULL;

  // printf ("Copy SgJovialLabelDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialLabelDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialLabelDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialLabelDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialLabelDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     std::string label_copy = p_label; 
  // Copy constructor parameter data member: label_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label_type
  // case: toBeCopied == COPY_DATA for label_type
     SgJovialLabelDeclaration::label_type_enum label_type_copy = p_label_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialLabelDeclaration(  startOfConstruct_copy, label_copy, label_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialLabelDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialLabelDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 99699 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialOverlayDeclaration::copy ( SgCopyHelp& help) const
   {
     SgJovialOverlayDeclaration* result = NULL;

  // printf ("Copy SgJovialOverlayDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialOverlayDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialOverlayDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialOverlayDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialOverlayDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: address_copy
     SgExpression* address_copy; 
  // case: not a listType for (using conditionalToCopyVariable)address
          if (get_address() != NULL) 
             { 
               address_copy = static_cast<SgExpression*>(help.copyAst(get_address())); 
             } 
            else 
             { 
               address_copy = NULL; 
             } 
  // Copy constructor parameter data member: overlay_copy
     SgExprListExp* overlay_copy; 
  // case: not a listType for (using conditionalToCopyVariable)overlay
          if (get_overlay() != NULL) 
             { 
               overlay_copy = static_cast<SgExprListExp*>(help.copyAst(get_overlay())); 
             } 
            else 
             { 
               overlay_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialOverlayDeclaration(  startOfConstruct_copy, address_copy, overlay_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)address
          if ( (address_copy != NULL) && (address_copy->get_parent() == NULL) && (isSgType(address_copy) == NULL) ) 
             { 
               address_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)overlay
          if ( (overlay_copy != NULL) && (overlay_copy->get_parent() == NULL) && (isSgType(overlay_copy) == NULL) ) 
             { 
               overlay_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialOverlayDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialOverlayDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 100056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealDecl::copy ( SgCopyHelp& help) const
   {
     SgNonrealDecl* result = NULL;

  // printf ("Copy SgNonrealDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNonrealDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNonrealDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNonrealDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNonrealDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNonrealDecl(  startOfConstruct_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgNonrealType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): template_parameter_position_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_position
  // case: toBeCopied == COPY_DATA for template_parameter_position
     int template_parameter_position_copy = p_template_parameter_position; 
     result->p_template_parameter_position = template_parameter_position_copy; 
  // Copy non-constructor parameter data member (access function): template_parameter_depth_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_depth
  // case: toBeCopied == COPY_DATA for template_parameter_depth
     int template_parameter_depth_copy = p_template_parameter_depth; 
     result->p_template_parameter_depth = template_parameter_depth_copy; 
  // Copy non-constructor parameter data member (access function): templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgDeclarationStatement* templateDeclaration_copy = p_templateDeclaration; 
     result->p_templateDeclaration = templateDeclaration_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_tpl_args()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tpl_args
  // case: toBeCopied == COPY_DATA for tpl_args
     SgTemplateArgumentPtrList tpl_args_copy = p_tpl_args; 
     result->p_tpl_args = tpl_args_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_tpl_params()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tpl_params
  // case: toBeCopied == COPY_DATA for tpl_params
     SgTemplateParameterPtrList tpl_params_copy = p_tpl_params; 
     result->p_tpl_params = tpl_params_copy; 
  // Copy non-constructor parameter data member (access function): is_class_member_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_class_member
  // case: toBeCopied == COPY_DATA for is_class_member
     bool is_class_member_copy = p_is_class_member; 
     result->p_is_class_member = is_class_member_copy; 
  // Copy non-constructor parameter data member (access function): is_template_param_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_template_param
  // case: toBeCopied == COPY_DATA for is_template_param
     bool is_template_param_copy = p_is_template_param; 
     result->p_is_template_param = is_template_param_copy; 
  // Copy non-constructor parameter data member (access function): is_template_template_param_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_template_template_param
  // case: toBeCopied == COPY_DATA for is_template_template_param
     bool is_template_template_param_copy = p_is_template_template_param; 
     result->p_is_template_template_param = is_template_template_param_copy; 
  // Copy non-constructor parameter data member (access function): is_nonreal_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_nonreal_template
  // case: toBeCopied == COPY_DATA for is_nonreal_template
     bool is_nonreal_template_copy = p_is_nonreal_template; 
     result->p_is_nonreal_template = is_nonreal_template_copy; 
  // Copy non-constructor parameter data member (access function): is_nonreal_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_nonreal_function
  // case: toBeCopied == COPY_DATA for is_nonreal_function
     bool is_nonreal_function_copy = p_is_nonreal_function; 
     result->p_is_nonreal_function = is_nonreal_function_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNonrealDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNonrealDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 100445 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEmptyDeclaration::copy ( SgCopyHelp& help) const
   {
     SgEmptyDeclaration* result = NULL;

  // printf ("Copy SgEmptyDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEmptyDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEmptyDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEmptyDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEmptyDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEmptyDeclaration(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEmptyDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEmptyDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 100770 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageBodyDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaPackageBodyDecl* result = NULL;

  // printf ("Copy SgAdaPackageBodyDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaPackageBodyDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaPackageBodyDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaPackageBodyDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaPackageBodyDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: definition_copy
     SgAdaPackageBody* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaPackageBody*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaPackageBodyDecl(  startOfConstruct_copy, name_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for spec
  // case: toBeCopied == COPY_DATA for spec
     SgAdaPackageSpecDecl* spec_copy = p_spec; 
     result->p_spec = spec_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaPackageBodyDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaPackageBodyDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 101125 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageSpecDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaPackageSpecDecl* result = NULL;

  // printf ("Copy SgAdaPackageSpecDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaPackageSpecDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaPackageSpecDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaPackageSpecDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaPackageSpecDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: definition_copy
     SgAdaPackageSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaPackageSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaPackageSpecDecl(  startOfConstruct_copy, name_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgAdaPackageBodyDecl* body_copy = p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaPackageSpecDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaPackageSpecDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 101480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRenamingDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaRenamingDecl* result = NULL;

  // printf ("Copy SgAdaRenamingDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaRenamingDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaRenamingDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaRenamingDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaRenamingDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: renamed_copy
     SgExpression* renamed_copy; 
  // case: not a listType for (using conditionalToCopyVariable)renamed
          if (get_renamed() != NULL) 
             { 
               renamed_copy = static_cast<SgExpression*>(help.copyAst(get_renamed())); 
             } 
            else 
             { 
               renamed_copy = NULL; 
             } 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaRenamingDecl(  startOfConstruct_copy, name_copy, renamed_copy, type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)renamed
          if ( (renamed_copy != NULL) && (renamed_copy->get_parent() == NULL) && (isSgType(renamed_copy) == NULL) ) 
             { 
               renamed_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaRenamingDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaRenamingDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 101829 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskSpecDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaTaskSpecDecl* result = NULL;

  // printf ("Copy SgAdaTaskSpecDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTaskSpecDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTaskSpecDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTaskSpecDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTaskSpecDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: definition_copy
     SgAdaTaskSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaTaskSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTaskSpecDecl(  startOfConstruct_copy, name_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaTaskType* type_copy = p_type; 
     result->p_type = type_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTaskSpecDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTaskSpecDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 102179 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskBodyDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaTaskBodyDecl* result = NULL;

  // printf ("Copy SgAdaTaskBodyDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTaskBodyDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTaskBodyDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTaskBodyDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTaskBodyDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: specificationDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specificationDeclaration
  // case: toBeCopied == COPY_DATA for specificationDeclaration
     SgDeclarationStatement* specificationDeclaration_copy = p_specificationDeclaration; 
  // Copy constructor parameter data member: definition_copy
     SgAdaTaskBody* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaTaskBody*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTaskBodyDecl(  startOfConstruct_copy, name_copy, specificationDeclaration_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTaskBodyDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTaskBodyDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 102528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskTypeDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaTaskTypeDecl* result = NULL;

  // printf ("Copy SgAdaTaskTypeDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTaskTypeDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTaskTypeDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTaskTypeDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTaskTypeDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: definition_copy
     SgAdaTaskSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaTaskSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTaskTypeDecl(  startOfConstruct_copy, name_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaTaskType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTaskTypeDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTaskTypeDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 102883 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedSpecDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaProtectedSpecDecl* result = NULL;

  // printf ("Copy SgAdaProtectedSpecDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaProtectedSpecDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaProtectedSpecDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaProtectedSpecDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaProtectedSpecDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: definition_copy
     SgAdaProtectedSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaProtectedSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaProtectedSpecDecl(  startOfConstruct_copy, name_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaProtectedType* type_copy = p_type; 
     result->p_type = type_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaProtectedSpecDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaProtectedSpecDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 103233 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedBodyDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaProtectedBodyDecl* result = NULL;

  // printf ("Copy SgAdaProtectedBodyDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaProtectedBodyDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaProtectedBodyDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaProtectedBodyDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaProtectedBodyDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: specificationDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specificationDeclaration
  // case: toBeCopied == COPY_DATA for specificationDeclaration
     SgDeclarationStatement* specificationDeclaration_copy = p_specificationDeclaration; 
  // Copy constructor parameter data member: definition_copy
     SgAdaProtectedBody* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaProtectedBody*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaProtectedBodyDecl(  startOfConstruct_copy, name_copy, specificationDeclaration_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaProtectedBodyDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaProtectedBodyDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 103582 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedTypeDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaProtectedTypeDecl* result = NULL;

  // printf ("Copy SgAdaProtectedTypeDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaProtectedTypeDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaProtectedTypeDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaProtectedTypeDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaProtectedTypeDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: definition_copy
     SgAdaProtectedSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaProtectedSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaProtectedTypeDecl(  startOfConstruct_copy, name_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaProtectedType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaProtectedTypeDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaProtectedTypeDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 103937 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRepresentationClause::copy ( SgCopyHelp& help) const
   {
     SgAdaRepresentationClause* result = NULL;

  // printf ("Copy SgAdaRepresentationClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaRepresentationClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaRepresentationClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaRepresentationClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaRepresentationClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: recordType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for recordType
  // case: toBeCopied == COPY_DATA for recordType
     SgType* recordType_copy = p_recordType; 
  // Copy constructor parameter data member: alignment_copy
     SgExpression* alignment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alignment
          if (get_alignment() != NULL) 
             { 
               alignment_copy = static_cast<SgExpression*>(help.copyAst(get_alignment())); 
             } 
            else 
             { 
               alignment_copy = NULL; 
             } 
  // Copy constructor parameter data member: components_copy
     SgBasicBlock* components_copy; 
  // case: not a listType for (using conditionalToCopyVariable)components
          if (get_components() != NULL) 
             { 
               components_copy = static_cast<SgBasicBlock*>(help.copyAst(get_components())); 
             } 
            else 
             { 
               components_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaRepresentationClause(  startOfConstruct_copy, recordType_copy, alignment_copy, components_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)alignment
          if ( (alignment_copy != NULL) && (alignment_copy->get_parent() == NULL) && (isSgType(alignment_copy) == NULL) ) 
             { 
               alignment_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)components
          if ( (components_copy != NULL) && (components_copy->get_parent() == NULL) && (isSgType(components_copy) == NULL) ) 
             { 
               components_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaRepresentationClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaRepresentationClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 104298 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaComponentClause::copy ( SgCopyHelp& help) const
   {
     SgAdaComponentClause* result = NULL;

  // printf ("Copy SgAdaComponentClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaComponentClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaComponentClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaComponentClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaComponentClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: component_copy
     SgVarRefExp* component_copy; 
  // case: not a listType for (using conditionalToCopyVariable)component
          if (get_component() != NULL) 
             { 
               component_copy = static_cast<SgVarRefExp*>(help.copyAst(get_component())); 
             } 
            else 
             { 
               component_copy = NULL; 
             } 
  // Copy constructor parameter data member: offset_copy
     SgExpression* offset_copy; 
  // case: not a listType for (using conditionalToCopyVariable)offset
          if (get_offset() != NULL) 
             { 
               offset_copy = static_cast<SgExpression*>(help.copyAst(get_offset())); 
             } 
            else 
             { 
               offset_copy = NULL; 
             } 
  // Copy constructor parameter data member: range_copy
     SgRangeExp* range_copy; 
  // case: not a listType for (using conditionalToCopyVariable)range
          if (get_range() != NULL) 
             { 
               range_copy = static_cast<SgRangeExp*>(help.copyAst(get_range())); 
             } 
            else 
             { 
               range_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaComponentClause(  startOfConstruct_copy, component_copy, offset_copy, range_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)component
          if ( (component_copy != NULL) && (component_copy->get_parent() == NULL) && (isSgType(component_copy) == NULL) ) 
             { 
               component_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)offset
          if ( (offset_copy != NULL) && (offset_copy->get_parent() == NULL) && (isSgType(offset_copy) == NULL) ) 
             { 
               offset_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)range
          if ( (range_copy != NULL) && (range_copy->get_parent() == NULL) && (isSgType(range_copy) == NULL) ) 
             { 
               range_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaComponentClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaComponentClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 104671 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAttributeClause::copy ( SgCopyHelp& help) const
   {
     SgAdaAttributeClause* result = NULL;

  // printf ("Copy SgAdaAttributeClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaAttributeClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaAttributeClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaAttributeClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaAttributeClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: attribute_copy
     SgAdaAttributeExp* attribute_copy; 
  // case: not a listType for (using conditionalToCopyVariable)attribute
          if (get_attribute() != NULL) 
             { 
               attribute_copy = static_cast<SgAdaAttributeExp*>(help.copyAst(get_attribute())); 
             } 
            else 
             { 
               attribute_copy = NULL; 
             } 
  // Copy constructor parameter data member: size_copy
     SgExpression* size_copy; 
  // case: not a listType for (using conditionalToCopyVariable)size
          if (get_size() != NULL) 
             { 
               size_copy = static_cast<SgExpression*>(help.copyAst(get_size())); 
             } 
            else 
             { 
               size_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaAttributeClause(  startOfConstruct_copy, attribute_copy, size_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)attribute
          if ( (attribute_copy != NULL) && (attribute_copy->get_parent() == NULL) && (isSgType(attribute_copy) == NULL) ) 
             { 
               attribute_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)size
          if ( (size_copy != NULL) && (size_copy->get_parent() == NULL) && (isSgType(size_copy) == NULL) ) 
             { 
               size_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaAttributeClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaAttributeClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 105028 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaEnumRepresentationClause::copy ( SgCopyHelp& help) const
   {
     SgAdaEnumRepresentationClause* result = NULL;

  // printf ("Copy SgAdaEnumRepresentationClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaEnumRepresentationClause*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaEnumRepresentationClause*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaEnumRepresentationClause: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaEnumRepresentationClause*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: enumType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enumType
  // case: toBeCopied == COPY_DATA for enumType
     SgType* enumType_copy = p_enumType; 
  // Copy constructor parameter data member: components_copy
     SgExprListExp* components_copy; 
  // case: not a listType for (using conditionalToCopyVariable)components
          if (get_components() != NULL) 
             { 
               components_copy = static_cast<SgExprListExp*>(help.copyAst(get_components())); 
             } 
            else 
             { 
               components_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaEnumRepresentationClause(  startOfConstruct_copy, enumType_copy, components_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)components
          if ( (components_copy != NULL) && (components_copy->get_parent() == NULL) && (isSgType(components_copy) == NULL) ) 
             { 
               components_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaEnumRepresentationClause = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaEnumRepresentationClause::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 105373 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaGenericDecl* result = NULL;

  // printf ("Copy SgAdaGenericDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaGenericDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaGenericDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaGenericDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaGenericDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: definition_copy
     SgAdaGenericDefn* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != NULL) 
             { 
               definition_copy = static_cast<SgAdaGenericDefn*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaGenericDecl(  startOfConstruct_copy, name_copy, definition_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_declaration != NULL) delete result->p_declaration; 
     result->p_declaration = declaration_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
          if ( (definition_copy != NULL) && (definition_copy->get_parent() == NULL) && (isSgType(definition_copy) == NULL) ) 
             { 
               definition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaGenericDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaGenericDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 105742 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFormalTypeDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaFormalTypeDecl* result = NULL;

  // printf ("Copy SgAdaFormalTypeDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaFormalTypeDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaFormalTypeDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaFormalTypeDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaFormalTypeDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaFormalTypeDecl(  startOfConstruct_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaFormalType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): discriminants_copy
     SgAdaParameterList* discriminants_copy; 
  // case: not a listType for (using conditionalToCopyVariable)discriminants
          if (get_discriminants() != NULL) 
             { 
               discriminants_copy = static_cast<SgAdaParameterList*>(help.copyAst(get_discriminants())); 
             } 
            else 
             { 
               discriminants_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_discriminants != NULL) delete result->p_discriminants; 
     result->p_discriminants = discriminants_copy; 
  // case: not a listType for (using conditionalToSetParent)discriminants
          if ( (discriminants_copy != NULL) && (discriminants_copy->get_parent() == NULL) && (isSgType(discriminants_copy) == NULL) ) 
             { 
               discriminants_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaFormalTypeDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaFormalTypeDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 106095 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDiscriminatedTypeDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaDiscriminatedTypeDecl* result = NULL;

  // printf ("Copy SgAdaDiscriminatedTypeDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaDiscriminatedTypeDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaDiscriminatedTypeDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaDiscriminatedTypeDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaDiscriminatedTypeDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: discriminantScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for discriminantScope
  // case: toBeCopied == COPY_DATA for discriminantScope
     SgDeclarationScope* discriminantScope_copy = p_discriminantScope; 
  // Copy constructor parameter data member: discriminants_copy
     SgAdaParameterList* discriminants_copy; 
  // case: not a listType for (using conditionalToCopyVariable)discriminants
          if (get_discriminants() != NULL) 
             { 
               discriminants_copy = static_cast<SgAdaParameterList*>(help.copyAst(get_discriminants())); 
             } 
            else 
             { 
               discriminants_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaDiscriminatedTypeDecl(  startOfConstruct_copy, discriminantScope_copy, discriminants_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): discriminatedDecl_copy
     SgDeclarationStatement* discriminatedDecl_copy; 
  // case: not a listType for (using conditionalToCopyVariable)discriminatedDecl
          if (get_discriminatedDecl() != NULL) 
             { 
               discriminatedDecl_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_discriminatedDecl())); 
             } 
            else 
             { 
               discriminatedDecl_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_discriminatedDecl != NULL) delete result->p_discriminatedDecl; 
     result->p_discriminatedDecl = discriminatedDecl_copy; 
  // case: not a listType for (using conditionalToSetParent)discriminatedDecl
          if ( (discriminatedDecl_copy != NULL) && (discriminatedDecl_copy->get_parent() == NULL) && (isSgType(discriminatedDecl_copy) == NULL) ) 
             { 
               discriminatedDecl_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaDiscriminatedType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)discriminants
          if ( (discriminants_copy != NULL) && (discriminants_copy->get_parent() == NULL) && (isSgType(discriminants_copy) == NULL) ) 
             { 
               discriminants_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaDiscriminatedTypeDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaDiscriminatedTypeDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 106469 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericInstanceDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaGenericInstanceDecl* result = NULL;

  // printf ("Copy SgAdaGenericInstanceDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaGenericInstanceDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaGenericInstanceDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaGenericInstanceDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaGenericInstanceDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: genericDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for genericDeclaration
  // case: toBeCopied == COPY_DATA for genericDeclaration
     SgDeclarationStatement* genericDeclaration_copy = p_genericDeclaration; 
  // Copy constructor parameter data member: instantiatedScope_copy
     SgScopeStatement* instantiatedScope_copy; 
  // case: not a listType for (using conditionalToCopyVariable)instantiatedScope
          if (get_instantiatedScope() != NULL) 
             { 
               instantiatedScope_copy = static_cast<SgScopeStatement*>(help.copyAst(get_instantiatedScope())); 
             } 
            else 
             { 
               instantiatedScope_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaGenericInstanceDecl(  startOfConstruct_copy, name_copy, genericDeclaration_copy, instantiatedScope_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): actual_parameters_copy
     SgExprListExp* actual_parameters_copy; 
  // case: not a listType for (using conditionalToCopyVariable)actual_parameters
          if (get_actual_parameters() != NULL) 
             { 
               actual_parameters_copy = static_cast<SgExprListExp*>(help.copyAst(get_actual_parameters())); 
             } 
            else 
             { 
               actual_parameters_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_actual_parameters != NULL) delete result->p_actual_parameters; 
     result->p_actual_parameters = actual_parameters_copy; 
  // case: not a listType for (using conditionalToSetParent)actual_parameters
          if ( (actual_parameters_copy != NULL) && (actual_parameters_copy->get_parent() == NULL) && (isSgType(actual_parameters_copy) == NULL) ) 
             { 
               actual_parameters_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)instantiatedScope
          if ( (instantiatedScope_copy != NULL) && (instantiatedScope_copy->get_parent() == NULL) && (isSgType(instantiatedScope_copy) == NULL) ) 
             { 
               instantiatedScope_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaGenericInstanceDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaGenericInstanceDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 106842 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFormalPackageDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaFormalPackageDecl* result = NULL;

  // printf ("Copy SgAdaFormalPackageDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaFormalPackageDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaFormalPackageDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaFormalPackageDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaFormalPackageDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgDeclarationStatement* declaration_copy = p_declaration; 
  // Copy constructor parameter data member: actual_parameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for actual_parameters
  // case: toBeCopied == COPY_DATA for actual_parameters
     SgExprListExp* actual_parameters_copy = p_actual_parameters; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaFormalPackageDecl(  startOfConstruct_copy, name_copy, declaration_copy, actual_parameters_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): prototype_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototype
  // case: toBeCopied == COPY_DATA for prototype
     SgDeclarationStatement* prototype_copy = p_prototype; 
     result->p_prototype = prototype_copy; 
  // Copy non-constructor parameter data member (access function): prototypeScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeScope
  // case: toBeCopied == COPY_DATA for prototypeScope
     SgScopeStatement* prototypeScope_copy = p_prototypeScope; 
     result->p_prototypeScope = prototypeScope_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaFormalPackageDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaFormalPackageDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 107189 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaParameterList::copy ( SgCopyHelp& help) const
   {
     SgAdaParameterList* result = NULL;

  // printf ("Copy SgAdaParameterList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaParameterList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaParameterList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaParameterList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaParameterList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaParameterList(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_parameters()
     SgDeclarationStatementPtrList parameters_copy; 
  // case: listType for parameters
  // case: listType (typeIsPointerToList == false) for parameters
     SgDeclarationStatementPtrList::const_iterator source_parameters_iterator = get_parameters().begin(); 
     for ( /* empty by design */; source_parameters_iterator != get_parameters().end(); ++source_parameters_iterator) 
        { 
          SgDeclarationStatement* source_list_element = *source_parameters_iterator; 
          SgDeclarationStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_parameters_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          parameters_copy.push_back(copy_list_element); 
        } 
     result->p_parameters = parameters_copy; 
  // case: listType for parameters
  // case: listType (typeIsPointerToList == false) for parameters
     SgDeclarationStatementPtrList::const_iterator parameters_iterator = result->get_parameters().begin(); 
     for ( /* empty by design */; parameters_iterator != result->get_parameters().end(); ++parameters_iterator) 
        { 
          SgDeclarationStatement* list_element = *parameters_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaParameterList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaParameterList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 107545 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaVariantDecl::copy ( SgCopyHelp& help) const
   {
     SgAdaVariantDecl* result = NULL;

  // printf ("Copy SgAdaVariantDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaVariantDecl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaVariantDecl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaVariantDecl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaVariantDecl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: discriminant_copy
     SgExpression* discriminant_copy; 
  // case: not a listType for (using conditionalToCopyVariable)discriminant
          if (get_discriminant() != NULL) 
             { 
               discriminant_copy = static_cast<SgExpression*>(help.copyAst(get_discriminant())); 
             } 
            else 
             { 
               discriminant_copy = NULL; 
             } 
  // Copy constructor parameter data member: variants_copy
     SgAdaUnscopedBlock* variants_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variants
          if (get_variants() != NULL) 
             { 
               variants_copy = static_cast<SgAdaUnscopedBlock*>(help.copyAst(get_variants())); 
             } 
            else 
             { 
               variants_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaVariantDecl(  startOfConstruct_copy, discriminant_copy, variants_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)discriminant
          if ( (discriminant_copy != NULL) && (discriminant_copy->get_parent() == NULL) && (isSgType(discriminant_copy) == NULL) ) 
             { 
               discriminant_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)variants
          if ( (variants_copy != NULL) && (variants_copy->get_parent() == NULL) && (isSgType(variants_copy) == NULL) ) 
             { 
               variants_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaVariantDecl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaVariantDecl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 107902 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExprStatement::copy ( SgCopyHelp& help) const
   {
     SgExprStatement* result = NULL;

  // printf ("Copy SgExprStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgExprStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgExprStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgExprStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgExprStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgExprStatement(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgExprStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgExprStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 108173 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLabelStatement::copy ( SgCopyHelp& help) const
   {
     SgLabelStatement* result = NULL;

  // printf ("Copy SgLabelStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLabelStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLabelStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLabelStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLabelStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     SgName label_copy = p_label; 
  // Copy constructor parameter data member: statement_copy
     SgStatement* statement_copy; 
  // case: not a listType for (using conditionalToCopyVariable)statement
          if (get_statement() != NULL) 
             { 
               statement_copy = static_cast<SgStatement*>(help.copyAst(get_statement())); 
             } 
            else 
             { 
               statement_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLabelStatement(  startOfConstruct_copy, label_copy, statement_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): label_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label_type
  // case: toBeCopied == COPY_DATA for label_type
     SgLabelStatement::label_type_enum label_type_copy = p_label_type; 
     result->p_label_type = label_type_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_unused_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_unused
  // case: toBeCopied == COPY_DATA for gnu_extension_unused
     bool gnu_extension_unused_copy = p_gnu_extension_unused; 
     result->p_gnu_extension_unused = gnu_extension_unused_copy; 
  // case: not a listType for (using conditionalToSetParent)statement
          if ( (statement_copy != NULL) && (statement_copy->get_parent() == NULL) && (isSgType(statement_copy) == NULL) ) 
             { 
               statement_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLabelStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLabelStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 108463 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCaseOptionStmt::copy ( SgCopyHelp& help) const
   {
     SgCaseOptionStmt* result = NULL;

  // printf ("Copy SgCaseOptionStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCaseOptionStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCaseOptionStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCaseOptionStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCaseOptionStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: key_copy
     SgExpression* key_copy; 
  // case: not a listType for (using conditionalToCopyVariable)key
          if (get_key() != NULL) 
             { 
               key_copy = static_cast<SgExpression*>(help.copyAst(get_key())); 
             } 
            else 
             { 
               key_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCaseOptionStmt(  startOfConstruct_copy, key_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): key_range_end_copy
     SgExpression* key_range_end_copy; 
  // case: not a listType for (using conditionalToCopyVariable)key_range_end
          if (get_key_range_end() != NULL) 
             { 
               key_range_end_copy = static_cast<SgExpression*>(help.copyAst(get_key_range_end())); 
             } 
            else 
             { 
               key_range_end_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_key_range_end != NULL) delete result->p_key_range_end; 
     result->p_key_range_end = key_range_end_copy; 
  // case: not a listType for (using conditionalToSetParent)key_range_end
          if ( (key_range_end_copy != NULL) && (key_range_end_copy->get_parent() == NULL) && (isSgType(key_range_end_copy) == NULL) ) 
             { 
               key_range_end_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): case_construct_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for case_construct_name
  // case: toBeCopied == COPY_DATA for case_construct_name
     std::string case_construct_name_copy = p_case_construct_name; 
     result->p_case_construct_name = case_construct_name_copy; 
  // Copy non-constructor parameter data member (access function): has_fall_through_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_fall_through
  // case: toBeCopied == COPY_DATA for has_fall_through
     bool has_fall_through_copy = p_has_fall_through; 
     result->p_has_fall_through = has_fall_through_copy; 
  // case: not a listType for (using conditionalToSetParent)key
          if ( (key_copy != NULL) && (key_copy->get_parent() == NULL) && (isSgType(key_copy) == NULL) ) 
             { 
               key_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCaseOptionStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCaseOptionStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 108779 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTryStmt::copy ( SgCopyHelp& help) const
   {
     SgTryStmt* result = NULL;

  // printf ("Copy SgTryStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTryStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTryStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTryStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTryStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTryStmt(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): catch_statement_seq_root_copy
     SgCatchStatementSeq* catch_statement_seq_root_copy; 
  // case: not a listType for (using conditionalToCopyVariable)catch_statement_seq_root
          if (get_catch_statement_seq_root() != NULL) 
             { 
               catch_statement_seq_root_copy = static_cast<SgCatchStatementSeq*>(help.copyAst(get_catch_statement_seq_root())); 
             } 
            else 
             { 
               catch_statement_seq_root_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_catch_statement_seq_root != NULL) delete result->p_catch_statement_seq_root; 
     result->p_catch_statement_seq_root = catch_statement_seq_root_copy; 
  // case: not a listType for (using conditionalToSetParent)catch_statement_seq_root
          if ( (catch_statement_seq_root_copy != NULL) && (catch_statement_seq_root_copy->get_parent() == NULL) && (isSgType(catch_statement_seq_root_copy) == NULL) ) 
             { 
               catch_statement_seq_root_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): else_body_copy
     SgStatement* else_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)else_body
          if (get_else_body() != NULL) 
             { 
               else_body_copy = static_cast<SgStatement*>(help.copyAst(get_else_body())); 
             } 
            else 
             { 
               else_body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_else_body != NULL) delete result->p_else_body; 
     result->p_else_body = else_body_copy; 
  // case: not a listType for (using conditionalToSetParent)else_body
          if ( (else_body_copy != NULL) && (else_body_copy->get_parent() == NULL) && (isSgType(else_body_copy) == NULL) ) 
             { 
               else_body_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): finally_body_copy
     SgStatement* finally_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)finally_body
          if (get_finally_body() != NULL) 
             { 
               finally_body_copy = static_cast<SgStatement*>(help.copyAst(get_finally_body())); 
             } 
            else 
             { 
               finally_body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_finally_body != NULL) delete result->p_finally_body; 
     result->p_finally_body = finally_body_copy; 
  // case: not a listType for (using conditionalToSetParent)finally_body
          if ( (finally_body_copy != NULL) && (finally_body_copy->get_parent() == NULL) && (isSgType(finally_body_copy) == NULL) ) 
             { 
               finally_body_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTryStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTryStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 109107 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDefaultOptionStmt::copy ( SgCopyHelp& help) const
   {
     SgDefaultOptionStmt* result = NULL;

  // printf ("Copy SgDefaultOptionStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDefaultOptionStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDefaultOptionStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDefaultOptionStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDefaultOptionStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDefaultOptionStmt(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): default_construct_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for default_construct_name
  // case: toBeCopied == COPY_DATA for default_construct_name
     std::string default_construct_name_copy = p_default_construct_name; 
     result->p_default_construct_name = default_construct_name_copy; 
  // Copy non-constructor parameter data member (access function): has_fall_through_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_fall_through
  // case: toBeCopied == COPY_DATA for has_fall_through
     bool has_fall_through_copy = p_has_fall_through; 
     result->p_has_fall_through = has_fall_through_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDefaultOptionStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDefaultOptionStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 109388 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBreakStmt::copy ( SgCopyHelp& help) const
   {
     SgBreakStmt* result = NULL;

  // printf ("Copy SgBreakStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBreakStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBreakStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBreakStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBreakStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBreakStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): do_string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for do_string_label
  // case: toBeCopied == COPY_DATA for do_string_label
     std::string do_string_label_copy = p_do_string_label; 
     result->p_do_string_label = do_string_label_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBreakStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBreakStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 109648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgContinueStmt::copy ( SgCopyHelp& help) const
   {
     SgContinueStmt* result = NULL;

  // printf ("Copy SgContinueStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgContinueStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgContinueStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgContinueStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgContinueStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgContinueStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): do_string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for do_string_label
  // case: toBeCopied == COPY_DATA for do_string_label
     std::string do_string_label_copy = p_do_string_label; 
     result->p_do_string_label = do_string_label_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgContinueStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgContinueStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 109908 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReturnStmt::copy ( SgCopyHelp& help) const
   {
     SgReturnStmt* result = NULL;

  // printf ("Copy SgReturnStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgReturnStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgReturnStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgReturnStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgReturnStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgReturnStmt(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgReturnStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgReturnStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 110179 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGotoStatement::copy ( SgCopyHelp& help) const
   {
     SgGotoStatement* result = NULL;

  // printf ("Copy SgGotoStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGotoStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGotoStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGotoStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGotoStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     SgLabelStatement* label_copy = p_label; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGotoStatement(  startOfConstruct_copy, label_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): label_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label_expression
  // case: toBeCopied == COPY_DATA for label_expression
     SgLabelRefExp* label_expression_copy = p_label_expression; 
     result->p_label_expression = label_expression_copy; 
  // Copy non-constructor parameter data member (access function): selector_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for selector_expression
  // case: toBeCopied == COPY_DATA for selector_expression
     SgExpression* selector_expression_copy = p_selector_expression; 
     result->p_selector_expression = selector_expression_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGotoStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGotoStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 110448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSpawnStmt::copy ( SgCopyHelp& help) const
   {
     SgSpawnStmt* result = NULL;

  // printf ("Copy SgSpawnStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSpawnStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSpawnStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSpawnStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSpawnStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: the_func_copy
     SgFunctionCallExp* the_func_copy; 
  // case: not a listType for (using conditionalToCopyVariable)the_func
          if (get_the_func() != NULL) 
             { 
               the_func_copy = static_cast<SgFunctionCallExp*>(help.copyAst(get_the_func())); 
             } 
            else 
             { 
               the_func_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSpawnStmt(  startOfConstruct_copy, the_func_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)the_func
          if ( (the_func_copy != NULL) && (the_func_copy->get_parent() == NULL) && (isSgType(the_func_copy) == NULL) ) 
             { 
               the_func_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSpawnStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSpawnStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 110719 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNullStatement::copy ( SgCopyHelp& help) const
   {
     SgNullStatement* result = NULL;

  // printf ("Copy SgNullStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNullStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNullStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNullStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNullStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNullStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNullStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNullStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 110974 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariantStatement::copy ( SgCopyHelp& help) const
   {
     SgVariantStatement* result = NULL;

  // printf ("Copy SgVariantStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVariantStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVariantStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVariantStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVariantStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVariantStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVariantStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVariantStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 111229 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgForInitStatement::copy ( SgCopyHelp& help) const
   {
     SgForInitStatement* result = NULL;

  // printf ("Copy SgForInitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgForInitStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgForInitStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgForInitStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgForInitStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgForInitStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_init_stmt()
     SgStatementPtrList init_stmt_copy; 
  // case: listType for init_stmt
  // case: listType (typeIsPointerToList == false) for init_stmt
     SgStatementPtrList::const_iterator source_init_stmt_iterator = get_init_stmt().begin(); 
     for ( /* empty by design */; source_init_stmt_iterator != get_init_stmt().end(); ++source_init_stmt_iterator) 
        { 
          SgStatement* source_list_element = *source_init_stmt_iterator; 
          SgStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_init_stmt_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          init_stmt_copy.push_back(copy_list_element); 
        } 
     result->p_init_stmt = init_stmt_copy; 
  // case: listType for init_stmt
  // case: listType (typeIsPointerToList == false) for init_stmt
     SgStatementPtrList::const_iterator init_stmt_iterator = result->get_init_stmt().begin(); 
     for ( /* empty by design */; init_stmt_iterator != result->get_init_stmt().end(); ++init_stmt_iterator) 
        { 
          SgStatement* list_element = *init_stmt_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgForInitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgForInitStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 111515 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCatchStatementSeq::copy ( SgCopyHelp& help) const
   {
     SgCatchStatementSeq* result = NULL;

  // printf ("Copy SgCatchStatementSeq = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCatchStatementSeq*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCatchStatementSeq*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCatchStatementSeq: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCatchStatementSeq*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCatchStatementSeq(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_catch_statement_seq()
     SgStatementPtrList catch_statement_seq_copy; 
  // case: listType for catch_statement_seq
  // case: listType (typeIsPointerToList == false) for catch_statement_seq
     SgStatementPtrList::const_iterator source_catch_statement_seq_iterator = get_catch_statement_seq().begin(); 
     for ( /* empty by design */; source_catch_statement_seq_iterator != get_catch_statement_seq().end(); ++source_catch_statement_seq_iterator) 
        { 
          SgStatement* source_list_element = *source_catch_statement_seq_iterator; 
          SgStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_catch_statement_seq_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          catch_statement_seq_copy.push_back(copy_list_element); 
        } 
     result->p_catch_statement_seq = catch_statement_seq_copy; 
  // case: listType for catch_statement_seq
  // case: listType (typeIsPointerToList == false) for catch_statement_seq
     SgStatementPtrList::const_iterator catch_statement_seq_iterator = result->get_catch_statement_seq().begin(); 
     for ( /* empty by design */; catch_statement_seq_iterator != result->get_catch_statement_seq().end(); ++catch_statement_seq_iterator) 
        { 
          SgStatement* list_element = *catch_statement_seq_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCatchStatementSeq = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCatchStatementSeq::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 111801 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgProcessControlStatement::copy ( SgCopyHelp& help) const
   {
     SgProcessControlStatement* result = NULL;

  // printf ("Copy SgProcessControlStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgProcessControlStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgProcessControlStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgProcessControlStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgProcessControlStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: code_copy
     SgExpression* code_copy; 
  // case: not a listType for (using conditionalToCopyVariable)code
          if (get_code() != NULL) 
             { 
               code_copy = static_cast<SgExpression*>(help.copyAst(get_code())); 
             } 
            else 
             { 
               code_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgProcessControlStatement(  startOfConstruct_copy, code_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): control_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for control_kind
  // case: toBeCopied == COPY_DATA for control_kind
     SgProcessControlStatement::control_enum control_kind_copy = p_control_kind; 
     result->p_control_kind = control_kind_copy; 
  // Copy non-constructor parameter data member (access function): quiet_copy
     SgExpression* quiet_copy; 
  // case: not a listType for (using conditionalToCopyVariable)quiet
          if (get_quiet() != NULL) 
             { 
               quiet_copy = static_cast<SgExpression*>(help.copyAst(get_quiet())); 
             } 
            else 
             { 
               quiet_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_quiet != NULL) delete result->p_quiet; 
     result->p_quiet = quiet_copy; 
  // case: not a listType for (using conditionalToSetParent)quiet
          if ( (quiet_copy != NULL) && (quiet_copy->get_parent() == NULL) && (isSgType(quiet_copy) == NULL) ) 
             { 
               quiet_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)code
          if ( (code_copy != NULL) && (code_copy->get_parent() == NULL) && (isSgType(code_copy) == NULL) ) 
             { 
               code_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgProcessControlStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgProcessControlStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 112096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIOStatement::copy ( SgCopyHelp& help) const
   {
     SgIOStatement* result = NULL;

  // printf ("Copy SgIOStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIOStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIOStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIOStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIOStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIOStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIOStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIOStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 112451 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPrintStatement::copy ( SgCopyHelp& help) const
   {
     SgPrintStatement* result = NULL;

  // printf ("Copy SgPrintStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPrintStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPrintStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPrintStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPrintStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPrintStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): format_copy
     SgExpression* format_copy; 
  // case: not a listType for (using conditionalToCopyVariable)format
          if (get_format() != NULL) 
             { 
               format_copy = static_cast<SgExpression*>(help.copyAst(get_format())); 
             } 
            else 
             { 
               format_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_format != NULL) delete result->p_format; 
     result->p_format = format_copy; 
  // case: not a listType for (using conditionalToSetParent)format
          if ( (format_copy != NULL) && (format_copy->get_parent() == NULL) && (isSgType(format_copy) == NULL) ) 
             { 
               format_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPrintStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPrintStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 112825 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReadStatement::copy ( SgCopyHelp& help) const
   {
     SgReadStatement* result = NULL;

  // printf ("Copy SgReadStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgReadStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgReadStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgReadStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgReadStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgReadStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): format_copy
     SgExpression* format_copy; 
  // case: not a listType for (using conditionalToCopyVariable)format
          if (get_format() != NULL) 
             { 
               format_copy = static_cast<SgExpression*>(help.copyAst(get_format())); 
             } 
            else 
             { 
               format_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_format != NULL) delete result->p_format; 
     result->p_format = format_copy; 
  // case: not a listType for (using conditionalToSetParent)format
          if ( (format_copy != NULL) && (format_copy->get_parent() == NULL) && (isSgType(format_copy) == NULL) ) 
             { 
               format_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): namelist_copy
     SgExpression* namelist_copy; 
  // case: not a listType for (using conditionalToCopyVariable)namelist
          if (get_namelist() != NULL) 
             { 
               namelist_copy = static_cast<SgExpression*>(help.copyAst(get_namelist())); 
             } 
            else 
             { 
               namelist_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_namelist != NULL) delete result->p_namelist; 
     result->p_namelist = namelist_copy; 
  // case: not a listType for (using conditionalToSetParent)namelist
          if ( (namelist_copy != NULL) && (namelist_copy->get_parent() == NULL) && (isSgType(namelist_copy) == NULL) ) 
             { 
               namelist_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): advance_copy
     SgExpression* advance_copy; 
  // case: not a listType for (using conditionalToCopyVariable)advance
          if (get_advance() != NULL) 
             { 
               advance_copy = static_cast<SgExpression*>(help.copyAst(get_advance())); 
             } 
            else 
             { 
               advance_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_advance != NULL) delete result->p_advance; 
     result->p_advance = advance_copy; 
  // case: not a listType for (using conditionalToSetParent)advance
          if ( (advance_copy != NULL) && (advance_copy->get_parent() == NULL) && (isSgType(advance_copy) == NULL) ) 
             { 
               advance_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): asynchronous_copy
     SgExpression* asynchronous_copy; 
  // case: not a listType for (using conditionalToCopyVariable)asynchronous
          if (get_asynchronous() != NULL) 
             { 
               asynchronous_copy = static_cast<SgExpression*>(help.copyAst(get_asynchronous())); 
             } 
            else 
             { 
               asynchronous_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_asynchronous != NULL) delete result->p_asynchronous; 
     result->p_asynchronous = asynchronous_copy; 
  // case: not a listType for (using conditionalToSetParent)asynchronous
          if ( (asynchronous_copy != NULL) && (asynchronous_copy->get_parent() == NULL) && (isSgType(asynchronous_copy) == NULL) ) 
             { 
               asynchronous_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): blank_copy
     SgExpression* blank_copy; 
  // case: not a listType for (using conditionalToCopyVariable)blank
          if (get_blank() != NULL) 
             { 
               blank_copy = static_cast<SgExpression*>(help.copyAst(get_blank())); 
             } 
            else 
             { 
               blank_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_blank != NULL) delete result->p_blank; 
     result->p_blank = blank_copy; 
  // case: not a listType for (using conditionalToSetParent)blank
          if ( (blank_copy != NULL) && (blank_copy->get_parent() == NULL) && (isSgType(blank_copy) == NULL) ) 
             { 
               blank_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): decimal_copy
     SgExpression* decimal_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decimal
          if (get_decimal() != NULL) 
             { 
               decimal_copy = static_cast<SgExpression*>(help.copyAst(get_decimal())); 
             } 
            else 
             { 
               decimal_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decimal != NULL) delete result->p_decimal; 
     result->p_decimal = decimal_copy; 
  // case: not a listType for (using conditionalToSetParent)decimal
          if ( (decimal_copy != NULL) && (decimal_copy->get_parent() == NULL) && (isSgType(decimal_copy) == NULL) ) 
             { 
               decimal_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): delim_copy
     SgExpression* delim_copy; 
  // case: not a listType for (using conditionalToCopyVariable)delim
          if (get_delim() != NULL) 
             { 
               delim_copy = static_cast<SgExpression*>(help.copyAst(get_delim())); 
             } 
            else 
             { 
               delim_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_delim != NULL) delete result->p_delim; 
     result->p_delim = delim_copy; 
  // case: not a listType for (using conditionalToSetParent)delim
          if ( (delim_copy != NULL) && (delim_copy->get_parent() == NULL) && (isSgType(delim_copy) == NULL) ) 
             { 
               delim_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): end_copy
     SgExpression* end_copy; 
  // case: not a listType for (using conditionalToCopyVariable)end
          if (get_end() != NULL) 
             { 
               end_copy = static_cast<SgExpression*>(help.copyAst(get_end())); 
             } 
            else 
             { 
               end_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_end != NULL) delete result->p_end; 
     result->p_end = end_copy; 
  // case: not a listType for (using conditionalToSetParent)end
          if ( (end_copy != NULL) && (end_copy->get_parent() == NULL) && (isSgType(end_copy) == NULL) ) 
             { 
               end_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): eor_copy
     SgExpression* eor_copy; 
  // case: not a listType for (using conditionalToCopyVariable)eor
          if (get_eor() != NULL) 
             { 
               eor_copy = static_cast<SgExpression*>(help.copyAst(get_eor())); 
             } 
            else 
             { 
               eor_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_eor != NULL) delete result->p_eor; 
     result->p_eor = eor_copy; 
  // case: not a listType for (using conditionalToSetParent)eor
          if ( (eor_copy != NULL) && (eor_copy->get_parent() == NULL) && (isSgType(eor_copy) == NULL) ) 
             { 
               eor_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): id_copy
     SgExpression* id_copy; 
  // case: not a listType for (using conditionalToCopyVariable)id
          if (get_id() != NULL) 
             { 
               id_copy = static_cast<SgExpression*>(help.copyAst(get_id())); 
             } 
            else 
             { 
               id_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_id != NULL) delete result->p_id; 
     result->p_id = id_copy; 
  // case: not a listType for (using conditionalToSetParent)id
          if ( (id_copy != NULL) && (id_copy->get_parent() == NULL) && (isSgType(id_copy) == NULL) ) 
             { 
               id_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): pad_copy
     SgExpression* pad_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pad
          if (get_pad() != NULL) 
             { 
               pad_copy = static_cast<SgExpression*>(help.copyAst(get_pad())); 
             } 
            else 
             { 
               pad_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pad != NULL) delete result->p_pad; 
     result->p_pad = pad_copy; 
  // case: not a listType for (using conditionalToSetParent)pad
          if ( (pad_copy != NULL) && (pad_copy->get_parent() == NULL) && (isSgType(pad_copy) == NULL) ) 
             { 
               pad_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): pos_copy
     SgExpression* pos_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pos
          if (get_pos() != NULL) 
             { 
               pos_copy = static_cast<SgExpression*>(help.copyAst(get_pos())); 
             } 
            else 
             { 
               pos_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pos != NULL) delete result->p_pos; 
     result->p_pos = pos_copy; 
  // case: not a listType for (using conditionalToSetParent)pos
          if ( (pos_copy != NULL) && (pos_copy->get_parent() == NULL) && (isSgType(pos_copy) == NULL) ) 
             { 
               pos_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): rec_copy
     SgExpression* rec_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rec
          if (get_rec() != NULL) 
             { 
               rec_copy = static_cast<SgExpression*>(help.copyAst(get_rec())); 
             } 
            else 
             { 
               rec_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rec != NULL) delete result->p_rec; 
     result->p_rec = rec_copy; 
  // case: not a listType for (using conditionalToSetParent)rec
          if ( (rec_copy != NULL) && (rec_copy->get_parent() == NULL) && (isSgType(rec_copy) == NULL) ) 
             { 
               rec_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): round_copy
     SgExpression* round_copy; 
  // case: not a listType for (using conditionalToCopyVariable)round
          if (get_round() != NULL) 
             { 
               round_copy = static_cast<SgExpression*>(help.copyAst(get_round())); 
             } 
            else 
             { 
               round_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_round != NULL) delete result->p_round; 
     result->p_round = round_copy; 
  // case: not a listType for (using conditionalToSetParent)round
          if ( (round_copy != NULL) && (round_copy->get_parent() == NULL) && (isSgType(round_copy) == NULL) ) 
             { 
               round_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): sign_copy
     SgExpression* sign_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sign
          if (get_sign() != NULL) 
             { 
               sign_copy = static_cast<SgExpression*>(help.copyAst(get_sign())); 
             } 
            else 
             { 
               sign_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sign != NULL) delete result->p_sign; 
     result->p_sign = sign_copy; 
  // case: not a listType for (using conditionalToSetParent)sign
          if ( (sign_copy != NULL) && (sign_copy->get_parent() == NULL) && (isSgType(sign_copy) == NULL) ) 
             { 
               sign_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): size_copy
     SgExpression* size_copy; 
  // case: not a listType for (using conditionalToCopyVariable)size
          if (get_size() != NULL) 
             { 
               size_copy = static_cast<SgExpression*>(help.copyAst(get_size())); 
             } 
            else 
             { 
               size_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_size != NULL) delete result->p_size; 
     result->p_size = size_copy; 
  // case: not a listType for (using conditionalToSetParent)size
          if ( (size_copy != NULL) && (size_copy->get_parent() == NULL) && (isSgType(size_copy) == NULL) ) 
             { 
               size_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgReadStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgReadStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 113484 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWriteStatement::copy ( SgCopyHelp& help) const
   {
     SgWriteStatement* result = NULL;

  // printf ("Copy SgWriteStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgWriteStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgWriteStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgWriteStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgWriteStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgWriteStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): format_copy
     SgExpression* format_copy; 
  // case: not a listType for (using conditionalToCopyVariable)format
          if (get_format() != NULL) 
             { 
               format_copy = static_cast<SgExpression*>(help.copyAst(get_format())); 
             } 
            else 
             { 
               format_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_format != NULL) delete result->p_format; 
     result->p_format = format_copy; 
  // case: not a listType for (using conditionalToSetParent)format
          if ( (format_copy != NULL) && (format_copy->get_parent() == NULL) && (isSgType(format_copy) == NULL) ) 
             { 
               format_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): namelist_copy
     SgExpression* namelist_copy; 
  // case: not a listType for (using conditionalToCopyVariable)namelist
          if (get_namelist() != NULL) 
             { 
               namelist_copy = static_cast<SgExpression*>(help.copyAst(get_namelist())); 
             } 
            else 
             { 
               namelist_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_namelist != NULL) delete result->p_namelist; 
     result->p_namelist = namelist_copy; 
  // case: not a listType for (using conditionalToSetParent)namelist
          if ( (namelist_copy != NULL) && (namelist_copy->get_parent() == NULL) && (isSgType(namelist_copy) == NULL) ) 
             { 
               namelist_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): advance_copy
     SgExpression* advance_copy; 
  // case: not a listType for (using conditionalToCopyVariable)advance
          if (get_advance() != NULL) 
             { 
               advance_copy = static_cast<SgExpression*>(help.copyAst(get_advance())); 
             } 
            else 
             { 
               advance_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_advance != NULL) delete result->p_advance; 
     result->p_advance = advance_copy; 
  // case: not a listType for (using conditionalToSetParent)advance
          if ( (advance_copy != NULL) && (advance_copy->get_parent() == NULL) && (isSgType(advance_copy) == NULL) ) 
             { 
               advance_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): asynchronous_copy
     SgExpression* asynchronous_copy; 
  // case: not a listType for (using conditionalToCopyVariable)asynchronous
          if (get_asynchronous() != NULL) 
             { 
               asynchronous_copy = static_cast<SgExpression*>(help.copyAst(get_asynchronous())); 
             } 
            else 
             { 
               asynchronous_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_asynchronous != NULL) delete result->p_asynchronous; 
     result->p_asynchronous = asynchronous_copy; 
  // case: not a listType for (using conditionalToSetParent)asynchronous
          if ( (asynchronous_copy != NULL) && (asynchronous_copy->get_parent() == NULL) && (isSgType(asynchronous_copy) == NULL) ) 
             { 
               asynchronous_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): blank_copy
     SgExpression* blank_copy; 
  // case: not a listType for (using conditionalToCopyVariable)blank
          if (get_blank() != NULL) 
             { 
               blank_copy = static_cast<SgExpression*>(help.copyAst(get_blank())); 
             } 
            else 
             { 
               blank_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_blank != NULL) delete result->p_blank; 
     result->p_blank = blank_copy; 
  // case: not a listType for (using conditionalToSetParent)blank
          if ( (blank_copy != NULL) && (blank_copy->get_parent() == NULL) && (isSgType(blank_copy) == NULL) ) 
             { 
               blank_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): decimal_copy
     SgExpression* decimal_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decimal
          if (get_decimal() != NULL) 
             { 
               decimal_copy = static_cast<SgExpression*>(help.copyAst(get_decimal())); 
             } 
            else 
             { 
               decimal_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decimal != NULL) delete result->p_decimal; 
     result->p_decimal = decimal_copy; 
  // case: not a listType for (using conditionalToSetParent)decimal
          if ( (decimal_copy != NULL) && (decimal_copy->get_parent() == NULL) && (isSgType(decimal_copy) == NULL) ) 
             { 
               decimal_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): delim_copy
     SgExpression* delim_copy; 
  // case: not a listType for (using conditionalToCopyVariable)delim
          if (get_delim() != NULL) 
             { 
               delim_copy = static_cast<SgExpression*>(help.copyAst(get_delim())); 
             } 
            else 
             { 
               delim_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_delim != NULL) delete result->p_delim; 
     result->p_delim = delim_copy; 
  // case: not a listType for (using conditionalToSetParent)delim
          if ( (delim_copy != NULL) && (delim_copy->get_parent() == NULL) && (isSgType(delim_copy) == NULL) ) 
             { 
               delim_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): end_copy
     SgExpression* end_copy; 
  // case: not a listType for (using conditionalToCopyVariable)end
          if (get_end() != NULL) 
             { 
               end_copy = static_cast<SgExpression*>(help.copyAst(get_end())); 
             } 
            else 
             { 
               end_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_end != NULL) delete result->p_end; 
     result->p_end = end_copy; 
  // case: not a listType for (using conditionalToSetParent)end
          if ( (end_copy != NULL) && (end_copy->get_parent() == NULL) && (isSgType(end_copy) == NULL) ) 
             { 
               end_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): eor_copy
     SgExpression* eor_copy; 
  // case: not a listType for (using conditionalToCopyVariable)eor
          if (get_eor() != NULL) 
             { 
               eor_copy = static_cast<SgExpression*>(help.copyAst(get_eor())); 
             } 
            else 
             { 
               eor_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_eor != NULL) delete result->p_eor; 
     result->p_eor = eor_copy; 
  // case: not a listType for (using conditionalToSetParent)eor
          if ( (eor_copy != NULL) && (eor_copy->get_parent() == NULL) && (isSgType(eor_copy) == NULL) ) 
             { 
               eor_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): id_copy
     SgExpression* id_copy; 
  // case: not a listType for (using conditionalToCopyVariable)id
          if (get_id() != NULL) 
             { 
               id_copy = static_cast<SgExpression*>(help.copyAst(get_id())); 
             } 
            else 
             { 
               id_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_id != NULL) delete result->p_id; 
     result->p_id = id_copy; 
  // case: not a listType for (using conditionalToSetParent)id
          if ( (id_copy != NULL) && (id_copy->get_parent() == NULL) && (isSgType(id_copy) == NULL) ) 
             { 
               id_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): pad_copy
     SgExpression* pad_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pad
          if (get_pad() != NULL) 
             { 
               pad_copy = static_cast<SgExpression*>(help.copyAst(get_pad())); 
             } 
            else 
             { 
               pad_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pad != NULL) delete result->p_pad; 
     result->p_pad = pad_copy; 
  // case: not a listType for (using conditionalToSetParent)pad
          if ( (pad_copy != NULL) && (pad_copy->get_parent() == NULL) && (isSgType(pad_copy) == NULL) ) 
             { 
               pad_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): pos_copy
     SgExpression* pos_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pos
          if (get_pos() != NULL) 
             { 
               pos_copy = static_cast<SgExpression*>(help.copyAst(get_pos())); 
             } 
            else 
             { 
               pos_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pos != NULL) delete result->p_pos; 
     result->p_pos = pos_copy; 
  // case: not a listType for (using conditionalToSetParent)pos
          if ( (pos_copy != NULL) && (pos_copy->get_parent() == NULL) && (isSgType(pos_copy) == NULL) ) 
             { 
               pos_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): rec_copy
     SgExpression* rec_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rec
          if (get_rec() != NULL) 
             { 
               rec_copy = static_cast<SgExpression*>(help.copyAst(get_rec())); 
             } 
            else 
             { 
               rec_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rec != NULL) delete result->p_rec; 
     result->p_rec = rec_copy; 
  // case: not a listType for (using conditionalToSetParent)rec
          if ( (rec_copy != NULL) && (rec_copy->get_parent() == NULL) && (isSgType(rec_copy) == NULL) ) 
             { 
               rec_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): round_copy
     SgExpression* round_copy; 
  // case: not a listType for (using conditionalToCopyVariable)round
          if (get_round() != NULL) 
             { 
               round_copy = static_cast<SgExpression*>(help.copyAst(get_round())); 
             } 
            else 
             { 
               round_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_round != NULL) delete result->p_round; 
     result->p_round = round_copy; 
  // case: not a listType for (using conditionalToSetParent)round
          if ( (round_copy != NULL) && (round_copy->get_parent() == NULL) && (isSgType(round_copy) == NULL) ) 
             { 
               round_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): sign_copy
     SgExpression* sign_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sign
          if (get_sign() != NULL) 
             { 
               sign_copy = static_cast<SgExpression*>(help.copyAst(get_sign())); 
             } 
            else 
             { 
               sign_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sign != NULL) delete result->p_sign; 
     result->p_sign = sign_copy; 
  // case: not a listType for (using conditionalToSetParent)sign
          if ( (sign_copy != NULL) && (sign_copy->get_parent() == NULL) && (isSgType(sign_copy) == NULL) ) 
             { 
               sign_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): size_copy
     SgExpression* size_copy; 
  // case: not a listType for (using conditionalToCopyVariable)size
          if (get_size() != NULL) 
             { 
               size_copy = static_cast<SgExpression*>(help.copyAst(get_size())); 
             } 
            else 
             { 
               size_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_size != NULL) delete result->p_size; 
     result->p_size = size_copy; 
  // case: not a listType for (using conditionalToSetParent)size
          if ( (size_copy != NULL) && (size_copy->get_parent() == NULL) && (isSgType(size_copy) == NULL) ) 
             { 
               size_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgWriteStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgWriteStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 114143 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOpenStatement::copy ( SgCopyHelp& help) const
   {
     SgOpenStatement* result = NULL;

  // printf ("Copy SgOpenStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOpenStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOpenStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOpenStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOpenStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOpenStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): file_copy
     SgExpression* file_copy; 
  // case: not a listType for (using conditionalToCopyVariable)file
          if (get_file() != NULL) 
             { 
               file_copy = static_cast<SgExpression*>(help.copyAst(get_file())); 
             } 
            else 
             { 
               file_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_file != NULL) delete result->p_file; 
     result->p_file = file_copy; 
  // case: not a listType for (using conditionalToSetParent)file
          if ( (file_copy != NULL) && (file_copy->get_parent() == NULL) && (isSgType(file_copy) == NULL) ) 
             { 
               file_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): status_copy
     SgExpression* status_copy; 
  // case: not a listType for (using conditionalToCopyVariable)status
          if (get_status() != NULL) 
             { 
               status_copy = static_cast<SgExpression*>(help.copyAst(get_status())); 
             } 
            else 
             { 
               status_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_status != NULL) delete result->p_status; 
     result->p_status = status_copy; 
  // case: not a listType for (using conditionalToSetParent)status
          if ( (status_copy != NULL) && (status_copy->get_parent() == NULL) && (isSgType(status_copy) == NULL) ) 
             { 
               status_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): access_copy
     SgExpression* access_copy; 
  // case: not a listType for (using conditionalToCopyVariable)access
          if (get_access() != NULL) 
             { 
               access_copy = static_cast<SgExpression*>(help.copyAst(get_access())); 
             } 
            else 
             { 
               access_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_access != NULL) delete result->p_access; 
     result->p_access = access_copy; 
  // case: not a listType for (using conditionalToSetParent)access
          if ( (access_copy != NULL) && (access_copy->get_parent() == NULL) && (isSgType(access_copy) == NULL) ) 
             { 
               access_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): form_copy
     SgExpression* form_copy; 
  // case: not a listType for (using conditionalToCopyVariable)form
          if (get_form() != NULL) 
             { 
               form_copy = static_cast<SgExpression*>(help.copyAst(get_form())); 
             } 
            else 
             { 
               form_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_form != NULL) delete result->p_form; 
     result->p_form = form_copy; 
  // case: not a listType for (using conditionalToSetParent)form
          if ( (form_copy != NULL) && (form_copy->get_parent() == NULL) && (isSgType(form_copy) == NULL) ) 
             { 
               form_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): recl_copy
     SgExpression* recl_copy; 
  // case: not a listType for (using conditionalToCopyVariable)recl
          if (get_recl() != NULL) 
             { 
               recl_copy = static_cast<SgExpression*>(help.copyAst(get_recl())); 
             } 
            else 
             { 
               recl_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_recl != NULL) delete result->p_recl; 
     result->p_recl = recl_copy; 
  // case: not a listType for (using conditionalToSetParent)recl
          if ( (recl_copy != NULL) && (recl_copy->get_parent() == NULL) && (isSgType(recl_copy) == NULL) ) 
             { 
               recl_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): blank_copy
     SgExpression* blank_copy; 
  // case: not a listType for (using conditionalToCopyVariable)blank
          if (get_blank() != NULL) 
             { 
               blank_copy = static_cast<SgExpression*>(help.copyAst(get_blank())); 
             } 
            else 
             { 
               blank_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_blank != NULL) delete result->p_blank; 
     result->p_blank = blank_copy; 
  // case: not a listType for (using conditionalToSetParent)blank
          if ( (blank_copy != NULL) && (blank_copy->get_parent() == NULL) && (isSgType(blank_copy) == NULL) ) 
             { 
               blank_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): position_copy
     SgExpression* position_copy; 
  // case: not a listType for (using conditionalToCopyVariable)position
          if (get_position() != NULL) 
             { 
               position_copy = static_cast<SgExpression*>(help.copyAst(get_position())); 
             } 
            else 
             { 
               position_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_position != NULL) delete result->p_position; 
     result->p_position = position_copy; 
  // case: not a listType for (using conditionalToSetParent)position
          if ( (position_copy != NULL) && (position_copy->get_parent() == NULL) && (isSgType(position_copy) == NULL) ) 
             { 
               position_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): action_copy
     SgExpression* action_copy; 
  // case: not a listType for (using conditionalToCopyVariable)action
          if (get_action() != NULL) 
             { 
               action_copy = static_cast<SgExpression*>(help.copyAst(get_action())); 
             } 
            else 
             { 
               action_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_action != NULL) delete result->p_action; 
     result->p_action = action_copy; 
  // case: not a listType for (using conditionalToSetParent)action
          if ( (action_copy != NULL) && (action_copy->get_parent() == NULL) && (isSgType(action_copy) == NULL) ) 
             { 
               action_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): delim_copy
     SgExpression* delim_copy; 
  // case: not a listType for (using conditionalToCopyVariable)delim
          if (get_delim() != NULL) 
             { 
               delim_copy = static_cast<SgExpression*>(help.copyAst(get_delim())); 
             } 
            else 
             { 
               delim_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_delim != NULL) delete result->p_delim; 
     result->p_delim = delim_copy; 
  // case: not a listType for (using conditionalToSetParent)delim
          if ( (delim_copy != NULL) && (delim_copy->get_parent() == NULL) && (isSgType(delim_copy) == NULL) ) 
             { 
               delim_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): pad_copy
     SgExpression* pad_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pad
          if (get_pad() != NULL) 
             { 
               pad_copy = static_cast<SgExpression*>(help.copyAst(get_pad())); 
             } 
            else 
             { 
               pad_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pad != NULL) delete result->p_pad; 
     result->p_pad = pad_copy; 
  // case: not a listType for (using conditionalToSetParent)pad
          if ( (pad_copy != NULL) && (pad_copy->get_parent() == NULL) && (isSgType(pad_copy) == NULL) ) 
             { 
               pad_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): round_copy
     SgExpression* round_copy; 
  // case: not a listType for (using conditionalToCopyVariable)round
          if (get_round() != NULL) 
             { 
               round_copy = static_cast<SgExpression*>(help.copyAst(get_round())); 
             } 
            else 
             { 
               round_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_round != NULL) delete result->p_round; 
     result->p_round = round_copy; 
  // case: not a listType for (using conditionalToSetParent)round
          if ( (round_copy != NULL) && (round_copy->get_parent() == NULL) && (isSgType(round_copy) == NULL) ) 
             { 
               round_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): sign_copy
     SgExpression* sign_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sign
          if (get_sign() != NULL) 
             { 
               sign_copy = static_cast<SgExpression*>(help.copyAst(get_sign())); 
             } 
            else 
             { 
               sign_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sign != NULL) delete result->p_sign; 
     result->p_sign = sign_copy; 
  // case: not a listType for (using conditionalToSetParent)sign
          if ( (sign_copy != NULL) && (sign_copy->get_parent() == NULL) && (isSgType(sign_copy) == NULL) ) 
             { 
               sign_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): asynchronous_copy
     SgExpression* asynchronous_copy; 
  // case: not a listType for (using conditionalToCopyVariable)asynchronous
          if (get_asynchronous() != NULL) 
             { 
               asynchronous_copy = static_cast<SgExpression*>(help.copyAst(get_asynchronous())); 
             } 
            else 
             { 
               asynchronous_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_asynchronous != NULL) delete result->p_asynchronous; 
     result->p_asynchronous = asynchronous_copy; 
  // case: not a listType for (using conditionalToSetParent)asynchronous
          if ( (asynchronous_copy != NULL) && (asynchronous_copy->get_parent() == NULL) && (isSgType(asynchronous_copy) == NULL) ) 
             { 
               asynchronous_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOpenStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOpenStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 114745 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCloseStatement::copy ( SgCopyHelp& help) const
   {
     SgCloseStatement* result = NULL;

  // printf ("Copy SgCloseStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCloseStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCloseStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCloseStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCloseStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCloseStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): status_copy
     SgExpression* status_copy; 
  // case: not a listType for (using conditionalToCopyVariable)status
          if (get_status() != NULL) 
             { 
               status_copy = static_cast<SgExpression*>(help.copyAst(get_status())); 
             } 
            else 
             { 
               status_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_status != NULL) delete result->p_status; 
     result->p_status = status_copy; 
  // case: not a listType for (using conditionalToSetParent)status
          if ( (status_copy != NULL) && (status_copy->get_parent() == NULL) && (isSgType(status_copy) == NULL) ) 
             { 
               status_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCloseStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCloseStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 115119 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInquireStatement::copy ( SgCopyHelp& help) const
   {
     SgInquireStatement* result = NULL;

  // printf ("Copy SgInquireStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgInquireStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgInquireStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgInquireStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgInquireStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgInquireStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgExpression* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (access function): access_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access
  // case: toBeCopied == COPY_DATA for access
     SgExpression* access_copy = p_access; 
     result->p_access = access_copy; 
  // Copy non-constructor parameter data member (access function): form_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for form
  // case: toBeCopied == COPY_DATA for form
     SgExpression* form_copy = p_form; 
     result->p_form = form_copy; 
  // Copy non-constructor parameter data member (access function): recl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for recl
  // case: toBeCopied == COPY_DATA for recl
     SgExpression* recl_copy = p_recl; 
     result->p_recl = recl_copy; 
  // Copy non-constructor parameter data member (access function): blank_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for blank
  // case: toBeCopied == COPY_DATA for blank
     SgExpression* blank_copy = p_blank; 
     result->p_blank = blank_copy; 
  // Copy non-constructor parameter data member (access function): exist_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exist
  // case: toBeCopied == COPY_DATA for exist
     SgExpression* exist_copy = p_exist; 
     result->p_exist = exist_copy; 
  // Copy non-constructor parameter data member (access function): opened_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for opened
  // case: toBeCopied == COPY_DATA for opened
     SgExpression* opened_copy = p_opened; 
     result->p_opened = opened_copy; 
  // Copy non-constructor parameter data member (access function): number_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for number
  // case: toBeCopied == COPY_DATA for number
     SgExpression* number_copy = p_number; 
     result->p_number = number_copy; 
  // Copy non-constructor parameter data member (access function): named_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named
  // case: toBeCopied == COPY_DATA for named
     SgExpression* named_copy = p_named; 
     result->p_named = named_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgExpression* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): sequential_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sequential
  // case: toBeCopied == COPY_DATA for sequential
     SgExpression* sequential_copy = p_sequential; 
     result->p_sequential = sequential_copy; 
  // Copy non-constructor parameter data member (access function): direct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for direct
  // case: toBeCopied == COPY_DATA for direct
     SgExpression* direct_copy = p_direct; 
     result->p_direct = direct_copy; 
  // Copy non-constructor parameter data member (access function): formatted_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for formatted
  // case: toBeCopied == COPY_DATA for formatted
     SgExpression* formatted_copy = p_formatted; 
     result->p_formatted = formatted_copy; 
  // Copy non-constructor parameter data member (access function): unformatted_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unformatted
  // case: toBeCopied == COPY_DATA for unformatted
     SgExpression* unformatted_copy = p_unformatted; 
     result->p_unformatted = unformatted_copy; 
  // Copy non-constructor parameter data member (access function): nextrec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nextrec
  // case: toBeCopied == COPY_DATA for nextrec
     SgExpression* nextrec_copy = p_nextrec; 
     result->p_nextrec = nextrec_copy; 
  // Copy non-constructor parameter data member (access function): position_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for position
  // case: toBeCopied == COPY_DATA for position
     SgExpression* position_copy = p_position; 
     result->p_position = position_copy; 
  // Copy non-constructor parameter data member (access function): action_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for action
  // case: toBeCopied == COPY_DATA for action
     SgExpression* action_copy = p_action; 
     result->p_action = action_copy; 
  // Copy non-constructor parameter data member (access function): read_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read
  // case: toBeCopied == COPY_DATA for read
     SgExpression* read_copy = p_read; 
     result->p_read = read_copy; 
  // Copy non-constructor parameter data member (access function): write_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for write
  // case: toBeCopied == COPY_DATA for write
     SgExpression* write_copy = p_write; 
     result->p_write = write_copy; 
  // Copy non-constructor parameter data member (access function): readwrite_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for readwrite
  // case: toBeCopied == COPY_DATA for readwrite
     SgExpression* readwrite_copy = p_readwrite; 
     result->p_readwrite = readwrite_copy; 
  // Copy non-constructor parameter data member (access function): delim_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delim
  // case: toBeCopied == COPY_DATA for delim
     SgExpression* delim_copy = p_delim; 
     result->p_delim = delim_copy; 
  // Copy non-constructor parameter data member (access function): pad_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pad
  // case: toBeCopied == COPY_DATA for pad
     SgExpression* pad_copy = p_pad; 
     result->p_pad = pad_copy; 
  // Copy non-constructor parameter data member (access function): asynchronous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asynchronous
  // case: toBeCopied == COPY_DATA for asynchronous
     SgExpression* asynchronous_copy = p_asynchronous; 
     result->p_asynchronous = asynchronous_copy; 
  // Copy non-constructor parameter data member (access function): decimal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decimal
  // case: toBeCopied == COPY_DATA for decimal
     SgExpression* decimal_copy = p_decimal; 
     result->p_decimal = decimal_copy; 
  // Copy non-constructor parameter data member (access function): stream_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stream
  // case: toBeCopied == COPY_DATA for stream
     SgExpression* stream_copy = p_stream; 
     result->p_stream = stream_copy; 
  // Copy non-constructor parameter data member (access function): size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     SgExpression* size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (access function): pending_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pending
  // case: toBeCopied == COPY_DATA for pending
     SgExpression* pending_copy = p_pending; 
     result->p_pending = pending_copy; 
  // Copy non-constructor parameter data member (access function): iolengthExp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iolengthExp
  // case: toBeCopied == COPY_DATA for iolengthExp
     SgVarRefExp* iolengthExp_copy = p_iolengthExp; 
     result->p_iolengthExp = iolengthExp_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgInquireStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgInquireStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 115614 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFlushStatement::copy ( SgCopyHelp& help) const
   {
     SgFlushStatement* result = NULL;

  // printf ("Copy SgFlushStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFlushStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFlushStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFlushStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFlushStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFlushStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFlushStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFlushStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 115969 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBackspaceStatement::copy ( SgCopyHelp& help) const
   {
     SgBackspaceStatement* result = NULL;

  // printf ("Copy SgBackspaceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBackspaceStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBackspaceStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBackspaceStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBackspaceStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBackspaceStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBackspaceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBackspaceStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 116324 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRewindStatement::copy ( SgCopyHelp& help) const
   {
     SgRewindStatement* result = NULL;

  // printf ("Copy SgRewindStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRewindStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRewindStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRewindStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRewindStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRewindStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRewindStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRewindStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 116679 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEndfileStatement::copy ( SgCopyHelp& help) const
   {
     SgEndfileStatement* result = NULL;

  // printf ("Copy SgEndfileStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEndfileStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEndfileStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEndfileStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEndfileStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEndfileStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEndfileStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEndfileStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 117034 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWaitStatement::copy ( SgCopyHelp& help) const
   {
     SgWaitStatement* result = NULL;

  // printf ("Copy SgWaitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgWaitStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgWaitStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgWaitStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgWaitStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgWaitStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != NULL) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != NULL) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
          if ( (io_stmt_list_copy != NULL) && (io_stmt_list_copy->get_parent() == NULL) && (isSgType(io_stmt_list_copy) == NULL) ) 
             { 
               io_stmt_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != NULL) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != NULL) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
          if ( (unit_copy != NULL) && (unit_copy->get_parent() == NULL) && (isSgType(unit_copy) == NULL) ) 
             { 
               unit_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != NULL) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != NULL) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
          if ( (iostat_copy != NULL) && (iostat_copy->get_parent() == NULL) && (isSgType(iostat_copy) == NULL) ) 
             { 
               iostat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != NULL) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != NULL) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
          if ( (err_copy != NULL) && (err_copy->get_parent() == NULL) && (isSgType(err_copy) == NULL) ) 
             { 
               err_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != NULL) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != NULL) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
          if ( (iomsg_copy != NULL) && (iomsg_copy->get_parent() == NULL) && (isSgType(iomsg_copy) == NULL) ) 
             { 
               iomsg_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgWaitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgWaitStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 117389 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFortranContinueStmt::copy ( SgCopyHelp& help) const
   {
     SgFortranContinueStmt* result = NULL;

  // printf ("Copy SgFortranContinueStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFortranContinueStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFortranContinueStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFortranContinueStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFortranContinueStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFortranContinueStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFortranContinueStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFortranContinueStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 117644 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWhereStatement::copy ( SgCopyHelp& help) const
   {
     SgWhereStatement* result = NULL;

  // printf ("Copy SgWhereStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgWhereStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgWhereStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgWhereStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgWhereStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: condition_copy
     SgExpression* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != NULL) 
             { 
               condition_copy = static_cast<SgExpression*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  // Copy constructor parameter data member: elsewhere_copy
     SgElseWhereStatement* elsewhere_copy; 
  // case: not a listType for (using conditionalToCopyVariable)elsewhere
          if (get_elsewhere() != NULL) 
             { 
               elsewhere_copy = static_cast<SgElseWhereStatement*>(help.copyAst(get_elsewhere())); 
             } 
            else 
             { 
               elsewhere_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgWhereStatement(  startOfConstruct_copy, condition_copy, body_copy, elsewhere_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
          if ( (condition_copy != NULL) && (condition_copy->get_parent() == NULL) && (isSgType(condition_copy) == NULL) ) 
             { 
               condition_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)elsewhere
          if ( (elsewhere_copy != NULL) && (elsewhere_copy->get_parent() == NULL) && (isSgType(elsewhere_copy) == NULL) ) 
             { 
               elsewhere_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgWhereStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgWhereStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 117962 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElseWhereStatement::copy ( SgCopyHelp& help) const
   {
     SgElseWhereStatement* result = NULL;

  // printf ("Copy SgElseWhereStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElseWhereStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElseWhereStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElseWhereStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElseWhereStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: condition_copy
     SgExpression* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != NULL) 
             { 
               condition_copy = static_cast<SgExpression*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  // Copy constructor parameter data member: elsewhere_copy
     SgElseWhereStatement* elsewhere_copy; 
  // case: not a listType for (using conditionalToCopyVariable)elsewhere
          if (get_elsewhere() != NULL) 
             { 
               elsewhere_copy = static_cast<SgElseWhereStatement*>(help.copyAst(get_elsewhere())); 
             } 
            else 
             { 
               elsewhere_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElseWhereStatement(  startOfConstruct_copy, condition_copy, body_copy, elsewhere_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
          if ( (condition_copy != NULL) && (condition_copy->get_parent() == NULL) && (isSgType(condition_copy) == NULL) ) 
             { 
               condition_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)elsewhere
          if ( (elsewhere_copy != NULL) && (elsewhere_copy->get_parent() == NULL) && (isSgType(elsewhere_copy) == NULL) ) 
             { 
               elsewhere_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElseWhereStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElseWhereStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 118265 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNullifyStatement::copy ( SgCopyHelp& help) const
   {
     SgNullifyStatement* result = NULL;

  // printf ("Copy SgNullifyStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNullifyStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNullifyStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNullifyStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNullifyStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNullifyStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): pointer_list_copy
     SgExprListExp* pointer_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pointer_list
          if (get_pointer_list() != NULL) 
             { 
               pointer_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_pointer_list())); 
             } 
            else 
             { 
               pointer_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pointer_list != NULL) delete result->p_pointer_list; 
     result->p_pointer_list = pointer_list_copy; 
  // case: not a listType for (using conditionalToSetParent)pointer_list
          if ( (pointer_list_copy != NULL) && (pointer_list_copy->get_parent() == NULL) && (isSgType(pointer_list_copy) == NULL) ) 
             { 
               pointer_list_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNullifyStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNullifyStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 118539 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgArithmeticIfStatement::copy ( SgCopyHelp& help) const
   {
     SgArithmeticIfStatement* result = NULL;

  // printf ("Copy SgArithmeticIfStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgArithmeticIfStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgArithmeticIfStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgArithmeticIfStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgArithmeticIfStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: conditional_copy
     SgExpression* conditional_copy; 
  // case: not a listType for (using conditionalToCopyVariable)conditional
          if (get_conditional() != NULL) 
             { 
               conditional_copy = static_cast<SgExpression*>(help.copyAst(get_conditional())); 
             } 
            else 
             { 
               conditional_copy = NULL; 
             } 
  // Copy constructor parameter data member: less_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for less_label
  // case: toBeCopied == COPY_DATA for less_label
     SgLabelRefExp* less_label_copy = p_less_label; 
  // Copy constructor parameter data member: equal_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for equal_label
  // case: toBeCopied == COPY_DATA for equal_label
     SgLabelRefExp* equal_label_copy = p_equal_label; 
  // Copy constructor parameter data member: greater_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for greater_label
  // case: toBeCopied == COPY_DATA for greater_label
     SgLabelRefExp* greater_label_copy = p_greater_label; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgArithmeticIfStatement(  startOfConstruct_copy, conditional_copy, less_label_copy, equal_label_copy, greater_label_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)conditional
          if ( (conditional_copy != NULL) && (conditional_copy->get_parent() == NULL) && (isSgType(conditional_copy) == NULL) ) 
             { 
               conditional_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgArithmeticIfStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgArithmeticIfStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 118822 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssignStatement::copy ( SgCopyHelp& help) const
   {
     SgAssignStatement* result = NULL;

  // printf ("Copy SgAssignStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAssignStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAssignStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAssignStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAssignStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     SgLabelRefExp* label_copy = p_label; 
  // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != NULL) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAssignStatement(  startOfConstruct_copy, label_copy, value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)value
          if ( (value_copy != NULL) && (value_copy->get_parent() == NULL) && (isSgType(value_copy) == NULL) ) 
             { 
               value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAssignStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAssignStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 119097 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgComputedGotoStatement::copy ( SgCopyHelp& help) const
   {
     SgComputedGotoStatement* result = NULL;

  // printf ("Copy SgComputedGotoStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgComputedGotoStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgComputedGotoStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgComputedGotoStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgComputedGotoStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: labelList_copy
     SgExprListExp* labelList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)labelList
          if (get_labelList() != NULL) 
             { 
               labelList_copy = static_cast<SgExprListExp*>(help.copyAst(get_labelList())); 
             } 
            else 
             { 
               labelList_copy = NULL; 
             } 
  // Copy constructor parameter data member: label_index_copy
     SgExpression* label_index_copy; 
  // case: not a listType for (using conditionalToCopyVariable)label_index
          if (get_label_index() != NULL) 
             { 
               label_index_copy = static_cast<SgExpression*>(help.copyAst(get_label_index())); 
             } 
            else 
             { 
               label_index_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgComputedGotoStatement(  startOfConstruct_copy, labelList_copy, label_index_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)labelList
          if ( (labelList_copy != NULL) && (labelList_copy->get_parent() == NULL) && (isSgType(labelList_copy) == NULL) ) 
             { 
               labelList_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)label_index
          if ( (label_index_copy != NULL) && (label_index_copy->get_parent() == NULL) && (isSgType(label_index_copy) == NULL) ) 
             { 
               label_index_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgComputedGotoStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgComputedGotoStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 119384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssignedGotoStatement::copy ( SgCopyHelp& help) const
   {
     SgAssignedGotoStatement* result = NULL;

  // printf ("Copy SgAssignedGotoStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAssignedGotoStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAssignedGotoStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAssignedGotoStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAssignedGotoStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: targets_copy
     SgExprListExp* targets_copy; 
  // case: not a listType for (using conditionalToCopyVariable)targets
          if (get_targets() != NULL) 
             { 
               targets_copy = static_cast<SgExprListExp*>(help.copyAst(get_targets())); 
             } 
            else 
             { 
               targets_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAssignedGotoStatement(  startOfConstruct_copy, targets_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)targets
          if ( (targets_copy != NULL) && (targets_copy->get_parent() == NULL) && (isSgType(targets_copy) == NULL) ) 
             { 
               targets_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAssignedGotoStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAssignedGotoStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 119655 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAllocateStatement::copy ( SgCopyHelp& help) const
   {
     SgAllocateStatement* result = NULL;

  // printf ("Copy SgAllocateStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAllocateStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAllocateStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAllocateStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAllocateStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAllocateStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): expr_list_copy
     SgExprListExp* expr_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expr_list
          if (get_expr_list() != NULL) 
             { 
               expr_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_expr_list())); 
             } 
            else 
             { 
               expr_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_expr_list != NULL) delete result->p_expr_list; 
     result->p_expr_list = expr_list_copy; 
  // case: not a listType for (using conditionalToSetParent)expr_list
          if ( (expr_list_copy != NULL) && (expr_list_copy->get_parent() == NULL) && (isSgType(expr_list_copy) == NULL) ) 
             { 
               expr_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): stat_expression_copy
     SgExpression* stat_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat_expression
          if (get_stat_expression() != NULL) 
             { 
               stat_expression_copy = static_cast<SgExpression*>(help.copyAst(get_stat_expression())); 
             } 
            else 
             { 
               stat_expression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat_expression != NULL) delete result->p_stat_expression; 
     result->p_stat_expression = stat_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)stat_expression
          if ( (stat_expression_copy != NULL) && (stat_expression_copy->get_parent() == NULL) && (isSgType(stat_expression_copy) == NULL) ) 
             { 
               stat_expression_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): errmsg_expression_copy
     SgExpression* errmsg_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)errmsg_expression
          if (get_errmsg_expression() != NULL) 
             { 
               errmsg_expression_copy = static_cast<SgExpression*>(help.copyAst(get_errmsg_expression())); 
             } 
            else 
             { 
               errmsg_expression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_errmsg_expression != NULL) delete result->p_errmsg_expression; 
     result->p_errmsg_expression = errmsg_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)errmsg_expression
          if ( (errmsg_expression_copy != NULL) && (errmsg_expression_copy->get_parent() == NULL) && (isSgType(errmsg_expression_copy) == NULL) ) 
             { 
               errmsg_expression_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): source_expression_copy
     SgExpression* source_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)source_expression
          if (get_source_expression() != NULL) 
             { 
               source_expression_copy = static_cast<SgExpression*>(help.copyAst(get_source_expression())); 
             } 
            else 
             { 
               source_expression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_source_expression != NULL) delete result->p_source_expression; 
     result->p_source_expression = source_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)source_expression
          if ( (source_expression_copy != NULL) && (source_expression_copy->get_parent() == NULL) && (isSgType(source_expression_copy) == NULL) ) 
             { 
               source_expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAllocateStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAllocateStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 119986 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeallocateStatement::copy ( SgCopyHelp& help) const
   {
     SgDeallocateStatement* result = NULL;

  // printf ("Copy SgDeallocateStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDeallocateStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDeallocateStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDeallocateStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDeallocateStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDeallocateStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): expr_list_copy
     SgExprListExp* expr_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expr_list
          if (get_expr_list() != NULL) 
             { 
               expr_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_expr_list())); 
             } 
            else 
             { 
               expr_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_expr_list != NULL) delete result->p_expr_list; 
     result->p_expr_list = expr_list_copy; 
  // case: not a listType for (using conditionalToSetParent)expr_list
          if ( (expr_list_copy != NULL) && (expr_list_copy->get_parent() == NULL) && (isSgType(expr_list_copy) == NULL) ) 
             { 
               expr_list_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): stat_expression_copy
     SgExpression* stat_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat_expression
          if (get_stat_expression() != NULL) 
             { 
               stat_expression_copy = static_cast<SgExpression*>(help.copyAst(get_stat_expression())); 
             } 
            else 
             { 
               stat_expression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat_expression != NULL) delete result->p_stat_expression; 
     result->p_stat_expression = stat_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)stat_expression
          if ( (stat_expression_copy != NULL) && (stat_expression_copy->get_parent() == NULL) && (isSgType(stat_expression_copy) == NULL) ) 
             { 
               stat_expression_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): errmsg_expression_copy
     SgExpression* errmsg_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)errmsg_expression
          if (get_errmsg_expression() != NULL) 
             { 
               errmsg_expression_copy = static_cast<SgExpression*>(help.copyAst(get_errmsg_expression())); 
             } 
            else 
             { 
               errmsg_expression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_errmsg_expression != NULL) delete result->p_errmsg_expression; 
     result->p_errmsg_expression = errmsg_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)errmsg_expression
          if ( (errmsg_expression_copy != NULL) && (errmsg_expression_copy->get_parent() == NULL) && (isSgType(errmsg_expression_copy) == NULL) ) 
             { 
               errmsg_expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDeallocateStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDeallocateStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 120298 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcNotifyStatement::copy ( SgCopyHelp& help) const
   {
     SgUpcNotifyStatement* result = NULL;

  // printf ("Copy SgUpcNotifyStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcNotifyStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcNotifyStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcNotifyStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcNotifyStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: notify_expression_copy
     SgExpression* notify_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)notify_expression
          if (get_notify_expression() != NULL) 
             { 
               notify_expression_copy = static_cast<SgExpression*>(help.copyAst(get_notify_expression())); 
             } 
            else 
             { 
               notify_expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcNotifyStatement(  startOfConstruct_copy, notify_expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)notify_expression
          if ( (notify_expression_copy != NULL) && (notify_expression_copy->get_parent() == NULL) && (isSgType(notify_expression_copy) == NULL) ) 
             { 
               notify_expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcNotifyStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcNotifyStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 120569 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcWaitStatement::copy ( SgCopyHelp& help) const
   {
     SgUpcWaitStatement* result = NULL;

  // printf ("Copy SgUpcWaitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcWaitStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcWaitStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcWaitStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcWaitStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: wait_expression_copy
     SgExpression* wait_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)wait_expression
          if (get_wait_expression() != NULL) 
             { 
               wait_expression_copy = static_cast<SgExpression*>(help.copyAst(get_wait_expression())); 
             } 
            else 
             { 
               wait_expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcWaitStatement(  startOfConstruct_copy, wait_expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)wait_expression
          if ( (wait_expression_copy != NULL) && (wait_expression_copy->get_parent() == NULL) && (isSgType(wait_expression_copy) == NULL) ) 
             { 
               wait_expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcWaitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcWaitStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 120840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcBarrierStatement::copy ( SgCopyHelp& help) const
   {
     SgUpcBarrierStatement* result = NULL;

  // printf ("Copy SgUpcBarrierStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcBarrierStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcBarrierStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcBarrierStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcBarrierStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: barrier_expression_copy
     SgExpression* barrier_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)barrier_expression
          if (get_barrier_expression() != NULL) 
             { 
               barrier_expression_copy = static_cast<SgExpression*>(help.copyAst(get_barrier_expression())); 
             } 
            else 
             { 
               barrier_expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcBarrierStatement(  startOfConstruct_copy, barrier_expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)barrier_expression
          if ( (barrier_expression_copy != NULL) && (barrier_expression_copy->get_parent() == NULL) && (isSgType(barrier_expression_copy) == NULL) ) 
             { 
               barrier_expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcBarrierStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcBarrierStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 121111 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcFenceStatement::copy ( SgCopyHelp& help) const
   {
     SgUpcFenceStatement* result = NULL;

  // printf ("Copy SgUpcFenceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcFenceStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcFenceStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcFenceStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcFenceStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcFenceStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcFenceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcFenceStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 121366 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpBarrierStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpBarrierStatement* result = NULL;

  // printf ("Copy SgOmpBarrierStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpBarrierStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpBarrierStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpBarrierStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpBarrierStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpBarrierStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpBarrierStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpBarrierStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 121621 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpTaskwaitStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpTaskwaitStatement* result = NULL;

  // printf ("Copy SgOmpTaskwaitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpTaskwaitStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpTaskwaitStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpTaskwaitStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpTaskwaitStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpTaskwaitStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpTaskwaitStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpTaskwaitStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 121876 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpFlushStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpFlushStatement* result = NULL;

  // printf ("Copy SgOmpFlushStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpFlushStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpFlushStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpFlushStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpFlushStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpFlushStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgVarRefExpPtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgVarRefExpPtrList::const_iterator source_variables_iterator = get_variables().begin(); 
     for ( /* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) 
        { 
          SgVarRefExp* source_list_element = *source_variables_iterator; 
          SgVarRefExp* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgVarRefExp*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          variables_copy.push_back(copy_list_element); 
        } 
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
     SgVarRefExpPtrList::const_iterator variables_iterator = result->get_variables().begin(); 
     for ( /* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) 
        { 
          SgVarRefExp* list_element = *variables_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpFlushStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpFlushStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 122162 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpBodyStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpBodyStatement* result = NULL;

  // printf ("Copy SgOmpBodyStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpBodyStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpBodyStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpBodyStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpBodyStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpBodyStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpBodyStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpBodyStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 122433 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpMasterStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpMasterStatement* result = NULL;

  // printf ("Copy SgOmpMasterStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpMasterStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpMasterStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpMasterStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpMasterStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpMasterStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpMasterStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpMasterStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 122704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpOrderedStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpOrderedStatement* result = NULL;

  // printf ("Copy SgOmpOrderedStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpOrderedStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpOrderedStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpOrderedStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpOrderedStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpOrderedStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpOrderedStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpOrderedStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 122975 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpCriticalStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpCriticalStatement* result = NULL;

  // printf ("Copy SgOmpCriticalStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpCriticalStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpCriticalStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpCriticalStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpCriticalStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpCriticalStatement(  startOfConstruct_copy, body_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpCriticalStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpCriticalStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 123250 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSectionStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpSectionStatement* result = NULL;

  // printf ("Copy SgOmpSectionStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpSectionStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpSectionStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpSectionStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpSectionStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpSectionStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpSectionStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpSectionStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 123521 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpWorkshareStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpWorkshareStatement* result = NULL;

  // printf ("Copy SgOmpWorkshareStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpWorkshareStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpWorkshareStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpWorkshareStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpWorkshareStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpWorkshareStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpWorkshareStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpWorkshareStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 123792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpClauseBodyStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpClauseBodyStatement* result = NULL;

  // printf ("Copy SgOmpClauseBodyStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpClauseBodyStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpClauseBodyStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpClauseBodyStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpClauseBodyStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpClauseBodyStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpClauseBodyStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpClauseBodyStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 124094 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpParallelStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpParallelStatement* result = NULL;

  // printf ("Copy SgOmpParallelStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpParallelStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpParallelStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpParallelStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpParallelStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpParallelStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpParallelStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpParallelStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 124396 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSingleStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpSingleStatement* result = NULL;

  // printf ("Copy SgOmpSingleStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpSingleStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpSingleStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpSingleStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpSingleStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpSingleStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpSingleStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpSingleStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 124698 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpAtomicStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpAtomicStatement* result = NULL;

  // printf ("Copy SgOmpAtomicStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpAtomicStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpAtomicStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpAtomicStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpAtomicStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpAtomicStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpAtomicStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpAtomicStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 125000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpTaskStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpTaskStatement* result = NULL;

  // printf ("Copy SgOmpTaskStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpTaskStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpTaskStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpTaskStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpTaskStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpTaskStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpTaskStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpTaskStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 125302 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpForStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpForStatement* result = NULL;

  // printf ("Copy SgOmpForStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpForStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpForStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpForStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpForStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpForStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpForStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpForStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 125604 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDoStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpDoStatement* result = NULL;

  // printf ("Copy SgOmpDoStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpDoStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpDoStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpDoStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpDoStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpDoStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpDoStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpDoStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 125906 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSectionsStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpSectionsStatement* result = NULL;

  // printf ("Copy SgOmpSectionsStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpSectionsStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpSectionsStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpSectionsStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpSectionsStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpSectionsStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpSectionsStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpSectionsStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 126208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpTargetStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpTargetStatement* result = NULL;

  // printf ("Copy SgOmpTargetStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpTargetStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpTargetStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpTargetStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpTargetStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpTargetStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpTargetStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpTargetStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 126510 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpTargetDataStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpTargetDataStatement* result = NULL;

  // printf ("Copy SgOmpTargetDataStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpTargetDataStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpTargetDataStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpTargetDataStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpTargetDataStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpTargetDataStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpTargetDataStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpTargetDataStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 126812 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSimdStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpSimdStatement* result = NULL;

  // printf ("Copy SgOmpSimdStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpSimdStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpSimdStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpSimdStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpSimdStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpSimdStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpSimdStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpSimdStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 127114 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpForSimdStatement::copy ( SgCopyHelp& help) const
   {
     SgOmpForSimdStatement* result = NULL;

  // printf ("Copy SgOmpForSimdStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOmpForSimdStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOmpForSimdStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOmpForSimdStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOmpForSimdStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOmpForSimdStatement(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator source_clauses_iterator = get_clauses().begin(); 
     for ( /* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) 
        { 
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          clauses_copy.push_back(copy_list_element); 
        } 
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
     SgOmpClausePtrList::const_iterator clauses_iterator = result->get_clauses().begin(); 
     for ( /* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) 
        { 
          SgOmpClause* list_element = *clauses_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOmpForSimdStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOmpForSimdStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 127416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSequenceStatement::copy ( SgCopyHelp& help) const
   {
     SgSequenceStatement* result = NULL;

  // printf ("Copy SgSequenceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSequenceStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSequenceStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSequenceStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSequenceStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSequenceStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSequenceStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSequenceStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 127671 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWithStatement::copy ( SgCopyHelp& help) const
   {
     SgWithStatement* result = NULL;

  // printf ("Copy SgWithStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgWithStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgWithStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgWithStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgWithStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgWithStatement(  startOfConstruct_copy, expression_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgWithStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgWithStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 127958 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPythonPrintStmt::copy ( SgCopyHelp& help) const
   {
     SgPythonPrintStmt* result = NULL;

  // printf ("Copy SgPythonPrintStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPythonPrintStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPythonPrintStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPythonPrintStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPythonPrintStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: destination_copy
     SgExpression* destination_copy; 
  // case: not a listType for (using conditionalToCopyVariable)destination
          if (get_destination() != NULL) 
             { 
               destination_copy = static_cast<SgExpression*>(help.copyAst(get_destination())); 
             } 
            else 
             { 
               destination_copy = NULL; 
             } 
  // Copy constructor parameter data member: values_copy
     SgExprListExp* values_copy; 
  // case: not a listType for (using conditionalToCopyVariable)values
          if (get_values() != NULL) 
             { 
               values_copy = static_cast<SgExprListExp*>(help.copyAst(get_values())); 
             } 
            else 
             { 
               values_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPythonPrintStmt(  startOfConstruct_copy, destination_copy, values_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)destination
          if ( (destination_copy != NULL) && (destination_copy->get_parent() == NULL) && (isSgType(destination_copy) == NULL) ) 
             { 
               destination_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)values
          if ( (values_copy != NULL) && (values_copy->get_parent() == NULL) && (isSgType(values_copy) == NULL) ) 
             { 
               values_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPythonPrintStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPythonPrintStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 128245 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPassStatement::copy ( SgCopyHelp& help) const
   {
     SgPassStatement* result = NULL;

  // printf ("Copy SgPassStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPassStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPassStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPassStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPassStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPassStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPassStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPassStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 128500 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssertStmt::copy ( SgCopyHelp& help) const
   {
     SgAssertStmt* result = NULL;

  // printf ("Copy SgAssertStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAssertStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAssertStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAssertStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAssertStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: test_copy
     SgExpression* test_copy; 
  // case: not a listType for (using conditionalToCopyVariable)test
          if (get_test() != NULL) 
             { 
               test_copy = static_cast<SgExpression*>(help.copyAst(get_test())); 
             } 
            else 
             { 
               test_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAssertStmt(  startOfConstruct_copy, test_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): exception_argument_copy
     SgExpression* exception_argument_copy; 
  // case: not a listType for (using conditionalToCopyVariable)exception_argument
          if (get_exception_argument() != NULL) 
             { 
               exception_argument_copy = static_cast<SgExpression*>(help.copyAst(get_exception_argument())); 
             } 
            else 
             { 
               exception_argument_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_exception_argument != NULL) delete result->p_exception_argument; 
     result->p_exception_argument = exception_argument_copy; 
  // case: not a listType for (using conditionalToSetParent)exception_argument
          if ( (exception_argument_copy != NULL) && (exception_argument_copy->get_parent() == NULL) && (isSgType(exception_argument_copy) == NULL) ) 
             { 
               exception_argument_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)test
          if ( (test_copy != NULL) && (test_copy->get_parent() == NULL) && (isSgType(test_copy) == NULL) ) 
             { 
               test_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAssertStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAssertStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 128790 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExecStatement::copy ( SgCopyHelp& help) const
   {
     SgExecStatement* result = NULL;

  // printf ("Copy SgExecStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgExecStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgExecStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgExecStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgExecStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: executable_copy
     SgExpression* executable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executable
          if (get_executable() != NULL) 
             { 
               executable_copy = static_cast<SgExpression*>(help.copyAst(get_executable())); 
             } 
            else 
             { 
               executable_copy = NULL; 
             } 
  // Copy constructor parameter data member: globals_copy
     SgExpression* globals_copy; 
  // case: not a listType for (using conditionalToCopyVariable)globals
          if (get_globals() != NULL) 
             { 
               globals_copy = static_cast<SgExpression*>(help.copyAst(get_globals())); 
             } 
            else 
             { 
               globals_copy = NULL; 
             } 
  // Copy constructor parameter data member: locals_copy
     SgExpression* locals_copy; 
  // case: not a listType for (using conditionalToCopyVariable)locals
          if (get_locals() != NULL) 
             { 
               locals_copy = static_cast<SgExpression*>(help.copyAst(get_locals())); 
             } 
            else 
             { 
               locals_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgExecStatement(  startOfConstruct_copy, executable_copy, globals_copy, locals_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)executable
          if ( (executable_copy != NULL) && (executable_copy->get_parent() == NULL) && (isSgType(executable_copy) == NULL) ) 
             { 
               executable_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)globals
          if ( (globals_copy != NULL) && (globals_copy->get_parent() == NULL) && (isSgType(globals_copy) == NULL) ) 
             { 
               globals_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)locals
          if ( (locals_copy != NULL) && (locals_copy->get_parent() == NULL) && (isSgType(locals_copy) == NULL) ) 
             { 
               locals_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgExecStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgExecStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 129093 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPythonGlobalStmt::copy ( SgCopyHelp& help) const
   {
     SgPythonGlobalStmt* result = NULL;

  // printf ("Copy SgPythonGlobalStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPythonGlobalStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPythonGlobalStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPythonGlobalStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPythonGlobalStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPythonGlobalStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_names()
     SgInitializedNamePtrList names_copy; 
  // case: listType for names
  // case: listType (typeIsPointerToList == false) for names
     SgInitializedNamePtrList::const_iterator source_names_iterator = get_names().begin(); 
     for ( /* empty by design */; source_names_iterator != get_names().end(); ++source_names_iterator) 
        { 
          SgInitializedName* source_list_element = *source_names_iterator; 
          SgInitializedName* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_names_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          names_copy.push_back(copy_list_element); 
        } 
     result->p_names = names_copy; 
  // case: listType for names
  // case: listType (typeIsPointerToList == false) for names
     SgInitializedNamePtrList::const_iterator names_iterator = result->get_names().begin(); 
     for ( /* empty by design */; names_iterator != result->get_names().end(); ++names_iterator) 
        { 
          SgInitializedName* list_element = *names_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPythonGlobalStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPythonGlobalStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 129379 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaThrowStatement::copy ( SgCopyHelp& help) const
   {
     SgJavaThrowStatement* result = NULL;

  // printf ("Copy SgJavaThrowStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaThrowStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaThrowStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaThrowStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaThrowStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: throwOp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for throwOp
  // case: toBeCopied == COPY_DATA for throwOp
     SgThrowOp* throwOp_copy = p_throwOp; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaThrowStatement(  startOfConstruct_copy, throwOp_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaThrowStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaThrowStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 129638 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaSynchronizedStatement::copy ( SgCopyHelp& help) const
   {
     SgJavaSynchronizedStatement* result = NULL;

  // printf ("Copy SgJavaSynchronizedStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaSynchronizedStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaSynchronizedStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaSynchronizedStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaSynchronizedStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaSynchronizedStatement(  startOfConstruct_copy, expression_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaSynchronizedStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaSynchronizedStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 129925 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsyncStmt::copy ( SgCopyHelp& help) const
   {
     SgAsyncStmt* result = NULL;

  // printf ("Copy SgAsyncStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsyncStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsyncStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsyncStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsyncStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsyncStmt(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsyncStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsyncStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 130196 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFinishStmt::copy ( SgCopyHelp& help) const
   {
     SgFinishStmt* result = NULL;

  // printf ("Copy SgFinishStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFinishStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFinishStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFinishStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFinishStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFinishStmt(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFinishStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFinishStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 130467 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAtStmt::copy ( SgCopyHelp& help) const
   {
     SgAtStmt* result = NULL;

  // printf ("Copy SgAtStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAtStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAtStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAtStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAtStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAtStmt(  startOfConstruct_copy, expression_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAtStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAtStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 130754 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAtomicStmt::copy ( SgCopyHelp& help) const
   {
     SgAtomicStmt* result = NULL;

  // printf ("Copy SgAtomicStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAtomicStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAtomicStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAtomicStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAtomicStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAtomicStmt(  startOfConstruct_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAtomicStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAtomicStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 131025 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWhenStmt::copy ( SgCopyHelp& help) const
   {
     SgWhenStmt* result = NULL;

  // printf ("Copy SgWhenStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgWhenStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgWhenStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgWhenStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgWhenStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgWhenStmt(  startOfConstruct_copy, expression_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgWhenStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgWhenStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 131312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImageControlStatement::copy ( SgCopyHelp& help) const
   {
     SgImageControlStatement* result = NULL;

  // printf ("Copy SgImageControlStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgImageControlStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgImageControlStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgImageControlStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgImageControlStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgImageControlStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != NULL) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != NULL) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
          if ( (stat_copy != NULL) && (stat_copy->get_parent() == NULL) && (isSgType(stat_copy) == NULL) ) 
             { 
               stat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != NULL) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != NULL) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
          if ( (err_msg_copy != NULL) && (err_msg_copy->get_parent() == NULL) && (isSgType(err_msg_copy) == NULL) ) 
             { 
               err_msg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != NULL) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != NULL) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
          if ( (acquired_lock_copy != NULL) && (acquired_lock_copy->get_parent() == NULL) && (isSgType(acquired_lock_copy) == NULL) ) 
             { 
               acquired_lock_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgImageControlStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgImageControlStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 131629 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSyncAllStatement::copy ( SgCopyHelp& help) const
   {
     SgSyncAllStatement* result = NULL;

  // printf ("Copy SgSyncAllStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSyncAllStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSyncAllStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSyncAllStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSyncAllStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSyncAllStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != NULL) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != NULL) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
          if ( (stat_copy != NULL) && (stat_copy->get_parent() == NULL) && (isSgType(stat_copy) == NULL) ) 
             { 
               stat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != NULL) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != NULL) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
          if ( (err_msg_copy != NULL) && (err_msg_copy->get_parent() == NULL) && (isSgType(err_msg_copy) == NULL) ) 
             { 
               err_msg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != NULL) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != NULL) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
          if ( (acquired_lock_copy != NULL) && (acquired_lock_copy->get_parent() == NULL) && (isSgType(acquired_lock_copy) == NULL) ) 
             { 
               acquired_lock_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSyncAllStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSyncAllStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 131946 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSyncImagesStatement::copy ( SgCopyHelp& help) const
   {
     SgSyncImagesStatement* result = NULL;

  // printf ("Copy SgSyncImagesStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSyncImagesStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSyncImagesStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSyncImagesStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSyncImagesStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: image_set_copy
     SgExpression* image_set_copy; 
  // case: not a listType for (using conditionalToCopyVariable)image_set
          if (get_image_set() != NULL) 
             { 
               image_set_copy = static_cast<SgExpression*>(help.copyAst(get_image_set())); 
             } 
            else 
             { 
               image_set_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSyncImagesStatement(  startOfConstruct_copy, image_set_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != NULL) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != NULL) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
          if ( (stat_copy != NULL) && (stat_copy->get_parent() == NULL) && (isSgType(stat_copy) == NULL) ) 
             { 
               stat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != NULL) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != NULL) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
          if ( (err_msg_copy != NULL) && (err_msg_copy->get_parent() == NULL) && (isSgType(err_msg_copy) == NULL) ) 
             { 
               err_msg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != NULL) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != NULL) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
          if ( (acquired_lock_copy != NULL) && (acquired_lock_copy->get_parent() == NULL) && (isSgType(acquired_lock_copy) == NULL) ) 
             { 
               acquired_lock_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)image_set
          if ( (image_set_copy != NULL) && (image_set_copy->get_parent() == NULL) && (isSgType(image_set_copy) == NULL) ) 
             { 
               image_set_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSyncImagesStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSyncImagesStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 132279 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSyncMemoryStatement::copy ( SgCopyHelp& help) const
   {
     SgSyncMemoryStatement* result = NULL;

  // printf ("Copy SgSyncMemoryStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSyncMemoryStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSyncMemoryStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSyncMemoryStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSyncMemoryStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSyncMemoryStatement(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != NULL) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != NULL) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
          if ( (stat_copy != NULL) && (stat_copy->get_parent() == NULL) && (isSgType(stat_copy) == NULL) ) 
             { 
               stat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != NULL) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != NULL) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
          if ( (err_msg_copy != NULL) && (err_msg_copy->get_parent() == NULL) && (isSgType(err_msg_copy) == NULL) ) 
             { 
               err_msg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != NULL) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != NULL) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
          if ( (acquired_lock_copy != NULL) && (acquired_lock_copy->get_parent() == NULL) && (isSgType(acquired_lock_copy) == NULL) ) 
             { 
               acquired_lock_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSyncMemoryStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSyncMemoryStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 132596 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSyncTeamStatement::copy ( SgCopyHelp& help) const
   {
     SgSyncTeamStatement* result = NULL;

  // printf ("Copy SgSyncTeamStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSyncTeamStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSyncTeamStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSyncTeamStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSyncTeamStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: team_value_copy
     SgExpression* team_value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)team_value
          if (get_team_value() != NULL) 
             { 
               team_value_copy = static_cast<SgExpression*>(help.copyAst(get_team_value())); 
             } 
            else 
             { 
               team_value_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSyncTeamStatement(  startOfConstruct_copy, team_value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != NULL) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != NULL) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
          if ( (stat_copy != NULL) && (stat_copy->get_parent() == NULL) && (isSgType(stat_copy) == NULL) ) 
             { 
               stat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != NULL) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != NULL) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
          if ( (err_msg_copy != NULL) && (err_msg_copy->get_parent() == NULL) && (isSgType(err_msg_copy) == NULL) ) 
             { 
               err_msg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != NULL) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != NULL) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
          if ( (acquired_lock_copy != NULL) && (acquired_lock_copy->get_parent() == NULL) && (isSgType(acquired_lock_copy) == NULL) ) 
             { 
               acquired_lock_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)team_value
          if ( (team_value_copy != NULL) && (team_value_copy->get_parent() == NULL) && (isSgType(team_value_copy) == NULL) ) 
             { 
               team_value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSyncTeamStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSyncTeamStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 132929 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLockStatement::copy ( SgCopyHelp& help) const
   {
     SgLockStatement* result = NULL;

  // printf ("Copy SgLockStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLockStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLockStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLockStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLockStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lock_variable_copy
     SgExpression* lock_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lock_variable
          if (get_lock_variable() != NULL) 
             { 
               lock_variable_copy = static_cast<SgExpression*>(help.copyAst(get_lock_variable())); 
             } 
            else 
             { 
               lock_variable_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLockStatement(  startOfConstruct_copy, lock_variable_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != NULL) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != NULL) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
          if ( (stat_copy != NULL) && (stat_copy->get_parent() == NULL) && (isSgType(stat_copy) == NULL) ) 
             { 
               stat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != NULL) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != NULL) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
          if ( (err_msg_copy != NULL) && (err_msg_copy->get_parent() == NULL) && (isSgType(err_msg_copy) == NULL) ) 
             { 
               err_msg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != NULL) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != NULL) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
          if ( (acquired_lock_copy != NULL) && (acquired_lock_copy->get_parent() == NULL) && (isSgType(acquired_lock_copy) == NULL) ) 
             { 
               acquired_lock_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)lock_variable
          if ( (lock_variable_copy != NULL) && (lock_variable_copy->get_parent() == NULL) && (isSgType(lock_variable_copy) == NULL) ) 
             { 
               lock_variable_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLockStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLockStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 133262 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnlockStatement::copy ( SgCopyHelp& help) const
   {
     SgUnlockStatement* result = NULL;

  // printf ("Copy SgUnlockStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnlockStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnlockStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnlockStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnlockStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lock_variable_copy
     SgExpression* lock_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lock_variable
          if (get_lock_variable() != NULL) 
             { 
               lock_variable_copy = static_cast<SgExpression*>(help.copyAst(get_lock_variable())); 
             } 
            else 
             { 
               lock_variable_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnlockStatement(  startOfConstruct_copy, lock_variable_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != NULL) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != NULL) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
          if ( (stat_copy != NULL) && (stat_copy->get_parent() == NULL) && (isSgType(stat_copy) == NULL) ) 
             { 
               stat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != NULL) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != NULL) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
          if ( (err_msg_copy != NULL) && (err_msg_copy->get_parent() == NULL) && (isSgType(err_msg_copy) == NULL) ) 
             { 
               err_msg_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != NULL) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != NULL) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
          if ( (acquired_lock_copy != NULL) && (acquired_lock_copy->get_parent() == NULL) && (isSgType(acquired_lock_copy) == NULL) ) 
             { 
               acquired_lock_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)lock_variable
          if ( (lock_variable_copy != NULL) && (lock_variable_copy->get_parent() == NULL) && (isSgType(lock_variable_copy) == NULL) ) 
             { 
               lock_variable_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnlockStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnlockStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 133595 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaExitStmt::copy ( SgCopyHelp& help) const
   {
     SgAdaExitStmt* result = NULL;

  // printf ("Copy SgAdaExitStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaExitStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaExitStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaExitStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaExitStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: loop_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for loop
  // case: toBeCopied == COPY_DATA for loop
     SgStatement* loop_copy = p_loop; 
  // Copy constructor parameter data member: condition_copy
     SgExpression* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != NULL) 
             { 
               condition_copy = static_cast<SgExpression*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = NULL; 
             } 
  // Copy constructor parameter data member: explicitLoopName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicitLoopName
  // case: toBeCopied == COPY_DATA for explicitLoopName
     bool explicitLoopName_copy = p_explicitLoopName; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaExitStmt(  startOfConstruct_copy, loop_copy, condition_copy, explicitLoopName_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
          if ( (condition_copy != NULL) && (condition_copy->get_parent() == NULL) && (isSgType(condition_copy) == NULL) ) 
             { 
               condition_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaExitStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaExitStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 133874 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDelayStmt::copy ( SgCopyHelp& help) const
   {
     SgAdaDelayStmt* result = NULL;

  // printf ("Copy SgAdaDelayStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaDelayStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaDelayStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaDelayStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaDelayStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: time_copy
     SgExpression* time_copy; 
  // case: not a listType for (using conditionalToCopyVariable)time
          if (get_time() != NULL) 
             { 
               time_copy = static_cast<SgExpression*>(help.copyAst(get_time())); 
             } 
            else 
             { 
               time_copy = NULL; 
             } 
  // Copy constructor parameter data member: isRelative_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRelative
  // case: toBeCopied == COPY_DATA for isRelative
     bool isRelative_copy = p_isRelative; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaDelayStmt(  startOfConstruct_copy, time_copy, isRelative_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)time
          if ( (time_copy != NULL) && (time_copy->get_parent() == NULL) && (isSgType(time_copy) == NULL) ) 
             { 
               time_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaDelayStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaDelayStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 134149 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaLoopStmt::copy ( SgCopyHelp& help) const
   {
     SgAdaLoopStmt* result = NULL;

  // printf ("Copy SgAdaLoopStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaLoopStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaLoopStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaLoopStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaLoopStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaLoopStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaLoopStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaLoopStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 134428 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaSelectStmt::copy ( SgCopyHelp& help) const
   {
     SgAdaSelectStmt* result = NULL;

  // printf ("Copy SgAdaSelectStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaSelectStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaSelectStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaSelectStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaSelectStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaSelectStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): select_path_copy
     SgAdaSelectAlternativeStmt* select_path_copy; 
  // case: not a listType for (using conditionalToCopyVariable)select_path
          if (get_select_path() != NULL) 
             { 
               select_path_copy = static_cast<SgAdaSelectAlternativeStmt*>(help.copyAst(get_select_path())); 
             } 
            else 
             { 
               select_path_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_select_path != NULL) delete result->p_select_path; 
     result->p_select_path = select_path_copy; 
  // case: not a listType for (using conditionalToSetParent)select_path
          if ( (select_path_copy != NULL) && (select_path_copy->get_parent() == NULL) && (isSgType(select_path_copy) == NULL) ) 
             { 
               select_path_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): or_path_copy
     SgAdaSelectAlternativeStmt* or_path_copy; 
  // case: not a listType for (using conditionalToCopyVariable)or_path
          if (get_or_path() != NULL) 
             { 
               or_path_copy = static_cast<SgAdaSelectAlternativeStmt*>(help.copyAst(get_or_path())); 
             } 
            else 
             { 
               or_path_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_or_path != NULL) delete result->p_or_path; 
     result->p_or_path = or_path_copy; 
  // case: not a listType for (using conditionalToSetParent)or_path
          if ( (or_path_copy != NULL) && (or_path_copy->get_parent() == NULL) && (isSgType(or_path_copy) == NULL) ) 
             { 
               or_path_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): else_path_copy
     SgBasicBlock* else_path_copy; 
  // case: not a listType for (using conditionalToCopyVariable)else_path
          if (get_else_path() != NULL) 
             { 
               else_path_copy = static_cast<SgBasicBlock*>(help.copyAst(get_else_path())); 
             } 
            else 
             { 
               else_path_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_else_path != NULL) delete result->p_else_path; 
     result->p_else_path = else_path_copy; 
  // case: not a listType for (using conditionalToSetParent)else_path
          if ( (else_path_copy != NULL) && (else_path_copy->get_parent() == NULL) && (isSgType(else_path_copy) == NULL) ) 
             { 
               else_path_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): abort_path_copy
     SgBasicBlock* abort_path_copy; 
  // case: not a listType for (using conditionalToCopyVariable)abort_path
          if (get_abort_path() != NULL) 
             { 
               abort_path_copy = static_cast<SgBasicBlock*>(help.copyAst(get_abort_path())); 
             } 
            else 
             { 
               abort_path_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_abort_path != NULL) delete result->p_abort_path; 
     result->p_abort_path = abort_path_copy; 
  // case: not a listType for (using conditionalToSetParent)abort_path
          if ( (abort_path_copy != NULL) && (abort_path_copy->get_parent() == NULL) && (isSgType(abort_path_copy) == NULL) ) 
             { 
               abort_path_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): select_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for select_type
  // case: toBeCopied == COPY_DATA for select_type
     SgAdaSelectStmt::select_type_enum select_type_copy = p_select_type; 
     result->p_select_type = select_type_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaSelectStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaSelectStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 134764 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaSelectAlternativeStmt::copy ( SgCopyHelp& help) const
   {
     SgAdaSelectAlternativeStmt* result = NULL;

  // printf ("Copy SgAdaSelectAlternativeStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaSelectAlternativeStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaSelectAlternativeStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaSelectAlternativeStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaSelectAlternativeStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaSelectAlternativeStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): guard_copy
     SgExpression* guard_copy; 
  // case: not a listType for (using conditionalToCopyVariable)guard
          if (get_guard() != NULL) 
             { 
               guard_copy = static_cast<SgExpression*>(help.copyAst(get_guard())); 
             } 
            else 
             { 
               guard_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_guard != NULL) delete result->p_guard; 
     result->p_guard = guard_copy; 
  // case: not a listType for (using conditionalToSetParent)guard
          if ( (guard_copy != NULL) && (guard_copy->get_parent() == NULL) && (isSgType(guard_copy) == NULL) ) 
             { 
               guard_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): next_copy
     SgAdaSelectAlternativeStmt* next_copy; 
  // case: not a listType for (using conditionalToCopyVariable)next
          if (get_next() != NULL) 
             { 
               next_copy = static_cast<SgAdaSelectAlternativeStmt*>(help.copyAst(get_next())); 
             } 
            else 
             { 
               next_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_next != NULL) delete result->p_next; 
     result->p_next = next_copy; 
  // case: not a listType for (using conditionalToSetParent)next
          if ( (next_copy != NULL) && (next_copy->get_parent() == NULL) && (isSgType(next_copy) == NULL) ) 
             { 
               next_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaSelectAlternativeStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaSelectAlternativeStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 135076 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTerminateStmt::copy ( SgCopyHelp& help) const
   {
     SgAdaTerminateStmt* result = NULL;

  // printf ("Copy SgAdaTerminateStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTerminateStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTerminateStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTerminateStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTerminateStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTerminateStmt(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTerminateStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTerminateStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 135331 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaUnscopedBlock::copy ( SgCopyHelp& help) const
   {
     SgAdaUnscopedBlock* result = NULL;

  // printf ("Copy SgAdaUnscopedBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaUnscopedBlock*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaUnscopedBlock*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaUnscopedBlock: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaUnscopedBlock*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaUnscopedBlock(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator source_statements_iterator = get_statements().begin(); 
     for ( /* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) 
        { 
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          statements_copy.push_back(copy_list_element); 
        } 
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
     SgStatementPtrList::const_iterator statements_iterator = result->get_statements().begin(); 
     for ( /* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) 
        { 
          SgStatement* list_element = *statements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaUnscopedBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaUnscopedBlock::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 135617 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaVariantWhenStmt::copy ( SgCopyHelp& help) const
   {
     SgAdaVariantWhenStmt* result = NULL;

  // printf ("Copy SgAdaVariantWhenStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaVariantWhenStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaVariantWhenStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaVariantWhenStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaVariantWhenStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: choices_copy
     SgExprListExp* choices_copy; 
  // case: not a listType for (using conditionalToCopyVariable)choices
          if (get_choices() != NULL) 
             { 
               choices_copy = static_cast<SgExprListExp*>(help.copyAst(get_choices())); 
             } 
            else 
             { 
               choices_copy = NULL; 
             } 
  // Copy constructor parameter data member: components_copy
     SgAdaUnscopedBlock* components_copy; 
  // case: not a listType for (using conditionalToCopyVariable)components
          if (get_components() != NULL) 
             { 
               components_copy = static_cast<SgAdaUnscopedBlock*>(help.copyAst(get_components())); 
             } 
            else 
             { 
               components_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaVariantWhenStmt(  startOfConstruct_copy, choices_copy, components_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)choices
          if ( (choices_copy != NULL) && (choices_copy->get_parent() == NULL) && (isSgType(choices_copy) == NULL) ) 
             { 
               choices_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)components
          if ( (components_copy != NULL) && (components_copy->get_parent() == NULL) && (isSgType(components_copy) == NULL) ) 
             { 
               components_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaVariantWhenStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaVariantWhenStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 135904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExpression::copy ( SgCopyHelp& help) const
   {
     SgExpression* result = NULL;

  // printf ("Copy SgExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgExpression(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 136180 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnaryOp::copy ( SgCopyHelp& help) const
   {
     SgUnaryOp* result = NULL;

  // printf ("Copy SgUnaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnaryOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnaryOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnaryOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnaryOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnaryOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnaryOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 136481 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExpressionRoot::copy ( SgCopyHelp& help) const
   {
     SgExpressionRoot* result = NULL;

  // printf ("Copy SgExpressionRoot = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgExpressionRoot*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgExpressionRoot*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgExpressionRoot: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgExpressionRoot*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgExpressionRoot(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgExpressionRoot = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgExpressionRoot::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 136782 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMinusOp::copy ( SgCopyHelp& help) const
   {
     SgMinusOp* result = NULL;

  // printf ("Copy SgMinusOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMinusOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMinusOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMinusOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMinusOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMinusOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMinusOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMinusOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 137083 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnaryAddOp::copy ( SgCopyHelp& help) const
   {
     SgUnaryAddOp* result = NULL;

  // printf ("Copy SgUnaryAddOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnaryAddOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnaryAddOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnaryAddOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnaryAddOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnaryAddOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnaryAddOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnaryAddOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 137384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNotOp::copy ( SgCopyHelp& help) const
   {
     SgNotOp* result = NULL;

  // printf ("Copy SgNotOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNotOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNotOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNotOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNotOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNotOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNotOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNotOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 137685 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPointerDerefExp::copy ( SgCopyHelp& help) const
   {
     SgPointerDerefExp* result = NULL;

  // printf ("Copy SgPointerDerefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPointerDerefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPointerDerefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPointerDerefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPointerDerefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPointerDerefExp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPointerDerefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPointerDerefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 137986 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAddressOfOp::copy ( SgCopyHelp& help) const
   {
     SgAddressOfOp* result = NULL;

  // printf ("Copy SgAddressOfOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAddressOfOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAddressOfOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAddressOfOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAddressOfOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAddressOfOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAddressOfOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAddressOfOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 138292 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMinusMinusOp::copy ( SgCopyHelp& help) const
   {
     SgMinusMinusOp* result = NULL;

  // printf ("Copy SgMinusMinusOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMinusMinusOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMinusMinusOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMinusMinusOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMinusMinusOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMinusMinusOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMinusMinusOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMinusMinusOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 138593 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPlusPlusOp::copy ( SgCopyHelp& help) const
   {
     SgPlusPlusOp* result = NULL;

  // printf ("Copy SgPlusPlusOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPlusPlusOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPlusPlusOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPlusPlusOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPlusPlusOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPlusPlusOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPlusPlusOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPlusPlusOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 138894 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitComplementOp::copy ( SgCopyHelp& help) const
   {
     SgBitComplementOp* result = NULL;

  // printf ("Copy SgBitComplementOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBitComplementOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBitComplementOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBitComplementOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBitComplementOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBitComplementOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBitComplementOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBitComplementOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 139195 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCastExp::copy ( SgCopyHelp& help) const
   {
     SgCastExp* result = NULL;

  // printf ("Copy SgCastExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCastExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCastExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCastExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCastExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
  // Copy constructor parameter data member: cast_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cast_type
  // case: toBeCopied == COPY_DATA for cast_type
     SgCastExp::cast_type_enum cast_type_copy = p_cast_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCastExp(  startOfConstruct_copy, operand_i_copy, expression_type_copy, cast_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): castContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for castContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for castContainsBaseTypeDefiningDeclaration
     bool castContainsBaseTypeDefiningDeclaration_copy = p_castContainsBaseTypeDefiningDeclaration; 
     result->p_castContainsBaseTypeDefiningDeclaration = castContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_for_pointer_to_member_class_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_for_pointer_to_member_class_length
  // case: toBeCopied == COPY_DATA for name_qualification_for_pointer_to_member_class_length
     int name_qualification_for_pointer_to_member_class_length_copy = p_name_qualification_for_pointer_to_member_class_length; 
     result->p_name_qualification_for_pointer_to_member_class_length = name_qualification_for_pointer_to_member_class_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for type_elaboration_for_pointer_to_member_class_required
     bool type_elaboration_for_pointer_to_member_class_required_copy = p_type_elaboration_for_pointer_to_member_class_required; 
     result->p_type_elaboration_for_pointer_to_member_class_required = type_elaboration_for_pointer_to_member_class_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for global_qualification_for_pointer_to_member_class_required
     bool global_qualification_for_pointer_to_member_class_required_copy = p_global_qualification_for_pointer_to_member_class_required; 
     result->p_global_qualification_for_pointer_to_member_class_required = global_qualification_for_pointer_to_member_class_required_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCastExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCastExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 139540 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgThrowOp::copy ( SgCopyHelp& help) const
   {
     SgThrowOp* result = NULL;

  // printf ("Copy SgThrowOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgThrowOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgThrowOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgThrowOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgThrowOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
  // Copy constructor parameter data member: throwKind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for throwKind
  // case: toBeCopied == COPY_DATA for throwKind
     SgThrowOp::e_throw_kind throwKind_copy = p_throwKind; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgThrowOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy, throwKind_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgThrowOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgThrowOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 139845 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRealPartOp::copy ( SgCopyHelp& help) const
   {
     SgRealPartOp* result = NULL;

  // printf ("Copy SgRealPartOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRealPartOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRealPartOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRealPartOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRealPartOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRealPartOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRealPartOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRealPartOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 140146 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImagPartOp::copy ( SgCopyHelp& help) const
   {
     SgImagPartOp* result = NULL;

  // printf ("Copy SgImagPartOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgImagPartOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgImagPartOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgImagPartOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgImagPartOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgImagPartOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgImagPartOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgImagPartOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 140447 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConjugateOp::copy ( SgCopyHelp& help) const
   {
     SgConjugateOp* result = NULL;

  // printf ("Copy SgConjugateOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgConjugateOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgConjugateOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgConjugateOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgConjugateOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgConjugateOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgConjugateOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgConjugateOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 140748 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUserDefinedUnaryOp::copy ( SgCopyHelp& help) const
   {
     SgUserDefinedUnaryOp* result = NULL;

  // printf ("Copy SgUserDefinedUnaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUserDefinedUnaryOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUserDefinedUnaryOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUserDefinedUnaryOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUserDefinedUnaryOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
  // Copy constructor parameter data member: operator_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operator_name
  // case: toBeCopied == COPY_DATA for operator_name
     SgName operator_name_copy = p_operator_name; 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgFunctionSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUserDefinedUnaryOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy, operator_name_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUserDefinedUnaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUserDefinedUnaryOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 141057 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMatrixTransposeOp::copy ( SgCopyHelp& help) const
   {
     SgMatrixTransposeOp* result = NULL;

  // printf ("Copy SgMatrixTransposeOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMatrixTransposeOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMatrixTransposeOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMatrixTransposeOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMatrixTransposeOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMatrixTransposeOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // Copy non-constructor parameter data member (access function): is_conjugate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_conjugate
  // case: toBeCopied == COPY_DATA for is_conjugate
     bool is_conjugate_copy = p_is_conjugate; 
     result->p_is_conjugate = is_conjugate_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMatrixTransposeOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMatrixTransposeOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 141363 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAbsOp::copy ( SgCopyHelp& help) const
   {
     SgAbsOp* result = NULL;

  // printf ("Copy SgAbsOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAbsOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAbsOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAbsOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAbsOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAbsOp(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAbsOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAbsOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 141664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBinaryOp::copy ( SgCopyHelp& help) const
   {
     SgBinaryOp* result = NULL;

  // printf ("Copy SgBinaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBinaryOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBinaryOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBinaryOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBinaryOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBinaryOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBinaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBinaryOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 141981 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgArrowExp::copy ( SgCopyHelp& help) const
   {
     SgArrowExp* result = NULL;

  // printf ("Copy SgArrowExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgArrowExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgArrowExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgArrowExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgArrowExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgArrowExp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgArrowExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgArrowExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 142298 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDotExp::copy ( SgCopyHelp& help) const
   {
     SgDotExp* result = NULL;

  // printf ("Copy SgDotExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDotExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDotExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDotExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDotExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDotExp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDotExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDotExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 142615 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDotStarOp::copy ( SgCopyHelp& help) const
   {
     SgDotStarOp* result = NULL;

  // printf ("Copy SgDotStarOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDotStarOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDotStarOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDotStarOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDotStarOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDotStarOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDotStarOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDotStarOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 142932 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgArrowStarOp::copy ( SgCopyHelp& help) const
   {
     SgArrowStarOp* result = NULL;

  // printf ("Copy SgArrowStarOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgArrowStarOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgArrowStarOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgArrowStarOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgArrowStarOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgArrowStarOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgArrowStarOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgArrowStarOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 143249 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEqualityOp::copy ( SgCopyHelp& help) const
   {
     SgEqualityOp* result = NULL;

  // printf ("Copy SgEqualityOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEqualityOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEqualityOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEqualityOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEqualityOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEqualityOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEqualityOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEqualityOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 143566 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLessThanOp::copy ( SgCopyHelp& help) const
   {
     SgLessThanOp* result = NULL;

  // printf ("Copy SgLessThanOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLessThanOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLessThanOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLessThanOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLessThanOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLessThanOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLessThanOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLessThanOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 143883 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGreaterThanOp::copy ( SgCopyHelp& help) const
   {
     SgGreaterThanOp* result = NULL;

  // printf ("Copy SgGreaterThanOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGreaterThanOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGreaterThanOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGreaterThanOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGreaterThanOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGreaterThanOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGreaterThanOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGreaterThanOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 144200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNotEqualOp::copy ( SgCopyHelp& help) const
   {
     SgNotEqualOp* result = NULL;

  // printf ("Copy SgNotEqualOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNotEqualOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNotEqualOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNotEqualOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNotEqualOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNotEqualOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNotEqualOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNotEqualOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 144517 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLessOrEqualOp::copy ( SgCopyHelp& help) const
   {
     SgLessOrEqualOp* result = NULL;

  // printf ("Copy SgLessOrEqualOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLessOrEqualOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLessOrEqualOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLessOrEqualOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLessOrEqualOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLessOrEqualOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLessOrEqualOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLessOrEqualOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 144834 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGreaterOrEqualOp::copy ( SgCopyHelp& help) const
   {
     SgGreaterOrEqualOp* result = NULL;

  // printf ("Copy SgGreaterOrEqualOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgGreaterOrEqualOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgGreaterOrEqualOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgGreaterOrEqualOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgGreaterOrEqualOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgGreaterOrEqualOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgGreaterOrEqualOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgGreaterOrEqualOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 145151 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAddOp::copy ( SgCopyHelp& help) const
   {
     SgAddOp* result = NULL;

  // printf ("Copy SgAddOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAddOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAddOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAddOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAddOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAddOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAddOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAddOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 145468 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSubtractOp::copy ( SgCopyHelp& help) const
   {
     SgSubtractOp* result = NULL;

  // printf ("Copy SgSubtractOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSubtractOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSubtractOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSubtractOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSubtractOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSubtractOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSubtractOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSubtractOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 145785 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMultiplyOp::copy ( SgCopyHelp& help) const
   {
     SgMultiplyOp* result = NULL;

  // printf ("Copy SgMultiplyOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMultiplyOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMultiplyOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMultiplyOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMultiplyOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMultiplyOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMultiplyOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMultiplyOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 146102 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDivideOp::copy ( SgCopyHelp& help) const
   {
     SgDivideOp* result = NULL;

  // printf ("Copy SgDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDivideOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDivideOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDivideOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDivideOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDivideOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDivideOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 146419 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntegerDivideOp::copy ( SgCopyHelp& help) const
   {
     SgIntegerDivideOp* result = NULL;

  // printf ("Copy SgIntegerDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIntegerDivideOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIntegerDivideOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIntegerDivideOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIntegerDivideOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIntegerDivideOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIntegerDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIntegerDivideOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 146736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModOp::copy ( SgCopyHelp& help) const
   {
     SgModOp* result = NULL;

  // printf ("Copy SgModOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgModOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgModOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgModOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgModOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgModOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgModOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgModOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 147053 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAndOp::copy ( SgCopyHelp& help) const
   {
     SgAndOp* result = NULL;

  // printf ("Copy SgAndOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAndOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAndOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAndOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAndOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAndOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAndOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAndOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 147370 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOrOp::copy ( SgCopyHelp& help) const
   {
     SgOrOp* result = NULL;

  // printf ("Copy SgOrOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgOrOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgOrOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgOrOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgOrOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgOrOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgOrOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgOrOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 147687 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitXorOp::copy ( SgCopyHelp& help) const
   {
     SgBitXorOp* result = NULL;

  // printf ("Copy SgBitXorOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBitXorOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBitXorOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBitXorOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBitXorOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBitXorOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBitXorOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBitXorOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 148004 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitAndOp::copy ( SgCopyHelp& help) const
   {
     SgBitAndOp* result = NULL;

  // printf ("Copy SgBitAndOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBitAndOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBitAndOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBitAndOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBitAndOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBitAndOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBitAndOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBitAndOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 148321 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitOrOp::copy ( SgCopyHelp& help) const
   {
     SgBitOrOp* result = NULL;

  // printf ("Copy SgBitOrOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBitOrOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBitOrOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBitOrOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBitOrOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBitOrOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBitOrOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBitOrOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 148638 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitEqvOp::copy ( SgCopyHelp& help) const
   {
     SgBitEqvOp* result = NULL;

  // printf ("Copy SgBitEqvOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBitEqvOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBitEqvOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBitEqvOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBitEqvOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBitEqvOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBitEqvOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBitEqvOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 148955 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCommaOpExp::copy ( SgCopyHelp& help) const
   {
     SgCommaOpExp* result = NULL;

  // printf ("Copy SgCommaOpExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCommaOpExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCommaOpExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCommaOpExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCommaOpExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCommaOpExp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCommaOpExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCommaOpExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 149272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLshiftOp::copy ( SgCopyHelp& help) const
   {
     SgLshiftOp* result = NULL;

  // printf ("Copy SgLshiftOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLshiftOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLshiftOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLshiftOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLshiftOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLshiftOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLshiftOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLshiftOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 149589 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRshiftOp::copy ( SgCopyHelp& help) const
   {
     SgRshiftOp* result = NULL;

  // printf ("Copy SgRshiftOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRshiftOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRshiftOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRshiftOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRshiftOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRshiftOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRshiftOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRshiftOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 149906 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPntrArrRefExp::copy ( SgCopyHelp& help) const
   {
     SgPntrArrRefExp* result = NULL;

  // printf ("Copy SgPntrArrRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPntrArrRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPntrArrRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPntrArrRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPntrArrRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPntrArrRefExp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPntrArrRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPntrArrRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 150223 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgScopeOp::copy ( SgCopyHelp& help) const
   {
     SgScopeOp* result = NULL;

  // printf ("Copy SgScopeOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgScopeOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgScopeOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgScopeOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgScopeOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgScopeOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgScopeOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgScopeOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 150540 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssignOp::copy ( SgCopyHelp& help) const
   {
     SgAssignOp* result = NULL;

  // printf ("Copy SgAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 150857 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExponentiationOp::copy ( SgCopyHelp& help) const
   {
     SgExponentiationOp* result = NULL;

  // printf ("Copy SgExponentiationOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgExponentiationOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgExponentiationOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgExponentiationOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgExponentiationOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgExponentiationOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgExponentiationOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgExponentiationOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 151174 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaUnsignedRshiftOp::copy ( SgCopyHelp& help) const
   {
     SgJavaUnsignedRshiftOp* result = NULL;

  // printf ("Copy SgJavaUnsignedRshiftOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaUnsignedRshiftOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaUnsignedRshiftOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaUnsignedRshiftOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaUnsignedRshiftOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaUnsignedRshiftOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaUnsignedRshiftOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaUnsignedRshiftOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 151491 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConcatenationOp::copy ( SgCopyHelp& help) const
   {
     SgConcatenationOp* result = NULL;

  // printf ("Copy SgConcatenationOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgConcatenationOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgConcatenationOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgConcatenationOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgConcatenationOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgConcatenationOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgConcatenationOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgConcatenationOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 151808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPointerAssignOp::copy ( SgCopyHelp& help) const
   {
     SgPointerAssignOp* result = NULL;

  // printf ("Copy SgPointerAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPointerAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPointerAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPointerAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPointerAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPointerAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPointerAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPointerAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 152125 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUserDefinedBinaryOp::copy ( SgCopyHelp& help) const
   {
     SgUserDefinedBinaryOp* result = NULL;

  // printf ("Copy SgUserDefinedBinaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUserDefinedBinaryOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUserDefinedBinaryOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUserDefinedBinaryOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUserDefinedBinaryOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
  // Copy constructor parameter data member: operator_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operator_name
  // case: toBeCopied == COPY_DATA for operator_name
     SgName operator_name_copy = p_operator_name; 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgFunctionSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUserDefinedBinaryOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy, operator_name_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUserDefinedBinaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUserDefinedBinaryOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 152450 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCompoundAssignOp::copy ( SgCopyHelp& help) const
   {
     SgCompoundAssignOp* result = NULL;

  // printf ("Copy SgCompoundAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCompoundAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCompoundAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCompoundAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCompoundAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCompoundAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCompoundAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCompoundAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 152767 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPlusAssignOp::copy ( SgCopyHelp& help) const
   {
     SgPlusAssignOp* result = NULL;

  // printf ("Copy SgPlusAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPlusAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPlusAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPlusAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPlusAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPlusAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPlusAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPlusAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 153084 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMinusAssignOp::copy ( SgCopyHelp& help) const
   {
     SgMinusAssignOp* result = NULL;

  // printf ("Copy SgMinusAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMinusAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMinusAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMinusAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMinusAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMinusAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMinusAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMinusAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 153401 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAndAssignOp::copy ( SgCopyHelp& help) const
   {
     SgAndAssignOp* result = NULL;

  // printf ("Copy SgAndAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAndAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAndAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAndAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAndAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAndAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAndAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAndAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 153718 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIorAssignOp::copy ( SgCopyHelp& help) const
   {
     SgIorAssignOp* result = NULL;

  // printf ("Copy SgIorAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIorAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIorAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIorAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIorAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIorAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIorAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIorAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 154035 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMultAssignOp::copy ( SgCopyHelp& help) const
   {
     SgMultAssignOp* result = NULL;

  // printf ("Copy SgMultAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMultAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMultAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMultAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMultAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMultAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMultAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMultAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 154352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDivAssignOp::copy ( SgCopyHelp& help) const
   {
     SgDivAssignOp* result = NULL;

  // printf ("Copy SgDivAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDivAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDivAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDivAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDivAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDivAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDivAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDivAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 154669 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModAssignOp::copy ( SgCopyHelp& help) const
   {
     SgModAssignOp* result = NULL;

  // printf ("Copy SgModAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgModAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgModAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgModAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgModAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgModAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgModAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgModAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 154986 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgXorAssignOp::copy ( SgCopyHelp& help) const
   {
     SgXorAssignOp* result = NULL;

  // printf ("Copy SgXorAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgXorAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgXorAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgXorAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgXorAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgXorAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgXorAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgXorAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 155303 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLshiftAssignOp::copy ( SgCopyHelp& help) const
   {
     SgLshiftAssignOp* result = NULL;

  // printf ("Copy SgLshiftAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLshiftAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLshiftAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLshiftAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLshiftAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLshiftAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLshiftAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLshiftAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 155620 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRshiftAssignOp::copy ( SgCopyHelp& help) const
   {
     SgRshiftAssignOp* result = NULL;

  // printf ("Copy SgRshiftAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRshiftAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRshiftAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRshiftAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRshiftAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRshiftAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRshiftAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRshiftAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 155937 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaUnsignedRshiftAssignOp::copy ( SgCopyHelp& help) const
   {
     SgJavaUnsignedRshiftAssignOp* result = NULL;

  // printf ("Copy SgJavaUnsignedRshiftAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaUnsignedRshiftAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaUnsignedRshiftAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaUnsignedRshiftAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaUnsignedRshiftAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaUnsignedRshiftAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaUnsignedRshiftAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaUnsignedRshiftAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 156254 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntegerDivideAssignOp::copy ( SgCopyHelp& help) const
   {
     SgIntegerDivideAssignOp* result = NULL;

  // printf ("Copy SgIntegerDivideAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIntegerDivideAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIntegerDivideAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIntegerDivideAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIntegerDivideAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIntegerDivideAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIntegerDivideAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIntegerDivideAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 156571 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExponentiationAssignOp::copy ( SgCopyHelp& help) const
   {
     SgExponentiationAssignOp* result = NULL;

  // printf ("Copy SgExponentiationAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgExponentiationAssignOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgExponentiationAssignOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgExponentiationAssignOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgExponentiationAssignOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgExponentiationAssignOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgExponentiationAssignOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgExponentiationAssignOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 156888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMembershipOp::copy ( SgCopyHelp& help) const
   {
     SgMembershipOp* result = NULL;

  // printf ("Copy SgMembershipOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMembershipOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMembershipOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMembershipOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMembershipOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMembershipOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMembershipOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMembershipOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 157205 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSpaceshipOp::copy ( SgCopyHelp& help) const
   {
     SgSpaceshipOp* result = NULL;

  // printf ("Copy SgSpaceshipOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSpaceshipOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSpaceshipOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSpaceshipOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSpaceshipOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSpaceshipOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSpaceshipOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSpaceshipOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 157522 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonMembershipOp::copy ( SgCopyHelp& help) const
   {
     SgNonMembershipOp* result = NULL;

  // printf ("Copy SgNonMembershipOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNonMembershipOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNonMembershipOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNonMembershipOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNonMembershipOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNonMembershipOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNonMembershipOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNonMembershipOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 157839 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIsOp::copy ( SgCopyHelp& help) const
   {
     SgIsOp* result = NULL;

  // printf ("Copy SgIsOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIsOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIsOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIsOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIsOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIsOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIsOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIsOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 158156 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIsNotOp::copy ( SgCopyHelp& help) const
   {
     SgIsNotOp* result = NULL;

  // printf ("Copy SgIsNotOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIsNotOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIsNotOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIsNotOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIsNotOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIsNotOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIsNotOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIsNotOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 158473 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDotDotExp::copy ( SgCopyHelp& help) const
   {
     SgDotDotExp* result = NULL;

  // printf ("Copy SgDotDotExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDotDotExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDotDotExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDotDotExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDotDotExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDotDotExp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDotDotExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDotDotExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 158790 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseOp::copy ( SgCopyHelp& help) const
   {
     SgElementwiseOp* result = NULL;

  // printf ("Copy SgElementwiseOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElementwiseOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElementwiseOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElementwiseOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElementwiseOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElementwiseOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElementwiseOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElementwiseOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 159107 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseMultiplyOp::copy ( SgCopyHelp& help) const
   {
     SgElementwiseMultiplyOp* result = NULL;

  // printf ("Copy SgElementwiseMultiplyOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElementwiseMultiplyOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElementwiseMultiplyOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElementwiseMultiplyOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElementwiseMultiplyOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElementwiseMultiplyOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElementwiseMultiplyOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElementwiseMultiplyOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 159424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwisePowerOp::copy ( SgCopyHelp& help) const
   {
     SgElementwisePowerOp* result = NULL;

  // printf ("Copy SgElementwisePowerOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElementwisePowerOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElementwisePowerOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElementwisePowerOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElementwisePowerOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElementwisePowerOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElementwisePowerOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElementwisePowerOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 159741 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseLeftDivideOp::copy ( SgCopyHelp& help) const
   {
     SgElementwiseLeftDivideOp* result = NULL;

  // printf ("Copy SgElementwiseLeftDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElementwiseLeftDivideOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElementwiseLeftDivideOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElementwiseLeftDivideOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElementwiseLeftDivideOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElementwiseLeftDivideOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElementwiseLeftDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElementwiseLeftDivideOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 160058 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseDivideOp::copy ( SgCopyHelp& help) const
   {
     SgElementwiseDivideOp* result = NULL;

  // printf ("Copy SgElementwiseDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElementwiseDivideOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElementwiseDivideOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElementwiseDivideOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElementwiseDivideOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElementwiseDivideOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElementwiseDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElementwiseDivideOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 160375 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseAddOp::copy ( SgCopyHelp& help) const
   {
     SgElementwiseAddOp* result = NULL;

  // printf ("Copy SgElementwiseAddOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElementwiseAddOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElementwiseAddOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElementwiseAddOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElementwiseAddOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElementwiseAddOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElementwiseAddOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElementwiseAddOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 160692 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseSubtractOp::copy ( SgCopyHelp& help) const
   {
     SgElementwiseSubtractOp* result = NULL;

  // printf ("Copy SgElementwiseSubtractOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgElementwiseSubtractOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgElementwiseSubtractOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgElementwiseSubtractOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgElementwiseSubtractOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgElementwiseSubtractOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgElementwiseSubtractOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgElementwiseSubtractOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 161009 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPowerOp::copy ( SgCopyHelp& help) const
   {
     SgPowerOp* result = NULL;

  // printf ("Copy SgPowerOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPowerOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPowerOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPowerOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPowerOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPowerOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPowerOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPowerOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 161326 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLeftDivideOp::copy ( SgCopyHelp& help) const
   {
     SgLeftDivideOp* result = NULL;

  // printf ("Copy SgLeftDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLeftDivideOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLeftDivideOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLeftDivideOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLeftDivideOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLeftDivideOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLeftDivideOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLeftDivideOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 161643 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRemOp::copy ( SgCopyHelp& help) const
   {
     SgRemOp* result = NULL;

  // printf ("Copy SgRemOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRemOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRemOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRemOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRemOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRemOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRemOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRemOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 161960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReplicationOp::copy ( SgCopyHelp& help) const
   {
     SgReplicationOp* result = NULL;

  // printf ("Copy SgReplicationOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgReplicationOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgReplicationOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgReplicationOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgReplicationOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgReplicationOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgReplicationOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgReplicationOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 162277 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAtOp::copy ( SgCopyHelp& help) const
   {
     SgAtOp* result = NULL;

  // printf ("Copy SgAtOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAtOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAtOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAtOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAtOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != NULL) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != NULL) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAtOp(  startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
          if ( (lhs_operand_i_copy != NULL) && (lhs_operand_i_copy->get_parent() == NULL) && (isSgType(lhs_operand_i_copy) == NULL) ) 
             { 
               lhs_operand_i_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
          if ( (rhs_operand_i_copy != NULL) && (rhs_operand_i_copy->get_parent() == NULL) && (isSgType(rhs_operand_i_copy) == NULL) ) 
             { 
               rhs_operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAtOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAtOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 162594 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExprListExp::copy ( SgCopyHelp& help) const
   {
     SgExprListExp* result = NULL;

  // printf ("Copy SgExprListExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgExprListExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgExprListExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgExprListExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgExprListExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgExprListExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgExpressionPtrList::const_iterator source_expressions_iterator = get_expressions().begin(); 
     for ( /* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) 
        { 
          SgExpression* source_list_element = *source_expressions_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          expressions_copy.push_back(copy_list_element); 
        } 
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgExpressionPtrList::const_iterator expressions_iterator = result->get_expressions().begin(); 
     for ( /* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) 
        { 
          SgExpression* list_element = *expressions_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgExprListExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgExprListExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 162901 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgListExp::copy ( SgCopyHelp& help) const
   {
     SgListExp* result = NULL;

  // printf ("Copy SgListExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgListExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgListExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgListExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgListExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgListExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgExpressionPtrList::const_iterator source_expressions_iterator = get_expressions().begin(); 
     for ( /* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) 
        { 
          SgExpression* source_list_element = *source_expressions_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          expressions_copy.push_back(copy_list_element); 
        } 
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgExpressionPtrList::const_iterator expressions_iterator = result->get_expressions().begin(); 
     for ( /* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) 
        { 
          SgExpression* list_element = *expressions_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgListExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgListExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 163208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTupleExp::copy ( SgCopyHelp& help) const
   {
     SgTupleExp* result = NULL;

  // printf ("Copy SgTupleExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTupleExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTupleExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTupleExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTupleExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTupleExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgExpressionPtrList::const_iterator source_expressions_iterator = get_expressions().begin(); 
     for ( /* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) 
        { 
          SgExpression* source_list_element = *source_expressions_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          expressions_copy.push_back(copy_list_element); 
        } 
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgExpressionPtrList::const_iterator expressions_iterator = result->get_expressions().begin(); 
     for ( /* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) 
        { 
          SgExpression* list_element = *expressions_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTupleExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTupleExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 163515 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMatrixExp::copy ( SgCopyHelp& help) const
   {
     SgMatrixExp* result = NULL;

  // printf ("Copy SgMatrixExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMatrixExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMatrixExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMatrixExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMatrixExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMatrixExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgExpressionPtrList::const_iterator source_expressions_iterator = get_expressions().begin(); 
     for ( /* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) 
        { 
          SgExpression* source_list_element = *source_expressions_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          expressions_copy.push_back(copy_list_element); 
        } 
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgExpressionPtrList::const_iterator expressions_iterator = result->get_expressions().begin(); 
     for ( /* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) 
        { 
          SgExpression* list_element = *expressions_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMatrixExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMatrixExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 163822 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgValueExp::copy ( SgCopyHelp& help) const
   {
     SgValueExp* result = NULL;

  // printf ("Copy SgValueExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgValueExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgValueExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgValueExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgValueExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgValueExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgValueExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgValueExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 164103 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBoolValExp::copy ( SgCopyHelp& help) const
   {
     SgBoolValExp* result = NULL;

  // printf ("Copy SgBoolValExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBoolValExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBoolValExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBoolValExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBoolValExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     int value_copy = p_value; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBoolValExp(  startOfConstruct_copy, value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBoolValExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBoolValExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 164388 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStringVal::copy ( SgCopyHelp& help) const
   {
     SgStringVal* result = NULL;

  // printf ("Copy SgStringVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStringVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStringVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStringVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStringVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     std::string value_copy = p_value; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStringVal(  startOfConstruct_copy, value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (access function): wcharString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wcharString
  // case: toBeCopied == COPY_DATA for wcharString
     bool wcharString_copy = p_wcharString; 
     result->p_wcharString = wcharString_copy; 
  // Copy non-constructor parameter data member (access function): stringDelimiter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stringDelimiter
  // case: toBeCopied == COPY_DATA for stringDelimiter
     char stringDelimiter_copy = p_stringDelimiter; 
     result->p_stringDelimiter = stringDelimiter_copy; 
  // Copy non-constructor parameter data member (access function): is16bitString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is16bitString
  // case: toBeCopied == COPY_DATA for is16bitString
     bool is16bitString_copy = p_is16bitString; 
     result->p_is16bitString = is16bitString_copy; 
  // Copy non-constructor parameter data member (access function): is32bitString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is32bitString
  // case: toBeCopied == COPY_DATA for is32bitString
     bool is32bitString_copy = p_is32bitString; 
     result->p_is32bitString = is32bitString_copy; 
  // Copy non-constructor parameter data member (access function): isRawString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRawString
  // case: toBeCopied == COPY_DATA for isRawString
     bool isRawString_copy = p_isRawString; 
     result->p_isRawString = isRawString_copy; 
  // Copy non-constructor parameter data member (access function): raw_string_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for raw_string_value
  // case: toBeCopied == COPY_DATA for raw_string_value
     std::string raw_string_value_copy = p_raw_string_value; 
     result->p_raw_string_value = raw_string_value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStringVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStringVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 164703 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgShortVal::copy ( SgCopyHelp& help) const
   {
     SgShortVal* result = NULL;

  // printf ("Copy SgShortVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgShortVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgShortVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgShortVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgShortVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     short value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgShortVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgShortVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgShortVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 164992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCharVal::copy ( SgCopyHelp& help) const
   {
     SgCharVal* result = NULL;

  // printf ("Copy SgCharVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCharVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCharVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCharVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCharVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     char value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCharVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCharVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCharVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 165281 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedCharVal::copy ( SgCopyHelp& help) const
   {
     SgUnsignedCharVal* result = NULL;

  // printf ("Copy SgUnsignedCharVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnsignedCharVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnsignedCharVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnsignedCharVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnsignedCharVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned char value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnsignedCharVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnsignedCharVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnsignedCharVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 165570 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWcharVal::copy ( SgCopyHelp& help) const
   {
     SgWcharVal* result = NULL;

  // printf ("Copy SgWcharVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgWcharVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgWcharVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgWcharVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgWcharVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: valueUL_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueUL
  // case: toBeCopied == COPY_DATA for valueUL
     unsigned long valueUL_copy = p_valueUL; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgWcharVal(  startOfConstruct_copy, valueUL_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgWcharVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgWcharVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 165859 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedShortVal::copy ( SgCopyHelp& help) const
   {
     SgUnsignedShortVal* result = NULL;

  // printf ("Copy SgUnsignedShortVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnsignedShortVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnsignedShortVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnsignedShortVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnsignedShortVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned short value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnsignedShortVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnsignedShortVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnsignedShortVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 166148 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntVal::copy ( SgCopyHelp& help) const
   {
     SgIntVal* result = NULL;

  // printf ("Copy SgIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIntVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIntVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIntVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIntVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     int value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIntVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIntVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 166437 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumVal::copy ( SgCopyHelp& help) const
   {
     SgEnumVal* result = NULL;

  // printf ("Copy SgEnumVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEnumVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEnumVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEnumVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEnumVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long long int value_copy = p_value; 
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgEnumDeclaration* declaration_copy = p_declaration; 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEnumVal(  startOfConstruct_copy, value_copy, declaration_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (access function): requiresNameQualification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualification
  // case: toBeCopied == COPY_DATA for requiresNameQualification
     bool requiresNameQualification_copy = p_requiresNameQualification; 
     result->p_requiresNameQualification = requiresNameQualification_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEnumVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEnumVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 166750 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedIntVal::copy ( SgCopyHelp& help) const
   {
     SgUnsignedIntVal* result = NULL;

  // printf ("Copy SgUnsignedIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnsignedIntVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnsignedIntVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnsignedIntVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnsignedIntVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned int value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnsignedIntVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnsignedIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnsignedIntVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 167039 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLongIntVal::copy ( SgCopyHelp& help) const
   {
     SgLongIntVal* result = NULL;

  // printf ("Copy SgLongIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLongIntVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLongIntVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLongIntVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLongIntVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long int value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLongIntVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLongIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLongIntVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 167328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLongLongIntVal::copy ( SgCopyHelp& help) const
   {
     SgLongLongIntVal* result = NULL;

  // printf ("Copy SgLongLongIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLongLongIntVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLongLongIntVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLongLongIntVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLongLongIntVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long long int value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLongLongIntVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLongLongIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLongLongIntVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 167617 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedLongLongIntVal::copy ( SgCopyHelp& help) const
   {
     SgUnsignedLongLongIntVal* result = NULL;

  // printf ("Copy SgUnsignedLongLongIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnsignedLongLongIntVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnsignedLongLongIntVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnsignedLongLongIntVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnsignedLongLongIntVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned long long int value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnsignedLongLongIntVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnsignedLongLongIntVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnsignedLongLongIntVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 167906 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedLongVal::copy ( SgCopyHelp& help) const
   {
     SgUnsignedLongVal* result = NULL;

  // printf ("Copy SgUnsignedLongVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnsignedLongVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnsignedLongVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnsignedLongVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnsignedLongVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned long value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnsignedLongVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnsignedLongVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnsignedLongVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 168195 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloatVal::copy ( SgCopyHelp& help) const
   {
     SgFloatVal* result = NULL;

  // printf ("Copy SgFloatVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFloatVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFloatVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFloatVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFloatVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     float value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFloatVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFloatVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFloatVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 168484 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDoubleVal::copy ( SgCopyHelp& help) const
   {
     SgDoubleVal* result = NULL;

  // printf ("Copy SgDoubleVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDoubleVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDoubleVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDoubleVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDoubleVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     double value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDoubleVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDoubleVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDoubleVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 168773 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLongDoubleVal::copy ( SgCopyHelp& help) const
   {
     SgLongDoubleVal* result = NULL;

  // printf ("Copy SgLongDoubleVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLongDoubleVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLongDoubleVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLongDoubleVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLongDoubleVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long double value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLongDoubleVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLongDoubleVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLongDoubleVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 169062 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgComplexVal::copy ( SgCopyHelp& help) const
   {
     SgComplexVal* result = NULL;

  // printf ("Copy SgComplexVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgComplexVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgComplexVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgComplexVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgComplexVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: real_value_copy
     SgValueExp* real_value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)real_value
          if (get_real_value() != NULL) 
             { 
               real_value_copy = static_cast<SgValueExp*>(help.copyAst(get_real_value())); 
             } 
            else 
             { 
               real_value_copy = NULL; 
             } 
  // Copy constructor parameter data member: imaginary_value_copy
     SgValueExp* imaginary_value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)imaginary_value
          if (get_imaginary_value() != NULL) 
             { 
               imaginary_value_copy = static_cast<SgValueExp*>(help.copyAst(get_imaginary_value())); 
             } 
            else 
             { 
               imaginary_value_copy = NULL; 
             } 
  // Copy constructor parameter data member: precisionType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for precisionType
  // case: toBeCopied == COPY_DATA for precisionType
     SgType* precisionType_copy = p_precisionType; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgComplexVal(  startOfConstruct_copy, real_value_copy, imaginary_value_copy, precisionType_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)real_value
          if ( (real_value_copy != NULL) && (real_value_copy->get_parent() == NULL) && (isSgType(real_value_copy) == NULL) ) 
             { 
               real_value_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)imaginary_value
          if ( (imaginary_value_copy != NULL) && (imaginary_value_copy->get_parent() == NULL) && (isSgType(imaginary_value_copy) == NULL) ) 
             { 
               imaginary_value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgComplexVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgComplexVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 169383 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcThreads::copy ( SgCopyHelp& help) const
   {
     SgUpcThreads* result = NULL;

  // printf ("Copy SgUpcThreads = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcThreads*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcThreads*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcThreads: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcThreads*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     int value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcThreads(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcThreads = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcThreads::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 169672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcMythread::copy ( SgCopyHelp& help) const
   {
     SgUpcMythread* result = NULL;

  // printf ("Copy SgUpcMythread = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcMythread*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcMythread*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcMythread: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcMythread*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     int value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcMythread(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcMythread = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcMythread::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 169961 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateParameterVal::copy ( SgCopyHelp& help) const
   {
     SgTemplateParameterVal* result = NULL;

  // printf ("Copy SgTemplateParameterVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateParameterVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateParameterVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateParameterVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateParameterVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: template_parameter_position_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_position
  // case: toBeCopied == COPY_DATA for template_parameter_position
     int template_parameter_position_copy = p_template_parameter_position; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateParameterVal(  startOfConstruct_copy, template_parameter_position_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (access function): valueType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueType
  // case: toBeCopied == COPY_DATA for valueType
     SgType* valueType_copy = p_valueType; 
     result->p_valueType = valueType_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateParameterVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateParameterVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 170255 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNullptrValExp::copy ( SgCopyHelp& help) const
   {
     SgNullptrValExp* result = NULL;

  // printf ("Copy SgNullptrValExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNullptrValExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNullptrValExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNullptrValExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNullptrValExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNullptrValExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNullptrValExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNullptrValExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 170536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgChar16Val::copy ( SgCopyHelp& help) const
   {
     SgChar16Val* result = NULL;

  // printf ("Copy SgChar16Val = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgChar16Val*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgChar16Val*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgChar16Val: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgChar16Val*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: valueUL_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueUL
  // case: toBeCopied == COPY_DATA for valueUL
     unsigned short valueUL_copy = p_valueUL; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgChar16Val(  startOfConstruct_copy, valueUL_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgChar16Val = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgChar16Val::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 170825 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgChar32Val::copy ( SgCopyHelp& help) const
   {
     SgChar32Val* result = NULL;

  // printf ("Copy SgChar32Val = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgChar32Val*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgChar32Val*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgChar32Val: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgChar32Val*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: valueUL_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueUL
  // case: toBeCopied == COPY_DATA for valueUL
     unsigned int valueUL_copy = p_valueUL; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgChar32Val(  startOfConstruct_copy, valueUL_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgChar32Val = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgChar32Val::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 171114 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloat80Val::copy ( SgCopyHelp& help) const
   {
     SgFloat80Val* result = NULL;

  // printf ("Copy SgFloat80Val = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFloat80Val*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFloat80Val*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFloat80Val: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFloat80Val*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long double value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFloat80Val(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFloat80Val = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFloat80Val::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 171403 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloat128Val::copy ( SgCopyHelp& help) const
   {
     SgFloat128Val* result = NULL;

  // printf ("Copy SgFloat128Val = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFloat128Val*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFloat128Val*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFloat128Val: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFloat128Val*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long double value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFloat128Val(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFloat128Val = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFloat128Val::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 171692 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVoidVal::copy ( SgCopyHelp& help) const
   {
     SgVoidVal* result = NULL;

  // printf ("Copy SgVoidVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVoidVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVoidVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVoidVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVoidVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVoidVal(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVoidVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVoidVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 171973 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFloatVal::copy ( SgCopyHelp& help) const
   {
     SgAdaFloatVal* result = NULL;

  // printf ("Copy SgAdaFloatVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaFloatVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaFloatVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaFloatVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaFloatVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaFloatVal(  startOfConstruct_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaFloatVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaFloatVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 172258 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSignedCharVal::copy ( SgCopyHelp& help) const
   {
     SgSignedCharVal* result = NULL;

  // printf ("Copy SgSignedCharVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSignedCharVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSignedCharVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSignedCharVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSignedCharVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     signed char value_copy = p_value; 
  // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSignedCharVal(  startOfConstruct_copy, value_copy, valueString_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSignedCharVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSignedCharVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 172547 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialBitVal::copy ( SgCopyHelp& help) const
   {
     SgJovialBitVal* result = NULL;

  // printf ("Copy SgJovialBitVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialBitVal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialBitVal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialBitVal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialBitVal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialBitVal(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != NULL) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_valueString
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
     result->p_valueString = valueString_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialBitVal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialBitVal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 172836 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCallExpression::copy ( SgCopyHelp& help) const
   {
     SgCallExpression* result = NULL;

  // printf ("Copy SgCallExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCallExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCallExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCallExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCallExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: function_copy
     SgExpression* function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)function
          if (get_function() != NULL) 
             { 
               function_copy = static_cast<SgExpression*>(help.copyAst(get_function())); 
             } 
            else 
             { 
               function_copy = NULL; 
             } 
  // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != NULL) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCallExpression(  startOfConstruct_copy, function_copy, args_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)function
          if ( (function_copy != NULL) && (function_copy->get_parent() == NULL) && (isSgType(function_copy) == NULL) ) 
             { 
               function_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)args
          if ( (args_copy != NULL) && (args_copy->get_parent() == NULL) && (isSgType(args_copy) == NULL) ) 
             { 
               args_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCallExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCallExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 173148 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionCallExp::copy ( SgCopyHelp& help) const
   {
     SgFunctionCallExp* result = NULL;

  // printf ("Copy SgFunctionCallExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionCallExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionCallExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionCallExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionCallExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: function_copy
     SgExpression* function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)function
          if (get_function() != NULL) 
             { 
               function_copy = static_cast<SgExpression*>(help.copyAst(get_function())); 
             } 
            else 
             { 
               function_copy = NULL; 
             } 
  // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != NULL) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionCallExp(  startOfConstruct_copy, function_copy, args_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): uses_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for uses_operator_syntax
  // case: toBeCopied == COPY_DATA for uses_operator_syntax
     bool uses_operator_syntax_copy = p_uses_operator_syntax; 
     result->p_uses_operator_syntax = uses_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): usesObjectCallSyntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usesObjectCallSyntax
  // case: toBeCopied == COPY_DATA for usesObjectCallSyntax
     bool usesObjectCallSyntax_copy = p_usesObjectCallSyntax; 
     result->p_usesObjectCallSyntax = usesObjectCallSyntax_copy; 
  // case: not a listType for (using conditionalToSetParent)function
          if ( (function_copy != NULL) && (function_copy->get_parent() == NULL) && (isSgType(function_copy) == NULL) ) 
             { 
               function_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)args
          if ( (args_copy != NULL) && (args_copy->get_parent() == NULL) && (isSgType(args_copy) == NULL) ) 
             { 
               args_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionCallExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionCallExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 173470 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCudaKernelCallExp::copy ( SgCopyHelp& help) const
   {
     SgCudaKernelCallExp* result = NULL;

  // printf ("Copy SgCudaKernelCallExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCudaKernelCallExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCudaKernelCallExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCudaKernelCallExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCudaKernelCallExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: function_copy
     SgExpression* function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)function
          if (get_function() != NULL) 
             { 
               function_copy = static_cast<SgExpression*>(help.copyAst(get_function())); 
             } 
            else 
             { 
               function_copy = NULL; 
             } 
  // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != NULL) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
  // Copy constructor parameter data member: exec_config_copy
     SgCudaKernelExecConfig* exec_config_copy; 
  // case: not a listType for (using conditionalToCopyVariable)exec_config
          if (get_exec_config() != NULL) 
             { 
               exec_config_copy = static_cast<SgCudaKernelExecConfig*>(help.copyAst(get_exec_config())); 
             } 
            else 
             { 
               exec_config_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCudaKernelCallExp(  startOfConstruct_copy, function_copy, args_copy, expression_type_copy, exec_config_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): uses_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for uses_operator_syntax
  // case: toBeCopied == COPY_DATA for uses_operator_syntax
     bool uses_operator_syntax_copy = p_uses_operator_syntax; 
     result->p_uses_operator_syntax = uses_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): usesObjectCallSyntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usesObjectCallSyntax
  // case: toBeCopied == COPY_DATA for usesObjectCallSyntax
     bool usesObjectCallSyntax_copy = p_usesObjectCallSyntax; 
     result->p_usesObjectCallSyntax = usesObjectCallSyntax_copy; 
  // case: not a listType for (using conditionalToSetParent)exec_config
          if ( (exec_config_copy != NULL) && (exec_config_copy->get_parent() == NULL) && (isSgType(exec_config_copy) == NULL) ) 
             { 
               exec_config_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)function
          if ( (function_copy != NULL) && (function_copy->get_parent() == NULL) && (isSgType(function_copy) == NULL) ) 
             { 
               function_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)args
          if ( (args_copy != NULL) && (args_copy->get_parent() == NULL) && (isSgType(args_copy) == NULL) ) 
             { 
               args_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCudaKernelCallExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCudaKernelCallExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 173808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcLocalsizeofExpression::copy ( SgCopyHelp& help) const
   {
     SgUpcLocalsizeofExpression* result = NULL;

  // printf ("Copy SgUpcLocalsizeofExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcLocalsizeofExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcLocalsizeofExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcLocalsizeofExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcLocalsizeofExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcLocalsizeofExpression(  startOfConstruct_copy, expression_copy, operand_type_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcLocalsizeofExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcLocalsizeofExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 174108 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcBlocksizeofExpression::copy ( SgCopyHelp& help) const
   {
     SgUpcBlocksizeofExpression* result = NULL;

  // printf ("Copy SgUpcBlocksizeofExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcBlocksizeofExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcBlocksizeofExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcBlocksizeofExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcBlocksizeofExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcBlocksizeofExpression(  startOfConstruct_copy, expression_copy, operand_type_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcBlocksizeofExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcBlocksizeofExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 174408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcElemsizeofExpression::copy ( SgCopyHelp& help) const
   {
     SgUpcElemsizeofExpression* result = NULL;

  // printf ("Copy SgUpcElemsizeofExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUpcElemsizeofExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUpcElemsizeofExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUpcElemsizeofExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUpcElemsizeofExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUpcElemsizeofExpression(  startOfConstruct_copy, expression_copy, operand_type_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUpcElemsizeofExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUpcElemsizeofExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 174708 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaInstanceOfOp::copy ( SgCopyHelp& help) const
   {
     SgJavaInstanceOfOp* result = NULL;

  // printf ("Copy SgJavaInstanceOfOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaInstanceOfOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaInstanceOfOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaInstanceOfOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaInstanceOfOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != NULL) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = NULL; 
             } 
  // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaInstanceOfOp(  startOfConstruct_copy, operand_expr_copy, operand_type_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
          if ( (operand_expr_copy != NULL) && (operand_expr_copy->get_parent() == NULL) && (isSgType(operand_expr_copy) == NULL) ) 
             { 
               operand_expr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaInstanceOfOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaInstanceOfOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 175008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSuperExp::copy ( SgCopyHelp& help) const
   {
     SgSuperExp* result = NULL;

  // printf ("Copy SgSuperExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSuperExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSuperExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSuperExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSuperExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: class_symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_symbol
  // case: toBeCopied == COPY_DATA for class_symbol
     SgClassSymbol* class_symbol_copy = p_class_symbol; 
  // Copy constructor parameter data member: pobj_super_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pobj_super
  // case: toBeCopied == COPY_DATA for pobj_super
     int pobj_super_copy = p_pobj_super; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSuperExp(  startOfConstruct_copy, class_symbol_copy, pobj_super_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSuperExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSuperExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 175292 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeIdOp::copy ( SgCopyHelp& help) const
   {
     SgTypeIdOp* result = NULL;

  // printf ("Copy SgTypeIdOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeIdOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeIdOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeIdOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeIdOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != NULL) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = NULL; 
             } 
  // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeIdOp(  startOfConstruct_copy, operand_expr_copy, operand_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_for_pointer_to_member_class_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_for_pointer_to_member_class_length
  // case: toBeCopied == COPY_DATA for name_qualification_for_pointer_to_member_class_length
     int name_qualification_for_pointer_to_member_class_length_copy = p_name_qualification_for_pointer_to_member_class_length; 
     result->p_name_qualification_for_pointer_to_member_class_length = name_qualification_for_pointer_to_member_class_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for type_elaboration_for_pointer_to_member_class_required
     bool type_elaboration_for_pointer_to_member_class_required_copy = p_type_elaboration_for_pointer_to_member_class_required; 
     result->p_type_elaboration_for_pointer_to_member_class_required = type_elaboration_for_pointer_to_member_class_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for global_qualification_for_pointer_to_member_class_required
     bool global_qualification_for_pointer_to_member_class_required_copy = p_global_qualification_for_pointer_to_member_class_required; 
     result->p_global_qualification_for_pointer_to_member_class_required = global_qualification_for_pointer_to_member_class_required_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
          if ( (operand_expr_copy != NULL) && (operand_expr_copy->get_parent() == NULL) && (isSgType(operand_expr_copy) == NULL) ) 
             { 
               operand_expr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeIdOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeIdOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 175618 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConditionalExp::copy ( SgCopyHelp& help) const
   {
     SgConditionalExp* result = NULL;

  // printf ("Copy SgConditionalExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgConditionalExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgConditionalExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgConditionalExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgConditionalExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: conditional_exp_copy
     SgExpression* conditional_exp_copy; 
  // case: not a listType for (using conditionalToCopyVariable)conditional_exp
          if (get_conditional_exp() != NULL) 
             { 
               conditional_exp_copy = static_cast<SgExpression*>(help.copyAst(get_conditional_exp())); 
             } 
            else 
             { 
               conditional_exp_copy = NULL; 
             } 
  // Copy constructor parameter data member: true_exp_copy
     SgExpression* true_exp_copy; 
  // case: not a listType for (using conditionalToCopyVariable)true_exp
          if (get_true_exp() != NULL) 
             { 
               true_exp_copy = static_cast<SgExpression*>(help.copyAst(get_true_exp())); 
             } 
            else 
             { 
               true_exp_copy = NULL; 
             } 
  // Copy constructor parameter data member: false_exp_copy
     SgExpression* false_exp_copy; 
  // case: not a listType for (using conditionalToCopyVariable)false_exp
          if (get_false_exp() != NULL) 
             { 
               false_exp_copy = static_cast<SgExpression*>(help.copyAst(get_false_exp())); 
             } 
            else 
             { 
               false_exp_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgConditionalExp(  startOfConstruct_copy, conditional_exp_copy, true_exp_copy, false_exp_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)conditional_exp
          if ( (conditional_exp_copy != NULL) && (conditional_exp_copy->get_parent() == NULL) && (isSgType(conditional_exp_copy) == NULL) ) 
             { 
               conditional_exp_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)true_exp
          if ( (true_exp_copy != NULL) && (true_exp_copy->get_parent() == NULL) && (isSgType(true_exp_copy) == NULL) ) 
             { 
               true_exp_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)false_exp
          if ( (false_exp_copy != NULL) && (false_exp_copy->get_parent() == NULL) && (isSgType(false_exp_copy) == NULL) ) 
             { 
               false_exp_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgConditionalExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgConditionalExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 175946 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNewExp::copy ( SgCopyHelp& help) const
   {
     SgNewExp* result = NULL;

  // printf ("Copy SgNewExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNewExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNewExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNewExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNewExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: specified_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specified_type
  // case: toBeCopied == COPY_DATA for specified_type
     SgType* specified_type_copy = p_specified_type; 
  // Copy constructor parameter data member: placement_args_copy
     SgExprListExp* placement_args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)placement_args
          if (get_placement_args() != NULL) 
             { 
               placement_args_copy = static_cast<SgExprListExp*>(help.copyAst(get_placement_args())); 
             } 
            else 
             { 
               placement_args_copy = NULL; 
             } 
  // Copy constructor parameter data member: constructor_args_copy
     SgConstructorInitializer* constructor_args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)constructor_args
          if (get_constructor_args() != NULL) 
             { 
               constructor_args_copy = static_cast<SgConstructorInitializer*>(help.copyAst(get_constructor_args())); 
             } 
            else 
             { 
               constructor_args_copy = NULL; 
             } 
  // Copy constructor parameter data member: builtin_args_copy
     SgExpression* builtin_args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)builtin_args
          if (get_builtin_args() != NULL) 
             { 
               builtin_args_copy = static_cast<SgExpression*>(help.copyAst(get_builtin_args())); 
             } 
            else 
             { 
               builtin_args_copy = NULL; 
             } 
  // Copy constructor parameter data member: need_global_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_global_specifier
  // case: toBeCopied == COPY_DATA for need_global_specifier
     short need_global_specifier_copy = p_need_global_specifier; 
  // Copy constructor parameter data member: newOperatorDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for newOperatorDeclaration
  // case: toBeCopied == COPY_DATA for newOperatorDeclaration
     SgFunctionDeclaration* newOperatorDeclaration_copy = p_newOperatorDeclaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNewExp(  startOfConstruct_copy, specified_type_copy, placement_args_copy, constructor_args_copy, builtin_args_copy, need_global_specifier_copy, newOperatorDeclaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_for_pointer_to_member_class_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_for_pointer_to_member_class_length
  // case: toBeCopied == COPY_DATA for name_qualification_for_pointer_to_member_class_length
     int name_qualification_for_pointer_to_member_class_length_copy = p_name_qualification_for_pointer_to_member_class_length; 
     result->p_name_qualification_for_pointer_to_member_class_length = name_qualification_for_pointer_to_member_class_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for type_elaboration_for_pointer_to_member_class_required
     bool type_elaboration_for_pointer_to_member_class_required_copy = p_type_elaboration_for_pointer_to_member_class_required; 
     result->p_type_elaboration_for_pointer_to_member_class_required = type_elaboration_for_pointer_to_member_class_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for global_qualification_for_pointer_to_member_class_required
     bool global_qualification_for_pointer_to_member_class_required_copy = p_global_qualification_for_pointer_to_member_class_required; 
     result->p_global_qualification_for_pointer_to_member_class_required = global_qualification_for_pointer_to_member_class_required_copy; 
  // case: not a listType for (using conditionalToSetParent)placement_args
          if ( (placement_args_copy != NULL) && (placement_args_copy->get_parent() == NULL) && (isSgType(placement_args_copy) == NULL) ) 
             { 
               placement_args_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)constructor_args
          if ( (constructor_args_copy != NULL) && (constructor_args_copy->get_parent() == NULL) && (isSgType(constructor_args_copy) == NULL) ) 
             { 
               constructor_args_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)builtin_args
          if ( (builtin_args_copy != NULL) && (builtin_args_copy->get_parent() == NULL) && (isSgType(builtin_args_copy) == NULL) ) 
             { 
               builtin_args_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNewExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNewExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 176312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeleteExp::copy ( SgCopyHelp& help) const
   {
     SgDeleteExp* result = NULL;

  // printf ("Copy SgDeleteExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDeleteExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDeleteExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDeleteExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDeleteExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: variable_copy
     SgExpression* variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variable
          if (get_variable() != NULL) 
             { 
               variable_copy = static_cast<SgExpression*>(help.copyAst(get_variable())); 
             } 
            else 
             { 
               variable_copy = NULL; 
             } 
  // Copy constructor parameter data member: is_array_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_array
  // case: toBeCopied == COPY_DATA for is_array
     short is_array_copy = p_is_array; 
  // Copy constructor parameter data member: need_global_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_global_specifier
  // case: toBeCopied == COPY_DATA for need_global_specifier
     short need_global_specifier_copy = p_need_global_specifier; 
  // Copy constructor parameter data member: deleteOperatorDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for deleteOperatorDeclaration
  // case: toBeCopied == COPY_DATA for deleteOperatorDeclaration
     SgFunctionDeclaration* deleteOperatorDeclaration_copy = p_deleteOperatorDeclaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDeleteExp(  startOfConstruct_copy, variable_copy, is_array_copy, need_global_specifier_copy, deleteOperatorDeclaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)variable
          if ( (variable_copy != NULL) && (variable_copy->get_parent() == NULL) && (isSgType(variable_copy) == NULL) ) 
             { 
               variable_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDeleteExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDeleteExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 176616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgThisExp::copy ( SgCopyHelp& help) const
   {
     SgThisExp* result = NULL;

  // printf ("Copy SgThisExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgThisExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgThisExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgThisExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgThisExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: class_symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_symbol
  // case: toBeCopied == COPY_DATA for class_symbol
     SgClassSymbol* class_symbol_copy = p_class_symbol; 
  // Copy constructor parameter data member: nonreal_symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_symbol
  // case: toBeCopied == COPY_DATA for nonreal_symbol
     SgNonrealSymbol* nonreal_symbol_copy = p_nonreal_symbol; 
  // Copy constructor parameter data member: pobj_this_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pobj_this
  // case: toBeCopied == COPY_DATA for pobj_this
     int pobj_this_copy = p_pobj_this; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgThisExp(  startOfConstruct_copy, class_symbol_copy, nonreal_symbol_copy, pobj_this_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgThisExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgThisExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 176904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRefExp::copy ( SgCopyHelp& help) const
   {
     SgRefExp* result = NULL;

  // printf ("Copy SgRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: type_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_name
  // case: toBeCopied == COPY_DATA for type_name
     SgType* type_name_copy = p_type_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRefExp(  startOfConstruct_copy, type_name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 177184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInitializer::copy ( SgCopyHelp& help) const
   {
     SgInitializer* result = NULL;

  // printf ("Copy SgInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgInitializer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgInitializer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgInitializer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgInitializer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgInitializer(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgInitializer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 177470 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAggregateInitializer::copy ( SgCopyHelp& help) const
   {
     SgAggregateInitializer* result = NULL;

  // printf ("Copy SgAggregateInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAggregateInitializer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAggregateInitializer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAggregateInitializer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAggregateInitializer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: initializers_copy
     SgExprListExp* initializers_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializers
          if (get_initializers() != NULL) 
             { 
               initializers_copy = static_cast<SgExprListExp*>(help.copyAst(get_initializers())); 
             } 
            else 
             { 
               initializers_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAggregateInitializer(  startOfConstruct_copy, initializers_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // Copy non-constructor parameter data member (access function): need_explicit_braces_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_explicit_braces
  // case: toBeCopied == COPY_DATA for need_explicit_braces
     bool need_explicit_braces_copy = p_need_explicit_braces; 
     result->p_need_explicit_braces = need_explicit_braces_copy; 
  // Copy non-constructor parameter data member (access function): uses_compound_literal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for uses_compound_literal
  // case: toBeCopied == COPY_DATA for uses_compound_literal
     bool uses_compound_literal_copy = p_uses_compound_literal; 
     result->p_uses_compound_literal = uses_compound_literal_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_type
     int name_qualification_length_for_type_copy = p_name_qualification_length_for_type; 
     result->p_name_qualification_length_for_type = name_qualification_length_for_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_type
     bool type_elaboration_required_for_type_copy = p_type_elaboration_required_for_type; 
     result->p_type_elaboration_required_for_type = type_elaboration_required_for_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_type
     bool global_qualification_required_for_type_copy = p_global_qualification_required_for_type; 
     result->p_global_qualification_required_for_type = global_qualification_required_for_type_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)initializers
          if ( (initializers_copy != NULL) && (initializers_copy->get_parent() == NULL) && (isSgType(initializers_copy) == NULL) ) 
             { 
               initializers_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAggregateInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAggregateInitializer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 177811 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCompoundInitializer::copy ( SgCopyHelp& help) const
   {
     SgCompoundInitializer* result = NULL;

  // printf ("Copy SgCompoundInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCompoundInitializer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCompoundInitializer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCompoundInitializer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCompoundInitializer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: initializers_copy
     SgExprListExp* initializers_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializers
          if (get_initializers() != NULL) 
             { 
               initializers_copy = static_cast<SgExprListExp*>(help.copyAst(get_initializers())); 
             } 
            else 
             { 
               initializers_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCompoundInitializer(  startOfConstruct_copy, initializers_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)initializers
          if ( (initializers_copy != NULL) && (initializers_copy->get_parent() == NULL) && (isSgType(initializers_copy) == NULL) ) 
             { 
               initializers_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCompoundInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCompoundInitializer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 178117 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConstructorInitializer::copy ( SgCopyHelp& help) const
   {
     SgConstructorInitializer* result = NULL;

  // printf ("Copy SgConstructorInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgConstructorInitializer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgConstructorInitializer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgConstructorInitializer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgConstructorInitializer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgMemberFunctionDeclaration* declaration_copy = p_declaration; 
  // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != NULL) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
  // Copy constructor parameter data member: need_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_name
  // case: toBeCopied == COPY_DATA for need_name
     bool need_name_copy = p_need_name; 
  // Copy constructor parameter data member: need_qualifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_qualifier
  // case: toBeCopied == COPY_DATA for need_qualifier
     bool need_qualifier_copy = p_need_qualifier; 
  // Copy constructor parameter data member: need_parenthesis_after_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_parenthesis_after_name
  // case: toBeCopied == COPY_DATA for need_parenthesis_after_name
     bool need_parenthesis_after_name_copy = p_need_parenthesis_after_name; 
  // Copy constructor parameter data member: associated_class_unknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associated_class_unknown
  // case: toBeCopied == COPY_DATA for associated_class_unknown
     bool associated_class_unknown_copy = p_associated_class_unknown; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgConstructorInitializer(  startOfConstruct_copy, declaration_copy, args_copy, expression_type_copy, need_name_copy, need_qualifier_copy, need_parenthesis_after_name_copy, associated_class_unknown_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): is_used_in_conditional_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_used_in_conditional
  // case: toBeCopied == COPY_DATA for is_used_in_conditional
     bool is_used_in_conditional_copy = p_is_used_in_conditional; 
     result->p_is_used_in_conditional = is_used_in_conditional_copy; 
  // case: not a listType for (using conditionalToSetParent)args
          if ( (args_copy != NULL) && (args_copy->get_parent() == NULL) && (isSgType(args_copy) == NULL) ) 
             { 
               args_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgConstructorInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgConstructorInitializer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 178463 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssignInitializer::copy ( SgCopyHelp& help) const
   {
     SgAssignInitializer* result = NULL;

  // printf ("Copy SgAssignInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAssignInitializer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAssignInitializer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAssignInitializer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAssignInitializer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != NULL) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAssignInitializer(  startOfConstruct_copy, operand_i_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
          if ( (operand_i_copy != NULL) && (operand_i_copy->get_parent() == NULL) && (isSgType(operand_i_copy) == NULL) ) 
             { 
               operand_i_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAssignInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAssignInitializer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 178769 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDesignatedInitializer::copy ( SgCopyHelp& help) const
   {
     SgDesignatedInitializer* result = NULL;

  // printf ("Copy SgDesignatedInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDesignatedInitializer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDesignatedInitializer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDesignatedInitializer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDesignatedInitializer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: designatorList_copy
     SgExprListExp* designatorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)designatorList
          if (get_designatorList() != NULL) 
             { 
               designatorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_designatorList())); 
             } 
            else 
             { 
               designatorList_copy = NULL; 
             } 
  // Copy constructor parameter data member: memberInit_copy
     SgInitializer* memberInit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)memberInit
          if (get_memberInit() != NULL) 
             { 
               memberInit_copy = static_cast<SgInitializer*>(help.copyAst(get_memberInit())); 
             } 
            else 
             { 
               memberInit_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDesignatedInitializer(  startOfConstruct_copy, designatorList_copy, memberInit_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)designatorList
          if ( (designatorList_copy != NULL) && (designatorList_copy->get_parent() == NULL) && (isSgType(designatorList_copy) == NULL) ) 
             { 
               designatorList_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)memberInit
          if ( (memberInit_copy != NULL) && (memberInit_copy->get_parent() == NULL) && (isSgType(memberInit_copy) == NULL) ) 
             { 
               memberInit_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDesignatedInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDesignatedInitializer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 179087 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBracedInitializer::copy ( SgCopyHelp& help) const
   {
     SgBracedInitializer* result = NULL;

  // printf ("Copy SgBracedInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgBracedInitializer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgBracedInitializer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgBracedInitializer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgBracedInitializer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: initializers_copy
     SgExprListExp* initializers_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializers
          if (get_initializers() != NULL) 
             { 
               initializers_copy = static_cast<SgExprListExp*>(help.copyAst(get_initializers())); 
             } 
            else 
             { 
               initializers_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgBracedInitializer(  startOfConstruct_copy, initializers_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)initializers
          if ( (initializers_copy != NULL) && (initializers_copy->get_parent() == NULL) && (isSgType(initializers_copy) == NULL) ) 
             { 
               initializers_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgBracedInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgBracedInitializer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 179393 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAncestorInitializer::copy ( SgCopyHelp& help) const
   {
     SgAdaAncestorInitializer* result = NULL;

  // printf ("Copy SgAdaAncestorInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaAncestorInitializer*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaAncestorInitializer*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaAncestorInitializer: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaAncestorInitializer*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: ancestor_copy
     SgExpression* ancestor_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ancestor
          if (get_ancestor() != NULL) 
             { 
               ancestor_copy = static_cast<SgExpression*>(help.copyAst(get_ancestor())); 
             } 
            else 
             { 
               ancestor_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaAncestorInitializer(  startOfConstruct_copy, ancestor_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)ancestor
          if ( (ancestor_copy != NULL) && (ancestor_copy->get_parent() == NULL) && (isSgType(ancestor_copy) == NULL) ) 
             { 
               ancestor_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaAncestorInitializer = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaAncestorInitializer::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 179695 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgStartOp::copy ( SgCopyHelp& help) const
   {
     SgVarArgStartOp* result = NULL;

  // printf ("Copy SgVarArgStartOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVarArgStartOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVarArgStartOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVarArgStartOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVarArgStartOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_copy
     SgExpression* lhs_operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand
          if (get_lhs_operand() != NULL) 
             { 
               lhs_operand_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand())); 
             } 
            else 
             { 
               lhs_operand_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_copy
     SgExpression* rhs_operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand
          if (get_rhs_operand() != NULL) 
             { 
               rhs_operand_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand())); 
             } 
            else 
             { 
               rhs_operand_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVarArgStartOp(  startOfConstruct_copy, lhs_operand_copy, rhs_operand_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand
          if ( (lhs_operand_copy != NULL) && (lhs_operand_copy->get_parent() == NULL) && (isSgType(lhs_operand_copy) == NULL) ) 
             { 
               lhs_operand_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand
          if ( (rhs_operand_copy != NULL) && (rhs_operand_copy->get_parent() == NULL) && (isSgType(rhs_operand_copy) == NULL) ) 
             { 
               rhs_operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVarArgStartOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVarArgStartOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 180007 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgOp::copy ( SgCopyHelp& help) const
   {
     SgVarArgOp* result = NULL;

  // printf ("Copy SgVarArgOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVarArgOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVarArgOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVarArgOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVarArgOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != NULL) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVarArgOp(  startOfConstruct_copy, operand_expr_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
          if ( (operand_expr_copy != NULL) && (operand_expr_copy->get_parent() == NULL) && (isSgType(operand_expr_copy) == NULL) ) 
             { 
               operand_expr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVarArgOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVarArgOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 180303 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgEndOp::copy ( SgCopyHelp& help) const
   {
     SgVarArgEndOp* result = NULL;

  // printf ("Copy SgVarArgEndOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVarArgEndOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVarArgEndOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVarArgEndOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVarArgEndOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != NULL) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVarArgEndOp(  startOfConstruct_copy, operand_expr_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
          if ( (operand_expr_copy != NULL) && (operand_expr_copy->get_parent() == NULL) && (isSgType(operand_expr_copy) == NULL) ) 
             { 
               operand_expr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVarArgEndOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVarArgEndOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 180599 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgCopyOp::copy ( SgCopyHelp& help) const
   {
     SgVarArgCopyOp* result = NULL;

  // printf ("Copy SgVarArgCopyOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVarArgCopyOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVarArgCopyOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVarArgCopyOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVarArgCopyOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_operand_copy
     SgExpression* lhs_operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand
          if (get_lhs_operand() != NULL) 
             { 
               lhs_operand_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand())); 
             } 
            else 
             { 
               lhs_operand_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_operand_copy
     SgExpression* rhs_operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand
          if (get_rhs_operand() != NULL) 
             { 
               rhs_operand_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand())); 
             } 
            else 
             { 
               rhs_operand_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVarArgCopyOp(  startOfConstruct_copy, lhs_operand_copy, rhs_operand_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand
          if ( (lhs_operand_copy != NULL) && (lhs_operand_copy->get_parent() == NULL) && (isSgType(lhs_operand_copy) == NULL) ) 
             { 
               lhs_operand_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand
          if ( (rhs_operand_copy != NULL) && (rhs_operand_copy->get_parent() == NULL) && (isSgType(rhs_operand_copy) == NULL) ) 
             { 
               rhs_operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVarArgCopyOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVarArgCopyOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 180911 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgStartOneOperandOp::copy ( SgCopyHelp& help) const
   {
     SgVarArgStartOneOperandOp* result = NULL;

  // printf ("Copy SgVarArgStartOneOperandOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVarArgStartOneOperandOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVarArgStartOneOperandOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVarArgStartOneOperandOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVarArgStartOneOperandOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != NULL) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = NULL; 
             } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVarArgStartOneOperandOp(  startOfConstruct_copy, operand_expr_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
          if ( (operand_expr_copy != NULL) && (operand_expr_copy->get_parent() == NULL) && (isSgType(operand_expr_copy) == NULL) ) 
             { 
               operand_expr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVarArgStartOneOperandOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVarArgStartOneOperandOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 181207 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNullExpression::copy ( SgCopyHelp& help) const
   {
     SgNullExpression* result = NULL;

  // printf ("Copy SgNullExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNullExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNullExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNullExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNullExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNullExpression(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNullExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNullExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 181483 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariantExpression::copy ( SgCopyHelp& help) const
   {
     SgVariantExpression* result = NULL;

  // printf ("Copy SgVariantExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVariantExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVariantExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVariantExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVariantExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVariantExpression(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVariantExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVariantExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 181759 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSubscriptExpression::copy ( SgCopyHelp& help) const
   {
     SgSubscriptExpression* result = NULL;

  // printf ("Copy SgSubscriptExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSubscriptExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSubscriptExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSubscriptExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSubscriptExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lowerBound_copy
     SgExpression* lowerBound_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lowerBound
          if (get_lowerBound() != NULL) 
             { 
               lowerBound_copy = static_cast<SgExpression*>(help.copyAst(get_lowerBound())); 
             } 
            else 
             { 
               lowerBound_copy = NULL; 
             } 
  // Copy constructor parameter data member: upperBound_copy
     SgExpression* upperBound_copy; 
  // case: not a listType for (using conditionalToCopyVariable)upperBound
          if (get_upperBound() != NULL) 
             { 
               upperBound_copy = static_cast<SgExpression*>(help.copyAst(get_upperBound())); 
             } 
            else 
             { 
               upperBound_copy = NULL; 
             } 
  // Copy constructor parameter data member: stride_copy
     SgExpression* stride_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stride
          if (get_stride() != NULL) 
             { 
               stride_copy = static_cast<SgExpression*>(help.copyAst(get_stride())); 
             } 
            else 
             { 
               stride_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSubscriptExpression(  startOfConstruct_copy, lowerBound_copy, upperBound_copy, stride_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)lowerBound
          if ( (lowerBound_copy != NULL) && (lowerBound_copy->get_parent() == NULL) && (isSgType(lowerBound_copy) == NULL) ) 
             { 
               lowerBound_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)upperBound
          if ( (upperBound_copy != NULL) && (upperBound_copy->get_parent() == NULL) && (isSgType(upperBound_copy) == NULL) ) 
             { 
               upperBound_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)stride
          if ( (stride_copy != NULL) && (stride_copy->get_parent() == NULL) && (isSgType(stride_copy) == NULL) ) 
             { 
               stride_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSubscriptExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSubscriptExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 182083 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgColonShapeExp::copy ( SgCopyHelp& help) const
   {
     SgColonShapeExp* result = NULL;

  // printf ("Copy SgColonShapeExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgColonShapeExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgColonShapeExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgColonShapeExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgColonShapeExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgColonShapeExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgColonShapeExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgColonShapeExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 182359 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsteriskShapeExp::copy ( SgCopyHelp& help) const
   {
     SgAsteriskShapeExp* result = NULL;

  // printf ("Copy SgAsteriskShapeExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsteriskShapeExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsteriskShapeExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsteriskShapeExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsteriskShapeExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsteriskShapeExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsteriskShapeExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsteriskShapeExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 182635 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImpliedDo::copy ( SgCopyHelp& help) const
   {
     SgImpliedDo* result = NULL;

  // printf ("Copy SgImpliedDo = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgImpliedDo*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgImpliedDo*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgImpliedDo: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgImpliedDo*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: do_var_initialization_copy
     SgExpression* do_var_initialization_copy; 
  // case: not a listType for (using conditionalToCopyVariable)do_var_initialization
          if (get_do_var_initialization() != NULL) 
             { 
               do_var_initialization_copy = static_cast<SgExpression*>(help.copyAst(get_do_var_initialization())); 
             } 
            else 
             { 
               do_var_initialization_copy = NULL; 
             } 
  // Copy constructor parameter data member: last_val_copy
     SgExpression* last_val_copy; 
  // case: not a listType for (using conditionalToCopyVariable)last_val
          if (get_last_val() != NULL) 
             { 
               last_val_copy = static_cast<SgExpression*>(help.copyAst(get_last_val())); 
             } 
            else 
             { 
               last_val_copy = NULL; 
             } 
  // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != NULL) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = NULL; 
             } 
  // Copy constructor parameter data member: object_list_copy
     SgExprListExp* object_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)object_list
          if (get_object_list() != NULL) 
             { 
               object_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_object_list())); 
             } 
            else 
             { 
               object_list_copy = NULL; 
             } 
  // Copy constructor parameter data member: implied_do_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for implied_do_scope
  // case: toBeCopied == COPY_DATA for implied_do_scope
     SgScopeStatement* implied_do_scope_copy = p_implied_do_scope; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgImpliedDo(  startOfConstruct_copy, do_var_initialization_copy, last_val_copy, increment_copy, object_list_copy, implied_do_scope_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)do_var_initialization
          if ( (do_var_initialization_copy != NULL) && (do_var_initialization_copy->get_parent() == NULL) && (isSgType(do_var_initialization_copy) == NULL) ) 
             { 
               do_var_initialization_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)last_val
          if ( (last_val_copy != NULL) && (last_val_copy->get_parent() == NULL) && (isSgType(last_val_copy) == NULL) ) 
             { 
               last_val_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)increment
          if ( (increment_copy != NULL) && (increment_copy->get_parent() == NULL) && (isSgType(increment_copy) == NULL) ) 
             { 
               increment_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)object_list
          if ( (object_list_copy != NULL) && (object_list_copy->get_parent() == NULL) && (isSgType(object_list_copy) == NULL) ) 
             { 
               object_list_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgImpliedDo = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgImpliedDo::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 182979 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIOItemExpression::copy ( SgCopyHelp& help) const
   {
     SgIOItemExpression* result = NULL;

  // printf ("Copy SgIOItemExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIOItemExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIOItemExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIOItemExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIOItemExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: io_item_copy
     SgExpression* io_item_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_item
          if (get_io_item() != NULL) 
             { 
               io_item_copy = static_cast<SgExpression*>(help.copyAst(get_io_item())); 
             } 
            else 
             { 
               io_item_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIOItemExpression(  startOfConstruct_copy, name_copy, io_item_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)io_item
          if ( (io_item_copy != NULL) && (io_item_copy->get_parent() == NULL) && (isSgType(io_item_copy) == NULL) ) 
             { 
               io_item_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIOItemExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIOItemExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 183275 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgActualArgumentExpression::copy ( SgCopyHelp& help) const
   {
     SgActualArgumentExpression* result = NULL;

  // printf ("Copy SgActualArgumentExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgActualArgumentExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgActualArgumentExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgActualArgumentExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgActualArgumentExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: argument_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_name
  // case: toBeCopied == COPY_DATA for argument_name
     SgName argument_name_copy = p_argument_name; 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgActualArgumentExpression(  startOfConstruct_copy, argument_name_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgActualArgumentExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgActualArgumentExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 183571 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStatementExpression::copy ( SgCopyHelp& help) const
   {
     SgStatementExpression* result = NULL;

  // printf ("Copy SgStatementExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStatementExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStatementExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStatementExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStatementExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: statement_copy
     SgStatement* statement_copy; 
  // case: not a listType for (using conditionalToCopyVariable)statement
          if (get_statement() != NULL) 
             { 
               statement_copy = static_cast<SgStatement*>(help.copyAst(get_statement())); 
             } 
            else 
             { 
               statement_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStatementExpression(  startOfConstruct_copy, statement_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)statement
          if ( (statement_copy != NULL) && (statement_copy->get_parent() == NULL) && (isSgType(statement_copy) == NULL) ) 
             { 
               statement_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStatementExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStatementExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 183863 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmOp::copy ( SgCopyHelp& help) const
   {
     SgAsmOp* result = NULL;

  // printf ("Copy SgAsmOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: constraint_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constraint
  // case: toBeCopied == COPY_DATA for constraint
     SgAsmOp::asm_operand_constraint_enum constraint_copy = p_constraint; 
  // Copy constructor parameter data member: modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgAsmOp::asm_operand_modifier_enum modifiers_copy = p_modifiers; 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmOp(  startOfConstruct_copy, constraint_copy, modifiers_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): recordRawAsmOperandDescriptions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for recordRawAsmOperandDescriptions
  // case: toBeCopied == COPY_DATA for recordRawAsmOperandDescriptions
     bool recordRawAsmOperandDescriptions_copy = p_recordRawAsmOperandDescriptions; 
     result->p_recordRawAsmOperandDescriptions = recordRawAsmOperandDescriptions_copy; 
  // Copy non-constructor parameter data member (access function): isOutputOperand_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isOutputOperand
  // case: toBeCopied == COPY_DATA for isOutputOperand
     bool isOutputOperand_copy = p_isOutputOperand; 
     result->p_isOutputOperand = isOutputOperand_copy; 
  // Copy non-constructor parameter data member (access function): constraintString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constraintString
  // case: toBeCopied == COPY_DATA for constraintString
     std::string constraintString_copy = p_constraintString; 
     result->p_constraintString = constraintString_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 184183 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCudaKernelExecConfig::copy ( SgCopyHelp& help) const
   {
     SgCudaKernelExecConfig* result = NULL;

  // printf ("Copy SgCudaKernelExecConfig = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCudaKernelExecConfig*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCudaKernelExecConfig*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCudaKernelExecConfig: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCudaKernelExecConfig*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: grid_copy
     SgExpression* grid_copy; 
  // case: not a listType for (using conditionalToCopyVariable)grid
          if (get_grid() != NULL) 
             { 
               grid_copy = static_cast<SgExpression*>(help.copyAst(get_grid())); 
             } 
            else 
             { 
               grid_copy = NULL; 
             } 
  // Copy constructor parameter data member: blocks_copy
     SgExpression* blocks_copy; 
  // case: not a listType for (using conditionalToCopyVariable)blocks
          if (get_blocks() != NULL) 
             { 
               blocks_copy = static_cast<SgExpression*>(help.copyAst(get_blocks())); 
             } 
            else 
             { 
               blocks_copy = NULL; 
             } 
  // Copy constructor parameter data member: shared_copy
     SgExpression* shared_copy; 
  // case: not a listType for (using conditionalToCopyVariable)shared
          if (get_shared() != NULL) 
             { 
               shared_copy = static_cast<SgExpression*>(help.copyAst(get_shared())); 
             } 
            else 
             { 
               shared_copy = NULL; 
             } 
  // Copy constructor parameter data member: stream_copy
     SgExpression* stream_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stream
          if (get_stream() != NULL) 
             { 
               stream_copy = static_cast<SgExpression*>(help.copyAst(get_stream())); 
             } 
            else 
             { 
               stream_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCudaKernelExecConfig(  startOfConstruct_copy, grid_copy, blocks_copy, shared_copy, stream_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)grid
          if ( (grid_copy != NULL) && (grid_copy->get_parent() == NULL) && (isSgType(grid_copy) == NULL) ) 
             { 
               grid_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)blocks
          if ( (blocks_copy != NULL) && (blocks_copy->get_parent() == NULL) && (isSgType(blocks_copy) == NULL) ) 
             { 
               blocks_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)shared
          if ( (shared_copy != NULL) && (shared_copy->get_parent() == NULL) && (isSgType(shared_copy) == NULL) ) 
             { 
               shared_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)stream
          if ( (stream_copy != NULL) && (stream_copy->get_parent() == NULL) && (isSgType(stream_copy) == NULL) ) 
             { 
               stream_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCudaKernelExecConfig = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCudaKernelExecConfig::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 184523 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnknownArrayOrFunctionReference::copy ( SgCopyHelp& help) const
   {
     SgUnknownArrayOrFunctionReference* result = NULL;

  // printf ("Copy SgUnknownArrayOrFunctionReference = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgUnknownArrayOrFunctionReference*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgUnknownArrayOrFunctionReference*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgUnknownArrayOrFunctionReference: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgUnknownArrayOrFunctionReference*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgUnknownArrayOrFunctionReference(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): named_reference_copy
     SgExpression* named_reference_copy; 
  // case: not a listType for (using conditionalToCopyVariable)named_reference
          if (get_named_reference() != NULL) 
             { 
               named_reference_copy = static_cast<SgExpression*>(help.copyAst(get_named_reference())); 
             } 
            else 
             { 
               named_reference_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_named_reference != NULL) delete result->p_named_reference; 
     result->p_named_reference = named_reference_copy; 
  // case: not a listType for (using conditionalToSetParent)named_reference
          if ( (named_reference_copy != NULL) && (named_reference_copy->get_parent() == NULL) && (isSgType(named_reference_copy) == NULL) ) 
             { 
               named_reference_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): expression_list_copy
     SgExprListExp* expression_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression_list
          if (get_expression_list() != NULL) 
             { 
               expression_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_expression_list())); 
             } 
            else 
             { 
               expression_list_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_expression_list != NULL) delete result->p_expression_list; 
     result->p_expression_list = expression_list_copy; 
  // case: not a listType for (using conditionalToSetParent)expression_list
          if ( (expression_list_copy != NULL) && (expression_list_copy->get_parent() == NULL) && (isSgType(expression_list_copy) == NULL) ) 
             { 
               expression_list_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgUnknownArrayOrFunctionReference = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgUnknownArrayOrFunctionReference::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 184842 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPseudoDestructorRefExp::copy ( SgCopyHelp& help) const
   {
     SgPseudoDestructorRefExp* result = NULL;

  // printf ("Copy SgPseudoDestructorRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgPseudoDestructorRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgPseudoDestructorRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgPseudoDestructorRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgPseudoDestructorRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: object_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for object_type
  // case: toBeCopied == COPY_DATA for object_type
     SgType* object_type_copy = p_object_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgPseudoDestructorRefExp(  startOfConstruct_copy, object_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
     result->p_expression_type = expression_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgPseudoDestructorRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgPseudoDestructorRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 185142 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCAFCoExpression::copy ( SgCopyHelp& help) const
   {
     SgCAFCoExpression* result = NULL;

  // printf ("Copy SgCAFCoExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCAFCoExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCAFCoExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCAFCoExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCAFCoExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: teamId_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for teamId
  // case: toBeCopied == COPY_DATA for teamId
     SgVarRefExp* teamId_copy = p_teamId; 
  // Copy constructor parameter data member: teamRank_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for teamRank
  // case: toBeCopied == COPY_DATA for teamRank
     SgExpression* teamRank_copy = p_teamRank; 
  // Copy constructor parameter data member: referData_copy
     SgExpression* referData_copy; 
  // case: not a listType for (using conditionalToCopyVariable)referData
          if (get_referData() != NULL) 
             { 
               referData_copy = static_cast<SgExpression*>(help.copyAst(get_referData())); 
             } 
            else 
             { 
               referData_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCAFCoExpression(  startOfConstruct_copy, teamId_copy, teamRank_copy, referData_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)referData
          if ( (referData_copy != NULL) && (referData_copy->get_parent() == NULL) && (isSgType(referData_copy) == NULL) ) 
             { 
               referData_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCAFCoExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCAFCoExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 185442 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSizeOfOp::copy ( SgCopyHelp& help) const
   {
     SgSizeOfOp* result = NULL;

  // printf ("Copy SgSizeOfOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSizeOfOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSizeOfOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSizeOfOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSizeOfOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != NULL) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = NULL; 
             } 
  // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSizeOfOp(  startOfConstruct_copy, operand_expr_copy, operand_type_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): sizeOfContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sizeOfContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for sizeOfContainsBaseTypeDefiningDeclaration
     bool sizeOfContainsBaseTypeDefiningDeclaration_copy = p_sizeOfContainsBaseTypeDefiningDeclaration; 
     result->p_sizeOfContainsBaseTypeDefiningDeclaration = sizeOfContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): is_objectless_nonstatic_data_member_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_objectless_nonstatic_data_member_reference
  // case: toBeCopied == COPY_DATA for is_objectless_nonstatic_data_member_reference
     bool is_objectless_nonstatic_data_member_reference_copy = p_is_objectless_nonstatic_data_member_reference; 
     result->p_is_objectless_nonstatic_data_member_reference = is_objectless_nonstatic_data_member_reference_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_for_pointer_to_member_class_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_for_pointer_to_member_class_length
  // case: toBeCopied == COPY_DATA for name_qualification_for_pointer_to_member_class_length
     int name_qualification_for_pointer_to_member_class_length_copy = p_name_qualification_for_pointer_to_member_class_length; 
     result->p_name_qualification_for_pointer_to_member_class_length = name_qualification_for_pointer_to_member_class_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for type_elaboration_for_pointer_to_member_class_required
     bool type_elaboration_for_pointer_to_member_class_required_copy = p_type_elaboration_for_pointer_to_member_class_required; 
     result->p_type_elaboration_for_pointer_to_member_class_required = type_elaboration_for_pointer_to_member_class_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for global_qualification_for_pointer_to_member_class_required
     bool global_qualification_for_pointer_to_member_class_required_copy = p_global_qualification_for_pointer_to_member_class_required; 
     result->p_global_qualification_for_pointer_to_member_class_required = global_qualification_for_pointer_to_member_class_required_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
          if ( (operand_expr_copy != NULL) && (operand_expr_copy->get_parent() == NULL) && (isSgType(operand_expr_copy) == NULL) ) 
             { 
               operand_expr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSizeOfOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSizeOfOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 185782 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLambdaRefExp::copy ( SgCopyHelp& help) const
   {
     SgLambdaRefExp* result = NULL;

  // printf ("Copy SgLambdaRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLambdaRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLambdaRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLambdaRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLambdaRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: functionDeclaration_copy
     SgFunctionDeclaration* functionDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)functionDeclaration
          if (get_functionDeclaration() != NULL) 
             { 
               functionDeclaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_functionDeclaration())); 
             } 
            else 
             { 
               functionDeclaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLambdaRefExp(  startOfConstruct_copy, functionDeclaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)functionDeclaration
          if ( (functionDeclaration_copy != NULL) && (functionDeclaration_copy->get_parent() == NULL) && (isSgType(functionDeclaration_copy) == NULL) ) 
             { 
               functionDeclaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLambdaRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLambdaRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 186074 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDictionaryExp::copy ( SgCopyHelp& help) const
   {
     SgDictionaryExp* result = NULL;

  // printf ("Copy SgDictionaryExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDictionaryExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDictionaryExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDictionaryExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDictionaryExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDictionaryExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_key_datum_pairs
     SgKeyDatumPairPtrList key_datum_pairs_copy; 
  // case: listType for key_datum_pairs
  // case: listType (typeIsPointerToList == false) for key_datum_pairs
     SgKeyDatumPairPtrList::const_iterator source_key_datum_pairs_iterator = get_key_datum_pairs().begin(); 
     for ( /* empty by design */; source_key_datum_pairs_iterator != get_key_datum_pairs().end(); ++source_key_datum_pairs_iterator) 
        { 
          SgKeyDatumPair* source_list_element = *source_key_datum_pairs_iterator; 
          SgKeyDatumPair* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgKeyDatumPair*>(help.copyAst(*source_key_datum_pairs_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          key_datum_pairs_copy.push_back(copy_list_element); 
        } 
     result->p_key_datum_pairs = key_datum_pairs_copy; 
  // case: listType for key_datum_pairs
  // case: listType (typeIsPointerToList == false) for key_datum_pairs
     SgKeyDatumPairPtrList::const_iterator key_datum_pairs_iterator = result->get_key_datum_pairs().begin(); 
     for ( /* empty by design */; key_datum_pairs_iterator != result->get_key_datum_pairs().end(); ++key_datum_pairs_iterator) 
        { 
          SgKeyDatumPair* list_element = *key_datum_pairs_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDictionaryExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDictionaryExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 186381 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgKeyDatumPair::copy ( SgCopyHelp& help) const
   {
     SgKeyDatumPair* result = NULL;

  // printf ("Copy SgKeyDatumPair = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgKeyDatumPair*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgKeyDatumPair*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgKeyDatumPair: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgKeyDatumPair*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: key_copy
     SgExpression* key_copy; 
  // case: not a listType for (using conditionalToCopyVariable)key
          if (get_key() != NULL) 
             { 
               key_copy = static_cast<SgExpression*>(help.copyAst(get_key())); 
             } 
            else 
             { 
               key_copy = NULL; 
             } 
  // Copy constructor parameter data member: datum_copy
     SgExpression* datum_copy; 
  // case: not a listType for (using conditionalToCopyVariable)datum
          if (get_datum() != NULL) 
             { 
               datum_copy = static_cast<SgExpression*>(help.copyAst(get_datum())); 
             } 
            else 
             { 
               datum_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgKeyDatumPair(  startOfConstruct_copy, key_copy, datum_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)key
          if ( (key_copy != NULL) && (key_copy->get_parent() == NULL) && (isSgType(key_copy) == NULL) ) 
             { 
               key_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)datum
          if ( (datum_copy != NULL) && (datum_copy->get_parent() == NULL) && (isSgType(datum_copy) == NULL) ) 
             { 
               datum_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgKeyDatumPair = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgKeyDatumPair::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 186689 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReferenceExp::copy ( SgCopyHelp& help) const
   {
     SgReferenceExp* result = NULL;

  // printf ("Copy SgReferenceExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgReferenceExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgReferenceExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgReferenceExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgReferenceExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgReferenceExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgReferenceExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgReferenceExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 186965 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarRefExp::copy ( SgCopyHelp& help) const
   {
     SgVarRefExp* result = NULL;

  // printf ("Copy SgVarRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVarRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVarRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVarRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVarRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgVariableSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVarRefExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVarRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVarRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 187265 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLabelRefExp::copy ( SgCopyHelp& help) const
   {
     SgLabelRefExp* result = NULL;

  // printf ("Copy SgLabelRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLabelRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLabelRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLabelRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLabelRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgLabelSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLabelRefExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLabelRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLabelRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 187545 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassNameRefExp::copy ( SgCopyHelp& help) const
   {
     SgClassNameRefExp* result = NULL;

  // printf ("Copy SgClassNameRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClassNameRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClassNameRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClassNameRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClassNameRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgClassSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClassNameRefExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClassNameRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClassNameRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 187825 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealRefExp::copy ( SgCopyHelp& help) const
   {
     SgNonrealRefExp* result = NULL;

  // printf ("Copy SgNonrealRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNonrealRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNonrealRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNonrealRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNonrealRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgNonrealSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNonrealRefExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNonrealRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNonrealRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 188120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgScopedRefExp::copy ( SgCopyHelp& help) const
   {
     SgScopedRefExp* result = NULL;

  // printf ("Copy SgScopedRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgScopedRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgScopedRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgScopedRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgScopedRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lhs_copy
     SgReferenceExp *  lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgReferenceExp * >(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  // Copy constructor parameter data member: rhs_copy
     SgReferenceExp *  rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgReferenceExp * >(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgScopedRefExp(  startOfConstruct_copy, lhs_copy, rhs_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgScopedRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgScopedRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 188428 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeRefExp::copy ( SgCopyHelp& help) const
   {
     SgTypeRefExp* result = NULL;

  // printf ("Copy SgTypeRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: named_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_type
  // case: toBeCopied == COPY_DATA for named_type
     SgNamedType *  named_type_copy = p_named_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeRefExp(  startOfConstruct_copy, named_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 188708 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionRefExp::copy ( SgCopyHelp& help) const
   {
     SgFunctionRefExp* result = NULL;

  // printf ("Copy SgFunctionRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_i_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol_i
  // case: toBeCopied == COPY_DATA for symbol_i
     SgFunctionSymbol* symbol_i_copy = p_symbol_i; 
  // Copy constructor parameter data member: function_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for function_type
  // case: toBeCopied == COPY_DATA for function_type
     SgFunctionType* function_type_copy = p_function_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionRefExp(  startOfConstruct_copy, symbol_i_copy, function_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 189012 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMemberFunctionRefExp::copy ( SgCopyHelp& help) const
   {
     SgMemberFunctionRefExp* result = NULL;

  // printf ("Copy SgMemberFunctionRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMemberFunctionRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMemberFunctionRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMemberFunctionRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMemberFunctionRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_i_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol_i
  // case: toBeCopied == COPY_DATA for symbol_i
     SgMemberFunctionSymbol* symbol_i_copy = p_symbol_i; 
  // Copy constructor parameter data member: virtual_call_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtual_call
  // case: toBeCopied == COPY_DATA for virtual_call
     int virtual_call_copy = p_virtual_call; 
  // Copy constructor parameter data member: function_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for function_type
  // case: toBeCopied == COPY_DATA for function_type
     SgFunctionType* function_type_copy = p_function_type; 
  // Copy constructor parameter data member: need_qualifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_qualifier
  // case: toBeCopied == COPY_DATA for need_qualifier
     int need_qualifier_copy = p_need_qualifier; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMemberFunctionRefExp(  startOfConstruct_copy, symbol_i_copy, virtual_call_copy, function_type_copy, need_qualifier_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMemberFunctionRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMemberFunctionRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 189319 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateFunctionRefExp::copy ( SgCopyHelp& help) const
   {
     SgTemplateFunctionRefExp* result = NULL;

  // printf ("Copy SgTemplateFunctionRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateFunctionRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateFunctionRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateFunctionRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateFunctionRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_i_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol_i
  // case: toBeCopied == COPY_DATA for symbol_i
     SgTemplateFunctionSymbol* symbol_i_copy = p_symbol_i; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateFunctionRefExp(  startOfConstruct_copy, symbol_i_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateFunctionRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateFunctionRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 189614 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateMemberFunctionRefExp::copy ( SgCopyHelp& help) const
   {
     SgTemplateMemberFunctionRefExp* result = NULL;

  // printf ("Copy SgTemplateMemberFunctionRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateMemberFunctionRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateMemberFunctionRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateMemberFunctionRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateMemberFunctionRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_i_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol_i
  // case: toBeCopied == COPY_DATA for symbol_i
     SgTemplateMemberFunctionSymbol* symbol_i_copy = p_symbol_i; 
  // Copy constructor parameter data member: virtual_call_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtual_call
  // case: toBeCopied == COPY_DATA for virtual_call
     int virtual_call_copy = p_virtual_call; 
  // Copy constructor parameter data member: need_qualifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_qualifier
  // case: toBeCopied == COPY_DATA for need_qualifier
     int need_qualifier_copy = p_need_qualifier; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateMemberFunctionRefExp(  startOfConstruct_copy, symbol_i_copy, virtual_call_copy, need_qualifier_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateMemberFunctionRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateMemberFunctionRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 189917 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgComprehension::copy ( SgCopyHelp& help) const
   {
     SgComprehension* result = NULL;

  // printf ("Copy SgComprehension = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgComprehension*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgComprehension*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgComprehension: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgComprehension*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: target_copy
     SgExpression* target_copy; 
  // case: not a listType for (using conditionalToCopyVariable)target
          if (get_target() != NULL) 
             { 
               target_copy = static_cast<SgExpression*>(help.copyAst(get_target())); 
             } 
            else 
             { 
               target_copy = NULL; 
             } 
  // Copy constructor parameter data member: iter_copy
     SgExpression* iter_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iter
          if (get_iter() != NULL) 
             { 
               iter_copy = static_cast<SgExpression*>(help.copyAst(get_iter())); 
             } 
            else 
             { 
               iter_copy = NULL; 
             } 
  // Copy constructor parameter data member: filters_copy
     SgExprListExp* filters_copy; 
  // case: not a listType for (using conditionalToCopyVariable)filters
          if (get_filters() != NULL) 
             { 
               filters_copy = static_cast<SgExprListExp*>(help.copyAst(get_filters())); 
             } 
            else 
             { 
               filters_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgComprehension(  startOfConstruct_copy, target_copy, iter_copy, filters_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)target
          if ( (target_copy != NULL) && (target_copy->get_parent() == NULL) && (isSgType(target_copy) == NULL) ) 
             { 
               target_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)iter
          if ( (iter_copy != NULL) && (iter_copy->get_parent() == NULL) && (isSgType(iter_copy) == NULL) ) 
             { 
               iter_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)filters
          if ( (filters_copy != NULL) && (filters_copy->get_parent() == NULL) && (isSgType(filters_copy) == NULL) ) 
             { 
               filters_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgComprehension = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgComprehension::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 190241 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgListComprehension::copy ( SgCopyHelp& help) const
   {
     SgListComprehension* result = NULL;

  // printf ("Copy SgListComprehension = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgListComprehension*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgListComprehension*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgListComprehension: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgListComprehension*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: element_copy
     SgExpression* element_copy; 
  // case: not a listType for (using conditionalToCopyVariable)element
          if (get_element() != NULL) 
             { 
               element_copy = static_cast<SgExpression*>(help.copyAst(get_element())); 
             } 
            else 
             { 
               element_copy = NULL; 
             } 
  // Copy constructor parameter data member: generators_copy
     SgExprListExp* generators_copy; 
  // case: not a listType for (using conditionalToCopyVariable)generators
          if (get_generators() != NULL) 
             { 
               generators_copy = static_cast<SgExprListExp*>(help.copyAst(get_generators())); 
             } 
            else 
             { 
               generators_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgListComprehension(  startOfConstruct_copy, element_copy, generators_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)element
          if ( (element_copy != NULL) && (element_copy->get_parent() == NULL) && (isSgType(element_copy) == NULL) ) 
             { 
               element_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)generators
          if ( (generators_copy != NULL) && (generators_copy->get_parent() == NULL) && (isSgType(generators_copy) == NULL) ) 
             { 
               generators_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgListComprehension = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgListComprehension::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 190549 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSetComprehension::copy ( SgCopyHelp& help) const
   {
     SgSetComprehension* result = NULL;

  // printf ("Copy SgSetComprehension = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgSetComprehension*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgSetComprehension*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgSetComprehension: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgSetComprehension*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: element_copy
     SgExpression* element_copy; 
  // case: not a listType for (using conditionalToCopyVariable)element
          if (get_element() != NULL) 
             { 
               element_copy = static_cast<SgExpression*>(help.copyAst(get_element())); 
             } 
            else 
             { 
               element_copy = NULL; 
             } 
  // Copy constructor parameter data member: generators_copy
     SgExprListExp* generators_copy; 
  // case: not a listType for (using conditionalToCopyVariable)generators
          if (get_generators() != NULL) 
             { 
               generators_copy = static_cast<SgExprListExp*>(help.copyAst(get_generators())); 
             } 
            else 
             { 
               generators_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgSetComprehension(  startOfConstruct_copy, element_copy, generators_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)element
          if ( (element_copy != NULL) && (element_copy->get_parent() == NULL) && (isSgType(element_copy) == NULL) ) 
             { 
               element_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)generators
          if ( (generators_copy != NULL) && (generators_copy->get_parent() == NULL) && (isSgType(generators_copy) == NULL) ) 
             { 
               generators_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgSetComprehension = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgSetComprehension::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 190857 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDictionaryComprehension::copy ( SgCopyHelp& help) const
   {
     SgDictionaryComprehension* result = NULL;

  // printf ("Copy SgDictionaryComprehension = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDictionaryComprehension*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDictionaryComprehension*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDictionaryComprehension: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDictionaryComprehension*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: element_copy
     SgKeyDatumPair* element_copy; 
  // case: not a listType for (using conditionalToCopyVariable)element
          if (get_element() != NULL) 
             { 
               element_copy = static_cast<SgKeyDatumPair*>(help.copyAst(get_element())); 
             } 
            else 
             { 
               element_copy = NULL; 
             } 
  // Copy constructor parameter data member: generators_copy
     SgExprListExp* generators_copy; 
  // case: not a listType for (using conditionalToCopyVariable)generators
          if (get_generators() != NULL) 
             { 
               generators_copy = static_cast<SgExprListExp*>(help.copyAst(get_generators())); 
             } 
            else 
             { 
               generators_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDictionaryComprehension(  startOfConstruct_copy, element_copy, generators_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)element
          if ( (element_copy != NULL) && (element_copy->get_parent() == NULL) && (isSgType(element_copy) == NULL) ) 
             { 
               element_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)generators
          if ( (generators_copy != NULL) && (generators_copy->get_parent() == NULL) && (isSgType(generators_copy) == NULL) ) 
             { 
               generators_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDictionaryComprehension = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDictionaryComprehension::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 191165 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNaryOp::copy ( SgCopyHelp& help) const
   {
     SgNaryOp* result = NULL;

  // printf ("Copy SgNaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNaryOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNaryOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNaryOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNaryOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNaryOp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operands
     SgExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgExpressionPtrList::const_iterator source_operands_iterator = get_operands().begin(); 
     for ( /* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) 
        { 
          SgExpression* source_list_element = *source_operands_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          operands_copy.push_back(copy_list_element); 
        } 
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgExpressionPtrList::const_iterator operands_iterator = result->get_operands().begin(); 
     for ( /* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) 
        { 
          SgExpression* list_element = *operands_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_operators
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operators
  // case: toBeCopied == COPY_DATA for operators
     VariantTList operators_copy = p_operators; 
     result->p_operators = operators_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNaryOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNaryOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 191477 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNaryBooleanOp::copy ( SgCopyHelp& help) const
   {
     SgNaryBooleanOp* result = NULL;

  // printf ("Copy SgNaryBooleanOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNaryBooleanOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNaryBooleanOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNaryBooleanOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNaryBooleanOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNaryBooleanOp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operands
     SgExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgExpressionPtrList::const_iterator source_operands_iterator = get_operands().begin(); 
     for ( /* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) 
        { 
          SgExpression* source_list_element = *source_operands_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          operands_copy.push_back(copy_list_element); 
        } 
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgExpressionPtrList::const_iterator operands_iterator = result->get_operands().begin(); 
     for ( /* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) 
        { 
          SgExpression* list_element = *operands_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_operators
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operators
  // case: toBeCopied == COPY_DATA for operators
     VariantTList operators_copy = p_operators; 
     result->p_operators = operators_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNaryBooleanOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNaryBooleanOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 191789 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNaryComparisonOp::copy ( SgCopyHelp& help) const
   {
     SgNaryComparisonOp* result = NULL;

  // printf ("Copy SgNaryComparisonOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNaryComparisonOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNaryComparisonOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNaryComparisonOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNaryComparisonOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNaryComparisonOp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operands
     SgExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgExpressionPtrList::const_iterator source_operands_iterator = get_operands().begin(); 
     for ( /* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) 
        { 
          SgExpression* source_list_element = *source_operands_iterator; 
          SgExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          operands_copy.push_back(copy_list_element); 
        } 
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgExpressionPtrList::const_iterator operands_iterator = result->get_operands().begin(); 
     for ( /* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) 
        { 
          SgExpression* list_element = *operands_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_operators
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operators
  // case: toBeCopied == COPY_DATA for operators
     VariantTList operators_copy = p_operators; 
     result->p_operators = operators_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNaryComparisonOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNaryComparisonOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 192101 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStringConversion::copy ( SgCopyHelp& help) const
   {
     SgStringConversion* result = NULL;

  // printf ("Copy SgStringConversion = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgStringConversion*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgStringConversion*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgStringConversion: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgStringConversion*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgStringConversion(  startOfConstruct_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgStringConversion = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgStringConversion::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 192393 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgYieldExpression::copy ( SgCopyHelp& help) const
   {
     SgYieldExpression* result = NULL;

  // printf ("Copy SgYieldExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgYieldExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgYieldExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgYieldExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgYieldExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != NULL) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgYieldExpression(  startOfConstruct_copy, value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)value
          if ( (value_copy != NULL) && (value_copy->get_parent() == NULL) && (isSgType(value_copy) == NULL) ) 
             { 
               value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgYieldExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgYieldExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 192685 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAlignOfOp::copy ( SgCopyHelp& help) const
   {
     SgAlignOfOp* result = NULL;

  // printf ("Copy SgAlignOfOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAlignOfOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAlignOfOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAlignOfOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAlignOfOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != NULL) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = NULL; 
             } 
  // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAlignOfOp(  startOfConstruct_copy, operand_expr_copy, operand_type_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): alignOfContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for alignOfContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for alignOfContainsBaseTypeDefiningDeclaration
     bool alignOfContainsBaseTypeDefiningDeclaration_copy = p_alignOfContainsBaseTypeDefiningDeclaration; 
     result->p_alignOfContainsBaseTypeDefiningDeclaration = alignOfContainsBaseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
          if ( (operand_expr_copy != NULL) && (operand_expr_copy->get_parent() == NULL) && (isSgType(operand_expr_copy) == NULL) ) 
             { 
               operand_expr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAlignOfOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAlignOfOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 193005 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRangeExp::copy ( SgCopyHelp& help) const
   {
     SgRangeExp* result = NULL;

  // printf ("Copy SgRangeExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRangeExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRangeExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRangeExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRangeExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRangeExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): start_copy
     SgExpression* start_copy; 
  // case: not a listType for (using conditionalToCopyVariable)start
          if (get_start() != NULL) 
             { 
               start_copy = static_cast<SgExpression*>(help.copyAst(get_start())); 
             } 
            else 
             { 
               start_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_start != NULL) delete result->p_start; 
     result->p_start = start_copy; 
  // case: not a listType for (using conditionalToSetParent)start
          if ( (start_copy != NULL) && (start_copy->get_parent() == NULL) && (isSgType(start_copy) == NULL) ) 
             { 
               start_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): end_copy
     SgExpression* end_copy; 
  // case: not a listType for (using conditionalToCopyVariable)end
          if (get_end() != NULL) 
             { 
               end_copy = static_cast<SgExpression*>(help.copyAst(get_end())); 
             } 
            else 
             { 
               end_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_end != NULL) delete result->p_end; 
     result->p_end = end_copy; 
  // case: not a listType for (using conditionalToSetParent)end
          if ( (end_copy != NULL) && (end_copy->get_parent() == NULL) && (isSgType(end_copy) == NULL) ) 
             { 
               end_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): stride_copy
     SgExpression* stride_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stride
          if (get_stride() != NULL) 
             { 
               stride_copy = static_cast<SgExpression*>(help.copyAst(get_stride())); 
             } 
            else 
             { 
               stride_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stride != NULL) delete result->p_stride; 
     result->p_stride = stride_copy; 
  // case: not a listType for (using conditionalToSetParent)stride
          if ( (stride_copy != NULL) && (stride_copy->get_parent() == NULL) && (isSgType(stride_copy) == NULL) ) 
             { 
               stride_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRangeExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRangeExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 193338 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMagicColonExp::copy ( SgCopyHelp& help) const
   {
     SgMagicColonExp* result = NULL;

  // printf ("Copy SgMagicColonExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMagicColonExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMagicColonExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMagicColonExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMagicColonExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMagicColonExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMagicColonExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMagicColonExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 193614 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeTraitBuiltinOperator::copy ( SgCopyHelp& help) const
   {
     SgTypeTraitBuiltinOperator* result = NULL;

  // printf ("Copy SgTypeTraitBuiltinOperator = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeTraitBuiltinOperator*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeTraitBuiltinOperator*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeTraitBuiltinOperator: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeTraitBuiltinOperator*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeTraitBuiltinOperator(  startOfConstruct_copy, name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_args()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for args
  // case: toBeCopied == COPY_DATA for args
     SgNodePtrList args_copy = p_args; 
     result->p_args = args_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeTraitBuiltinOperator = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeTraitBuiltinOperator::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 193899 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCompoundLiteralExp::copy ( SgCopyHelp& help) const
   {
     SgCompoundLiteralExp* result = NULL;

  // printf ("Copy SgCompoundLiteralExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCompoundLiteralExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCompoundLiteralExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCompoundLiteralExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCompoundLiteralExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgVariableSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCompoundLiteralExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCompoundLiteralExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCompoundLiteralExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 194179 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaAnnotation::copy ( SgCopyHelp& help) const
   {
     SgJavaAnnotation* result = NULL;

  // printf ("Copy SgJavaAnnotation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaAnnotation*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaAnnotation*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaAnnotation: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaAnnotation*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaAnnotation(  startOfConstruct_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaAnnotation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaAnnotation::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 194459 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaMarkerAnnotation::copy ( SgCopyHelp& help) const
   {
     SgJavaMarkerAnnotation* result = NULL;

  // printf ("Copy SgJavaMarkerAnnotation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaMarkerAnnotation*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaMarkerAnnotation*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaMarkerAnnotation: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaMarkerAnnotation*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaMarkerAnnotation(  startOfConstruct_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaMarkerAnnotation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaMarkerAnnotation::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 194739 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaSingleMemberAnnotation::copy ( SgCopyHelp& help) const
   {
     SgJavaSingleMemberAnnotation* result = NULL;

  // printf ("Copy SgJavaSingleMemberAnnotation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaSingleMemberAnnotation*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaSingleMemberAnnotation*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaSingleMemberAnnotation: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaSingleMemberAnnotation*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
  // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != NULL) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaSingleMemberAnnotation(  startOfConstruct_copy, expression_type_copy, value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)value
          if ( (value_copy != NULL) && (value_copy->get_parent() == NULL) && (isSgType(value_copy) == NULL) ) 
             { 
               value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaSingleMemberAnnotation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaSingleMemberAnnotation::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 195035 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaNormalAnnotation::copy ( SgCopyHelp& help) const
   {
     SgJavaNormalAnnotation* result = NULL;

  // printf ("Copy SgJavaNormalAnnotation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaNormalAnnotation*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaNormalAnnotation*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaNormalAnnotation: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaNormalAnnotation*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaNormalAnnotation(  startOfConstruct_copy, expression_type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_value_pair_list()
     SgJavaMemberValuePairPtrList value_pair_list_copy; 
  // case: listType for value_pair_list
  // case: listType (typeIsPointerToList == false) for value_pair_list
     SgJavaMemberValuePairPtrList::const_iterator source_value_pair_list_iterator = get_value_pair_list().begin(); 
     for ( /* empty by design */; source_value_pair_list_iterator != get_value_pair_list().end(); ++source_value_pair_list_iterator) 
        { 
          SgJavaMemberValuePair* source_list_element = *source_value_pair_list_iterator; 
          SgJavaMemberValuePair* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgJavaMemberValuePair*>(help.copyAst(*source_value_pair_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          value_pair_list_copy.push_back(copy_list_element); 
        } 
     result->p_value_pair_list = value_pair_list_copy; 
  // case: listType for value_pair_list
  // case: listType (typeIsPointerToList == false) for value_pair_list
     SgJavaMemberValuePairPtrList::const_iterator value_pair_list_iterator = result->get_value_pair_list().begin(); 
     for ( /* empty by design */; value_pair_list_iterator != result->get_value_pair_list().end(); ++value_pair_list_iterator) 
        { 
          SgJavaMemberValuePair* list_element = *value_pair_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaNormalAnnotation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaNormalAnnotation::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 195346 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaTypeExpression::copy ( SgCopyHelp& help) const
   {
     SgJavaTypeExpression* result = NULL;

  // printf ("Copy SgJavaTypeExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaTypeExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaTypeExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaTypeExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaTypeExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaTypeExpression(  startOfConstruct_copy, type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaTypeExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaTypeExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 195626 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeExpression::copy ( SgCopyHelp& help) const
   {
     SgTypeExpression* result = NULL;

  // printf ("Copy SgTypeExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeExpression(  startOfConstruct_copy, type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 195906 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassExp::copy ( SgCopyHelp& help) const
   {
     SgClassExp* result = NULL;

  // printf ("Copy SgClassExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClassExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClassExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClassExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClassExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: class_symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_symbol
  // case: toBeCopied == COPY_DATA for class_symbol
     SgClassSymbol* class_symbol_copy = p_class_symbol; 
  // Copy constructor parameter data member: pobj_class_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pobj_class
  // case: toBeCopied == COPY_DATA for pobj_class
     int pobj_class_copy = p_pobj_class; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClassExp(  startOfConstruct_copy, class_symbol_copy, pobj_class_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClassExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClassExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 196190 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionParameterRefExp::copy ( SgCopyHelp& help) const
   {
     SgFunctionParameterRefExp* result = NULL;

  // printf ("Copy SgFunctionParameterRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionParameterRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionParameterRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionParameterRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionParameterRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: parameter_number_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameter_number
  // case: toBeCopied == COPY_DATA for parameter_number
     int parameter_number_copy = p_parameter_number; 
  // Copy constructor parameter data member: parameter_levels_up_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameter_levels_up
  // case: toBeCopied == COPY_DATA for parameter_levels_up
     int parameter_levels_up_copy = p_parameter_levels_up; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionParameterRefExp(  startOfConstruct_copy, parameter_number_copy, parameter_levels_up_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): parameter_expression_copy
     SgExpression* parameter_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameter_expression
          if (get_parameter_expression() != NULL) 
             { 
               parameter_expression_copy = static_cast<SgExpression*>(help.copyAst(get_parameter_expression())); 
             } 
            else 
             { 
               parameter_expression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameter_expression != NULL) delete result->p_parameter_expression; 
     result->p_parameter_expression = parameter_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)parameter_expression
          if ( (parameter_expression_copy != NULL) && (parameter_expression_copy->get_parent() == NULL) && (isSgType(parameter_expression_copy) == NULL) ) 
             { 
               parameter_expression_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (access function): parameter_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameter_type
  // case: toBeCopied == COPY_DATA for parameter_type
     SgType* parameter_type_copy = p_parameter_type; 
     result->p_parameter_type = parameter_type_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionParameterRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionParameterRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 196498 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLambdaExp::copy ( SgCopyHelp& help) const
   {
     SgLambdaExp* result = NULL;

  // printf ("Copy SgLambdaExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLambdaExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLambdaExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLambdaExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLambdaExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: lambda_capture_list_copy
     SgLambdaCaptureList* lambda_capture_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lambda_capture_list
          if (get_lambda_capture_list() != NULL) 
             { 
               lambda_capture_list_copy = static_cast<SgLambdaCaptureList*>(help.copyAst(get_lambda_capture_list())); 
             } 
            else 
             { 
               lambda_capture_list_copy = NULL; 
             } 
  // Copy constructor parameter data member: lambda_closure_class_copy
     SgClassDeclaration* lambda_closure_class_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lambda_closure_class
          if (get_lambda_closure_class() != NULL) 
             { 
               lambda_closure_class_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_lambda_closure_class())); 
             } 
            else 
             { 
               lambda_closure_class_copy = NULL; 
             } 
  // Copy constructor parameter data member: lambda_function_copy
     SgFunctionDeclaration* lambda_function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lambda_function
          if (get_lambda_function() != NULL) 
             { 
               lambda_function_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_lambda_function())); 
             } 
            else 
             { 
               lambda_function_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLambdaExp(  startOfConstruct_copy, lambda_capture_list_copy, lambda_closure_class_copy, lambda_function_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): is_mutable_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_mutable
  // case: toBeCopied == COPY_DATA for is_mutable
     bool is_mutable_copy = p_is_mutable; 
     result->p_is_mutable = is_mutable_copy; 
  // Copy non-constructor parameter data member (access function): capture_default_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for capture_default
  // case: toBeCopied == COPY_DATA for capture_default
     bool capture_default_copy = p_capture_default; 
     result->p_capture_default = capture_default_copy; 
  // Copy non-constructor parameter data member (access function): default_is_by_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for default_is_by_reference
  // case: toBeCopied == COPY_DATA for default_is_by_reference
     bool default_is_by_reference_copy = p_default_is_by_reference; 
     result->p_default_is_by_reference = default_is_by_reference_copy; 
  // Copy non-constructor parameter data member (access function): explicit_return_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_return_type
  // case: toBeCopied == COPY_DATA for explicit_return_type
     bool explicit_return_type_copy = p_explicit_return_type; 
     result->p_explicit_return_type = explicit_return_type_copy; 
  // Copy non-constructor parameter data member (access function): has_parameter_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_parameter_decl
  // case: toBeCopied == COPY_DATA for has_parameter_decl
     bool has_parameter_decl_copy = p_has_parameter_decl; 
     result->p_has_parameter_decl = has_parameter_decl_copy; 
  // Copy non-constructor parameter data member (access function): is_device_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_device
  // case: toBeCopied == COPY_DATA for is_device
     bool is_device_copy = p_is_device; 
     result->p_is_device = is_device_copy; 
  // case: not a listType for (using conditionalToSetParent)lambda_capture_list
          if ( (lambda_capture_list_copy != NULL) && (lambda_capture_list_copy->get_parent() == NULL) && (isSgType(lambda_capture_list_copy) == NULL) ) 
             { 
               lambda_capture_list_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)lambda_closure_class
          if ( (lambda_closure_class_copy != NULL) && (lambda_closure_class_copy->get_parent() == NULL) && (isSgType(lambda_closure_class_copy) == NULL) ) 
             { 
               lambda_closure_class_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)lambda_function
          if ( (lambda_function_copy != NULL) && (lambda_function_copy->get_parent() == NULL) && (isSgType(lambda_function_copy) == NULL) ) 
             { 
               lambda_function_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLambdaExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLambdaExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 196852 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgHereExp::copy ( SgCopyHelp& help) const
   {
     SgHereExp* result = NULL;

  // printf ("Copy SgHereExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgHereExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgHereExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgHereExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgHereExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgHereExp(  startOfConstruct_copy, expression_type_copy, expression_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgHereExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgHereExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 197148 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAtExp::copy ( SgCopyHelp& help) const
   {
     SgAtExp* result = NULL;

  // printf ("Copy SgAtExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAtExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAtExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAtExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAtExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAtExp(  startOfConstruct_copy, expression_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAtExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAtExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 197456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFinishExp::copy ( SgCopyHelp& help) const
   {
     SgFinishExp* result = NULL;

  // printf ("Copy SgFinishExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFinishExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFinishExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFinishExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFinishExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != NULL) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = NULL; 
             } 
  // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFinishExp(  startOfConstruct_copy, expression_copy, body_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
          if ( (expression_copy != NULL) && (expression_copy->get_parent() == NULL) && (isSgType(expression_copy) == NULL) ) 
             { 
               expression_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFinishExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFinishExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 197764 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNoexceptOp::copy ( SgCopyHelp& help) const
   {
     SgNoexceptOp* result = NULL;

  // printf ("Copy SgNoexceptOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNoexceptOp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNoexceptOp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNoexceptOp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNoexceptOp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != NULL) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNoexceptOp(  startOfConstruct_copy, operand_expr_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
          if ( (operand_expr_copy != NULL) && (operand_expr_copy->get_parent() == NULL) && (isSgType(operand_expr_copy) == NULL) ) 
             { 
               operand_expr_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNoexceptOp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNoexceptOp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 198056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskRefExp::copy ( SgCopyHelp& help) const
   {
     SgAdaTaskRefExp* result = NULL;

  // printf ("Copy SgAdaTaskRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTaskRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTaskRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTaskRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTaskRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgAdaTaskSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTaskRefExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTaskRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTaskRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 198336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedRefExp::copy ( SgCopyHelp& help) const
   {
     SgAdaProtectedRefExp* result = NULL;

  // printf ("Copy SgAdaProtectedRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaProtectedRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaProtectedRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaProtectedRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaProtectedRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgAdaProtectedSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaProtectedRefExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaProtectedRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaProtectedRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 198616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFoldExpression::copy ( SgCopyHelp& help) const
   {
     SgFoldExpression* result = NULL;

  // printf ("Copy SgFoldExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFoldExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFoldExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFoldExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFoldExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: operands_copy
     SgExpression* operands_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operands
          if (get_operands() != NULL) 
             { 
               operands_copy = static_cast<SgExpression*>(help.copyAst(get_operands())); 
             } 
            else 
             { 
               operands_copy = NULL; 
             } 
  // Copy constructor parameter data member: operator_token_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operator_token
  // case: toBeCopied == COPY_DATA for operator_token
     std::string operator_token_copy = p_operator_token; 
  // Copy constructor parameter data member: is_left_associative_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_left_associative
  // case: toBeCopied == COPY_DATA for is_left_associative
     bool is_left_associative_copy = p_is_left_associative; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFoldExpression(  startOfConstruct_copy, operands_copy, operator_token_copy, is_left_associative_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)operands
          if ( (operands_copy != NULL) && (operands_copy->get_parent() == NULL) && (isSgType(operands_copy) == NULL) ) 
             { 
               operands_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFoldExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFoldExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 198916 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAwaitExpression::copy ( SgCopyHelp& help) const
   {
     SgAwaitExpression* result = NULL;

  // printf ("Copy SgAwaitExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAwaitExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAwaitExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAwaitExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAwaitExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != NULL) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAwaitExpression(  startOfConstruct_copy, value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)value
          if ( (value_copy != NULL) && (value_copy->get_parent() == NULL) && (isSgType(value_copy) == NULL) ) 
             { 
               value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAwaitExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAwaitExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 199208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgChooseExpression::copy ( SgCopyHelp& help) const
   {
     SgChooseExpression* result = NULL;

  // printf ("Copy SgChooseExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgChooseExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgChooseExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgChooseExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgChooseExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != NULL) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgChooseExpression(  startOfConstruct_copy, value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)value
          if ( (value_copy != NULL) && (value_copy->get_parent() == NULL) && (isSgType(value_copy) == NULL) ) 
             { 
               value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgChooseExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgChooseExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 199500 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAttributeExp::copy ( SgCopyHelp& help) const
   {
     SgAdaAttributeExp* result = NULL;

  // printf ("Copy SgAdaAttributeExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaAttributeExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaAttributeExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaAttributeExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaAttributeExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute
  // case: toBeCopied == COPY_DATA for attribute
     SgName attribute_copy = p_attribute; 
  // Copy constructor parameter data member: object_copy
     SgExpression* object_copy; 
  // case: not a listType for (using conditionalToCopyVariable)object
          if (get_object() != NULL) 
             { 
               object_copy = static_cast<SgExpression*>(help.copyAst(get_object())); 
             } 
            else 
             { 
               object_copy = NULL; 
             } 
  // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != NULL) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = NULL; 
             } 
  // Copy constructor parameter data member: attrType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attrType
  // case: toBeCopied == COPY_DATA for attrType
     SgType* attrType_copy = p_attrType; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaAttributeExp(  startOfConstruct_copy, attribute_copy, object_copy, args_copy, attrType_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)object
          if ( (object_copy != NULL) && (object_copy->get_parent() == NULL) && (isSgType(object_copy) == NULL) ) 
             { 
               object_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)args
          if ( (args_copy != NULL) && (args_copy->get_parent() == NULL) && (isSgType(args_copy) == NULL) ) 
             { 
               args_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaAttributeExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaAttributeExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 199816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialTablePresetExp::copy ( SgCopyHelp& help) const
   {
     SgJovialTablePresetExp* result = NULL;

  // printf ("Copy SgJovialTablePresetExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialTablePresetExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialTablePresetExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialTablePresetExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialTablePresetExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: preset_list_copy
     SgExprListExp* preset_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)preset_list
          if (get_preset_list() != NULL) 
             { 
               preset_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_preset_list())); 
             } 
            else 
             { 
               preset_list_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialTablePresetExp(  startOfConstruct_copy, preset_list_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)preset_list
          if ( (preset_list_copy != NULL) && (preset_list_copy->get_parent() == NULL) && (isSgType(preset_list_copy) == NULL) ) 
             { 
               preset_list_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialTablePresetExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialTablePresetExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 200108 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialPresetPositionExp::copy ( SgCopyHelp& help) const
   {
     SgJovialPresetPositionExp* result = NULL;

  // printf ("Copy SgJovialPresetPositionExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJovialPresetPositionExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJovialPresetPositionExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJovialPresetPositionExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJovialPresetPositionExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: indices_copy
     SgExprListExp* indices_copy; 
  // case: not a listType for (using conditionalToCopyVariable)indices
          if (get_indices() != NULL) 
             { 
               indices_copy = static_cast<SgExprListExp*>(help.copyAst(get_indices())); 
             } 
            else 
             { 
               indices_copy = NULL; 
             } 
  // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != NULL) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJovialPresetPositionExp(  startOfConstruct_copy, indices_copy, value_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // case: not a listType for (using conditionalToSetParent)indices
          if ( (indices_copy != NULL) && (indices_copy->get_parent() == NULL) && (isSgType(indices_copy) == NULL) ) 
             { 
               indices_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)value
          if ( (value_copy != NULL) && (value_copy->get_parent() == NULL) && (isSgType(value_copy) == NULL) ) 
             { 
               value_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJovialPresetPositionExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJovialPresetPositionExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 200416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaOthersExp::copy ( SgCopyHelp& help) const
   {
     SgAdaOthersExp* result = NULL;

  // printf ("Copy SgAdaOthersExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaOthersExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaOthersExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaOthersExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaOthersExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaOthersExp(  startOfConstruct_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaOthersExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaOthersExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 200692 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRenamingRefExp::copy ( SgCopyHelp& help) const
   {
     SgAdaRenamingRefExp* result = NULL;

  // printf ("Copy SgAdaRenamingRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaRenamingRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaRenamingRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaRenamingRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaRenamingRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgAdaRenamingSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaRenamingRefExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaRenamingRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaRenamingRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 200972 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaUnitRefExp::copy ( SgCopyHelp& help) const
   {
     SgAdaUnitRefExp* result = NULL;

  // printf ("Copy SgAdaUnitRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaUnitRefExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaUnitRefExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaUnitRefExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaUnitRefExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = NULL; 
     if ( p_startOfConstruct != NULL ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = NULL; 
        } 
  // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaUnitRefExp(  startOfConstruct_copy, symbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = NULL; 
     if ( p_endOfConstruct != NULL ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != NULL) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = NULL; 
     if ( p_attachedPreprocessingInfoPtr != NULL ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != NULL) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = NULL; 
     if ( p_operatorPosition != NULL ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != NULL) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaUnitRefExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaUnitRefExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 201252 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariableSymbol::copy ( SgCopyHelp& help) const
   {
     SgVariableSymbol* result = NULL;

  // printf ("Copy SgVariableSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgVariableSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgVariableSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgVariableSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgVariableSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgVariableSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgVariableSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgVariableSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 201457 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateVariableSymbol::copy ( SgCopyHelp& help) const
   {
     SgTemplateVariableSymbol* result = NULL;

  // printf ("Copy SgTemplateVariableSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateVariableSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateVariableSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateVariableSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateVariableSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateVariableSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateVariableSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateVariableSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 201662 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealSymbol::copy ( SgCopyHelp& help) const
   {
     SgNonrealSymbol* result = NULL;

  // printf ("Copy SgNonrealSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNonrealSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNonrealSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNonrealSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNonrealSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgNonrealDecl* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgNonrealDecl*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNonrealSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNonrealSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNonrealSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 201879 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionSymbol::copy ( SgCopyHelp& help) const
   {
     SgFunctionSymbol* result = NULL;

  // printf ("Copy SgFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 202096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMemberFunctionSymbol::copy ( SgCopyHelp& help) const
   {
     SgMemberFunctionSymbol* result = NULL;

  // printf ("Copy SgMemberFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgMemberFunctionSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgMemberFunctionSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgMemberFunctionSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgMemberFunctionSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgMemberFunctionSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgMemberFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgMemberFunctionSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 202313 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateMemberFunctionSymbol::copy ( SgCopyHelp& help) const
   {
     SgTemplateMemberFunctionSymbol* result = NULL;

  // printf ("Copy SgTemplateMemberFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateMemberFunctionSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateMemberFunctionSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateMemberFunctionSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateMemberFunctionSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateMemberFunctionSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateMemberFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateMemberFunctionSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 202530 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateFunctionSymbol::copy ( SgCopyHelp& help) const
   {
     SgTemplateFunctionSymbol* result = NULL;

  // printf ("Copy SgTemplateFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateFunctionSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateFunctionSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateFunctionSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateFunctionSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateFunctionSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateFunctionSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 202747 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRenameSymbol::copy ( SgCopyHelp& help) const
   {
     SgRenameSymbol* result = NULL;

  // printf ("Copy SgRenameSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgRenameSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgRenameSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgRenameSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgRenameSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: original_symbol_copy
     SgSymbol* original_symbol_copy; 
  // case: not a listType for (using conditionalToCopyVariable)original_symbol
          if (get_original_symbol() != NULL) 
             { 
               original_symbol_copy = static_cast<SgSymbol*>(help.copyAst(get_original_symbol())); 
             } 
            else 
             { 
               original_symbol_copy = NULL; 
             } 
  // Copy constructor parameter data member: new_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_name
  // case: toBeCopied == COPY_DATA for new_name
     SgName new_name_copy = p_new_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgRenameSymbol(  declaration_copy, original_symbol_copy, new_name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)original_symbol
          if ( (original_symbol_copy != NULL) && (original_symbol_copy->get_parent() == NULL) && (isSgType(original_symbol_copy) == NULL) ) 
             { 
               original_symbol_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgRenameSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgRenameSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 202984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaInheritedFunctionSymbol::copy ( SgCopyHelp& help) const
   {
     SgAdaInheritedFunctionSymbol* result = NULL;

  // printf ("Copy SgAdaInheritedFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaInheritedFunctionSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaInheritedFunctionSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaInheritedFunctionSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaInheritedFunctionSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: derivedFunctionType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for derivedFunctionType
  // case: toBeCopied == COPY_DATA for derivedFunctionType
     SgFunctionType* derivedFunctionType_copy = p_derivedFunctionType; 
  // Copy constructor parameter data member: associatedDerivedType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedDerivedType
  // case: toBeCopied == COPY_DATA for associatedDerivedType
     SgNamedType* associatedDerivedType_copy = p_associatedDerivedType; 
  // Copy constructor parameter data member: publiclyVisibleFunctionSymbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for publiclyVisibleFunctionSymbol
  // case: toBeCopied == COPY_DATA for publiclyVisibleFunctionSymbol
     SgFunctionSymbol* publiclyVisibleFunctionSymbol_copy = p_publiclyVisibleFunctionSymbol; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaInheritedFunctionSymbol(  declaration_copy, derivedFunctionType_copy, associatedDerivedType_copy, publiclyVisibleFunctionSymbol_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaInheritedFunctionSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaInheritedFunctionSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 203213 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionTypeSymbol::copy ( SgCopyHelp& help) const
   {
     SgFunctionTypeSymbol* result = NULL;

  // printf ("Copy SgFunctionTypeSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgFunctionTypeSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgFunctionTypeSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgFunctionTypeSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgFunctionTypeSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgFunctionTypeSymbol(  name_copy, type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgFunctionTypeSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgFunctionTypeSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 203422 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateSymbol::copy ( SgCopyHelp& help) const
   {
     SgTemplateSymbol* result = NULL;

  // printf ("Copy SgTemplateSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgTemplateDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgTemplateDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 203639 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumFieldSymbol::copy ( SgCopyHelp& help) const
   {
     SgEnumFieldSymbol* result = NULL;

  // printf ("Copy SgEnumFieldSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEnumFieldSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEnumFieldSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEnumFieldSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEnumFieldSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEnumFieldSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEnumFieldSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEnumFieldSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 203844 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSymbol::copy ( SgCopyHelp& help) const
   {
     SgTypeSymbol* result = NULL;

  // printf ("Copy SgTypeSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypeSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypeSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypeSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypeSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypeSymbol(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypeSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypeSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 204044 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassSymbol::copy ( SgCopyHelp& help) const
   {
     SgClassSymbol* result = NULL;

  // printf ("Copy SgClassSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgClassSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgClassSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgClassSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgClassSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgClassDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgClassSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgClassSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgClassSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 204261 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateClassSymbol::copy ( SgCopyHelp& help) const
   {
     SgTemplateClassSymbol* result = NULL;

  // printf ("Copy SgTemplateClassSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateClassSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateClassSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateClassSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateClassSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgClassDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateClassSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateClassSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateClassSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 204478 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypedefSymbol::copy ( SgCopyHelp& help) const
   {
     SgTypedefSymbol* result = NULL;

  // printf ("Copy SgTypedefSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTypedefSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTypedefSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTypedefSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTypedefSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgTypedefDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgTypedefDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTypedefSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTypedefSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTypedefSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 204695 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateTypedefSymbol::copy ( SgCopyHelp& help) const
   {
     SgTemplateTypedefSymbol* result = NULL;

  // printf ("Copy SgTemplateTypedefSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgTemplateTypedefSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgTemplateTypedefSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgTemplateTypedefSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgTemplateTypedefSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgTypedefDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgTypedefDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgTemplateTypedefSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgTemplateTypedefSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgTemplateTypedefSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 204912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumSymbol::copy ( SgCopyHelp& help) const
   {
     SgEnumSymbol* result = NULL;

  // printf ("Copy SgEnumSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgEnumSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgEnumSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgEnumSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgEnumSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgEnumDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgEnumDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgEnumSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgEnumSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgEnumSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 205129 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLabelSymbol::copy ( SgCopyHelp& help) const
   {
     SgLabelSymbol* result = NULL;

  // printf ("Copy SgLabelSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgLabelSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgLabelSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgLabelSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgLabelSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgLabelStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgLabelStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgLabelSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): fortran_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_statement
  // case: toBeCopied == COPY_DATA for fortran_statement
     SgStatement* fortran_statement_copy = p_fortran_statement; 
     result->p_fortran_statement = fortran_statement_copy; 
  // Copy non-constructor parameter data member (access function): fortran_alternate_return_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_alternate_return_parameter
  // case: toBeCopied == COPY_DATA for fortran_alternate_return_parameter
     SgInitializedName* fortran_alternate_return_parameter_copy = p_fortran_alternate_return_parameter; 
     result->p_fortran_alternate_return_parameter = fortran_alternate_return_parameter_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label_value
  // case: toBeCopied == COPY_DATA for numeric_label_value
     int numeric_label_value_copy = p_numeric_label_value; 
     result->p_numeric_label_value = numeric_label_value_copy; 
  // Copy non-constructor parameter data member (access function): label_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label_type
  // case: toBeCopied == COPY_DATA for label_type
     SgLabelSymbol::label_type_enum label_type_copy = p_label_type; 
     result->p_label_type = label_type_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgLabelSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgLabelSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 205366 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDefaultSymbol::copy ( SgCopyHelp& help) const
   {
     SgDefaultSymbol* result = NULL;

  // printf ("Copy SgDefaultSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgDefaultSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgDefaultSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgDefaultSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgDefaultSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgDefaultSymbol(  type_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgDefaultSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgDefaultSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 205571 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamespaceSymbol::copy ( SgCopyHelp& help) const
   {
     SgNamespaceSymbol* result = NULL;

  // printf ("Copy SgNamespaceSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgNamespaceSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgNamespaceSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgNamespaceSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgNamespaceSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
  // Copy constructor parameter data member: declaration_copy
     SgNamespaceDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgNamespaceDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: aliasDeclaration_copy
     SgNamespaceAliasDeclarationStatement* aliasDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)aliasDeclaration
          if (get_aliasDeclaration() != NULL) 
             { 
               aliasDeclaration_copy = static_cast<SgNamespaceAliasDeclarationStatement*>(help.copyAst(get_aliasDeclaration())); 
             } 
            else 
             { 
               aliasDeclaration_copy = NULL; 
             } 
  // Copy constructor parameter data member: isAlias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAlias
  // case: toBeCopied == COPY_DATA for isAlias
     bool isAlias_copy = p_isAlias; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgNamespaceSymbol(  name_copy, declaration_copy, aliasDeclaration_copy, isAlias_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 
  // case: not a listType for (using conditionalToSetParent)aliasDeclaration
          if ( (aliasDeclaration_copy != NULL) && (aliasDeclaration_copy->get_parent() == NULL) && (isSgType(aliasDeclaration_copy) == NULL) ) 
             { 
               aliasDeclaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgNamespaceSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgNamespaceSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 205812 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntrinsicSymbol::copy ( SgCopyHelp& help) const
   {
     SgIntrinsicSymbol* result = NULL;

  // printf ("Copy SgIntrinsicSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgIntrinsicSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgIntrinsicSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgIntrinsicSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgIntrinsicSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgIntrinsicSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgIntrinsicSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgIntrinsicSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 206017 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModuleSymbol::copy ( SgCopyHelp& help) const
   {
     SgModuleSymbol* result = NULL;

  // printf ("Copy SgModuleSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgModuleSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgModuleSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgModuleSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgModuleSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgModuleStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgModuleStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgModuleSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgModuleSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgModuleSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 206234 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInterfaceSymbol::copy ( SgCopyHelp& help) const
   {
     SgInterfaceSymbol* result = NULL;

  // printf ("Copy SgInterfaceSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgInterfaceSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgInterfaceSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgInterfaceSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgInterfaceSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInterfaceStatement* declaration_copy = p_declaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgInterfaceSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgInterfaceSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgInterfaceSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 206439 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCommonSymbol::copy ( SgCopyHelp& help) const
   {
     SgCommonSymbol* result = NULL;

  // printf ("Copy SgCommonSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgCommonSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgCommonSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgCommonSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgCommonSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgCommonSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgCommonSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgCommonSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 206644 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAliasSymbol::copy ( SgCopyHelp& help) const
   {
     SgAliasSymbol* result = NULL;

  // printf ("Copy SgAliasSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAliasSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAliasSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAliasSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAliasSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: alias_copy
     SgSymbol* alias_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alias
          if (get_alias() != NULL) 
             { 
               alias_copy = static_cast<SgSymbol*>(help.copyAst(get_alias())); 
             } 
            else 
             { 
               alias_copy = NULL; 
             } 
  // Copy constructor parameter data member: isRenamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRenamed
  // case: toBeCopied == COPY_DATA for isRenamed
     bool isRenamed_copy = p_isRenamed; 
  // Copy constructor parameter data member: new_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_name
  // case: toBeCopied == COPY_DATA for new_name
     SgName new_name_copy = p_new_name; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAliasSymbol(  alias_copy, isRenamed_copy, new_name_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (list access function): result->get_causal_nodes()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for causal_nodes
  // case: toBeCopied == COPY_DATA for causal_nodes
     SgNodePtrList causal_nodes_copy = p_causal_nodes; 
     result->p_causal_nodes = causal_nodes_copy; 
  // case: not a listType for (using conditionalToSetParent)alias
          if ( (alias_copy != NULL) && (alias_copy->get_parent() == NULL) && (isSgType(alias_copy) == NULL) ) 
             { 
               alias_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAliasSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAliasSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 206874 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryAddressSymbol::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryAddressSymbol* result = NULL;

  // printf ("Copy SgAsmBinaryAddressSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryAddressSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryAddressSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryAddressSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryAddressSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: address_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address_name
  // case: toBeCopied == COPY_DATA for address_name
     SgName address_name_copy = p_address_name; 
  // Copy constructor parameter data member: address_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     SgAsmInstruction* address_copy = p_address; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryAddressSymbol(  address_name_copy, address_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryAddressSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryAddressSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 207083 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryDataSymbol::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryDataSymbol* result = NULL;

  // printf ("Copy SgAsmBinaryDataSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryDataSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryDataSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryDataSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryDataSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: variable_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variable_name
  // case: toBeCopied == COPY_DATA for variable_name
     SgName variable_name_copy = p_variable_name; 
  // Copy constructor parameter data member: address_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     SgAsmInstruction* address_copy = p_address; 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryDataSymbol(  variable_name_copy, address_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryDataSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryDataSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 207292 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaLabelSymbol::copy ( SgCopyHelp& help) const
   {
     SgJavaLabelSymbol* result = NULL;

  // printf ("Copy SgJavaLabelSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgJavaLabelSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgJavaLabelSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgJavaLabelSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgJavaLabelSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgJavaLabelStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgJavaLabelStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgJavaLabelSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgJavaLabelSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgJavaLabelSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 207509 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageSymbol::copy ( SgCopyHelp& help) const
   {
     SgAdaPackageSymbol* result = NULL;

  // printf ("Copy SgAdaPackageSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaPackageSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaPackageSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaPackageSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaPackageSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaPackageSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaPackageSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaPackageSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 207726 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskSymbol::copy ( SgCopyHelp& help) const
   {
     SgAdaTaskSymbol* result = NULL;

  // printf ("Copy SgAdaTaskSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaTaskSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaTaskSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaTaskSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaTaskSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaTaskSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaTaskSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaTaskSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 207943 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedSymbol::copy ( SgCopyHelp& help) const
   {
     SgAdaProtectedSymbol* result = NULL;

  // printf ("Copy SgAdaProtectedSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaProtectedSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaProtectedSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaProtectedSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaProtectedSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaProtectedSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaProtectedSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaProtectedSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 208160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRenamingSymbol::copy ( SgCopyHelp& help) const
   {
     SgAdaRenamingSymbol* result = NULL;

  // printf ("Copy SgAdaRenamingSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaRenamingSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaRenamingSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaRenamingSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaRenamingSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgAdaRenamingDecl* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgAdaRenamingDecl*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaRenamingSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaRenamingSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaRenamingSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 208377 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericSymbol::copy ( SgCopyHelp& help) const
   {
     SgAdaGenericSymbol* result = NULL;

  // printf ("Copy SgAdaGenericSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaGenericSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaGenericSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaGenericSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaGenericSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaGenericSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaGenericSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaGenericSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 208594 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericInstanceSymbol::copy ( SgCopyHelp& help) const
   {
     SgAdaGenericInstanceSymbol* result = NULL;

  // printf ("Copy SgAdaGenericInstanceSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaGenericInstanceSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaGenericInstanceSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaGenericInstanceSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaGenericInstanceSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgAdaGenericInstanceDecl* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgAdaGenericInstanceDecl*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaGenericInstanceSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaGenericInstanceSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaGenericInstanceSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 208811 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFormalPackageSymbol::copy ( SgCopyHelp& help) const
   {
     SgAdaFormalPackageSymbol* result = NULL;

  // printf ("Copy SgAdaFormalPackageSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAdaFormalPackageSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAdaFormalPackageSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAdaFormalPackageSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAdaFormalPackageSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
  // Copy constructor parameter data member: declaration_copy
     SgAdaFormalPackageDecl* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != NULL) 
             { 
               declaration_copy = static_cast<SgAdaFormalPackageDecl*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = NULL; 
             } 
 
/* #line 35 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAdaFormalPackageSymbol(  declaration_copy );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = NULL; 
     if ( p_attributeMechanism != NULL ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = NULL; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != NULL) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
          if ( (declaration_copy != NULL) && (declaration_copy->get_parent() == NULL) && (isSgType(declaration_copy) == NULL) ) 
             { 
               declaration_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAdaFormalPackageSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAdaFormalPackageSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 209028 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNode::copy ( SgCopyHelp& help) const
   {
     SgAsmNode* result = NULL;

  // printf ("Copy SgAsmNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNode*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNode*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNode: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNode*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNode(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNode::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 209211 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilNode::copy ( SgCopyHelp& help) const
   {
     SgAsmCilNode* result = NULL;

  // printf ("Copy SgAsmCilNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilNode*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilNode*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilNode: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilNode*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilNode(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilNode::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 209394 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilDataStream::copy ( SgCopyHelp& help) const
   {
     SgAsmCilDataStream* result = NULL;

  // printf ("Copy SgAsmCilDataStream = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilDataStream*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilDataStream*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilDataStream: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilDataStream*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilDataStream(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilDataStream = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilDataStream::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 209598 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilErrorHeap::copy ( SgCopyHelp& help) const
   {
     SgAsmCilErrorHeap* result = NULL;

  // printf ("Copy SgAsmCilErrorHeap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilErrorHeap*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilErrorHeap*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilErrorHeap: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilErrorHeap*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilErrorHeap(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilErrorHeap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilErrorHeap::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 209802 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMetadataHeap::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMetadataHeap* result = NULL;

  // printf ("Copy SgAsmCilMetadataHeap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMetadataHeap*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMetadataHeap*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMetadataHeap: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMetadataHeap*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMetadataHeap(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ReservedAlwaysZero
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ReservedAlwaysZero
  // case: toBeCopied == COPY_DATA for ReservedAlwaysZero
     uint32_t ReservedAlwaysZero_copy = p_ReservedAlwaysZero; 
     result->p_ReservedAlwaysZero = ReservedAlwaysZero_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MajorVersion
  // case: toBeCopied == COPY_DATA for MajorVersion
     uint8_t MajorVersion_copy = p_MajorVersion; 
     result->p_MajorVersion = MajorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MinorVersion
  // case: toBeCopied == COPY_DATA for MinorVersion
     uint8_t MinorVersion_copy = p_MinorVersion; 
     result->p_MinorVersion = MinorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_HeapSizes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for HeapSizes
  // case: toBeCopied == COPY_DATA for HeapSizes
     uint8_t HeapSizes_copy = p_HeapSizes; 
     result->p_HeapSizes = HeapSizes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ReservedAlwaysOne
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ReservedAlwaysOne
  // case: toBeCopied == COPY_DATA for ReservedAlwaysOne
     uint8_t ReservedAlwaysOne_copy = p_ReservedAlwaysOne; 
     result->p_ReservedAlwaysOne = ReservedAlwaysOne_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Valid
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Valid
  // case: toBeCopied == COPY_DATA for Valid
     uint64_t Valid_copy = p_Valid; 
     result->p_Valid = Valid_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Sorted
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Sorted
  // case: toBeCopied == COPY_DATA for Sorted
     uint64_t Sorted_copy = p_Sorted; 
     result->p_Sorted = Sorted_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_NumberOfRows
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NumberOfRows
  // case: toBeCopied == COPY_DATA for NumberOfRows
     std::vector<uint32_t> NumberOfRows_copy = p_NumberOfRows; 
     result->p_NumberOfRows = NumberOfRows_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_DataSizeFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for DataSizeFlags
  // case: toBeCopied == COPY_DATA for DataSizeFlags
     uint64_t DataSizeFlags_copy = p_DataSizeFlags; 
     result->p_DataSizeFlags = DataSizeFlags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_AssemblyTable
     SgAsmCilAssemblyTable* AssemblyTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyTable
          if (get_AssemblyTable() != NULL) 
             { 
               AssemblyTable_copy = static_cast<SgAsmCilAssemblyTable*>(help.copyAst(get_AssemblyTable())); 
             } 
            else 
             { 
               AssemblyTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyTable != NULL) delete result->p_AssemblyTable; 
     result->p_AssemblyTable = AssemblyTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyTable
          if ( (AssemblyTable_copy != NULL) && (AssemblyTable_copy->get_parent() == NULL) && (isSgType(AssemblyTable_copy) == NULL) ) 
             { 
               AssemblyTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_AssemblyOSTable
     SgAsmCilAssemblyOSTable* AssemblyOSTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyOSTable
          if (get_AssemblyOSTable() != NULL) 
             { 
               AssemblyOSTable_copy = static_cast<SgAsmCilAssemblyOSTable*>(help.copyAst(get_AssemblyOSTable())); 
             } 
            else 
             { 
               AssemblyOSTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyOSTable != NULL) delete result->p_AssemblyOSTable; 
     result->p_AssemblyOSTable = AssemblyOSTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyOSTable
          if ( (AssemblyOSTable_copy != NULL) && (AssemblyOSTable_copy->get_parent() == NULL) && (isSgType(AssemblyOSTable_copy) == NULL) ) 
             { 
               AssemblyOSTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_AssemblyProcessorTable
     SgAsmCilAssemblyProcessorTable* AssemblyProcessorTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyProcessorTable
          if (get_AssemblyProcessorTable() != NULL) 
             { 
               AssemblyProcessorTable_copy = static_cast<SgAsmCilAssemblyProcessorTable*>(help.copyAst(get_AssemblyProcessorTable())); 
             } 
            else 
             { 
               AssemblyProcessorTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyProcessorTable != NULL) delete result->p_AssemblyProcessorTable; 
     result->p_AssemblyProcessorTable = AssemblyProcessorTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyProcessorTable
          if ( (AssemblyProcessorTable_copy != NULL) && (AssemblyProcessorTable_copy->get_parent() == NULL) && (isSgType(AssemblyProcessorTable_copy) == NULL) ) 
             { 
               AssemblyProcessorTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_AssemblyRefTable
     SgAsmCilAssemblyRefTable* AssemblyRefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyRefTable
          if (get_AssemblyRefTable() != NULL) 
             { 
               AssemblyRefTable_copy = static_cast<SgAsmCilAssemblyRefTable*>(help.copyAst(get_AssemblyRefTable())); 
             } 
            else 
             { 
               AssemblyRefTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyRefTable != NULL) delete result->p_AssemblyRefTable; 
     result->p_AssemblyRefTable = AssemblyRefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyRefTable
          if ( (AssemblyRefTable_copy != NULL) && (AssemblyRefTable_copy->get_parent() == NULL) && (isSgType(AssemblyRefTable_copy) == NULL) ) 
             { 
               AssemblyRefTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_AssemblyRefOSTable
     SgAsmCilAssemblyRefOSTable* AssemblyRefOSTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyRefOSTable
          if (get_AssemblyRefOSTable() != NULL) 
             { 
               AssemblyRefOSTable_copy = static_cast<SgAsmCilAssemblyRefOSTable*>(help.copyAst(get_AssemblyRefOSTable())); 
             } 
            else 
             { 
               AssemblyRefOSTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyRefOSTable != NULL) delete result->p_AssemblyRefOSTable; 
     result->p_AssemblyRefOSTable = AssemblyRefOSTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyRefOSTable
          if ( (AssemblyRefOSTable_copy != NULL) && (AssemblyRefOSTable_copy->get_parent() == NULL) && (isSgType(AssemblyRefOSTable_copy) == NULL) ) 
             { 
               AssemblyRefOSTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_AssemblyRefProcessorTable
     SgAsmCilAssemblyRefProcessorTable* AssemblyRefProcessorTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyRefProcessorTable
          if (get_AssemblyRefProcessorTable() != NULL) 
             { 
               AssemblyRefProcessorTable_copy = static_cast<SgAsmCilAssemblyRefProcessorTable*>(help.copyAst(get_AssemblyRefProcessorTable())); 
             } 
            else 
             { 
               AssemblyRefProcessorTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyRefProcessorTable != NULL) delete result->p_AssemblyRefProcessorTable; 
     result->p_AssemblyRefProcessorTable = AssemblyRefProcessorTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyRefProcessorTable
          if ( (AssemblyRefProcessorTable_copy != NULL) && (AssemblyRefProcessorTable_copy->get_parent() == NULL) && (isSgType(AssemblyRefProcessorTable_copy) == NULL) ) 
             { 
               AssemblyRefProcessorTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ClassLayoutTable
     SgAsmCilClassLayoutTable* ClassLayoutTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ClassLayoutTable
          if (get_ClassLayoutTable() != NULL) 
             { 
               ClassLayoutTable_copy = static_cast<SgAsmCilClassLayoutTable*>(help.copyAst(get_ClassLayoutTable())); 
             } 
            else 
             { 
               ClassLayoutTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ClassLayoutTable != NULL) delete result->p_ClassLayoutTable; 
     result->p_ClassLayoutTable = ClassLayoutTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ClassLayoutTable
          if ( (ClassLayoutTable_copy != NULL) && (ClassLayoutTable_copy->get_parent() == NULL) && (isSgType(ClassLayoutTable_copy) == NULL) ) 
             { 
               ClassLayoutTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ConstantTable
     SgAsmCilConstantTable* ConstantTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ConstantTable
          if (get_ConstantTable() != NULL) 
             { 
               ConstantTable_copy = static_cast<SgAsmCilConstantTable*>(help.copyAst(get_ConstantTable())); 
             } 
            else 
             { 
               ConstantTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ConstantTable != NULL) delete result->p_ConstantTable; 
     result->p_ConstantTable = ConstantTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ConstantTable
          if ( (ConstantTable_copy != NULL) && (ConstantTable_copy->get_parent() == NULL) && (isSgType(ConstantTable_copy) == NULL) ) 
             { 
               ConstantTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_CustomAttributeTable
     SgAsmCilCustomAttributeTable* CustomAttributeTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)CustomAttributeTable
          if (get_CustomAttributeTable() != NULL) 
             { 
               CustomAttributeTable_copy = static_cast<SgAsmCilCustomAttributeTable*>(help.copyAst(get_CustomAttributeTable())); 
             } 
            else 
             { 
               CustomAttributeTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_CustomAttributeTable != NULL) delete result->p_CustomAttributeTable; 
     result->p_CustomAttributeTable = CustomAttributeTable_copy; 
  // case: not a listType for (using conditionalToSetParent)CustomAttributeTable
          if ( (CustomAttributeTable_copy != NULL) && (CustomAttributeTable_copy->get_parent() == NULL) && (isSgType(CustomAttributeTable_copy) == NULL) ) 
             { 
               CustomAttributeTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_DeclSecurityTable
     SgAsmCilDeclSecurityTable* DeclSecurityTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)DeclSecurityTable
          if (get_DeclSecurityTable() != NULL) 
             { 
               DeclSecurityTable_copy = static_cast<SgAsmCilDeclSecurityTable*>(help.copyAst(get_DeclSecurityTable())); 
             } 
            else 
             { 
               DeclSecurityTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_DeclSecurityTable != NULL) delete result->p_DeclSecurityTable; 
     result->p_DeclSecurityTable = DeclSecurityTable_copy; 
  // case: not a listType for (using conditionalToSetParent)DeclSecurityTable
          if ( (DeclSecurityTable_copy != NULL) && (DeclSecurityTable_copy->get_parent() == NULL) && (isSgType(DeclSecurityTable_copy) == NULL) ) 
             { 
               DeclSecurityTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_EventTable
     SgAsmCilEventTable* EventTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)EventTable
          if (get_EventTable() != NULL) 
             { 
               EventTable_copy = static_cast<SgAsmCilEventTable*>(help.copyAst(get_EventTable())); 
             } 
            else 
             { 
               EventTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_EventTable != NULL) delete result->p_EventTable; 
     result->p_EventTable = EventTable_copy; 
  // case: not a listType for (using conditionalToSetParent)EventTable
          if ( (EventTable_copy != NULL) && (EventTable_copy->get_parent() == NULL) && (isSgType(EventTable_copy) == NULL) ) 
             { 
               EventTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_EventMapTable
     SgAsmCilEventMapTable* EventMapTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)EventMapTable
          if (get_EventMapTable() != NULL) 
             { 
               EventMapTable_copy = static_cast<SgAsmCilEventMapTable*>(help.copyAst(get_EventMapTable())); 
             } 
            else 
             { 
               EventMapTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_EventMapTable != NULL) delete result->p_EventMapTable; 
     result->p_EventMapTable = EventMapTable_copy; 
  // case: not a listType for (using conditionalToSetParent)EventMapTable
          if ( (EventMapTable_copy != NULL) && (EventMapTable_copy->get_parent() == NULL) && (isSgType(EventMapTable_copy) == NULL) ) 
             { 
               EventMapTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ExportedTypeTable
     SgAsmCilExportedTypeTable* ExportedTypeTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ExportedTypeTable
          if (get_ExportedTypeTable() != NULL) 
             { 
               ExportedTypeTable_copy = static_cast<SgAsmCilExportedTypeTable*>(help.copyAst(get_ExportedTypeTable())); 
             } 
            else 
             { 
               ExportedTypeTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ExportedTypeTable != NULL) delete result->p_ExportedTypeTable; 
     result->p_ExportedTypeTable = ExportedTypeTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ExportedTypeTable
          if ( (ExportedTypeTable_copy != NULL) && (ExportedTypeTable_copy->get_parent() == NULL) && (isSgType(ExportedTypeTable_copy) == NULL) ) 
             { 
               ExportedTypeTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_FieldTable
     SgAsmCilFieldTable* FieldTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FieldTable
          if (get_FieldTable() != NULL) 
             { 
               FieldTable_copy = static_cast<SgAsmCilFieldTable*>(help.copyAst(get_FieldTable())); 
             } 
            else 
             { 
               FieldTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FieldTable != NULL) delete result->p_FieldTable; 
     result->p_FieldTable = FieldTable_copy; 
  // case: not a listType for (using conditionalToSetParent)FieldTable
          if ( (FieldTable_copy != NULL) && (FieldTable_copy->get_parent() == NULL) && (isSgType(FieldTable_copy) == NULL) ) 
             { 
               FieldTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_FieldLayoutTable
     SgAsmCilFieldLayoutTable* FieldLayoutTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FieldLayoutTable
          if (get_FieldLayoutTable() != NULL) 
             { 
               FieldLayoutTable_copy = static_cast<SgAsmCilFieldLayoutTable*>(help.copyAst(get_FieldLayoutTable())); 
             } 
            else 
             { 
               FieldLayoutTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FieldLayoutTable != NULL) delete result->p_FieldLayoutTable; 
     result->p_FieldLayoutTable = FieldLayoutTable_copy; 
  // case: not a listType for (using conditionalToSetParent)FieldLayoutTable
          if ( (FieldLayoutTable_copy != NULL) && (FieldLayoutTable_copy->get_parent() == NULL) && (isSgType(FieldLayoutTable_copy) == NULL) ) 
             { 
               FieldLayoutTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_FieldMarshalTable
     SgAsmCilFieldMarshalTable* FieldMarshalTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FieldMarshalTable
          if (get_FieldMarshalTable() != NULL) 
             { 
               FieldMarshalTable_copy = static_cast<SgAsmCilFieldMarshalTable*>(help.copyAst(get_FieldMarshalTable())); 
             } 
            else 
             { 
               FieldMarshalTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FieldMarshalTable != NULL) delete result->p_FieldMarshalTable; 
     result->p_FieldMarshalTable = FieldMarshalTable_copy; 
  // case: not a listType for (using conditionalToSetParent)FieldMarshalTable
          if ( (FieldMarshalTable_copy != NULL) && (FieldMarshalTable_copy->get_parent() == NULL) && (isSgType(FieldMarshalTable_copy) == NULL) ) 
             { 
               FieldMarshalTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_FieldRVATable
     SgAsmCilFieldRVATable* FieldRVATable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FieldRVATable
          if (get_FieldRVATable() != NULL) 
             { 
               FieldRVATable_copy = static_cast<SgAsmCilFieldRVATable*>(help.copyAst(get_FieldRVATable())); 
             } 
            else 
             { 
               FieldRVATable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FieldRVATable != NULL) delete result->p_FieldRVATable; 
     result->p_FieldRVATable = FieldRVATable_copy; 
  // case: not a listType for (using conditionalToSetParent)FieldRVATable
          if ( (FieldRVATable_copy != NULL) && (FieldRVATable_copy->get_parent() == NULL) && (isSgType(FieldRVATable_copy) == NULL) ) 
             { 
               FieldRVATable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_FileTable
     SgAsmCilFileTable* FileTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FileTable
          if (get_FileTable() != NULL) 
             { 
               FileTable_copy = static_cast<SgAsmCilFileTable*>(help.copyAst(get_FileTable())); 
             } 
            else 
             { 
               FileTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FileTable != NULL) delete result->p_FileTable; 
     result->p_FileTable = FileTable_copy; 
  // case: not a listType for (using conditionalToSetParent)FileTable
          if ( (FileTable_copy != NULL) && (FileTable_copy->get_parent() == NULL) && (isSgType(FileTable_copy) == NULL) ) 
             { 
               FileTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_GenericParamTable
     SgAsmCilGenericParamTable* GenericParamTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)GenericParamTable
          if (get_GenericParamTable() != NULL) 
             { 
               GenericParamTable_copy = static_cast<SgAsmCilGenericParamTable*>(help.copyAst(get_GenericParamTable())); 
             } 
            else 
             { 
               GenericParamTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_GenericParamTable != NULL) delete result->p_GenericParamTable; 
     result->p_GenericParamTable = GenericParamTable_copy; 
  // case: not a listType for (using conditionalToSetParent)GenericParamTable
          if ( (GenericParamTable_copy != NULL) && (GenericParamTable_copy->get_parent() == NULL) && (isSgType(GenericParamTable_copy) == NULL) ) 
             { 
               GenericParamTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_GenericParamConstraintTable
     SgAsmCilGenericParamConstraintTable* GenericParamConstraintTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)GenericParamConstraintTable
          if (get_GenericParamConstraintTable() != NULL) 
             { 
               GenericParamConstraintTable_copy = static_cast<SgAsmCilGenericParamConstraintTable*>(help.copyAst(get_GenericParamConstraintTable())); 
             } 
            else 
             { 
               GenericParamConstraintTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_GenericParamConstraintTable != NULL) delete result->p_GenericParamConstraintTable; 
     result->p_GenericParamConstraintTable = GenericParamConstraintTable_copy; 
  // case: not a listType for (using conditionalToSetParent)GenericParamConstraintTable
          if ( (GenericParamConstraintTable_copy != NULL) && (GenericParamConstraintTable_copy->get_parent() == NULL) && (isSgType(GenericParamConstraintTable_copy) == NULL) ) 
             { 
               GenericParamConstraintTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ImplMapTable
     SgAsmCilImplMapTable* ImplMapTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ImplMapTable
          if (get_ImplMapTable() != NULL) 
             { 
               ImplMapTable_copy = static_cast<SgAsmCilImplMapTable*>(help.copyAst(get_ImplMapTable())); 
             } 
            else 
             { 
               ImplMapTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ImplMapTable != NULL) delete result->p_ImplMapTable; 
     result->p_ImplMapTable = ImplMapTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ImplMapTable
          if ( (ImplMapTable_copy != NULL) && (ImplMapTable_copy->get_parent() == NULL) && (isSgType(ImplMapTable_copy) == NULL) ) 
             { 
               ImplMapTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_InterfaceImplTable
     SgAsmCilInterfaceImplTable* InterfaceImplTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)InterfaceImplTable
          if (get_InterfaceImplTable() != NULL) 
             { 
               InterfaceImplTable_copy = static_cast<SgAsmCilInterfaceImplTable*>(help.copyAst(get_InterfaceImplTable())); 
             } 
            else 
             { 
               InterfaceImplTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_InterfaceImplTable != NULL) delete result->p_InterfaceImplTable; 
     result->p_InterfaceImplTable = InterfaceImplTable_copy; 
  // case: not a listType for (using conditionalToSetParent)InterfaceImplTable
          if ( (InterfaceImplTable_copy != NULL) && (InterfaceImplTable_copy->get_parent() == NULL) && (isSgType(InterfaceImplTable_copy) == NULL) ) 
             { 
               InterfaceImplTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ManifestResourceTable
     SgAsmCilManifestResourceTable* ManifestResourceTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ManifestResourceTable
          if (get_ManifestResourceTable() != NULL) 
             { 
               ManifestResourceTable_copy = static_cast<SgAsmCilManifestResourceTable*>(help.copyAst(get_ManifestResourceTable())); 
             } 
            else 
             { 
               ManifestResourceTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ManifestResourceTable != NULL) delete result->p_ManifestResourceTable; 
     result->p_ManifestResourceTable = ManifestResourceTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ManifestResourceTable
          if ( (ManifestResourceTable_copy != NULL) && (ManifestResourceTable_copy->get_parent() == NULL) && (isSgType(ManifestResourceTable_copy) == NULL) ) 
             { 
               ManifestResourceTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_MemberRefTable
     SgAsmCilMemberRefTable* MemberRefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MemberRefTable
          if (get_MemberRefTable() != NULL) 
             { 
               MemberRefTable_copy = static_cast<SgAsmCilMemberRefTable*>(help.copyAst(get_MemberRefTable())); 
             } 
            else 
             { 
               MemberRefTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MemberRefTable != NULL) delete result->p_MemberRefTable; 
     result->p_MemberRefTable = MemberRefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MemberRefTable
          if ( (MemberRefTable_copy != NULL) && (MemberRefTable_copy->get_parent() == NULL) && (isSgType(MemberRefTable_copy) == NULL) ) 
             { 
               MemberRefTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_MethodDefTable
     SgAsmCilMethodDefTable* MethodDefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MethodDefTable
          if (get_MethodDefTable() != NULL) 
             { 
               MethodDefTable_copy = static_cast<SgAsmCilMethodDefTable*>(help.copyAst(get_MethodDefTable())); 
             } 
            else 
             { 
               MethodDefTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MethodDefTable != NULL) delete result->p_MethodDefTable; 
     result->p_MethodDefTable = MethodDefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MethodDefTable
          if ( (MethodDefTable_copy != NULL) && (MethodDefTable_copy->get_parent() == NULL) && (isSgType(MethodDefTable_copy) == NULL) ) 
             { 
               MethodDefTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_MethodImplTable
     SgAsmCilMethodImplTable* MethodImplTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MethodImplTable
          if (get_MethodImplTable() != NULL) 
             { 
               MethodImplTable_copy = static_cast<SgAsmCilMethodImplTable*>(help.copyAst(get_MethodImplTable())); 
             } 
            else 
             { 
               MethodImplTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MethodImplTable != NULL) delete result->p_MethodImplTable; 
     result->p_MethodImplTable = MethodImplTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MethodImplTable
          if ( (MethodImplTable_copy != NULL) && (MethodImplTable_copy->get_parent() == NULL) && (isSgType(MethodImplTable_copy) == NULL) ) 
             { 
               MethodImplTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_MethodSemanticsTable
     SgAsmCilMethodSemanticsTable* MethodSemanticsTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MethodSemanticsTable
          if (get_MethodSemanticsTable() != NULL) 
             { 
               MethodSemanticsTable_copy = static_cast<SgAsmCilMethodSemanticsTable*>(help.copyAst(get_MethodSemanticsTable())); 
             } 
            else 
             { 
               MethodSemanticsTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MethodSemanticsTable != NULL) delete result->p_MethodSemanticsTable; 
     result->p_MethodSemanticsTable = MethodSemanticsTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MethodSemanticsTable
          if ( (MethodSemanticsTable_copy != NULL) && (MethodSemanticsTable_copy->get_parent() == NULL) && (isSgType(MethodSemanticsTable_copy) == NULL) ) 
             { 
               MethodSemanticsTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_MethodSpecTable
     SgAsmCilMethodSpecTable* MethodSpecTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MethodSpecTable
          if (get_MethodSpecTable() != NULL) 
             { 
               MethodSpecTable_copy = static_cast<SgAsmCilMethodSpecTable*>(help.copyAst(get_MethodSpecTable())); 
             } 
            else 
             { 
               MethodSpecTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MethodSpecTable != NULL) delete result->p_MethodSpecTable; 
     result->p_MethodSpecTable = MethodSpecTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MethodSpecTable
          if ( (MethodSpecTable_copy != NULL) && (MethodSpecTable_copy->get_parent() == NULL) && (isSgType(MethodSpecTable_copy) == NULL) ) 
             { 
               MethodSpecTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ModuleTable
     SgAsmCilModuleTable* ModuleTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ModuleTable
          if (get_ModuleTable() != NULL) 
             { 
               ModuleTable_copy = static_cast<SgAsmCilModuleTable*>(help.copyAst(get_ModuleTable())); 
             } 
            else 
             { 
               ModuleTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ModuleTable != NULL) delete result->p_ModuleTable; 
     result->p_ModuleTable = ModuleTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ModuleTable
          if ( (ModuleTable_copy != NULL) && (ModuleTable_copy->get_parent() == NULL) && (isSgType(ModuleTable_copy) == NULL) ) 
             { 
               ModuleTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ModuleRefTable
     SgAsmCilModuleRefTable* ModuleRefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ModuleRefTable
          if (get_ModuleRefTable() != NULL) 
             { 
               ModuleRefTable_copy = static_cast<SgAsmCilModuleRefTable*>(help.copyAst(get_ModuleRefTable())); 
             } 
            else 
             { 
               ModuleRefTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ModuleRefTable != NULL) delete result->p_ModuleRefTable; 
     result->p_ModuleRefTable = ModuleRefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ModuleRefTable
          if ( (ModuleRefTable_copy != NULL) && (ModuleRefTable_copy->get_parent() == NULL) && (isSgType(ModuleRefTable_copy) == NULL) ) 
             { 
               ModuleRefTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_NestedClassTable
     SgAsmCilNestedClassTable* NestedClassTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)NestedClassTable
          if (get_NestedClassTable() != NULL) 
             { 
               NestedClassTable_copy = static_cast<SgAsmCilNestedClassTable*>(help.copyAst(get_NestedClassTable())); 
             } 
            else 
             { 
               NestedClassTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_NestedClassTable != NULL) delete result->p_NestedClassTable; 
     result->p_NestedClassTable = NestedClassTable_copy; 
  // case: not a listType for (using conditionalToSetParent)NestedClassTable
          if ( (NestedClassTable_copy != NULL) && (NestedClassTable_copy->get_parent() == NULL) && (isSgType(NestedClassTable_copy) == NULL) ) 
             { 
               NestedClassTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ParamTable
     SgAsmCilParamTable* ParamTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ParamTable
          if (get_ParamTable() != NULL) 
             { 
               ParamTable_copy = static_cast<SgAsmCilParamTable*>(help.copyAst(get_ParamTable())); 
             } 
            else 
             { 
               ParamTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ParamTable != NULL) delete result->p_ParamTable; 
     result->p_ParamTable = ParamTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ParamTable
          if ( (ParamTable_copy != NULL) && (ParamTable_copy->get_parent() == NULL) && (isSgType(ParamTable_copy) == NULL) ) 
             { 
               ParamTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_PropertyTable
     SgAsmCilPropertyTable* PropertyTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)PropertyTable
          if (get_PropertyTable() != NULL) 
             { 
               PropertyTable_copy = static_cast<SgAsmCilPropertyTable*>(help.copyAst(get_PropertyTable())); 
             } 
            else 
             { 
               PropertyTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_PropertyTable != NULL) delete result->p_PropertyTable; 
     result->p_PropertyTable = PropertyTable_copy; 
  // case: not a listType for (using conditionalToSetParent)PropertyTable
          if ( (PropertyTable_copy != NULL) && (PropertyTable_copy->get_parent() == NULL) && (isSgType(PropertyTable_copy) == NULL) ) 
             { 
               PropertyTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_PropertyMapTable
     SgAsmCilPropertyMapTable* PropertyMapTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)PropertyMapTable
          if (get_PropertyMapTable() != NULL) 
             { 
               PropertyMapTable_copy = static_cast<SgAsmCilPropertyMapTable*>(help.copyAst(get_PropertyMapTable())); 
             } 
            else 
             { 
               PropertyMapTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_PropertyMapTable != NULL) delete result->p_PropertyMapTable; 
     result->p_PropertyMapTable = PropertyMapTable_copy; 
  // case: not a listType for (using conditionalToSetParent)PropertyMapTable
          if ( (PropertyMapTable_copy != NULL) && (PropertyMapTable_copy->get_parent() == NULL) && (isSgType(PropertyMapTable_copy) == NULL) ) 
             { 
               PropertyMapTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_StandAloneSigTable
     SgAsmCilStandAloneSigTable* StandAloneSigTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)StandAloneSigTable
          if (get_StandAloneSigTable() != NULL) 
             { 
               StandAloneSigTable_copy = static_cast<SgAsmCilStandAloneSigTable*>(help.copyAst(get_StandAloneSigTable())); 
             } 
            else 
             { 
               StandAloneSigTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_StandAloneSigTable != NULL) delete result->p_StandAloneSigTable; 
     result->p_StandAloneSigTable = StandAloneSigTable_copy; 
  // case: not a listType for (using conditionalToSetParent)StandAloneSigTable
          if ( (StandAloneSigTable_copy != NULL) && (StandAloneSigTable_copy->get_parent() == NULL) && (isSgType(StandAloneSigTable_copy) == NULL) ) 
             { 
               StandAloneSigTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_TypeDefTable
     SgAsmCilTypeDefTable* TypeDefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)TypeDefTable
          if (get_TypeDefTable() != NULL) 
             { 
               TypeDefTable_copy = static_cast<SgAsmCilTypeDefTable*>(help.copyAst(get_TypeDefTable())); 
             } 
            else 
             { 
               TypeDefTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_TypeDefTable != NULL) delete result->p_TypeDefTable; 
     result->p_TypeDefTable = TypeDefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)TypeDefTable
          if ( (TypeDefTable_copy != NULL) && (TypeDefTable_copy->get_parent() == NULL) && (isSgType(TypeDefTable_copy) == NULL) ) 
             { 
               TypeDefTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_TypeRefTable
     SgAsmCilTypeRefTable* TypeRefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)TypeRefTable
          if (get_TypeRefTable() != NULL) 
             { 
               TypeRefTable_copy = static_cast<SgAsmCilTypeRefTable*>(help.copyAst(get_TypeRefTable())); 
             } 
            else 
             { 
               TypeRefTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_TypeRefTable != NULL) delete result->p_TypeRefTable; 
     result->p_TypeRefTable = TypeRefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)TypeRefTable
          if ( (TypeRefTable_copy != NULL) && (TypeRefTable_copy->get_parent() == NULL) && (isSgType(TypeRefTable_copy) == NULL) ) 
             { 
               TypeRefTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_TypeSpecTable
     SgAsmCilTypeSpecTable* TypeSpecTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)TypeSpecTable
          if (get_TypeSpecTable() != NULL) 
             { 
               TypeSpecTable_copy = static_cast<SgAsmCilTypeSpecTable*>(help.copyAst(get_TypeSpecTable())); 
             } 
            else 
             { 
               TypeSpecTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_TypeSpecTable != NULL) delete result->p_TypeSpecTable; 
     result->p_TypeSpecTable = TypeSpecTable_copy; 
  // case: not a listType for (using conditionalToSetParent)TypeSpecTable
          if ( (TypeSpecTable_copy != NULL) && (TypeSpecTable_copy->get_parent() == NULL) && (isSgType(TypeSpecTable_copy) == NULL) ) 
             { 
               TypeSpecTable_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMetadataHeap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMetadataHeap::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 210773 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilUint32Heap::copy ( SgCopyHelp& help) const
   {
     SgAsmCilUint32Heap* result = NULL;

  // printf ("Copy SgAsmCilUint32Heap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilUint32Heap*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilUint32Heap*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilUint32Heap: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilUint32Heap*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilUint32Heap(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Stream
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Stream
  // case: toBeCopied == COPY_DATA for Stream
     std::vector<uint32_t> Stream_copy = p_Stream; 
     result->p_Stream = Stream_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilUint32Heap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilUint32Heap::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 210982 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilUint8Heap::copy ( SgCopyHelp& help) const
   {
     SgAsmCilUint8Heap* result = NULL;

  // printf ("Copy SgAsmCilUint8Heap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilUint8Heap*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilUint8Heap*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilUint8Heap: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilUint8Heap*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilUint8Heap(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Stream
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Stream
  // case: toBeCopied == COPY_DATA for Stream
     std::vector<uint8_t> Stream_copy = p_Stream; 
     result->p_Stream = Stream_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilUint8Heap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilUint8Heap::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 211191 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilExceptionData::copy ( SgCopyHelp& help) const
   {
     SgAsmCilExceptionData* result = NULL;

  // printf ("Copy SgAsmCilExceptionData = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilExceptionData*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilExceptionData*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilExceptionData: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilExceptionData*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilExceptionData(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     std::uint32_t flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_tryOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tryOffset
  // case: toBeCopied == COPY_DATA for tryOffset
     std::uint32_t tryOffset_copy = p_tryOffset; 
     result->p_tryOffset = tryOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_tryLength
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tryLength
  // case: toBeCopied == COPY_DATA for tryLength
     std::uint32_t tryLength_copy = p_tryLength; 
     result->p_tryLength = tryLength_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_handlerOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for handlerOffset
  // case: toBeCopied == COPY_DATA for handlerOffset
     std::uint32_t handlerOffset_copy = p_handlerOffset; 
     result->p_handlerOffset = handlerOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_handlerLength
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for handlerLength
  // case: toBeCopied == COPY_DATA for handlerLength
     std::uint32_t handlerLength_copy = p_handlerLength; 
     result->p_handlerLength = handlerLength_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_classTokenOrFilterOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for classTokenOrFilterOffset
  // case: toBeCopied == COPY_DATA for classTokenOrFilterOffset
     std::uint32_t classTokenOrFilterOffset_copy = p_classTokenOrFilterOffset; 
     result->p_classTokenOrFilterOffset = classTokenOrFilterOffset_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilExceptionData = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilExceptionData::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 211405 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMetadata::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMetadata* result = NULL;

  // printf ("Copy SgAsmCilMetadata = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMetadata*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMetadata*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMetadata: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMetadata*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMetadata(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMetadata = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMetadata::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 211588 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssembly::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssembly* result = NULL;

  // printf ("Copy SgAsmCilAssembly = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssembly*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssembly*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssembly: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssembly*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssembly(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_HashAlgId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for HashAlgId
  // case: toBeCopied == COPY_DATA for HashAlgId
     uint32_t HashAlgId_copy = p_HashAlgId; 
     result->p_HashAlgId = HashAlgId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MajorVersion
  // case: toBeCopied == COPY_DATA for MajorVersion
     uint16_t MajorVersion_copy = p_MajorVersion; 
     result->p_MajorVersion = MajorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MinorVersion
  // case: toBeCopied == COPY_DATA for MinorVersion
     uint16_t MinorVersion_copy = p_MinorVersion; 
     result->p_MinorVersion = MinorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_BuildNumber
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for BuildNumber
  // case: toBeCopied == COPY_DATA for BuildNumber
     uint16_t BuildNumber_copy = p_BuildNumber; 
     result->p_BuildNumber = BuildNumber_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_RevisionNumber
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for RevisionNumber
  // case: toBeCopied == COPY_DATA for RevisionNumber
     uint16_t RevisionNumber_copy = p_RevisionNumber; 
     result->p_RevisionNumber = RevisionNumber_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_PublicKey
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PublicKey
  // case: toBeCopied == COPY_DATA for PublicKey
     uint32_t PublicKey_copy = p_PublicKey; 
     result->p_PublicKey = PublicKey_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Culture
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Culture
  // case: toBeCopied == COPY_DATA for Culture
     uint32_t Culture_copy = p_Culture; 
     result->p_Culture = Culture_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssembly = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssembly::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 211817 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyOS::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyOS* result = NULL;

  // printf ("Copy SgAsmCilAssemblyOS = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyOS*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyOS*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyOS: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyOS*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyOS(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_OSPlatformID
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSPlatformID
  // case: toBeCopied == COPY_DATA for OSPlatformID
     uint32_t OSPlatformID_copy = p_OSPlatformID; 
     result->p_OSPlatformID = OSPlatformID_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_OSMajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSMajorVersion
  // case: toBeCopied == COPY_DATA for OSMajorVersion
     uint32_t OSMajorVersion_copy = p_OSMajorVersion; 
     result->p_OSMajorVersion = OSMajorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_OSMinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSMinorVersion
  // case: toBeCopied == COPY_DATA for OSMinorVersion
     uint32_t OSMinorVersion_copy = p_OSMinorVersion; 
     result->p_OSMinorVersion = OSMinorVersion_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyOS = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyOS::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 212016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyOSTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyOSTable* result = NULL;

  // printf ("Copy SgAsmCilAssemblyOSTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyOSTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyOSTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyOSTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyOSTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyOSTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyOS*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyOS*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilAssemblyOS* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyOS* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyOS*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyOS*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilAssemblyOS* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyOSTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyOSTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 212231 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyProcessor::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyProcessor* result = NULL;

  // printf ("Copy SgAsmCilAssemblyProcessor = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyProcessor*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyProcessor*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyProcessor: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyProcessor*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyProcessor(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Processor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Processor
  // case: toBeCopied == COPY_DATA for Processor
     uint32_t Processor_copy = p_Processor; 
     result->p_Processor = Processor_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyProcessor = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyProcessor::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 212420 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyProcessorTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyProcessorTable* result = NULL;

  // printf ("Copy SgAsmCilAssemblyProcessorTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyProcessorTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyProcessorTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyProcessorTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyProcessorTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyProcessorTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyProcessor*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyProcessor*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilAssemblyProcessor* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyProcessor* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyProcessor*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyProcessor*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilAssemblyProcessor* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyProcessorTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyProcessorTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 212635 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRef::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyRef* result = NULL;

  // printf ("Copy SgAsmCilAssemblyRef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyRef*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyRef*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyRef: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyRef*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyRef(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_MajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MajorVersion
  // case: toBeCopied == COPY_DATA for MajorVersion
     uint16_t MajorVersion_copy = p_MajorVersion; 
     result->p_MajorVersion = MajorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MinorVersion
  // case: toBeCopied == COPY_DATA for MinorVersion
     uint16_t MinorVersion_copy = p_MinorVersion; 
     result->p_MinorVersion = MinorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_BuildNumber
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for BuildNumber
  // case: toBeCopied == COPY_DATA for BuildNumber
     uint16_t BuildNumber_copy = p_BuildNumber; 
     result->p_BuildNumber = BuildNumber_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_RevisionNumber
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for RevisionNumber
  // case: toBeCopied == COPY_DATA for RevisionNumber
     uint16_t RevisionNumber_copy = p_RevisionNumber; 
     result->p_RevisionNumber = RevisionNumber_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_PublicKeyOrToken
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PublicKeyOrToken
  // case: toBeCopied == COPY_DATA for PublicKeyOrToken
     uint32_t PublicKeyOrToken_copy = p_PublicKeyOrToken; 
     result->p_PublicKeyOrToken = PublicKeyOrToken_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Culture
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Culture
  // case: toBeCopied == COPY_DATA for Culture
     uint32_t Culture_copy = p_Culture; 
     result->p_Culture = Culture_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_HashValue
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for HashValue
  // case: toBeCopied == COPY_DATA for HashValue
     uint32_t HashValue_copy = p_HashValue; 
     result->p_HashValue = HashValue_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyRef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyRef::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 212864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefOS::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyRefOS* result = NULL;

  // printf ("Copy SgAsmCilAssemblyRefOS = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyRefOS*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyRefOS*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyRefOS: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyRefOS*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyRefOS(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_OSPlatformID
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSPlatformID
  // case: toBeCopied == COPY_DATA for OSPlatformID
     uint32_t OSPlatformID_copy = p_OSPlatformID; 
     result->p_OSPlatformID = OSPlatformID_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_OSMajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSMajorVersion
  // case: toBeCopied == COPY_DATA for OSMajorVersion
     uint32_t OSMajorVersion_copy = p_OSMajorVersion; 
     result->p_OSMajorVersion = OSMajorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_OSMinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSMinorVersion
  // case: toBeCopied == COPY_DATA for OSMinorVersion
     uint32_t OSMinorVersion_copy = p_OSMinorVersion; 
     result->p_OSMinorVersion = OSMinorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_AssemblyRefOS
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for AssemblyRefOS
  // case: toBeCopied == COPY_DATA for AssemblyRefOS
     uint32_t AssemblyRefOS_copy = p_AssemblyRefOS; 
     result->p_AssemblyRefOS = AssemblyRefOS_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyRefOS = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyRefOS::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 213068 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefOSTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyRefOSTable* result = NULL;

  // printf ("Copy SgAsmCilAssemblyRefOSTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyRefOSTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyRefOSTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyRefOSTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyRefOSTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyRefOSTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyRefOS*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyRefOS*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilAssemblyRefOS* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyRefOS* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyRefOS*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyRefOS*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilAssemblyRefOS* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyRefOSTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyRefOSTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 213283 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefProcessor::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyRefProcessor* result = NULL;

  // printf ("Copy SgAsmCilAssemblyRefProcessor = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyRefProcessor*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyRefProcessor*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyRefProcessor: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyRefProcessor*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyRefProcessor(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Processor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Processor
  // case: toBeCopied == COPY_DATA for Processor
     uint32_t Processor_copy = p_Processor; 
     result->p_Processor = Processor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_AssemblyRef
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for AssemblyRef
  // case: toBeCopied == COPY_DATA for AssemblyRef
     uint32_t AssemblyRef_copy = p_AssemblyRef; 
     result->p_AssemblyRef = AssemblyRef_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyRefProcessor = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyRefProcessor::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 213477 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefProcessorTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyRefProcessorTable* result = NULL;

  // printf ("Copy SgAsmCilAssemblyRefProcessorTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyRefProcessorTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyRefProcessorTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyRefProcessorTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyRefProcessorTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyRefProcessorTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyRefProcessor*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyRefProcessor*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilAssemblyRefProcessor* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyRefProcessor* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyRefProcessor*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyRefProcessor*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilAssemblyRefProcessor* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyRefProcessorTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyRefProcessorTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 213692 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyRefTable* result = NULL;

  // printf ("Copy SgAsmCilAssemblyRefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyRefTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyRefTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyRefTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyRefTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyRefTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyRef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyRef*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilAssemblyRef* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyRef* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyRef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssemblyRef*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilAssemblyRef* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyRefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyRefTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 213907 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilAssemblyTable* result = NULL;

  // printf ("Copy SgAsmCilAssemblyTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilAssemblyTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilAssemblyTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilAssemblyTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilAssemblyTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilAssemblyTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssembly*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssembly*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilAssembly* source_list_element = *source_elements_iterator; 
          SgAsmCilAssembly* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilAssembly*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilAssembly*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilAssembly* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilAssemblyTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilAssemblyTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 214122 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilClassLayout::copy ( SgCopyHelp& help) const
   {
     SgAsmCilClassLayout* result = NULL;

  // printf ("Copy SgAsmCilClassLayout = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilClassLayout*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilClassLayout*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilClassLayout: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilClassLayout*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilClassLayout(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_PackingSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PackingSize
  // case: toBeCopied == COPY_DATA for PackingSize
     uint16_t PackingSize_copy = p_PackingSize; 
     result->p_PackingSize = PackingSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ClassSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ClassSize
  // case: toBeCopied == COPY_DATA for ClassSize
     uint32_t ClassSize_copy = p_ClassSize; 
     result->p_ClassSize = ClassSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilClassLayout = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilClassLayout::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 214321 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilClassLayoutTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilClassLayoutTable* result = NULL;

  // printf ("Copy SgAsmCilClassLayoutTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilClassLayoutTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilClassLayoutTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilClassLayoutTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilClassLayoutTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilClassLayoutTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilClassLayout*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilClassLayout*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilClassLayout* source_list_element = *source_elements_iterator; 
          SgAsmCilClassLayout* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilClassLayout*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilClassLayout*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilClassLayout* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilClassLayoutTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilClassLayoutTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 214536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilConstant::copy ( SgCopyHelp& help) const
   {
     SgAsmCilConstant* result = NULL;

  // printf ("Copy SgAsmCilConstant = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilConstant*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilConstant*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilConstant: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilConstant*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilConstant(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Type
  // case: toBeCopied == COPY_DATA for Type
     uint8_t Type_copy = p_Type; 
     result->p_Type = Type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Padding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Padding
  // case: toBeCopied == COPY_DATA for Padding
     uint8_t Padding_copy = p_Padding; 
     result->p_Padding = Padding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Value
  // case: toBeCopied == COPY_DATA for Value
     uint32_t Value_copy = p_Value; 
     result->p_Value = Value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilConstant = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilConstant::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 214740 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilConstantTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilConstantTable* result = NULL;

  // printf ("Copy SgAsmCilConstantTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilConstantTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilConstantTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilConstantTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilConstantTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilConstantTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilConstant*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilConstant*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilConstant* source_list_element = *source_elements_iterator; 
          SgAsmCilConstant* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilConstant*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilConstant*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilConstant* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilConstantTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilConstantTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 214955 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilCustomAttribute::copy ( SgCopyHelp& help) const
   {
     SgAsmCilCustomAttribute* result = NULL;

  // printf ("Copy SgAsmCilCustomAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilCustomAttribute*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilCustomAttribute*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilCustomAttribute: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilCustomAttribute*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilCustomAttribute(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Type
  // case: toBeCopied == COPY_DATA for Type
     uint32_t Type_copy = p_Type; 
     result->p_Type = Type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Value
  // case: toBeCopied == COPY_DATA for Value
     uint32_t Value_copy = p_Value; 
     result->p_Value = Value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilCustomAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilCustomAttribute::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 215154 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilCustomAttributeTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilCustomAttributeTable* result = NULL;

  // printf ("Copy SgAsmCilCustomAttributeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilCustomAttributeTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilCustomAttributeTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilCustomAttributeTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilCustomAttributeTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilCustomAttributeTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilCustomAttribute*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilCustomAttribute*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilCustomAttribute* source_list_element = *source_elements_iterator; 
          SgAsmCilCustomAttribute* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilCustomAttribute*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilCustomAttribute*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilCustomAttribute* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilCustomAttributeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilCustomAttributeTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 215369 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilDeclSecurity::copy ( SgCopyHelp& help) const
   {
     SgAsmCilDeclSecurity* result = NULL;

  // printf ("Copy SgAsmCilDeclSecurity = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilDeclSecurity*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilDeclSecurity*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilDeclSecurity: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilDeclSecurity*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilDeclSecurity(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Action
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Action
  // case: toBeCopied == COPY_DATA for Action
     uint16_t Action_copy = p_Action; 
     result->p_Action = Action_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_PermissionSet
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PermissionSet
  // case: toBeCopied == COPY_DATA for PermissionSet
     uint32_t PermissionSet_copy = p_PermissionSet; 
     result->p_PermissionSet = PermissionSet_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilDeclSecurity = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilDeclSecurity::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 215568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilDeclSecurityTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilDeclSecurityTable* result = NULL;

  // printf ("Copy SgAsmCilDeclSecurityTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilDeclSecurityTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilDeclSecurityTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilDeclSecurityTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilDeclSecurityTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilDeclSecurityTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilDeclSecurity*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilDeclSecurity*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilDeclSecurity* source_list_element = *source_elements_iterator; 
          SgAsmCilDeclSecurity* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilDeclSecurity*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilDeclSecurity*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilDeclSecurity* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilDeclSecurityTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilDeclSecurityTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 215783 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilEvent::copy ( SgCopyHelp& help) const
   {
     SgAsmCilEvent* result = NULL;

  // printf ("Copy SgAsmCilEvent = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilEvent*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilEvent*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilEvent: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilEvent*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilEvent(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_EventFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EventFlags
  // case: toBeCopied == COPY_DATA for EventFlags
     uint16_t EventFlags_copy = p_EventFlags; 
     result->p_EventFlags = EventFlags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_EventType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EventType
  // case: toBeCopied == COPY_DATA for EventType
     uint32_t EventType_copy = p_EventType; 
     result->p_EventType = EventType_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilEvent = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilEvent::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 215982 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilEventMap::copy ( SgCopyHelp& help) const
   {
     SgAsmCilEventMap* result = NULL;

  // printf ("Copy SgAsmCilEventMap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilEventMap*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilEventMap*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilEventMap: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilEventMap*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilEventMap(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_EventList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EventList
  // case: toBeCopied == COPY_DATA for EventList
     uint32_t EventList_copy = p_EventList; 
     result->p_EventList = EventList_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilEventMap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilEventMap::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 216176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilEventMapTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilEventMapTable* result = NULL;

  // printf ("Copy SgAsmCilEventMapTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilEventMapTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilEventMapTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilEventMapTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilEventMapTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilEventMapTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilEventMap*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilEventMap*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilEventMap* source_list_element = *source_elements_iterator; 
          SgAsmCilEventMap* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilEventMap*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilEventMap*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilEventMap* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilEventMapTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilEventMapTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 216391 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilEventTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilEventTable* result = NULL;

  // printf ("Copy SgAsmCilEventTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilEventTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilEventTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilEventTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilEventTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilEventTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilEvent*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilEvent*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilEvent* source_list_element = *source_elements_iterator; 
          SgAsmCilEvent* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilEvent*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilEvent*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilEvent* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilEventTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilEventTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 216606 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilExportedType::copy ( SgCopyHelp& help) const
   {
     SgAsmCilExportedType* result = NULL;

  // printf ("Copy SgAsmCilExportedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilExportedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilExportedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilExportedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilExportedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilExportedType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_EventFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EventFlags
  // case: toBeCopied == COPY_DATA for EventFlags
     uint32_t EventFlags_copy = p_EventFlags; 
     result->p_EventFlags = EventFlags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_TypeDefIdName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeDefIdName
  // case: toBeCopied == COPY_DATA for TypeDefIdName
     uint32_t TypeDefIdName_copy = p_TypeDefIdName; 
     result->p_TypeDefIdName = TypeDefIdName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_TypeName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeName
  // case: toBeCopied == COPY_DATA for TypeName
     uint32_t TypeName_copy = p_TypeName; 
     result->p_TypeName = TypeName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_TypeNamespace
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeNamespace
  // case: toBeCopied == COPY_DATA for TypeNamespace
     uint32_t TypeNamespace_copy = p_TypeNamespace; 
     result->p_TypeNamespace = TypeNamespace_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Implementation
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Implementation
  // case: toBeCopied == COPY_DATA for Implementation
     uint32_t Implementation_copy = p_Implementation; 
     result->p_Implementation = Implementation_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilExportedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilExportedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 216815 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilExportedTypeTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilExportedTypeTable* result = NULL;

  // printf ("Copy SgAsmCilExportedTypeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilExportedTypeTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilExportedTypeTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilExportedTypeTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilExportedTypeTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilExportedTypeTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilExportedType*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilExportedType*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilExportedType* source_list_element = *source_elements_iterator; 
          SgAsmCilExportedType* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilExportedType*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilExportedType*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilExportedType* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilExportedTypeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilExportedTypeTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 217030 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilField::copy ( SgCopyHelp& help) const
   {
     SgAsmCilField* result = NULL;

  // printf ("Copy SgAsmCilField = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilField*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilField*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilField: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilField*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilField(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilField = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilField::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 217229 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldLayout::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFieldLayout* result = NULL;

  // printf ("Copy SgAsmCilFieldLayout = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFieldLayout*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFieldLayout*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFieldLayout: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFieldLayout*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFieldLayout(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Field
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Field
  // case: toBeCopied == COPY_DATA for Field
     uint32_t Field_copy = p_Field; 
     result->p_Field = Field_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFieldLayout = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFieldLayout::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 217423 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldLayoutTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFieldLayoutTable* result = NULL;

  // printf ("Copy SgAsmCilFieldLayoutTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFieldLayoutTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFieldLayoutTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFieldLayoutTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFieldLayoutTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFieldLayoutTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilFieldLayout*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilFieldLayout*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilFieldLayout* source_list_element = *source_elements_iterator; 
          SgAsmCilFieldLayout* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilFieldLayout*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilFieldLayout*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilFieldLayout* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFieldLayoutTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFieldLayoutTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 217638 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldMarshal::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFieldMarshal* result = NULL;

  // printf ("Copy SgAsmCilFieldMarshal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFieldMarshal*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFieldMarshal*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFieldMarshal: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFieldMarshal*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFieldMarshal(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_NativeType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NativeType
  // case: toBeCopied == COPY_DATA for NativeType
     uint32_t NativeType_copy = p_NativeType; 
     result->p_NativeType = NativeType_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFieldMarshal = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFieldMarshal::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 217832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldMarshalTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFieldMarshalTable* result = NULL;

  // printf ("Copy SgAsmCilFieldMarshalTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFieldMarshalTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFieldMarshalTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFieldMarshalTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFieldMarshalTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFieldMarshalTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilFieldMarshal*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilFieldMarshal*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilFieldMarshal* source_list_element = *source_elements_iterator; 
          SgAsmCilFieldMarshal* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilFieldMarshal*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilFieldMarshal*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilFieldMarshal* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFieldMarshalTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFieldMarshalTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 218047 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldRVA::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFieldRVA* result = NULL;

  // printf ("Copy SgAsmCilFieldRVA = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFieldRVA*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFieldRVA*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFieldRVA: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFieldRVA*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFieldRVA(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_RVA
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for RVA
  // case: toBeCopied == COPY_DATA for RVA
     uint32_t RVA_copy = p_RVA; 
     result->p_RVA = RVA_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Field
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Field
  // case: toBeCopied == COPY_DATA for Field
     uint32_t Field_copy = p_Field; 
     result->p_Field = Field_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFieldRVA = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFieldRVA::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 218241 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldRVATable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFieldRVATable* result = NULL;

  // printf ("Copy SgAsmCilFieldRVATable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFieldRVATable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFieldRVATable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFieldRVATable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFieldRVATable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFieldRVATable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilFieldRVA*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilFieldRVA*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilFieldRVA* source_list_element = *source_elements_iterator; 
          SgAsmCilFieldRVA* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilFieldRVA*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilFieldRVA*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilFieldRVA* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFieldRVATable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFieldRVATable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 218456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFieldTable* result = NULL;

  // printf ("Copy SgAsmCilFieldTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFieldTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFieldTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFieldTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFieldTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFieldTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilField*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilField*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilField* source_list_element = *source_elements_iterator; 
          SgAsmCilField* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilField*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilField*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilField* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFieldTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFieldTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 218671 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFile::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFile* result = NULL;

  // printf ("Copy SgAsmCilFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFile*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFile*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFile: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFile*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFile(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_HashValue
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for HashValue
  // case: toBeCopied == COPY_DATA for HashValue
     uint32_t HashValue_copy = p_HashValue; 
     result->p_HashValue = HashValue_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFile::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 218870 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFileTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilFileTable* result = NULL;

  // printf ("Copy SgAsmCilFileTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilFileTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilFileTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilFileTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilFileTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilFileTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilFile*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilFile*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilFile* source_list_element = *source_elements_iterator; 
          SgAsmCilFile* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilFile*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilFile*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilFile* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilFileTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilFileTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 219085 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilGenericParamConstraint::copy ( SgCopyHelp& help) const
   {
     SgAsmCilGenericParamConstraint* result = NULL;

  // printf ("Copy SgAsmCilGenericParamConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilGenericParamConstraint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilGenericParamConstraint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilGenericParamConstraint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilGenericParamConstraint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilGenericParamConstraint(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Owner
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Owner
  // case: toBeCopied == COPY_DATA for Owner
     uint32_t Owner_copy = p_Owner; 
     result->p_Owner = Owner_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Constraint
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Constraint
  // case: toBeCopied == COPY_DATA for Constraint
     uint32_t Constraint_copy = p_Constraint; 
     result->p_Constraint = Constraint_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilGenericParamConstraint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilGenericParamConstraint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 219279 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilGenericParamConstraintTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilGenericParamConstraintTable* result = NULL;

  // printf ("Copy SgAsmCilGenericParamConstraintTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilGenericParamConstraintTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilGenericParamConstraintTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilGenericParamConstraintTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilGenericParamConstraintTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilGenericParamConstraintTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilGenericParamConstraint*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilGenericParamConstraint*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilGenericParamConstraint* source_list_element = *source_elements_iterator; 
          SgAsmCilGenericParamConstraint* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilGenericParamConstraint*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilGenericParamConstraint*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilGenericParamConstraint* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilGenericParamConstraintTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilGenericParamConstraintTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 219494 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilGenericParam::copy ( SgCopyHelp& help) const
   {
     SgAsmCilGenericParam* result = NULL;

  // printf ("Copy SgAsmCilGenericParam = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilGenericParam*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilGenericParam*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilGenericParam: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilGenericParam*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilGenericParam(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Number
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Number
  // case: toBeCopied == COPY_DATA for Number
     uint16_t Number_copy = p_Number; 
     result->p_Number = Number_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Owner
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Owner
  // case: toBeCopied == COPY_DATA for Owner
     uint32_t Owner_copy = p_Owner; 
     result->p_Owner = Owner_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilGenericParam = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilGenericParam::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 219698 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilGenericParamTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilGenericParamTable* result = NULL;

  // printf ("Copy SgAsmCilGenericParamTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilGenericParamTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilGenericParamTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilGenericParamTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilGenericParamTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilGenericParamTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilGenericParam*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilGenericParam*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilGenericParam* source_list_element = *source_elements_iterator; 
          SgAsmCilGenericParam* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilGenericParam*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilGenericParam*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilGenericParam* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilGenericParamTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilGenericParamTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 219913 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilImplMap::copy ( SgCopyHelp& help) const
   {
     SgAsmCilImplMap* result = NULL;

  // printf ("Copy SgAsmCilImplMap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilImplMap*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilImplMap*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilImplMap: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilImplMap*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilImplMap(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_MappingFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MappingFlags
  // case: toBeCopied == COPY_DATA for MappingFlags
     uint16_t MappingFlags_copy = p_MappingFlags; 
     result->p_MappingFlags = MappingFlags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MemberForwarded
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MemberForwarded
  // case: toBeCopied == COPY_DATA for MemberForwarded
     uint32_t MemberForwarded_copy = p_MemberForwarded; 
     result->p_MemberForwarded = MemberForwarded_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ImportName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ImportName
  // case: toBeCopied == COPY_DATA for ImportName
     uint32_t ImportName_copy = p_ImportName; 
     result->p_ImportName = ImportName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ImportScope
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ImportScope
  // case: toBeCopied == COPY_DATA for ImportScope
     uint32_t ImportScope_copy = p_ImportScope; 
     result->p_ImportScope = ImportScope_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilImplMap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilImplMap::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 220117 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilImplMapTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilImplMapTable* result = NULL;

  // printf ("Copy SgAsmCilImplMapTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilImplMapTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilImplMapTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilImplMapTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilImplMapTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilImplMapTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilImplMap*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilImplMap*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilImplMap* source_list_element = *source_elements_iterator; 
          SgAsmCilImplMap* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilImplMap*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilImplMap*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilImplMap* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilImplMapTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilImplMapTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 220332 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilInterfaceImpl::copy ( SgCopyHelp& help) const
   {
     SgAsmCilInterfaceImpl* result = NULL;

  // printf ("Copy SgAsmCilInterfaceImpl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilInterfaceImpl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilInterfaceImpl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilInterfaceImpl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilInterfaceImpl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilInterfaceImpl(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Class
  // case: toBeCopied == COPY_DATA for Class
     uint32_t Class_copy = p_Class; 
     result->p_Class = Class_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Interface
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Interface
  // case: toBeCopied == COPY_DATA for Interface
     uint32_t Interface_copy = p_Interface; 
     result->p_Interface = Interface_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilInterfaceImpl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilInterfaceImpl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 220526 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilInterfaceImplTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilInterfaceImplTable* result = NULL;

  // printf ("Copy SgAsmCilInterfaceImplTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilInterfaceImplTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilInterfaceImplTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilInterfaceImplTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilInterfaceImplTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilInterfaceImplTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilInterfaceImpl*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilInterfaceImpl*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilInterfaceImpl* source_list_element = *source_elements_iterator; 
          SgAsmCilInterfaceImpl* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilInterfaceImpl*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilInterfaceImpl*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilInterfaceImpl* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilInterfaceImplTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilInterfaceImplTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 220741 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilManifestResource::copy ( SgCopyHelp& help) const
   {
     SgAsmCilManifestResource* result = NULL;

  // printf ("Copy SgAsmCilManifestResource = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilManifestResource*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilManifestResource*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilManifestResource: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilManifestResource*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilManifestResource(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Implementation
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Implementation
  // case: toBeCopied == COPY_DATA for Implementation
     uint32_t Implementation_copy = p_Implementation; 
     result->p_Implementation = Implementation_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilManifestResource = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilManifestResource::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 220945 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilManifestResourceTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilManifestResourceTable* result = NULL;

  // printf ("Copy SgAsmCilManifestResourceTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilManifestResourceTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilManifestResourceTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilManifestResourceTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilManifestResourceTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilManifestResourceTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilManifestResource*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilManifestResource*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilManifestResource* source_list_element = *source_elements_iterator; 
          SgAsmCilManifestResource* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilManifestResource*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilManifestResource*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilManifestResource* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilManifestResourceTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilManifestResourceTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 221160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMemberRef::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMemberRef* result = NULL;

  // printf ("Copy SgAsmCilMemberRef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMemberRef*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMemberRef*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMemberRef: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMemberRef*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMemberRef(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Class
  // case: toBeCopied == COPY_DATA for Class
     uint32_t Class_copy = p_Class; 
     result->p_Class = Class_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMemberRef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMemberRef::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 221359 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMemberRefTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMemberRefTable* result = NULL;

  // printf ("Copy SgAsmCilMemberRefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMemberRefTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMemberRefTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMemberRefTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMemberRefTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMemberRefTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMemberRef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMemberRef*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilMemberRef* source_list_element = *source_elements_iterator; 
          SgAsmCilMemberRef* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilMemberRef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMemberRef*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilMemberRef* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMemberRefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMemberRefTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 221574 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodDef::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodDef* result = NULL;

  // printf ("Copy SgAsmCilMethodDef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodDef*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodDef*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodDef: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodDef*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodDef(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_RVA
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for RVA
  // case: toBeCopied == COPY_DATA for RVA
     uint32_t RVA_copy = p_RVA; 
     result->p_RVA = RVA_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ImplFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ImplFlags
  // case: toBeCopied == COPY_DATA for ImplFlags
     uint16_t ImplFlags_copy = p_ImplFlags; 
     result->p_ImplFlags = ImplFlags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ParamList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ParamList
  // case: toBeCopied == COPY_DATA for ParamList
     uint32_t ParamList_copy = p_ParamList; 
     result->p_ParamList = ParamList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgAsmBlock* body_copy = p_body; 
     result->p_body = body_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_initLocals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for initLocals
  // case: toBeCopied == COPY_DATA for initLocals
     bool initLocals_copy = p_initLocals; 
     result->p_initLocals = initLocals_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_stackSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stackSize
  // case: toBeCopied == COPY_DATA for stackSize
     uint32_t stackSize_copy = p_stackSize; 
     result->p_stackSize = stackSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_hasMoreSections
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasMoreSections
  // case: toBeCopied == COPY_DATA for hasMoreSections
     bool hasMoreSections_copy = p_hasMoreSections; 
     result->p_hasMoreSections = hasMoreSections_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_localVarSigTok
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for localVarSigTok
  // case: toBeCopied == COPY_DATA for localVarSigTok
     uint32_t localVarSigTok_copy = p_localVarSigTok; 
     result->p_localVarSigTok = localVarSigTok_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_methodData
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for methodData
  // case: toBeCopied == COPY_DATA for methodData
     std::vector<SgAsmCilMethodData*> methodData_copy = p_methodData; 
     result->p_methodData = methodData_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bodyState
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bodyState
  // case: toBeCopied == COPY_DATA for bodyState
     int bodyState_copy = p_bodyState; 
     result->p_bodyState = bodyState_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodDef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodDef::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 221823 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodDefTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodDefTable* result = NULL;

  // printf ("Copy SgAsmCilMethodDefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodDefTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodDefTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodDefTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodDefTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodDefTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMethodDef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMethodDef*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilMethodDef* source_list_element = *source_elements_iterator; 
          SgAsmCilMethodDef* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilMethodDef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMethodDef*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilMethodDef* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodDefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodDefTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 222038 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodImpl::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodImpl* result = NULL;

  // printf ("Copy SgAsmCilMethodImpl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodImpl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodImpl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodImpl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodImpl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodImpl(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Class
  // case: toBeCopied == COPY_DATA for Class
     uint32_t Class_copy = p_Class; 
     result->p_Class = Class_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MethodBody
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MethodBody
  // case: toBeCopied == COPY_DATA for MethodBody
     uint32_t MethodBody_copy = p_MethodBody; 
     result->p_MethodBody = MethodBody_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MethodDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MethodDeclaration
  // case: toBeCopied == COPY_DATA for MethodDeclaration
     uint32_t MethodDeclaration_copy = p_MethodDeclaration; 
     result->p_MethodDeclaration = MethodDeclaration_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodImpl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodImpl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 222237 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodImplTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodImplTable* result = NULL;

  // printf ("Copy SgAsmCilMethodImplTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodImplTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodImplTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodImplTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodImplTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodImplTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMethodImpl*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMethodImpl*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilMethodImpl* source_list_element = *source_elements_iterator; 
          SgAsmCilMethodImpl* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilMethodImpl*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMethodImpl*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilMethodImpl* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodImplTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodImplTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 222452 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodSemantics::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodSemantics* result = NULL;

  // printf ("Copy SgAsmCilMethodSemantics = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodSemantics*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodSemantics*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodSemantics: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodSemantics*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodSemantics(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Semantics
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Semantics
  // case: toBeCopied == COPY_DATA for Semantics
     uint16_t Semantics_copy = p_Semantics; 
     result->p_Semantics = Semantics_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Method
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Method
  // case: toBeCopied == COPY_DATA for Method
     uint32_t Method_copy = p_Method; 
     result->p_Method = Method_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Association
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Association
  // case: toBeCopied == COPY_DATA for Association
     uint32_t Association_copy = p_Association; 
     result->p_Association = Association_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodSemantics = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodSemantics::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 222651 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodSemanticsTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodSemanticsTable* result = NULL;

  // printf ("Copy SgAsmCilMethodSemanticsTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodSemanticsTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodSemanticsTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodSemanticsTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodSemanticsTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodSemanticsTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMethodSemantics*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMethodSemantics*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilMethodSemantics* source_list_element = *source_elements_iterator; 
          SgAsmCilMethodSemantics* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilMethodSemantics*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMethodSemantics*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilMethodSemantics* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodSemanticsTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodSemanticsTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 222866 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodSpec::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodSpec* result = NULL;

  // printf ("Copy SgAsmCilMethodSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodSpec*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodSpec*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodSpec: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodSpec*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodSpec(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Method
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Method
  // case: toBeCopied == COPY_DATA for Method
     uint32_t Method_copy = p_Method; 
     result->p_Method = Method_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Instantiation
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Instantiation
  // case: toBeCopied == COPY_DATA for Instantiation
     uint32_t Instantiation_copy = p_Instantiation; 
     result->p_Instantiation = Instantiation_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodSpec::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 223060 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodSpecTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodSpecTable* result = NULL;

  // printf ("Copy SgAsmCilMethodSpecTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodSpecTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodSpecTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodSpecTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodSpecTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodSpecTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMethodSpec*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMethodSpec*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilMethodSpec* source_list_element = *source_elements_iterator; 
          SgAsmCilMethodSpec* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilMethodSpec*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilMethodSpec*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilMethodSpec* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodSpecTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodSpecTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 223275 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilModule::copy ( SgCopyHelp& help) const
   {
     SgAsmCilModule* result = NULL;

  // printf ("Copy SgAsmCilModule = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilModule*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilModule*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilModule: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilModule*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilModule(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Generation
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Generation
  // case: toBeCopied == COPY_DATA for Generation
     uint16_t Generation_copy = p_Generation; 
     result->p_Generation = Generation_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Mvid
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Mvid
  // case: toBeCopied == COPY_DATA for Mvid
     uint32_t Mvid_copy = p_Mvid; 
     result->p_Mvid = Mvid_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Encld
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Encld
  // case: toBeCopied == COPY_DATA for Encld
     uint32_t Encld_copy = p_Encld; 
     result->p_Encld = Encld_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_EncBaseId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EncBaseId
  // case: toBeCopied == COPY_DATA for EncBaseId
     uint32_t EncBaseId_copy = p_EncBaseId; 
     result->p_EncBaseId = EncBaseId_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilModule = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilModule::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 223484 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilModuleRef::copy ( SgCopyHelp& help) const
   {
     SgAsmCilModuleRef* result = NULL;

  // printf ("Copy SgAsmCilModuleRef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilModuleRef*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilModuleRef*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilModuleRef: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilModuleRef*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilModuleRef(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilModuleRef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilModuleRef::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 223673 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilModuleRefTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilModuleRefTable* result = NULL;

  // printf ("Copy SgAsmCilModuleRefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilModuleRefTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilModuleRefTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilModuleRefTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilModuleRefTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilModuleRefTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilModuleRef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilModuleRef*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilModuleRef* source_list_element = *source_elements_iterator; 
          SgAsmCilModuleRef* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilModuleRef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilModuleRef*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilModuleRef* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilModuleRefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilModuleRefTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 223888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilModuleTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilModuleTable* result = NULL;

  // printf ("Copy SgAsmCilModuleTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilModuleTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilModuleTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilModuleTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilModuleTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilModuleTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilModule*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilModule*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilModule* source_list_element = *source_elements_iterator; 
          SgAsmCilModule* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilModule*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilModule*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilModule* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilModuleTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilModuleTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 224103 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilNestedClass::copy ( SgCopyHelp& help) const
   {
     SgAsmCilNestedClass* result = NULL;

  // printf ("Copy SgAsmCilNestedClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilNestedClass*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilNestedClass*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilNestedClass: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilNestedClass*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilNestedClass(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_NestedClass
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NestedClass
  // case: toBeCopied == COPY_DATA for NestedClass
     uint32_t NestedClass_copy = p_NestedClass; 
     result->p_NestedClass = NestedClass_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_EnclosingClass
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EnclosingClass
  // case: toBeCopied == COPY_DATA for EnclosingClass
     uint32_t EnclosingClass_copy = p_EnclosingClass; 
     result->p_EnclosingClass = EnclosingClass_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilNestedClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilNestedClass::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 224297 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilNestedClassTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilNestedClassTable* result = NULL;

  // printf ("Copy SgAsmCilNestedClassTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilNestedClassTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilNestedClassTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilNestedClassTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilNestedClassTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilNestedClassTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilNestedClass*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilNestedClass*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilNestedClass* source_list_element = *source_elements_iterator; 
          SgAsmCilNestedClass* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilNestedClass*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilNestedClass*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilNestedClass* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilNestedClassTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilNestedClassTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 224512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilParam::copy ( SgCopyHelp& help) const
   {
     SgAsmCilParam* result = NULL;

  // printf ("Copy SgAsmCilParam = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilParam*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilParam*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilParam: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilParam*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilParam(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Sequence
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Sequence
  // case: toBeCopied == COPY_DATA for Sequence
     uint16_t Sequence_copy = p_Sequence; 
     result->p_Sequence = Sequence_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilParam = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilParam::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 224711 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilParamTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilParamTable* result = NULL;

  // printf ("Copy SgAsmCilParamTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilParamTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilParamTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilParamTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilParamTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilParamTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilParam*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilParam*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilParam* source_list_element = *source_elements_iterator; 
          SgAsmCilParam* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilParam*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilParam*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilParam* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilParamTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilParamTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 224926 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilProperty::copy ( SgCopyHelp& help) const
   {
     SgAsmCilProperty* result = NULL;

  // printf ("Copy SgAsmCilProperty = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilProperty*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilProperty*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilProperty: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilProperty*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilProperty(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Type
  // case: toBeCopied == COPY_DATA for Type
     uint32_t Type_copy = p_Type; 
     result->p_Type = Type_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilProperty = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilProperty::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 225125 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilPropertyMap::copy ( SgCopyHelp& help) const
   {
     SgAsmCilPropertyMap* result = NULL;

  // printf ("Copy SgAsmCilPropertyMap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilPropertyMap*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilPropertyMap*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilPropertyMap: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilPropertyMap*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilPropertyMap(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_PropertyList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PropertyList
  // case: toBeCopied == COPY_DATA for PropertyList
     uint32_t PropertyList_copy = p_PropertyList; 
     result->p_PropertyList = PropertyList_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilPropertyMap = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilPropertyMap::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 225319 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilPropertyMapTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilPropertyMapTable* result = NULL;

  // printf ("Copy SgAsmCilPropertyMapTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilPropertyMapTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilPropertyMapTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilPropertyMapTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilPropertyMapTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilPropertyMapTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilPropertyMap*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilPropertyMap*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilPropertyMap* source_list_element = *source_elements_iterator; 
          SgAsmCilPropertyMap* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilPropertyMap*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilPropertyMap*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilPropertyMap* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilPropertyMapTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilPropertyMapTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 225534 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilPropertyTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilPropertyTable* result = NULL;

  // printf ("Copy SgAsmCilPropertyTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilPropertyTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilPropertyTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilPropertyTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilPropertyTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilPropertyTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilProperty*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilProperty*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilProperty* source_list_element = *source_elements_iterator; 
          SgAsmCilProperty* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilProperty*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilProperty*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilProperty* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilPropertyTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilPropertyTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 225749 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilStandAloneSig::copy ( SgCopyHelp& help) const
   {
     SgAsmCilStandAloneSig* result = NULL;

  // printf ("Copy SgAsmCilStandAloneSig = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilStandAloneSig*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilStandAloneSig*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilStandAloneSig: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilStandAloneSig*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilStandAloneSig(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilStandAloneSig = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilStandAloneSig::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 225938 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilStandAloneSigTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilStandAloneSigTable* result = NULL;

  // printf ("Copy SgAsmCilStandAloneSigTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilStandAloneSigTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilStandAloneSigTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilStandAloneSigTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilStandAloneSigTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilStandAloneSigTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilStandAloneSig*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilStandAloneSig*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilStandAloneSig* source_list_element = *source_elements_iterator; 
          SgAsmCilStandAloneSig* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilStandAloneSig*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilStandAloneSig*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilStandAloneSig* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilStandAloneSigTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilStandAloneSigTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 226153 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeDef::copy ( SgCopyHelp& help) const
   {
     SgAsmCilTypeDef* result = NULL;

  // printf ("Copy SgAsmCilTypeDef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilTypeDef*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilTypeDef*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilTypeDef: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilTypeDef*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilTypeDef(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_TypeName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeName
  // case: toBeCopied == COPY_DATA for TypeName
     uint32_t TypeName_copy = p_TypeName; 
     result->p_TypeName = TypeName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_TypeNamespace
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeNamespace
  // case: toBeCopied == COPY_DATA for TypeNamespace
     uint32_t TypeNamespace_copy = p_TypeNamespace; 
     result->p_TypeNamespace = TypeNamespace_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Extends
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Extends
  // case: toBeCopied == COPY_DATA for Extends
     uint32_t Extends_copy = p_Extends; 
     result->p_Extends = Extends_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_FieldList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for FieldList
  // case: toBeCopied == COPY_DATA for FieldList
     uint32_t FieldList_copy = p_FieldList; 
     result->p_FieldList = FieldList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MethodList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MethodList
  // case: toBeCopied == COPY_DATA for MethodList
     uint32_t MethodList_copy = p_MethodList; 
     result->p_MethodList = MethodList_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilTypeDef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilTypeDef::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 226367 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeDefTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilTypeDefTable* result = NULL;

  // printf ("Copy SgAsmCilTypeDefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilTypeDefTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilTypeDefTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilTypeDefTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilTypeDefTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilTypeDefTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilTypeDef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilTypeDef*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilTypeDef* source_list_element = *source_elements_iterator; 
          SgAsmCilTypeDef* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilTypeDef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilTypeDef*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilTypeDef* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilTypeDefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilTypeDefTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 226582 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeRef::copy ( SgCopyHelp& help) const
   {
     SgAsmCilTypeRef* result = NULL;

  // printf ("Copy SgAsmCilTypeRef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilTypeRef*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilTypeRef*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilTypeRef: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilTypeRef*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilTypeRef(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_ResolutionScope
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ResolutionScope
  // case: toBeCopied == COPY_DATA for ResolutionScope
     uint16_t ResolutionScope_copy = p_ResolutionScope; 
     result->p_ResolutionScope = ResolutionScope_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_TypeName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeName
  // case: toBeCopied == COPY_DATA for TypeName
     uint32_t TypeName_copy = p_TypeName; 
     result->p_TypeName = TypeName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_TypeNamespace
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeNamespace
  // case: toBeCopied == COPY_DATA for TypeNamespace
     uint32_t TypeNamespace_copy = p_TypeNamespace; 
     result->p_TypeNamespace = TypeNamespace_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilTypeRef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilTypeRef::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 226781 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeRefTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilTypeRefTable* result = NULL;

  // printf ("Copy SgAsmCilTypeRefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilTypeRefTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilTypeRefTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilTypeRefTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilTypeRefTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilTypeRefTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilTypeRef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilTypeRef*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilTypeRef* source_list_element = *source_elements_iterator; 
          SgAsmCilTypeRef* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilTypeRef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilTypeRef*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilTypeRef* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilTypeRefTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilTypeRefTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 226996 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeSpec::copy ( SgCopyHelp& help) const
   {
     SgAsmCilTypeSpec* result = NULL;

  // printf ("Copy SgAsmCilTypeSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilTypeSpec*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilTypeSpec*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilTypeSpec: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilTypeSpec*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilTypeSpec(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilTypeSpec = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilTypeSpec::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 227185 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeSpecTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCilTypeSpecTable* result = NULL;

  // printf ("Copy SgAsmCilTypeSpecTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilTypeSpecTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilTypeSpecTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilTypeSpecTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilTypeSpecTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilTypeSpecTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilTypeSpec*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilTypeSpec*>::const_iterator source_elements_iterator = get_elements().begin(); 
     for ( /* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) 
        { 
          SgAsmCilTypeSpec* source_list_element = *source_elements_iterator; 
          SgAsmCilTypeSpec* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilTypeSpec*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          elements_copy.push_back(copy_list_element); 
        } 
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
     std::vector<SgAsmCilTypeSpec*>::const_iterator elements_iterator = result->get_elements().begin(); 
     for ( /* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) 
        { 
          SgAsmCilTypeSpec* list_element = *elements_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilTypeSpecTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilTypeSpecTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 227400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMetadataRoot::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMetadataRoot* result = NULL;

  // printf ("Copy SgAsmCilMetadataRoot = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMetadataRoot*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMetadataRoot*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMetadataRoot: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMetadataRoot*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMetadataRoot(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MajorVersion
  // case: toBeCopied == COPY_DATA for MajorVersion
     uint16_t MajorVersion_copy = p_MajorVersion; 
     result->p_MajorVersion = MajorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_MinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MinorVersion
  // case: toBeCopied == COPY_DATA for MinorVersion
     uint16_t MinorVersion_copy = p_MinorVersion; 
     result->p_MinorVersion = MinorVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Reserved0
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Reserved0
  // case: toBeCopied == COPY_DATA for Reserved0
     uint32_t Reserved0_copy = p_Reserved0; 
     result->p_Reserved0 = Reserved0_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Length
  // case: toBeCopied == COPY_DATA for Length
     uint32_t Length_copy = p_Length; 
     result->p_Length = Length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Version
  // case: toBeCopied == COPY_DATA for Version
     std::string Version_copy = p_Version; 
     result->p_Version = Version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_VersionPadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for VersionPadding
  // case: toBeCopied == COPY_DATA for VersionPadding
     uint32_t VersionPadding_copy = p_VersionPadding; 
     result->p_VersionPadding = VersionPadding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_NumberOfStreams
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NumberOfStreams
  // case: toBeCopied == COPY_DATA for NumberOfStreams
     uint16_t NumberOfStreams_copy = p_NumberOfStreams; 
     result->p_NumberOfStreams = NumberOfStreams_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_Streams
     std::vector<SgAsmCilDataStream*> Streams_copy; 
  // case: listType for Streams
  // case: listType (typeIsPointerToList == false) for Streams
     std::vector<SgAsmCilDataStream*>::const_iterator source_Streams_iterator = get_Streams().begin(); 
     for ( /* empty by design */; source_Streams_iterator != get_Streams().end(); ++source_Streams_iterator) 
        { 
          SgAsmCilDataStream* source_list_element = *source_Streams_iterator; 
          SgAsmCilDataStream* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCilDataStream*>(help.copyAst(*source_Streams_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          Streams_copy.push_back(copy_list_element); 
        } 
     result->p_Streams = Streams_copy; 
  // case: listType for Streams
  // case: listType (typeIsPointerToList == false) for Streams
     std::vector<SgAsmCilDataStream*>::const_iterator Streams_iterator = result->get_Streams().begin(); 
     for ( /* empty by design */; Streams_iterator != result->get_Streams().end(); ++Streams_iterator) 
        { 
          SgAsmCilDataStream* list_element = *Streams_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_ErrorFree
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ErrorFree
  // case: toBeCopied == COPY_DATA for ErrorFree
     bool ErrorFree_copy = p_ErrorFree; 
     result->p_ErrorFree = ErrorFree_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMetadataRoot = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMetadataRoot::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 227665 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodData::copy ( SgCopyHelp& help) const
   {
     SgAsmCilMethodData* result = NULL;

  // printf ("Copy SgAsmCilMethodData = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilMethodData*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilMethodData*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilMethodData: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilMethodData*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilMethodData(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     uint64_t kind_copy = p_kind; 
     result->p_kind = kind_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dataSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dataSize
  // case: toBeCopied == COPY_DATA for dataSize
     std::uint32_t dataSize_copy = p_dataSize; 
     result->p_dataSize = dataSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_clauses
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clauses
  // case: toBeCopied == COPY_DATA for clauses
     std::vector<SgAsmCilExceptionData*> clauses_copy = p_clauses; 
     result->p_clauses = clauses_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilMethodData = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilMethodData::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 227864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmExecutableFileFormat::copy ( SgCopyHelp& help) const
   {
     SgAsmExecutableFileFormat* result = NULL;

  // printf ("Copy SgAsmExecutableFileFormat = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmExecutableFileFormat*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmExecutableFileFormat*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmExecutableFileFormat: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmExecutableFileFormat*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmExecutableFileFormat(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmExecutableFileFormat = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmExecutableFileFormat::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 228047 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCoffSymbolList::copy ( SgCopyHelp& help) const
   {
     SgAsmCoffSymbolList* result = NULL;

  // printf ("Copy SgAsmCoffSymbolList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCoffSymbolList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCoffSymbolList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCoffSymbolList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCoffSymbolList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCoffSymbolList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_symbols
     SgAsmCoffSymbolPtrList symbols_copy; 
  // case: listType for symbols
  // case: listType (typeIsPointerToList == false) for symbols
     SgAsmCoffSymbolPtrList::const_iterator source_symbols_iterator = get_symbols().begin(); 
     for ( /* empty by design */; source_symbols_iterator != get_symbols().end(); ++source_symbols_iterator) 
        { 
          SgAsmCoffSymbol* source_list_element = *source_symbols_iterator; 
          SgAsmCoffSymbol* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmCoffSymbol*>(help.copyAst(*source_symbols_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          symbols_copy.push_back(copy_list_element); 
        } 
     result->p_symbols = symbols_copy; 
  // case: listType for symbols
  // case: listType (typeIsPointerToList == false) for symbols
     SgAsmCoffSymbolPtrList::const_iterator symbols_iterator = result->get_symbols().begin(); 
     for ( /* empty by design */; symbols_iterator != result->get_symbols().end(); ++symbols_iterator) 
        { 
          SgAsmCoffSymbol* list_element = *symbols_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCoffSymbolList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCoffSymbolList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 228262 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfInformation::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfInformation* result = NULL;

  // printf ("Copy SgAsmDwarfInformation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfInformation*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfInformation*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfInformation: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfInformation*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfInformation(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfInformation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfInformation::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 228445 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCompilationUnitList::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfCompilationUnitList* result = NULL;

  // printf ("Copy SgAsmDwarfCompilationUnitList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfCompilationUnitList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfCompilationUnitList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfCompilationUnitList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfCompilationUnitList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfCompilationUnitList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_cu_list
     SgAsmDwarfCompilationUnitPtrList cu_list_copy; 
  // case: listType for cu_list
  // case: listType (typeIsPointerToList == false) for cu_list
     SgAsmDwarfCompilationUnitPtrList::const_iterator source_cu_list_iterator = get_cu_list().begin(); 
     for ( /* empty by design */; source_cu_list_iterator != get_cu_list().end(); ++source_cu_list_iterator) 
        { 
          SgAsmDwarfCompilationUnit* source_list_element = *source_cu_list_iterator; 
          SgAsmDwarfCompilationUnit* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmDwarfCompilationUnit*>(help.copyAst(*source_cu_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          cu_list_copy.push_back(copy_list_element); 
        } 
     result->p_cu_list = cu_list_copy; 
  // case: listType for cu_list
  // case: listType (typeIsPointerToList == false) for cu_list
     SgAsmDwarfCompilationUnitPtrList::const_iterator cu_list_iterator = result->get_cu_list().begin(); 
     for ( /* empty by design */; cu_list_iterator != result->get_cu_list().end(); ++cu_list_iterator) 
        { 
          SgAsmDwarfCompilationUnit* list_element = *cu_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfCompilationUnitList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfCompilationUnitList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 228660 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfConstruct::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfConstruct* result = NULL;

  // printf ("Copy SgAsmDwarfConstruct = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfConstruct*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfConstruct*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfConstruct: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfConstruct*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfConstruct(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfConstruct = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfConstruct::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 228869 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfAccessDeclaration::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfAccessDeclaration* result = NULL;

  // printf ("Copy SgAsmDwarfAccessDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfAccessDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfAccessDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfAccessDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfAccessDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfAccessDeclaration(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfAccessDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfAccessDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 229078 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfArrayType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfArrayType* result = NULL;

  // printf ("Copy SgAsmDwarfArrayType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfArrayType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfArrayType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfArrayType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfArrayType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfArrayType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfArrayType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfArrayType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 229306 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfBaseType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfBaseType* result = NULL;

  // printf ("Copy SgAsmDwarfBaseType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfBaseType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfBaseType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfBaseType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfBaseType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfBaseType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfBaseType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfBaseType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 229515 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCatchBlock::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfCatchBlock* result = NULL;

  // printf ("Copy SgAsmDwarfCatchBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfCatchBlock*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfCatchBlock*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfCatchBlock: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfCatchBlock*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfCatchBlock(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfCatchBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfCatchBlock::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 229724 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfClassTemplate::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfClassTemplate* result = NULL;

  // printf ("Copy SgAsmDwarfClassTemplate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfClassTemplate*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfClassTemplate*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfClassTemplate: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfClassTemplate*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfClassTemplate(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfClassTemplate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfClassTemplate::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 229933 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfClassType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfClassType* result = NULL;

  // printf ("Copy SgAsmDwarfClassType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfClassType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfClassType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfClassType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfClassType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfClassType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfClassType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfClassType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 230161 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCommonBlock::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfCommonBlock* result = NULL;

  // printf ("Copy SgAsmDwarfCommonBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfCommonBlock*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfCommonBlock*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfCommonBlock: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfCommonBlock*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfCommonBlock(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfCommonBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfCommonBlock::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 230389 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCommonInclusion::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfCommonInclusion* result = NULL;

  // printf ("Copy SgAsmDwarfCommonInclusion = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfCommonInclusion*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfCommonInclusion*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfCommonInclusion: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfCommonInclusion*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfCommonInclusion(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfCommonInclusion = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfCommonInclusion::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 230598 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCompilationUnit::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfCompilationUnit* result = NULL;

  // printf ("Copy SgAsmDwarfCompilationUnit = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfCompilationUnit*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfCompilationUnit*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfCompilationUnit: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfCompilationUnit*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfCompilationUnit(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_producer
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for producer
  // case: toBeCopied == COPY_DATA for producer
     std::string producer_copy = p_producer; 
     result->p_producer = producer_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_language
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for language
  // case: toBeCopied == COPY_DATA for language
     std::string language_copy = p_language; 
     result->p_language = language_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_low_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for low_pc
  // case: toBeCopied == COPY_DATA for low_pc
     uint64_t low_pc_copy = p_low_pc; 
     result->p_low_pc = low_pc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_hi_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hi_pc
  // case: toBeCopied == COPY_DATA for hi_pc
     uint64_t hi_pc_copy = p_hi_pc; 
     result->p_hi_pc = hi_pc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_version_stamp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version_stamp
  // case: toBeCopied == COPY_DATA for version_stamp
     int version_stamp_copy = p_version_stamp; 
     result->p_version_stamp = version_stamp_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_abbrev_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for abbrev_offset
  // case: toBeCopied == COPY_DATA for abbrev_offset
     uint64_t abbrev_offset_copy = p_abbrev_offset; 
     result->p_abbrev_offset = abbrev_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_address_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address_size
  // case: toBeCopied == COPY_DATA for address_size
     uint64_t address_size_copy = p_address_size; 
     result->p_address_size = address_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset_length
  // case: toBeCopied == COPY_DATA for offset_length
     uint64_t offset_length_copy = p_offset_length; 
     result->p_offset_length = offset_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_line_info
     SgAsmDwarfLineList* line_info_copy; 
  // case: not a listType for (using conditionalToCopyVariable)line_info
          if (get_line_info() != NULL) 
             { 
               line_info_copy = static_cast<SgAsmDwarfLineList*>(help.copyAst(get_line_info())); 
             } 
            else 
             { 
               line_info_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_line_info != NULL) delete result->p_line_info; 
     result->p_line_info = line_info_copy; 
  // case: not a listType for (using conditionalToSetParent)line_info
          if ( (line_info_copy != NULL) && (line_info_copy->get_parent() == NULL) && (isSgType(line_info_copy) == NULL) ) 
             { 
               line_info_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_language_constructs
     SgAsmDwarfConstructList* language_constructs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)language_constructs
          if (get_language_constructs() != NULL) 
             { 
               language_constructs_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_language_constructs())); 
             } 
            else 
             { 
               language_constructs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_language_constructs != NULL) delete result->p_language_constructs; 
     result->p_language_constructs = language_constructs_copy; 
  // case: not a listType for (using conditionalToSetParent)language_constructs
          if ( (language_constructs_copy != NULL) && (language_constructs_copy->get_parent() == NULL) && (isSgType(language_constructs_copy) == NULL) ) 
             { 
               language_constructs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_macro_info
     SgAsmDwarfMacroList* macro_info_copy; 
  // case: not a listType for (using conditionalToCopyVariable)macro_info
          if (get_macro_info() != NULL) 
             { 
               macro_info_copy = static_cast<SgAsmDwarfMacroList*>(help.copyAst(get_macro_info())); 
             } 
            else 
             { 
               macro_info_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_macro_info != NULL) delete result->p_macro_info; 
     result->p_macro_info = macro_info_copy; 
  // case: not a listType for (using conditionalToSetParent)macro_info
          if ( (macro_info_copy != NULL) && (macro_info_copy->get_parent() == NULL) && (isSgType(macro_info_copy) == NULL) ) 
             { 
               macro_info_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfCompilationUnit = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfCompilationUnit::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 230904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCondition::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfCondition* result = NULL;

  // printf ("Copy SgAsmDwarfCondition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfCondition*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfCondition*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfCondition: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfCondition*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfCondition(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfCondition = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfCondition::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 231113 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfConstant::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfConstant* result = NULL;

  // printf ("Copy SgAsmDwarfConstant = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfConstant*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfConstant*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfConstant: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfConstant*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfConstant(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfConstant = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfConstant::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 231322 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfConstType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfConstType* result = NULL;

  // printf ("Copy SgAsmDwarfConstType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfConstType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfConstType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfConstType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfConstType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfConstType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfConstType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfConstType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 231531 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfDwarfProcedure::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfDwarfProcedure* result = NULL;

  // printf ("Copy SgAsmDwarfDwarfProcedure = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfDwarfProcedure*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfDwarfProcedure*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfDwarfProcedure: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfDwarfProcedure*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfDwarfProcedure(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfDwarfProcedure = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfDwarfProcedure::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 231740 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfEntryPoint::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfEntryPoint* result = NULL;

  // printf ("Copy SgAsmDwarfEntryPoint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfEntryPoint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfEntryPoint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfEntryPoint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfEntryPoint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfEntryPoint(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfEntryPoint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfEntryPoint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 231949 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfEnumerationType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfEnumerationType* result = NULL;

  // printf ("Copy SgAsmDwarfEnumerationType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfEnumerationType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfEnumerationType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfEnumerationType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfEnumerationType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfEnumerationType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfEnumerationType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfEnumerationType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 232177 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfEnumerator::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfEnumerator* result = NULL;

  // printf ("Copy SgAsmDwarfEnumerator = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfEnumerator*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfEnumerator*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfEnumerator: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfEnumerator*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfEnumerator(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfEnumerator = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfEnumerator::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 232386 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFileType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfFileType* result = NULL;

  // printf ("Copy SgAsmDwarfFileType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfFileType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfFileType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfFileType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfFileType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfFileType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfFileType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfFileType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 232595 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFormalParameter::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfFormalParameter* result = NULL;

  // printf ("Copy SgAsmDwarfFormalParameter = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfFormalParameter*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfFormalParameter*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfFormalParameter: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfFormalParameter*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfFormalParameter(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfFormalParameter = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfFormalParameter::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 232804 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFormatLabel::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfFormatLabel* result = NULL;

  // printf ("Copy SgAsmDwarfFormatLabel = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfFormatLabel*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfFormatLabel*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfFormatLabel: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfFormatLabel*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfFormatLabel(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfFormatLabel = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfFormatLabel::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 233013 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFriend::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfFriend* result = NULL;

  // printf ("Copy SgAsmDwarfFriend = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfFriend*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfFriend*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfFriend: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfFriend*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfFriend(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfFriend = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfFriend::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 233222 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFunctionTemplate::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfFunctionTemplate* result = NULL;

  // printf ("Copy SgAsmDwarfFunctionTemplate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfFunctionTemplate*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfFunctionTemplate*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfFunctionTemplate: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfFunctionTemplate*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfFunctionTemplate(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfFunctionTemplate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfFunctionTemplate::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 233431 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfImportedDeclaration::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfImportedDeclaration* result = NULL;

  // printf ("Copy SgAsmDwarfImportedDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfImportedDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfImportedDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfImportedDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfImportedDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfImportedDeclaration(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfImportedDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfImportedDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 233640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfImportedModule::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfImportedModule* result = NULL;

  // printf ("Copy SgAsmDwarfImportedModule = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfImportedModule*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfImportedModule*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfImportedModule: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfImportedModule*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfImportedModule(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfImportedModule = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfImportedModule::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 233849 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfImportedUnit::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfImportedUnit* result = NULL;

  // printf ("Copy SgAsmDwarfImportedUnit = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfImportedUnit*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfImportedUnit*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfImportedUnit: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfImportedUnit*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfImportedUnit(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfImportedUnit = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfImportedUnit::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 234058 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfInheritance::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfInheritance* result = NULL;

  // printf ("Copy SgAsmDwarfInheritance = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfInheritance*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfInheritance*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfInheritance: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfInheritance*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfInheritance(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfInheritance = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfInheritance::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 234267 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfInlinedSubroutine::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfInlinedSubroutine* result = NULL;

  // printf ("Copy SgAsmDwarfInlinedSubroutine = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfInlinedSubroutine*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfInlinedSubroutine*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfInlinedSubroutine: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfInlinedSubroutine*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfInlinedSubroutine(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfInlinedSubroutine = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfInlinedSubroutine::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 234495 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfInterfaceType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfInterfaceType* result = NULL;

  // printf ("Copy SgAsmDwarfInterfaceType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfInterfaceType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfInterfaceType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfInterfaceType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfInterfaceType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfInterfaceType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfInterfaceType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfInterfaceType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 234704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfLabel::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfLabel* result = NULL;

  // printf ("Copy SgAsmDwarfLabel = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfLabel*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfLabel*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfLabel: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfLabel*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfLabel(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfLabel = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfLabel::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 234913 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfLexicalBlock::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfLexicalBlock* result = NULL;

  // printf ("Copy SgAsmDwarfLexicalBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfLexicalBlock*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfLexicalBlock*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfLexicalBlock: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfLexicalBlock*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfLexicalBlock(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfLexicalBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfLexicalBlock::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 235141 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfMember::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfMember* result = NULL;

  // printf ("Copy SgAsmDwarfMember = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfMember*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfMember*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfMember: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfMember*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfMember(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfMember = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfMember::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 235350 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfModule::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfModule* result = NULL;

  // printf ("Copy SgAsmDwarfModule = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfModule*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfModule*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfModule: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfModule*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfModule(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfModule = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfModule::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 235559 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfMutableType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfMutableType* result = NULL;

  // printf ("Copy SgAsmDwarfMutableType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfMutableType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfMutableType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfMutableType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfMutableType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfMutableType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfMutableType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfMutableType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 235768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfNamelist::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfNamelist* result = NULL;

  // printf ("Copy SgAsmDwarfNamelist = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfNamelist*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfNamelist*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfNamelist: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfNamelist*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfNamelist(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfNamelist = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfNamelist::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 235977 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfNamelistItem::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfNamelistItem* result = NULL;

  // printf ("Copy SgAsmDwarfNamelistItem = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfNamelistItem*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfNamelistItem*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfNamelistItem: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfNamelistItem*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfNamelistItem(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfNamelistItem = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfNamelistItem::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 236186 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfNamespace::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfNamespace* result = NULL;

  // printf ("Copy SgAsmDwarfNamespace = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfNamespace*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfNamespace*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfNamespace: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfNamespace*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfNamespace(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfNamespace = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfNamespace::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 236414 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfPackedType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfPackedType* result = NULL;

  // printf ("Copy SgAsmDwarfPackedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfPackedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfPackedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfPackedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfPackedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfPackedType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfPackedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfPackedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 236623 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfPartialUnit::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfPartialUnit* result = NULL;

  // printf ("Copy SgAsmDwarfPartialUnit = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfPartialUnit*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfPartialUnit*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfPartialUnit: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfPartialUnit*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfPartialUnit(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfPartialUnit = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfPartialUnit::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 236832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfPointerType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfPointerType* result = NULL;

  // printf ("Copy SgAsmDwarfPointerType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfPointerType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfPointerType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfPointerType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfPointerType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfPointerType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfPointerType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfPointerType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 237041 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfPtrToMemberType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfPtrToMemberType* result = NULL;

  // printf ("Copy SgAsmDwarfPtrToMemberType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfPtrToMemberType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfPtrToMemberType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfPtrToMemberType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfPtrToMemberType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfPtrToMemberType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfPtrToMemberType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfPtrToMemberType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 237250 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfReferenceType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfReferenceType* result = NULL;

  // printf ("Copy SgAsmDwarfReferenceType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfReferenceType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfReferenceType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfReferenceType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfReferenceType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfReferenceType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfReferenceType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfReferenceType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 237459 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfRestrictType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfRestrictType* result = NULL;

  // printf ("Copy SgAsmDwarfRestrictType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfRestrictType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfRestrictType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfRestrictType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfRestrictType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfRestrictType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfRestrictType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfRestrictType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 237668 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSetType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfSetType* result = NULL;

  // printf ("Copy SgAsmDwarfSetType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfSetType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfSetType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfSetType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfSetType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfSetType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfSetType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfSetType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 237877 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSharedType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfSharedType* result = NULL;

  // printf ("Copy SgAsmDwarfSharedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfSharedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfSharedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfSharedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfSharedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfSharedType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfSharedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfSharedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 238086 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfStringType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfStringType* result = NULL;

  // printf ("Copy SgAsmDwarfStringType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfStringType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfStringType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfStringType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfStringType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfStringType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfStringType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfStringType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 238295 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfStructureType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfStructureType* result = NULL;

  // printf ("Copy SgAsmDwarfStructureType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfStructureType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfStructureType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfStructureType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfStructureType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfStructureType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfStructureType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfStructureType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 238523 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSubprogram::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfSubprogram* result = NULL;

  // printf ("Copy SgAsmDwarfSubprogram = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfSubprogram*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfSubprogram*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfSubprogram: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfSubprogram*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfSubprogram(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfSubprogram = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfSubprogram::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 238751 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSubrangeType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfSubrangeType* result = NULL;

  // printf ("Copy SgAsmDwarfSubrangeType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfSubrangeType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfSubrangeType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfSubrangeType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfSubrangeType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfSubrangeType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfSubrangeType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfSubrangeType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 238960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSubroutineType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfSubroutineType* result = NULL;

  // printf ("Copy SgAsmDwarfSubroutineType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfSubroutineType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfSubroutineType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfSubroutineType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfSubroutineType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfSubroutineType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfSubroutineType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfSubroutineType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 239188 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfTemplateTypeParameter::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfTemplateTypeParameter* result = NULL;

  // printf ("Copy SgAsmDwarfTemplateTypeParameter = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfTemplateTypeParameter*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfTemplateTypeParameter*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfTemplateTypeParameter: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfTemplateTypeParameter*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfTemplateTypeParameter(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfTemplateTypeParameter = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfTemplateTypeParameter::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 239397 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfTemplateValueParameter::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfTemplateValueParameter* result = NULL;

  // printf ("Copy SgAsmDwarfTemplateValueParameter = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfTemplateValueParameter*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfTemplateValueParameter*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfTemplateValueParameter: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfTemplateValueParameter*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfTemplateValueParameter(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfTemplateValueParameter = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfTemplateValueParameter::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 239606 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfThrownType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfThrownType* result = NULL;

  // printf ("Copy SgAsmDwarfThrownType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfThrownType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfThrownType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfThrownType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfThrownType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfThrownType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfThrownType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfThrownType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 239815 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfTryBlock::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfTryBlock* result = NULL;

  // printf ("Copy SgAsmDwarfTryBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfTryBlock*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfTryBlock*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfTryBlock: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfTryBlock*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfTryBlock(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfTryBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfTryBlock::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 240024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfTypedef::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfTypedef* result = NULL;

  // printf ("Copy SgAsmDwarfTypedef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfTypedef*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfTypedef*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfTypedef: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfTypedef*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfTypedef(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfTypedef = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfTypedef::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 240233 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUnionType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfUnionType* result = NULL;

  // printf ("Copy SgAsmDwarfUnionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfUnionType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfUnionType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfUnionType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfUnionType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfUnionType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != NULL) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != NULL) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
          if ( (body_copy != NULL) && (body_copy->get_parent() == NULL) && (isSgType(body_copy) == NULL) ) 
             { 
               body_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfUnionType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfUnionType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 240461 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUnknownConstruct::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfUnknownConstruct* result = NULL;

  // printf ("Copy SgAsmDwarfUnknownConstruct = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfUnknownConstruct*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfUnknownConstruct*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfUnknownConstruct: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfUnknownConstruct*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfUnknownConstruct(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfUnknownConstruct = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfUnknownConstruct::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 240670 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUnspecifiedParameters::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfUnspecifiedParameters* result = NULL;

  // printf ("Copy SgAsmDwarfUnspecifiedParameters = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfUnspecifiedParameters*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfUnspecifiedParameters*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfUnspecifiedParameters: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfUnspecifiedParameters*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfUnspecifiedParameters(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfUnspecifiedParameters = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfUnspecifiedParameters::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 240879 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUnspecifiedType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfUnspecifiedType* result = NULL;

  // printf ("Copy SgAsmDwarfUnspecifiedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfUnspecifiedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfUnspecifiedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfUnspecifiedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfUnspecifiedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfUnspecifiedType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfUnspecifiedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfUnspecifiedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 241088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUpcRelaxedType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfUpcRelaxedType* result = NULL;

  // printf ("Copy SgAsmDwarfUpcRelaxedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfUpcRelaxedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfUpcRelaxedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfUpcRelaxedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfUpcRelaxedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfUpcRelaxedType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfUpcRelaxedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfUpcRelaxedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 241297 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUpcSharedType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfUpcSharedType* result = NULL;

  // printf ("Copy SgAsmDwarfUpcSharedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfUpcSharedType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfUpcSharedType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfUpcSharedType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfUpcSharedType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfUpcSharedType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfUpcSharedType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfUpcSharedType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 241506 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUpcStrictType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfUpcStrictType* result = NULL;

  // printf ("Copy SgAsmDwarfUpcStrictType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfUpcStrictType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfUpcStrictType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfUpcStrictType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfUpcStrictType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfUpcStrictType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfUpcStrictType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfUpcStrictType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 241715 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfVariable::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfVariable* result = NULL;

  // printf ("Copy SgAsmDwarfVariable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfVariable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfVariable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfVariable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfVariable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfVariable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfVariable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfVariable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 241924 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfVariant::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfVariant* result = NULL;

  // printf ("Copy SgAsmDwarfVariant = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfVariant*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfVariant*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfVariant: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfVariant*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfVariant(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfVariant = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfVariant::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 242133 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfVariantPart::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfVariantPart* result = NULL;

  // printf ("Copy SgAsmDwarfVariantPart = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfVariantPart*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfVariantPart*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfVariantPart: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfVariantPart*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfVariantPart(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfVariantPart = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfVariantPart::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 242342 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfVolatileType::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfVolatileType* result = NULL;

  // printf ("Copy SgAsmDwarfVolatileType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfVolatileType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfVolatileType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfVolatileType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfVolatileType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfVolatileType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfVolatileType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfVolatileType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 242551 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfWithStmt::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfWithStmt* result = NULL;

  // printf ("Copy SgAsmDwarfWithStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfWithStmt*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfWithStmt*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfWithStmt: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfWithStmt*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfWithStmt(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfWithStmt = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfWithStmt::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 242760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfConstructList::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfConstructList* result = NULL;

  // printf ("Copy SgAsmDwarfConstructList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfConstructList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfConstructList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfConstructList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfConstructList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfConstructList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_list
     SgAsmDwarfConstructPtrList list_copy; 
  // case: listType for list
  // case: listType (typeIsPointerToList == false) for list
     SgAsmDwarfConstructPtrList::const_iterator source_list_iterator = get_list().begin(); 
     for ( /* empty by design */; source_list_iterator != get_list().end(); ++source_list_iterator) 
        { 
          SgAsmDwarfConstruct* source_list_element = *source_list_iterator; 
          SgAsmDwarfConstruct* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmDwarfConstruct*>(help.copyAst(*source_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          list_copy.push_back(copy_list_element); 
        } 
     result->p_list = list_copy; 
  // case: listType for list
  // case: listType (typeIsPointerToList == false) for list
     SgAsmDwarfConstructPtrList::const_iterator list_iterator = result->get_list().begin(); 
     for ( /* empty by design */; list_iterator != result->get_list().end(); ++list_iterator) 
        { 
          SgAsmDwarfConstruct* list_element = *list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfConstructList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfConstructList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 242975 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfLine::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfLine* result = NULL;

  // printf ("Copy SgAsmDwarfLine = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfLine*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfLine*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfLine: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfLine*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfLine(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     uint64_t address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file_id
  // case: toBeCopied == COPY_DATA for file_id
     int file_id_copy = p_file_id; 
     result->p_file_id = file_id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for line
  // case: toBeCopied == COPY_DATA for line
     int line_copy = p_line; 
     result->p_line = line_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for column
  // case: toBeCopied == COPY_DATA for column
     int column_copy = p_column; 
     result->p_column = column_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfLine = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfLine::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 243179 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfLineList::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfLineList* result = NULL;

  // printf ("Copy SgAsmDwarfLineList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfLineList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfLineList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfLineList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfLineList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfLineList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_line_list
     SgAsmDwarfLinePtrList line_list_copy; 
  // case: listType for line_list
  // case: listType (typeIsPointerToList == false) for line_list
     SgAsmDwarfLinePtrList::const_iterator source_line_list_iterator = get_line_list().begin(); 
     for ( /* empty by design */; source_line_list_iterator != get_line_list().end(); ++source_line_list_iterator) 
        { 
          SgAsmDwarfLine* source_list_element = *source_line_list_iterator; 
          SgAsmDwarfLine* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmDwarfLine*>(help.copyAst(*source_line_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          line_list_copy.push_back(copy_list_element); 
        } 
     result->p_line_list = line_list_copy; 
  // case: listType for line_list
  // case: listType (typeIsPointerToList == false) for line_list
     SgAsmDwarfLinePtrList::const_iterator line_list_iterator = result->get_line_list().begin(); 
     for ( /* empty by design */; line_list_iterator != result->get_line_list().end(); ++line_list_iterator) 
        { 
          SgAsmDwarfLine* list_element = *line_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfLineList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfLineList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 243394 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfMacro::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfMacro* result = NULL;

  // printf ("Copy SgAsmDwarfMacro = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfMacro*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfMacro*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfMacro: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfMacro*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfMacro(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_macro_string
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for macro_string
  // case: toBeCopied == COPY_DATA for macro_string
     std::string macro_string_copy = p_macro_string; 
     result->p_macro_string = macro_string_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfMacro = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfMacro::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 243583 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfMacroList::copy ( SgCopyHelp& help) const
   {
     SgAsmDwarfMacroList* result = NULL;

  // printf ("Copy SgAsmDwarfMacroList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDwarfMacroList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDwarfMacroList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDwarfMacroList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDwarfMacroList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDwarfMacroList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_macro_list
     SgAsmDwarfMacroPtrList macro_list_copy; 
  // case: listType for macro_list
  // case: listType (typeIsPointerToList == false) for macro_list
     SgAsmDwarfMacroPtrList::const_iterator source_macro_list_iterator = get_macro_list().begin(); 
     for ( /* empty by design */; source_macro_list_iterator != get_macro_list().end(); ++source_macro_list_iterator) 
        { 
          SgAsmDwarfMacro* source_list_element = *source_macro_list_iterator; 
          SgAsmDwarfMacro* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmDwarfMacro*>(help.copyAst(*source_macro_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          macro_list_copy.push_back(copy_list_element); 
        } 
     result->p_macro_list = macro_list_copy; 
  // case: listType for macro_list
  // case: listType (typeIsPointerToList == false) for macro_list
     SgAsmDwarfMacroPtrList::const_iterator macro_list_iterator = result->get_macro_list().begin(); 
     for ( /* empty by design */; macro_list_iterator != result->get_macro_list().end(); ++macro_list_iterator) 
        { 
          SgAsmDwarfMacro* list_element = *macro_list_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDwarfMacroList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDwarfMacroList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 243798 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfDynamicEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmElfDynamicEntry* result = NULL;

  // printf ("Copy SgAsmElfDynamicEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfDynamicEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfDynamicEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfDynamicEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfDynamicEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfDynamicEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_d_tag
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for d_tag
  // case: toBeCopied == COPY_DATA for d_tag
     SgAsmElfDynamicEntry::EntryType d_tag_copy = p_d_tag; 
     result->p_d_tag = d_tag_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_d_val
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for d_val
  // case: toBeCopied == COPY_DATA for d_val
     Rose::BinaryAnalysis::RelativeVirtualAddress d_val_copy = p_d_val; 
     result->p_d_val = d_val_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfDynamicEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfDynamicEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 244016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfDynamicEntryList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfDynamicEntryList* result = NULL;

  // printf ("Copy SgAsmElfDynamicEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfDynamicEntryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfDynamicEntryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfDynamicEntryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfDynamicEntryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfDynamicEntryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmElfDynamicEntryPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfDynamicEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfDynamicEntryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 244205 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameEntryCI::copy ( SgCopyHelp& help) const
   {
     SgAsmElfEHFrameEntryCI* result = NULL;

  // printf ("Copy SgAsmElfEHFrameEntryCI = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfEHFrameEntryCI*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfEHFrameEntryCI*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfEHFrameEntryCI: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfEHFrameEntryCI*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfEHFrameEntryCI(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version
  // case: toBeCopied == COPY_DATA for version
     int version_copy = p_version; 
     result->p_version = version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_augmentation_string
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for augmentation_string
  // case: toBeCopied == COPY_DATA for augmentation_string
     std::string augmentation_string_copy = p_augmentation_string; 
     result->p_augmentation_string = augmentation_string_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_eh_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for eh_data
  // case: toBeCopied == COPY_DATA for eh_data
     uint64_t eh_data_copy = p_eh_data; 
     result->p_eh_data = eh_data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_code_alignment_factor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for code_alignment_factor
  // case: toBeCopied == COPY_DATA for code_alignment_factor
     uint64_t code_alignment_factor_copy = p_code_alignment_factor; 
     result->p_code_alignment_factor = code_alignment_factor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data_alignment_factor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_alignment_factor
  // case: toBeCopied == COPY_DATA for data_alignment_factor
     int64_t data_alignment_factor_copy = p_data_alignment_factor; 
     result->p_data_alignment_factor = data_alignment_factor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_augmentation_data_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for augmentation_data_length
  // case: toBeCopied == COPY_DATA for augmentation_data_length
     uint64_t augmentation_data_length_copy = p_augmentation_data_length; 
     result->p_augmentation_data_length = augmentation_data_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lsda_encoding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lsda_encoding
  // case: toBeCopied == COPY_DATA for lsda_encoding
     int lsda_encoding_copy = p_lsda_encoding; 
     result->p_lsda_encoding = lsda_encoding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_prh_encoding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prh_encoding
  // case: toBeCopied == COPY_DATA for prh_encoding
     int prh_encoding_copy = p_prh_encoding; 
     result->p_prh_encoding = prh_encoding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_prh_arg
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prh_arg
  // case: toBeCopied == COPY_DATA for prh_arg
     unsigned prh_arg_copy = p_prh_arg; 
     result->p_prh_arg = prh_arg_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_prh_addr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prh_addr
  // case: toBeCopied == COPY_DATA for prh_addr
     Rose::BinaryAnalysis::Address prh_addr_copy = p_prh_addr; 
     result->p_prh_addr = prh_addr_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_addr_encoding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for addr_encoding
  // case: toBeCopied == COPY_DATA for addr_encoding
     int addr_encoding_copy = p_addr_encoding; 
     result->p_addr_encoding = addr_encoding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sig_frame
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sig_frame
  // case: toBeCopied == COPY_DATA for sig_frame
     bool sig_frame_copy = p_sig_frame; 
     result->p_sig_frame = sig_frame_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_instructions
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for instructions
  // case: toBeCopied == COPY_DATA for instructions
     SgUnsignedCharList instructions_copy = p_instructions; 
     result->p_instructions = instructions_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fd_entries
     SgAsmElfEHFrameEntryFDList* fd_entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fd_entries
          if (get_fd_entries() != NULL) 
             { 
               fd_entries_copy = static_cast<SgAsmElfEHFrameEntryFDList*>(help.copyAst(get_fd_entries())); 
             } 
            else 
             { 
               fd_entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_fd_entries != NULL) delete result->p_fd_entries; 
     result->p_fd_entries = fd_entries_copy; 
  // case: not a listType for (using conditionalToSetParent)fd_entries
          if ( (fd_entries_copy != NULL) && (fd_entries_copy->get_parent() == NULL) && (isSgType(fd_entries_copy) == NULL) ) 
             { 
               fd_entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfEHFrameEntryCI = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfEHFrameEntryCI::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 244473 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameEntryCIList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfEHFrameEntryCIList* result = NULL;

  // printf ("Copy SgAsmElfEHFrameEntryCIList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfEHFrameEntryCIList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfEHFrameEntryCIList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfEHFrameEntryCIList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfEHFrameEntryCIList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfEHFrameEntryCIList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmElfEHFrameEntryCIPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfEHFrameEntryCIList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfEHFrameEntryCIList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 244662 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameEntryFD::copy ( SgCopyHelp& help) const
   {
     SgAsmElfEHFrameEntryFD* result = NULL;

  // printf ("Copy SgAsmElfEHFrameEntryFD = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfEHFrameEntryFD*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfEHFrameEntryFD*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfEHFrameEntryFD: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfEHFrameEntryFD*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfEHFrameEntryFD(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_begin_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for begin_rva
  // case: toBeCopied == COPY_DATA for begin_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress begin_rva_copy = p_begin_rva; 
     result->p_begin_rva = begin_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_augmentation_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for augmentation_data
  // case: toBeCopied == COPY_DATA for augmentation_data
     SgUnsignedCharList augmentation_data_copy = p_augmentation_data; 
     result->p_augmentation_data = augmentation_data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_instructions
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for instructions
  // case: toBeCopied == COPY_DATA for instructions
     SgUnsignedCharList instructions_copy = p_instructions; 
     result->p_instructions = instructions_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfEHFrameEntryFD = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfEHFrameEntryFD::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 244866 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameEntryFDList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfEHFrameEntryFDList* result = NULL;

  // printf ("Copy SgAsmElfEHFrameEntryFDList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfEHFrameEntryFDList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfEHFrameEntryFDList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfEHFrameEntryFDList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfEHFrameEntryFDList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfEHFrameEntryFDList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmElfEHFrameEntryFDPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfEHFrameEntryFDList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfEHFrameEntryFDList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 245055 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfNoteEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmElfNoteEntry* result = NULL;

  // printf ("Copy SgAsmElfNoteEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfNoteEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfNoteEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfNoteEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfNoteEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfNoteEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     unsigned type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_payload
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for payload
  // case: toBeCopied == COPY_DATA for payload
     SgUnsignedCharList payload_copy = p_payload; 
     result->p_payload = payload_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfNoteEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfNoteEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 245268 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfNoteEntryList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfNoteEntryList* result = NULL;

  // printf ("Copy SgAsmElfNoteEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfNoteEntryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfNoteEntryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfNoteEntryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfNoteEntryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfNoteEntryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmElfNoteEntryPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfNoteEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfNoteEntryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 245457 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfRelocEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmElfRelocEntry* result = NULL;

  // printf ("Copy SgAsmElfRelocEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfRelocEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfRelocEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfRelocEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfRelocEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfRelocEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_r_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for r_offset
  // case: toBeCopied == COPY_DATA for r_offset
     Rose::BinaryAnalysis::Address r_offset_copy = p_r_offset; 
     result->p_r_offset = r_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_r_addend
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for r_addend
  // case: toBeCopied == COPY_DATA for r_addend
     Rose::BinaryAnalysis::Address r_addend_copy = p_r_addend; 
     result->p_r_addend = r_addend_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sym
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sym
  // case: toBeCopied == COPY_DATA for sym
     unsigned long sym_copy = p_sym; 
     result->p_sym = sym_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmElfRelocEntry::RelocType type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfRelocEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfRelocEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 245666 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfRelocEntryList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfRelocEntryList* result = NULL;

  // printf ("Copy SgAsmElfRelocEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfRelocEntryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfRelocEntryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfRelocEntryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfRelocEntryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfRelocEntryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfRelocEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfRelocEntryPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmElfRelocEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfRelocEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmElfRelocEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfRelocEntryPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmElfRelocEntry* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfRelocEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfRelocEntryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 245881 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSectionTableEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSectionTableEntry* result = NULL;

  // printf ("Copy SgAsmElfSectionTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSectionTableEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSectionTableEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSectionTableEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSectionTableEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSectionTableEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_sh_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_name
  // case: toBeCopied == COPY_DATA for sh_name
     unsigned sh_name_copy = p_sh_name; 
     result->p_sh_name = sh_name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_type
  // case: toBeCopied == COPY_DATA for sh_type
     SgAsmElfSectionTableEntry::SectionType sh_type_copy = p_sh_type; 
     result->p_sh_type = sh_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_link
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_link
  // case: toBeCopied == COPY_DATA for sh_link
     unsigned long sh_link_copy = p_sh_link; 
     result->p_sh_link = sh_link_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_info
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_info
  // case: toBeCopied == COPY_DATA for sh_info
     unsigned long sh_info_copy = p_sh_info; 
     result->p_sh_info = sh_info_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_flags
  // case: toBeCopied == COPY_DATA for sh_flags
     uint64_t sh_flags_copy = p_sh_flags; 
     result->p_sh_flags = sh_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_addr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_addr
  // case: toBeCopied == COPY_DATA for sh_addr
     Rose::BinaryAnalysis::Address sh_addr_copy = p_sh_addr; 
     result->p_sh_addr = sh_addr_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_offset
  // case: toBeCopied == COPY_DATA for sh_offset
     Rose::BinaryAnalysis::Address sh_offset_copy = p_sh_offset; 
     result->p_sh_offset = sh_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_size
  // case: toBeCopied == COPY_DATA for sh_size
     Rose::BinaryAnalysis::Address sh_size_copy = p_sh_size; 
     result->p_sh_size = sh_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_addralign
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_addralign
  // case: toBeCopied == COPY_DATA for sh_addralign
     Rose::BinaryAnalysis::Address sh_addralign_copy = p_sh_addralign; 
     result->p_sh_addralign = sh_addralign_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sh_entsize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_entsize
  // case: toBeCopied == COPY_DATA for sh_entsize
     Rose::BinaryAnalysis::Address sh_entsize_copy = p_sh_entsize; 
     result->p_sh_entsize = sh_entsize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSectionTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSectionTableEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 246120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSegmentTableEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSegmentTableEntry* result = NULL;

  // printf ("Copy SgAsmElfSegmentTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSegmentTableEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSegmentTableEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSegmentTableEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSegmentTableEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSegmentTableEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     size_t index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmElfSegmentTableEntry::SegmentType type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     SgAsmElfSegmentTableEntry::SegmentFlags flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_vaddr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vaddr
  // case: toBeCopied == COPY_DATA for vaddr
     Rose::BinaryAnalysis::Address vaddr_copy = p_vaddr; 
     result->p_vaddr = vaddr_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_paddr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for paddr
  // case: toBeCopied == COPY_DATA for paddr
     Rose::BinaryAnalysis::Address paddr_copy = p_paddr; 
     result->p_paddr = paddr_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_filesz
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for filesz
  // case: toBeCopied == COPY_DATA for filesz
     Rose::BinaryAnalysis::Address filesz_copy = p_filesz; 
     result->p_filesz = filesz_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_memsz
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for memsz
  // case: toBeCopied == COPY_DATA for memsz
     Rose::BinaryAnalysis::Address memsz_copy = p_memsz; 
     result->p_memsz = memsz_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_align
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for align
  // case: toBeCopied == COPY_DATA for align
     Rose::BinaryAnalysis::Address align_copy = p_align; 
     result->p_align = align_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSegmentTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSegmentTableEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 246354 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSegmentTableEntryList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSegmentTableEntryList* result = NULL;

  // printf ("Copy SgAsmElfSegmentTableEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSegmentTableEntryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSegmentTableEntryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSegmentTableEntryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSegmentTableEntryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSegmentTableEntryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSegmentTableEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSegmentTableEntryPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmElfSegmentTableEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfSegmentTableEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSegmentTableEntryPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmElfSegmentTableEntry* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSegmentTableEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSegmentTableEntryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 246569 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymbolList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymbolList* result = NULL;

  // printf ("Copy SgAsmElfSymbolList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymbolList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymbolList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymbolList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymbolList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymbolList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_symbols
     SgAsmElfSymbolPtrList symbols_copy; 
  // case: listType for symbols
  // case: listType (typeIsPointerToList == false) for symbols
     SgAsmElfSymbolPtrList::const_iterator source_symbols_iterator = get_symbols().begin(); 
     for ( /* empty by design */; source_symbols_iterator != get_symbols().end(); ++source_symbols_iterator) 
        { 
          SgAsmElfSymbol* source_list_element = *source_symbols_iterator; 
          SgAsmElfSymbol* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmElfSymbol*>(help.copyAst(*source_symbols_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          symbols_copy.push_back(copy_list_element); 
        } 
     result->p_symbols = symbols_copy; 
  // case: listType for symbols
  // case: listType (typeIsPointerToList == false) for symbols
     SgAsmElfSymbolPtrList::const_iterator symbols_iterator = result->get_symbols().begin(); 
     for ( /* empty by design */; symbols_iterator != result->get_symbols().end(); ++symbols_iterator) 
        { 
          SgAsmElfSymbol* list_element = *symbols_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymbolList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymbolList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 246784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedAux::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverDefinedAux* result = NULL;

  // printf ("Copy SgAsmElfSymverDefinedAux = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverDefinedAux*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverDefinedAux*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverDefinedAux: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverDefinedAux*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverDefinedAux(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverDefinedAux = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverDefinedAux::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 246987 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedAuxList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverDefinedAuxList* result = NULL;

  // printf ("Copy SgAsmElfSymverDefinedAuxList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverDefinedAuxList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverDefinedAuxList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverDefinedAuxList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverDefinedAuxList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverDefinedAuxList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverDefinedAuxPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverDefinedAuxPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmElfSymverDefinedAux* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverDefinedAux* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverDefinedAux*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverDefinedAuxPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmElfSymverDefinedAux* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverDefinedAuxList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverDefinedAuxList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 247202 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverDefinedEntry* result = NULL;

  // printf ("Copy SgAsmElfSymverDefinedEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverDefinedEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverDefinedEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverDefinedEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverDefinedEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverDefinedEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version
  // case: toBeCopied == COPY_DATA for version
     size_t version_copy = p_version; 
     result->p_version = version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     int flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     size_t index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_hash
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hash
  // case: toBeCopied == COPY_DATA for hash
     uint32_t hash_copy = p_hash; 
     result->p_hash = hash_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverDefinedAuxList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != NULL) 
             { 
               entries_copy = static_cast<SgAsmElfSymverDefinedAuxList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != NULL) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
          if ( (entries_copy != NULL) && (entries_copy->get_parent() == NULL) && (isSgType(entries_copy) == NULL) ) 
             { 
               entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverDefinedEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverDefinedEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 247425 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedEntryList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverDefinedEntryList* result = NULL;

  // printf ("Copy SgAsmElfSymverDefinedEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverDefinedEntryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverDefinedEntryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverDefinedEntryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverDefinedEntryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverDefinedEntryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverDefinedEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverDefinedEntryPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmElfSymverDefinedEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverDefinedEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverDefinedEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverDefinedEntryPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmElfSymverDefinedEntry* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverDefinedEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverDefinedEntryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 247640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverEntry* result = NULL;

  // printf ("Copy SgAsmElfSymverEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     size_t value_copy = p_value; 
     result->p_value = value_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 247829 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverEntryList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverEntryList* result = NULL;

  // printf ("Copy SgAsmElfSymverEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverEntryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverEntryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverEntryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverEntryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverEntryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverEntryPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmElfSymverEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverEntryPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmElfSymverEntry* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverEntryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 248044 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededAux::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverNeededAux* result = NULL;

  // printf ("Copy SgAsmElfSymverNeededAux = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverNeededAux*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverNeededAux*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverNeededAux: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverNeededAux*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverNeededAux(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_hash
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hash
  // case: toBeCopied == COPY_DATA for hash
     uint32_t hash_copy = p_hash; 
     result->p_hash = hash_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     int flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_other
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for other
  // case: toBeCopied == COPY_DATA for other
     size_t other_copy = p_other; 
     result->p_other = other_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverNeededAux = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverNeededAux::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 248262 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededAuxList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverNeededAuxList* result = NULL;

  // printf ("Copy SgAsmElfSymverNeededAuxList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverNeededAuxList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverNeededAuxList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverNeededAuxList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverNeededAuxList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverNeededAuxList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverNeededAuxPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverNeededAuxPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmElfSymverNeededAux* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverNeededAux* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverNeededAux*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverNeededAuxPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmElfSymverNeededAux* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverNeededAuxList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverNeededAuxList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 248477 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverNeededEntry* result = NULL;

  // printf ("Copy SgAsmElfSymverNeededEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverNeededEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverNeededEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverNeededEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverNeededEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverNeededEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version
  // case: toBeCopied == COPY_DATA for version
     size_t version_copy = p_version; 
     result->p_version = version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileName
     SgAsmGenericString* fileName_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fileName
          if (get_fileName() != NULL) 
             { 
               fileName_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_fileName())); 
             } 
            else 
             { 
               fileName_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_fileName != NULL) delete result->p_fileName; 
     result->p_fileName = fileName_copy; 
  // case: not a listType for (using conditionalToSetParent)fileName
          if ( (fileName_copy != NULL) && (fileName_copy->get_parent() == NULL) && (isSgType(fileName_copy) == NULL) ) 
             { 
               fileName_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverNeededAuxList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != NULL) 
             { 
               entries_copy = static_cast<SgAsmElfSymverNeededAuxList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != NULL) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
          if ( (entries_copy != NULL) && (entries_copy->get_parent() == NULL) && (isSgType(entries_copy) == NULL) ) 
             { 
               entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverNeededEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverNeededEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 248704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededEntryList::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverNeededEntryList* result = NULL;

  // printf ("Copy SgAsmElfSymverNeededEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverNeededEntryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverNeededEntryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverNeededEntryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverNeededEntryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverNeededEntryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverNeededEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverNeededEntryPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmElfSymverNeededEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverNeededEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverNeededEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmElfSymverNeededEntryPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmElfSymverNeededEntry* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverNeededEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverNeededEntryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 248919 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericDLL::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericDLL* result = NULL;

  // printf ("Copy SgAsmGenericDLL = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericDLL*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericDLL*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericDLL: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericDLL*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericDLL(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_symbols
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbols
  // case: toBeCopied == COPY_DATA for symbols
     SgStringList symbols_copy = p_symbols; 
     result->p_symbols = symbols_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericDLL = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericDLL::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 249127 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericDLLList::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericDLLList* result = NULL;

  // printf ("Copy SgAsmGenericDLLList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericDLLList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericDLLList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericDLLList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericDLLList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericDLLList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_dlls
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dlls
  // case: toBeCopied == COPY_DATA for dlls
     SgAsmGenericDLLPtrList dlls_copy = p_dlls; 
     result->p_dlls = dlls_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericDLLList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericDLLList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 249316 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericFile::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericFile* result = NULL;

  // printf ("Copy SgAsmGenericFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericFile*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericFile*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericFile: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericFile*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericFile(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_dwarfInfo
     SgAsmDwarfCompilationUnitList* dwarfInfo_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dwarfInfo
          if (get_dwarfInfo() != NULL) 
             { 
               dwarfInfo_copy = static_cast<SgAsmDwarfCompilationUnitList*>(help.copyAst(get_dwarfInfo())); 
             } 
            else 
             { 
               dwarfInfo_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dwarfInfo != NULL) delete result->p_dwarfInfo; 
     result->p_dwarfInfo = dwarfInfo_copy; 
  // case: not a listType for (using conditionalToSetParent)dwarfInfo
          if ( (dwarfInfo_copy != NULL) && (dwarfInfo_copy->get_parent() == NULL) && (isSgType(dwarfInfo_copy) == NULL) ) 
             { 
               dwarfInfo_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fd
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fd
  // case: toBeCopied == COPY_DATA for fd
     int fd_copy = p_fd; 
     result->p_fd = fd_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sb
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sb
  // case: toBeCopied == COPY_DATA for sb
     struct stat sb_copy = p_sb; 
     result->p_sb = sb_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_headers
     SgAsmGenericHeaderList* headers_copy; 
  // case: not a listType for (using conditionalToCopyVariable)headers
          if (get_headers() != NULL) 
             { 
               headers_copy = static_cast<SgAsmGenericHeaderList*>(help.copyAst(get_headers())); 
             } 
            else 
             { 
               headers_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_headers != NULL) delete result->p_headers; 
     result->p_headers = headers_copy; 
  // case: not a listType for (using conditionalToSetParent)headers
          if ( (headers_copy != NULL) && (headers_copy->get_parent() == NULL) && (isSgType(headers_copy) == NULL) ) 
             { 
               headers_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_holes
     SgAsmGenericSectionList* holes_copy; 
  // case: not a listType for (using conditionalToCopyVariable)holes
          if (get_holes() != NULL) 
             { 
               holes_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_holes())); 
             } 
            else 
             { 
               holes_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_holes != NULL) delete result->p_holes; 
     result->p_holes = holes_copy; 
  // case: not a listType for (using conditionalToSetParent)holes
          if ( (holes_copy != NULL) && (holes_copy->get_parent() == NULL) && (isSgType(holes_copy) == NULL) ) 
             { 
               holes_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_truncateZeros
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for truncateZeros
  // case: toBeCopied == COPY_DATA for truncateZeros
     bool truncateZeros_copy = p_truncateZeros; 
     result->p_truncateZeros = truncateZeros_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_trackingReferences
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for trackingReferences
  // case: toBeCopied == COPY_DATA for trackingReferences
     bool trackingReferences_copy = p_trackingReferences; 
     result->p_trackingReferences = trackingReferences_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_referencedExtents
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for referencedExtents
  // case: toBeCopied == COPY_DATA for referencedExtents
     Rose::BinaryAnalysis::AddressIntervalSet referencedExtents_copy = p_referencedExtents; 
     result->p_referencedExtents = referencedExtents_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_neuter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for neuter
  // case: toBeCopied == COPY_DATA for neuter
     bool neuter_copy = p_neuter; 
     result->p_neuter = neuter_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericFile::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 249597 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericFormat::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericFormat* result = NULL;

  // printf ("Copy SgAsmGenericFormat = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericFormat*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericFormat*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericFormat: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericFormat*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericFormat(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_family
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for family
  // case: toBeCopied == COPY_DATA for family
     SgAsmGenericFormat::ExecFamily family_copy = p_family; 
     result->p_family = family_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericFormat::ExecPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sex
  // case: toBeCopied == COPY_DATA for sex
     Rose::BinaryAnalysis::ByteOrder::Endianness sex_copy = p_sex; 
     result->p_sex = sex_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version
  // case: toBeCopied == COPY_DATA for version
     unsigned version_copy = p_version; 
     result->p_version = version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isCurrentVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCurrentVersion
  // case: toBeCopied == COPY_DATA for isCurrentVersion
     bool isCurrentVersion_copy = p_isCurrentVersion; 
     result->p_isCurrentVersion = isCurrentVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_abi
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for abi
  // case: toBeCopied == COPY_DATA for abi
     SgAsmGenericFormat::ExecABI abi_copy = p_abi; 
     result->p_abi = abi_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_abiVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for abiVersion
  // case: toBeCopied == COPY_DATA for abiVersion
     unsigned abiVersion_copy = p_abiVersion; 
     result->p_abiVersion = abiVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_wordSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wordSize
  // case: toBeCopied == COPY_DATA for wordSize
     size_t wordSize_copy = p_wordSize; 
     result->p_wordSize = wordSize_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericFormat = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericFormat::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 249821 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericHeaderList::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericHeaderList* result = NULL;

  // printf ("Copy SgAsmGenericHeaderList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericHeaderList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericHeaderList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericHeaderList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericHeaderList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericHeaderList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_headers
     SgAsmGenericHeaderPtrList headers_copy; 
  // case: listType for headers
  // case: listType (typeIsPointerToList == false) for headers
     SgAsmGenericHeaderPtrList::const_iterator source_headers_iterator = get_headers().begin(); 
     for ( /* empty by design */; source_headers_iterator != get_headers().end(); ++source_headers_iterator) 
        { 
          SgAsmGenericHeader* source_list_element = *source_headers_iterator; 
          SgAsmGenericHeader* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmGenericHeader*>(help.copyAst(*source_headers_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          headers_copy.push_back(copy_list_element); 
        } 
     result->p_headers = headers_copy; 
  // case: listType for headers
  // case: listType (typeIsPointerToList == false) for headers
     SgAsmGenericHeaderPtrList::const_iterator headers_iterator = result->get_headers().begin(); 
     for ( /* empty by design */; headers_iterator != result->get_headers().end(); ++headers_iterator) 
        { 
          SgAsmGenericHeader* list_element = *headers_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericHeaderList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericHeaderList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 250036 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericSection::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericSection* result = NULL;

  // printf ("Copy SgAsmGenericSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 250315 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCliHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmCliHeader* result = NULL;

  // printf ("Copy SgAsmCliHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCliHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCliHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCliHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCliHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCliHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_cb
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cb
  // case: toBeCopied == COPY_DATA for cb
     uint32_t cb_copy = p_cb; 
     result->p_cb = cb_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorRuntimeVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorRuntimeVersion
  // case: toBeCopied == COPY_DATA for majorRuntimeVersion
     uint16_t majorRuntimeVersion_copy = p_majorRuntimeVersion; 
     result->p_majorRuntimeVersion = majorRuntimeVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_minorRuntimeVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorRuntimeVersion
  // case: toBeCopied == COPY_DATA for minorRuntimeVersion
     uint16_t minorRuntimeVersion_copy = p_minorRuntimeVersion; 
     result->p_minorRuntimeVersion = minorRuntimeVersion_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_metaData
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for metaData
  // case: toBeCopied == COPY_DATA for metaData
     uint64_t metaData_copy = p_metaData; 
     result->p_metaData = metaData_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     uint32_t flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryPointToken
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryPointToken
  // case: toBeCopied == COPY_DATA for entryPointToken
     uint32_t entryPointToken_copy = p_entryPointToken; 
     result->p_entryPointToken = entryPointToken_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_resources
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for resources
  // case: toBeCopied == COPY_DATA for resources
     uint64_t resources_copy = p_resources; 
     result->p_resources = resources_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_strongNameSignature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strongNameSignature
  // case: toBeCopied == COPY_DATA for strongNameSignature
     uint64_t strongNameSignature_copy = p_strongNameSignature; 
     result->p_strongNameSignature = strongNameSignature_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_codeManagerTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for codeManagerTable
  // case: toBeCopied == COPY_DATA for codeManagerTable
     uint64_t codeManagerTable_copy = p_codeManagerTable; 
     result->p_codeManagerTable = codeManagerTable_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_vTableFixups
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vTableFixups
  // case: toBeCopied == COPY_DATA for vTableFixups
     uint64_t vTableFixups_copy = p_vTableFixups; 
     result->p_vTableFixups = vTableFixups_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_exportAddressTableJumps
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exportAddressTableJumps
  // case: toBeCopied == COPY_DATA for exportAddressTableJumps
     uint64_t exportAddressTableJumps_copy = p_exportAddressTableJumps; 
     result->p_exportAddressTableJumps = exportAddressTableJumps_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_managedNativeHeader
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for managedNativeHeader
  // case: toBeCopied == COPY_DATA for managedNativeHeader
     uint64_t managedNativeHeader_copy = p_managedNativeHeader; 
     result->p_managedNativeHeader = managedNativeHeader_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_metadataRoot
     SgAsmCilMetadataRoot* metadataRoot_copy; 
  // case: not a listType for (using conditionalToCopyVariable)metadataRoot
          if (get_metadataRoot() != NULL) 
             { 
               metadataRoot_copy = static_cast<SgAsmCilMetadataRoot*>(help.copyAst(get_metadataRoot())); 
             } 
            else 
             { 
               metadataRoot_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_metadataRoot != NULL) delete result->p_metadataRoot; 
     result->p_metadataRoot = metadataRoot_copy; 
  // case: not a listType for (using conditionalToSetParent)metadataRoot
          if ( (metadataRoot_copy != NULL) && (metadataRoot_copy->get_parent() == NULL) && (isSgType(metadataRoot_copy) == NULL) ) 
             { 
               metadataRoot_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCliHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCliHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 250673 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCoffSymbolTable::copy ( SgCopyHelp& help) const
   {
     SgAsmCoffSymbolTable* result = NULL;

  // printf ("Copy SgAsmCoffSymbolTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCoffSymbolTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCoffSymbolTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCoffSymbolTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCoffSymbolTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCoffSymbolTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_strtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strtab
  // case: toBeCopied == COPY_DATA for strtab
     SgAsmGenericSection* strtab_copy = p_strtab; 
     result->p_strtab = strtab_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_symbols
     SgAsmCoffSymbolList* symbols_copy; 
  // case: not a listType for (using conditionalToCopyVariable)symbols
          if (get_symbols() != NULL) 
             { 
               symbols_copy = static_cast<SgAsmCoffSymbolList*>(help.copyAst(get_symbols())); 
             } 
            else 
             { 
               symbols_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_symbols != NULL) delete result->p_symbols; 
     result->p_symbols = symbols_copy; 
  // case: not a listType for (using conditionalToSetParent)symbols
          if ( (symbols_copy != NULL) && (symbols_copy->get_parent() == NULL) && (isSgType(symbols_copy) == NULL) ) 
             { 
               symbols_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCoffSymbolTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCoffSymbolTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 250976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDOSExtendedHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmDOSExtendedHeader* result = NULL;

  // printf ("Copy SgAsmDOSExtendedHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDOSExtendedHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDOSExtendedHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDOSExtendedHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDOSExtendedHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDOSExtendedHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res1
  // case: toBeCopied == COPY_DATA for e_res1
     unsigned e_res1_copy = p_e_res1; 
     result->p_e_res1 = e_res1_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_oemid
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_oemid
  // case: toBeCopied == COPY_DATA for e_oemid
     unsigned e_oemid_copy = p_e_oemid; 
     result->p_e_oemid = e_oemid_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_oeminfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_oeminfo
  // case: toBeCopied == COPY_DATA for e_oeminfo
     unsigned e_oeminfo_copy = p_e_oeminfo; 
     result->p_e_oeminfo = e_oeminfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_res2
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res2
  // case: toBeCopied == COPY_DATA for e_res2
     unsigned e_res2_copy = p_e_res2; 
     result->p_e_res2 = e_res2_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_res3
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res3
  // case: toBeCopied == COPY_DATA for e_res3
     unsigned e_res3_copy = p_e_res3; 
     result->p_e_res3 = e_res3_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_res4
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res4
  // case: toBeCopied == COPY_DATA for e_res4
     unsigned e_res4_copy = p_e_res4; 
     result->p_e_res4 = e_res4_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_res5
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res5
  // case: toBeCopied == COPY_DATA for e_res5
     unsigned e_res5_copy = p_e_res5; 
     result->p_e_res5 = e_res5_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_res6
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res6
  // case: toBeCopied == COPY_DATA for e_res6
     unsigned e_res6_copy = p_e_res6; 
     result->p_e_res6 = e_res6_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_lfanew
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_lfanew
  // case: toBeCopied == COPY_DATA for e_lfanew
     Rose::BinaryAnalysis::Address e_lfanew_copy = p_e_lfanew; 
     result->p_e_lfanew = e_lfanew_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDOSExtendedHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDOSExtendedHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 251300 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSection* result = NULL;

  // printf ("Copy SgAsmElfSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 251622 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfDynamicSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfDynamicSection* result = NULL;

  // printf ("Copy SgAsmElfDynamicSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfDynamicSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfDynamicSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfDynamicSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfDynamicSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfDynamicSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfDynamicEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != NULL) 
             { 
               entries_copy = static_cast<SgAsmElfDynamicEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != NULL) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
          if ( (entries_copy != NULL) && (entries_copy->get_parent() == NULL) && (isSgType(entries_copy) == NULL) ) 
             { 
               entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfDynamicSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfDynamicSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 251963 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfEHFrameSection* result = NULL;

  // printf ("Copy SgAsmElfEHFrameSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfEHFrameSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfEHFrameSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfEHFrameSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfEHFrameSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfEHFrameSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ci_entries
     SgAsmElfEHFrameEntryCIList* ci_entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ci_entries
          if (get_ci_entries() != NULL) 
             { 
               ci_entries_copy = static_cast<SgAsmElfEHFrameEntryCIList*>(help.copyAst(get_ci_entries())); 
             } 
            else 
             { 
               ci_entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ci_entries != NULL) delete result->p_ci_entries; 
     result->p_ci_entries = ci_entries_copy; 
  // case: not a listType for (using conditionalToSetParent)ci_entries
          if ( (ci_entries_copy != NULL) && (ci_entries_copy->get_parent() == NULL) && (isSgType(ci_entries_copy) == NULL) ) 
             { 
               ci_entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfEHFrameSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfEHFrameSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 252304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfNoteSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfNoteSection* result = NULL;

  // printf ("Copy SgAsmElfNoteSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfNoteSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfNoteSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfNoteSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfNoteSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfNoteSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfNoteEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != NULL) 
             { 
               entries_copy = static_cast<SgAsmElfNoteEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != NULL) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
          if ( (entries_copy != NULL) && (entries_copy->get_parent() == NULL) && (isSgType(entries_copy) == NULL) ) 
             { 
               entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfNoteSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfNoteSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 252645 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfRelocSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfRelocSection* result = NULL;

  // printf ("Copy SgAsmElfRelocSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfRelocSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfRelocSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfRelocSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfRelocSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfRelocSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_usesAddend
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usesAddend
  // case: toBeCopied == COPY_DATA for usesAddend
     bool usesAddend_copy = p_usesAddend; 
     result->p_usesAddend = usesAddend_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_targetSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for targetSection
  // case: toBeCopied == COPY_DATA for targetSection
     SgAsmElfSection* targetSection_copy = p_targetSection; 
     result->p_targetSection = targetSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfRelocEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != NULL) 
             { 
               entries_copy = static_cast<SgAsmElfRelocEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != NULL) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
          if ( (entries_copy != NULL) && (entries_copy->get_parent() == NULL) && (isSgType(entries_copy) == NULL) ) 
             { 
               entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfRelocSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfRelocSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 252996 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfStringSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfStringSection* result = NULL;

  // printf ("Copy SgAsmElfStringSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfStringSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfStringSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfStringSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfStringSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfStringSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_strtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strtab
  // case: toBeCopied == COPY_DATA for strtab
     SgAsmElfStrtab* strtab_copy = p_strtab; 
     result->p_strtab = strtab_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfStringSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfStringSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 253323 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymbolSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymbolSection* result = NULL;

  // printf ("Copy SgAsmElfSymbolSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymbolSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymbolSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymbolSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymbolSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymbolSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_isDynamic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDynamic
  // case: toBeCopied == COPY_DATA for isDynamic
     bool isDynamic_copy = p_isDynamic; 
     result->p_isDynamic = isDynamic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_symbols
     SgAsmElfSymbolList* symbols_copy; 
  // case: not a listType for (using conditionalToCopyVariable)symbols
          if (get_symbols() != NULL) 
             { 
               symbols_copy = static_cast<SgAsmElfSymbolList*>(help.copyAst(get_symbols())); 
             } 
            else 
             { 
               symbols_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_symbols != NULL) delete result->p_symbols; 
     result->p_symbols = symbols_copy; 
  // case: not a listType for (using conditionalToSetParent)symbols
          if ( (symbols_copy != NULL) && (symbols_copy->get_parent() == NULL) && (isSgType(symbols_copy) == NULL) ) 
             { 
               symbols_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymbolSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymbolSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 253669 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverDefinedSection* result = NULL;

  // printf ("Copy SgAsmElfSymverDefinedSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverDefinedSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverDefinedSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverDefinedSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverDefinedSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverDefinedSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverDefinedEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != NULL) 
             { 
               entries_copy = static_cast<SgAsmElfSymverDefinedEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != NULL) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
          if ( (entries_copy != NULL) && (entries_copy->get_parent() == NULL) && (isSgType(entries_copy) == NULL) ) 
             { 
               entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverDefinedSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverDefinedSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 254010 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverNeededSection* result = NULL;

  // printf ("Copy SgAsmElfSymverNeededSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverNeededSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverNeededSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverNeededSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverNeededSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverNeededSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverNeededEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != NULL) 
             { 
               entries_copy = static_cast<SgAsmElfSymverNeededEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != NULL) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
          if ( (entries_copy != NULL) && (entries_copy->get_parent() == NULL) && (isSgType(entries_copy) == NULL) ) 
             { 
               entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverNeededSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverNeededSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 254351 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverSection::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymverSection* result = NULL;

  // printf ("Copy SgAsmElfSymverSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymverSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymverSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymverSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymverSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymverSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != NULL) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != NULL) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
          if ( (sectionEntry_copy != NULL) && (sectionEntry_copy->get_parent() == NULL) && (isSgType(sectionEntry_copy) == NULL) ) 
             { 
               sectionEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != NULL) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != NULL) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
          if ( (segmentEntry_copy != NULL) && (segmentEntry_copy->get_parent() == NULL) && (isSgType(segmentEntry_copy) == NULL) ) 
             { 
               segmentEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != NULL) 
             { 
               entries_copy = static_cast<SgAsmElfSymverEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != NULL) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
          if ( (entries_copy != NULL) && (entries_copy->get_parent() == NULL) && (isSgType(entries_copy) == NULL) ) 
             { 
               entries_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymverSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymverSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 254692 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSectionTable::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSectionTable* result = NULL;

  // printf ("Copy SgAsmElfSectionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSectionTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSectionTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSectionTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSectionTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSectionTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSectionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSectionTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 254971 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSegmentTable::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSegmentTable* result = NULL;

  // printf ("Copy SgAsmElfSegmentTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSegmentTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSegmentTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSegmentTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSegmentTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSegmentTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSegmentTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSegmentTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 255250 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericHeader* result = NULL;

  // printf ("Copy SgAsmGenericHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != NULL) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != NULL) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
          if ( (executableFormat_copy != NULL) && (executableFormat_copy->get_parent() == NULL) && (isSgType(executableFormat_copy) == NULL) ) 
             { 
               executableFormat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != NULL) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != NULL) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
          if ( (dlls_copy != NULL) && (dlls_copy->get_parent() == NULL) && (isSgType(dlls_copy) == NULL) ) 
             { 
               dlls_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != NULL) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != NULL) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
          if ( (sections_copy != NULL) && (sections_copy->get_parent() == NULL) && (isSgType(sections_copy) == NULL) ) 
             { 
               sections_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 255606 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDOSFileHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmDOSFileHeader* result = NULL;

  // printf ("Copy SgAsmDOSFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDOSFileHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDOSFileHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDOSFileHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDOSFileHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDOSFileHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != NULL) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != NULL) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
          if ( (executableFormat_copy != NULL) && (executableFormat_copy->get_parent() == NULL) && (isSgType(executableFormat_copy) == NULL) ) 
             { 
               executableFormat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != NULL) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != NULL) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
          if ( (dlls_copy != NULL) && (dlls_copy->get_parent() == NULL) && (isSgType(dlls_copy) == NULL) ) 
             { 
               dlls_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != NULL) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != NULL) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
          if ( (sections_copy != NULL) && (sections_copy->get_parent() == NULL) && (isSgType(sections_copy) == NULL) ) 
             { 
               sections_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_e_last_page_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_last_page_size
  // case: toBeCopied == COPY_DATA for e_last_page_size
     uint16_t e_last_page_size_copy = p_e_last_page_size; 
     result->p_e_last_page_size = e_last_page_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_total_pages
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_total_pages
  // case: toBeCopied == COPY_DATA for e_total_pages
     uint16_t e_total_pages_copy = p_e_total_pages; 
     result->p_e_total_pages = e_total_pages_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nrelocs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nrelocs
  // case: toBeCopied == COPY_DATA for e_nrelocs
     uint16_t e_nrelocs_copy = p_e_nrelocs; 
     result->p_e_nrelocs = e_nrelocs_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_header_paragraphs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_header_paragraphs
  // case: toBeCopied == COPY_DATA for e_header_paragraphs
     uint16_t e_header_paragraphs_copy = p_e_header_paragraphs; 
     result->p_e_header_paragraphs = e_header_paragraphs_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_minalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_minalloc
  // case: toBeCopied == COPY_DATA for e_minalloc
     uint16_t e_minalloc_copy = p_e_minalloc; 
     result->p_e_minalloc = e_minalloc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_maxalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_maxalloc
  // case: toBeCopied == COPY_DATA for e_maxalloc
     uint16_t e_maxalloc_copy = p_e_maxalloc; 
     result->p_e_maxalloc = e_maxalloc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_ss
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ss
  // case: toBeCopied == COPY_DATA for e_ss
     uint16_t e_ss_copy = p_e_ss; 
     result->p_e_ss = e_ss_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_sp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_sp
  // case: toBeCopied == COPY_DATA for e_sp
     uint16_t e_sp_copy = p_e_sp; 
     result->p_e_sp = e_sp_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_cksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_cksum
  // case: toBeCopied == COPY_DATA for e_cksum
     uint16_t e_cksum_copy = p_e_cksum; 
     result->p_e_cksum = e_cksum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_ip
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ip
  // case: toBeCopied == COPY_DATA for e_ip
     uint16_t e_ip_copy = p_e_ip; 
     result->p_e_ip = e_ip_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_cs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_cs
  // case: toBeCopied == COPY_DATA for e_cs
     uint16_t e_cs_copy = p_e_cs; 
     result->p_e_cs = e_cs_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_overlay
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_overlay
  // case: toBeCopied == COPY_DATA for e_overlay
     uint16_t e_overlay_copy = p_e_overlay; 
     result->p_e_overlay = e_overlay_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_relocs_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_relocs_offset
  // case: toBeCopied == COPY_DATA for e_relocs_offset
     Rose::BinaryAnalysis::Address e_relocs_offset_copy = p_e_relocs_offset; 
     result->p_e_relocs_offset = e_relocs_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res1
  // case: toBeCopied == COPY_DATA for e_res1
     unsigned e_res1_copy = p_e_res1; 
     result->p_e_res1 = e_res1_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_relocs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relocs
  // case: toBeCopied == COPY_DATA for relocs
     SgAsmGenericSection* relocs_copy = p_relocs; 
     result->p_relocs = relocs_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rm_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rm_section
  // case: toBeCopied == COPY_DATA for rm_section
     SgAsmGenericSection* rm_section_copy = p_rm_section; 
     result->p_rm_section = rm_section_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDOSFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDOSFileHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 256042 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfFileHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmElfFileHeader* result = NULL;

  // printf ("Copy SgAsmElfFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfFileHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfFileHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfFileHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfFileHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfFileHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != NULL) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != NULL) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
          if ( (executableFormat_copy != NULL) && (executableFormat_copy->get_parent() == NULL) && (isSgType(executableFormat_copy) == NULL) ) 
             { 
               executableFormat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != NULL) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != NULL) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
          if ( (dlls_copy != NULL) && (dlls_copy->get_parent() == NULL) && (isSgType(dlls_copy) == NULL) ) 
             { 
               dlls_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != NULL) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != NULL) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
          if ( (sections_copy != NULL) && (sections_copy->get_parent() == NULL) && (isSgType(sections_copy) == NULL) ) 
             { 
               sections_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_e_ident_file_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ident_file_class
  // case: toBeCopied == COPY_DATA for e_ident_file_class
     unsigned char e_ident_file_class_copy = p_e_ident_file_class; 
     result->p_e_ident_file_class = e_ident_file_class_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_ident_data_encoding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ident_data_encoding
  // case: toBeCopied == COPY_DATA for e_ident_data_encoding
     unsigned char e_ident_data_encoding_copy = p_e_ident_data_encoding; 
     result->p_e_ident_data_encoding = e_ident_data_encoding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_ident_file_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ident_file_version
  // case: toBeCopied == COPY_DATA for e_ident_file_version
     unsigned char e_ident_file_version_copy = p_e_ident_file_version; 
     result->p_e_ident_file_version = e_ident_file_version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_ident_padding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ident_padding
  // case: toBeCopied == COPY_DATA for e_ident_padding
     SgUnsignedCharList e_ident_padding_copy = p_e_ident_padding; 
     result->p_e_ident_padding = e_ident_padding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_type
  // case: toBeCopied == COPY_DATA for e_type
     unsigned long e_type_copy = p_e_type; 
     result->p_e_type = e_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_machine
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_machine
  // case: toBeCopied == COPY_DATA for e_machine
     unsigned long e_machine_copy = p_e_machine; 
     result->p_e_machine = e_machine_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags
  // case: toBeCopied == COPY_DATA for e_flags
     unsigned long e_flags_copy = p_e_flags; 
     result->p_e_flags = e_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_ehsize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ehsize
  // case: toBeCopied == COPY_DATA for e_ehsize
     unsigned long e_ehsize_copy = p_e_ehsize; 
     result->p_e_ehsize = e_ehsize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_phextrasz
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for phextrasz
  // case: toBeCopied == COPY_DATA for phextrasz
     unsigned long phextrasz_copy = p_phextrasz; 
     result->p_phextrasz = phextrasz_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_phnum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_phnum
  // case: toBeCopied == COPY_DATA for e_phnum
     unsigned long e_phnum_copy = p_e_phnum; 
     result->p_e_phnum = e_phnum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shextrasz
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shextrasz
  // case: toBeCopied == COPY_DATA for shextrasz
     unsigned long shextrasz_copy = p_shextrasz; 
     result->p_shextrasz = shextrasz_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_shnum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_shnum
  // case: toBeCopied == COPY_DATA for e_shnum
     unsigned long e_shnum_copy = p_e_shnum; 
     result->p_e_shnum = e_shnum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_shstrndx
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_shstrndx
  // case: toBeCopied == COPY_DATA for e_shstrndx
     unsigned long e_shstrndx_copy = p_e_shstrndx; 
     result->p_e_shstrndx = e_shstrndx_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionTable
  // case: toBeCopied == COPY_DATA for sectionTable
     SgAsmElfSectionTable* sectionTable_copy = p_sectionTable; 
     result->p_sectionTable = sectionTable_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_segmentTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for segmentTable
  // case: toBeCopied == COPY_DATA for segmentTable
     SgAsmElfSegmentTable* segmentTable_copy = p_segmentTable; 
     result->p_segmentTable = segmentTable_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfFileHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 256473 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmFileHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmFileHeader* result = NULL;

  // printf ("Copy SgAsmJvmFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmFileHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmFileHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmFileHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmFileHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmFileHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != NULL) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != NULL) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
          if ( (executableFormat_copy != NULL) && (executableFormat_copy->get_parent() == NULL) && (isSgType(executableFormat_copy) == NULL) ) 
             { 
               executableFormat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != NULL) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != NULL) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
          if ( (dlls_copy != NULL) && (dlls_copy->get_parent() == NULL) && (isSgType(dlls_copy) == NULL) ) 
             { 
               dlls_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != NULL) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != NULL) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
          if ( (sections_copy != NULL) && (sections_copy->get_parent() == NULL) && (isSgType(sections_copy) == NULL) ) 
             { 
               sections_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_minor_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minor_version
  // case: toBeCopied == COPY_DATA for minor_version
     uint16_t minor_version_copy = p_minor_version; 
     result->p_minor_version = minor_version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_major_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for major_version
  // case: toBeCopied == COPY_DATA for major_version
     uint16_t major_version_copy = p_major_version; 
     result->p_major_version = major_version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_flags
  // case: toBeCopied == COPY_DATA for access_flags
     uint16_t access_flags_copy = p_access_flags; 
     result->p_access_flags = access_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_this_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for this_class
  // case: toBeCopied == COPY_DATA for this_class
     uint16_t this_class_copy = p_this_class; 
     result->p_this_class = this_class_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_super_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for super_class
  // case: toBeCopied == COPY_DATA for super_class
     uint16_t super_class_copy = p_super_class; 
     result->p_super_class = super_class_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_constant_pool
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constant_pool
  // case: toBeCopied == COPY_DATA for constant_pool
     SgAsmJvmConstantPool* constant_pool_copy = p_constant_pool; 
     result->p_constant_pool = constant_pool_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_interfaces
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for interfaces
  // case: toBeCopied == COPY_DATA for interfaces
     std::list<uint16_t> interfaces_copy = p_interfaces; 
     result->p_interfaces = interfaces_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_field_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for field_table
  // case: toBeCopied == COPY_DATA for field_table
     SgAsmJvmFieldTable* field_table_copy = p_field_table; 
     result->p_field_table = field_table_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_method_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for method_table
  // case: toBeCopied == COPY_DATA for method_table
     SgAsmJvmMethodTable* method_table_copy = p_method_table; 
     result->p_method_table = method_table_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_table
  // case: toBeCopied == COPY_DATA for attribute_table
     SgAsmJvmAttributeTable* attribute_table_copy = p_attribute_table; 
     result->p_attribute_table = attribute_table_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmFileHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 256879 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEFileHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmLEFileHeader* result = NULL;

  // printf ("Copy SgAsmLEFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLEFileHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLEFileHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLEFileHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLEFileHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLEFileHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != NULL) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != NULL) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
          if ( (executableFormat_copy != NULL) && (executableFormat_copy->get_parent() == NULL) && (isSgType(executableFormat_copy) == NULL) ) 
             { 
               executableFormat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != NULL) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != NULL) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
          if ( (dlls_copy != NULL) && (dlls_copy->get_parent() == NULL) && (isSgType(dlls_copy) == NULL) ) 
             { 
               dlls_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != NULL) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != NULL) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
          if ( (sections_copy != NULL) && (sections_copy->get_parent() == NULL) && (isSgType(sections_copy) == NULL) ) 
             { 
               sections_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_e_byte_order
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_byte_order
  // case: toBeCopied == COPY_DATA for e_byte_order
     unsigned e_byte_order_copy = p_e_byte_order; 
     result->p_e_byte_order = e_byte_order_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_word_order
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_word_order
  // case: toBeCopied == COPY_DATA for e_word_order
     unsigned e_word_order_copy = p_e_word_order; 
     result->p_e_word_order = e_word_order_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_format_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_format_level
  // case: toBeCopied == COPY_DATA for e_format_level
     unsigned e_format_level_copy = p_e_format_level; 
     result->p_e_format_level = e_format_level_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_cpu_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_cpu_type
  // case: toBeCopied == COPY_DATA for e_cpu_type
     unsigned e_cpu_type_copy = p_e_cpu_type; 
     result->p_e_cpu_type = e_cpu_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_os_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_os_type
  // case: toBeCopied == COPY_DATA for e_os_type
     unsigned e_os_type_copy = p_e_os_type; 
     result->p_e_os_type = e_os_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_module_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_module_version
  // case: toBeCopied == COPY_DATA for e_module_version
     unsigned e_module_version_copy = p_e_module_version; 
     result->p_e_module_version = e_module_version_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags
  // case: toBeCopied == COPY_DATA for e_flags
     unsigned e_flags_copy = p_e_flags; 
     result->p_e_flags = e_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_eip_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_eip_section
  // case: toBeCopied == COPY_DATA for e_eip_section
     unsigned e_eip_section_copy = p_e_eip_section; 
     result->p_e_eip_section = e_eip_section_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_esp_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_esp_section
  // case: toBeCopied == COPY_DATA for e_esp_section
     unsigned e_esp_section_copy = p_e_esp_section; 
     result->p_e_esp_section = e_esp_section_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_last_page_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_last_page_size
  // case: toBeCopied == COPY_DATA for e_last_page_size
     unsigned e_last_page_size_copy = p_e_last_page_size; 
     result->p_e_last_page_size = e_last_page_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_page_offset_shift
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_page_offset_shift
  // case: toBeCopied == COPY_DATA for e_page_offset_shift
     unsigned e_page_offset_shift_copy = p_e_page_offset_shift; 
     result->p_e_page_offset_shift = e_page_offset_shift_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_fixup_sect_cksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fixup_sect_cksum
  // case: toBeCopied == COPY_DATA for e_fixup_sect_cksum
     unsigned e_fixup_sect_cksum_copy = p_e_fixup_sect_cksum; 
     result->p_e_fixup_sect_cksum = e_fixup_sect_cksum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_loader_sect_cksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_loader_sect_cksum
  // case: toBeCopied == COPY_DATA for e_loader_sect_cksum
     unsigned e_loader_sect_cksum_copy = p_e_loader_sect_cksum; 
     result->p_e_loader_sect_cksum = e_loader_sect_cksum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_secttab_nentries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_secttab_nentries
  // case: toBeCopied == COPY_DATA for e_secttab_nentries
     unsigned e_secttab_nentries_copy = p_e_secttab_nentries; 
     result->p_e_secttab_nentries = e_secttab_nentries_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_rsrctab_nentries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_rsrctab_nentries
  // case: toBeCopied == COPY_DATA for e_rsrctab_nentries
     unsigned e_rsrctab_nentries_copy = p_e_rsrctab_nentries; 
     result->p_e_rsrctab_nentries = e_rsrctab_nentries_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_fmtdirtab_nentries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fmtdirtab_nentries
  // case: toBeCopied == COPY_DATA for e_fmtdirtab_nentries
     unsigned e_fmtdirtab_nentries_copy = p_e_fmtdirtab_nentries; 
     result->p_e_fmtdirtab_nentries = e_fmtdirtab_nentries_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_import_modtab_nentries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_import_modtab_nentries
  // case: toBeCopied == COPY_DATA for e_import_modtab_nentries
     unsigned e_import_modtab_nentries_copy = p_e_import_modtab_nentries; 
     result->p_e_import_modtab_nentries = e_import_modtab_nentries_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_preload_npages
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_preload_npages
  // case: toBeCopied == COPY_DATA for e_preload_npages
     unsigned e_preload_npages_copy = p_e_preload_npages; 
     result->p_e_preload_npages = e_preload_npages_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nonresnametab_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nonresnametab_size
  // case: toBeCopied == COPY_DATA for e_nonresnametab_size
     unsigned e_nonresnametab_size_copy = p_e_nonresnametab_size; 
     result->p_e_nonresnametab_size = e_nonresnametab_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nonresnametab_cksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nonresnametab_cksum
  // case: toBeCopied == COPY_DATA for e_nonresnametab_cksum
     unsigned e_nonresnametab_cksum_copy = p_e_nonresnametab_cksum; 
     result->p_e_nonresnametab_cksum = e_nonresnametab_cksum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_auto_ds_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_auto_ds_section
  // case: toBeCopied == COPY_DATA for e_auto_ds_section
     unsigned e_auto_ds_section_copy = p_e_auto_ds_section; 
     result->p_e_auto_ds_section = e_auto_ds_section_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_debug_info_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_debug_info_size
  // case: toBeCopied == COPY_DATA for e_debug_info_size
     unsigned e_debug_info_size_copy = p_e_debug_info_size; 
     result->p_e_debug_info_size = e_debug_info_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_num_instance_preload
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_num_instance_preload
  // case: toBeCopied == COPY_DATA for e_num_instance_preload
     unsigned e_num_instance_preload_copy = p_e_num_instance_preload; 
     result->p_e_num_instance_preload = e_num_instance_preload_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_num_instance_demand
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_num_instance_demand
  // case: toBeCopied == COPY_DATA for e_num_instance_demand
     unsigned e_num_instance_demand_copy = p_e_num_instance_demand; 
     result->p_e_num_instance_demand = e_num_instance_demand_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_heap_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_heap_size
  // case: toBeCopied == COPY_DATA for e_heap_size
     unsigned e_heap_size_copy = p_e_heap_size; 
     result->p_e_heap_size = e_heap_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_npages
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_npages
  // case: toBeCopied == COPY_DATA for e_npages
     Rose::BinaryAnalysis::Address e_npages_copy = p_e_npages; 
     result->p_e_npages = e_npages_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_eip
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_eip
  // case: toBeCopied == COPY_DATA for e_eip
     Rose::BinaryAnalysis::Address e_eip_copy = p_e_eip; 
     result->p_e_eip = e_eip_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_esp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_esp
  // case: toBeCopied == COPY_DATA for e_esp
     Rose::BinaryAnalysis::Address e_esp_copy = p_e_esp; 
     result->p_e_esp = e_esp_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_page_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_page_size
  // case: toBeCopied == COPY_DATA for e_page_size
     Rose::BinaryAnalysis::Address e_page_size_copy = p_e_page_size; 
     result->p_e_page_size = e_page_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_fixup_sect_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fixup_sect_size
  // case: toBeCopied == COPY_DATA for e_fixup_sect_size
     Rose::BinaryAnalysis::Address e_fixup_sect_size_copy = p_e_fixup_sect_size; 
     result->p_e_fixup_sect_size = e_fixup_sect_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_loader_sect_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_loader_sect_size
  // case: toBeCopied == COPY_DATA for e_loader_sect_size
     Rose::BinaryAnalysis::Address e_loader_sect_size_copy = p_e_loader_sect_size; 
     result->p_e_loader_sect_size = e_loader_sect_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_secttab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_secttab_rfo
  // case: toBeCopied == COPY_DATA for e_secttab_rfo
     Rose::BinaryAnalysis::Address e_secttab_rfo_copy = p_e_secttab_rfo; 
     result->p_e_secttab_rfo = e_secttab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_pagetab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_pagetab_rfo
  // case: toBeCopied == COPY_DATA for e_pagetab_rfo
     Rose::BinaryAnalysis::Address e_pagetab_rfo_copy = p_e_pagetab_rfo; 
     result->p_e_pagetab_rfo = e_pagetab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_iterpages_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_iterpages_offset
  // case: toBeCopied == COPY_DATA for e_iterpages_offset
     Rose::BinaryAnalysis::Address e_iterpages_offset_copy = p_e_iterpages_offset; 
     result->p_e_iterpages_offset = e_iterpages_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_rsrctab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_rsrctab_rfo
  // case: toBeCopied == COPY_DATA for e_rsrctab_rfo
     Rose::BinaryAnalysis::Address e_rsrctab_rfo_copy = p_e_rsrctab_rfo; 
     result->p_e_rsrctab_rfo = e_rsrctab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_resnametab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_resnametab_rfo
  // case: toBeCopied == COPY_DATA for e_resnametab_rfo
     Rose::BinaryAnalysis::Address e_resnametab_rfo_copy = p_e_resnametab_rfo; 
     result->p_e_resnametab_rfo = e_resnametab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_entrytab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_entrytab_rfo
  // case: toBeCopied == COPY_DATA for e_entrytab_rfo
     Rose::BinaryAnalysis::Address e_entrytab_rfo_copy = p_e_entrytab_rfo; 
     result->p_e_entrytab_rfo = e_entrytab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_fmtdirtab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fmtdirtab_rfo
  // case: toBeCopied == COPY_DATA for e_fmtdirtab_rfo
     Rose::BinaryAnalysis::Address e_fmtdirtab_rfo_copy = p_e_fmtdirtab_rfo; 
     result->p_e_fmtdirtab_rfo = e_fmtdirtab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_fixup_pagetab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fixup_pagetab_rfo
  // case: toBeCopied == COPY_DATA for e_fixup_pagetab_rfo
     Rose::BinaryAnalysis::Address e_fixup_pagetab_rfo_copy = p_e_fixup_pagetab_rfo; 
     result->p_e_fixup_pagetab_rfo = e_fixup_pagetab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_fixup_rectab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fixup_rectab_rfo
  // case: toBeCopied == COPY_DATA for e_fixup_rectab_rfo
     Rose::BinaryAnalysis::Address e_fixup_rectab_rfo_copy = p_e_fixup_rectab_rfo; 
     result->p_e_fixup_rectab_rfo = e_fixup_rectab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_import_modtab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_import_modtab_rfo
  // case: toBeCopied == COPY_DATA for e_import_modtab_rfo
     Rose::BinaryAnalysis::Address e_import_modtab_rfo_copy = p_e_import_modtab_rfo; 
     result->p_e_import_modtab_rfo = e_import_modtab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_import_proctab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_import_proctab_rfo
  // case: toBeCopied == COPY_DATA for e_import_proctab_rfo
     Rose::BinaryAnalysis::Address e_import_proctab_rfo_copy = p_e_import_proctab_rfo; 
     result->p_e_import_proctab_rfo = e_import_proctab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_ppcksumtab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ppcksumtab_rfo
  // case: toBeCopied == COPY_DATA for e_ppcksumtab_rfo
     Rose::BinaryAnalysis::Address e_ppcksumtab_rfo_copy = p_e_ppcksumtab_rfo; 
     result->p_e_ppcksumtab_rfo = e_ppcksumtab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_data_pages_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_data_pages_offset
  // case: toBeCopied == COPY_DATA for e_data_pages_offset
     Rose::BinaryAnalysis::Address e_data_pages_offset_copy = p_e_data_pages_offset; 
     result->p_e_data_pages_offset = e_data_pages_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nonresnametab_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nonresnametab_offset
  // case: toBeCopied == COPY_DATA for e_nonresnametab_offset
     Rose::BinaryAnalysis::Address e_nonresnametab_offset_copy = p_e_nonresnametab_offset; 
     result->p_e_nonresnametab_offset = e_nonresnametab_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_debug_info_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_debug_info_rfo
  // case: toBeCopied == COPY_DATA for e_debug_info_rfo
     Rose::BinaryAnalysis::Address e_debug_info_rfo_copy = p_e_debug_info_rfo; 
     result->p_e_debug_info_rfo = e_debug_info_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dos2Header
     SgAsmDOSExtendedHeader* dos2Header_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dos2Header
          if (get_dos2Header() != NULL) 
             { 
               dos2Header_copy = static_cast<SgAsmDOSExtendedHeader*>(help.copyAst(get_dos2Header())); 
             } 
            else 
             { 
               dos2Header_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dos2Header != NULL) delete result->p_dos2Header; 
     result->p_dos2Header = dos2Header_copy; 
  // case: not a listType for (using conditionalToSetParent)dos2Header
          if ( (dos2Header_copy != NULL) && (dos2Header_copy->get_parent() == NULL) && (isSgType(dos2Header_copy) == NULL) ) 
             { 
               dos2Header_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sectionTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionTable
  // case: toBeCopied == COPY_DATA for sectionTable
     SgAsmLESectionTable* sectionTable_copy = p_sectionTable; 
     result->p_sectionTable = sectionTable_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_pageTable
     SgAsmLEPageTable* pageTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pageTable
          if (get_pageTable() != NULL) 
             { 
               pageTable_copy = static_cast<SgAsmLEPageTable*>(help.copyAst(get_pageTable())); 
             } 
            else 
             { 
               pageTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pageTable != NULL) delete result->p_pageTable; 
     result->p_pageTable = pageTable_copy; 
  // case: not a listType for (using conditionalToSetParent)pageTable
          if ( (pageTable_copy != NULL) && (pageTable_copy->get_parent() == NULL) && (isSgType(pageTable_copy) == NULL) ) 
             { 
               pageTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_residentNameTable
     SgAsmLENameTable* residentNameTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)residentNameTable
          if (get_residentNameTable() != NULL) 
             { 
               residentNameTable_copy = static_cast<SgAsmLENameTable*>(help.copyAst(get_residentNameTable())); 
             } 
            else 
             { 
               residentNameTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_residentNameTable != NULL) delete result->p_residentNameTable; 
     result->p_residentNameTable = residentNameTable_copy; 
  // case: not a listType for (using conditionalToSetParent)residentNameTable
          if ( (residentNameTable_copy != NULL) && (residentNameTable_copy->get_parent() == NULL) && (isSgType(residentNameTable_copy) == NULL) ) 
             { 
               residentNameTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_nonresidentNameTable
     SgAsmLENameTable* nonresidentNameTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)nonresidentNameTable
          if (get_nonresidentNameTable() != NULL) 
             { 
               nonresidentNameTable_copy = static_cast<SgAsmLENameTable*>(help.copyAst(get_nonresidentNameTable())); 
             } 
            else 
             { 
               nonresidentNameTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_nonresidentNameTable != NULL) delete result->p_nonresidentNameTable; 
     result->p_nonresidentNameTable = nonresidentNameTable_copy; 
  // case: not a listType for (using conditionalToSetParent)nonresidentNameTable
          if ( (nonresidentNameTable_copy != NULL) && (nonresidentNameTable_copy->get_parent() == NULL) && (isSgType(nonresidentNameTable_copy) == NULL) ) 
             { 
               nonresidentNameTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_entryTable
     SgAsmLEEntryTable* entryTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entryTable
          if (get_entryTable() != NULL) 
             { 
               entryTable_copy = static_cast<SgAsmLEEntryTable*>(help.copyAst(get_entryTable())); 
             } 
            else 
             { 
               entryTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entryTable != NULL) delete result->p_entryTable; 
     result->p_entryTable = entryTable_copy; 
  // case: not a listType for (using conditionalToSetParent)entryTable
          if ( (entryTable_copy != NULL) && (entryTable_copy->get_parent() == NULL) && (isSgType(entryTable_copy) == NULL) ) 
             { 
               entryTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_relocationTable
     SgAsmLERelocTable* relocationTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)relocationTable
          if (get_relocationTable() != NULL) 
             { 
               relocationTable_copy = static_cast<SgAsmLERelocTable*>(help.copyAst(get_relocationTable())); 
             } 
            else 
             { 
               relocationTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_relocationTable != NULL) delete result->p_relocationTable; 
     result->p_relocationTable = relocationTable_copy; 
  // case: not a listType for (using conditionalToSetParent)relocationTable
          if ( (relocationTable_copy != NULL) && (relocationTable_copy->get_parent() == NULL) && (isSgType(relocationTable_copy) == NULL) ) 
             { 
               relocationTable_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLEFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLEFileHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 257584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEFileHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmNEFileHeader* result = NULL;

  // printf ("Copy SgAsmNEFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNEFileHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNEFileHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNEFileHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNEFileHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNEFileHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != NULL) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != NULL) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
          if ( (executableFormat_copy != NULL) && (executableFormat_copy->get_parent() == NULL) && (isSgType(executableFormat_copy) == NULL) ) 
             { 
               executableFormat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != NULL) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != NULL) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
          if ( (dlls_copy != NULL) && (dlls_copy->get_parent() == NULL) && (isSgType(dlls_copy) == NULL) ) 
             { 
               dlls_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != NULL) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != NULL) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
          if ( (sections_copy != NULL) && (sections_copy->get_parent() == NULL) && (isSgType(sections_copy) == NULL) ) 
             { 
               sections_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_e_linker_major
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_linker_major
  // case: toBeCopied == COPY_DATA for e_linker_major
     unsigned e_linker_major_copy = p_e_linker_major; 
     result->p_e_linker_major = e_linker_major_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_linker_minor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_linker_minor
  // case: toBeCopied == COPY_DATA for e_linker_minor
     unsigned e_linker_minor_copy = p_e_linker_minor; 
     result->p_e_linker_minor = e_linker_minor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_checksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_checksum
  // case: toBeCopied == COPY_DATA for e_checksum
     unsigned e_checksum_copy = p_e_checksum; 
     result->p_e_checksum = e_checksum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_flags1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags1
  // case: toBeCopied == COPY_DATA for e_flags1
     unsigned e_flags1_copy = p_e_flags1; 
     result->p_e_flags1 = e_flags1_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_autodata_sn
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_autodata_sn
  // case: toBeCopied == COPY_DATA for e_autodata_sn
     unsigned e_autodata_sn_copy = p_e_autodata_sn; 
     result->p_e_autodata_sn = e_autodata_sn_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_bss_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_bss_size
  // case: toBeCopied == COPY_DATA for e_bss_size
     unsigned e_bss_size_copy = p_e_bss_size; 
     result->p_e_bss_size = e_bss_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_stack_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_stack_size
  // case: toBeCopied == COPY_DATA for e_stack_size
     unsigned e_stack_size_copy = p_e_stack_size; 
     result->p_e_stack_size = e_stack_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_csip
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_csip
  // case: toBeCopied == COPY_DATA for e_csip
     unsigned e_csip_copy = p_e_csip; 
     result->p_e_csip = e_csip_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_sssp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_sssp
  // case: toBeCopied == COPY_DATA for e_sssp
     unsigned e_sssp_copy = p_e_sssp; 
     result->p_e_sssp = e_sssp_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nsections
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nsections
  // case: toBeCopied == COPY_DATA for e_nsections
     unsigned e_nsections_copy = p_e_nsections; 
     result->p_e_nsections = e_nsections_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nmodrefs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nmodrefs
  // case: toBeCopied == COPY_DATA for e_nmodrefs
     unsigned e_nmodrefs_copy = p_e_nmodrefs; 
     result->p_e_nmodrefs = e_nmodrefs_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nnonresnames
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nnonresnames
  // case: toBeCopied == COPY_DATA for e_nnonresnames
     unsigned e_nnonresnames_copy = p_e_nnonresnames; 
     result->p_e_nnonresnames = e_nnonresnames_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nmovable_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nmovable_entries
  // case: toBeCopied == COPY_DATA for e_nmovable_entries
     unsigned e_nmovable_entries_copy = p_e_nmovable_entries; 
     result->p_e_nmovable_entries = e_nmovable_entries_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_sector_align
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_sector_align
  // case: toBeCopied == COPY_DATA for e_sector_align
     unsigned e_sector_align_copy = p_e_sector_align; 
     result->p_e_sector_align = e_sector_align_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nresources
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nresources
  // case: toBeCopied == COPY_DATA for e_nresources
     unsigned e_nresources_copy = p_e_nresources; 
     result->p_e_nresources = e_nresources_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_exetype
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_exetype
  // case: toBeCopied == COPY_DATA for e_exetype
     unsigned e_exetype_copy = p_e_exetype; 
     result->p_e_exetype = e_exetype_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_flags2
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags2
  // case: toBeCopied == COPY_DATA for e_flags2
     unsigned e_flags2_copy = p_e_flags2; 
     result->p_e_flags2 = e_flags2_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res1
  // case: toBeCopied == COPY_DATA for e_res1
     unsigned e_res1_copy = p_e_res1; 
     result->p_e_res1 = e_res1_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_winvers
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_winvers
  // case: toBeCopied == COPY_DATA for e_winvers
     unsigned e_winvers_copy = p_e_winvers; 
     result->p_e_winvers = e_winvers_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_entrytab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_entrytab_rfo
  // case: toBeCopied == COPY_DATA for e_entrytab_rfo
     Rose::BinaryAnalysis::Address e_entrytab_rfo_copy = p_e_entrytab_rfo; 
     result->p_e_entrytab_rfo = e_entrytab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_entrytab_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_entrytab_size
  // case: toBeCopied == COPY_DATA for e_entrytab_size
     Rose::BinaryAnalysis::Address e_entrytab_size_copy = p_e_entrytab_size; 
     result->p_e_entrytab_size = e_entrytab_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_sectab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_sectab_rfo
  // case: toBeCopied == COPY_DATA for e_sectab_rfo
     Rose::BinaryAnalysis::Address e_sectab_rfo_copy = p_e_sectab_rfo; 
     result->p_e_sectab_rfo = e_sectab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_rsrctab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_rsrctab_rfo
  // case: toBeCopied == COPY_DATA for e_rsrctab_rfo
     Rose::BinaryAnalysis::Address e_rsrctab_rfo_copy = p_e_rsrctab_rfo; 
     result->p_e_rsrctab_rfo = e_rsrctab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_resnametab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_resnametab_rfo
  // case: toBeCopied == COPY_DATA for e_resnametab_rfo
     Rose::BinaryAnalysis::Address e_resnametab_rfo_copy = p_e_resnametab_rfo; 
     result->p_e_resnametab_rfo = e_resnametab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_modreftab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_modreftab_rfo
  // case: toBeCopied == COPY_DATA for e_modreftab_rfo
     Rose::BinaryAnalysis::Address e_modreftab_rfo_copy = p_e_modreftab_rfo; 
     result->p_e_modreftab_rfo = e_modreftab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_importnametab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_importnametab_rfo
  // case: toBeCopied == COPY_DATA for e_importnametab_rfo
     Rose::BinaryAnalysis::Address e_importnametab_rfo_copy = p_e_importnametab_rfo; 
     result->p_e_importnametab_rfo = e_importnametab_rfo_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nonresnametab_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nonresnametab_offset
  // case: toBeCopied == COPY_DATA for e_nonresnametab_offset
     Rose::BinaryAnalysis::Address e_nonresnametab_offset_copy = p_e_nonresnametab_offset; 
     result->p_e_nonresnametab_offset = e_nonresnametab_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_fastload_sector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fastload_sector
  // case: toBeCopied == COPY_DATA for e_fastload_sector
     Rose::BinaryAnalysis::Address e_fastload_sector_copy = p_e_fastload_sector; 
     result->p_e_fastload_sector = e_fastload_sector_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_fastload_nsectors
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fastload_nsectors
  // case: toBeCopied == COPY_DATA for e_fastload_nsectors
     Rose::BinaryAnalysis::Address e_fastload_nsectors_copy = p_e_fastload_nsectors; 
     result->p_e_fastload_nsectors = e_fastload_nsectors_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dos2Header
     SgAsmDOSExtendedHeader* dos2Header_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dos2Header
          if (get_dos2Header() != NULL) 
             { 
               dos2Header_copy = static_cast<SgAsmDOSExtendedHeader*>(help.copyAst(get_dos2Header())); 
             } 
            else 
             { 
               dos2Header_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dos2Header != NULL) delete result->p_dos2Header; 
     result->p_dos2Header = dos2Header_copy; 
  // case: not a listType for (using conditionalToSetParent)dos2Header
          if ( (dos2Header_copy != NULL) && (dos2Header_copy->get_parent() == NULL) && (isSgType(dos2Header_copy) == NULL) ) 
             { 
               dos2Header_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sectionTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionTable
  // case: toBeCopied == COPY_DATA for sectionTable
     SgAsmNESectionTable* sectionTable_copy = p_sectionTable; 
     result->p_sectionTable = sectionTable_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_residentNameTable
     SgAsmNENameTable* residentNameTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)residentNameTable
          if (get_residentNameTable() != NULL) 
             { 
               residentNameTable_copy = static_cast<SgAsmNENameTable*>(help.copyAst(get_residentNameTable())); 
             } 
            else 
             { 
               residentNameTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_residentNameTable != NULL) delete result->p_residentNameTable; 
     result->p_residentNameTable = residentNameTable_copy; 
  // case: not a listType for (using conditionalToSetParent)residentNameTable
          if ( (residentNameTable_copy != NULL) && (residentNameTable_copy->get_parent() == NULL) && (isSgType(residentNameTable_copy) == NULL) ) 
             { 
               residentNameTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_nonresidentNameTable
     SgAsmNENameTable* nonresidentNameTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)nonresidentNameTable
          if (get_nonresidentNameTable() != NULL) 
             { 
               nonresidentNameTable_copy = static_cast<SgAsmNENameTable*>(help.copyAst(get_nonresidentNameTable())); 
             } 
            else 
             { 
               nonresidentNameTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_nonresidentNameTable != NULL) delete result->p_nonresidentNameTable; 
     result->p_nonresidentNameTable = nonresidentNameTable_copy; 
  // case: not a listType for (using conditionalToSetParent)nonresidentNameTable
          if ( (nonresidentNameTable_copy != NULL) && (nonresidentNameTable_copy->get_parent() == NULL) && (isSgType(nonresidentNameTable_copy) == NULL) ) 
             { 
               nonresidentNameTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_moduleTable
     SgAsmNEModuleTable* moduleTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)moduleTable
          if (get_moduleTable() != NULL) 
             { 
               moduleTable_copy = static_cast<SgAsmNEModuleTable*>(help.copyAst(get_moduleTable())); 
             } 
            else 
             { 
               moduleTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_moduleTable != NULL) delete result->p_moduleTable; 
     result->p_moduleTable = moduleTable_copy; 
  // case: not a listType for (using conditionalToSetParent)moduleTable
          if ( (moduleTable_copy != NULL) && (moduleTable_copy->get_parent() == NULL) && (isSgType(moduleTable_copy) == NULL) ) 
             { 
               moduleTable_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_entryTable
     SgAsmNEEntryTable* entryTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entryTable
          if (get_entryTable() != NULL) 
             { 
               entryTable_copy = static_cast<SgAsmNEEntryTable*>(help.copyAst(get_entryTable())); 
             } 
            else 
             { 
               entryTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entryTable != NULL) delete result->p_entryTable; 
     result->p_entryTable = entryTable_copy; 
  // case: not a listType for (using conditionalToSetParent)entryTable
          if ( (entryTable_copy != NULL) && (entryTable_copy->get_parent() == NULL) && (isSgType(entryTable_copy) == NULL) ) 
             { 
               entryTable_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNEFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNEFileHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 258185 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEFileHeader::copy ( SgCopyHelp& help) const
   {
     SgAsmPEFileHeader* result = NULL;

  // printf ("Copy SgAsmPEFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEFileHeader*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEFileHeader*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEFileHeader: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEFileHeader*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEFileHeader(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != NULL) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != NULL) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
          if ( (executableFormat_copy != NULL) && (executableFormat_copy->get_parent() == NULL) && (isSgType(executableFormat_copy) == NULL) ) 
             { 
               executableFormat_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != NULL) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != NULL) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
          if ( (dlls_copy != NULL) && (dlls_copy->get_parent() == NULL) && (isSgType(dlls_copy) == NULL) ) 
             { 
               dlls_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != NULL) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != NULL) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
          if ( (sections_copy != NULL) && (sections_copy->get_parent() == NULL) && (isSgType(sections_copy) == NULL) ) 
             { 
               sections_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_e_cpu_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_cpu_type
  // case: toBeCopied == COPY_DATA for e_cpu_type
     unsigned e_cpu_type_copy = p_e_cpu_type; 
     result->p_e_cpu_type = e_cpu_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nsections
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nsections
  // case: toBeCopied == COPY_DATA for e_nsections
     unsigned e_nsections_copy = p_e_nsections; 
     result->p_e_nsections = e_nsections_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_time
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_time
  // case: toBeCopied == COPY_DATA for e_time
     unsigned e_time_copy = p_e_time; 
     result->p_e_time = e_time_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_coff_symtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_coff_symtab
  // case: toBeCopied == COPY_DATA for e_coff_symtab
     Rose::BinaryAnalysis::Address e_coff_symtab_copy = p_e_coff_symtab; 
     result->p_e_coff_symtab = e_coff_symtab_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_nt_hdr_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nt_hdr_size
  // case: toBeCopied == COPY_DATA for e_nt_hdr_size
     Rose::BinaryAnalysis::Address e_nt_hdr_size_copy = p_e_nt_hdr_size; 
     result->p_e_nt_hdr_size = e_nt_hdr_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_coff_nsyms
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_coff_nsyms
  // case: toBeCopied == COPY_DATA for e_coff_nsyms
     unsigned e_coff_nsyms_copy = p_e_coff_nsyms; 
     result->p_e_coff_nsyms = e_coff_nsyms_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags
  // case: toBeCopied == COPY_DATA for e_flags
     unsigned e_flags_copy = p_e_flags; 
     result->p_e_flags = e_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_opt_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_opt_magic
  // case: toBeCopied == COPY_DATA for e_opt_magic
     unsigned e_opt_magic_copy = p_e_opt_magic; 
     result->p_e_opt_magic = e_opt_magic_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_lmajor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_lmajor
  // case: toBeCopied == COPY_DATA for e_lmajor
     unsigned e_lmajor_copy = p_e_lmajor; 
     result->p_e_lmajor = e_lmajor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_lminor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_lminor
  // case: toBeCopied == COPY_DATA for e_lminor
     unsigned e_lminor_copy = p_e_lminor; 
     result->p_e_lminor = e_lminor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_code_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_code_size
  // case: toBeCopied == COPY_DATA for e_code_size
     unsigned e_code_size_copy = p_e_code_size; 
     result->p_e_code_size = e_code_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_data_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_data_size
  // case: toBeCopied == COPY_DATA for e_data_size
     unsigned e_data_size_copy = p_e_data_size; 
     result->p_e_data_size = e_data_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_bss_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_bss_size
  // case: toBeCopied == COPY_DATA for e_bss_size
     unsigned e_bss_size_copy = p_e_bss_size; 
     result->p_e_bss_size = e_bss_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_code_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_code_rva
  // case: toBeCopied == COPY_DATA for e_code_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress e_code_rva_copy = p_e_code_rva; 
     result->p_e_code_rva = e_code_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_data_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_data_rva
  // case: toBeCopied == COPY_DATA for e_data_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress e_data_rva_copy = p_e_data_rva; 
     result->p_e_data_rva = e_data_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_section_align
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_section_align
  // case: toBeCopied == COPY_DATA for e_section_align
     unsigned e_section_align_copy = p_e_section_align; 
     result->p_e_section_align = e_section_align_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_file_align
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_file_align
  // case: toBeCopied == COPY_DATA for e_file_align
     unsigned e_file_align_copy = p_e_file_align; 
     result->p_e_file_align = e_file_align_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_os_major
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_os_major
  // case: toBeCopied == COPY_DATA for e_os_major
     unsigned e_os_major_copy = p_e_os_major; 
     result->p_e_os_major = e_os_major_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_os_minor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_os_minor
  // case: toBeCopied == COPY_DATA for e_os_minor
     unsigned e_os_minor_copy = p_e_os_minor; 
     result->p_e_os_minor = e_os_minor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_user_major
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_user_major
  // case: toBeCopied == COPY_DATA for e_user_major
     unsigned e_user_major_copy = p_e_user_major; 
     result->p_e_user_major = e_user_major_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_user_minor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_user_minor
  // case: toBeCopied == COPY_DATA for e_user_minor
     unsigned e_user_minor_copy = p_e_user_minor; 
     result->p_e_user_minor = e_user_minor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_subsys_major
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_subsys_major
  // case: toBeCopied == COPY_DATA for e_subsys_major
     unsigned e_subsys_major_copy = p_e_subsys_major; 
     result->p_e_subsys_major = e_subsys_major_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_subsys_minor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_subsys_minor
  // case: toBeCopied == COPY_DATA for e_subsys_minor
     unsigned e_subsys_minor_copy = p_e_subsys_minor; 
     result->p_e_subsys_minor = e_subsys_minor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_reserved9
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_reserved9
  // case: toBeCopied == COPY_DATA for e_reserved9
     unsigned e_reserved9_copy = p_e_reserved9; 
     result->p_e_reserved9 = e_reserved9_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_image_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_image_size
  // case: toBeCopied == COPY_DATA for e_image_size
     unsigned e_image_size_copy = p_e_image_size; 
     result->p_e_image_size = e_image_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_header_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_header_size
  // case: toBeCopied == COPY_DATA for e_header_size
     unsigned e_header_size_copy = p_e_header_size; 
     result->p_e_header_size = e_header_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_file_checksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_file_checksum
  // case: toBeCopied == COPY_DATA for e_file_checksum
     unsigned e_file_checksum_copy = p_e_file_checksum; 
     result->p_e_file_checksum = e_file_checksum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_subsystem
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_subsystem
  // case: toBeCopied == COPY_DATA for e_subsystem
     unsigned e_subsystem_copy = p_e_subsystem; 
     result->p_e_subsystem = e_subsystem_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_dll_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_dll_flags
  // case: toBeCopied == COPY_DATA for e_dll_flags
     unsigned e_dll_flags_copy = p_e_dll_flags; 
     result->p_e_dll_flags = e_dll_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_stack_reserve_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_stack_reserve_size
  // case: toBeCopied == COPY_DATA for e_stack_reserve_size
     unsigned e_stack_reserve_size_copy = p_e_stack_reserve_size; 
     result->p_e_stack_reserve_size = e_stack_reserve_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_stack_commit_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_stack_commit_size
  // case: toBeCopied == COPY_DATA for e_stack_commit_size
     unsigned e_stack_commit_size_copy = p_e_stack_commit_size; 
     result->p_e_stack_commit_size = e_stack_commit_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_heap_reserve_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_heap_reserve_size
  // case: toBeCopied == COPY_DATA for e_heap_reserve_size
     unsigned e_heap_reserve_size_copy = p_e_heap_reserve_size; 
     result->p_e_heap_reserve_size = e_heap_reserve_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_heap_commit_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_heap_commit_size
  // case: toBeCopied == COPY_DATA for e_heap_commit_size
     unsigned e_heap_commit_size_copy = p_e_heap_commit_size; 
     result->p_e_heap_commit_size = e_heap_commit_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_loader_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_loader_flags
  // case: toBeCopied == COPY_DATA for e_loader_flags
     unsigned e_loader_flags_copy = p_e_loader_flags; 
     result->p_e_loader_flags = e_loader_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_num_rvasize_pairs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_num_rvasize_pairs
  // case: toBeCopied == COPY_DATA for e_num_rvasize_pairs
     unsigned e_num_rvasize_pairs_copy = p_e_num_rvasize_pairs; 
     result->p_e_num_rvasize_pairs = e_num_rvasize_pairs_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rvaSizePairs
     SgAsmPERVASizePairList* rvaSizePairs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rvaSizePairs
          if (get_rvaSizePairs() != NULL) 
             { 
               rvaSizePairs_copy = static_cast<SgAsmPERVASizePairList*>(help.copyAst(get_rvaSizePairs())); 
             } 
            else 
             { 
               rvaSizePairs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rvaSizePairs != NULL) delete result->p_rvaSizePairs; 
     result->p_rvaSizePairs = rvaSizePairs_copy; 
  // case: not a listType for (using conditionalToSetParent)rvaSizePairs
          if ( (rvaSizePairs_copy != NULL) && (rvaSizePairs_copy->get_parent() == NULL) && (isSgType(rvaSizePairs_copy) == NULL) ) 
             { 
               rvaSizePairs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_sectionTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionTable
  // case: toBeCopied == COPY_DATA for sectionTable
     SgAsmPESectionTable* sectionTable_copy = p_sectionTable; 
     result->p_sectionTable = sectionTable_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_coffSymbolTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for coffSymbolTable
  // case: toBeCopied == COPY_DATA for coffSymbolTable
     SgAsmCoffSymbolTable* coffSymbolTable_copy = p_coffSymbolTable; 
     result->p_coffSymbolTable = coffSymbolTable_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEFileHeader = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEFileHeader::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 258745 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmAttributeTable::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmAttributeTable* result = NULL;

  // printf ("Copy SgAsmJvmAttributeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmAttributeTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmAttributeTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmAttributeTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmAttributeTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmAttributeTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attributes
     SgAsmJvmAttributePtrList attributes_copy; 
  // case: listType for attributes
  // case: listType (typeIsPointerToList == false) for attributes
     SgAsmJvmAttributePtrList::const_iterator source_attributes_iterator = get_attributes().begin(); 
     for ( /* empty by design */; source_attributes_iterator != get_attributes().end(); ++source_attributes_iterator) 
        { 
          SgAsmJvmAttribute* source_list_element = *source_attributes_iterator; 
          SgAsmJvmAttribute* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmJvmAttribute*>(help.copyAst(*source_attributes_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          attributes_copy.push_back(copy_list_element); 
        } 
     result->p_attributes = attributes_copy; 
  // case: listType for attributes
  // case: listType (typeIsPointerToList == false) for attributes
     SgAsmJvmAttributePtrList::const_iterator attributes_iterator = result->get_attributes().begin(); 
     for ( /* empty by design */; attributes_iterator != result->get_attributes().end(); ++attributes_iterator) 
        { 
          SgAsmJvmAttribute* list_element = *attributes_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmAttributeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmAttributeTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 259055 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmConstantPool::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmConstantPool* result = NULL;

  // printf ("Copy SgAsmJvmConstantPool = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmConstantPool*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmConstantPool*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmConstantPool: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmConstantPool*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmConstantPool(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmJvmConstantPoolEntryPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmConstantPool = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmConstantPool::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 259339 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmFieldTable::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmFieldTable* result = NULL;

  // printf ("Copy SgAsmJvmFieldTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmFieldTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmFieldTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmFieldTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmFieldTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmFieldTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fields
     SgAsmJvmFieldPtrList fields_copy; 
  // case: listType for fields
  // case: listType (typeIsPointerToList == false) for fields
     SgAsmJvmFieldPtrList::const_iterator source_fields_iterator = get_fields().begin(); 
     for ( /* empty by design */; source_fields_iterator != get_fields().end(); ++source_fields_iterator) 
        { 
          SgAsmJvmField* source_list_element = *source_fields_iterator; 
          SgAsmJvmField* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmJvmField*>(help.copyAst(*source_fields_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          fields_copy.push_back(copy_list_element); 
        } 
     result->p_fields = fields_copy; 
  // case: listType for fields
  // case: listType (typeIsPointerToList == false) for fields
     SgAsmJvmFieldPtrList::const_iterator fields_iterator = result->get_fields().begin(); 
     for ( /* empty by design */; fields_iterator != result->get_fields().end(); ++fields_iterator) 
        { 
          SgAsmJvmField* list_element = *fields_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmFieldTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmFieldTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 259649 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmMethodTable::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmMethodTable* result = NULL;

  // printf ("Copy SgAsmJvmMethodTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmMethodTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmMethodTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmMethodTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmMethodTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmMethodTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_methods
     SgAsmJvmMethodPtrList methods_copy; 
  // case: listType for methods
  // case: listType (typeIsPointerToList == false) for methods
     SgAsmJvmMethodPtrList::const_iterator source_methods_iterator = get_methods().begin(); 
     for ( /* empty by design */; source_methods_iterator != get_methods().end(); ++source_methods_iterator) 
        { 
          SgAsmJvmMethod* source_list_element = *source_methods_iterator; 
          SgAsmJvmMethod* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmJvmMethod*>(help.copyAst(*source_methods_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          methods_copy.push_back(copy_list_element); 
        } 
     result->p_methods = methods_copy; 
  // case: listType for methods
  // case: listType (typeIsPointerToList == false) for methods
     SgAsmJvmMethodPtrList::const_iterator methods_iterator = result->get_methods().begin(); 
     for ( /* empty by design */; methods_iterator != result->get_methods().end(); ++methods_iterator) 
        { 
          SgAsmJvmMethod* list_element = *methods_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmMethodTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmMethodTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 259959 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEEntryTable::copy ( SgCopyHelp& help) const
   {
     SgAsmLEEntryTable* result = NULL;

  // printf ("Copy SgAsmLEEntryTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLEEntryTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLEEntryTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLEEntryTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLEEntryTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLEEntryTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bundle_sizes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bundle_sizes
  // case: toBeCopied == COPY_DATA for bundle_sizes
     SgSizeTList bundle_sizes_copy = p_bundle_sizes; 
     result->p_bundle_sizes = bundle_sizes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmLEEntryPointPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmLEEntryPointPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmLEEntryPoint* source_list_element = *source_entries_iterator; 
          SgAsmLEEntryPoint* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmLEEntryPoint*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmLEEntryPointPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmLEEntryPoint* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLEEntryTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLEEntryTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 260274 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLENameTable::copy ( SgCopyHelp& help) const
   {
     SgAsmLENameTable* result = NULL;

  // printf ("Copy SgAsmLENameTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLENameTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLENameTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLENameTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLENameTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLENameTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_names
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for names
  // case: toBeCopied == COPY_DATA for names
     SgStringList names_copy = p_names; 
     result->p_names = names_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ordinals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinals
  // case: toBeCopied == COPY_DATA for ordinals
     SgUnsignedList ordinals_copy = p_ordinals; 
     result->p_ordinals = ordinals_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLENameTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLENameTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 260563 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEPageTable::copy ( SgCopyHelp& help) const
   {
     SgAsmLEPageTable* result = NULL;

  // printf ("Copy SgAsmLEPageTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLEPageTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLEPageTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLEPageTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLEPageTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLEPageTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmLEPageTableEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmLEPageTableEntryPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmLEPageTableEntry* source_list_element = *source_entries_iterator; 
          SgAsmLEPageTableEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmLEPageTableEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmLEPageTableEntryPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmLEPageTableEntry* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLEPageTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLEPageTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 260873 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLERelocTable::copy ( SgCopyHelp& help) const
   {
     SgAsmLERelocTable* result = NULL;

  // printf ("Copy SgAsmLERelocTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLERelocTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLERelocTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLERelocTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLERelocTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLERelocTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmLERelocEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmLERelocEntryPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmLERelocEntry* source_list_element = *source_entries_iterator; 
          SgAsmLERelocEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmLERelocEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmLERelocEntryPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmLERelocEntry* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLERelocTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLERelocTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 261183 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLESection::copy ( SgCopyHelp& help) const
   {
     SgAsmLESection* result = NULL;

  // printf ("Copy SgAsmLESection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLESection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLESection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLESection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLESection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLESection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionTableEntry
     SgAsmLESectionTableEntry* sectionTableEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionTableEntry
          if (get_sectionTableEntry() != NULL) 
             { 
               sectionTableEntry_copy = static_cast<SgAsmLESectionTableEntry*>(help.copyAst(get_sectionTableEntry())); 
             } 
            else 
             { 
               sectionTableEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionTableEntry != NULL) delete result->p_sectionTableEntry; 
     result->p_sectionTableEntry = sectionTableEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionTableEntry
          if ( (sectionTableEntry_copy != NULL) && (sectionTableEntry_copy->get_parent() == NULL) && (isSgType(sectionTableEntry_copy) == NULL) ) 
             { 
               sectionTableEntry_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLESection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLESection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 261481 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLESectionTable::copy ( SgCopyHelp& help) const
   {
     SgAsmLESectionTable* result = NULL;

  // printf ("Copy SgAsmLESectionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLESectionTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLESectionTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLESectionTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLESectionTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLESectionTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLESectionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLESectionTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 261760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEEntryTable::copy ( SgCopyHelp& help) const
   {
     SgAsmNEEntryTable* result = NULL;

  // printf ("Copy SgAsmNEEntryTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNEEntryTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNEEntryTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNEEntryTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNEEntryTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNEEntryTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bundle_sizes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bundle_sizes
  // case: toBeCopied == COPY_DATA for bundle_sizes
     SgSizeTList bundle_sizes_copy = p_bundle_sizes; 
     result->p_bundle_sizes = bundle_sizes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmNEEntryPointPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmNEEntryPointPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmNEEntryPoint* source_list_element = *source_entries_iterator; 
          SgAsmNEEntryPoint* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmNEEntryPoint*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmNEEntryPointPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmNEEntryPoint* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNEEntryTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNEEntryTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 262075 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEModuleTable::copy ( SgCopyHelp& help) const
   {
     SgAsmNEModuleTable* result = NULL;

  // printf ("Copy SgAsmNEModuleTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNEModuleTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNEModuleTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNEModuleTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNEModuleTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNEModuleTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_strtab
     SgAsmNEStringTable* strtab_copy; 
  // case: not a listType for (using conditionalToCopyVariable)strtab
          if (get_strtab() != NULL) 
             { 
               strtab_copy = static_cast<SgAsmNEStringTable*>(help.copyAst(get_strtab())); 
             } 
            else 
             { 
               strtab_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_strtab != NULL) delete result->p_strtab; 
     result->p_strtab = strtab_copy; 
  // case: not a listType for (using conditionalToSetParent)strtab
          if ( (strtab_copy != NULL) && (strtab_copy->get_parent() == NULL) && (isSgType(strtab_copy) == NULL) ) 
             { 
               strtab_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_nameOffsets
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOffsets
  // case: toBeCopied == COPY_DATA for nameOffsets
     SgAddressList nameOffsets_copy = p_nameOffsets; 
     result->p_nameOffsets = nameOffsets_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_names
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for names
  // case: toBeCopied == COPY_DATA for names
     SgStringList names_copy = p_names; 
     result->p_names = names_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNEModuleTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNEModuleTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 262383 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNENameTable::copy ( SgCopyHelp& help) const
   {
     SgAsmNENameTable* result = NULL;

  // printf ("Copy SgAsmNENameTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNENameTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNENameTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNENameTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNENameTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNENameTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_names
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for names
  // case: toBeCopied == COPY_DATA for names
     SgStringList names_copy = p_names; 
     result->p_names = names_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ordinals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinals
  // case: toBeCopied == COPY_DATA for ordinals
     SgUnsignedList ordinals_copy = p_ordinals; 
     result->p_ordinals = ordinals_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNENameTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNENameTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 262672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNERelocTable::copy ( SgCopyHelp& help) const
   {
     SgAsmNERelocTable* result = NULL;

  // printf ("Copy SgAsmNERelocTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNERelocTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNERelocTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNERelocTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNERelocTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNERelocTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmNERelocEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmNERelocEntryPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmNERelocEntry* source_list_element = *source_entries_iterator; 
          SgAsmNERelocEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmNERelocEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmNERelocEntryPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmNERelocEntry* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNERelocTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNERelocTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 262982 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNESection::copy ( SgCopyHelp& help) const
   {
     SgAsmNESection* result = NULL;

  // printf ("Copy SgAsmNESection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNESection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNESection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNESection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNESection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNESection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionTableEntry
     SgAsmNESectionTableEntry* sectionTableEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionTableEntry
          if (get_sectionTableEntry() != NULL) 
             { 
               sectionTableEntry_copy = static_cast<SgAsmNESectionTableEntry*>(help.copyAst(get_sectionTableEntry())); 
             } 
            else 
             { 
               sectionTableEntry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionTableEntry != NULL) delete result->p_sectionTableEntry; 
     result->p_sectionTableEntry = sectionTableEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionTableEntry
          if ( (sectionTableEntry_copy != NULL) && (sectionTableEntry_copy->get_parent() == NULL) && (isSgType(sectionTableEntry_copy) == NULL) ) 
             { 
               sectionTableEntry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_relocationTable
     SgAsmNERelocTable* relocationTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)relocationTable
          if (get_relocationTable() != NULL) 
             { 
               relocationTable_copy = static_cast<SgAsmNERelocTable*>(help.copyAst(get_relocationTable())); 
             } 
            else 
             { 
               relocationTable_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_relocationTable != NULL) delete result->p_relocationTable; 
     result->p_relocationTable = relocationTable_copy; 
  // case: not a listType for (using conditionalToSetParent)relocationTable
          if ( (relocationTable_copy != NULL) && (relocationTable_copy->get_parent() == NULL) && (isSgType(relocationTable_copy) == NULL) ) 
             { 
               relocationTable_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNESection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNESection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 263299 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNESectionTable::copy ( SgCopyHelp& help) const
   {
     SgAsmNESectionTable* result = NULL;

  // printf ("Copy SgAsmNESectionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNESectionTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNESectionTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNESectionTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNESectionTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNESectionTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sector
  // case: toBeCopied == COPY_DATA for sector
     unsigned sector_copy = p_sector; 
     result->p_sector = sector_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_physicalSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physicalSize
  // case: toBeCopied == COPY_DATA for physicalSize
     Rose::BinaryAnalysis::Address physicalSize_copy = p_physicalSize; 
     result->p_physicalSize = physicalSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_virtualSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtualSize
  // case: toBeCopied == COPY_DATA for virtualSize
     Rose::BinaryAnalysis::Address virtualSize_copy = p_virtualSize; 
     result->p_virtualSize = virtualSize_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNESectionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNESectionTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 263598 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEStringTable::copy ( SgCopyHelp& help) const
   {
     SgAsmNEStringTable* result = NULL;

  // printf ("Copy SgAsmNEStringTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNEStringTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNEStringTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNEStringTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNEStringTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNEStringTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNEStringTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNEStringTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 263877 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPESection::copy ( SgCopyHelp& help) const
   {
     SgAsmPESection* result = NULL;

  // printf ("Copy SgAsmPESection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPESection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPESection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPESection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPESection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPESection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_section_entry
     SgAsmPESectionTableEntry* section_entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)section_entry
          if (get_section_entry() != NULL) 
             { 
               section_entry_copy = static_cast<SgAsmPESectionTableEntry*>(help.copyAst(get_section_entry())); 
             } 
            else 
             { 
               section_entry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_section_entry != NULL) delete result->p_section_entry; 
     result->p_section_entry = section_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)section_entry
          if ( (section_entry_copy != NULL) && (section_entry_copy->get_parent() == NULL) && (isSgType(section_entry_copy) == NULL) ) 
             { 
               section_entry_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPESection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPESection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 264175 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEExportSection::copy ( SgCopyHelp& help) const
   {
     SgAsmPEExportSection* result = NULL;

  // printf ("Copy SgAsmPEExportSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEExportSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEExportSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEExportSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEExportSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEExportSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_section_entry
     SgAsmPESectionTableEntry* section_entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)section_entry
          if (get_section_entry() != NULL) 
             { 
               section_entry_copy = static_cast<SgAsmPESectionTableEntry*>(help.copyAst(get_section_entry())); 
             } 
            else 
             { 
               section_entry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_section_entry != NULL) delete result->p_section_entry; 
     result->p_section_entry = section_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)section_entry
          if ( (section_entry_copy != NULL) && (section_entry_copy->get_parent() == NULL) && (isSgType(section_entry_copy) == NULL) ) 
             { 
               section_entry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_exportDirectory
     SgAsmPEExportDirectory* exportDirectory_copy; 
  // case: not a listType for (using conditionalToCopyVariable)exportDirectory
          if (get_exportDirectory() != NULL) 
             { 
               exportDirectory_copy = static_cast<SgAsmPEExportDirectory*>(help.copyAst(get_exportDirectory())); 
             } 
            else 
             { 
               exportDirectory_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_exportDirectory != NULL) delete result->p_exportDirectory; 
     result->p_exportDirectory = exportDirectory_copy; 
  // case: not a listType for (using conditionalToSetParent)exportDirectory
          if ( (exportDirectory_copy != NULL) && (exportDirectory_copy->get_parent() == NULL) && (isSgType(exportDirectory_copy) == NULL) ) 
             { 
               exportDirectory_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_exports
     SgAsmPEExportEntryList* exports_copy; 
  // case: not a listType for (using conditionalToCopyVariable)exports
          if (get_exports() != NULL) 
             { 
               exports_copy = static_cast<SgAsmPEExportEntryList*>(help.copyAst(get_exports())); 
             } 
            else 
             { 
               exports_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_exports != NULL) delete result->p_exports; 
     result->p_exports = exports_copy; 
  // case: not a listType for (using conditionalToSetParent)exports
          if ( (exports_copy != NULL) && (exports_copy->get_parent() == NULL) && (isSgType(exports_copy) == NULL) ) 
             { 
               exports_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEExportSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEExportSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 264511 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportSection::copy ( SgCopyHelp& help) const
   {
     SgAsmPEImportSection* result = NULL;

  // printf ("Copy SgAsmPEImportSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEImportSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEImportSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEImportSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEImportSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEImportSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_section_entry
     SgAsmPESectionTableEntry* section_entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)section_entry
          if (get_section_entry() != NULL) 
             { 
               section_entry_copy = static_cast<SgAsmPESectionTableEntry*>(help.copyAst(get_section_entry())); 
             } 
            else 
             { 
               section_entry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_section_entry != NULL) delete result->p_section_entry; 
     result->p_section_entry = section_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)section_entry
          if ( (section_entry_copy != NULL) && (section_entry_copy->get_parent() == NULL) && (isSgType(section_entry_copy) == NULL) ) 
             { 
               section_entry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_importDirectories
     SgAsmPEImportDirectoryList* importDirectories_copy; 
  // case: not a listType for (using conditionalToCopyVariable)importDirectories
          if (get_importDirectories() != NULL) 
             { 
               importDirectories_copy = static_cast<SgAsmPEImportDirectoryList*>(help.copyAst(get_importDirectories())); 
             } 
            else 
             { 
               importDirectories_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_importDirectories != NULL) delete result->p_importDirectories; 
     result->p_importDirectories = importDirectories_copy; 
  // case: not a listType for (using conditionalToSetParent)importDirectories
          if ( (importDirectories_copy != NULL) && (importDirectories_copy->get_parent() == NULL) && (isSgType(importDirectories_copy) == NULL) ) 
             { 
               importDirectories_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEImportSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEImportSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 264828 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEStringSection::copy ( SgCopyHelp& help) const
   {
     SgAsmPEStringSection* result = NULL;

  // printf ("Copy SgAsmPEStringSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEStringSection*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEStringSection*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEStringSection: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEStringSection*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEStringSection(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_section_entry
     SgAsmPESectionTableEntry* section_entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)section_entry
          if (get_section_entry() != NULL) 
             { 
               section_entry_copy = static_cast<SgAsmPESectionTableEntry*>(help.copyAst(get_section_entry())); 
             } 
            else 
             { 
               section_entry_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_section_entry != NULL) delete result->p_section_entry; 
     result->p_section_entry = section_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)section_entry
          if ( (section_entry_copy != NULL) && (section_entry_copy->get_parent() == NULL) && (isSgType(section_entry_copy) == NULL) ) 
             { 
               section_entry_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_strtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strtab
  // case: toBeCopied == COPY_DATA for strtab
     SgAsmCoffStrtab* strtab_copy = p_strtab; 
     result->p_strtab = strtab_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEStringSection = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEStringSection::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 265131 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPESectionTable::copy ( SgCopyHelp& help) const
   {
     SgAsmPESectionTable* result = NULL;

  // printf ("Copy SgAsmPESectionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPESectionTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPESectionTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPESectionTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPESectionTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPESectionTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPESectionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPESectionTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 265410 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericSectionList::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericSectionList* result = NULL;

  // printf ("Copy SgAsmGenericSectionList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericSectionList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericSectionList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericSectionList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericSectionList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericSectionList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionPtrList sections_copy; 
  // case: listType for sections
  // case: listType (typeIsPointerToList == false) for sections
     SgAsmGenericSectionPtrList::const_iterator source_sections_iterator = get_sections().begin(); 
     for ( /* empty by design */; source_sections_iterator != get_sections().end(); ++source_sections_iterator) 
        { 
          SgAsmGenericSection* source_list_element = *source_sections_iterator; 
          SgAsmGenericSection* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmGenericSection*>(help.copyAst(*source_sections_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          sections_copy.push_back(copy_list_element); 
        } 
     result->p_sections = sections_copy; 
  // case: listType for sections
  // case: listType (typeIsPointerToList == false) for sections
     SgAsmGenericSectionPtrList::const_iterator sections_iterator = result->get_sections().begin(); 
     for ( /* empty by design */; sections_iterator != result->get_sections().end(); ++sections_iterator) 
        { 
          SgAsmGenericSection* list_element = *sections_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericSectionList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericSectionList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 265625 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericString::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericString* result = NULL;

  // printf ("Copy SgAsmGenericString = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericString*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericString*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericString: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericString*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericString(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericString = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericString::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 265808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBasicString::copy ( SgCopyHelp& help) const
   {
     SgAsmBasicString* result = NULL;

  // printf ("Copy SgAsmBasicString = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBasicString*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBasicString*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBasicString: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBasicString*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBasicString(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_string
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     std::string string_copy = p_string; 
     result->p_string = string_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBasicString = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBasicString::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 265997 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStoredString::copy ( SgCopyHelp& help) const
   {
     SgAsmStoredString* result = NULL;

  // printf ("Copy SgAsmStoredString = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmStoredString*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmStoredString*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmStoredString: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmStoredString*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmStoredString(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_storage
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storage
  // case: toBeCopied == COPY_DATA for storage
     SgAsmStringStorage* storage_copy = p_storage; 
     result->p_storage = storage_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmStoredString = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmStoredString::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 266186 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericStrtab::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericStrtab* result = NULL;

  // printf ("Copy SgAsmGenericStrtab = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericStrtab*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericStrtab*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericStrtab: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericStrtab*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericStrtab(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_container
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for container
  // case: toBeCopied == COPY_DATA for container
     SgAsmGenericSection* container_copy = p_container; 
     result->p_container = container_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_storageList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storageList
  // case: toBeCopied == COPY_DATA for storageList
     SgAsmGenericStrtab::referenced_t storageList_copy = p_storageList; 
     result->p_storageList = storageList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_freeList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for freeList
  // case: toBeCopied == COPY_DATA for freeList
     Rose::BinaryAnalysis::AddressIntervalSet freeList_copy = p_freeList; 
     result->p_freeList = freeList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dontFree
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dontFree
  // case: toBeCopied == COPY_DATA for dontFree
     SgAsmStringStorage* dontFree_copy = p_dontFree; 
     result->p_dontFree = dontFree_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_numberFreed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numberFreed
  // case: toBeCopied == COPY_DATA for numberFreed
     size_t numberFreed_copy = p_numberFreed; 
     result->p_numberFreed = numberFreed_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericStrtab = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericStrtab::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 266395 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCoffStrtab::copy ( SgCopyHelp& help) const
   {
     SgAsmCoffStrtab* result = NULL;

  // printf ("Copy SgAsmCoffStrtab = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCoffStrtab*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCoffStrtab*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCoffStrtab: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCoffStrtab*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCoffStrtab(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_container
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for container
  // case: toBeCopied == COPY_DATA for container
     SgAsmGenericSection* container_copy = p_container; 
     result->p_container = container_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_storageList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storageList
  // case: toBeCopied == COPY_DATA for storageList
     SgAsmGenericStrtab::referenced_t storageList_copy = p_storageList; 
     result->p_storageList = storageList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_freeList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for freeList
  // case: toBeCopied == COPY_DATA for freeList
     Rose::BinaryAnalysis::AddressIntervalSet freeList_copy = p_freeList; 
     result->p_freeList = freeList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dontFree
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dontFree
  // case: toBeCopied == COPY_DATA for dontFree
     SgAsmStringStorage* dontFree_copy = p_dontFree; 
     result->p_dontFree = dontFree_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_numberFreed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numberFreed
  // case: toBeCopied == COPY_DATA for numberFreed
     size_t numberFreed_copy = p_numberFreed; 
     result->p_numberFreed = numberFreed_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCoffStrtab = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCoffStrtab::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 266604 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfStrtab::copy ( SgCopyHelp& help) const
   {
     SgAsmElfStrtab* result = NULL;

  // printf ("Copy SgAsmElfStrtab = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfStrtab*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfStrtab*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfStrtab: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfStrtab*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfStrtab(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_container
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for container
  // case: toBeCopied == COPY_DATA for container
     SgAsmGenericSection* container_copy = p_container; 
     result->p_container = container_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_storageList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storageList
  // case: toBeCopied == COPY_DATA for storageList
     SgAsmGenericStrtab::referenced_t storageList_copy = p_storageList; 
     result->p_storageList = storageList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_freeList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for freeList
  // case: toBeCopied == COPY_DATA for freeList
     Rose::BinaryAnalysis::AddressIntervalSet freeList_copy = p_freeList; 
     result->p_freeList = freeList_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dontFree
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dontFree
  // case: toBeCopied == COPY_DATA for dontFree
     SgAsmStringStorage* dontFree_copy = p_dontFree; 
     result->p_dontFree = dontFree_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_numberFreed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numberFreed
  // case: toBeCopied == COPY_DATA for numberFreed
     size_t numberFreed_copy = p_numberFreed; 
     result->p_numberFreed = numberFreed_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfStrtab = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfStrtab::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 266813 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericSymbol::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericSymbol* result = NULL;

  // printf ("Copy SgAsmGenericSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericSymbol(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_definitionState
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definitionState
  // case: toBeCopied == COPY_DATA for definitionState
     SgAsmGenericSymbol::SymbolDefState definitionState_copy = p_definitionState; 
     result->p_definitionState = definitionState_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_binding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding
  // case: toBeCopied == COPY_DATA for binding
     SgAsmGenericSymbol::SymbolBinding binding_copy = p_binding; 
     result->p_binding = binding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmGenericSymbol::SymbolType type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     Rose::BinaryAnalysis::Address value_copy = p_value; 
     result->p_value = value_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bound
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound
  // case: toBeCopied == COPY_DATA for bound
     SgAsmGenericSection* bound_copy = p_bound; 
     result->p_bound = bound_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 267046 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCoffSymbol::copy ( SgCopyHelp& help) const
   {
     SgAsmCoffSymbol* result = NULL;

  // printf ("Copy SgAsmCoffSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCoffSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCoffSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCoffSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCoffSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCoffSymbol(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_definitionState
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definitionState
  // case: toBeCopied == COPY_DATA for definitionState
     SgAsmGenericSymbol::SymbolDefState definitionState_copy = p_definitionState; 
     result->p_definitionState = definitionState_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_binding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding
  // case: toBeCopied == COPY_DATA for binding
     SgAsmGenericSymbol::SymbolBinding binding_copy = p_binding; 
     result->p_binding = binding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmGenericSymbol::SymbolType type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     Rose::BinaryAnalysis::Address value_copy = p_value; 
     result->p_value = value_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bound
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound
  // case: toBeCopied == COPY_DATA for bound
     SgAsmGenericSection* bound_copy = p_bound; 
     result->p_bound = bound_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_st_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_name
  // case: toBeCopied == COPY_DATA for st_name
     std::string st_name_copy = p_st_name; 
     result->p_st_name = st_name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_st_name_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_name_offset
  // case: toBeCopied == COPY_DATA for st_name_offset
     Rose::BinaryAnalysis::Address st_name_offset_copy = p_st_name_offset; 
     result->p_st_name_offset = st_name_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_st_section_num
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_section_num
  // case: toBeCopied == COPY_DATA for st_section_num
     int st_section_num_copy = p_st_section_num; 
     result->p_st_section_num = st_section_num_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_st_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_type
  // case: toBeCopied == COPY_DATA for st_type
     unsigned st_type_copy = p_st_type; 
     result->p_st_type = st_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_st_storage_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_storage_class
  // case: toBeCopied == COPY_DATA for st_storage_class
     unsigned st_storage_class_copy = p_st_storage_class; 
     result->p_st_storage_class = st_storage_class_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_st_num_aux_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_num_aux_entries
  // case: toBeCopied == COPY_DATA for st_num_aux_entries
     unsigned st_num_aux_entries_copy = p_st_num_aux_entries; 
     result->p_st_num_aux_entries = st_num_aux_entries_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_auxiliaryData
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for auxiliaryData
  // case: toBeCopied == COPY_DATA for auxiliaryData
     SgUnsignedCharList auxiliaryData_copy = p_auxiliaryData; 
     result->p_auxiliaryData = auxiliaryData_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCoffSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCoffSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 267314 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymbol::copy ( SgCopyHelp& help) const
   {
     SgAsmElfSymbol* result = NULL;

  // printf ("Copy SgAsmElfSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmElfSymbol*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmElfSymbol*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmElfSymbol: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmElfSymbol*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmElfSymbol(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_definitionState
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definitionState
  // case: toBeCopied == COPY_DATA for definitionState
     SgAsmGenericSymbol::SymbolDefState definitionState_copy = p_definitionState; 
     result->p_definitionState = definitionState_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_binding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding
  // case: toBeCopied == COPY_DATA for binding
     SgAsmGenericSymbol::SymbolBinding binding_copy = p_binding; 
     result->p_binding = binding_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmGenericSymbol::SymbolType type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     Rose::BinaryAnalysis::Address value_copy = p_value; 
     result->p_value = value_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bound
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound
  // case: toBeCopied == COPY_DATA for bound
     SgAsmGenericSection* bound_copy = p_bound; 
     result->p_bound = bound_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_st_info
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_info
  // case: toBeCopied == COPY_DATA for st_info
     unsigned char st_info_copy = p_st_info; 
     result->p_st_info = st_info_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_st_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_res1
  // case: toBeCopied == COPY_DATA for st_res1
     unsigned char st_res1_copy = p_st_res1; 
     result->p_st_res1 = st_res1_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_st_shndx
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_shndx
  // case: toBeCopied == COPY_DATA for st_shndx
     unsigned st_shndx_copy = p_st_shndx; 
     result->p_st_shndx = st_shndx_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_st_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_size
  // case: toBeCopied == COPY_DATA for st_size
     Rose::BinaryAnalysis::Address st_size_copy = p_st_size; 
     result->p_st_size = st_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmElfSymbol = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmElfSymbol::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 267572 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericSymbolList::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericSymbolList* result = NULL;

  // printf ("Copy SgAsmGenericSymbolList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericSymbolList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericSymbolList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericSymbolList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericSymbolList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericSymbolList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_symbols
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbols
  // case: toBeCopied == COPY_DATA for symbols
     SgAsmGenericSymbolPtrList symbols_copy = p_symbols; 
     result->p_symbols = symbols_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericSymbolList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericSymbolList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 267761 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEEntryPoint::copy ( SgCopyHelp& help) const
   {
     SgAsmLEEntryPoint* result = NULL;

  // printf ("Copy SgAsmLEEntryPoint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLEEntryPoint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLEEntryPoint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLEEntryPoint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLEEntryPoint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLEEntryPoint(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmLEEntryPointPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmLEEntryPointPtrList::const_iterator source_entries_iterator = get_entries().begin(); 
     for ( /* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) 
        { 
          SgAsmLEEntryPoint* source_list_element = *source_entries_iterator; 
          SgAsmLEEntryPoint* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmLEEntryPoint*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          entries_copy.push_back(copy_list_element); 
        } 
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
     SgAsmLEEntryPointPtrList::const_iterator entries_iterator = result->get_entries().begin(); 
     for ( /* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) 
        { 
          SgAsmLEEntryPoint* list_element = *entries_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_objnum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objnum
  // case: toBeCopied == COPY_DATA for objnum
     unsigned objnum_copy = p_objnum; 
     result->p_objnum = objnum_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entry_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entry_type
  // case: toBeCopied == COPY_DATA for entry_type
     unsigned entry_type_copy = p_entry_type; 
     result->p_entry_type = entry_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for res1
  // case: toBeCopied == COPY_DATA for res1
     unsigned res1_copy = p_res1; 
     result->p_res1 = res1_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entry_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entry_offset
  // case: toBeCopied == COPY_DATA for entry_offset
     Rose::BinaryAnalysis::Address entry_offset_copy = p_entry_offset; 
     result->p_entry_offset = entry_offset_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLEEntryPoint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLEEntryPoint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 268001 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEPageTableEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmLEPageTableEntry* result = NULL;

  // printf ("Copy SgAsmLEPageTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLEPageTableEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLEPageTableEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLEPageTableEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLEPageTableEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLEPageTableEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_pageno
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pageno
  // case: toBeCopied == COPY_DATA for pageno
     unsigned pageno_copy = p_pageno; 
     result->p_pageno = pageno_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLEPageTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLEPageTableEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 268195 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLESectionTableEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmLESectionTableEntry* result = NULL;

  // printf ("Copy SgAsmLESectionTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmLESectionTableEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmLESectionTableEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmLESectionTableEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmLESectionTableEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmLESectionTableEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_pageMapIndex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pageMapIndex
  // case: toBeCopied == COPY_DATA for pageMapIndex
     unsigned pageMapIndex_copy = p_pageMapIndex; 
     result->p_pageMapIndex = pageMapIndex_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_pageMapNEntries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pageMapNEntries
  // case: toBeCopied == COPY_DATA for pageMapNEntries
     unsigned pageMapNEntries_copy = p_pageMapNEntries; 
     result->p_pageMapNEntries = pageMapNEntries_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for res1
  // case: toBeCopied == COPY_DATA for res1
     unsigned res1_copy = p_res1; 
     result->p_res1 = res1_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseAddr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseAddr
  // case: toBeCopied == COPY_DATA for baseAddr
     Rose::BinaryAnalysis::Address baseAddr_copy = p_baseAddr; 
     result->p_baseAddr = baseAddr_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmLESectionTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmLESectionTableEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 268409 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEEntryPoint::copy ( SgCopyHelp& help) const
   {
     SgAsmNEEntryPoint* result = NULL;

  // printf ("Copy SgAsmNEEntryPoint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNEEntryPoint*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNEEntryPoint*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNEEntryPoint: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNEEntryPoint*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNEEntryPoint(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     SgAsmNEEntryPoint::NEEntryFlags flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_int3f
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for int3f
  // case: toBeCopied == COPY_DATA for int3f
     unsigned int3f_copy = p_int3f; 
     result->p_int3f = int3f_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionIndex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionIndex
  // case: toBeCopied == COPY_DATA for sectionIndex
     unsigned sectionIndex_copy = p_sectionIndex; 
     result->p_sectionIndex = sectionIndex_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sectionOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionOffset
  // case: toBeCopied == COPY_DATA for sectionOffset
     unsigned sectionOffset_copy = p_sectionOffset; 
     result->p_sectionOffset = sectionOffset_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNEEntryPoint = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNEEntryPoint::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 268613 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNERelocEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmNERelocEntry* result = NULL;

  // printf ("Copy SgAsmNERelocEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNERelocEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNERelocEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNERelocEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNERelocEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNERelocEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_src_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for src_type
  // case: toBeCopied == COPY_DATA for src_type
     SgAsmNERelocEntry::NERelocSrcType src_type_copy = p_src_type; 
     result->p_src_type = src_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_modifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgAsmNERelocEntry::NERelocModifiers modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_tgt_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tgt_type
  // case: toBeCopied == COPY_DATA for tgt_type
     SgAsmNERelocEntry::NERelocTgtType tgt_type_copy = p_tgt_type; 
     result->p_tgt_type = tgt_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     SgAsmNERelocEntry::NERelocFlags flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_src_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for src_offset
  // case: toBeCopied == COPY_DATA for src_offset
     Rose::BinaryAnalysis::Address src_offset_copy = p_src_offset; 
     result->p_src_offset = src_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_iref
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iref
  // case: toBeCopied == COPY_DATA for iref
     SgAsmNERelocEntry::iref_type iref_copy = p_iref; 
     result->p_iref = iref_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_iord
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iord
  // case: toBeCopied == COPY_DATA for iord
     SgAsmNERelocEntry::iord_type iord_copy = p_iord; 
     result->p_iord = iord_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_iname
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iname
  // case: toBeCopied == COPY_DATA for iname
     SgAsmNERelocEntry::iname_type iname_copy = p_iname; 
     result->p_iname = iname_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_osfixup
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for osfixup
  // case: toBeCopied == COPY_DATA for osfixup
     SgAsmNERelocEntry::osfixup_type osfixup_copy = p_osfixup; 
     result->p_osfixup = osfixup_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNERelocEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNERelocEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 268842 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNESectionTableEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmNESectionTableEntry* result = NULL;

  // printf ("Copy SgAsmNESectionTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNESectionTableEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNESectionTableEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNESectionTableEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNESectionTableEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNESectionTableEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sector
  // case: toBeCopied == COPY_DATA for sector
     unsigned sector_copy = p_sector; 
     result->p_sector = sector_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_physicalSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physicalSize
  // case: toBeCopied == COPY_DATA for physicalSize
     Rose::BinaryAnalysis::Address physicalSize_copy = p_physicalSize; 
     result->p_physicalSize = physicalSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_virtualSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtualSize
  // case: toBeCopied == COPY_DATA for virtualSize
     Rose::BinaryAnalysis::Address virtualSize_copy = p_virtualSize; 
     result->p_virtualSize = virtualSize_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNESectionTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNESectionTableEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 269046 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEExportDirectory::copy ( SgCopyHelp& help) const
   {
     SgAsmPEExportDirectory* result = NULL;

  // printf ("Copy SgAsmPEExportDirectory = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEExportDirectory*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEExportDirectory*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEExportDirectory: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEExportDirectory*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEExportDirectory(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for res1
  // case: toBeCopied == COPY_DATA for res1
     unsigned res1_copy = p_res1; 
     result->p_res1 = res1_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_timestamp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for timestamp
  // case: toBeCopied == COPY_DATA for timestamp
     time_t timestamp_copy = p_timestamp; 
     result->p_timestamp = timestamp_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_vmajor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vmajor
  // case: toBeCopied == COPY_DATA for vmajor
     unsigned vmajor_copy = p_vmajor; 
     result->p_vmajor = vmajor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_vminor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vminor
  // case: toBeCopied == COPY_DATA for vminor
     unsigned vminor_copy = p_vminor; 
     result->p_vminor = vminor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_rva
  // case: toBeCopied == COPY_DATA for name_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress name_rva_copy = p_name_rva; 
     result->p_name_rva = name_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ord_base
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ord_base
  // case: toBeCopied == COPY_DATA for ord_base
     unsigned ord_base_copy = p_ord_base; 
     result->p_ord_base = ord_base_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_expaddr_n
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expaddr_n
  // case: toBeCopied == COPY_DATA for expaddr_n
     size_t expaddr_n_copy = p_expaddr_n; 
     result->p_expaddr_n = expaddr_n_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nameptr_n
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameptr_n
  // case: toBeCopied == COPY_DATA for nameptr_n
     size_t nameptr_n_copy = p_nameptr_n; 
     result->p_nameptr_n = nameptr_n_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_expaddr_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expaddr_rva
  // case: toBeCopied == COPY_DATA for expaddr_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress expaddr_rva_copy = p_expaddr_rva; 
     result->p_expaddr_rva = expaddr_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nameptr_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameptr_rva
  // case: toBeCopied == COPY_DATA for nameptr_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress nameptr_rva_copy = p_nameptr_rva; 
     result->p_nameptr_rva = nameptr_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ordinals_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinals_rva
  // case: toBeCopied == COPY_DATA for ordinals_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress ordinals_rva_copy = p_ordinals_rva; 
     result->p_ordinals_rva = ordinals_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEExportDirectory = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEExportDirectory::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 269304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEExportEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmPEExportEntry* result = NULL;

  // printf ("Copy SgAsmPEExportEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEExportEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEExportEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEExportEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEExportEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEExportEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_ordinal
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinal
  // case: toBeCopied == COPY_DATA for ordinal
     unsigned ordinal_copy = p_ordinal; 
     result->p_ordinal = ordinal_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_exportRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exportRva
  // case: toBeCopied == COPY_DATA for exportRva
     Rose::BinaryAnalysis::RelativeVirtualAddress exportRva_copy = p_exportRva; 
     result->p_exportRva = exportRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_forwarder
     SgAsmGenericString* forwarder_copy; 
  // case: not a listType for (using conditionalToCopyVariable)forwarder
          if (get_forwarder() != NULL) 
             { 
               forwarder_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_forwarder())); 
             } 
            else 
             { 
               forwarder_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_forwarder != NULL) delete result->p_forwarder; 
     result->p_forwarder = forwarder_copy; 
  // case: not a listType for (using conditionalToSetParent)forwarder
          if ( (forwarder_copy != NULL) && (forwarder_copy->get_parent() == NULL) && (isSgType(forwarder_copy) == NULL) ) 
             { 
               forwarder_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEExportEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEExportEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 269536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEExportEntryList::copy ( SgCopyHelp& help) const
   {
     SgAsmPEExportEntryList* result = NULL;

  // printf ("Copy SgAsmPEExportEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEExportEntryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEExportEntryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEExportEntryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEExportEntryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEExportEntryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_exports
     SgAsmPEExportEntryPtrList exports_copy; 
  // case: listType for exports
  // case: listType (typeIsPointerToList == false) for exports
     SgAsmPEExportEntryPtrList::const_iterator source_exports_iterator = get_exports().begin(); 
     for ( /* empty by design */; source_exports_iterator != get_exports().end(); ++source_exports_iterator) 
        { 
          SgAsmPEExportEntry* source_list_element = *source_exports_iterator; 
          SgAsmPEExportEntry* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmPEExportEntry*>(help.copyAst(*source_exports_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          exports_copy.push_back(copy_list_element); 
        } 
     result->p_exports = exports_copy; 
  // case: listType for exports
  // case: listType (typeIsPointerToList == false) for exports
     SgAsmPEExportEntryPtrList::const_iterator exports_iterator = result->get_exports().begin(); 
     for ( /* empty by design */; exports_iterator != result->get_exports().end(); ++exports_iterator) 
        { 
          SgAsmPEExportEntry* list_element = *exports_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEExportEntryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEExportEntryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 269751 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportDirectory::copy ( SgCopyHelp& help) const
   {
     SgAsmPEImportDirectory* result = NULL;

  // printf ("Copy SgAsmPEImportDirectory = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEImportDirectory*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEImportDirectory*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEImportDirectory: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEImportDirectory*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEImportDirectory(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_dllName
     SgAsmGenericString* dllName_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dllName
          if (get_dllName() != NULL) 
             { 
               dllName_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_dllName())); 
             } 
            else 
             { 
               dllName_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dllName != NULL) delete result->p_dllName; 
     result->p_dllName = dllName_copy; 
  // case: not a listType for (using conditionalToSetParent)dllName
          if ( (dllName_copy != NULL) && (dllName_copy->get_parent() == NULL) && (isSgType(dllName_copy) == NULL) ) 
             { 
               dllName_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_dllNameRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dllNameRva
  // case: toBeCopied == COPY_DATA for dllNameRva
     Rose::BinaryAnalysis::RelativeVirtualAddress dllNameRva_copy = p_dllNameRva; 
     result->p_dllNameRva = dllNameRva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dll_name_nalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dll_name_nalloc
  // case: toBeCopied == COPY_DATA for dll_name_nalloc
     size_t dll_name_nalloc_copy = p_dll_name_nalloc; 
     result->p_dll_name_nalloc = dll_name_nalloc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_time
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for time
  // case: toBeCopied == COPY_DATA for time
     time_t time_copy = p_time; 
     result->p_time = time_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_forwarder_chain
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forwarder_chain
  // case: toBeCopied == COPY_DATA for forwarder_chain
     unsigned forwarder_chain_copy = p_forwarder_chain; 
     result->p_forwarder_chain = forwarder_chain_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ilt_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ilt_rva
  // case: toBeCopied == COPY_DATA for ilt_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress ilt_rva_copy = p_ilt_rva; 
     result->p_ilt_rva = ilt_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ilt_nalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ilt_nalloc
  // case: toBeCopied == COPY_DATA for ilt_nalloc
     size_t ilt_nalloc_copy = p_ilt_nalloc; 
     result->p_ilt_nalloc = ilt_nalloc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_iat_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iat_rva
  // case: toBeCopied == COPY_DATA for iat_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress iat_rva_copy = p_iat_rva; 
     result->p_iat_rva = iat_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_iat_nalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iat_nalloc
  // case: toBeCopied == COPY_DATA for iat_nalloc
     size_t iat_nalloc_copy = p_iat_nalloc; 
     result->p_iat_nalloc = iat_nalloc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_imports
     SgAsmPEImportItemList* imports_copy; 
  // case: not a listType for (using conditionalToCopyVariable)imports
          if (get_imports() != NULL) 
             { 
               imports_copy = static_cast<SgAsmPEImportItemList*>(help.copyAst(get_imports())); 
             } 
            else 
             { 
               imports_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_imports != NULL) delete result->p_imports; 
     result->p_imports = imports_copy; 
  // case: not a listType for (using conditionalToSetParent)imports
          if ( (imports_copy != NULL) && (imports_copy->get_parent() == NULL) && (isSgType(imports_copy) == NULL) ) 
             { 
               imports_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEImportDirectory = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEImportDirectory::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 270013 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportDirectoryList::copy ( SgCopyHelp& help) const
   {
     SgAsmPEImportDirectoryList* result = NULL;

  // printf ("Copy SgAsmPEImportDirectoryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEImportDirectoryList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEImportDirectoryList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEImportDirectoryList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEImportDirectoryList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEImportDirectoryList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_vector
     SgAsmPEImportDirectoryPtrList vector_copy; 
  // case: listType for vector
  // case: listType (typeIsPointerToList == false) for vector
     SgAsmPEImportDirectoryPtrList::const_iterator source_vector_iterator = get_vector().begin(); 
     for ( /* empty by design */; source_vector_iterator != get_vector().end(); ++source_vector_iterator) 
        { 
          SgAsmPEImportDirectory* source_list_element = *source_vector_iterator; 
          SgAsmPEImportDirectory* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmPEImportDirectory*>(help.copyAst(*source_vector_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          vector_copy.push_back(copy_list_element); 
        } 
     result->p_vector = vector_copy; 
  // case: listType for vector
  // case: listType (typeIsPointerToList == false) for vector
     SgAsmPEImportDirectoryPtrList::const_iterator vector_iterator = result->get_vector().begin(); 
     for ( /* empty by design */; vector_iterator != result->get_vector().end(); ++vector_iterator) 
        { 
          SgAsmPEImportDirectory* list_element = *vector_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEImportDirectoryList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEImportDirectoryList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 270228 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportItem::copy ( SgCopyHelp& help) const
   {
     SgAsmPEImportItem* result = NULL;

  // printf ("Copy SgAsmPEImportItem = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEImportItem*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEImportItem*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEImportItem: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEImportItem*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEImportItem(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_by_ordinal
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for by_ordinal
  // case: toBeCopied == COPY_DATA for by_ordinal
     bool by_ordinal_copy = p_by_ordinal; 
     result->p_by_ordinal = by_ordinal_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_ordinal
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinal
  // case: toBeCopied == COPY_DATA for ordinal
     unsigned ordinal_copy = p_ordinal; 
     result->p_ordinal = ordinal_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_hint
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hint
  // case: toBeCopied == COPY_DATA for hint
     unsigned hint_copy = p_hint; 
     result->p_hint = hint_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != NULL) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != NULL) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
          if ( (name_copy != NULL) && (name_copy->get_parent() == NULL) && (isSgType(name_copy) == NULL) ) 
             { 
               name_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_hintname_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hintname_rva
  // case: toBeCopied == COPY_DATA for hintname_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress hintname_rva_copy = p_hintname_rva; 
     result->p_hintname_rva = hintname_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_hintname_nalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hintname_nalloc
  // case: toBeCopied == COPY_DATA for hintname_nalloc
     size_t hintname_nalloc_copy = p_hintname_nalloc; 
     result->p_hintname_nalloc = hintname_nalloc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bound_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound_rva
  // case: toBeCopied == COPY_DATA for bound_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress bound_rva_copy = p_bound_rva; 
     result->p_bound_rva = bound_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_iat_written
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iat_written
  // case: toBeCopied == COPY_DATA for iat_written
     bool iat_written_copy = p_iat_written; 
     result->p_iat_written = iat_written_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEImportItem = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEImportItem::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 270466 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportItemList::copy ( SgCopyHelp& help) const
   {
     SgAsmPEImportItemList* result = NULL;

  // printf ("Copy SgAsmPEImportItemList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPEImportItemList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPEImportItemList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPEImportItemList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPEImportItemList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPEImportItemList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_vector
     SgAsmPEImportItemPtrList vector_copy; 
  // case: listType for vector
  // case: listType (typeIsPointerToList == false) for vector
     SgAsmPEImportItemPtrList::const_iterator source_vector_iterator = get_vector().begin(); 
     for ( /* empty by design */; source_vector_iterator != get_vector().end(); ++source_vector_iterator) 
        { 
          SgAsmPEImportItem* source_list_element = *source_vector_iterator; 
          SgAsmPEImportItem* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmPEImportItem*>(help.copyAst(*source_vector_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          vector_copy.push_back(copy_list_element); 
        } 
     result->p_vector = vector_copy; 
  // case: listType for vector
  // case: listType (typeIsPointerToList == false) for vector
     SgAsmPEImportItemPtrList::const_iterator vector_iterator = result->get_vector().begin(); 
     for ( /* empty by design */; vector_iterator != result->get_vector().end(); ++vector_iterator) 
        { 
          SgAsmPEImportItem* list_element = *vector_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPEImportItemList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPEImportItemList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 270681 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPERVASizePair::copy ( SgCopyHelp& help) const
   {
     SgAsmPERVASizePair* result = NULL;

  // printf ("Copy SgAsmPERVASizePair = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPERVASizePair*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPERVASizePair*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPERVASizePair: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPERVASizePair*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPERVASizePair(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_e_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_rva
  // case: toBeCopied == COPY_DATA for e_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress e_rva_copy = p_e_rva; 
     result->p_e_rva = e_rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_e_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_size
  // case: toBeCopied == COPY_DATA for e_size
     Rose::BinaryAnalysis::Address e_size_copy = p_e_size; 
     result->p_e_size = e_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for section
  // case: toBeCopied == COPY_DATA for section
     SgAsmGenericSection* section_copy = p_section; 
     result->p_section = section_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPERVASizePair = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPERVASizePair::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 270880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPERVASizePairList::copy ( SgCopyHelp& help) const
   {
     SgAsmPERVASizePairList* result = NULL;

  // printf ("Copy SgAsmPERVASizePairList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPERVASizePairList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPERVASizePairList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPERVASizePairList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPERVASizePairList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPERVASizePairList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_pairs
     SgAsmPERVASizePairPtrList pairs_copy; 
  // case: listType for pairs
  // case: listType (typeIsPointerToList == false) for pairs
     SgAsmPERVASizePairPtrList::const_iterator source_pairs_iterator = get_pairs().begin(); 
     for ( /* empty by design */; source_pairs_iterator != get_pairs().end(); ++source_pairs_iterator) 
        { 
          SgAsmPERVASizePair* source_list_element = *source_pairs_iterator; 
          SgAsmPERVASizePair* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmPERVASizePair*>(help.copyAst(*source_pairs_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          pairs_copy.push_back(copy_list_element); 
        } 
     result->p_pairs = pairs_copy; 
  // case: listType for pairs
  // case: listType (typeIsPointerToList == false) for pairs
     SgAsmPERVASizePairPtrList::const_iterator pairs_iterator = result->get_pairs().begin(); 
     for ( /* empty by design */; pairs_iterator != result->get_pairs().end(); ++pairs_iterator) 
        { 
          SgAsmPERVASizePair* list_element = *pairs_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPERVASizePairList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPERVASizePairList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 271095 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPESectionTableEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmPESectionTableEntry* result = NULL;

  // printf ("Copy SgAsmPESectionTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPESectionTableEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPESectionTableEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPESectionTableEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPESectionTableEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPESectionTableEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_virtual_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtual_size
  // case: toBeCopied == COPY_DATA for virtual_size
     Rose::BinaryAnalysis::Address virtual_size_copy = p_virtual_size; 
     result->p_virtual_size = virtual_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rva
  // case: toBeCopied == COPY_DATA for rva
     Rose::BinaryAnalysis::Address rva_copy = p_rva; 
     result->p_rva = rva_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_physical_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physical_size
  // case: toBeCopied == COPY_DATA for physical_size
     Rose::BinaryAnalysis::Address physical_size_copy = p_physical_size; 
     result->p_physical_size = physical_size_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_physical_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physical_offset
  // case: toBeCopied == COPY_DATA for physical_offset
     Rose::BinaryAnalysis::Address physical_offset_copy = p_physical_offset; 
     result->p_physical_offset = physical_offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_coff_line_nums
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for coff_line_nums
  // case: toBeCopied == COPY_DATA for coff_line_nums
     unsigned coff_line_nums_copy = p_coff_line_nums; 
     result->p_coff_line_nums = coff_line_nums_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_n_relocs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for n_relocs
  // case: toBeCopied == COPY_DATA for n_relocs
     unsigned n_relocs_copy = p_n_relocs; 
     result->p_n_relocs = n_relocs_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_n_coff_line_nums
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for n_coff_line_nums
  // case: toBeCopied == COPY_DATA for n_coff_line_nums
     unsigned n_coff_line_nums_copy = p_n_coff_line_nums; 
     result->p_n_coff_line_nums = n_coff_line_nums_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPESectionTableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPESectionTableEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 271324 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStringStorage::copy ( SgCopyHelp& help) const
   {
     SgAsmStringStorage* result = NULL;

  // printf ("Copy SgAsmStringStorage = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmStringStorage*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmStringStorage*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmStringStorage: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmStringStorage*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmStringStorage(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_strtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strtab
  // case: toBeCopied == COPY_DATA for strtab
     SgAsmGenericStrtab* strtab_copy = p_strtab; 
     result->p_strtab = strtab_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_string
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     std::string string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmStringStorage = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmStringStorage::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 271523 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmExpression* result = NULL;

  // printf ("Copy SgAsmExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 271717 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmAarch32Coprocessor::copy ( SgCopyHelp& help) const
   {
     SgAsmAarch32Coprocessor* result = NULL;

  // printf ("Copy SgAsmAarch32Coprocessor = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmAarch32Coprocessor*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmAarch32Coprocessor*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmAarch32Coprocessor: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmAarch32Coprocessor*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmAarch32Coprocessor(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmAarch32Coprocessor = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmAarch32Coprocessor::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 271911 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryExpression* result = NULL;

  // printf ("Copy SgAsmBinaryExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 272143 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryAdd::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryAdd* result = NULL;

  // printf ("Copy SgAsmBinaryAdd = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryAdd*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryAdd*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryAdd: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryAdd*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryAdd(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryAdd = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryAdd::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 272375 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryAsr::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryAsr* result = NULL;

  // printf ("Copy SgAsmBinaryAsr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryAsr*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryAsr*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryAsr: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryAsr*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryAsr(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryAsr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryAsr::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 272607 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryConcat::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryConcat* result = NULL;

  // printf ("Copy SgAsmBinaryConcat = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryConcat*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryConcat*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryConcat: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryConcat*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryConcat(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryConcat = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryConcat::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 272839 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryDivide::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryDivide* result = NULL;

  // printf ("Copy SgAsmBinaryDivide = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryDivide*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryDivide*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryDivide: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryDivide*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryDivide(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryDivide = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryDivide::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 273071 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryLsl::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryLsl* result = NULL;

  // printf ("Copy SgAsmBinaryLsl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryLsl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryLsl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryLsl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryLsl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryLsl(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryLsl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryLsl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 273303 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryLsr::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryLsr* result = NULL;

  // printf ("Copy SgAsmBinaryLsr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryLsr*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryLsr*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryLsr: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryLsr*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryLsr(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryLsr = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryLsr::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 273535 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryMod::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryMod* result = NULL;

  // printf ("Copy SgAsmBinaryMod = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryMod*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryMod*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryMod: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryMod*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryMod(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryMod = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryMod::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 273767 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryMsl::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryMsl* result = NULL;

  // printf ("Copy SgAsmBinaryMsl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryMsl*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryMsl*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryMsl: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryMsl*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryMsl(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryMsl = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryMsl::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 273999 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryMultiply::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryMultiply* result = NULL;

  // printf ("Copy SgAsmBinaryMultiply = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryMultiply*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryMultiply*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryMultiply: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryMultiply*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryMultiply(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryMultiply = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryMultiply::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 274231 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryPostupdate::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryPostupdate* result = NULL;

  // printf ("Copy SgAsmBinaryPostupdate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryPostupdate*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryPostupdate*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryPostupdate: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryPostupdate*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryPostupdate(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryPostupdate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryPostupdate::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 274463 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryPreupdate::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryPreupdate* result = NULL;

  // printf ("Copy SgAsmBinaryPreupdate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryPreupdate*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryPreupdate*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryPreupdate: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryPreupdate*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryPreupdate(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryPreupdate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryPreupdate::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 274695 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryRor::copy ( SgCopyHelp& help) const
   {
     SgAsmBinaryRor* result = NULL;

  // printf ("Copy SgAsmBinaryRor = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinaryRor*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinaryRor*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinaryRor: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinaryRor*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinaryRor(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinaryRor = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinaryRor::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 274927 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinarySubtract::copy ( SgCopyHelp& help) const
   {
     SgAsmBinarySubtract* result = NULL;

  // printf ("Copy SgAsmBinarySubtract = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBinarySubtract*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBinarySubtract*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBinarySubtract: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBinarySubtract*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBinarySubtract(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != NULL) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != NULL) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
          if ( (lhs_copy != NULL) && (lhs_copy->get_parent() == NULL) && (isSgType(lhs_copy) == NULL) ) 
             { 
               lhs_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != NULL) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != NULL) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
          if ( (rhs_copy != NULL) && (rhs_copy->get_parent() == NULL) && (isSgType(rhs_copy) == NULL) ) 
             { 
               rhs_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBinarySubtract = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBinarySubtract::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 275159 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmByteOrder::copy ( SgCopyHelp& help) const
   {
     SgAsmByteOrder* result = NULL;

  // printf ("Copy SgAsmByteOrder = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmByteOrder*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmByteOrder*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmByteOrder: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmByteOrder*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmByteOrder(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmByteOrder = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmByteOrder::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 275353 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCommonSubExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmCommonSubExpression* result = NULL;

  // printf ("Copy SgAsmCommonSubExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCommonSubExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCommonSubExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCommonSubExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCommonSubExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCommonSubExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_subexpression
     SgAsmExpression* subexpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)subexpression
          if (get_subexpression() != NULL) 
             { 
               subexpression_copy = static_cast<SgAsmExpression*>(help.copyAst(get_subexpression())); 
             } 
            else 
             { 
               subexpression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_subexpression != NULL) delete result->p_subexpression; 
     result->p_subexpression = subexpression_copy; 
  // case: not a listType for (using conditionalToSetParent)subexpression
          if ( (subexpression_copy != NULL) && (subexpression_copy->get_parent() == NULL) && (isSgType(subexpression_copy) == NULL) ) 
             { 
               subexpression_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCommonSubExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCommonSubExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 275566 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmControlFlagsExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmControlFlagsExpression* result = NULL;

  // printf ("Copy SgAsmControlFlagsExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmControlFlagsExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmControlFlagsExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmControlFlagsExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmControlFlagsExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmControlFlagsExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bitFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitFlags
  // case: toBeCopied == COPY_DATA for bitFlags
     unsigned long bitFlags_copy = p_bitFlags; 
     result->p_bitFlags = bitFlags_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmControlFlagsExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmControlFlagsExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 275765 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmExprListExp::copy ( SgCopyHelp& help) const
   {
     SgAsmExprListExp* result = NULL;

  // printf ("Copy SgAsmExprListExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmExprListExp*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmExprListExp*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmExprListExp: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmExprListExp*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmExprListExp(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgAsmExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgAsmExpressionPtrList::const_iterator source_expressions_iterator = get_expressions().begin(); 
     for ( /* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) 
        { 
          SgAsmExpression* source_list_element = *source_expressions_iterator; 
          SgAsmExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          expressions_copy.push_back(copy_list_element); 
        } 
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
     SgAsmExpressionPtrList::const_iterator expressions_iterator = result->get_expressions().begin(); 
     for ( /* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) 
        { 
          SgAsmExpression* list_element = *expressions_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmExprListExp = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmExprListExp::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 275990 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmMemoryReferenceExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmMemoryReferenceExpression* result = NULL;

  // printf ("Copy SgAsmMemoryReferenceExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmMemoryReferenceExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmMemoryReferenceExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmMemoryReferenceExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmMemoryReferenceExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmMemoryReferenceExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_address
     SgAsmExpression* address_copy; 
  // case: not a listType for (using conditionalToCopyVariable)address
          if (get_address() != NULL) 
             { 
               address_copy = static_cast<SgAsmExpression*>(help.copyAst(get_address())); 
             } 
            else 
             { 
               address_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_address != NULL) delete result->p_address; 
     result->p_address = address_copy; 
  // case: not a listType for (using conditionalToSetParent)address
          if ( (address_copy != NULL) && (address_copy->get_parent() == NULL) && (isSgType(address_copy) == NULL) ) 
             { 
               address_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_segment
     SgAsmExpression* segment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segment
          if (get_segment() != NULL) 
             { 
               segment_copy = static_cast<SgAsmExpression*>(help.copyAst(get_segment())); 
             } 
            else 
             { 
               segment_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segment != NULL) delete result->p_segment; 
     result->p_segment = segment_copy; 
  // case: not a listType for (using conditionalToSetParent)segment
          if ( (segment_copy != NULL) && (segment_copy->get_parent() == NULL) && (isSgType(segment_copy) == NULL) ) 
             { 
               segment_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmMemoryReferenceExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmMemoryReferenceExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 276222 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmRegisterNames::copy ( SgCopyHelp& help) const
   {
     SgAsmRegisterNames* result = NULL;

  // printf ("Copy SgAsmRegisterNames = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmRegisterNames*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmRegisterNames*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmRegisterNames: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmRegisterNames*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmRegisterNames(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_registers
     SgAsmRegisterReferenceExpressionPtrList registers_copy; 
  // case: listType for registers
  // case: listType (typeIsPointerToList == false) for registers
     SgAsmRegisterReferenceExpressionPtrList::const_iterator source_registers_iterator = get_registers().begin(); 
     for ( /* empty by design */; source_registers_iterator != get_registers().end(); ++source_registers_iterator) 
        { 
          SgAsmRegisterReferenceExpression* source_list_element = *source_registers_iterator; 
          SgAsmRegisterReferenceExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmRegisterReferenceExpression*>(help.copyAst(*source_registers_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          registers_copy.push_back(copy_list_element); 
        } 
     result->p_registers = registers_copy; 
  // case: listType for registers
  // case: listType (typeIsPointerToList == false) for registers
     SgAsmRegisterReferenceExpressionPtrList::const_iterator registers_iterator = result->get_registers().begin(); 
     for ( /* empty by design */; registers_iterator != result->get_registers().end(); ++registers_iterator) 
        { 
          SgAsmRegisterReferenceExpression* list_element = *registers_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_mask
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mask
  // case: toBeCopied == COPY_DATA for mask
     unsigned mask_copy = p_mask; 
     result->p_mask = mask_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmRegisterNames = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmRegisterNames::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 276452 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmRegisterReferenceExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmRegisterReferenceExpression* result = NULL;

  // printf ("Copy SgAsmRegisterReferenceExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmRegisterReferenceExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmRegisterReferenceExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmRegisterReferenceExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmRegisterReferenceExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmRegisterReferenceExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_descriptor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor
  // case: toBeCopied == COPY_DATA for descriptor
     Rose::BinaryAnalysis::RegisterDescriptor descriptor_copy = p_descriptor; 
     result->p_descriptor = descriptor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_adjustment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adjustment
  // case: toBeCopied == COPY_DATA for adjustment
     int adjustment_copy = p_adjustment; 
     result->p_adjustment = adjustment_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmRegisterReferenceExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmRegisterReferenceExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 276656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDirectRegisterExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmDirectRegisterExpression* result = NULL;

  // printf ("Copy SgAsmDirectRegisterExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmDirectRegisterExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmDirectRegisterExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmDirectRegisterExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmDirectRegisterExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmDirectRegisterExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_descriptor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor
  // case: toBeCopied == COPY_DATA for descriptor
     Rose::BinaryAnalysis::RegisterDescriptor descriptor_copy = p_descriptor; 
     result->p_descriptor = descriptor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_adjustment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adjustment
  // case: toBeCopied == COPY_DATA for adjustment
     int adjustment_copy = p_adjustment; 
     result->p_adjustment = adjustment_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmDirectRegisterExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmDirectRegisterExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 276860 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmIndirectRegisterExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmIndirectRegisterExpression* result = NULL;

  // printf ("Copy SgAsmIndirectRegisterExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmIndirectRegisterExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmIndirectRegisterExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmIndirectRegisterExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmIndirectRegisterExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmIndirectRegisterExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_descriptor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor
  // case: toBeCopied == COPY_DATA for descriptor
     Rose::BinaryAnalysis::RegisterDescriptor descriptor_copy = p_descriptor; 
     result->p_descriptor = descriptor_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_adjustment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adjustment
  // case: toBeCopied == COPY_DATA for adjustment
     int adjustment_copy = p_adjustment; 
     result->p_adjustment = adjustment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_stride
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stride
  // case: toBeCopied == COPY_DATA for stride
     Rose::BinaryAnalysis::RegisterDescriptor stride_copy = p_stride; 
     result->p_stride = stride_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::RegisterDescriptor offset_copy = p_offset; 
     result->p_offset = offset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     size_t index_copy = p_index; 
     result->p_index = index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_modulus
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modulus
  // case: toBeCopied == COPY_DATA for modulus
     size_t modulus_copy = p_modulus; 
     result->p_modulus = modulus_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmIndirectRegisterExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmIndirectRegisterExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 277084 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmRiscOperation::copy ( SgCopyHelp& help) const
   {
     SgAsmRiscOperation* result = NULL;

  // printf ("Copy SgAsmRiscOperation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmRiscOperation*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmRiscOperation*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmRiscOperation: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmRiscOperation*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmRiscOperation(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_riscOperator
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for riscOperator
  // case: toBeCopied == COPY_DATA for riscOperator
     SgAsmRiscOperation::RiscOperator riscOperator_copy = p_riscOperator; 
     result->p_riscOperator = riscOperator_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operands
     SgAsmExprListExp* operands_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operands
          if (get_operands() != NULL) 
             { 
               operands_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_operands())); 
             } 
            else 
             { 
               operands_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operands != NULL) delete result->p_operands; 
     result->p_operands = operands_copy; 
  // case: not a listType for (using conditionalToSetParent)operands
          if ( (operands_copy != NULL) && (operands_copy->get_parent() == NULL) && (isSgType(operands_copy) == NULL) ) 
             { 
               operands_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmRiscOperation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmRiscOperation::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 277302 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStackExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmStackExpression* result = NULL;

  // printf ("Copy SgAsmStackExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmStackExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmStackExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmStackExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmStackExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmStackExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_stackPosition
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stackPosition
  // case: toBeCopied == COPY_DATA for stackPosition
     int stackPosition_copy = p_stackPosition; 
     result->p_stackPosition = stackPosition_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmStackExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmStackExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 277501 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmUnaryExpression* result = NULL;

  // printf ("Copy SgAsmUnaryExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmUnaryExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmUnaryExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmUnaryExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmUnaryExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmUnaryExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != NULL) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != NULL) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
          if ( (operand_copy != NULL) && (operand_copy->get_parent() == NULL) && (isSgType(operand_copy) == NULL) ) 
             { 
               operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmUnaryExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmUnaryExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 277714 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryMinus::copy ( SgCopyHelp& help) const
   {
     SgAsmUnaryMinus* result = NULL;

  // printf ("Copy SgAsmUnaryMinus = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmUnaryMinus*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmUnaryMinus*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmUnaryMinus: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmUnaryMinus*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmUnaryMinus(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != NULL) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != NULL) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
          if ( (operand_copy != NULL) && (operand_copy->get_parent() == NULL) && (isSgType(operand_copy) == NULL) ) 
             { 
               operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmUnaryMinus = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmUnaryMinus::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 277927 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryPlus::copy ( SgCopyHelp& help) const
   {
     SgAsmUnaryPlus* result = NULL;

  // printf ("Copy SgAsmUnaryPlus = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmUnaryPlus*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmUnaryPlus*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmUnaryPlus: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmUnaryPlus*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmUnaryPlus(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != NULL) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != NULL) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
          if ( (operand_copy != NULL) && (operand_copy->get_parent() == NULL) && (isSgType(operand_copy) == NULL) ) 
             { 
               operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmUnaryPlus = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmUnaryPlus::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 278140 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryRrx::copy ( SgCopyHelp& help) const
   {
     SgAsmUnaryRrx* result = NULL;

  // printf ("Copy SgAsmUnaryRrx = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmUnaryRrx*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmUnaryRrx*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmUnaryRrx: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmUnaryRrx*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmUnaryRrx(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != NULL) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != NULL) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
          if ( (operand_copy != NULL) && (operand_copy->get_parent() == NULL) && (isSgType(operand_copy) == NULL) ) 
             { 
               operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmUnaryRrx = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmUnaryRrx::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 278353 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnarySignedExtend::copy ( SgCopyHelp& help) const
   {
     SgAsmUnarySignedExtend* result = NULL;

  // printf ("Copy SgAsmUnarySignedExtend = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmUnarySignedExtend*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmUnarySignedExtend*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmUnarySignedExtend: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmUnarySignedExtend*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmUnarySignedExtend(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != NULL) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != NULL) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
          if ( (operand_copy != NULL) && (operand_copy->get_parent() == NULL) && (isSgType(operand_copy) == NULL) ) 
             { 
               operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmUnarySignedExtend = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmUnarySignedExtend::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 278566 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryTruncate::copy ( SgCopyHelp& help) const
   {
     SgAsmUnaryTruncate* result = NULL;

  // printf ("Copy SgAsmUnaryTruncate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmUnaryTruncate*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmUnaryTruncate*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmUnaryTruncate: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmUnaryTruncate*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmUnaryTruncate(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != NULL) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != NULL) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
          if ( (operand_copy != NULL) && (operand_copy->get_parent() == NULL) && (isSgType(operand_copy) == NULL) ) 
             { 
               operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmUnaryTruncate = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmUnaryTruncate::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 278779 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryUnsignedExtend::copy ( SgCopyHelp& help) const
   {
     SgAsmUnaryUnsignedExtend* result = NULL;

  // printf ("Copy SgAsmUnaryUnsignedExtend = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmUnaryUnsignedExtend*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmUnaryUnsignedExtend*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmUnaryUnsignedExtend: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmUnaryUnsignedExtend*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmUnaryUnsignedExtend(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != NULL) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != NULL) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
          if ( (operand_copy != NULL) && (operand_copy->get_parent() == NULL) && (isSgType(operand_copy) == NULL) ) 
             { 
               operand_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmUnaryUnsignedExtend = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmUnaryUnsignedExtend::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 278992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmValueExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmValueExpression* result = NULL;

  // printf ("Copy SgAsmValueExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmValueExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmValueExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmValueExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmValueExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmValueExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_unfoldedExpression
     SgAsmValueExpression* unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unfoldedExpression
          if (get_unfoldedExpression() != NULL) 
             { 
               unfoldedExpression_copy = static_cast<SgAsmValueExpression*>(help.copyAst(get_unfoldedExpression())); 
             } 
            else 
             { 
               unfoldedExpression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unfoldedExpression != NULL) delete result->p_unfoldedExpression; 
     result->p_unfoldedExpression = unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToSetParent)unfoldedExpression
          if ( (unfoldedExpression_copy != NULL) && (unfoldedExpression_copy->get_parent() == NULL) && (isSgType(unfoldedExpression_copy) == NULL) ) 
             { 
               unfoldedExpression_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_bitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitOffset
  // case: toBeCopied == COPY_DATA for bitOffset
     unsigned short bitOffset_copy = p_bitOffset; 
     result->p_bitOffset = bitOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bitSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitSize
  // case: toBeCopied == COPY_DATA for bitSize
     unsigned short bitSize_copy = p_bitSize; 
     result->p_bitSize = bitSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_symbol
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
     result->p_symbol = symbol_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmValueExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmValueExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 279220 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmConstantExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmConstantExpression* result = NULL;

  // printf ("Copy SgAsmConstantExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmConstantExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmConstantExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmConstantExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmConstantExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmConstantExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_unfoldedExpression
     SgAsmValueExpression* unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unfoldedExpression
          if (get_unfoldedExpression() != NULL) 
             { 
               unfoldedExpression_copy = static_cast<SgAsmValueExpression*>(help.copyAst(get_unfoldedExpression())); 
             } 
            else 
             { 
               unfoldedExpression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unfoldedExpression != NULL) delete result->p_unfoldedExpression; 
     result->p_unfoldedExpression = unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToSetParent)unfoldedExpression
          if ( (unfoldedExpression_copy != NULL) && (unfoldedExpression_copy->get_parent() == NULL) && (isSgType(unfoldedExpression_copy) == NULL) ) 
             { 
               unfoldedExpression_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_bitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitOffset
  // case: toBeCopied == COPY_DATA for bitOffset
     unsigned short bitOffset_copy = p_bitOffset; 
     result->p_bitOffset = bitOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bitSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitSize
  // case: toBeCopied == COPY_DATA for bitSize
     unsigned short bitSize_copy = p_bitSize; 
     result->p_bitSize = bitSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_symbol
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
     result->p_symbol = symbol_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bitVector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitVector
  // case: toBeCopied == COPY_DATA for bitVector
     Sawyer::Container::BitVector bitVector_copy = p_bitVector; 
     result->p_bitVector = bitVector_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmConstantExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmConstantExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 279453 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmFloatValueExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmFloatValueExpression* result = NULL;

  // printf ("Copy SgAsmFloatValueExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmFloatValueExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmFloatValueExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmFloatValueExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmFloatValueExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmFloatValueExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_unfoldedExpression
     SgAsmValueExpression* unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unfoldedExpression
          if (get_unfoldedExpression() != NULL) 
             { 
               unfoldedExpression_copy = static_cast<SgAsmValueExpression*>(help.copyAst(get_unfoldedExpression())); 
             } 
            else 
             { 
               unfoldedExpression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unfoldedExpression != NULL) delete result->p_unfoldedExpression; 
     result->p_unfoldedExpression = unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToSetParent)unfoldedExpression
          if ( (unfoldedExpression_copy != NULL) && (unfoldedExpression_copy->get_parent() == NULL) && (isSgType(unfoldedExpression_copy) == NULL) ) 
             { 
               unfoldedExpression_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_bitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitOffset
  // case: toBeCopied == COPY_DATA for bitOffset
     unsigned short bitOffset_copy = p_bitOffset; 
     result->p_bitOffset = bitOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bitSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitSize
  // case: toBeCopied == COPY_DATA for bitSize
     unsigned short bitSize_copy = p_bitSize; 
     result->p_bitSize = bitSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_symbol
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
     result->p_symbol = symbol_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bitVector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitVector
  // case: toBeCopied == COPY_DATA for bitVector
     Sawyer::Container::BitVector bitVector_copy = p_bitVector; 
     result->p_bitVector = bitVector_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmFloatValueExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmFloatValueExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 279686 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmIntegerValueExpression::copy ( SgCopyHelp& help) const
   {
     SgAsmIntegerValueExpression* result = NULL;

  // printf ("Copy SgAsmIntegerValueExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmIntegerValueExpression*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmIntegerValueExpression*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmIntegerValueExpression: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmIntegerValueExpression*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmIntegerValueExpression(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_unfoldedExpression
     SgAsmValueExpression* unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unfoldedExpression
          if (get_unfoldedExpression() != NULL) 
             { 
               unfoldedExpression_copy = static_cast<SgAsmValueExpression*>(help.copyAst(get_unfoldedExpression())); 
             } 
            else 
             { 
               unfoldedExpression_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unfoldedExpression != NULL) delete result->p_unfoldedExpression; 
     result->p_unfoldedExpression = unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToSetParent)unfoldedExpression
          if ( (unfoldedExpression_copy != NULL) && (unfoldedExpression_copy->get_parent() == NULL) && (isSgType(unfoldedExpression_copy) == NULL) ) 
             { 
               unfoldedExpression_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_bitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitOffset
  // case: toBeCopied == COPY_DATA for bitOffset
     unsigned short bitOffset_copy = p_bitOffset; 
     result->p_bitOffset = bitOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bitSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitSize
  // case: toBeCopied == COPY_DATA for bitSize
     unsigned short bitSize_copy = p_bitSize; 
     result->p_bitSize = bitSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_symbol
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
     result->p_symbol = symbol_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bitVector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitVector
  // case: toBeCopied == COPY_DATA for bitVector
     Sawyer::Container::BitVector bitVector_copy = p_bitVector; 
     result->p_bitVector = bitVector_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseNode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseNode
  // case: toBeCopied == COPY_DATA for baseNode
     SgNode* baseNode_copy = p_baseNode; 
     result->p_baseNode = baseNode_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmIntegerValueExpression = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmIntegerValueExpression::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 279924 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericFileList::copy ( SgCopyHelp& help) const
   {
     SgAsmGenericFileList* result = NULL;

  // printf ("Copy SgAsmGenericFileList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmGenericFileList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmGenericFileList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmGenericFileList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmGenericFileList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmGenericFileList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_files
     SgAsmGenericFilePtrList files_copy; 
  // case: listType for files
  // case: listType (typeIsPointerToList == false) for files
     SgAsmGenericFilePtrList::const_iterator source_files_iterator = get_files().begin(); 
     for ( /* empty by design */; source_files_iterator != get_files().end(); ++source_files_iterator) 
        { 
          SgAsmGenericFile* source_list_element = *source_files_iterator; 
          SgAsmGenericFile* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmGenericFile*>(help.copyAst(*source_files_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          files_copy.push_back(copy_list_element); 
        } 
     result->p_files = files_copy; 
  // case: listType for files
  // case: listType (typeIsPointerToList == false) for files
     SgAsmGenericFilePtrList::const_iterator files_iterator = result->get_files().begin(); 
     for ( /* empty by design */; files_iterator != result->get_files().end(); ++files_iterator) 
        { 
          SgAsmGenericFile* list_element = *files_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmGenericFileList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmGenericFileList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 280139 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmInstructionList::copy ( SgCopyHelp& help) const
   {
     SgAsmInstructionList* result = NULL;

  // printf ("Copy SgAsmInstructionList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmInstructionList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmInstructionList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmInstructionList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmInstructionList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmInstructionList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_instructions
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for instructions
  // case: toBeCopied == COPY_DATA for instructions
     SgAsmInstructionPtrList instructions_copy = p_instructions; 
     result->p_instructions = instructions_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmInstructionList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmInstructionList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 280328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmInterpretation::copy ( SgCopyHelp& help) const
   {
     SgAsmInterpretation* result = NULL;

  // printf ("Copy SgAsmInterpretation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmInterpretation*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmInterpretation*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmInterpretation: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmInterpretation*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmInterpretation(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_headers
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for headers
  // case: toBeCopied == COPY_DATA for headers
     SgAsmGenericHeaderList* headers_copy = p_headers; 
     result->p_headers = headers_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_globalBlock
     SgAsmBlock* globalBlock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)globalBlock
          if (get_globalBlock() != NULL) 
             { 
               globalBlock_copy = static_cast<SgAsmBlock*>(help.copyAst(get_globalBlock())); 
             } 
            else 
             { 
               globalBlock_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_globalBlock != NULL) delete result->p_globalBlock; 
     result->p_globalBlock = globalBlock_copy; 
  // case: not a listType for (using conditionalToSetParent)globalBlock
          if ( (globalBlock_copy != NULL) && (globalBlock_copy->get_parent() == NULL) && (isSgType(globalBlock_copy) == NULL) ) 
             { 
               globalBlock_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_coverageComputed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for coverageComputed
  // case: toBeCopied == COPY_DATA for coverageComputed
     bool coverageComputed_copy = p_coverageComputed; 
     result->p_coverageComputed = coverageComputed_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_percentageCoverage
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for percentageCoverage
  // case: toBeCopied == COPY_DATA for percentageCoverage
     double percentageCoverage_copy = p_percentageCoverage; 
     result->p_percentageCoverage = percentageCoverage_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmInterpretation = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmInterpretation::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 280546 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmInterpretationList::copy ( SgCopyHelp& help) const
   {
     SgAsmInterpretationList* result = NULL;

  // printf ("Copy SgAsmInterpretationList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmInterpretationList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmInterpretationList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmInterpretationList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmInterpretationList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmInterpretationList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_interpretations
     SgAsmInterpretationPtrList interpretations_copy; 
  // case: listType for interpretations
  // case: listType (typeIsPointerToList == false) for interpretations
     SgAsmInterpretationPtrList::const_iterator source_interpretations_iterator = get_interpretations().begin(); 
     for ( /* empty by design */; source_interpretations_iterator != get_interpretations().end(); ++source_interpretations_iterator) 
        { 
          SgAsmInterpretation* source_list_element = *source_interpretations_iterator; 
          SgAsmInterpretation* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmInterpretation*>(help.copyAst(*source_interpretations_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          interpretations_copy.push_back(copy_list_element); 
        } 
     result->p_interpretations = interpretations_copy; 
  // case: listType for interpretations
  // case: listType (typeIsPointerToList == false) for interpretations
     SgAsmInterpretationPtrList::const_iterator interpretations_iterator = result->get_interpretations().begin(); 
     for ( /* empty by design */; interpretations_iterator != result->get_interpretations().end(); ++interpretations_iterator) 
        { 
          SgAsmInterpretation* list_element = *interpretations_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmInterpretationList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmInterpretationList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 280761 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmNode::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmNode* result = NULL;

  // printf ("Copy SgAsmJvmNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmNode*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmNode*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmNode: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmNode*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmNode(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmNode = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmNode::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 280944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmAttribute::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmAttribute* result = NULL;

  // printf ("Copy SgAsmJvmAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmAttribute*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmAttribute*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmAttribute: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmAttribute*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmAttribute(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmAttribute::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 281138 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmBootstrapMethods::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmBootstrapMethods* result = NULL;

  // printf ("Copy SgAsmJvmBootstrapMethods = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmBootstrapMethods*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmBootstrapMethods*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmBootstrapMethods: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmBootstrapMethods*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmBootstrapMethods(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bootstrap_methods
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bootstrap_methods
  // case: toBeCopied == COPY_DATA for bootstrap_methods
     SgAsmJvmBootstrapMethodPtrList bootstrap_methods_copy = p_bootstrap_methods; 
     result->p_bootstrap_methods = bootstrap_methods_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmBootstrapMethods = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmBootstrapMethods::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 281337 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmCodeAttribute::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmCodeAttribute* result = NULL;

  // printf ("Copy SgAsmJvmCodeAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmCodeAttribute*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmCodeAttribute*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmCodeAttribute: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmCodeAttribute*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmCodeAttribute(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_max_stack
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for max_stack
  // case: toBeCopied == COPY_DATA for max_stack
     uint16_t max_stack_copy = p_max_stack; 
     result->p_max_stack = max_stack_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_max_locals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for max_locals
  // case: toBeCopied == COPY_DATA for max_locals
     uint16_t max_locals_copy = p_max_locals; 
     result->p_max_locals = max_locals_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_code_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for code_length
  // case: toBeCopied == COPY_DATA for code_length
     uint32_t code_length_copy = p_code_length; 
     result->p_code_length = code_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_code
     const char* code_copy; 
  // case: typeName == char* or char** for code
     if (p_code != NULL) 
          code_copy = strdup(p_code); 
     code_copy = p_code; 
     result->p_code = code_copy;
  // case: typeName == char* or char** for code
  // Copy non-constructor parameter data member (no access function): result->p_exception_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exception_table
  // case: toBeCopied == COPY_DATA for exception_table
     SgAsmJvmExceptionTable* exception_table_copy = p_exception_table; 
     result->p_exception_table = exception_table_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_table
  // case: toBeCopied == COPY_DATA for attribute_table
     SgAsmJvmAttributeTable* attribute_table_copy = p_attribute_table; 
     result->p_attribute_table = attribute_table_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_code_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for code_offset
  // case: toBeCopied == COPY_DATA for code_offset
     Rose::BinaryAnalysis::Address code_offset_copy = p_code_offset; 
     result->p_code_offset = code_offset_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmCodeAttribute = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmCodeAttribute::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 281569 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmConstantValue::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmConstantValue* result = NULL;

  // printf ("Copy SgAsmJvmConstantValue = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmConstantValue*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmConstantValue*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmConstantValue: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmConstantValue*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmConstantValue(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_constantvalue_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constantvalue_index
  // case: toBeCopied == COPY_DATA for constantvalue_index
     uint16_t constantvalue_index_copy = p_constantvalue_index; 
     result->p_constantvalue_index = constantvalue_index_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmConstantValue = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmConstantValue::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 281768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmDeprecated::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmDeprecated* result = NULL;

  // printf ("Copy SgAsmJvmDeprecated = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmDeprecated*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmDeprecated*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmDeprecated: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmDeprecated*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmDeprecated(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmDeprecated = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmDeprecated::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 281962 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmEnclosingMethod::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmEnclosingMethod* result = NULL;

  // printf ("Copy SgAsmJvmEnclosingMethod = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmEnclosingMethod*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmEnclosingMethod*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmEnclosingMethod: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmEnclosingMethod*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmEnclosingMethod(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_class_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_index
  // case: toBeCopied == COPY_DATA for class_index
     uint16_t class_index_copy = p_class_index; 
     result->p_class_index = class_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_method_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for method_index
  // case: toBeCopied == COPY_DATA for method_index
     uint16_t method_index_copy = p_method_index; 
     result->p_method_index = method_index_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmEnclosingMethod = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmEnclosingMethod::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 282166 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmExceptions::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmExceptions* result = NULL;

  // printf ("Copy SgAsmJvmExceptions = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmExceptions*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmExceptions*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmExceptions: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmExceptions*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmExceptions(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_exception_index_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exception_index_table
  // case: toBeCopied == COPY_DATA for exception_index_table
     SgUnsigned16List exception_index_table_copy = p_exception_index_table; 
     result->p_exception_index_table = exception_index_table_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmExceptions = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmExceptions::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 282365 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmInnerClasses::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmInnerClasses* result = NULL;

  // printf ("Copy SgAsmJvmInnerClasses = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmInnerClasses*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmInnerClasses*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmInnerClasses: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmInnerClasses*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmInnerClasses(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_classes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for classes
  // case: toBeCopied == COPY_DATA for classes
     SgAsmJvmInnerClassesEntryPtrList classes_copy = p_classes; 
     result->p_classes = classes_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmInnerClasses = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmInnerClasses::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 282564 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLineNumberTable::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmLineNumberTable* result = NULL;

  // printf ("Copy SgAsmJvmLineNumberTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmLineNumberTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmLineNumberTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmLineNumberTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmLineNumberTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmLineNumberTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_line_number_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for line_number_table
  // case: toBeCopied == COPY_DATA for line_number_table
     SgAsmJvmLineNumberEntryPtrList line_number_table_copy = p_line_number_table; 
     result->p_line_number_table = line_number_table_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmLineNumberTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmLineNumberTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 282763 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLocalVariableTable::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmLocalVariableTable* result = NULL;

  // printf ("Copy SgAsmJvmLocalVariableTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmLocalVariableTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmLocalVariableTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmLocalVariableTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmLocalVariableTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmLocalVariableTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_local_variable_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for local_variable_table
  // case: toBeCopied == COPY_DATA for local_variable_table
     SgAsmJvmLocalVariableEntryPtrList local_variable_table_copy = p_local_variable_table; 
     result->p_local_variable_table = local_variable_table_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmLocalVariableTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmLocalVariableTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 282962 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLocalVariableTypeTable::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmLocalVariableTypeTable* result = NULL;

  // printf ("Copy SgAsmJvmLocalVariableTypeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmLocalVariableTypeTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmLocalVariableTypeTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmLocalVariableTypeTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmLocalVariableTypeTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmLocalVariableTypeTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_local_variable_type_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for local_variable_type_table
  // case: toBeCopied == COPY_DATA for local_variable_type_table
     SgAsmJvmLocalVariableTypeEntryPtrList local_variable_type_table_copy = p_local_variable_type_table; 
     result->p_local_variable_type_table = local_variable_type_table_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmLocalVariableTypeTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmLocalVariableTypeTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 283161 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmMethodParameters::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmMethodParameters* result = NULL;

  // printf ("Copy SgAsmJvmMethodParameters = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmMethodParameters*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmMethodParameters*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmMethodParameters: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmMethodParameters*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmMethodParameters(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_parameters
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameters
  // case: toBeCopied == COPY_DATA for parameters
     SgAsmJvmMethodParametersEntryPtrList parameters_copy = p_parameters; 
     result->p_parameters = parameters_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmMethodParameters = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmMethodParameters::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 283360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmModuleMainClass::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmModuleMainClass* result = NULL;

  // printf ("Copy SgAsmJvmModuleMainClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmModuleMainClass*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmModuleMainClass*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmModuleMainClass: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmModuleMainClass*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmModuleMainClass(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_main_class_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for main_class_index
  // case: toBeCopied == COPY_DATA for main_class_index
     uint16_t main_class_index_copy = p_main_class_index; 
     result->p_main_class_index = main_class_index_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmModuleMainClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmModuleMainClass::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 283559 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmNestHost::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmNestHost* result = NULL;

  // printf ("Copy SgAsmJvmNestHost = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmNestHost*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmNestHost*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmNestHost: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmNestHost*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmNestHost(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_host_class_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for host_class_index
  // case: toBeCopied == COPY_DATA for host_class_index
     uint16_t host_class_index_copy = p_host_class_index; 
     result->p_host_class_index = host_class_index_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmNestHost = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmNestHost::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 283758 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmNestMembers::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmNestMembers* result = NULL;

  // printf ("Copy SgAsmJvmNestMembers = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmNestMembers*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmNestMembers*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmNestMembers: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmNestMembers*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmNestMembers(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_classes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for classes
  // case: toBeCopied == COPY_DATA for classes
     SgUnsigned16List classes_copy = p_classes; 
     result->p_classes = classes_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmNestMembers = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmNestMembers::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 283957 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmSignature::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmSignature* result = NULL;

  // printf ("Copy SgAsmJvmSignature = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmSignature*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmSignature*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmSignature: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmSignature*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmSignature(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_signature_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for signature_index
  // case: toBeCopied == COPY_DATA for signature_index
     uint16_t signature_index_copy = p_signature_index; 
     result->p_signature_index = signature_index_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmSignature = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmSignature::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 284156 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmSourceFile::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmSourceFile* result = NULL;

  // printf ("Copy SgAsmJvmSourceFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmSourceFile*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmSourceFile*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmSourceFile: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmSourceFile*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmSourceFile(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_sourcefile_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourcefile_index
  // case: toBeCopied == COPY_DATA for sourcefile_index
     uint16_t sourcefile_index_copy = p_sourcefile_index; 
     result->p_sourcefile_index = sourcefile_index_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmSourceFile = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmSourceFile::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 284355 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmStackMapTable::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmStackMapTable* result = NULL;

  // printf ("Copy SgAsmJvmStackMapTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmStackMapTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmStackMapTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmStackMapTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmStackMapTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmStackMapTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmJvmStackMapFramePtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmStackMapTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmStackMapTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 284554 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmSynthetic::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmSynthetic* result = NULL;

  // printf ("Copy SgAsmJvmSynthetic = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmSynthetic*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmSynthetic*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmSynthetic: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmSynthetic*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmSynthetic(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmSynthetic = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmSynthetic::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 284748 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmBootstrapMethod::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmBootstrapMethod* result = NULL;

  // printf ("Copy SgAsmJvmBootstrapMethod = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmBootstrapMethod*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmBootstrapMethod*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmBootstrapMethod: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmBootstrapMethod*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmBootstrapMethod(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_bootstrap_method_ref
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bootstrap_method_ref
  // case: toBeCopied == COPY_DATA for bootstrap_method_ref
     uint16_t bootstrap_method_ref_copy = p_bootstrap_method_ref; 
     result->p_bootstrap_method_ref = bootstrap_method_ref_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bootstrap_arguments
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bootstrap_arguments
  // case: toBeCopied == COPY_DATA for bootstrap_arguments
     SgUnsigned16List bootstrap_arguments_copy = p_bootstrap_arguments; 
     result->p_bootstrap_arguments = bootstrap_arguments_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmBootstrapMethod = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmBootstrapMethod::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 284942 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmClass::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmClass* result = NULL;

  // printf ("Copy SgAsmJvmClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmClass*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmClass*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmClass: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmClass*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmClass(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmClass = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmClass::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 285125 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmConstantPoolEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmConstantPoolEntry* result = NULL;

  // printf ("Copy SgAsmJvmConstantPoolEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmConstantPoolEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmConstantPoolEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmConstantPoolEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmConstantPoolEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmConstantPoolEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_tag
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tag
  // case: toBeCopied == COPY_DATA for tag
     SgAsmJvmConstantPoolEntry::Kind tag_copy = p_tag; 
     result->p_tag = tag_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bytes
  // case: toBeCopied == COPY_DATA for bytes
     uint32_t bytes_copy = p_bytes; 
     result->p_bytes = bytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_hi_bytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hi_bytes
  // case: toBeCopied == COPY_DATA for hi_bytes
     uint32_t hi_bytes_copy = p_hi_bytes; 
     result->p_hi_bytes = hi_bytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_low_bytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for low_bytes
  // case: toBeCopied == COPY_DATA for low_bytes
     uint32_t low_bytes_copy = p_low_bytes; 
     result->p_low_bytes = low_bytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_bootstrap_method_attr_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bootstrap_method_attr_index
  // case: toBeCopied == COPY_DATA for bootstrap_method_attr_index
     uint16_t bootstrap_method_attr_index_copy = p_bootstrap_method_attr_index; 
     result->p_bootstrap_method_attr_index = bootstrap_method_attr_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_class_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_index
  // case: toBeCopied == COPY_DATA for class_index
     uint16_t class_index_copy = p_class_index; 
     result->p_class_index = class_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_descriptor_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor_index
  // case: toBeCopied == COPY_DATA for descriptor_index
     uint16_t descriptor_index_copy = p_descriptor_index; 
     result->p_descriptor_index = descriptor_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_and_type_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_and_type_index
  // case: toBeCopied == COPY_DATA for name_and_type_index
     uint16_t name_and_type_index_copy = p_name_and_type_index; 
     result->p_name_and_type_index = name_and_type_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_reference_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reference_index
  // case: toBeCopied == COPY_DATA for reference_index
     uint16_t reference_index_copy = p_reference_index; 
     result->p_reference_index = reference_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_reference_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reference_kind
  // case: toBeCopied == COPY_DATA for reference_kind
     uint8_t reference_kind_copy = p_reference_kind; 
     result->p_reference_kind = reference_kind_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_string_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_index
  // case: toBeCopied == COPY_DATA for string_index
     uint16_t string_index_copy = p_string_index; 
     result->p_string_index = string_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for length
  // case: toBeCopied == COPY_DATA for length
     uint16_t length_copy = p_length; 
     result->p_length = length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_utf8_bytes
     char* utf8_bytes_copy; 
  // case: typeName == char* or char** for utf8_bytes
     if (p_utf8_bytes != NULL) 
          utf8_bytes_copy = strdup(p_utf8_bytes); 
     utf8_bytes_copy = p_utf8_bytes; 
     result->p_utf8_bytes = utf8_bytes_copy;
  // case: typeName == char* or char** for utf8_bytes

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmConstantPoolEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmConstantPoolEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 285382 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmExceptionHandler::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmExceptionHandler* result = NULL;

  // printf ("Copy SgAsmJvmExceptionHandler = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmExceptionHandler*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmExceptionHandler*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmExceptionHandler: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmExceptionHandler*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmExceptionHandler(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_start_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for start_pc
  // case: toBeCopied == COPY_DATA for start_pc
     uint16_t start_pc_copy = p_start_pc; 
     result->p_start_pc = start_pc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_end_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_pc
  // case: toBeCopied == COPY_DATA for end_pc
     uint16_t end_pc_copy = p_end_pc; 
     result->p_end_pc = end_pc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_handler_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for handler_pc
  // case: toBeCopied == COPY_DATA for handler_pc
     uint16_t handler_pc_copy = p_handler_pc; 
     result->p_handler_pc = handler_pc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_catch_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for catch_type
  // case: toBeCopied == COPY_DATA for catch_type
     uint16_t catch_type_copy = p_catch_type; 
     result->p_catch_type = catch_type_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmExceptionHandler = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmExceptionHandler::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 285586 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmExceptionTable::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmExceptionTable* result = NULL;

  // printf ("Copy SgAsmJvmExceptionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmExceptionTable*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmExceptionTable*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmExceptionTable: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmExceptionTable*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmExceptionTable(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_handlers
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for handlers
  // case: toBeCopied == COPY_DATA for handlers
     SgAsmJvmExceptionHandlerPtrList handlers_copy = p_handlers; 
     result->p_handlers = handlers_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmExceptionTable = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmExceptionTable::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 285775 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmField::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmField* result = NULL;

  // printf ("Copy SgAsmJvmField = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmField*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmField*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmField: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmField*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmField(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_flags
  // case: toBeCopied == COPY_DATA for access_flags
     uint16_t access_flags_copy = p_access_flags; 
     result->p_access_flags = access_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_descriptor_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor_index
  // case: toBeCopied == COPY_DATA for descriptor_index
     uint16_t descriptor_index_copy = p_descriptor_index; 
     result->p_descriptor_index = descriptor_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_table
     SgAsmJvmAttributeTable* attribute_table_copy; 
  // case: not a listType for (using conditionalToCopyVariable)attribute_table
          if (get_attribute_table() != NULL) 
             { 
               attribute_table_copy = static_cast<SgAsmJvmAttributeTable*>(help.copyAst(get_attribute_table())); 
             } 
            else 
             { 
               attribute_table_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attribute_table != NULL) delete result->p_attribute_table; 
     result->p_attribute_table = attribute_table_copy; 
  // case: not a listType for (using conditionalToSetParent)attribute_table
          if ( (attribute_table_copy != NULL) && (attribute_table_copy->get_parent() == NULL) && (isSgType(attribute_table_copy) == NULL) ) 
             { 
               attribute_table_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmField = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmField::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 285993 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmInnerClassesEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmInnerClassesEntry* result = NULL;

  // printf ("Copy SgAsmJvmInnerClassesEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmInnerClassesEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmInnerClassesEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmInnerClassesEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmInnerClassesEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmInnerClassesEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_inner_class_info_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inner_class_info_index
  // case: toBeCopied == COPY_DATA for inner_class_info_index
     uint16_t inner_class_info_index_copy = p_inner_class_info_index; 
     result->p_inner_class_info_index = inner_class_info_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_outer_class_info_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outer_class_info_index
  // case: toBeCopied == COPY_DATA for outer_class_info_index
     uint16_t outer_class_info_index_copy = p_outer_class_info_index; 
     result->p_outer_class_info_index = outer_class_info_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_inner_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inner_name_index
  // case: toBeCopied == COPY_DATA for inner_name_index
     uint16_t inner_name_index_copy = p_inner_name_index; 
     result->p_inner_name_index = inner_name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_inner_class_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inner_class_access_flags
  // case: toBeCopied == COPY_DATA for inner_class_access_flags
     uint16_t inner_class_access_flags_copy = p_inner_class_access_flags; 
     result->p_inner_class_access_flags = inner_class_access_flags_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmInnerClassesEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmInnerClassesEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 286197 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLineNumberEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmLineNumberEntry* result = NULL;

  // printf ("Copy SgAsmJvmLineNumberEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmLineNumberEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmLineNumberEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmLineNumberEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmLineNumberEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmLineNumberEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_start_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for start_pc
  // case: toBeCopied == COPY_DATA for start_pc
     uint16_t start_pc_copy = p_start_pc; 
     result->p_start_pc = start_pc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_line_number
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for line_number
  // case: toBeCopied == COPY_DATA for line_number
     uint16_t line_number_copy = p_line_number; 
     result->p_line_number = line_number_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmLineNumberEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmLineNumberEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 286391 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLocalVariableEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmLocalVariableEntry* result = NULL;

  // printf ("Copy SgAsmJvmLocalVariableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmLocalVariableEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmLocalVariableEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmLocalVariableEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmLocalVariableEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmLocalVariableEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_start_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for start_pc
  // case: toBeCopied == COPY_DATA for start_pc
     uint16_t start_pc_copy = p_start_pc; 
     result->p_start_pc = start_pc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for length
  // case: toBeCopied == COPY_DATA for length
     uint16_t length_copy = p_length; 
     result->p_length = length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_descriptor_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor_index
  // case: toBeCopied == COPY_DATA for descriptor_index
     uint16_t descriptor_index_copy = p_descriptor_index; 
     result->p_descriptor_index = descriptor_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     uint16_t index_copy = p_index; 
     result->p_index = index_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmLocalVariableEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmLocalVariableEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 286600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLocalVariableTypeEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmLocalVariableTypeEntry* result = NULL;

  // printf ("Copy SgAsmJvmLocalVariableTypeEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmLocalVariableTypeEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmLocalVariableTypeEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmLocalVariableTypeEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmLocalVariableTypeEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmLocalVariableTypeEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_start_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for start_pc
  // case: toBeCopied == COPY_DATA for start_pc
     uint16_t start_pc_copy = p_start_pc; 
     result->p_start_pc = start_pc_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for length
  // case: toBeCopied == COPY_DATA for length
     uint16_t length_copy = p_length; 
     result->p_length = length_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_signature_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for signature_index
  // case: toBeCopied == COPY_DATA for signature_index
     uint16_t signature_index_copy = p_signature_index; 
     result->p_signature_index = signature_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     uint16_t index_copy = p_index; 
     result->p_index = index_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmLocalVariableTypeEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmLocalVariableTypeEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 286809 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmMethod::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmMethod* result = NULL;

  // printf ("Copy SgAsmJvmMethod = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmMethod*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmMethod*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmMethod: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmMethod*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmMethod(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_flags
  // case: toBeCopied == COPY_DATA for access_flags
     uint16_t access_flags_copy = p_access_flags; 
     result->p_access_flags = access_flags_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_descriptor_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor_index
  // case: toBeCopied == COPY_DATA for descriptor_index
     uint16_t descriptor_index_copy = p_descriptor_index; 
     result->p_descriptor_index = descriptor_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_attribute_table
     SgAsmJvmAttributeTable* attribute_table_copy; 
  // case: not a listType for (using conditionalToCopyVariable)attribute_table
          if (get_attribute_table() != NULL) 
             { 
               attribute_table_copy = static_cast<SgAsmJvmAttributeTable*>(help.copyAst(get_attribute_table())); 
             } 
            else 
             { 
               attribute_table_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attribute_table != NULL) delete result->p_attribute_table; 
     result->p_attribute_table = attribute_table_copy; 
  // case: not a listType for (using conditionalToSetParent)attribute_table
          if ( (attribute_table_copy != NULL) && (attribute_table_copy->get_parent() == NULL) && (isSgType(attribute_table_copy) == NULL) ) 
             { 
               attribute_table_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_instruction_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for instruction_list
  // case: toBeCopied == COPY_DATA for instruction_list
     SgAsmInstructionList* instruction_list_copy = p_instruction_list; 
     result->p_instruction_list = instruction_list_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmMethod = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmMethod::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 287032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmMethodParametersEntry::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmMethodParametersEntry* result = NULL;

  // printf ("Copy SgAsmJvmMethodParametersEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmMethodParametersEntry*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmMethodParametersEntry*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmMethodParametersEntry: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmMethodParametersEntry*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmMethodParametersEntry(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_flags
  // case: toBeCopied == COPY_DATA for access_flags
     uint16_t access_flags_copy = p_access_flags; 
     result->p_access_flags = access_flags_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmMethodParametersEntry = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmMethodParametersEntry::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 287226 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmStackMapFrame::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmStackMapFrame* result = NULL;

  // printf ("Copy SgAsmJvmStackMapFrame = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmStackMapFrame*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmStackMapFrame*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmStackMapFrame: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmStackMapFrame*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmStackMapFrame(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_stack
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stack
  // case: toBeCopied == COPY_DATA for stack
     SgAsmJvmStackMapVerificationTypePtrList stack_copy = p_stack; 
     result->p_stack = stack_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_locals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for locals
  // case: toBeCopied == COPY_DATA for locals
     SgAsmJvmStackMapVerificationTypePtrList locals_copy = p_locals; 
     result->p_locals = locals_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_frame_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frame_type
  // case: toBeCopied == COPY_DATA for frame_type
     uint8_t frame_type_copy = p_frame_type; 
     result->p_frame_type = frame_type_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset_delta
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset_delta
  // case: toBeCopied == COPY_DATA for offset_delta
     uint16_t offset_delta_copy = p_offset_delta; 
     result->p_offset_delta = offset_delta_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmStackMapFrame = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmStackMapFrame::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 287430 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmStackMapVerificationType::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmStackMapVerificationType* result = NULL;

  // printf ("Copy SgAsmJvmStackMapVerificationType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmStackMapVerificationType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmStackMapVerificationType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmStackMapVerificationType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmStackMapVerificationType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmStackMapVerificationType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_tag
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tag
  // case: toBeCopied == COPY_DATA for tag
     uint8_t tag_copy = p_tag; 
     result->p_tag = tag_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_cpool_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cpool_index
  // case: toBeCopied == COPY_DATA for cpool_index
     uint16_t cpool_index_copy = p_cpool_index; 
     result->p_cpool_index = cpool_index_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint16_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmStackMapVerificationType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmStackMapVerificationType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 287629 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmOperandList::copy ( SgCopyHelp& help) const
   {
     SgAsmOperandList* result = NULL;

  // printf ("Copy SgAsmOperandList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmOperandList*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmOperandList*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmOperandList: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmOperandList*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmOperandList(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_operands
     SgAsmExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgAsmExpressionPtrList::const_iterator source_operands_iterator = get_operands().begin(); 
     for ( /* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) 
        { 
          SgAsmExpression* source_list_element = *source_operands_iterator; 
          SgAsmExpression* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          operands_copy.push_back(copy_list_element); 
        } 
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
     SgAsmExpressionPtrList::const_iterator operands_iterator = result->get_operands().begin(); 
     for ( /* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) 
        { 
          SgAsmExpression* list_element = *operands_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmOperandList = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmOperandList::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 287844 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStatement::copy ( SgCopyHelp& help) const
   {
     SgAsmStatement* result = NULL;

  // printf ("Copy SgAsmStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmStatement*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmStatement*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmStatement: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmStatement*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmStatement(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmStatement = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmStatement::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 288038 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBlock::copy ( SgCopyHelp& help) const
   {
     SgAsmBlock* result = NULL;

  // printf ("Copy SgAsmBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmBlock*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmBlock*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmBlock: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmBlock*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmBlock(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     Rose::BinaryAnalysis::Address id_copy = p_id; 
     result->p_id = id_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_reason
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reason
  // case: toBeCopied == COPY_DATA for reason
     unsigned reason_copy = p_reason; 
     result->p_reason = reason_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_statementList
     SgAsmStatementPtrList statementList_copy; 
  // case: listType for statementList
  // case: listType (typeIsPointerToList == false) for statementList
     SgAsmStatementPtrList::const_iterator source_statementList_iterator = get_statementList().begin(); 
     for ( /* empty by design */; source_statementList_iterator != get_statementList().end(); ++source_statementList_iterator) 
        { 
          SgAsmStatement* source_list_element = *source_statementList_iterator; 
          SgAsmStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmStatement*>(help.copyAst(*source_statementList_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          statementList_copy.push_back(copy_list_element); 
        } 
     result->p_statementList = statementList_copy; 
  // case: listType for statementList
  // case: listType (typeIsPointerToList == false) for statementList
     SgAsmStatementPtrList::const_iterator statementList_iterator = result->get_statementList().begin(); 
     for ( /* empty by design */; statementList_iterator != result->get_statementList().end(); ++statementList_iterator) 
        { 
          SgAsmStatement* list_element = *statementList_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_successors
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for successors
  // case: toBeCopied == COPY_DATA for successors
     SgAsmIntegerValuePtrList successors_copy = p_successors; 
     result->p_successors = successors_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_successorsComplete
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for successorsComplete
  // case: toBeCopied == COPY_DATA for successorsComplete
     bool successorsComplete_copy = p_successorsComplete; 
     result->p_successorsComplete = successorsComplete_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_immediateDominator
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for immediateDominator
  // case: toBeCopied == COPY_DATA for immediateDominator
     SgAsmBlock* immediateDominator_copy = p_immediateDominator; 
     result->p_immediateDominator = immediateDominator_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_cachedVertex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cachedVertex
  // case: toBeCopied == COPY_DATA for cachedVertex
     size_t cachedVertex_copy = p_cachedVertex; 
     result->p_cachedVertex = cachedVertex_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_codeLikelihood
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for codeLikelihood
  // case: toBeCopied == COPY_DATA for codeLikelihood
     double codeLikelihood_copy = p_codeLikelihood; 
     result->p_codeLikelihood = codeLikelihood_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_stackDeltaOut
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stackDeltaOut
  // case: toBeCopied == COPY_DATA for stackDeltaOut
     int64_t stackDeltaOut_copy = p_stackDeltaOut; 
     result->p_stackDeltaOut = stackDeltaOut_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmBlock = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmBlock::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 288303 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmInstruction::copy ( SgCopyHelp& help) const
   {
     SgAsmInstruction* result = NULL;

  // printf ("Copy SgAsmInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmInstruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmInstruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmInstruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmInstruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmInstruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmInstruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 288550 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilInstruction::copy ( SgCopyHelp& help) const
   {
     SgAsmCilInstruction* result = NULL;

  // printf ("Copy SgAsmCilInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmCilInstruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmCilInstruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmCilInstruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmCilInstruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmCilInstruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::CilInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmCilInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmCilInstruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 288802 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmInstruction::copy ( SgCopyHelp& help) const
   {
     SgAsmJvmInstruction* result = NULL;

  // printf ("Copy SgAsmJvmInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmJvmInstruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmJvmInstruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmJvmInstruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmJvmInstruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmJvmInstruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::JvmInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmJvmInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmJvmInstruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 289054 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmM68kInstruction::copy ( SgCopyHelp& help) const
   {
     SgAsmM68kInstruction* result = NULL;

  // printf ("Copy SgAsmM68kInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmM68kInstruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmM68kInstruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmM68kInstruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmM68kInstruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmM68kInstruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::M68kInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_dataFormat
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dataFormat
  // case: toBeCopied == COPY_DATA for dataFormat
     Rose::BinaryAnalysis::M68kDataFormat dataFormat_copy = p_dataFormat; 
     result->p_dataFormat = dataFormat_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmM68kInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmM68kInstruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 289311 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmMipsInstruction::copy ( SgCopyHelp& help) const
   {
     SgAsmMipsInstruction* result = NULL;

  // printf ("Copy SgAsmMipsInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmMipsInstruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmMipsInstruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmMipsInstruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmMipsInstruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmMipsInstruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::MipsInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmMipsInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmMipsInstruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 289563 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNullInstruction::copy ( SgCopyHelp& help) const
   {
     SgAsmNullInstruction* result = NULL;

  // printf ("Copy SgAsmNullInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmNullInstruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmNullInstruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmNullInstruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmNullInstruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmNullInstruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmNullInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmNullInstruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 289810 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPowerpcInstruction::copy ( SgCopyHelp& help) const
   {
     SgAsmPowerpcInstruction* result = NULL;

  // printf ("Copy SgAsmPowerpcInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPowerpcInstruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPowerpcInstruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPowerpcInstruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPowerpcInstruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPowerpcInstruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::PowerpcInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPowerpcInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPowerpcInstruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 290062 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUserInstruction::copy ( SgCopyHelp& help) const
   {
     SgAsmUserInstruction* result = NULL;

  // printf ("Copy SgAsmUserInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmUserInstruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmUserInstruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmUserInstruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmUserInstruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmUserInstruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     unsigned kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmUserInstruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmUserInstruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 290314 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmX86Instruction::copy ( SgCopyHelp& help) const
   {
     SgAsmX86Instruction* result = NULL;

  // printf ("Copy SgAsmX86Instruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmX86Instruction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmX86Instruction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmX86Instruction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmX86Instruction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmX86Instruction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != NULL) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != NULL) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
          if ( (operandList_copy != NULL) && (operandList_copy->get_parent() == NULL) && (isSgType(operandList_copy) == NULL) ) 
             { 
               operandList_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != NULL) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != NULL) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
          if ( (semantics_copy != NULL) && (semantics_copy->get_parent() == NULL) && (isSgType(semantics_copy) == NULL) ) 
             { 
               semantics_copy->set_parent(result); 
             } 
  // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::X86InstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_baseSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseSize
  // case: toBeCopied == COPY_DATA for baseSize
     Rose::BinaryAnalysis::X86InstructionSize baseSize_copy = p_baseSize; 
     result->p_baseSize = baseSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_operandSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operandSize
  // case: toBeCopied == COPY_DATA for operandSize
     Rose::BinaryAnalysis::X86InstructionSize operandSize_copy = p_operandSize; 
     result->p_operandSize = operandSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_addressSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for addressSize
  // case: toBeCopied == COPY_DATA for addressSize
     Rose::BinaryAnalysis::X86InstructionSize addressSize_copy = p_addressSize; 
     result->p_addressSize = addressSize_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_lockPrefix
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lockPrefix
  // case: toBeCopied == COPY_DATA for lockPrefix
     bool lockPrefix_copy = p_lockPrefix; 
     result->p_lockPrefix = lockPrefix_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_repeatPrefix
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for repeatPrefix
  // case: toBeCopied == COPY_DATA for repeatPrefix
     Rose::BinaryAnalysis::X86RepeatPrefix repeatPrefix_copy = p_repeatPrefix; 
     result->p_repeatPrefix = repeatPrefix_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_branchPrediction
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for branchPrediction
  // case: toBeCopied == COPY_DATA for branchPrediction
     Rose::BinaryAnalysis::X86BranchPrediction branchPrediction_copy = p_branchPrediction; 
     result->p_branchPrediction = branchPrediction_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_segmentOverride
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for segmentOverride
  // case: toBeCopied == COPY_DATA for segmentOverride
     Rose::BinaryAnalysis::X86SegmentRegister segmentOverride_copy = p_segmentOverride; 
     result->p_segmentOverride = segmentOverride_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmX86Instruction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmX86Instruction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 290601 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStaticData::copy ( SgCopyHelp& help) const
   {
     SgAsmStaticData* result = NULL;

  // printf ("Copy SgAsmStaticData = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmStaticData*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmStaticData*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmStaticData: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmStaticData*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmStaticData(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmStaticData = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmStaticData::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 290800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmSynthesizedDeclaration::copy ( SgCopyHelp& help) const
   {
     SgAsmSynthesizedDeclaration* result = NULL;

  // printf ("Copy SgAsmSynthesizedDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmSynthesizedDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmSynthesizedDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmSynthesizedDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmSynthesizedDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmSynthesizedDeclaration(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmSynthesizedDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmSynthesizedDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 290994 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmFunction::copy ( SgCopyHelp& help) const
   {
     SgAsmFunction* result = NULL;

  // printf ("Copy SgAsmFunction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmFunction*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmFunction*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmFunction: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmFunction*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmFunction(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_reason
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reason
  // case: toBeCopied == COPY_DATA for reason
     unsigned reason_copy = p_reason; 
     result->p_reason = reason_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_reasonComment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reasonComment
  // case: toBeCopied == COPY_DATA for reasonComment
     std::string reasonComment_copy = p_reasonComment; 
     result->p_reasonComment = reasonComment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_functionKind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionKind
  // case: toBeCopied == COPY_DATA for functionKind
     SgAsmFunction::function_kind_enum functionKind_copy = p_functionKind; 
     result->p_functionKind = functionKind_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_mayReturn
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mayReturn
  // case: toBeCopied == COPY_DATA for mayReturn
     SgAsmFunction::MayReturn mayReturn_copy = p_mayReturn; 
     result->p_mayReturn = mayReturn_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nameMd5
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameMd5
  // case: toBeCopied == COPY_DATA for nameMd5
     std::string nameMd5_copy = p_nameMd5; 
     result->p_nameMd5 = nameMd5_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_statementList
     SgAsmStatementPtrList statementList_copy; 
  // case: listType for statementList
  // case: listType (typeIsPointerToList == false) for statementList
     SgAsmStatementPtrList::const_iterator source_statementList_iterator = get_statementList().begin(); 
     for ( /* empty by design */; source_statementList_iterator != get_statementList().end(); ++source_statementList_iterator) 
        { 
          SgAsmStatement* source_list_element = *source_statementList_iterator; 
          SgAsmStatement* copy_list_element = NULL; 
          if (source_list_element != NULL) 
             { 
               copy_list_element = static_cast<SgAsmStatement*>(help.copyAst(*source_statementList_iterator)); 
             } 
            else 
             { 
               copy_list_element = NULL; 
             } 
          statementList_copy.push_back(copy_list_element); 
        } 
     result->p_statementList = statementList_copy; 
  // case: listType for statementList
  // case: listType (typeIsPointerToList == false) for statementList
     SgAsmStatementPtrList::const_iterator statementList_iterator = result->get_statementList().begin(); 
     for ( /* empty by design */; statementList_iterator != result->get_statementList().end(); ++statementList_iterator) 
        { 
          SgAsmStatement* list_element = *statementList_iterator; 
          if ( (list_element != NULL) && (list_element->get_parent() == NULL) && (isSgType(list_element) == NULL) ) 
             { 
               list_element->set_parent(result); 
             } 
        } 
  // Copy non-constructor parameter data member (no access function): result->p_dest
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dest
  // case: toBeCopied == COPY_DATA for dest
     SgAsmStatementPtrList dest_copy = p_dest; 
     result->p_dest = dest_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_entryVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryVa
  // case: toBeCopied == COPY_DATA for entryVa
     Rose::BinaryAnalysis::Address entryVa_copy = p_entryVa; 
     result->p_entryVa = entryVa_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_symbolTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbolTable
  // case: toBeCopied == COPY_DATA for symbolTable
     SgSymbolTable* symbolTable_copy = p_symbolTable; 
     result->p_symbolTable = symbolTable_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_cachedVertex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cachedVertex
  // case: toBeCopied == COPY_DATA for cachedVertex
     size_t cachedVertex_copy = p_cachedVertex; 
     result->p_cachedVertex = cachedVertex_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_stackDelta
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stackDelta
  // case: toBeCopied == COPY_DATA for stackDelta
     int64_t stackDelta_copy = p_stackDelta; 
     result->p_stackDelta = stackDelta_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_callingConvention
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for callingConvention
  // case: toBeCopied == COPY_DATA for callingConvention
     std::string callingConvention_copy = p_callingConvention; 
     result->p_callingConvention = callingConvention_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmFunction = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmFunction::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 291279 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmSynthesizedDataStructureDeclaration::copy ( SgCopyHelp& help) const
   {
     SgAsmSynthesizedDataStructureDeclaration* result = NULL;

  // printf ("Copy SgAsmSynthesizedDataStructureDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmSynthesizedDataStructureDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmSynthesizedDataStructureDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmSynthesizedDataStructureDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmSynthesizedDataStructureDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmSynthesizedDataStructureDeclaration(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmSynthesizedDataStructureDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmSynthesizedDataStructureDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 291473 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmSynthesizedFieldDeclaration::copy ( SgCopyHelp& help) const
   {
     SgAsmSynthesizedFieldDeclaration* result = NULL;

  // printf ("Copy SgAsmSynthesizedFieldDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmSynthesizedFieldDeclaration*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmSynthesizedFieldDeclaration*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmSynthesizedFieldDeclaration: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmSynthesizedFieldDeclaration*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmSynthesizedFieldDeclaration(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmSynthesizedFieldDeclaration = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmSynthesizedFieldDeclaration::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 291677 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmType::copy ( SgCopyHelp& help) const
   {
     SgAsmType* result = NULL;

  // printf ("Copy SgAsmType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 291860 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmScalarType::copy ( SgCopyHelp& help) const
   {
     SgAsmScalarType* result = NULL;

  // printf ("Copy SgAsmScalarType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmScalarType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmScalarType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmScalarType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmScalarType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmScalarType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_minorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorOrder
  // case: toBeCopied == COPY_DATA for minorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness minorOrder_copy = p_minorOrder; 
     result->p_minorOrder = minorOrder_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorOrder
  // case: toBeCopied == COPY_DATA for majorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness majorOrder_copy = p_majorOrder; 
     result->p_majorOrder = majorOrder_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorNBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorNBytes
  // case: toBeCopied == COPY_DATA for majorNBytes
     size_t majorNBytes_copy = p_majorNBytes; 
     result->p_majorNBytes = majorNBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nBits
  // case: toBeCopied == COPY_DATA for nBits
     size_t nBits_copy = p_nBits; 
     result->p_nBits = nBits_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmScalarType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmScalarType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 292064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmFloatType::copy ( SgCopyHelp& help) const
   {
     SgAsmFloatType* result = NULL;

  // printf ("Copy SgAsmFloatType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmFloatType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmFloatType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmFloatType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmFloatType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmFloatType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_minorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorOrder
  // case: toBeCopied == COPY_DATA for minorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness minorOrder_copy = p_minorOrder; 
     result->p_minorOrder = minorOrder_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorOrder
  // case: toBeCopied == COPY_DATA for majorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness majorOrder_copy = p_majorOrder; 
     result->p_majorOrder = majorOrder_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorNBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorNBytes
  // case: toBeCopied == COPY_DATA for majorNBytes
     size_t majorNBytes_copy = p_majorNBytes; 
     result->p_majorNBytes = majorNBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nBits
  // case: toBeCopied == COPY_DATA for nBits
     size_t nBits_copy = p_nBits; 
     result->p_nBits = nBits_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_significandOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for significandOffset
  // case: toBeCopied == COPY_DATA for significandOffset
     size_t significandOffset_copy = p_significandOffset; 
     result->p_significandOffset = significandOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_significandNBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for significandNBits
  // case: toBeCopied == COPY_DATA for significandNBits
     size_t significandNBits_copy = p_significandNBits; 
     result->p_significandNBits = significandNBits_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_signBitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for signBitOffset
  // case: toBeCopied == COPY_DATA for signBitOffset
     size_t signBitOffset_copy = p_signBitOffset; 
     result->p_signBitOffset = signBitOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_exponentOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exponentOffset
  // case: toBeCopied == COPY_DATA for exponentOffset
     size_t exponentOffset_copy = p_exponentOffset; 
     result->p_exponentOffset = exponentOffset_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_exponentNBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exponentNBits
  // case: toBeCopied == COPY_DATA for exponentNBits
     size_t exponentNBits_copy = p_exponentNBits; 
     result->p_exponentNBits = exponentNBits_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_exponentBias
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exponentBias
  // case: toBeCopied == COPY_DATA for exponentBias
     uint64_t exponentBias_copy = p_exponentBias; 
     result->p_exponentBias = exponentBias_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmFloatType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmFloatType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 292303 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmIntegerType::copy ( SgCopyHelp& help) const
   {
     SgAsmIntegerType* result = NULL;

  // printf ("Copy SgAsmIntegerType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmIntegerType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmIntegerType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmIntegerType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmIntegerType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmIntegerType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_minorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorOrder
  // case: toBeCopied == COPY_DATA for minorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness minorOrder_copy = p_minorOrder; 
     result->p_minorOrder = minorOrder_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorOrder
  // case: toBeCopied == COPY_DATA for majorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness majorOrder_copy = p_majorOrder; 
     result->p_majorOrder = majorOrder_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorNBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorNBytes
  // case: toBeCopied == COPY_DATA for majorNBytes
     size_t majorNBytes_copy = p_majorNBytes; 
     result->p_majorNBytes = majorNBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nBits
  // case: toBeCopied == COPY_DATA for nBits
     size_t nBits_copy = p_nBits; 
     result->p_nBits = nBits_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_isSigned
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSigned
  // case: toBeCopied == COPY_DATA for isSigned
     bool isSigned_copy = p_isSigned; 
     result->p_isSigned = isSigned_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmIntegerType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmIntegerType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 292512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPointerType::copy ( SgCopyHelp& help) const
   {
     SgAsmPointerType* result = NULL;

  // printf ("Copy SgAsmPointerType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmPointerType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmPointerType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmPointerType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmPointerType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmPointerType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_minorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorOrder
  // case: toBeCopied == COPY_DATA for minorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness minorOrder_copy = p_minorOrder; 
     result->p_minorOrder = minorOrder_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorOrder
  // case: toBeCopied == COPY_DATA for majorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness majorOrder_copy = p_majorOrder; 
     result->p_majorOrder = majorOrder_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_majorNBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorNBytes
  // case: toBeCopied == COPY_DATA for majorNBytes
     size_t majorNBytes_copy = p_majorNBytes; 
     result->p_majorNBytes = majorNBytes_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_nBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nBits
  // case: toBeCopied == COPY_DATA for nBits
     size_t nBits_copy = p_nBits; 
     result->p_nBits = nBits_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_subtype
     SgAsmType * subtype_copy; 
  // case: not a listType for (using conditionalToCopyVariable)subtype
          if (get_subtype() != NULL) 
             { 
               subtype_copy = static_cast<SgAsmType *>(help.copyAst(get_subtype())); 
             } 
            else 
             { 
               subtype_copy = NULL; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_subtype != NULL) delete result->p_subtype; 
     result->p_subtype = subtype_copy; 
  // case: not a listType for (using conditionalToSetParent)subtype
          if ( (subtype_copy != NULL) && (subtype_copy->get_parent() == NULL) && (isSgType(subtype_copy) == NULL) ) 
             { 
               subtype_copy->set_parent(result); 
             } 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmPointerType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmPointerType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 292735 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmVectorType::copy ( SgCopyHelp& help) const
   {
     SgAsmVectorType* result = NULL;

  // printf ("Copy SgAsmVectorType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmVectorType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmVectorType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmVectorType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmVectorType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmVectorType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class
  // Copy non-constructor parameter data member (no access function): result->p_nElmts
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nElmts
  // case: toBeCopied == COPY_DATA for nElmts
     size_t nElmts_copy = p_nElmts; 
     result->p_nElmts = nElmts_copy; 
  // Copy non-constructor parameter data member (no access function): result->p_elmtType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for elmtType
  // case: toBeCopied == COPY_DATA for elmtType
     SgAsmType* elmtType_copy = p_elmtType; 
     result->p_elmtType = elmtType_copy; 

/* #line 43 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmVectorType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmVectorType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }


/* #line 292929 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmVoidType::copy ( SgCopyHelp& help) const
   {
     SgAsmVoidType* result = NULL;

  // printf ("Copy SgAsmVoidType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
  // The default value of the depth is 0, so after this call the depth is 1!
     help.incrementDepth();

#if 0
  // DQ (10/22/2005): This is a useful test for the copy mechanism (in preprocessor.C)
  // but it is not generally true that things can only be copied once!
  // Check and see if this has been marked as copied! (detects recursive copy bug)
  // AstAttribute* existingAttribute = const_cast<SgAsmVoidType*>(this)->attribute["copied"];
     bool previouslyCopied = const_cast<SgAsmVoidType*>(this)->attribute.exists("copied");
     if (previouslyCopied == true)
        {
          this->get_file_info()->display("Called from copy SgAsmVoidType: debug");
        }
     ROSE_ASSERT(previouslyCopied == false);

  // If not previously copied then build an attribute (on the heap) to mark it as copied
     AstAttribute* newAttribute = new AstAttribute();
     ROSE_ASSERT(newAttribute != NULL);

  // Add it to the AST (so it can be found later in any other attempt to copy this AST node)
     const_cast<SgAsmVoidType*>(this)->attribute.add("copied",newAttribute);
#endif

  // Copy data members from base classes
 

  // Build an empty copy of this object (will be filled in, but 
  // the parent can't be set and must be set by the caller)
     result = new SgAsmVoidType(  );
     ROSE_ASSERT(result != NULL);

  // Copy data members of "this" class


  // DQ (10/9/2007): comment out to test template problem ... This breaks test2004_76.C and test2004_142.C
  // DQ (10/5/2007): Add custom function to be called to fixup IR node specific details.
  // fixupCopy(result,help);

  // DQ (10/16/2007): The semantics of copy should include setting the parents of all 
  // the Sg_File_Info objects that are built for the new IR nodes.
     SgLocatedNode* locatedNode = isSgLocatedNode(result);
     if (locatedNode != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = locatedNode->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(locatedNode);
          ROSE_ASSERT(start->get_parent() != NULL);

          Sg_File_Info* end = locatedNode->get_endOfConstruct();

       // DQ (10/27/2007): Some input AST's have not been constructed properly so that the copy of the endOfConstruct was not made.
       // Warn about this but allow it for now so that the outliner can be allowed to work (under older semantics).
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
             }
            else
             {
#if 0
            // Debugging information
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
#endif
               end->set_parent(locatedNode);
               ROSE_ASSERT(end->get_parent() != NULL);
             }

          SgExpression* expression = isSgExpression(result);
          if (isSgExpression(this) != NULL)
             {
               Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
               if (operatorPosition != NULL)
                  {
#if 0
                 // Debugging information
                    if (operatorPosition->get_parent() == NULL)
                       {
                         printf ("Detected a result->get_operatorPosition() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                       }
#endif
                    operatorPosition->set_parent(expression);
                    ROSE_ASSERT(operatorPosition->get_parent() != NULL);
                  }
             }
        }

  // DQ (10/16/2007): Also set the Sg_File_Info parents for the SgInitializedName
     SgInitializedName* initializedName = isSgInitializedName(result);
     if (initializedName != NULL)
        {
       // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
          Sg_File_Info* start = initializedName->get_startOfConstruct();
          ROSE_ASSERT(start != NULL);
#if 0
       // Debugging information
          if (start->get_parent() == NULL)
             {
               printf ("Detected a result->get_startOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
             }
#endif
          start->set_parent(initializedName);
          ROSE_ASSERT(start->get_parent() != NULL);

#if 0
       // DQ (10/16/2007): Note that a SgInitializedName does not have a get_endOfConstruct() member function yet with g++ this does compile!
          Sg_File_Info* end = initializedName->get_endOfConstruct();

       // DQ (10/16/2007): For the moment allow the initializedName->get_endOfConstruct() to be NULL and issue a warning.
       // ROSE_ASSERT(end != NULL);
          if (end == NULL)
             {
               printf ("Detected a result->get_endOfConstruct() == NULL for result = %p = %s \n",result,result->class_name().c_str());
             }
            else
             {
               if (end->get_parent() == NULL)
                  {
                    printf ("Detected a result->get_endOfConstruct() with NULL parent for result = %p = %s \n",result,result->class_name().c_str());
                  }
               end->set_parent(initializedName);
               ROSE_ASSERT(end->get_parent() != NULL);
             }
#endif
        }

  // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
     help.insertCopiedNodePair(this,result);

  // printf ("End of copy SgAsmVoidType = %p = %s \n",this,SageInterface::get_name(this).c_str());

  // Record the depth of the copy so that we build an clear internal state (new feature) 
  // used to associated old and new symbols in the original AST and the copy of the AST.
     help.decrementDepth();

  // Test if this is the root of the copy!
     if (help.get_depth() == 0)
        {
       // This is the original calling node.

       // Call the fixup mechanism to correct details of the AST copy mechanism.
       // printf ("Calling SgAsmVoidType::fixupCopy() (from root of AST being copied) \n");
#if ALT_FIXUP_COPY
       // DQ (11/7/2007): These need to be called separately (see documentation)
          fixupCopy_scopes (result,help);
          fixupCopy_symbols (result,help);
          fixupCopy_references (result,help);
#else
          fixupCopy(result,help);
#endif
       // Allow this to be called recursively, so accumulate the state.
       // Also, clear the state in the SgCopyHelp object.
       // help.clearState();
        }

     return result;
   }



